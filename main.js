/* Cute Obsidian Workbench */
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/main.ts
var main_exports = {};
__export(main_exports, {
  default: () => CuteObsidianWorkbenchPlugin
});
module.exports = __toCommonJS(main_exports);
var import_obsidian86 = require("obsidian");

// src/core/DashboardStore.ts
var DASHBOARD_PAGES = [
  { id: "overview", label: "\u603B\u89C8", icon: "home", description: "\u4ECA\u65E5\u6982\u89C8\u4E0E\u5FEB\u6377\u5165\u53E3" },
  { id: "research", label: "\u79D1\u7814", icon: "flask-conical", description: "\u8BBA\u6587\u3001\u5B9E\u9A8C\u548C\u79D1\u7814\u8BB0\u5F55" },
  { id: "reading", label: "\u9605\u8BFB", icon: "book-open", description: "\u8BFB\u4E66\u961F\u5217\u4E0E\u6458\u5F55\u8FDB\u5EA6" },
  { id: "fitness", label: "\u5065\u8EAB", icon: "dumbbell", description: "\u8BAD\u7EC3\u3001\u6062\u590D\u548C\u4E60\u60EF\u6253\u5361" },
  { id: "finance", label: "\u7406\u8D22", icon: "coins", description: "\u9884\u7B97\u3001\u8D44\u4EA7\u548C\u6295\u8D44\u89C2\u5BDF" },
  { id: "tasks", label: "\u4EFB\u52A1\u7BA1\u7406", icon: "list-todo", description: "\u628A\u60F3\u505A\u7684\u4E8B\uFF0C\u53D8\u6210\u6B63\u5728\u53D1\u751F\u7684\u4E8B" },
  { id: "goals", label: "\u76EE\u6807\u7BA1\u7406", icon: "target", description: "\u957F\u671F\u76EE\u6807\u4E0E\u9636\u6BB5\u8BA1\u5212" },
  { id: "modules", label: "\u6A21\u5757\u7BA1\u7406", icon: "layout-grid", description: "\u6A21\u5757\u542F\u7528\u3001\u5E03\u5C40\u548C\u6570\u636E\u7ED1\u5B9A" }
];
var AVAILABLE_MODULES = [
  { type: "weekly-completion", title: "\u672C\u5468\u5B8C\u6210\u7387", description: "\u7EDF\u8BA1\u4ECA\u65E5\u7126\u70B9\u548C\u4E60\u60EF\u7684\u672C\u5468\u5B8C\u6210\u7387\u3002", page: "overview", icon: "badge-percent", defaultWidth: "sm" },
  { type: "pending-tasks", title: "\u5F85\u529E\u4EFB\u52A1", description: "\u663E\u793A\u672A\u5B8C\u6210\u7684\u4ECA\u65E5\u7126\u70B9\u4EFB\u52A1\u3002", page: "overview", icon: "clipboard-list", defaultWidth: "sm" },
  { type: "today-focus-stat", title: "\u4ECA\u65E5\u4E13\u6CE8", description: "\u8BB0\u5F55\u4ECA\u65E5\u4E13\u6CE8\u65F6\u957F\u3002", page: "overview", icon: "headphones", defaultWidth: "sm" },
  { type: "checkin-streak", title: "\u8FDE\u7EED\u6253\u5361", description: "\u663E\u793A\u8FDE\u7EED\u5B8C\u6210\u4E60\u60EF\u7684\u5929\u6570\u3002", page: "overview", icon: "flame", defaultWidth: "sm" },
  { type: "today-focus", title: "\u4ECA\u65E5\u7126\u70B9", description: "\u79D1\u7814\u3001\u9605\u8BFB\u3001\u5065\u8EAB\u3001\u7406\u8D22\u548C\u4E2A\u4EBA\u4EFB\u52A1\u3002", page: "overview", icon: "target", defaultWidth: "md", defaultHeight: "md" },
  { type: "habit-overview", title: "\u6253\u5361\u603B\u89C8", description: "\u67E5\u770B\u5E76\u5207\u6362\u672C\u5468\u4E60\u60EF\u6253\u5361\u72B6\u6001\u3002", page: "overview", icon: "calendar-check", defaultWidth: "md", defaultHeight: "md" },
  { type: "monthly-progress", title: "\u672C\u6708\u8FDB\u5EA6", description: "\u6309\u9875\u9762\u8FFD\u8E2A\u672C\u6708\u5B8C\u6210\u7387\u3002", page: "overview", icon: "bar-chart-3", defaultWidth: "md" },
  { type: "monthly-calendar", title: "\u6708\u5EA6\u65E5\u5386", description: "\u72EC\u7ACB\u6708\u5386\uFF0C\u9884\u7559\u7B14\u8BB0\u3001\u4EFB\u52A1\u548C\u4E8B\u4EF6\u6807\u8BB0\u3002", page: "overview", icon: "calendar-days", defaultWidth: "md", defaultHeight: "md" },
  { type: "recent-notes", title: "\u6700\u8FD1\u7B14\u8BB0", description: "\u4E3A\u540E\u7EED NoteService \u9884\u7559\u7684\u6700\u8FD1\u6587\u4EF6\u5217\u8868\u3002", page: "overview", icon: "file-text", defaultWidth: "md" },
  { type: "quick-actions", title: "\u5FEB\u6377\u64CD\u4F5C", description: "\u65B0\u5EFA\u7B14\u8BB0\u3001\u6DFB\u52A0\u4EFB\u52A1\u3001\u6253\u5F00\u65E5\u5386\u7B49\u52A8\u4F5C\u5165\u53E3\u3002", page: "overview", icon: "zap", defaultWidth: "md" },
  { type: "contribution-heatmap", title: "\u5E74\u5EA6\u8D21\u732E\u56FE", description: "\u6309\u6708\u4EFD\u5C55\u793A Markdown \u521B\u5EFA\u6570\u91CF\u70ED\u529B\u56FE\u3002", page: "overview", icon: "activity", defaultWidth: "full", defaultHeight: "sm" },
  { type: "memo", title: "Memo", description: "\u968F\u624B\u8BB0\u5F55\u7075\u611F\u548C\u60F3\u6CD5\u3002", page: "overview", icon: "sticky-note", defaultWidth: "md" },
  { type: "todo", title: "Todo", description: "\u8F7B\u91CF\u6E05\u5355\u6A21\u5757\u3002", page: "overview", icon: "list-checks", defaultWidth: "md" },
  { type: "projects", title: "Projects", description: "\u9879\u76EE\u5361\u7247\u548C\u5173\u8054\u7B14\u8BB0\u3002", page: "overview", icon: "folder-kanban", defaultWidth: "md" },
  { type: "notes", title: "Notes", description: "\u7B14\u8BB0\u5165\u53E3\u96C6\u5408\u3002", page: "overview", icon: "notebook-tabs", defaultWidth: "md" },
  { type: "research-projects", title: "\u7814\u7A76\u9879\u76EE\u603B\u89C8", description: "\u79D1\u7814\u9879\u76EE\u4E0E\u9636\u6BB5\u72B6\u6001\u3002", page: "research", icon: "layers", defaultWidth: "md" },
  { type: "reading-queue", title: "\u8BBA\u6587\u9605\u8BFB\u961F\u5217", description: "\u8BBA\u6587\u9605\u8BFB\u72B6\u6001\u548C\u4F18\u5148\u7EA7\u3002", page: "research", icon: "book-marked", defaultWidth: "md" },
  { type: "paper-field-manager", title: "\u8BBA\u6587\u5B57\u6BB5\u7BA1\u7406", description: "\u7BA1\u7406\u8BBA\u6587\u9605\u8BFB\u72B6\u6001\u3001\u4F1A\u8BAE\u671F\u520A\u548C\u6807\u7B7E\u3002", page: "research", icon: "sliders-horizontal", defaultWidth: "md" },
  { type: "research-checkin", title: "\u672C\u5468\u79D1\u7814\u6253\u5361", description: "\u8BBA\u6587\u3001\u5B9E\u9A8C\u3001\u5199\u4F5C\u548C\u7EC4\u4F1A\u51C6\u5907\u3002", page: "research", icon: "calendar-check", defaultWidth: "md" },
  { type: "experiment-plan", title: "\u5B9E\u9A8C\u8BA1\u5212", description: "\u8BA1\u5212\u4E2D\u7684\u5B9E\u9A8C\u4EFB\u52A1\u3002", page: "research", icon: "clipboard-check", defaultWidth: "md" },
  { type: "experiment-records", title: "\u5B9E\u9A8C\u8BB0\u5F55", description: "\u6700\u8FD1\u5B9E\u9A8C\u8BB0\u5F55\u4E0E\u7B14\u8BB0\u5165\u53E3\u3002", page: "research", icon: "file-clock", defaultWidth: "md" },
  { type: "data-analysis-tasks", title: "\u6570\u636E\u5206\u6790\u4EFB\u52A1", description: "\u6570\u636E\u5904\u7406\u3001\u5206\u6790\u548C\u56FE\u8868\u4EFB\u52A1\u3002", page: "research", icon: "bar-chart-3", defaultWidth: "md" },
  { type: "literature-notes", title: "\u6587\u732E\u7B14\u8BB0", description: "\u5DF2\u7ED1\u5B9A\u7684\u8BBA\u6587\u9605\u8BFB\u7B14\u8BB0\u3002", page: "research", icon: "notebook-text", defaultWidth: "md" },
  { type: "research-timeline", title: "\u4F1A\u8BAE / DDL \u65F6\u95F4\u7EBF", description: "\u91CD\u8981\u4F1A\u8BAE\u3001\u622A\u6B62\u65E5\u671F\u548C\u6C47\u62A5\u5B89\u6392\u3002", page: "research", icon: "calendar-clock", defaultWidth: "md" },
  { type: "research-memo", title: "\u79D1\u7814\u7075\u611F Memo", description: "\u8BB0\u5F55\u7A81\u7136\u5192\u51FA\u6765\u7684\u79D1\u7814\u60F3\u6CD5\u3002", page: "research", icon: "lightbulb", defaultWidth: "md" },
  { type: "current-reading", title: "\u5F53\u524D\u9605\u8BFB", description: "\u5F53\u524D\u6B63\u5728\u8BFB\u7684\u4E66\u548C\u8FDB\u5EA6\u3002", page: "reading", icon: "book-open-check", defaultWidth: "md" },
  { type: "bookshelf", title: "\u4E66\u67B6 Bookshelf", description: "\u4E66\u7C4D\u5361\u7247\u548C\u9605\u8BFB\u72B6\u6001\u3002", page: "reading", icon: "library", defaultWidth: "md" },
  { type: "reading-plan", title: "\u9605\u8BFB\u8BA1\u5212", description: "\u8FD1\u671F\u9605\u8BFB\u5B89\u6392\u3002", page: "reading", icon: "calendar-range", defaultWidth: "md" },
  { type: "reading-checkin", title: "\u672C\u5468\u9605\u8BFB\u6253\u5361", description: "\u672C\u5468\u9605\u8BFB\u4E60\u60EF\u6253\u5361\u3002", page: "reading", icon: "calendar-check", defaultWidth: "md" },
  { type: "reading-notes", title: "\u9605\u8BFB\u7B14\u8BB0", description: "\u9605\u8BFB\u7B14\u8BB0\u5165\u53E3\u3002", page: "reading", icon: "notebook-tabs", defaultWidth: "md" },
  { type: "reading-quotes", title: "\u91D1\u53E5\u6458\u5F55", description: "\u503C\u5F97\u53CD\u590D\u770B\u7684\u53E5\u5B50\u3002", page: "reading", icon: "quote", defaultWidth: "md" },
  { type: "reading-tag-manager", title: "\u6807\u7B7E\u7BA1\u7406", description: "\u7BA1\u7406\u9605\u8BFB\u4E66\u7C4D\u6807\u7B7E\u3002", page: "reading", icon: "tags", defaultWidth: "md" },
  { type: "finished-books", title: "\u5DF2\u8BFB\u6E05\u5355", description: "\u5DF2\u7ECF\u8BFB\u5B8C\u7684\u4E66\u3002", page: "reading", icon: "badge-check", defaultWidth: "md" },
  { type: "wishlist-books", title: "\u60F3\u8BFB\u6E05\u5355", description: "\u51C6\u5907\u5F00\u59CB\u7684\u4E66\u3002", page: "reading", icon: "bookmark-plus", defaultWidth: "md" },
  { type: "reading-stats", title: "\u9605\u8BFB\u8FDB\u5EA6\u7EDF\u8BA1", description: "\u9875\u6570\u3001\u5B8C\u6210\u7387\u548C\u9605\u8BFB\u6570\u91CF\u3002", page: "reading", icon: "pie-chart", defaultWidth: "md" },
  { type: "reading-heatmap", title: "\u6708\u5EA6\u9605\u8BFB\u70ED\u529B\u56FE", description: "\u6309\u65E5\u5C55\u793A\u672C\u6708\u9605\u8BFB\u6D3B\u52A8\u3002", page: "reading", icon: "activity", defaultWidth: "md" },
  { type: "ai-reading-review", title: "AI \u9605\u8BFB\u590D\u76D8", description: "AI \u590D\u76D8\u80FD\u529B\u5360\u4F4D\u5361\u3002", page: "reading", icon: "sparkles", defaultWidth: "md" },
  { type: "today-workout", title: "\u4ECA\u65E5\u8BAD\u7EC3", description: "\u4ECA\u5929\u7684\u8BAD\u7EC3\u5B89\u6392\u548C\u5B8C\u6210\u72B6\u6001\u3002", page: "fitness", icon: "dumbbell", defaultWidth: "md" },
  { type: "workout-plan", title: "\u8BAD\u7EC3\u8BA1\u5212", description: "\u8FD1\u671F\u8BAD\u7EC3\u8BA1\u5212\u3002", page: "fitness", icon: "clipboard-list", defaultWidth: "md" },
  { type: "fitness-checkin", title: "\u672C\u5468\u5065\u8EAB\u6253\u5361", description: "\u8BAD\u7EC3\u3001\u996E\u6C34\u3001\u7761\u7720\u548C\u6062\u590D\u6253\u5361\u3002", page: "fitness", icon: "calendar-check", defaultWidth: "md" },
  { type: "body-measurements", title: "\u4F53\u91CD\u4E0E\u56F4\u5EA6\u8BB0\u5F55", description: "\u4F53\u91CD\u3001BMI \u548C\u8EAB\u4F53\u56F4\u5EA6\u53D8\u5316\u3002", page: "fitness", icon: "ruler", defaultWidth: "md" },
  { type: "cardio-strength-plan", title: "\u6709\u6C27 / \u529B\u91CF\u5B89\u6392", description: "\u5E73\u8861\u6709\u6C27\u548C\u529B\u91CF\u8BAD\u7EC3\u3002", page: "fitness", icon: "heart-pulse", defaultWidth: "md" },
  { type: "water-sleep-habits", title: "\u4E60\u60EF", description: "\u996E\u6C34\u3001\u7761\u7720\u548C\u81EA\u5B9A\u4E49\u5065\u5EB7\u4E60\u60EF\u3002", page: "fitness", icon: "moon", defaultWidth: "md" },
  { type: "fitness-stats", title: "\u8D8B\u52BF\u56FE", description: "\u89C2\u5BDF\u8EAB\u4F53\u3001\u8FD0\u52A8\u548C\u6267\u884C\u8D8B\u52BF\u3002", page: "fitness", icon: "line-chart", defaultWidth: "lg" },
  { type: "workout-log", title: "\u8FD0\u52A8\u65E5\u5FD7", description: "\u6700\u8FD1\u5B8C\u6210\u7684\u8BAD\u7EC3\u8BB0\u5F55\u3002", page: "fitness", icon: "notebook-text", defaultWidth: "md" },
  { type: "fitness-goals", title: "\u5065\u8EAB\u76EE\u6807\u8FDB\u5EA6", description: "\u8FFD\u8E2A\u5065\u8EAB\u76EE\u6807\u5B8C\u6210\u5EA6\u3002", page: "fitness", icon: "target", defaultWidth: "md" },
  { type: "health-reminders", title: "\u5065\u5EB7\u63D0\u9192", description: "\u6062\u590D\u3001\u70ED\u8EAB\u548C\u4F11\u606F\u63D0\u9192\u3002", page: "fitness", icon: "bell-ring", defaultWidth: "md" },
  { type: "fitness-heatmap", title: "\u6708\u5EA6\u8FD0\u52A8\u70ED\u529B\u56FE", description: "\u6309\u65E5\u5C55\u793A\u8FD0\u52A8\u6D3B\u8DC3\u5EA6\u3002", page: "fitness", icon: "activity", defaultWidth: "md" },
  { type: "monthly-budget", title: "\u672C\u6708\u9884\u7B97", description: "\u9884\u7B97\u3001\u652F\u51FA\u548C\u5269\u4F59\u989D\u5EA6\u3002", page: "finance", icon: "wallet-cards", defaultWidth: "md" },
  { type: "finance-ledger", title: "\u8BB0\u8D26", description: "\u5FEB\u901F\u8BB0\u5F55\u6BCF\u65E5\u6536\u5165\u4E0E\u652F\u51FA\u3002", page: "finance", icon: "circle-plus", defaultWidth: "md" },
  { type: "expense-categories", title: "\u652F\u51FA\u5206\u7C7B", description: "\u672C\u6708\u5206\u7C7B\u652F\u51FA\u5360\u6BD4\u3002", page: "finance", icon: "chart-pie", defaultWidth: "md" },
  { type: "account-overview", title: "\u8D26\u6237\u603B\u89C8", description: "\u4E2A\u4EBA\u8D26\u6237\u4F59\u989D\u6982\u89C8\u3002", page: "finance", icon: "landmark", defaultWidth: "md" },
  { type: "saving-goals", title: "\u50A8\u84C4\u76EE\u6807", description: "\u50A8\u84C4\u76EE\u6807\u8FDB\u5EA6\u3002", page: "finance", icon: "piggy-bank", defaultWidth: "md" },
  { type: "bill-reminders", title: "\u8D26\u5355\u63D0\u9192", description: "\u5373\u5C06\u5230\u671F\u7684\u8D26\u5355\u3002", page: "finance", icon: "receipt", defaultWidth: "md" },
  { type: "finance-checkin", title: "\u672C\u5468\u7406\u8D22\u6253\u5361", description: "\u8BB0\u8D26\u3001\u590D\u76D8\u548C\u9884\u7B97\u68C0\u67E5\u3002", page: "finance", icon: "calendar-check", defaultWidth: "md" },
  { type: "income-expense-trend", title: "\u6536\u652F\u8D8B\u52BF", description: "\u672C\u6708\u6536\u5165\u548C\u652F\u51FA\u8D70\u52BF\u3002", page: "finance", icon: "line-chart", defaultWidth: "md" },
  { type: "finance-todos", title: "\u672C\u6708\u8BB0\u8D26\u5F85\u529E", description: "\u672C\u6708\u8D22\u52A1\u5F85\u529E\u4E8B\u9879\u3002", page: "finance", icon: "list-checks", defaultWidth: "md" },
  { type: "investment-watch", title: "\u6295\u8D44\u89C2\u5BDF", description: "\u4E2A\u4EBA\u6295\u8D44\u89C2\u5BDF\uFF0C\u4E0D\u6267\u884C\u4EA4\u6613\u3002", page: "finance", icon: "candlestick-chart", defaultWidth: "md" },
  { type: "expense-heatmap", title: "\u6708\u5EA6\u652F\u51FA\u70ED\u529B\u56FE", description: "\u6309\u65E5\u5C55\u793A\u652F\u51FA\u5BC6\u5EA6\u3002", page: "finance", icon: "activity", defaultWidth: "md" },
  { type: "yearly-goals", title: "\u5E74\u5EA6\u76EE\u6807", description: "\u5168\u5E74\u76EE\u6807\u548C\u5B8C\u6210\u8FDB\u5EA6\u3002", page: "goals", icon: "flag", defaultWidth: "md" },
  { type: "quarterly-okr", title: "\u5B63\u5EA6\u76EE\u6807", description: "\u6309\u5E74\u4EFD\u548C\u5B63\u5EA6\u63A8\u8FDB\u7684\u76EE\u6807\u3002", page: "goals", icon: "target", defaultWidth: "md" },
  { type: "monthly-key-results", title: "\u6708\u5EA6\u76EE\u6807", description: "\u672C\u6708\u9700\u8981\u63A8\u8FDB\u7684\u76EE\u6807\u3002", page: "goals", icon: "list-checks", defaultWidth: "md" },
  { type: "goal-breakdown", title: "\u76EE\u6807\u62C6\u89E3", description: "\u628A\u76EE\u6807\u62C6\u5230\u884C\u52A8\u5C42\u3002", page: "goals", icon: "git-branch", defaultWidth: "md" },
  { type: "milestone-timeline", title: "\u91CC\u7A0B\u7891\u65F6\u95F4\u7EBF", description: "\u76EE\u6807\u91CC\u7A0B\u7891\u548C\u65E5\u671F\u3002", page: "goals", icon: "milestone", defaultWidth: "md" },
  { type: "priority-matrix", title: "\u4F18\u5148\u7EA7\u77E9\u9635", description: "\u91CD\u8981\u7D27\u6025\u56DB\u8C61\u9650\u3002", page: "goals", icon: "layout-dashboard", defaultWidth: "md" },
  { type: "goals-checkin", title: "\u672C\u5468\u76EE\u6807\u6253\u5361", description: "\u76EE\u6807\u63A8\u8FDB\u4E60\u60EF\u6253\u5361\u3002", page: "goals", icon: "calendar-check", defaultWidth: "md" },
  { type: "review-checklist", title: "\u590D\u76D8\u6E05\u5355", description: "\u5468\u590D\u76D8\u548C\u6708\u590D\u76D8\u4E8B\u9879\u3002", page: "goals", icon: "clipboard-check", defaultWidth: "md" },
  { type: "risks-blockers", title: "\u98CE\u9669\u4E0E\u963B\u788D", description: "\u8BC6\u522B\u98CE\u9669\u5E76\u8BB0\u5F55\u89E3\u51B3\u65B9\u6848\u3002", page: "goals", icon: "triangle-alert", defaultWidth: "md" },
  { type: "long-term-progress", title: "\u957F\u671F\u8FDB\u5C55", description: "\u76EE\u6807\u957F\u671F\u8D8B\u52BF\u548C\u5B8C\u6210\u7387\u3002", page: "goals", icon: "trending-up", defaultWidth: "md" },
  { type: "enabled-modules-overview", title: "\u5DF2\u542F\u7528\u6A21\u5757\u6982\u89C8", description: "\u7EDF\u8BA1\u5F53\u524D\u9875\u9762\u548C\u6574\u4E2A\u5DE5\u4F5C\u53F0\u542F\u7528\u6A21\u5757\u3002", page: "modules", icon: "panel-top", defaultWidth: "md" },
  { type: "home-layout-manager", title: "\u9875\u9762\u5E03\u5C40\u7BA1\u7406", description: "\u4E3A\u6BCF\u4E2A\u9875\u9762\u5355\u72EC\u8BBE\u7F6E\u9ED8\u8BA4\u3001\u7D27\u51D1\u6216\u6781\u7B80\u5E03\u5C40\u3002", page: "modules", icon: "layout-template", defaultWidth: "md" },
  { type: "section-manager", title: "\u529F\u80FD\u5206\u533A\u7BA1\u7406", description: "\u6309\u9875\u9762\u7BA1\u7406\u542F\u7528\u3001\u9690\u85CF\u3001\u5220\u9664\u3001\u6392\u5E8F\u3001\u989C\u8272\u3001\u5BBD\u5EA6\u548C\u81EA\u5B9A\u4E49\u5206\u533A\u3002", page: "modules", icon: "rows-3", defaultWidth: "full", defaultHeight: "lg" },
  { type: "banner-background-settings", title: "Banner \u80CC\u666F\u8BBE\u7F6E", description: "\u8BBE\u7F6E\u63A8\u8350\u58C1\u7EB8\u3001\u672C\u5730\u56FE\u7247\u3001\u7EAF\u8272\u80CC\u666F\u548C\u906E\u7F69\u3002", page: "modules", icon: "image", defaultWidth: "md" },
  { type: "calendar-widget-settings", title: "\u65E5\u5386\u7EC4\u4EF6\u8BBE\u7F6E", description: "\u63A7\u5236\u65E5\u671F\u6807\u8BB0\u3001\u5468\u8D77\u59CB\u65E5\u548C\u9AD8\u4EAE\u989C\u8272\u3002", page: "modules", icon: "calendar-days", defaultWidth: "md" },
  { type: "apex-habit-settings", title: "Apex \u6253\u5361\u6A21\u5757\u8BBE\u7F6E", description: "\u7BA1\u7406\u9996\u9875\u6253\u5361\u5C55\u793A\u548C\u81EA\u5B9A\u4E49\u6253\u5361\u9879\u76EE\u3002", page: "modules", icon: "calendar-check", defaultWidth: "md" },
  { type: "quick-action-settings", title: "\u5FEB\u6377\u64CD\u4F5C\u914D\u7F6E", description: "\u63A7\u5236\u5185\u7F6E\u5FEB\u6377\u64CD\u4F5C\u548C\u81EA\u5B9A\u4E49\u5165\u53E3\u3002", page: "modules", icon: "zap", defaultWidth: "md" },
  { type: "theme-color-settings", title: "\u4E3B\u9898\u4E0E\u914D\u8272", description: "\u8C03\u6574 CSS variables\u3001\u5706\u89D2\u548C\u5B57\u4F53\u5927\u5C0F\u3002", page: "modules", icon: "palette", defaultWidth: "md" },
  { type: "data-source-status", title: "\u6570\u636E\u6E90", description: "\u67E5\u770B\u7B14\u8BB0\u3001\u4EFB\u52A1\u3001\u65E5\u5386\u3001\u4E60\u60EF\u3001\u9605\u8BFB\u548C\u79D1\u7814\u662F\u5426\u542F\u7528\u3002", page: "modules", icon: "database", defaultWidth: "md" }
];
function createSection(page, type, title, order, width = "md", height = "md") {
  return {
    id: `${page}-${type}`,
    page,
    type,
    title,
    order,
    enabled: true,
    width,
    height
  };
}
function nowIso() {
  return (/* @__PURE__ */ new Date()).toISOString();
}
function todayKey() {
  return formatDateKey(/* @__PURE__ */ new Date());
}
function currentMonthKey() {
  return todayKey().slice(0, 7);
}
function createDefaultCheckInDefinitions() {
  const createdAt = (/* @__PURE__ */ new Date("2026-09-14T08:00:00.000Z")).getTime();
  const create = (moduleId, id, title, order, icon, color) => ({
    id,
    moduleId,
    title,
    icon,
    color,
    enabled: true,
    order,
    createdAt,
    updatedAt: createdAt
  });
  return [
    create("overview", "reading", "\u9605\u8BFB", 10, "book-open", "#ff8fbc"),
    create("overview", "fitness", "\u5065\u8EAB", 20, "dumbbell", "#7bd88f"),
    create("overview", "finance", "\u7406\u8D22", 30, "coins", "#ffd166"),
    create("overview", "writing", "\u5199\u4F5C", 40, "pencil", "#9ecbff"),
    create("overview", "study", "\u5B66\u4E60", 50, "graduation-cap", "#c9a7ff"),
    create("research", "research-reading-paper", "\u9605\u8BFB\u8BBA\u6587", 10, "book-marked", "#9ecbff"),
    create("research", "research-experiment", "\u5B9E\u9A8C", 20, "flask-conical", "#c9a7ff"),
    create("research", "research-writing", "\u5199\u4F5C", 30, "pencil", "#ff8fbc"),
    create("research", "research-data", "\u6574\u7406\u6570\u636E", 40, "database", "#7bd88f"),
    create("research", "research-meeting", "\u7EC4\u4F1A\u51C6\u5907", 50, "users", "#ffd166"),
    create("reading", "reading-pages", "\u8BFB\u4E66", 10, "book-open", "#ff8fbc"),
    create("reading", "reading-note", "\u5199\u7B14\u8BB0", 20, "notebook-tabs", "#9ecbff"),
    create("reading", "reading-quote", "\u6458\u5F55", 30, "quote", "#ffd166"),
    create("reading", "reading-review", "\u590D\u76D8", 40, "rotate-ccw", "#c9a7ff"),
    create("reading", "reading-plan", "\u8BA1\u5212", 50, "calendar-range", "#7bd88f"),
    create("fitness", "fitness-workout", "\u8BAD\u7EC3", 10, "dumbbell", "#7bd88f"),
    create("fitness", "fitness-water", "\u996E\u6C34", 20, "droplets", "#9ecbff"),
    create("fitness", "fitness-sleep", "\u7761\u7720", 30, "moon", "#c9a7ff"),
    create("fitness", "fitness-stretch", "\u62C9\u4F38", 40, "activity", "#ffd166"),
    create("fitness", "fitness-recovery", "\u6062\u590D", 50, "heart-pulse", "#ff8fbc"),
    create("finance", "finance-record", "\u8BB0\u8D26", 10, "receipt", "#ffd166"),
    create("finance", "finance-budget", "\u9884\u7B97", 20, "wallet-cards", "#ff8fbc"),
    create("finance", "finance-review", "\u590D\u76D8", 30, "rotate-ccw", "#c9a7ff"),
    create("finance", "finance-save", "\u50A8\u84C4", 40, "piggy-bank", "#7bd88f"),
    create("finance", "finance-invest", "\u89C2\u5BDF", 50, "candlestick-chart", "#9ecbff"),
    create("goals", "goals-plan", "\u8BA1\u5212", 10, "calendar-check", "#9ecbff"),
    create("goals", "goals-action", "\u884C\u52A8", 20, "target", "#ff8fbc"),
    create("goals", "goals-review", "\u590D\u76D8", 30, "rotate-ccw", "#c9a7ff"),
    create("goals", "goals-focus", "\u805A\u7126", 40, "focus", "#ffd166"),
    create("goals", "goals-adjust", "\u8C03\u6574", 50, "sliders-horizontal", "#7bd88f")
  ];
}
var DEFAULT_MODULE_LAYOUTS = {
  overview: { mode: "default", columns: 12, sections: {} },
  research: { mode: "default", columns: 12, sections: {} },
  reading: { mode: "default", columns: 12, sections: {} },
  fitness: { mode: "default", columns: 12, sections: {} },
  finance: { mode: "default", columns: 12, sections: {} },
  tasks: { mode: "default", columns: 12, sections: {} },
  goals: { mode: "default", columns: 12, sections: {} },
  modules: { mode: "default", columns: 12, sections: {} }
};
var DEFAULT_DATA = {
  dataVersion: "0.7.0",
  currentPage: "overview",
  sections: [
    {
      id: "overview-weekly-completion",
      page: "overview",
      type: "weekly-completion",
      title: "\u672C\u5468\u5B8C\u6210\u7387",
      order: 10,
      enabled: true,
      width: "sm",
      height: "sm"
    },
    {
      id: "overview-pending-tasks",
      page: "overview",
      type: "pending-tasks",
      title: "\u5F85\u529E\u4EFB\u52A1",
      order: 20,
      enabled: true,
      width: "sm",
      height: "sm"
    },
    {
      id: "overview-today-focus-stat",
      page: "overview",
      type: "today-focus-stat",
      title: "\u4ECA\u65E5\u4E13\u6CE8",
      order: 30,
      enabled: true,
      width: "sm",
      height: "sm"
    },
    {
      id: "overview-checkin-streak",
      page: "overview",
      type: "checkin-streak",
      title: "\u8FDE\u7EED\u6253\u5361",
      order: 40,
      enabled: true,
      width: "sm",
      height: "sm"
    },
    {
      id: "overview-today-focus",
      page: "overview",
      type: "today-focus",
      title: "\u4ECA\u65E5\u7126\u70B9",
      order: 50,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "overview-habit-overview",
      page: "overview",
      type: "habit-overview",
      title: "\u6253\u5361\u603B\u89C8",
      order: 60,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "overview-monthly-progress",
      page: "overview",
      type: "monthly-progress",
      title: "\u672C\u6708\u8FDB\u5EA6",
      order: 70,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "overview-monthly-calendar",
      page: "overview",
      type: "monthly-calendar",
      title: "\u6708\u5EA6\u65E5\u5386",
      order: 80,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "overview-recent-notes",
      page: "overview",
      type: "recent-notes",
      title: "\u6700\u8FD1\u7B14\u8BB0",
      order: 90,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "overview-quick-actions",
      page: "overview",
      type: "quick-actions",
      title: "\u5FEB\u6377\u64CD\u4F5C",
      order: 100,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "overview-contribution-heatmap",
      page: "overview",
      type: "contribution-heatmap",
      title: "\u5E74\u5EA6\u8D21\u732E\u56FE",
      order: 110,
      enabled: true,
      width: "full",
      height: "sm"
    },
    {
      id: "research-projects",
      page: "research",
      type: "research-projects",
      title: "\u7814\u7A76\u9879\u76EE\u603B\u89C8",
      order: 10,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "research-reading-queue",
      page: "research",
      type: "reading-queue",
      title: "\u8BBA\u6587\u9605\u8BFB\u961F\u5217",
      order: 20,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "research-paper-field-manager",
      page: "research",
      type: "paper-field-manager",
      title: "\u8BBA\u6587\u5B57\u6BB5\u7BA1\u7406",
      order: 25,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "research-checkin",
      page: "research",
      type: "research-checkin",
      title: "\u672C\u5468\u79D1\u7814\u6253\u5361",
      order: 30,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "research-experiment-plan",
      page: "research",
      type: "experiment-plan",
      title: "\u5B9E\u9A8C\u8BA1\u5212",
      order: 40,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "research-experiment-records",
      page: "research",
      type: "experiment-records",
      title: "\u5B9E\u9A8C\u8BB0\u5F55",
      order: 50,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "research-data-analysis",
      page: "research",
      type: "data-analysis-tasks",
      title: "\u6570\u636E\u5206\u6790\u4EFB\u52A1",
      order: 60,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "research-literature-notes",
      page: "research",
      type: "literature-notes",
      title: "\u6587\u732E\u7B14\u8BB0",
      order: 70,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "research-timeline",
      page: "research",
      type: "research-timeline",
      title: "\u4F1A\u8BAE / DDL \u65F6\u95F4\u7EBF",
      order: 80,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "research-memo",
      page: "research",
      type: "research-memo",
      title: "\u79D1\u7814\u7075\u611F Memo",
      order: 90,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "reading-current",
      page: "reading",
      type: "current-reading",
      title: "\u5F53\u524D\u9605\u8BFB",
      order: 10,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "reading-bookshelf",
      page: "reading",
      type: "bookshelf",
      title: "\u4E66\u67B6 Bookshelf",
      order: 20,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "reading-plan",
      page: "reading",
      type: "reading-plan",
      title: "\u9605\u8BFB\u8BA1\u5212",
      order: 30,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "reading-checkin",
      page: "reading",
      type: "reading-checkin",
      title: "\u672C\u5468\u9605\u8BFB\u6253\u5361",
      order: 40,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "reading-notes",
      page: "reading",
      type: "reading-notes",
      title: "\u9605\u8BFB\u7B14\u8BB0",
      order: 50,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "reading-quotes",
      page: "reading",
      type: "reading-quotes",
      title: "\u91D1\u53E5\u6458\u5F55",
      order: 60,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "reading-finished",
      page: "reading",
      type: "finished-books",
      title: "\u5DF2\u8BFB\u6E05\u5355",
      order: 80,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "reading-wishlist",
      page: "reading",
      type: "wishlist-books",
      title: "\u60F3\u8BFB\u6E05\u5355",
      order: 90,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "reading-tag-manager",
      page: "reading",
      type: "reading-tag-manager",
      title: "\u6807\u7B7E\u7BA1\u7406",
      order: 70,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "reading-stats",
      page: "reading",
      type: "reading-stats",
      title: "\u9605\u8BFB\u8FDB\u5EA6\u7EDF\u8BA1",
      order: 100,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "reading-heatmap",
      page: "reading",
      type: "reading-heatmap",
      title: "\u6708\u5EA6\u9605\u8BFB\u70ED\u529B\u56FE",
      order: 110,
      enabled: true,
      width: "md",
      height: "md"
    },
    {
      id: "reading-ai-review",
      page: "reading",
      type: "ai-reading-review",
      title: "AI \u9605\u8BFB\u590D\u76D8",
      order: 120,
      enabled: true,
      width: "md",
      height: "md"
    },
    createSection("fitness", "today-workout", "\u4ECA\u65E5\u8BAD\u7EC3", 10),
    createSection("fitness", "workout-plan", "\u8BAD\u7EC3\u8BA1\u5212", 20),
    createSection("fitness", "fitness-checkin", "\u672C\u5468\u5065\u8EAB\u6253\u5361", 30),
    createSection("fitness", "body-measurements", "\u4F53\u91CD\u4E0E\u56F4\u5EA6\u8BB0\u5F55", 40),
    createSection("fitness", "cardio-strength-plan", "\u6709\u6C27 / \u529B\u91CF\u5B89\u6392", 50),
    createSection("fitness", "water-sleep-habits", "\u4E60\u60EF", 60),
    createSection("fitness", "fitness-stats", "\u8D8B\u52BF\u56FE", 70, "lg"),
    createSection("fitness", "workout-log", "\u8FD0\u52A8\u65E5\u5FD7", 80),
    createSection("fitness", "fitness-goals", "\u5065\u8EAB\u76EE\u6807\u8FDB\u5EA6", 90),
    createSection("fitness", "health-reminders", "\u5065\u5EB7\u63D0\u9192", 100),
    createSection("fitness", "fitness-heatmap", "\u6708\u5EA6\u8FD0\u52A8\u70ED\u529B\u56FE", 110),
    createSection("finance", "monthly-budget", "\u672C\u6708\u9884\u7B97", 10),
    createSection("finance", "finance-ledger", "\u8BB0\u8D26", 20),
    createSection("finance", "expense-categories", "\u652F\u51FA\u5206\u7C7B", 30),
    createSection("finance", "account-overview", "\u8D26\u6237\u603B\u89C8", 40),
    createSection("finance", "saving-goals", "\u50A8\u84C4\u76EE\u6807", 50),
    createSection("finance", "bill-reminders", "\u8D26\u5355\u63D0\u9192", 60),
    createSection("finance", "finance-checkin", "\u672C\u5468\u7406\u8D22\u6253\u5361", 70),
    createSection("finance", "income-expense-trend", "\u6536\u652F\u8D8B\u52BF", 80),
    createSection("finance", "finance-todos", "\u672C\u6708\u8BB0\u8D26\u5F85\u529E", 90),
    createSection("finance", "investment-watch", "\u6295\u8D44\u89C2\u5BDF", 100),
    createSection("finance", "expense-heatmap", "\u6708\u5EA6\u652F\u51FA\u70ED\u529B\u56FE", 110),
    createSection("goals", "yearly-goals", "\u5E74\u5EA6\u76EE\u6807", 10),
    createSection("goals", "quarterly-okr", "\u5B63\u5EA6\u76EE\u6807", 20),
    createSection("goals", "monthly-key-results", "\u6708\u5EA6\u76EE\u6807", 30),
    createSection("goals", "goal-breakdown", "\u76EE\u6807\u62C6\u89E3", 40),
    createSection("goals", "milestone-timeline", "\u91CC\u7A0B\u7891\u65F6\u95F4\u7EBF", 50),
    createSection("goals", "priority-matrix", "\u4F18\u5148\u7EA7\u77E9\u9635", 60),
    createSection("goals", "goals-checkin", "\u672C\u5468\u76EE\u6807\u6253\u5361", 70),
    createSection("goals", "review-checklist", "\u590D\u76D8\u6E05\u5355", 80),
    createSection("goals", "risks-blockers", "\u98CE\u9669\u4E0E\u963B\u788D", 90),
    createSection("goals", "long-term-progress", "\u957F\u671F\u8FDB\u5C55", 100),
    createSection("modules", "enabled-modules-overview", "\u5DF2\u542F\u7528\u6A21\u5757\u6982\u89C8", 10),
    createSection("modules", "home-layout-manager", "\u9875\u9762\u5E03\u5C40\u7BA1\u7406", 20),
    createSection("modules", "section-manager", "\u529F\u80FD\u5206\u533A\u7BA1\u7406", 30, "full", "lg"),
    createSection("modules", "banner-background-settings", "Banner \u80CC\u666F\u8BBE\u7F6E", 50),
    createSection("modules", "calendar-widget-settings", "\u65E5\u5386\u7EC4\u4EF6\u8BBE\u7F6E", 60),
    createSection("modules", "apex-habit-settings", "Apex \u6253\u5361\u6A21\u5757\u8BBE\u7F6E", 70),
    createSection("modules", "quick-action-settings", "\u5FEB\u6377\u64CD\u4F5C\u914D\u7F6E", 80),
    createSection("modules", "theme-color-settings", "\u4E3B\u9898\u4E0E\u914D\u8272", 90),
    createSection("modules", "data-source-status", "\u6570\u636E\u6E90", 100)
  ],
  moduleLayouts: structuredClone(DEFAULT_MODULE_LAYOUTS),
  banner: {
    message: "\u8981\u6210\u529F\uFF0C\u5148\u53D1\u75AF\uFF0C\u4E0D\u987E\u4E00\u5207\u5411\u524D\u51B2\u3002",
    subtitle: "\u628A\u60F3\u6CD5\u53D8\u6210\u884C\u52A8\uFF0C\u8BA9\u6BCF\u4E00\u5929\u90FD\u66F4\u9760\u8FD1\u7406\u60F3\u7684\u81EA\u5DF1\u3002",
    background: "pink-paper",
    backgroundPosition: "center",
    overlay: true,
    opacity: 0.88,
    sidebarAvatar: { type: "preset", value: "dog" },
    bannerAvatar: { type: "preset", value: "dog" }
  },
  habits: {},
  todayFocusTasks: [
    { id: "focus-research", label: "\u63A8\u8FDB\u8BBA\u6587\u6216\u5B9E\u9A8C\u8BB0\u5F55", category: "\u79D1\u7814", completed: true },
    { id: "focus-reading", label: "\u9605\u8BFB\u5E76\u6574\u7406\u4E00\u6761\u7B14\u8BB0", category: "\u9605\u8BFB", completed: true },
    { id: "focus-fitness", label: "\u5B8C\u6210\u4ECA\u65E5\u8BAD\u7EC3\u6216\u62C9\u4F38", category: "\u5065\u8EAB", completed: false },
    { id: "focus-finance", label: "\u68C0\u67E5\u9884\u7B97\u4E0E\u8D26\u5355", category: "\u7406\u8D22", completed: false },
    { id: "focus-personal", label: "\u590D\u76D8\u4ECA\u5929\u7684\u8BA1\u5212", category: "\u4E2A\u4EBA", completed: false }
  ],
  researchProjects: [
    {
      id: "project-medical-vlm",
      title: "\u591A\u6A21\u6001\u5927\u6A21\u578B\u5728\u533B\u5B66\u5F71\u50CF\u4E2D\u7684\u5E94\u7528",
      status: "\u8FDB\u884C\u4E2D",
      progress: 70,
      startDate: "2026-08-01",
      deadline: "2026-10-30",
      tags: ["VLM", "\u533B\u5B66\u5F71\u50CF"]
    },
    {
      id: "project-graph-drug",
      title: "\u57FA\u4E8E\u56FE\u795E\u7ECF\u7F51\u7EDC\u7684\u836F\u7269\u91CD\u5B9A\u4F4D\u7814\u7A76",
      status: "\u64B0\u5199\u4E2D",
      progress: 40,
      startDate: "2026-07-15",
      deadline: "2026-11-15",
      tags: ["GNN", "Drug"]
    },
    {
      id: "project-single-cell",
      title: "\u5355\u7EC6\u80DE\u65F6\u7A7A\u8F6C\u5F55\u7EC4\u6570\u636E\u5206\u6790\u65B9\u6CD5",
      status: "\u5DF2\u5B8C\u6210",
      progress: 100,
      startDate: "2026-06-10",
      deadline: "2026-09-01",
      tags: ["scRNA-seq"]
    }
  ],
  researchPapers: [
    { id: "paper-survey-llm", title: "A Survey on Multimodal LLMs", venue: "CVPR", venueId: "venue-cvpr", year: 2024, status: "\u8FDB\u884C\u4E2D", statusId: "paper-status-reading", readingProgress: 62, tagIds: ["paper-tag-multimodal"], createdAt: 1789344e6, updatedAt: 1789344e6 },
    { id: "paper-single-cell", title: "Single-cell foundation models", venue: "Nature", venueId: "venue-nature", year: 2024, status: "\u672A\u8BFB", statusId: "paper-status-unread", readingProgress: 0, tagIds: ["paper-tag-biology"], createdAt: 1789344e6, updatedAt: 1789344e6 },
    { id: "paper-gnn-drug", title: "Graph Neural Networks for Drug Discovery", venue: "ICLR", venueId: "venue-iclr", year: 2024, status: "\u8FDB\u884C\u4E2D", statusId: "paper-status-reading", readingProgress: 45, tagIds: ["paper-tag-gnn"], createdAt: 1789344e6, updatedAt: 1789344e6 }
  ],
  literatureNotes: [
    { id: "lit-note-survey-llm", title: "A Survey on Multimodal LLMs \u9605\u8BFB\u7B14\u8BB0", notePath: "", paperReadingId: "paper-survey-llm", createdAt: 1789344e6, updatedAt: 1789344e6 }
  ],
  paperStatuses: [
    { id: "paper-status-unread", name: "\u672A\u8BFB", color: "#f8a8c4", order: 10 },
    { id: "paper-status-planned", name: "\u8BA1\u5212\u9605\u8BFB", color: "#ffd166", order: 20 },
    { id: "paper-status-reading", name: "\u9605\u8BFB\u4E2D", color: "#76c7f2", order: 30 },
    { id: "paper-status-finished", name: "\u5DF2\u8BFB", color: "#7bd88f", order: 40 },
    { id: "paper-status-paused", name: "\u6682\u505C", color: "#c9b6ff", order: 50 }
  ],
  paperVenues: [
    { id: "venue-cvpr", name: "CVPR", type: "conference", color: "#76c7f2" },
    { id: "venue-iclr", name: "ICLR", type: "conference", color: "#c9b6ff" },
    { id: "venue-nature", name: "Nature", type: "journal", color: "#7bd88f" },
    { id: "venue-chi", name: "CHI", type: "conference", color: "#f8a8c4" }
  ],
  paperTags: [
    { id: "paper-tag-multimodal", name: "Multimodal", color: "#f8a8c4" },
    { id: "paper-tag-biology", name: "Biology", color: "#7bd88f" },
    { id: "paper-tag-gnn", name: "GNN", color: "#c9b6ff" },
    { id: "paper-tag-wearable", name: "Wearable", color: "#ffd166" }
  ],
  experimentPlans: [
    { id: "exp-cell-drug", title: "\u7EC6\u80DE\u7CFB\u4F20\u4EE3\u4E0E\u836F\u7269\u5904\u7406", date: "2026-09-14", status: "\u8FDB\u884C\u4E2D" },
    { id: "exp-western", title: "Western Blot \u5B9E\u9A8C", date: "2026-09-16", status: "\u672A\u5F00\u59CB" },
    { id: "exp-flow", title: "\u6D41\u5F0F\u7EC6\u80DE\u672F FACS", date: "2026-09-18", status: "\u8BA1\u5212\u4E2D" }
  ],
  experimentRecords: [
    { id: "record-drug", title: "\u7EC6\u80DE\u836F\u7269\u5904\u7406\u8BB0\u5F55", date: "2026-09-13", status: "\u5DF2\u5B8C\u6210", experimentPlanId: "exp-cell-drug" },
    { id: "record-wb", title: "WB \u6761\u5E26\u7ED3\u679C", date: "2026-09-12", status: "\u5DF2\u5B8C\u6210", experimentPlanId: "exp-western" },
    { id: "record-flow", title: "\u6D41\u5F0F\u6570\u636E\u5206\u6790", date: "2026-09-10", status: "\u8FDB\u884C\u4E2D" }
  ],
  researchDeadlines: [
    { id: "ddl-report", title: "\u7EC4\u4F1A\u5B9E\u9A8C\u8FDB\u5C55\u6C47\u62A5", date: "2026-09-18", type: "\u7EC4\u4F1A", priority: "medium" },
    { id: "ddl-dataset", title: "\u84DD\u91D1\u7533\u8BF7\u4E66\u63D0\u4EA4", date: "2026-10-05", type: "DDL", priority: "high" },
    { id: "ddl-neurips", title: "NeurIPS \u6295\u7A3F\u622A\u6B62", date: "2026-10-09", type: "DDL", priority: "high" }
  ],
  researchMemos: [
    "\u628A\u56FE\u795E\u7ECF\u7F51\u7EDC\u548C\u533B\u5B66\u5F71\u50CF\u9884\u8BAD\u7EC3\u7ED3\u5408\uFF0C\u770B\u770B\u80FD\u5426\u6539\u5584\u5C0F\u6837\u672C\u573A\u666F\u3002",
    "\u5BF9\u6BD4\u5B66\u4E60\u6846\u67B6\u4E5F\u8BB8\u53EF\u4EE5\u4F5C\u4E3A\u8BBA\u6587\u65B9\u6CD5\u90E8\u5206\u7684\u4E3B\u7EBF\u3002",
    "\u7EC4\u4F1A\u524D\u6574\u7406\u4E00\u6B21\u5931\u8D25\u5B9E\u9A8C\uFF0C\u53EF\u80FD\u6BD4\u53EA\u5C55\u793A\u6210\u529F\u7ED3\u679C\u66F4\u6709\u4EF7\u503C\u3002"
  ],
  dataAnalysisTasks: [
    { id: "analysis-scrna", title: "scRNA-seq \u6570\u636E\u9884\u5904\u7406", progress: 80, status: "\u8FDB\u884C\u4E2D" },
    { id: "analysis-deg", title: "\u5DEE\u5F02\u57FA\u56E0\u5206\u6790", progress: 100, status: "\u5DF2\u5B8C\u6210" },
    { id: "analysis-chart", title: "\u53EF\u89C6\u5316\u56FE\u8868\u751F\u6210", progress: 30, status: "\u8FDB\u884C\u4E2D" },
    { id: "analysis-paper", title: "\u8BBA\u6587\u56FE\u8868\u6574\u7406", progress: 50, status: "\u8FDB\u884C\u4E2D" }
  ],
  books: [
    {
      id: "book-deep-work",
      title: "\u6DF1\u5EA6\u5DE5\u4F5C",
      author: "Cal Newport",
      totalPages: 304,
      currentPage: 188,
      status: "\u5728\u8BFB",
      rating: 4,
      startDate: "2026-09-01",
      tagIds: ["book-tag-efficiency", "book-tag-focus"],
      tags: ["\u6548\u7387", "\u4E13\u6CE8"]
    },
    {
      id: "book-atomic-habits",
      title: "Atomic Habits",
      author: "James Clear",
      totalPages: 320,
      currentPage: 320,
      status: "\u5DF2\u8BFB",
      rating: 5,
      startDate: "2026-08-01",
      finishDate: "2026-08-21",
      tagIds: ["book-tag-habit"],
      tags: ["\u4E60\u60EF"]
    },
    {
      id: "book-thinking",
      title: "Thinking, Fast and Slow",
      author: "Daniel Kahneman",
      totalPages: 499,
      currentPage: 0,
      status: "\u60F3\u8BFB",
      tagIds: ["book-tag-psychology"],
      tags: ["\u5FC3\u7406\u5B66"]
    }
  ],
  readingQuotes: [
    { id: "quote-1", text: "\u4E13\u6CE8\u4E0D\u662F\u62D2\u7EDD\u4E16\u754C\uFF0C\u800C\u662F\u9009\u62E9\u6B64\u523B\u771F\u6B63\u91CD\u8981\u7684\u4E8B\u3002", source: "\u6DF1\u5EA6\u5DE5\u4F5C", bookId: "book-deep-work", style: "sticky", backgroundColor: "#fff1ad" },
    { id: "quote-2", text: "\u5FAE\u5C0F\u4E60\u60EF\u4F1A\u5728\u65F6\u95F4\u91CC\u590D\u5229\u3002", source: "Atomic Habits", bookId: "book-atomic-habits", style: "soft", backgroundColor: "#ffe0ed" }
  ],
  wantToReadItems: [
    { id: "want-design-book", title: "\u8BBE\u8BA1\u5FC3\u7406\u5B66", author: "Donald A. Norman", summary: "\u8865\u4E00\u4E0B\u4EA7\u54C1\u8BBE\u8BA1\u548C\u53EF\u7528\u6027\u57FA\u7840\u3002", status: "pending", createdAt: "2026-09-01T08:00:00.000Z", updatedAt: "2026-09-01T08:00:00.000Z" }
  ],
  bookTags: [
    { id: "book-tag-efficiency", name: "\u6548\u7387", color: "#f8a8c4", order: 10 },
    { id: "book-tag-focus", name: "\u4E13\u6CE8", color: "#76c7f2", order: 20 },
    { id: "book-tag-habit", name: "\u4E60\u60EF", color: "#7bd88f", order: 30 },
    { id: "book-tag-psychology", name: "\u5FC3\u7406\u5B66", color: "#c9b6ff", order: 40 }
  ],
  readingNotes: [],
  readingPlans: [
    { id: "plan-deep-work-sep", bookId: "book-deep-work", startDate: "2026-09-01", endDate: "2026-09-30", targetPages: 304, goal: "\u8BFB\u5B8C\u6574\u672C\u4E66", progress: 62, note: "\u5B8C\u6210\u5168\u4E66\u5E76\u6574\u7406\u6DF1\u5EA6\u5DE5\u4F5C\u5B9E\u8DF5\u6E05\u5355\u3002", status: "active", createdAt: "2026-09-01T08:00:00.000Z", updatedAt: "2026-09-01T08:00:00.000Z" },
    { id: "plan-thinking-oct", bookId: "book-thinking", startDate: "2026-10-01", endDate: "2026-10-31", targetPages: 180, goal: "\u9605\u8BFB\u5224\u65AD\u4E0E\u51B3\u7B56\u76F8\u5173\u7AE0\u8282", progress: 0, note: "\u5148\u8BFB\u5224\u65AD\u4E0E\u51B3\u7B56\u76F8\u5173\u7AE0\u8282\u3002", status: "planned", createdAt: "2026-09-01T08:00:00.000Z", updatedAt: "2026-09-01T08:00:00.000Z" }
  ],
  workouts: [
    { id: "workout-1", date: "2026-09-14", type: "\u529B\u91CF", duration: 45, calories: 320, completed: false, note: "\u4E0B\u80A2\u529B\u91CF + \u6838\u5FC3" },
    { id: "workout-2", date: "2026-09-12", type: "\u6709\u6C27", duration: 35, calories: 260, completed: true, note: "\u692D\u5706\u673A\u4E2D\u7B49\u5F3A\u5EA6" },
    { id: "workout-3", date: "2026-09-10", type: "\u62C9\u4F38", duration: 20, calories: 80, completed: true, note: "\u80A9\u9888\u548C\u9ACB\u90E8\u653E\u677E" }
  ],
  trainingPlans: [
    {
      id: "training-plan-weight",
      title: "\u6BCF\u5468\u4E09\u7EC3\u51CF\u91CD\u8BA1\u5212",
      fitnessGoalId: "fitness-goal-weight",
      startDate: "2026-09-01",
      endDate: "2026-12-31",
      weeklyFrequency: 3,
      description: "\u5468\u4E00\u529B\u91CF\uFF0C\u5468\u4E09\u6709\u6C27\uFF0C\u5468\u4E94\u5168\u8EAB\u5FAA\u73AF\u3002",
      note: "\u6839\u636E\u72B6\u6001\u8C03\u6574\u5F3A\u5EA6\u3002",
      status: "active",
      createdAt: "2026-09-01T08:00:00.000Z",
      updatedAt: "2026-09-14T08:00:00.000Z"
    }
  ],
  bodyMeasurements: [
    { id: "measure-2026-09-01", date: "2026-09-01", weight: 58.8, bmi: 21.6, waist: 70, chest: 84, hip: 91, note: "", createdAt: "2026-09-01T08:00:00.000Z", updatedAt: "2026-09-01T08:00:00.000Z" },
    { id: "measure-2026-09-08", date: "2026-09-08", weight: 58.2, bmi: 21.4, waist: 69, chest: 84, hip: 90, note: "", createdAt: "2026-09-08T08:00:00.000Z", updatedAt: "2026-09-08T08:00:00.000Z" },
    { id: "measure-2026-09-14", date: "2026-09-14", weight: 57.9, bmi: 21.3, waist: 68, chest: 84, hip: 90, note: "", createdAt: "2026-09-14T08:00:00.000Z", updatedAt: "2026-09-14T08:00:00.000Z" }
  ],
  fitnessGoals: [
    { id: "fitness-goal-weight", title: "\u7A33\u5B9A\u4F53\u91CD", type: "\u51CF\u91CD", description: "\u7A33\u5B9A\u4E0B\u964D\u5230\u66F4\u8F7B\u76C8\u7684\u72B6\u6001\u3002", currentValue: 57.9, targetValue: 56.5, unit: "kg", targetUnit: "kg", progress: 46, startDate: "2026-09-01", deadline: "2026-12-31", status: "active", createdAt: "2026-09-01T08:00:00.000Z", updatedAt: "2026-09-14T08:00:00.000Z" },
    { id: "fitness-goal-cardio", title: "\u672C\u6708\u6709\u6C27", type: "\u8DD1\u6B65", currentValue: 210, targetValue: 600, unit: "min", targetUnit: "min", progress: 35, startDate: "2026-09-01", deadline: "2026-09-30", status: "active", createdAt: "2026-09-01T08:00:00.000Z", updatedAt: "2026-09-14T08:00:00.000Z" },
    { id: "fitness-goal-strength", title: "\u529B\u91CF\u8BAD\u7EC3", type: "\u529B\u91CF", currentValue: 6, targetValue: 12, unit: "\u6B21", targetUnit: "\u6B21", progress: 50, startDate: "2026-09-01", deadline: "2026-09-30", status: "active", createdAt: "2026-09-01T08:00:00.000Z", updatedAt: "2026-09-14T08:00:00.000Z" }
  ],
  healthReminders: [
    { id: "health-warmup", title: "\u8BAD\u7EC3\u524D\u70ED\u8EAB 8 \u5206\u949F\u3002", date: "2026-09-14", time: "18:30", repeatType: "once", repeatDays: [], note: "", enabled: true, createdAt: "2026-09-14T08:00:00.000Z", updatedAt: "2026-09-14T08:00:00.000Z" },
    { id: "health-stand", title: "\u4E45\u5750 50 \u5206\u949F\u540E\u8D77\u8EAB\u6D3B\u52A8\u3002", date: "2026-09-14", time: "15:30", repeatType: "daily", repeatDays: [], note: "", enabled: true, createdAt: "2026-09-14T08:00:00.000Z", updatedAt: "2026-09-14T08:00:00.000Z" },
    { id: "health-protein", title: "\u529B\u91CF\u65E5\u540E\u8865\u5145\u86CB\u767D\u8D28\u548C\u7761\u7720\u3002", date: "2026-09-14", time: "20:30", repeatType: "once", repeatDays: [], note: "", enabled: true, createdAt: "2026-09-14T08:00:00.000Z", updatedAt: "2026-09-14T08:00:00.000Z" },
    { id: "health-recovery", title: "\u72B6\u6001\u5DEE\u65F6\u5141\u8BB8\u964D\u5F3A\u5EA6\uFF0C\u4E0D\u786C\u625B\u3002", date: "2026-09-14", time: "21:30", repeatType: "daily", repeatDays: [], note: "", enabled: true, createdAt: "2026-09-14T08:00:00.000Z", updatedAt: "2026-09-14T08:00:00.000Z" }
  ],
  healthReminderLogs: [],
  transactions: [
    { id: "tx-1", type: "income", category: "\u5DE5\u8D44", amount: 12e3, date: "2026-09-01", note: "\u6708\u6536\u5165" },
    { id: "tx-2", type: "expense", category: "\u9910\u996E", amount: 860, date: "2026-09-03", note: "\u5916\u98DF\u4E0E\u5496\u5561" },
    { id: "tx-3", type: "expense", category: "\u4EA4\u901A", amount: 220, date: "2026-09-05", note: "\u901A\u52E4" },
    { id: "tx-4", type: "expense", category: "\u5B66\u4E60", amount: 399, date: "2026-09-08", note: "\u8BFE\u7A0B\u8BA2\u9605" },
    { id: "tx-5", type: "expense", category: "\u8D2D\u7269", amount: 520, date: "2026-09-12", note: "\u65E5\u7528\u54C1" }
  ],
  budgets: [
    { id: "budget-food", category: "\u9910\u996E", amount: 1800, spent: 860 },
    { id: "budget-transport", category: "\u4EA4\u901A", amount: 500, spent: 220 },
    { id: "budget-study", category: "\u5B66\u4E60", amount: 800, spent: 399 },
    { id: "budget-shopping", category: "\u8D2D\u7269", amount: 1200, spent: 520 }
  ],
  accounts: [
    { id: "account-cash", name: "\u73B0\u91D1\u94B1\u5305", type: "\u73B0\u91D1", balance: 800 },
    { id: "account-card", name: "\u50A8\u84C4\u5361", type: "\u50A8\u84C4\u5361", balance: 32600 },
    { id: "account-invest", name: "\u57FA\u91D1\u8D26\u6237", type: "\u6295\u8D44\u8D26\u6237", balance: 18800 }
  ],
  savingGoals: [
    { id: "saving-emergency", title: "\u5E94\u6025\u91D1", current: 18e3, target: 3e4, deadline: "2026-12-31" },
    { id: "saving-travel", title: "\u65C5\u884C\u57FA\u91D1", current: 3600, target: 8e3, deadline: "2026-10-31" }
  ],
  bills: [
    { id: "bill-rent", title: "\u623F\u79DF", amount: 3e3, dueDate: "2026-09-20", paid: false },
    { id: "bill-phone", title: "\u624B\u673A\u5957\u9910", amount: 89, dueDate: "2026-09-18", paid: false },
    { id: "bill-card", title: "\u4FE1\u7528\u5361\u8FD8\u6B3E", amount: 1260, dueDate: "2026-09-25", paid: false }
  ],
  financeTodos: [
    { id: "finance-todo-transport", title: "\u8BB0\u5F55\u672C\u5468\u4EA4\u901A\u652F\u51FA", completed: false },
    { id: "finance-todo-food", title: "\u68C0\u67E5\u9910\u996E\u9884\u7B97", completed: true },
    { id: "finance-todo-bills", title: "\u786E\u8BA4\u8D26\u5355\u63D0\u9192", completed: false },
    { id: "finance-todo-invest", title: "\u6574\u7406\u6295\u8D44\u89C2\u5BDF\u7B14\u8BB0", completed: false }
  ],
  tasks: [],
  taskProjects: [],
  taskSettings: {},
  goals: [
    {
      id: "goal-research",
      title: "\u5B8C\u6210\u4E00\u7BC7\u9AD8\u8D28\u91CF\u79D1\u7814\u8BBA\u6587",
      description: "\u5B8C\u6210\u5B9E\u9A8C\u3001\u64B0\u5199\u521D\u7A3F\u5E76\u8FDB\u5165\u6295\u7A3F\u51C6\u5907\u3002",
      category: "\u79D1\u7814",
      progress: 62,
      deadline: "2026-12-20",
      status: "\u8FDB\u884C\u4E2D"
    },
    {
      id: "goal-health",
      title: "\u5EFA\u7ACB\u7A33\u5B9A\u5065\u5EB7\u8282\u594F",
      description: "\u6BCF\u5468\u8BAD\u7EC3\u3001\u89C4\u5F8B\u7761\u7720\u3001\u4FDD\u6301\u80FD\u91CF\u3002",
      category: "\u5065\u5EB7",
      progress: 48,
      deadline: "2026-12-31",
      status: "\u8FDB\u884C\u4E2D"
    },
    {
      id: "goal-finance",
      title: "\u5B8C\u6210\u5E74\u5EA6\u50A8\u84C4\u8BA1\u5212",
      description: "\u63A7\u5236\u9884\u7B97\uFF0C\u63D0\u9AD8\u50A8\u84C4\u7387\u3002",
      category: "\u7406\u8D22",
      progress: 55,
      deadline: "2026-12-31",
      status: "\u8FDB\u884C\u4E2D"
    }
  ],
  goalActions: [],
  objectives: [
    { id: "obj-q3-research", title: "Q3 \u5B8C\u6210\u8BBA\u6587\u65B9\u6CD5\u548C\u5B9E\u9A8C\u95ED\u73AF", quarter: "2026 Q3", progress: 72 },
    { id: "obj-q4-life", title: "Q4 \u5EFA\u7ACB\u53EF\u6301\u7EED\u5DE5\u4F5C\u751F\u6D3B\u7CFB\u7EDF", quarter: "2026 Q4", progress: 35 }
  ],
  keyResults: [
    { id: "kr-exp", objectiveId: "obj-q3-research", title: "\u5B8C\u6210 3 \u7EC4\u5173\u952E\u5B9E\u9A8C", progress: 80, completed: false },
    { id: "kr-draft", objectiveId: "obj-q3-research", title: "\u8BBA\u6587\u521D\u7A3F\u8FBE\u5230\u53EF\u5185\u5BA1\u72B6\u6001", progress: 60, completed: false },
    { id: "kr-training", objectiveId: "obj-q4-life", title: "\u8FDE\u7EED 8 \u5468\u8BAD\u7EC3\u4E0D\u5C11\u4E8E 3 \u6B21", progress: 45, completed: false }
  ],
  milestones: [
    { id: "mile-exp", goalId: "goal-research", title: "\u5B8C\u6210\u6838\u5FC3\u5B9E\u9A8C", date: "2026-09-30", status: "\u8FDB\u884C\u4E2D" },
    { id: "mile-draft", goalId: "goal-research", title: "\u5B8C\u6210\u8BBA\u6587\u521D\u7A3F", date: "2026-10-20", status: "\u672A\u5F00\u59CB" },
    { id: "mile-review", goalId: "goal-research", title: "\u5B8C\u6210\u7EC4\u5185\u53CD\u9988\u4FEE\u6539", date: "2026-11-10", status: "\u672A\u5F00\u59CB" }
  ],
  risks: [
    { id: "risk-time", title: "\u5B9E\u9A8C\u6392\u671F\u88AB\u538B\u7F29", level: "high", solution: "\u63D0\u524D\u9884\u7EA6\u8BBE\u5907\uFF0C\u51C6\u5907\u66FF\u4EE3\u5B9E\u9A8C\u65B9\u6848\u3002" },
    { id: "risk-energy", title: "\u7761\u7720\u4E0D\u8DB3\u5F71\u54CD\u6267\u884C", level: "medium", solution: "\u665A\u95F4\u56FA\u5B9A\u6536\u5C3E\uFF0C\u51CF\u5C11\u4E34\u7761\u524D\u8F93\u5165\u3002" }
  ],
  reviewItems: [
    { id: "review-week-focus", date: "2026-09-14", title: "\u672C\u5468\u76EE\u6807\u63A8\u8FDB\u590D\u76D8", content: "\u68C0\u67E5\u6700\u91CD\u8981\u7684\u76EE\u6807\u662F\u5426\u771F\u7684\u5411\u524D\u63A8\u8FDB\u3002", status: "todo", createdAt: "2026-09-14T08:00:00.000Z", updatedAt: "2026-09-14T08:00:00.000Z" },
    { id: "review-cut-busywork", date: "2026-09-14", title: "\u51CF\u5C11\u5FD9\u788C\u611F\u4EFB\u52A1", content: "\u8BC6\u522B\u53EA\u662F\u5E26\u6765\u5FD9\u788C\u611F\u3001\u4F46\u4E0D\u4EA7\u751F\u7ED3\u679C\u7684\u4EFB\u52A1\u3002", status: "todo", createdAt: "2026-09-14T08:00:00.000Z", updatedAt: "2026-09-14T08:00:00.000Z" }
  ],
  calendarSettings: {
    showNoteMarkers: true,
    showTaskMarkers: true,
    showEventMarkers: true,
    weekStartsOn: "monday",
    highlightColor: "#f23b8d"
  },
  calendarTodos: [],
  apexHabitSettings: {
    showOnOverview: true,
    showStreak: true,
    showWeeklyProgress: true,
    customHabits: [
      { id: "custom-reading", label: "\u9605\u8BFB", enabled: true, order: 10 },
      { id: "custom-fitness", label: "\u5065\u8EAB", enabled: true, order: 20 },
      { id: "custom-finance", label: "\u7406\u8D22", enabled: true, order: 30 },
      { id: "custom-writing", label: "\u5199\u4F5C", enabled: true, order: 40 },
      { id: "custom-study", label: "\u5B66\u4E60", enabled: true, order: 50 }
    ]
  },
  checkInDefinitions: createDefaultCheckInDefinitions(),
  checkInRecords: [],
  quickActions: [
    { id: "quick-new-note", label: "\u65B0\u5EFA\u7B14\u8BB0", enabled: true, order: 10, type: "new-note" },
    { id: "quick-daily-note", label: "\u6253\u5F00\u4ECA\u65E5\u7B14\u8BB0", enabled: true, order: 20, type: "daily-note" },
    { id: "quick-search", label: "\u641C\u7D22", enabled: true, order: 30, type: "search" },
    { id: "quick-templates", label: "\u6253\u5F00\u6A21\u677F", enabled: true, order: 40, type: "templates" },
    { id: "quick-graph", label: "\u6253\u5F00\u56FE\u8C31", enabled: true, order: 50, type: "graph" }
  ],
  focusSettings: {
    focusDuration: 25,
    breakDuration: 5,
    autoStartBreak: false,
    autoStartNextFocus: false,
    defaultBackground: "pink"
  },
  focusState: {
    isRunning: false,
    isPaused: false,
    mode: "focus",
    remainingSeconds: 25 * 60
  },
  focusRecords: [],
  fitnessDailyRecords: [
    { date: "2026-09-14", waterCups: 5, waterGoal: 8, waterNote: "", sleepHours: 7, sleepGoal: 8, bedtime: "23:30", wakeTime: "07:00", sleepNote: "", updatedAt: "2026-09-14T08:00:00.000Z" }
  ],
  fitnessHabitDefinitions: [
    { id: "fitness-habit-steps", name: "\u6B65\u6570", targetName: "\u6BCF\u65E5\u6B65\u6570\u76EE\u6807", targetValue: 1e4, unit: "\u6B65", order: 10, createdAt: "2026-09-14T08:00:00.000Z", updatedAt: "2026-09-14T08:00:00.000Z" }
  ],
  fitnessHabitRecords: [
    { date: "2026-09-14", habitId: "fitness-habit-steps", actualValue: 8530, note: "", updatedAt: "2026-09-14T08:00:00.000Z" }
  ],
  investmentWatchItems: [
    { id: "watch-hs300", name: "\u6CAA\u6DF1300", code: "CSI300", price: 3800, changePercent: 0.8, type: "\u6307\u6570" },
    { id: "watch-btc", name: "\u6BD4\u7279\u5E01", code: "BTC", price: 65e3, changePercent: -1.2, type: "\u52A0\u5BC6\u8D44\u4EA7" },
    { id: "watch-gold", name: "\u9EC4\u91D1", code: "XAU", price: 2380, changePercent: 0.3, type: "\u5546\u54C1" }
  ],
  investmentSnapshots: [
    { id: "snapshot-watch-hs300-2026-09-14", investmentId: "watch-hs300", date: "2026-09-14", price: 3800, changePercent: 0.8, note: "", createdAt: "2026-09-14T08:00:00.000Z" },
    { id: "snapshot-watch-btc-2026-09-14", investmentId: "watch-btc", date: "2026-09-14", price: 65e3, changePercent: -1.2, note: "", createdAt: "2026-09-14T08:00:00.000Z" },
    { id: "snapshot-watch-gold-2026-09-14", investmentId: "watch-gold", date: "2026-09-14", price: 2380, changePercent: 0.3, note: "", createdAt: "2026-09-14T08:00:00.000Z" }
  ],
  priorityMatrixItems: [
    { id: "priority-1", title: "\u672C\u5468\u5FC5\u987B\u4EA4\u4ED8\u7684\u5173\u952E\u7ED3\u679C", quadrant: "important-urgent", note: "\u4F18\u5148\u5904\u7406", completed: false },
    { id: "priority-2", title: "\u957F\u671F\u76EE\u6807\u3001\u5065\u5EB7\u8282\u594F\u3001\u80FD\u529B\u5EFA\u8BBE", quadrant: "important-not-urgent", note: "\u6BCF\u5929\u63A8\u8FDB", completed: false },
    { id: "priority-3", title: "\u4E34\u65F6\u6D88\u606F\u3001\u6D41\u7A0B\u6027\u5904\u7406", quadrant: "not-important-urgent", note: "\u96C6\u4E2D\u6279\u5904\u7406", completed: false },
    { id: "priority-4", title: "\u4F4E\u4EF7\u503C\u6D88\u8017\uFF0C\u5C3D\u91CF\u51CF\u5C11", quadrant: "not-important-not-urgent", note: "\u51CF\u5C11\u6295\u5165", completed: false }
  ],
  theme: {
    cuteBg: "#ffd1e2",
    cuteCard: "#fff7df",
    cutePrimary: "#f23b8d",
    cuteSecondary: "#ffd166",
    cuteText: "#4a1f19",
    cuteBorder: "rgba(242, 59, 141, 0.26)",
    cuteRadius: 18,
    cuteShadow: "0 10px 28px rgba(190, 66, 120, 0.16)",
    cardOpacity: 0.86,
    textureStrength: 0.5,
    fontSize: 14
  },
  userSettings: {
    showLeftSidebar: true,
    weekStartsOn: "monday",
    dateFormat: "YYYY-MM-DD",
    overviewLayout: "default"
  }
};
var DashboardStore = class {
  constructor(loadPluginData, savePluginData) {
    this.loadPluginData = loadPluginData;
    this.savePluginData = savePluginData;
    this.data = structuredClone(DEFAULT_DATA);
  }
  async load() {
    const savedData = await this.loadPluginData();
    this.data = this.mergeWithDefaults(savedData);
  }
  async save() {
    await this.savePluginData(this.data);
  }
  getData() {
    return this.data;
  }
  getPages() {
    return DASHBOARD_PAGES;
  }
  getAvailableModules(page) {
    return AVAILABLE_MODULES.filter((module2) => module2.page === "all" || module2.page === page);
  }
  getAllSections() {
    return [...this.data.sections].sort((left, right) => {
      if (left.page === right.page) return left.order - right.order;
      return left.page.localeCompare(right.page);
    });
  }
  getSectionsForPage(page) {
    const layout = this.getModuleLayout(page);
    return this.data.sections.filter((section) => section.page === page && section.enabled).sort((left, right) => this.getSectionLayoutOrder(left, layout) - this.getSectionLayoutOrder(right, layout));
  }
  getModuleLayout(page) {
    var _a;
    return (_a = this.data.moduleLayouts[page]) != null ? _a : DEFAULT_MODULE_LAYOUTS[page];
  }
  async setCurrentPage(page) {
    this.data.currentPage = page;
    await this.save();
  }
  async addSection(page, moduleType) {
    var _a, _b, _c;
    const moduleDefinition = this.getAvailableModules(page).find((module2) => module2.type === moduleType);
    const existingSections = this.data.sections.filter((section2) => section2.page === page);
    const nextOrder = existingSections.reduce((max, section2) => Math.max(max, section2.order), 0) + 10;
    const section = {
      id: `${page}-${moduleType}-${Date.now()}`,
      page,
      type: moduleType,
      title: (_a = moduleDefinition == null ? void 0 : moduleDefinition.title) != null ? _a : moduleType,
      order: nextOrder,
      enabled: true,
      width: (_b = moduleDefinition == null ? void 0 : moduleDefinition.defaultWidth) != null ? _b : "md",
      height: (_c = moduleDefinition == null ? void 0 : moduleDefinition.defaultHeight) != null ? _c : "sm",
      config: {}
    };
    this.data.sections.push(section);
    this.ensureSectionLayout(page, section.id, nextOrder);
    await this.save();
    return section;
  }
  async addCustomSection(input) {
    var _a;
    const existingSections = this.data.sections.filter((section2) => section2.page === input.page);
    const nextOrder = (_a = input.order) != null ? _a : existingSections.reduce((max, section2) => Math.max(max, section2.order), 0) + 10;
    const section = {
      id: input.id || `${input.page}-${input.type}-${Date.now()}`,
      page: input.page,
      type: input.type,
      title: input.title,
      order: nextOrder,
      enabled: true,
      width: "md",
      height: "md",
      config: {
        description: input.description,
        cardColor: input.color,
        customType: input.type
      }
    };
    this.data.sections.push(section);
    this.ensureSectionLayout(input.page, section.id, nextOrder);
    await this.save();
    return section;
  }
  async removeSection(sectionId) {
    var _a, _b;
    const section = this.data.sections.find((item) => item.id === sectionId);
    this.data.sections = this.data.sections.filter((section2) => section2.id !== sectionId);
    if (section) {
      (_b = (_a = this.data.moduleLayouts[section.page]) == null ? void 0 : _a.sections) == null ? true : delete _b[sectionId];
    }
    await this.save();
  }
  async setSectionEnabled(sectionId, enabled) {
    const section = this.data.sections.find((item) => item.id === sectionId);
    if (!section) return;
    section.enabled = enabled;
    await this.save();
  }
  async updateSection(sectionId, updates) {
    const section = this.data.sections.find((item) => item.id === sectionId);
    if (!section) return;
    Object.assign(section, updates);
    await this.save();
  }
  async updateSectionConfig(sectionId, updates) {
    const section = this.data.sections.find((item) => item.id === sectionId);
    if (!section) return;
    section.config = { ...section.config, ...updates };
    await this.save();
  }
  async moveSection(sectionId, direction) {
    const section = this.data.sections.find((item) => item.id === sectionId);
    if (!section) return;
    const pageSections = this.data.sections.filter((item) => item.page === section.page && item.enabled).sort((left, right) => left.order - right.order);
    const currentIndex = pageSections.findIndex((item) => item.id === sectionId);
    const targetIndex = direction === "up" ? currentIndex - 1 : currentIndex + 1;
    const target = pageSections[targetIndex];
    if (!target) return;
    const currentOrder = section.order;
    section.order = target.order;
    target.order = currentOrder;
    await this.save();
  }
  async reorderSections(page, orderedIds) {
    const orderMap = new Map(orderedIds.map((id, index) => [id, (index + 1) * 10]));
    this.data.sections.forEach((section) => {
      var _a;
      const order = orderMap.get(section.id);
      if (section.page === page && order !== void 0) {
        section.order = order;
        this.ensureSectionLayout(page, section.id, order);
        const sectionLayout = (_a = this.data.moduleLayouts[page].sections) == null ? void 0 : _a[section.id];
        if (sectionLayout) sectionLayout.order = order;
      }
    });
    await this.save();
  }
  async setOverviewLayout(layout) {
    this.data.userSettings.overviewLayout = layout;
    this.data.moduleLayouts.overview = this.normalizeModuleLayout({ ...this.data.moduleLayouts.overview, mode: layout });
    await this.save();
  }
  async setModuleLayoutMode(page, mode) {
    this.data.moduleLayouts[page] = this.normalizeModuleLayout({
      ...this.getModuleLayout(page),
      mode
    });
    if (page === "overview" && mode !== "custom") {
      this.data.userSettings.overviewLayout = mode;
    }
    await this.save();
  }
  async updateModuleLayout(page, updates) {
    this.data.moduleLayouts[page] = this.normalizeModuleLayout({
      ...this.getModuleLayout(page),
      ...updates
    });
    await this.save();
  }
  async updateSectionLayout(page, sectionId, updates) {
    var _a;
    const section = this.data.sections.find((item) => item.id === sectionId && item.page === page);
    if (!section) return;
    this.ensureSectionLayout(page, sectionId, section.order);
    const layout = this.data.moduleLayouts[page];
    const sectionLayout = (_a = layout.sections) == null ? void 0 : _a[sectionId];
    if (!sectionLayout) return;
    Object.assign(sectionLayout, updates);
    this.data.moduleLayouts[page] = this.normalizeModuleLayout(layout);
    await this.save();
  }
  async moveSectionLayout(page, sectionId, direction) {
    var _a, _b, _c, _d;
    const layout = this.data.moduleLayouts[page];
    const pageSections = this.data.sections.filter((section) => section.page === page).sort((left, right) => this.getSectionLayoutOrder(left, layout) - this.getSectionLayoutOrder(right, layout));
    const currentIndex = pageSections.findIndex((section) => section.id === sectionId);
    const targetIndex = direction === "up" ? currentIndex - 1 : currentIndex + 1;
    const target = pageSections[targetIndex];
    const current = pageSections[currentIndex];
    if (!current || !target) return;
    this.ensureSectionLayout(page, current.id, current.order);
    this.ensureSectionLayout(page, target.id, target.order);
    const currentLayout = (_a = this.data.moduleLayouts[page].sections) == null ? void 0 : _a[current.id];
    const targetLayout = (_b = this.data.moduleLayouts[page].sections) == null ? void 0 : _b[target.id];
    if (!currentLayout || !targetLayout) return;
    const currentOrder = (_c = currentLayout.order) != null ? _c : current.order;
    currentLayout.order = (_d = targetLayout.order) != null ? _d : target.order;
    targetLayout.order = currentOrder;
    await this.save();
  }
  async updateUserSettings(settings) {
    this.data.userSettings = {
      ...this.data.userSettings,
      ...settings
    };
    await this.save();
  }
  async updateBanner(updates) {
    this.data.banner = { ...this.data.banner, ...updates };
    await this.save();
  }
  async updateSidebarAvatar(avatar) {
    this.data.banner.sidebarAvatar = avatar;
    await this.save();
  }
  async updateBannerAvatar(avatar) {
    this.data.banner.bannerAvatar = avatar;
    await this.save();
  }
  async updateBannerMessage(message) {
    this.data.banner.message = message;
    await this.save();
  }
  async updateCalendarSettings(updates) {
    this.data.calendarSettings = { ...this.data.calendarSettings, ...updates };
    this.data.userSettings.weekStartsOn = this.data.calendarSettings.weekStartsOn;
    await this.save();
  }
  getCalendarTodos(date) {
    const todos = [...this.data.calendarTodos].sort((left, right) => left.createdAt.localeCompare(right.createdAt));
    return date ? todos.filter((todo) => todo.date === date) : todos;
  }
  async addCalendarTodo(title, date, category = "\u5F85\u529E") {
    this.data.calendarTodos.push({
      id: `calendar-todo-${Date.now()}`,
      title,
      date,
      completed: false,
      category,
      createdAt: (/* @__PURE__ */ new Date()).toISOString()
    });
    await this.save();
  }
  async updateCalendarTodo(todoId, updates) {
    const todo = this.data.calendarTodos.find((item) => item.id === todoId);
    if (!todo) return;
    Object.assign(todo, updates);
    await this.save();
  }
  async toggleCalendarTodo(todoId) {
    const todo = this.data.calendarTodos.find((item) => item.id === todoId);
    if (!todo) return;
    todo.completed = !todo.completed;
    await this.save();
  }
  async deleteCalendarTodo(todoId) {
    this.data.calendarTodos = this.data.calendarTodos.filter((todo) => todo.id !== todoId);
    await this.save();
  }
  async updateTheme(updates) {
    this.data.theme = { ...this.data.theme, ...updates };
    await this.save();
  }
  async updateApexHabitSettings(updates) {
    this.data.apexHabitSettings = { ...this.data.apexHabitSettings, ...updates };
    if (updates.showOnOverview !== void 0) {
      this.setSectionEnabledInMemory("overview-habit-overview", updates.showOnOverview);
    }
    if (updates.showStreak !== void 0) {
      this.setSectionEnabledInMemory("overview-checkin-streak", updates.showStreak);
    }
    if (updates.showWeeklyProgress !== void 0) {
      this.setSectionEnabledInMemory("overview-weekly-completion", updates.showWeeklyProgress);
    }
    await this.save();
  }
  async addCustomHabit(label) {
    const nextOrder = this.data.apexHabitSettings.customHabits.reduce((max, habit) => Math.max(max, habit.order), 0) + 10;
    const id = `habit-${Date.now()}`;
    this.data.apexHabitSettings.customHabits.push({
      id,
      label,
      enabled: true,
      order: nextOrder
    });
    this.data.checkInDefinitions.push({
      id,
      moduleId: "overview",
      title: label,
      icon: "sparkles",
      color: "#ff8fbc",
      enabled: true,
      order: nextOrder,
      createdAt: Date.now(),
      updatedAt: Date.now()
    });
    await this.save();
  }
  async updateCustomHabit(habitId, updates) {
    const habit = this.data.apexHabitSettings.customHabits.find((item) => item.id === habitId);
    if (!habit) return;
    Object.assign(habit, updates);
    const definition = this.data.checkInDefinitions.find((item) => item.id === habitId || item.id === habitId.replace(/^custom-/, ""));
    if (definition) {
      if (updates.label !== void 0) definition.title = updates.label;
      if (updates.enabled !== void 0) definition.enabled = updates.enabled;
      if (updates.order !== void 0) definition.order = updates.order;
      definition.updatedAt = Date.now();
    }
    await this.save();
  }
  async deleteCustomHabit(habitId) {
    this.data.apexHabitSettings.customHabits = this.data.apexHabitSettings.customHabits.filter((item) => item.id !== habitId);
    const definition = this.data.checkInDefinitions.find((item) => item.id === habitId || item.id === habitId.replace(/^custom-/, ""));
    if (definition) {
      definition.archived = true;
      definition.enabled = false;
      definition.updatedAt = Date.now();
    }
    await this.save();
  }
  async reorderCustomHabits(orderedIds) {
    const orderMap = new Map(orderedIds.map((id, index) => [id, (index + 1) * 10]));
    this.data.apexHabitSettings.customHabits.forEach((habit) => {
      const order = orderMap.get(habit.id);
      if (order !== void 0) habit.order = order;
      const definition = this.data.checkInDefinitions.find((item) => item.id === habit.id || item.id === habit.id.replace(/^custom-/, ""));
      if (definition && order !== void 0) {
        definition.order = order;
        definition.updatedAt = Date.now();
      }
    });
    await this.save();
  }
  async updateQuickAction(actionId, updates) {
    const action = this.data.quickActions.find((item) => item.id === actionId);
    if (!action) return;
    Object.assign(action, updates);
    await this.save();
  }
  async addQuickAction(label, target) {
    const nextOrder = this.data.quickActions.reduce((max, action) => Math.max(max, action.order), 0) + 10;
    this.data.quickActions.push({
      id: `quick-${Date.now()}`,
      label,
      enabled: true,
      order: nextOrder,
      type: "custom",
      target
    });
    await this.save();
  }
  getFocusSettings() {
    return this.data.focusSettings;
  }
  getFocusState() {
    return this.resolveFocusState();
  }
  getFocusRecords() {
    return [...this.data.focusRecords].sort((left, right) => right.createdAt.localeCompare(left.createdAt));
  }
  getTodayFocusRecords() {
    const today6 = formatDateKey(/* @__PURE__ */ new Date());
    return this.getFocusRecords().filter((record) => record.date === today6);
  }
  async updateFocusSettings(updates) {
    var _a, _b;
    this.data.focusSettings = {
      ...this.data.focusSettings,
      ...updates,
      focusDuration: Math.max(1, Math.round((_a = updates.focusDuration) != null ? _a : this.data.focusSettings.focusDuration)),
      breakDuration: Math.max(1, Math.round((_b = updates.breakDuration) != null ? _b : this.data.focusSettings.breakDuration))
    };
    if (!this.data.focusState.isRunning) {
      this.data.focusState.remainingSeconds = this.data.focusSettings.focusDuration * 60;
    }
    await this.save();
  }
  async startFocusSession(task = "", durationMinutes = this.data.focusSettings.focusDuration, background = ((_a) => (_a = this.data.focusSettings.defaultBackground) != null ? _a : "pink")(), backgroundDataUrl) {
    const plannedDuration = Math.max(1, Math.round(durationMinutes));
    this.data.focusSettings.focusDuration = plannedDuration;
    this.data.focusSettings.defaultBackground = background;
    this.data.focusState = {
      isRunning: true,
      isPaused: false,
      mode: "focus",
      startedAt: (/* @__PURE__ */ new Date()).toISOString(),
      remainingSeconds: plannedDuration * 60,
      currentTask: task.trim(),
      background,
      backgroundDataUrl,
      plannedDuration
    };
    await this.save();
  }
  async pauseFocusSession() {
    const state = this.resolveFocusState();
    if (!state.isRunning || state.isPaused) return;
    this.data.focusState = {
      ...state,
      isPaused: true,
      pausedAt: (/* @__PURE__ */ new Date()).toISOString(),
      startedAt: void 0
    };
    await this.save();
  }
  async resumeFocusSession() {
    const state = this.resolveFocusState();
    if (!state.isRunning || !state.isPaused) return;
    this.data.focusState = {
      ...state,
      isPaused: false,
      pausedAt: void 0,
      startedAt: (/* @__PURE__ */ new Date()).toISOString()
    };
    await this.save();
  }
  async endFocusSession(completed = false, saveRecord = true) {
    const state = this.resolveFocusState();
    if (saveRecord && state.mode === "focus") {
      this.pushFocusRecord(state, completed);
    }
    this.data.focusState = {
      isRunning: false,
      isPaused: false,
      mode: "focus",
      remainingSeconds: this.data.focusSettings.focusDuration * 60
    };
    await this.save();
  }
  async updateFocusRecord(recordId, updates) {
    var _a, _b, _c, _d, _e;
    const record = this.data.focusRecords.find((item) => item.id === recordId);
    if (!record) return;
    Object.assign(record, updates);
    record.duration = (_c = (_b = (_a = updates.actualDurationMinutes) != null ? _a : updates.duration) != null ? _b : record.actualDurationMinutes) != null ? _c : record.duration;
    record.actualDurationMinutes = record.duration;
    record.plannedDuration = (_e = (_d = updates.plannedDurationMinutes) != null ? _d : updates.plannedDuration) != null ? _e : record.plannedDuration;
    record.plannedDurationMinutes = record.plannedDuration;
    await this.save();
  }
  async deleteFocusRecord(recordId) {
    this.data.focusRecords = this.data.focusRecords.filter((record) => record.id !== recordId);
    await this.save();
  }
  async completeCurrentFocusPhase() {
    const state = this.resolveFocusState();
    if (state.mode === "focus") {
      this.pushFocusRecord(state, true);
      this.data.focusState = {
        isRunning: this.data.focusSettings.autoStartBreak,
        isPaused: !this.data.focusSettings.autoStartBreak,
        mode: "break",
        startedAt: this.data.focusSettings.autoStartBreak ? (/* @__PURE__ */ new Date()).toISOString() : void 0,
        remainingSeconds: this.data.focusSettings.breakDuration * 60,
        currentTask: state.currentTask,
        background: state.background,
        backgroundDataUrl: state.backgroundDataUrl,
        plannedDuration: this.data.focusSettings.breakDuration
      };
    } else {
      this.data.focusState = {
        isRunning: this.data.focusSettings.autoStartNextFocus,
        isPaused: !this.data.focusSettings.autoStartNextFocus,
        mode: "focus",
        startedAt: this.data.focusSettings.autoStartNextFocus ? (/* @__PURE__ */ new Date()).toISOString() : void 0,
        remainingSeconds: this.data.focusSettings.focusDuration * 60,
        currentTask: state.currentTask,
        background: state.background,
        backgroundDataUrl: state.backgroundDataUrl,
        plannedDuration: this.data.focusSettings.focusDuration
      };
    }
    await this.save();
  }
  getTodayFocusMinutes() {
    return this.getTodayFocusRecords().reduce((sum, record) => sum + record.duration, 0);
  }
  getTodayPomodoroCount() {
    return this.getTodayFocusRecords().filter((record) => record.completed).length;
  }
  pushFocusRecord(state, completed) {
    var _a, _b;
    const plannedDuration = (_a = state.plannedDuration) != null ? _a : this.data.focusSettings.focusDuration;
    const elapsedMinutes = Math.max(0, Math.ceil((plannedDuration * 60 - state.remainingSeconds) / 60));
    const actualDuration = completed ? plannedDuration : elapsedMinutes;
    if (actualDuration <= 0) return;
    const endedAt = (/* @__PURE__ */ new Date()).toISOString();
    this.data.focusRecords.push({
      id: `focus-record-${Date.now()}`,
      date: formatDateKey(/* @__PURE__ */ new Date()),
      task: ((_b = state.currentTask) == null ? void 0 : _b.trim()) || "\u4E13\u6CE8",
      duration: actualDuration,
      actualDurationMinutes: actualDuration,
      completed,
      createdAt: endedAt,
      startedAt: state.startedAt,
      endedAt,
      plannedDuration,
      plannedDurationMinutes: plannedDuration,
      background: state.background,
      backgroundDataUrl: state.backgroundDataUrl
    });
  }
  exportData() {
    return JSON.stringify(this.data, null, 2);
  }
  async importData(data) {
    this.data = this.mergeWithDefaults(data);
    await this.save();
  }
  async resetToDefaults() {
    this.data = structuredClone(DEFAULT_DATA);
    await this.save();
  }
  getTodayFocusTasks() {
    return this.data.todayFocusTasks;
  }
  async toggleTodayFocusTask(taskId) {
    const task = this.data.todayFocusTasks.find((item) => item.id === taskId);
    if (!task) {
      return;
    }
    task.completed = !task.completed;
    await this.save();
  }
  async addTodayFocusTask(label, category = "\u4E2A\u4EBA", date = formatDateKey(/* @__PURE__ */ new Date())) {
    this.data.todayFocusTasks.push({
      id: `focus-${Date.now()}`,
      label,
      category,
      completed: false,
      date
    });
    await this.save();
  }
  getTodayFocusTasksForDate(date = formatDateKey(/* @__PURE__ */ new Date())) {
    return this.data.todayFocusTasks.filter((task) => {
      var _a;
      return ((_a = task.date) != null ? _a : formatDateKey(/* @__PURE__ */ new Date())) === date;
    });
  }
  async updateTodayFocusTask(taskId, updates) {
    const task = this.data.todayFocusTasks.find((item) => item.id === taskId);
    if (!task) return;
    Object.assign(task, updates);
    await this.save();
  }
  async deleteTodayFocusTask(taskId) {
    this.data.todayFocusTasks = this.data.todayFocusTasks.filter((item) => item.id !== taskId);
    await this.save();
  }
  getResearchProjects() {
    return this.data.researchProjects;
  }
  async addResearchProject(project) {
    this.data.researchProjects.push(project);
    await this.save();
  }
  async updateResearchProject(projectId, updates) {
    const project = this.data.researchProjects.find((item) => item.id === projectId);
    if (!project) return;
    Object.assign(project, updates);
    await this.save();
  }
  async deleteResearchProject(projectId) {
    this.data.researchProjects = this.data.researchProjects.filter((item) => item.id !== projectId);
    this.data.researchPapers.forEach((paper) => {
      if (paper.researchProjectId === projectId) paper.researchProjectId = void 0;
    });
    this.data.experimentPlans.forEach((plan) => {
      if (plan.researchProjectId === projectId) plan.researchProjectId = void 0;
    });
    await this.save();
  }
  getResearchPapers() {
    return this.data.researchPapers;
  }
  async addResearchPaper(paper) {
    this.data.researchPapers.push(this.normalizeResearchPaper(paper));
    await this.save();
  }
  async updateResearchPaper(paperId, updates) {
    const paper = this.data.researchPapers.find((item) => item.id === paperId);
    if (!paper) return;
    Object.assign(paper, updates);
    Object.assign(paper, this.normalizeResearchPaper(paper));
    await this.save();
  }
  async deleteResearchPaper(paperId) {
    this.data.researchPapers = this.data.researchPapers.filter((item) => item.id !== paperId);
    this.data.literatureNotes.forEach((note) => {
      if (note.paperReadingId === paperId) note.paperReadingId = void 0;
    });
    await this.save();
  }
  async deletePaperReading(paperId) {
    await this.deleteResearchPaper(paperId);
  }
  getLiteratureNotes() {
    return this.data.literatureNotes.filter((note) => note.notePath);
  }
  getLiteratureNotesForPaper(paperId) {
    return this.getLiteratureNotes().filter((note) => note.paperReadingId === paperId);
  }
  async addLiteratureNote(note) {
    this.data.literatureNotes.push(this.normalizeLiteratureNote(note));
    await this.save();
  }
  async upsertLiteratureNote(note) {
    var _a;
    const existing = (_a = this.data.literatureNotes.find((item) => item.id === note.id)) != null ? _a : this.data.literatureNotes.find((item) => item.paperReadingId === note.paperReadingId && item.notePath === note.notePath);
    if (existing) {
      Object.assign(existing, this.normalizeLiteratureNote({ ...existing, ...note, id: existing.id }));
    } else {
      this.data.literatureNotes.push(this.normalizeLiteratureNote(note));
    }
    await this.save();
  }
  async updateLiteratureNote(noteId, updates) {
    const note = this.data.literatureNotes.find((item) => item.id === noteId);
    if (!note) return;
    Object.assign(note, updates, { updatedAt: Date.now() });
    Object.assign(note, this.normalizeLiteratureNote(note));
    await this.save();
  }
  async deleteLiteratureNote(noteId, options) {
    const note = this.data.literatureNotes.find((item) => item.id === noteId);
    if (note && (options == null ? void 0 : options.clearPaperNotePath)) {
      this.data.researchPapers.forEach((paper) => {
        if (paper.notePath === note.notePath) paper.notePath = void 0;
      });
    }
    this.data.literatureNotes = this.data.literatureNotes.filter((item) => item.id !== noteId);
    await this.save();
  }
  async cleanupInvalidLiteratureNotes(validPaths) {
    const before = this.data.literatureNotes.length;
    this.data.literatureNotes = this.data.literatureNotes.filter((note) => validPaths.has(note.notePath));
    const removed = before - this.data.literatureNotes.length;
    if (removed > 0) await this.save();
    return removed;
  }
  async importZoteroPapers(items) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
    let created = 0;
    let updated = 0;
    let skipped = 0;
    for (const item of items) {
      if (!item.title.trim()) {
        skipped += 1;
        continue;
      }
      const venueId = item.venue ? await this.ensurePaperVenue(item.venue) : void 0;
      const existing = this.findExistingZoteroPaper(item);
      if (existing) {
        Object.assign(existing, this.normalizeResearchPaper({
          ...existing,
          title: item.title,
          venue: (_a = item.venue) != null ? _a : existing.venue,
          venueId: venueId != null ? venueId : existing.venueId,
          year: (_b = item.year) != null ? _b : existing.year,
          paperUrl: existing.paperUrl || item.paperUrl,
          doi: (_c = item.doi) != null ? _c : existing.doi,
          citekey: (_d = item.citekey) != null ? _d : existing.citekey,
          zoteroItemKey: (_e = item.zoteroItemKey) != null ? _e : existing.zoteroItemKey,
          tagIds: (_f = existing.tagIds) != null ? _f : [],
          updatedAt: Date.now()
        }));
        updated += 1;
      } else {
        this.data.researchPapers.push(this.normalizeResearchPaper({
          id: `paper-${Date.now()}-${created}`,
          title: item.title,
          venue: (_g = item.venue) != null ? _g : "",
          venueId,
          year: (_h = item.year) != null ? _h : (/* @__PURE__ */ new Date()).getFullYear(),
          statusId: (_j = (_i = this.data.paperStatuses[0]) == null ? void 0 : _i.id) != null ? _j : "paper-status-unread",
          readingProgress: 0,
          paperUrl: item.paperUrl,
          doi: item.doi,
          readingStartDate: void 0,
          readingEndDate: void 0,
          tagIds: [],
          createdAt: Date.now(),
          updatedAt: Date.now(),
          zoteroItemKey: item.zoteroItemKey,
          citekey: item.citekey
        }));
        created += 1;
      }
    }
    await this.save();
    return { created, updated, skipped };
  }
  getPaperStatuses() {
    return [...this.data.paperStatuses].sort((a, b) => a.order - b.order);
  }
  getPaperVenues() {
    return this.data.paperVenues;
  }
  getPaperTags() {
    return this.data.paperTags;
  }
  async addPaperStatus(status) {
    this.data.paperStatuses.push(status);
    await this.save();
  }
  async updatePaperStatus(statusId, updates) {
    const status = this.data.paperStatuses.find((item) => item.id === statusId);
    if (!status) return;
    Object.assign(status, updates);
    await this.save();
  }
  async deletePaperStatus(statusId, migrateToStatusId) {
    const inUse = this.data.researchPapers.some((paper) => paper.statusId === statusId);
    if (inUse && !migrateToStatusId) return false;
    if (inUse) {
      this.data.researchPapers.forEach((paper) => {
        if (paper.statusId === statusId) paper.statusId = migrateToStatusId;
      });
    }
    this.data.paperStatuses = this.data.paperStatuses.filter((item) => item.id !== statusId);
    await this.save();
    return true;
  }
  async addPaperVenue(venue) {
    this.data.paperVenues.push(venue);
    await this.save();
  }
  async updatePaperVenue(venueId, updates) {
    const venue = this.data.paperVenues.find((item) => item.id === venueId);
    if (!venue) return;
    Object.assign(venue, updates);
    await this.save();
  }
  async deletePaperVenue(venueId, migrateToVenueId) {
    const inUse = this.data.researchPapers.some((paper) => paper.venueId === venueId);
    if (inUse && !migrateToVenueId) return false;
    if (inUse) {
      this.data.researchPapers.forEach((paper) => {
        if (paper.venueId === venueId) paper.venueId = migrateToVenueId;
      });
    }
    this.data.paperVenues = this.data.paperVenues.filter((item) => item.id !== venueId);
    await this.save();
    return true;
  }
  async addPaperTag(tag) {
    this.data.paperTags.push(tag);
    await this.save();
  }
  async updatePaperTag(tagId, updates) {
    const tag = this.data.paperTags.find((item) => item.id === tagId);
    if (!tag) return;
    Object.assign(tag, updates);
    await this.save();
  }
  async deletePaperTag(tagId) {
    this.data.paperTags = this.data.paperTags.filter((item) => item.id !== tagId);
    this.data.researchPapers.forEach((paper) => {
      var _a;
      paper.tagIds = ((_a = paper.tagIds) != null ? _a : []).filter((id) => id !== tagId);
    });
    await this.save();
  }
  getExperimentPlans() {
    return this.data.experimentPlans;
  }
  getExperimentRecords() {
    return this.data.experimentRecords;
  }
  async addExperiment(mode, experiment) {
    const items = mode === "plan" ? this.data.experimentPlans : this.data.experimentRecords;
    items.push(experiment);
    await this.save();
  }
  async updateExperiment(mode, experimentId, updates) {
    const items = mode === "plan" ? this.data.experimentPlans : this.data.experimentRecords;
    const experiment = items.find((item) => item.id === experimentId);
    if (!experiment) return;
    Object.assign(experiment, updates);
    await this.save();
  }
  async deleteExperiment(mode, experimentId) {
    if (mode === "plan") {
      this.data.experimentPlans = this.data.experimentPlans.filter((item) => item.id !== experimentId);
      this.data.experimentRecords.forEach((record) => {
        if (record.experimentPlanId === experimentId) record.experimentPlanId = void 0;
      });
    } else {
      this.data.experimentRecords = this.data.experimentRecords.filter((item) => item.id !== experimentId);
    }
    await this.save();
  }
  async migrateExperimentRecordsToPlan(fromPlanId, toPlanId) {
    this.data.experimentRecords.forEach((record) => {
      if (record.experimentPlanId === fromPlanId) record.experimentPlanId = toPlanId;
    });
    await this.save();
  }
  getResearchDeadlines() {
    return this.data.researchDeadlines;
  }
  async addResearchDeadline(deadline) {
    this.data.researchDeadlines.push(deadline);
    await this.save();
  }
  async updateResearchDeadline(deadlineId, updates) {
    const deadline = this.data.researchDeadlines.find((item) => item.id === deadlineId);
    if (!deadline) return;
    Object.assign(deadline, updates);
    await this.save();
  }
  async deleteResearchDeadline(deadlineId) {
    this.data.researchDeadlines = this.data.researchDeadlines.filter((item) => item.id !== deadlineId);
    await this.save();
  }
  getResearchMemos() {
    return this.data.researchMemos;
  }
  async addResearchMemo(memo) {
    this.data.researchMemos.push(memo);
    await this.save();
  }
  async updateResearchMemo(index, memo) {
    if (!this.data.researchMemos[index]) return;
    this.data.researchMemos[index] = memo;
    await this.save();
  }
  async deleteResearchMemo(index) {
    this.data.researchMemos.splice(index, 1);
    await this.save();
  }
  getDataAnalysisTasks() {
    return this.data.dataAnalysisTasks;
  }
  async addDataAnalysisTask(task) {
    this.data.dataAnalysisTasks.push(task);
    await this.save();
  }
  async updateDataAnalysisTask(taskId, updates) {
    const task = this.data.dataAnalysisTasks.find((item) => item.id === taskId);
    if (!task) return;
    Object.assign(task, updates);
    await this.save();
  }
  async deleteDataAnalysisTask(taskId) {
    this.data.dataAnalysisTasks = this.data.dataAnalysisTasks.filter((item) => item.id !== taskId);
    await this.save();
  }
  getBooks() {
    return this.data.books;
  }
  getReadingQuotes() {
    return this.data.readingQuotes;
  }
  getWantToReadItems() {
    return this.data.wantToReadItems;
  }
  getBookTags() {
    return this.data.bookTags;
  }
  getReadingPlans() {
    return this.data.readingPlans.map((plan) => this.withComputedReadingPlanStatus(plan));
  }
  getReadingNotes() {
    return this.data.readingNotes;
  }
  getReadingNotesForBook(bookId) {
    return this.data.readingNotes.filter((note) => note.bookId === bookId);
  }
  async addReadingNote(note) {
    this.data.readingNotes.push(this.normalizeReadingNote(note));
    await this.save();
  }
  async updateReadingNote(noteId, updates) {
    const note = this.data.readingNotes.find((item) => item.id === noteId);
    if (!note) return;
    Object.assign(note, updates, { updatedAt: (/* @__PURE__ */ new Date()).toISOString() });
    Object.assign(note, this.normalizeReadingNote(note));
    await this.save();
  }
  async deleteReadingNote(noteId) {
    this.data.readingNotes = this.data.readingNotes.filter((item) => item.id !== noteId);
    await this.save();
  }
  async addReadingPlan(plan) {
    const normalized = this.normalizeReadingPlan(plan);
    this.data.readingPlans.push(normalized);
    await this.save();
  }
  async updateReadingPlan(planId, updates) {
    const plan = this.data.readingPlans.find((item) => item.id === planId);
    if (!plan) return;
    Object.assign(plan, updates, { updatedAt: (/* @__PURE__ */ new Date()).toISOString() });
    Object.assign(plan, this.normalizeReadingPlan(plan));
    await this.save();
  }
  async deleteReadingPlan(planId) {
    this.data.readingPlans = this.data.readingPlans.filter((item) => item.id !== planId);
    await this.save();
  }
  async completeReadingPlan(planId) {
    const plan = this.data.readingPlans.find((item) => item.id === planId);
    if (!plan) return;
    const now = (/* @__PURE__ */ new Date()).toISOString();
    Object.assign(plan, { status: "completed", progress: 100, completedDate: formatDateKey(/* @__PURE__ */ new Date()), completedAt: now, updatedAt: now });
    await this.save();
  }
  shouldShowActiveReadingPlan(plan, dateKey = formatDateKey(/* @__PURE__ */ new Date())) {
    var _a, _b;
    const completedDate = (_b = plan.completedDate) != null ? _b : (_a = plan.completedAt) == null ? void 0 : _a.slice(0, 10);
    return !completedDate || completedDate >= dateKey;
  }
  async addReadingQuote(quote) {
    this.data.readingQuotes.push(this.normalizeReadingQuote(quote));
    await this.save();
  }
  async updateReadingQuote(quoteId, updates) {
    const quote = this.data.readingQuotes.find((item) => item.id === quoteId);
    if (!quote) return;
    Object.assign(quote, updates);
    Object.assign(quote, this.normalizeReadingQuote(quote));
    await this.save();
  }
  async deleteReadingQuote(quoteId) {
    this.data.readingQuotes = this.data.readingQuotes.filter((item) => item.id !== quoteId);
    await this.save();
  }
  async addWantToReadItem(item) {
    this.data.wantToReadItems.push(this.normalizeWantToReadItem(item));
    await this.save();
  }
  async updateWantToReadItem(itemId, updates) {
    const item = this.data.wantToReadItems.find((entry) => entry.id === itemId);
    if (!item) return;
    Object.assign(item, updates, { updatedAt: (/* @__PURE__ */ new Date()).toISOString() });
    Object.assign(item, this.normalizeWantToReadItem(item));
    await this.save();
  }
  async deleteWantToReadItem(itemId) {
    this.data.wantToReadItems = this.data.wantToReadItems.filter((item) => item.id !== itemId);
    await this.save();
  }
  async addBookTag(tag) {
    this.data.bookTags.push(this.normalizeBookTag(tag, this.data.bookTags.length));
    await this.save();
  }
  async updateBookTag(tagId, updates) {
    const tag = this.data.bookTags.find((item) => item.id === tagId);
    if (!tag) return;
    Object.assign(tag, updates);
    Object.assign(tag, this.normalizeBookTag(tag, this.data.bookTags.indexOf(tag)));
    this.syncLegacyBookTags();
    await this.save();
  }
  async deleteBookTag(tagId) {
    this.data.bookTags = this.data.bookTags.filter((tag) => tag.id !== tagId);
    this.data.books.forEach((book) => {
      var _a;
      book.tagIds = ((_a = book.tagIds) != null ? _a : []).filter((id) => id !== tagId);
    });
    this.syncLegacyBookTags();
    await this.save();
  }
  async addBook(book) {
    this.data.books.push(this.normalizeBook(book));
    await this.save();
  }
  async updateBook(bookId, updates) {
    const book = this.data.books.find((item) => item.id === bookId);
    if (!book) return;
    Object.assign(book, updates);
    Object.assign(book, this.normalizeBook(book));
    await this.save();
  }
  async deleteBook(bookId) {
    this.data.books = this.data.books.filter((item) => item.id !== bookId);
    await this.save();
  }
  async updateBookPage(bookId, currentPage) {
    const book = this.data.books.find((item) => item.id === bookId);
    if (!book) {
      return;
    }
    book.currentPage = Math.max(0, Math.min(currentPage, book.totalPages));
    if (book.currentPage > 0 && book.readingStatus === "want-to-read") {
      book.readingStatus = "reading";
      book.status = "\u5728\u8BFB";
      book.startDate = book.startDate || formatDateKey(/* @__PURE__ */ new Date());
    }
    await this.save();
  }
  async completeBook(bookId) {
    const book = this.data.books.find((item) => item.id === bookId);
    if (!book) {
      return;
    }
    book.status = "\u5DF2\u8BFB";
    book.readingStatus = "finished";
    book.currentPage = book.totalPages;
    book.finishDate = formatDateKey(/* @__PURE__ */ new Date());
    book.startDate = book.startDate || book.finishDate;
    await this.save();
  }
  async updateBookReadingStatus(bookId, readingStatus) {
    const book = this.data.books.find((item) => item.id === bookId);
    if (!book || !readingStatus) return;
    book.readingStatus = readingStatus;
    book.status = this.legacyBookStatus(readingStatus);
    if (readingStatus === "reading") book.startDate = book.startDate || formatDateKey(/* @__PURE__ */ new Date());
    if (readingStatus === "finished") {
      book.currentPage = book.totalPages;
      book.startDate = book.startDate || formatDateKey(/* @__PURE__ */ new Date());
      book.finishDate = book.finishDate || formatDateKey(/* @__PURE__ */ new Date());
    }
    await this.save();
  }
  async updateBookShelfStatus(bookId, shelfStatus) {
    const book = this.data.books.find((item) => item.id === bookId);
    if (!book || !shelfStatus) return;
    book.shelfStatus = shelfStatus;
    await this.save();
  }
  getWorkouts() {
    return this.data.workouts.map((workout) => this.normalizeWorkout(workout));
  }
  async addWorkout(workout) {
    this.data.workouts.push(this.normalizeWorkout(workout));
    this.data.workouts.sort((left, right) => right.date.localeCompare(left.date));
    await this.save();
  }
  async updateWorkout(workoutId, updates) {
    const workout = this.data.workouts.find((item) => item.id === workoutId);
    if (!workout) return;
    Object.assign(workout, updates, { updatedAt: nowIso() });
    Object.assign(workout, this.normalizeWorkout(workout));
    this.data.workouts.sort((left, right) => right.date.localeCompare(left.date));
    await this.save();
  }
  async deleteWorkout(workoutId) {
    this.data.workouts = this.data.workouts.filter((item) => item.id !== workoutId);
    await this.save();
  }
  getTrainingPlans() {
    return this.data.trainingPlans.map((plan) => this.withComputedTrainingPlanStatus(plan));
  }
  async addTrainingPlan(plan) {
    this.data.trainingPlans.push(this.normalizeTrainingPlan(plan));
    await this.save();
  }
  async updateTrainingPlan(planId, updates) {
    const plan = this.data.trainingPlans.find((item) => item.id === planId);
    if (!plan) return;
    Object.assign(plan, updates, { updatedAt: nowIso() });
    Object.assign(plan, this.normalizeTrainingPlan(plan));
    await this.save();
  }
  async completeTrainingPlan(planId) {
    const plan = this.data.trainingPlans.find((item) => item.id === planId);
    if (!plan) return;
    Object.assign(plan, { status: "completed", completedDate: todayKey(), updatedAt: nowIso() });
    await this.save();
  }
  async deleteTrainingPlan(planId) {
    this.data.trainingPlans = this.data.trainingPlans.filter((item) => item.id !== planId);
    this.data.workouts.forEach((workout) => {
      if (workout.trainingPlanId === planId) workout.trainingPlanId = void 0;
    });
    await this.save();
  }
  getBodyMeasurements() {
    return this.data.bodyMeasurements;
  }
  async addBodyMeasurement(measurement) {
    var _a, _b;
    const timestamp = nowIso();
    this.data.bodyMeasurements.push({
      ...measurement,
      id: (_a = measurement.id) != null ? _a : `measure-${Date.now()}`,
      createdAt: (_b = measurement.createdAt) != null ? _b : timestamp,
      updatedAt: timestamp
    });
    this.data.bodyMeasurements.sort((left, right) => left.date.localeCompare(right.date));
    await this.save();
  }
  async updateBodyMeasurement(measurementId, updates) {
    const measurement = this.data.bodyMeasurements.find((item) => {
      var _a;
      return ((_a = item.id) != null ? _a : item.date) === measurementId;
    });
    if (!measurement) return;
    Object.assign(measurement, updates, { updatedAt: nowIso() });
    await this.save();
  }
  async upsertBodyMeasurementForDate(measurement) {
    var _a;
    const existing = this.data.bodyMeasurements.find((item) => item.date === measurement.date);
    if (existing) {
      await this.updateBodyMeasurement((_a = existing.id) != null ? _a : existing.date, measurement);
      return;
    }
    await this.addBodyMeasurement(measurement);
  }
  async deleteBodyMeasurement(measurementId) {
    this.data.bodyMeasurements = this.data.bodyMeasurements.filter((item) => {
      var _a;
      return ((_a = item.id) != null ? _a : item.date) !== measurementId;
    });
    await this.save();
  }
  getFitnessDailyRecord(date = formatDateKey(/* @__PURE__ */ new Date())) {
    const record = this.data.fitnessDailyRecords.find((item) => item.date === date);
    return record != null ? record : { date, waterCups: 0, waterGoal: 8, waterNote: "", sleepHours: 0, sleepGoal: 8, bedtime: "", wakeTime: "", sleepNote: "" };
  }
  async updateFitnessDailyRecord(date, updates) {
    let record = this.data.fitnessDailyRecords.find((item) => item.date === date);
    if (!record) {
      record = this.getFitnessDailyRecord(date);
      this.data.fitnessDailyRecords.push(record);
    }
    Object.assign(record, updates, { date, updatedAt: nowIso() });
    await this.save();
  }
  getFitnessHabitDefinitions() {
    return [...this.data.fitnessHabitDefinitions].sort((left, right) => left.order - right.order);
  }
  getFitnessHabitRecords(date) {
    return date ? this.data.fitnessHabitRecords.filter((item) => item.date === date) : this.data.fitnessHabitRecords;
  }
  async addFitnessHabitDefinition(definition) {
    const timestamp = nowIso();
    const maxOrder = Math.max(0, ...this.data.fitnessHabitDefinitions.map((item) => item.order));
    this.data.fitnessHabitDefinitions.push({
      ...definition,
      id: `fitness-habit-${Date.now()}`,
      order: maxOrder + 10,
      createdAt: timestamp,
      updatedAt: timestamp
    });
    await this.save();
  }
  async updateFitnessHabitDefinition(definitionId, updates) {
    const definition = this.data.fitnessHabitDefinitions.find((item) => item.id === definitionId);
    if (!definition) return;
    Object.assign(definition, updates, { updatedAt: nowIso() });
    await this.save();
  }
  async deleteFitnessHabitDefinition(definitionId) {
    this.data.fitnessHabitDefinitions = this.data.fitnessHabitDefinitions.filter((item) => item.id !== definitionId);
    this.data.fitnessHabitRecords = this.data.fitnessHabitRecords.filter((item) => item.habitId !== definitionId);
    await this.save();
  }
  async moveFitnessHabitDefinition(definitionId, direction) {
    const definitions = this.getFitnessHabitDefinitions();
    const index = definitions.findIndex((item) => item.id === definitionId);
    const nextIndex = index + direction;
    if (index < 0 || nextIndex < 0 || nextIndex >= definitions.length) return;
    const currentOrder = definitions[index].order;
    definitions[index].order = definitions[nextIndex].order;
    definitions[nextIndex].order = currentOrder;
    await this.save();
  }
  async updateFitnessHabitRecord(date, habitId, updates) {
    let record = this.data.fitnessHabitRecords.find((item) => item.date === date && item.habitId === habitId);
    if (!record) {
      record = { date, habitId, actualValue: 0, note: "", updatedAt: nowIso() };
      this.data.fitnessHabitRecords.push(record);
    }
    Object.assign(record, updates, { date, habitId, updatedAt: nowIso() });
    await this.save();
  }
  getFitnessGoals() {
    return this.data.fitnessGoals;
  }
  async addFitnessGoal(goal) {
    var _a, _b, _c, _d, _e, _f, _g;
    const timestamp = nowIso();
    this.data.fitnessGoals.push({
      ...goal,
      currentValue: (_b = (_a = goal.currentValue) != null ? _a : goal.current) != null ? _b : 0,
      targetValue: (_d = (_c = goal.targetValue) != null ? _c : goal.target) != null ? _d : 0,
      startDate: (_e = goal.startDate) != null ? _e : todayKey(),
      status: (_f = goal.status) != null ? _f : "active",
      createdAt: (_g = goal.createdAt) != null ? _g : timestamp,
      updatedAt: timestamp
    });
    await this.save();
  }
  async updateFitnessGoal(goalId, updates) {
    const goal = this.data.fitnessGoals.find((item) => item.id === goalId);
    if (!goal) return;
    Object.assign(goal, updates, { updatedAt: nowIso() });
    await this.save();
  }
  async completeFitnessGoal(goalId) {
    var _a, _b, _c, _d;
    const goal = this.data.fitnessGoals.find((item) => item.id === goalId);
    if (!goal) return;
    Object.assign(goal, {
      status: "completed",
      completedDate: todayKey(),
      currentValue: (_d = (_c = (_b = (_a = goal.targetValue) != null ? _a : goal.target) != null ? _b : goal.currentValue) != null ? _c : goal.current) != null ? _d : 0,
      updatedAt: nowIso()
    });
    await this.save();
  }
  async deleteFitnessGoal(goalId) {
    this.data.fitnessGoals = this.data.fitnessGoals.filter((item) => item.id !== goalId);
    this.data.trainingPlans.forEach((plan) => {
      if (plan.fitnessGoalId === goalId) plan.fitnessGoalId = void 0;
    });
    this.data.workouts.forEach((workout) => {
      if (workout.fitnessGoalId === goalId) workout.fitnessGoalId = void 0;
    });
    await this.save();
  }
  getHealthReminders() {
    return this.data.healthReminders;
  }
  async addHealthReminder(reminder) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    const timestamp = nowIso();
    const reminderData = typeof reminder === "string" ? { title: reminder } : reminder;
    this.data.healthReminders.push({
      id: (_a = reminderData.id) != null ? _a : `health-${Date.now()}`,
      title: reminderData.title,
      date: (_b = reminderData.date) != null ? _b : todayKey(),
      time: (_c = reminderData.time) != null ? _c : "09:00",
      repeatType: (_d = reminderData.repeatType) != null ? _d : "once",
      repeatDays: (_e = reminderData.repeatDays) != null ? _e : [],
      note: (_f = reminderData.note) != null ? _f : "",
      enabled: (_g = reminderData.enabled) != null ? _g : true,
      createdAt: (_h = reminderData.createdAt) != null ? _h : timestamp,
      updatedAt: timestamp
    });
    await this.save();
  }
  async updateHealthReminder(reminderId, updates) {
    const reminder = this.data.healthReminders.find((item) => item.id === reminderId);
    if (!reminder) return;
    if (typeof updates === "string") {
      reminder.title = updates;
    } else {
      Object.assign(reminder, updates);
    }
    reminder.updatedAt = nowIso();
    await this.save();
  }
  async deleteHealthReminder(reminderId) {
    this.data.healthReminders = this.data.healthReminders.filter((item) => item.id !== reminderId);
    await this.save();
  }
  getHealthReminderLogs() {
    return this.data.healthReminderLogs;
  }
  async addHealthReminderLog(log) {
    if (this.data.healthReminderLogs.some((item) => item.id === log.id)) return;
    this.data.healthReminderLogs.push(log);
    await this.save();
  }
  async updateHealthReminderLog(logId, updates) {
    const log = this.data.healthReminderLogs.find((item) => item.id === logId);
    if (!log) return;
    Object.assign(log, updates);
    await this.save();
  }
  getTransactions() {
    return this.data.transactions.filter((transaction) => !this.isBudgetAdjustmentTransaction(transaction));
  }
  async updateTransaction(transactionId, updates) {
    const transaction = this.data.transactions.find((item) => item.id === transactionId);
    if (!transaction) return;
    Object.assign(transaction, updates);
    this.recalculateBudgetSpent();
    await this.save();
  }
  async deleteTransaction(transactionId) {
    this.data.transactions = this.data.transactions.filter((item) => item.id !== transactionId);
    this.recalculateBudgetSpent();
    await this.save();
  }
  getBudgets() {
    return this.data.budgets.filter((budget) => budget.role !== "monthly-limit");
  }
  getMonthlyBudgetLimit(period = currentMonthKey()) {
    const monthly = this.data.budgets.find((budget) => budget.role === "monthly-limit" && budget.period === period);
    if (monthly) return monthly.amount;
    return this.getBudgets().reduce((sum, budget) => sum + budget.amount, 0);
  }
  async setMonthlyBudgetLimit(amount, period = currentMonthKey()) {
    let budget = this.data.budgets.find((item) => item.role === "monthly-limit" && item.period === period);
    if (!budget) {
      budget = { id: `budget-limit-${period}`, category: "\u6708\u9884\u7B97", amount: 0, spent: 0, role: "monthly-limit", period };
      this.data.budgets.unshift(budget);
    }
    budget.amount = Math.max(0, amount);
    await this.save();
  }
  async addBudget(budget) {
    this.data.budgets.push(budget);
    this.recalculateBudgetSpent();
    await this.save();
  }
  async updateBudget(budgetId, updates) {
    const budget = this.data.budgets.find((item) => item.id === budgetId);
    if (!budget) return;
    Object.assign(budget, updates);
    this.recalculateBudgetSpent();
    await this.save();
  }
  async deleteBudget(budgetId) {
    const budget = this.data.budgets.find((item) => item.id === budgetId);
    if (!budget) return false;
    if (this.data.transactions.some((transaction) => transaction.category === budget.category)) {
      return false;
    }
    this.data.budgets = this.data.budgets.filter((item) => item.id !== budgetId);
    await this.save();
    return true;
  }
  getAccounts() {
    return this.data.accounts;
  }
  getTotalAssets() {
    return this.data.accounts.reduce((sum, account) => sum + account.balance, 0);
  }
  async addAccount(account) {
    this.data.accounts.push(this.normalizeAccount(account));
    await this.save();
  }
  async updateAccount(accountId, updates) {
    const account = this.data.accounts.find((item) => item.id === accountId);
    if (!account) return;
    Object.assign(account, updates);
    Object.assign(account, this.normalizeAccount(account));
    await this.save();
  }
  async deleteAccount(accountId) {
    this.data.accounts = this.data.accounts.filter((item) => item.id !== accountId);
    this.data.transactions.forEach((transaction) => {
      if (transaction.accountId === accountId) transaction.accountId = void 0;
    });
    await this.save();
  }
  getInvestmentWatchItems() {
    return this.data.investmentWatchItems;
  }
  async addInvestmentWatchItem(item) {
    var _a;
    this.data.investmentWatchItems.push(item);
    await this.addInvestmentSnapshot({
      id: `snapshot-${item.id}-${Date.now()}`,
      investmentId: item.id,
      date: todayKey(),
      price: item.price,
      changePercent: item.changePercent,
      note: (_a = item.note) != null ? _a : "",
      createdAt: nowIso()
    }, false);
    await this.save();
  }
  async updateInvestmentWatchItem(itemId, updates) {
    var _a;
    const item = this.data.investmentWatchItems.find((entry) => entry.id === itemId);
    if (!item) return;
    Object.assign(item, updates);
    await this.addInvestmentSnapshot({
      id: `snapshot-${item.id}-${Date.now()}`,
      investmentId: item.id,
      date: todayKey(),
      price: item.price,
      changePercent: item.changePercent,
      note: (_a = item.note) != null ? _a : "",
      createdAt: nowIso()
    }, false);
    await this.save();
  }
  async deleteInvestmentWatchItem(itemId) {
    this.data.investmentWatchItems = this.data.investmentWatchItems.filter((item) => item.id !== itemId);
    await this.save();
  }
  getInvestmentSnapshots() {
    return this.data.investmentSnapshots;
  }
  async addInvestmentSnapshot(snapshot, save = true) {
    this.data.investmentSnapshots.push(snapshot);
    if (save) {
      await this.save();
    }
  }
  getSavingGoals() {
    return this.data.savingGoals;
  }
  async addSavingGoal(goal) {
    this.data.savingGoals.push(goal);
    await this.save();
  }
  async updateSavingGoal(goalId, updates) {
    const goal = this.data.savingGoals.find((item) => item.id === goalId);
    if (!goal) return;
    Object.assign(goal, updates);
    await this.save();
  }
  async deleteSavingGoal(goalId) {
    this.data.savingGoals = this.data.savingGoals.filter((item) => item.id !== goalId);
    await this.save();
  }
  getBills() {
    return this.data.bills;
  }
  async addBill(bill) {
    this.data.bills.push(bill);
    await this.save();
  }
  async updateBill(billId, updates) {
    const bill = this.data.bills.find((item) => item.id === billId);
    if (!bill) return;
    Object.assign(bill, updates);
    await this.save();
  }
  async deleteBill(billId) {
    this.data.bills = this.data.bills.filter((item) => item.id !== billId);
    await this.save();
  }
  getFinanceTodos() {
    return this.data.financeTodos;
  }
  async addFinanceTodo(todo) {
    var _a, _b, _c, _d, _e;
    const timestamp = nowIso();
    const values = typeof todo === "string" ? { title: todo } : todo;
    this.data.financeTodos.push({
      id: (_a = values.id) != null ? _a : `finance-todo-${Date.now()}`,
      title: values.title,
      completed: (_b = values.completed) != null ? _b : false,
      date: (_c = values.date) != null ? _c : todayKey(),
      note: (_d = values.note) != null ? _d : "",
      createdAt: (_e = values.createdAt) != null ? _e : timestamp,
      updatedAt: timestamp
    });
    await this.save();
  }
  async updateFinanceTodo(todoId, updates) {
    const todo = this.data.financeTodos.find((item) => item.id === todoId);
    if (!todo) return;
    Object.assign(todo, updates, { updatedAt: nowIso() });
    await this.save();
  }
  async deleteFinanceTodo(todoId) {
    this.data.financeTodos = this.data.financeTodos.filter((item) => item.id !== todoId);
    await this.save();
  }
  getTasks() {
    this.generateDueRecurringTasks(false);
    return this.data.tasks.map((task) => this.normalizeTask(task));
  }
  async addTask(task) {
    var _a;
    const normalized = this.normalizeTask({ ...task, id: (_a = task.id) != null ? _a : `task-${Date.now()}` });
    this.data.tasks.push(normalized);
    await this.save();
    return normalized;
  }
  async updateTask(taskId, updates) {
    var _a;
    const task = this.data.tasks.find((item) => item.id === taskId);
    if (!task) return;
    const nextStatus = updates.status;
    Object.assign(task, updates, { updatedAt: nowIso() });
    if (nextStatus === "done") task.completedDate = (_a = task.completedDate) != null ? _a : todayKey();
    if (nextStatus && nextStatus !== "done") task.completedDate = void 0;
    Object.assign(task, this.normalizeTask(task));
    await this.save();
  }
  async deleteTask(taskId, mode = "keep-children") {
    const childIds = this.data.tasks.filter((task) => task.parentTaskId === taskId).map((task) => task.id);
    this.data.tasks = this.data.tasks.filter((task) => task.id !== taskId && (mode === "delete-children" ? !childIds.includes(task.id) : true));
    if (mode === "keep-children") {
      this.data.tasks.forEach((task) => {
        if (task.parentTaskId === taskId) task.parentTaskId = void 0;
      });
    }
    await this.save();
  }
  getTaskProjects() {
    return this.data.taskProjects.map((project) => this.normalizeTaskProject(project));
  }
  async addTaskProject(project) {
    var _a;
    const normalized = this.normalizeTaskProject({ ...project, id: (_a = project.id) != null ? _a : `task-project-${Date.now()}` });
    this.data.taskProjects.push(normalized);
    await this.save();
    return normalized;
  }
  async updateTaskProject(projectId, updates) {
    const project = this.data.taskProjects.find((item) => item.id === projectId);
    if (!project) return;
    Object.assign(project, updates);
    Object.assign(project, this.normalizeTaskProject(project));
    await this.save();
  }
  async deleteTaskProject(projectId) {
    this.data.taskProjects = this.data.taskProjects.filter((project) => project.id !== projectId);
    this.data.tasks.forEach((task) => {
      if (task.projectId === projectId) task.projectId = void 0;
    });
    await this.save();
  }
  async updateTaskSettings(updates) {
    this.data.taskSettings = { ...this.data.taskSettings, ...updates };
    await this.save();
  }
  getTaskSettings() {
    return { ...this.data.taskSettings };
  }
  async generateDueRecurringTasks(save = true) {
    const today6 = todayKey();
    let changed = false;
    this.data.tasks.forEach((task) => {
      var _a, _b;
      if (!((_a = task.recurrence) == null ? void 0 : _a.enabled) || task.recurrence.frequency === "none") return;
      const nextDate = (_b = task.recurrence.nextDate) != null ? _b : task.plannedDate;
      if (!nextDate || nextDate > today6 || task.recurrence.lastGeneratedDate === nextDate) return;
      const generatedId = `task-rec-${task.id}-${nextDate}`;
      if (!this.data.tasks.some((item) => item.id === generatedId)) {
        this.data.tasks.push(this.normalizeTask({
          ...task,
          id: generatedId,
          status: "todo",
          plannedDate: nextDate,
          completedDate: void 0,
          parentTaskId: void 0,
          recurrence: { frequency: "none", enabled: false },
          createdAt: nowIso(),
          updatedAt: nowIso()
        }));
      }
      task.recurrence.lastGeneratedDate = nextDate;
      task.recurrence.nextDate = this.nextRecurrenceDate(nextDate, task.recurrence.frequency, task.recurrence.interval);
      task.updatedAt = nowIso();
      changed = true;
    });
    if (changed && save) await this.save();
  }
  getGoals() {
    return this.data.goals;
  }
  getObjectives() {
    return this.data.objectives;
  }
  async addObjective(objective) {
    this.data.objectives.push(this.normalizeObjective(objective));
    await this.save();
  }
  async updateObjective(objectiveId, updates) {
    var _a;
    const objective = this.data.objectives.find((item) => item.id === objectiveId);
    if (!objective) return;
    Object.assign(objective, updates, { updatedAt: Date.now() });
    const period = this.parseObjectivePeriod(objective);
    objective.year = period.year;
    objective.quarterNumber = period.quarterNumber;
    objective.quarter = `${period.year} Q${period.quarterNumber}`;
    objective.progress = Math.max(0, Math.min(100, objective.progress));
    if (objective.progress >= 100) {
      objective.completedDate = (_a = objective.completedDate) != null ? _a : formatDateKey(/* @__PURE__ */ new Date());
    } else {
      objective.completedDate = void 0;
    }
    await this.save();
  }
  async deleteObjective(objectiveId) {
    this.data.objectives = this.data.objectives.filter((item) => item.id !== objectiveId);
    this.data.keyResults = this.data.keyResults.filter((item) => item.objectiveId !== objectiveId);
    await this.save();
  }
  getKeyResults() {
    return this.data.keyResults;
  }
  getMilestones() {
    return this.data.milestones;
  }
  getGoalActions() {
    return this.data.goalActions.map((action) => this.withGoalActionComputedState(action));
  }
  getGoalActionsForGoal(goalId) {
    return this.getGoalActions().filter((action) => action.goalId === goalId);
  }
  getRisks() {
    return this.data.risks;
  }
  getReviewItems() {
    return this.data.reviewItems;
  }
  async addReviewItem(item) {
    this.data.reviewItems.push(this.normalizeReviewItem(item));
    await this.save();
  }
  async updateReviewItem(itemId, updates) {
    const item = this.data.reviewItems.find((review) => review.id === itemId);
    if (!item) return;
    Object.assign(item, updates, { updatedAt: (/* @__PURE__ */ new Date()).toISOString() });
    Object.assign(item, this.normalizeReviewItem(item));
    await this.save();
  }
  async deleteReviewItem(itemId) {
    this.data.reviewItems = this.data.reviewItems.filter((item) => item.id !== itemId);
    await this.save();
  }
  async addGoal(goal) {
    this.data.goals.push(goal);
    await this.save();
  }
  async deleteGoal(goalId) {
    this.data.goals = this.data.goals.filter((item) => item.id !== goalId);
    this.data.milestones = this.data.milestones.filter((item) => item.goalId !== goalId);
    this.data.goalActions = this.data.goalActions.filter((item) => item.goalId !== goalId);
    await this.save();
  }
  async updateGoal(goalId, updates) {
    var _a;
    const goal = this.data.goals.find((item) => item.id === goalId);
    if (!goal) {
      return;
    }
    Object.assign(goal, updates);
    goal.progress = Math.max(0, Math.min(100, goal.progress));
    if (updates.progress !== void 0 && goal.progress < 100 && updates.status === void 0) {
      goal.status = "\u8FDB\u884C\u4E2D";
      goal.completedDate = void 0;
    } else if (goal.progress >= 100 || goal.status === "\u5DF2\u5B8C\u6210") {
      goal.status = "\u5DF2\u5B8C\u6210";
      goal.progress = 100;
      goal.completedDate = (_a = goal.completedDate) != null ? _a : formatDateKey(/* @__PURE__ */ new Date());
    } else {
      goal.completedDate = void 0;
    }
    goal.updatedAt = Date.now();
    await this.save();
  }
  async updateGoalProgress(goalId, progress) {
    await this.updateGoal(goalId, { progress });
  }
  async addGoalAction(action) {
    const normalized = this.normalizeGoalAction(action);
    this.data.goalActions.push(normalized);
    this.syncGoalActionWithAncestors(normalized);
    await this.save();
  }
  async updateGoalAction(actionId, updates) {
    var _a;
    const action = this.data.goalActions.find((item) => item.id === actionId);
    if (!action) return;
    Object.assign(action, updates, { updatedAt: Date.now() });
    if (updates.status === "completed") {
      action.progress = 100;
      action.completedDate = (_a = action.completedDate) != null ? _a : formatDateKey(/* @__PURE__ */ new Date());
    }
    if (updates.status && updates.status !== "completed") {
      action.completedDate = updates.completedDate;
    }
    const normalized = this.normalizeGoalAction(action);
    Object.assign(action, normalized);
    this.syncGoalActionWithAncestors(action);
    await this.save();
  }
  async toggleGoalActionCompleted(actionId) {
    var _a;
    const action = this.data.goalActions.find((item) => item.id === actionId);
    if (!action) return;
    if (action.status === "completed") {
      Object.assign(action, { status: "in-progress", completedDate: void 0, progress: Math.min((_a = action.progress) != null ? _a : 0, 90), updatedAt: Date.now() });
    } else {
      Object.assign(action, { status: "completed", completedDate: formatDateKey(/* @__PURE__ */ new Date()), progress: 100, updatedAt: Date.now() });
    }
    this.syncGoalActionWithAncestors(action);
    await this.save();
  }
  async deleteGoalAction(actionId) {
    const ids = /* @__PURE__ */ new Set([actionId]);
    let changed = true;
    while (changed) {
      changed = false;
      this.data.goalActions.forEach((action) => {
        if (action.parentId && ids.has(action.parentId) && !ids.has(action.id)) {
          ids.add(action.id);
          changed = true;
        }
      });
    }
    this.data.goalActions = this.data.goalActions.filter((action) => !ids.has(action.id));
    ids.forEach((id) => this.removeAutoSegmentsForGoalAction(id));
    await this.save();
  }
  getGoalActionProgress(actionId) {
    var _a;
    const action = this.data.goalActions.find((item) => item.id === actionId);
    if (!action) return 0;
    const children = this.data.goalActions.filter((item) => item.parentId === actionId);
    if (children.length === 0 || action.progressMode === "manual") return Math.max(0, Math.min(100, (_a = action.progress) != null ? _a : action.status === "completed" ? 100 : 0));
    const total = children.reduce((sum, child) => sum + this.getGoalActionProgress(child.id), 0);
    return Math.round(total / children.length);
  }
  withGoalActionComputedState(action) {
    const progress = this.getGoalActionProgress(action.id);
    const status = action.status !== "completed" && action.deadline && action.deadline < formatDateKey(/* @__PURE__ */ new Date()) ? "overdue" : action.status;
    return { ...action, status, progress };
  }
  normalizeGoalAction(action) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i;
    const timestamp = Date.now();
    const startDate = action.startDate || formatDateKey(/* @__PURE__ */ new Date());
    const deadline = action.deadline || startDate;
    return {
      id: (_a = action.id) != null ? _a : `goal-action-${timestamp}`,
      goalId: action.goalId,
      parentId: action.parentId,
      title: action.title || "\u76EE\u6807\u4EFB\u52A1",
      description: (_b = action.description) != null ? _b : "",
      status: (_c = action.status) != null ? _c : "todo",
      startDate,
      deadline,
      completedDate: action.completedDate,
      progress: Math.max(0, Math.min(100, (_d = action.progress) != null ? _d : 0)),
      progressMode: (_e = action.progressMode) != null ? _e : "auto",
      durationDays: this.getNaturalDurationDays(startDate, deadline),
      isMilestone: (_f = action.isMilestone) != null ? _f : false,
      milestoneDate: action.milestoneDate,
      importance: action.importance,
      urgency: action.urgency,
      note: (_g = action.note) != null ? _g : "",
      collapsed: (_h = action.collapsed) != null ? _h : false,
      createdAt: (_i = action.createdAt) != null ? _i : timestamp,
      updatedAt: timestamp
    };
  }
  shouldShowActiveGoal(goal, dateKey = formatDateKey(/* @__PURE__ */ new Date())) {
    return !goal.completedDate || goal.completedDate >= dateKey;
  }
  shouldShowActiveObjective(objective, dateKey = formatDateKey(/* @__PURE__ */ new Date())) {
    return !objective.completedDate || objective.completedDate >= dateKey;
  }
  shouldShowActiveKeyResult(keyResult, dateKey = formatDateKey(/* @__PURE__ */ new Date())) {
    return !keyResult.completedDate || keyResult.completedDate >= dateKey;
  }
  shouldShowActiveGoalAction(action, dateKey = formatDateKey(/* @__PURE__ */ new Date())) {
    return !action.completedDate || action.completedDate >= dateKey;
  }
  normalizeObjective(objective) {
    var _a, _b, _c, _d, _e, _f;
    const period = this.parseObjectivePeriod(objective);
    const progress = Math.max(0, Math.min(100, (_a = objective.progress) != null ? _a : 0));
    const completedDate = progress >= 100 ? (_b = objective.completedDate) != null ? _b : formatDateKey(/* @__PURE__ */ new Date()) : void 0;
    return {
      ...objective,
      title: objective.title || "\u5B63\u5EA6\u76EE\u6807",
      quarter: `${period.year} Q${period.quarterNumber}`,
      year: period.year,
      quarterNumber: period.quarterNumber,
      progress,
      completedDate,
      autoGenerated: (_c = objective.autoGenerated) != null ? _c : false,
      sourceGoalActionId: objective.sourceGoalActionId,
      segmentType: objective.segmentType,
      segmentStartDate: objective.segmentStartDate,
      segmentEndDate: objective.segmentEndDate,
      segmentNote: (_d = objective.segmentNote) != null ? _d : "",
      segmentProgress: (_e = objective.segmentProgress) != null ? _e : progress,
      updatedAt: (_f = objective.updatedAt) != null ? _f : Date.now()
    };
  }
  parseObjectivePeriod(objective) {
    var _a, _b, _c, _d;
    const current = /* @__PURE__ */ new Date();
    const quarterMatch = typeof objective.quarter === "string" ? objective.quarter.match(/(\d{4})\s*Q([1-4])/i) : null;
    const parsedQuarter = Number((_b = (_a = quarterMatch == null ? void 0 : quarterMatch[2]) != null ? _a : objective.quarterNumber) != null ? _b : Math.floor(current.getMonth() / 3) + 1);
    return {
      year: Number((_d = (_c = quarterMatch == null ? void 0 : quarterMatch[1]) != null ? _c : objective.year) != null ? _d : current.getFullYear()),
      quarterNumber: Math.max(1, Math.min(4, parsedQuarter || 1))
    };
  }
  normalizeKeyResult(keyResult) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    const now = /* @__PURE__ */ new Date();
    const progress = Math.max(0, Math.min(100, (_a = keyResult.progress) != null ? _a : 0));
    const completed = keyResult.completed || progress >= 100;
    return {
      ...keyResult,
      title: keyResult.title || "\u6708\u5EA6\u76EE\u6807",
      year: (_b = keyResult.year) != null ? _b : now.getFullYear(),
      month: Math.max(1, Math.min(12, (_c = keyResult.month) != null ? _c : now.getMonth() + 1)),
      progress: completed ? 100 : progress,
      completed,
      completedDate: completed ? (_d = keyResult.completedDate) != null ? _d : formatDateKey(/* @__PURE__ */ new Date()) : void 0,
      autoGenerated: (_e = keyResult.autoGenerated) != null ? _e : false,
      sourceGoalActionId: keyResult.sourceGoalActionId,
      segmentType: keyResult.segmentType,
      segmentStartDate: keyResult.segmentStartDate,
      segmentEndDate: keyResult.segmentEndDate,
      segmentNote: (_f = keyResult.segmentNote) != null ? _f : "",
      segmentProgress: (_g = keyResult.segmentProgress) != null ? _g : progress,
      updatedAt: (_h = keyResult.updatedAt) != null ? _h : Date.now()
    };
  }
  normalizeReviewItem(item) {
    var _a, _b, _c, _d, _e;
    const now = (/* @__PURE__ */ new Date()).toISOString();
    return {
      id: (_a = item.id) != null ? _a : `review-${Date.now()}`,
      date: item.date || formatDateKey(/* @__PURE__ */ new Date()),
      title: item.title || "\u590D\u76D8\u8BB0\u5F55",
      content: (_b = item.content) != null ? _b : "",
      status: (_c = item.status) != null ? _c : "todo",
      createdAt: (_d = item.createdAt) != null ? _d : now,
      updatedAt: (_e = item.updatedAt) != null ? _e : now
    };
  }
  async addKeyResult(keyResult) {
    this.data.keyResults.push(this.normalizeKeyResult(keyResult));
    await this.save();
  }
  async updateKeyResult(keyResultId, updates) {
    var _a;
    const keyResult = this.data.keyResults.find((item) => item.id === keyResultId);
    if (!keyResult) return;
    Object.assign(keyResult, updates, { updatedAt: Date.now() });
    keyResult.progress = Math.max(0, Math.min(100, keyResult.progress));
    keyResult.completed = keyResult.completed || keyResult.progress >= 100;
    if (keyResult.completed) {
      keyResult.progress = 100;
      keyResult.completedDate = (_a = keyResult.completedDate) != null ? _a : formatDateKey(/* @__PURE__ */ new Date());
    } else {
      keyResult.completedDate = void 0;
    }
    await this.save();
  }
  async deleteKeyResult(keyResultId) {
    this.data.keyResults = this.data.keyResults.filter((item) => item.id !== keyResultId);
    await this.save();
  }
  async toggleKeyResult(keyResultId) {
    const keyResult = this.data.keyResults.find((item) => item.id === keyResultId);
    if (!keyResult) {
      return;
    }
    keyResult.completed = !keyResult.completed;
    keyResult.progress = keyResult.completed ? 100 : Math.min(keyResult.progress, 90);
    keyResult.completedDate = keyResult.completed ? formatDateKey(/* @__PURE__ */ new Date()) : void 0;
    keyResult.updatedAt = Date.now();
    await this.save();
  }
  async addMilestone(milestone) {
    this.data.milestones.push(milestone);
    await this.save();
  }
  async updateMilestone(milestoneId, updates) {
    const milestone = this.data.milestones.find((item) => item.id === milestoneId);
    if (!milestone) return;
    Object.assign(milestone, updates);
    await this.save();
  }
  async deleteMilestone(milestoneId) {
    this.data.milestones = this.data.milestones.filter((item) => item.id !== milestoneId);
    await this.save();
  }
  async addRisk(risk) {
    this.data.risks.push(risk);
    await this.save();
  }
  async updateRisk(riskId, updates) {
    const risk = this.data.risks.find((item) => item.id === riskId);
    if (!risk) return;
    Object.assign(risk, updates);
    await this.save();
  }
  async deleteRisk(riskId) {
    this.data.risks = this.data.risks.filter((item) => item.id !== riskId);
    await this.save();
  }
  async addTransaction(transaction) {
    this.data.transactions.push({ ...transaction, amount: Math.abs(transaction.amount) });
    this.recalculateBudgetSpent();
    await this.save();
  }
  getMonthlyIncome() {
    return this.getCurrentMonthTransactions().filter((transaction) => transaction.type === "income").reduce((sum, transaction) => sum + transaction.amount, 0);
  }
  getMonthlyExpense() {
    return this.getCurrentMonthTransactions().filter((transaction) => transaction.type === "expense").reduce((sum, transaction) => sum + transaction.amount, 0);
  }
  getBudgetRemaining() {
    return this.getMonthlyBudgetLimit() - this.getMonthlyExpense();
  }
  getSavingRate() {
    const income = this.getMonthlyIncome();
    if (income <= 0) {
      return 0;
    }
    return Math.round((income - this.getMonthlyExpense()) / income * 100);
  }
  getCurrentMonthTransactions() {
    const now = /* @__PURE__ */ new Date();
    const prefix = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, "0")}`;
    return this.getTransactions().filter((transaction) => transaction.date.startsWith(prefix));
  }
  getPriorityMatrixItems() {
    return this.data.priorityMatrixItems;
  }
  async addPriorityMatrixItem(item) {
    this.data.priorityMatrixItems.push(item);
    await this.save();
  }
  async updatePriorityMatrixItem(itemId, updates) {
    const item = this.data.priorityMatrixItems.find((entry) => entry.id === itemId);
    if (!item) return;
    Object.assign(item, updates);
    await this.save();
  }
  async deletePriorityMatrixItem(itemId) {
    this.data.priorityMatrixItems = this.data.priorityMatrixItems.filter((item) => item.id !== itemId);
    await this.save();
  }
  recalculateBudgetSpent() {
    this.getBudgets().forEach((budget) => {
      budget.spent = this.getCurrentMonthTransactions().filter((transaction) => transaction.type === "expense" && transaction.category === budget.category).reduce((sum, transaction) => sum + transaction.amount, 0);
    });
  }
  isBudgetAdjustmentTransaction(transaction) {
    return transaction.note === "\u6708\u5EA6\u6536\u652F\u7BA1\u7406\u8C03\u6574" && (transaction.category === "\u6708\u5EA6\u6536\u5165\u8C03\u6574" || transaction.category === "\u6708\u5EA6\u652F\u51FA\u8C03\u6574");
  }
  setSectionEnabledInMemory(sectionId, enabled) {
    const section = this.data.sections.find((item) => item.id === sectionId);
    if (section) {
      section.enabled = enabled;
    }
  }
  isHabitCompleted(habitId, date) {
    return this.isCheckInCompleted(habitId, date);
  }
  async toggleHabit(habitId, date) {
    await this.toggleCheckIn(habitId, date);
  }
  getCheckInDefinitions(moduleId, includeArchived = false) {
    return this.data.checkInDefinitions.filter((definition) => (includeArchived || !definition.archived) && (!moduleId || definition.moduleId === moduleId)).sort((left, right) => {
      if (left.moduleId === right.moduleId) return left.order - right.order;
      return this.getCheckInModuleOrder(left.moduleId) - this.getCheckInModuleOrder(right.moduleId);
    });
  }
  getActiveCheckInDefinitions(moduleId) {
    return this.getCheckInDefinitions(moduleId).filter((definition) => definition.enabled);
  }
  getCheckInRecords(checkInId) {
    return this.data.checkInRecords.filter((record) => !checkInId || record.checkInId === checkInId);
  }
  getCheckInRecordsByDate(date) {
    return this.data.checkInRecords.filter((record) => record.date === date);
  }
  isCheckInCompleted(checkInId, date) {
    return this.data.checkInRecords.some((record) => record.checkInId === checkInId && record.date === date);
  }
  async toggleCheckIn(checkInId, date) {
    const index = this.data.checkInRecords.findIndex((record) => record.checkInId === checkInId && record.date === date);
    if (index >= 0) {
      this.data.checkInRecords.splice(index, 1);
      await this.save();
      return false;
    }
    this.data.checkInRecords.push({
      id: `checkin-record-${checkInId}-${date}`,
      checkInId,
      date,
      completedAt: Date.now()
    });
    this.dedupeCheckInRecordsInMemory();
    await this.save();
    return true;
  }
  async addCheckInDefinition(input) {
    var _a, _b, _c, _d, _e, _f;
    const nextOrder = this.getCheckInDefinitions(input.moduleId, true).reduce((max, item) => Math.max(max, item.order), 0) + 10;
    const now = Date.now();
    this.data.checkInDefinitions.push({
      id: (_a = input.id) != null ? _a : `checkin-${input.moduleId}-${now}`,
      moduleId: input.moduleId,
      title: input.title,
      icon: (_b = input.icon) != null ? _b : "circle",
      color: (_c = input.color) != null ? _c : "#ff8fbc",
      enabled: (_d = input.enabled) != null ? _d : true,
      archived: false,
      order: (_e = input.order) != null ? _e : nextOrder,
      createdAt: (_f = input.createdAt) != null ? _f : now,
      updatedAt: now
    });
    await this.save();
  }
  async updateCheckInDefinition(definitionId, updates) {
    var _a;
    const definition = this.data.checkInDefinitions.find((item) => item.id === definitionId);
    if (!definition) return;
    if (updates.enabled === false && definition.enabled) {
      updates.inactiveFrom = (_a = updates.inactiveFrom) != null ? _a : todayKey();
    }
    if (updates.enabled === true && !definition.enabled) {
      updates.inactiveFrom = void 0;
      updates.archived = false;
    }
    Object.assign(definition, updates, { updatedAt: Date.now() });
    await this.save();
  }
  async archiveCheckInDefinition(definitionId) {
    await this.updateCheckInDefinition(definitionId, { archived: true, enabled: false, inactiveFrom: todayKey() });
  }
  async reorderCheckInDefinitions(moduleId, orderedIds) {
    const orderMap = new Map(orderedIds.map((id, index) => [id, (index + 1) * 10]));
    this.data.checkInDefinitions.forEach((definition) => {
      const order = orderMap.get(definition.id);
      if (definition.moduleId === moduleId && order !== void 0) {
        definition.order = order;
        definition.updatedAt = Date.now();
      }
    });
    await this.save();
  }
  getWeeklyCompletionRate() {
    const habitCompletion = [];
    this.getCurrentWeekDates().forEach((date) => {
      this.getActiveCheckInDefinitions().forEach((habit) => {
        habitCompletion.push(this.isCheckInCompleted(habit.id, date));
      });
    });
    const focusCompletion = this.data.todayFocusTasks.map((task) => task.completed);
    const items = [...habitCompletion, ...focusCompletion];
    const done = items.filter(Boolean).length;
    return items.length === 0 ? 0 : Math.round(done / items.length * 100);
  }
  getPendingTaskCount() {
    return this.data.todayFocusTasks.filter((task) => !task.completed).length;
  }
  getCheckinStreakDays() {
    const today6 = /* @__PURE__ */ new Date();
    let streak = 0;
    for (let offset = 0; offset < 366; offset += 1) {
      const date = new Date(today6);
      date.setDate(today6.getDate() - offset);
      const key = formatDateKey(date);
      const habits = this.getActiveCheckInDefinitions();
      const allDone = habits.length > 0 && habits.every((habit) => this.isCheckInCompleted(habit.id, key));
      if (!allDone) {
        break;
      }
      streak += 1;
    }
    return streak;
  }
  resolveFocusState() {
    const state = { ...this.data.focusState };
    if (!state.isRunning || state.isPaused || !state.startedAt) {
      return state;
    }
    const elapsedSeconds = Math.max(0, Math.floor((Date.now() - new Date(state.startedAt).getTime()) / 1e3));
    const remainingSeconds = Math.max(0, state.remainingSeconds - elapsedSeconds);
    return {
      ...state,
      remainingSeconds
    };
  }
  getCurrentWeekDates() {
    const today6 = /* @__PURE__ */ new Date();
    const day = today6.getDay();
    const mondayOffset = day === 0 ? -6 : 1 - day;
    const monday = new Date(today6);
    monday.setDate(today6.getDate() + mondayOffset);
    return Array.from({ length: 7 }, (_, index) => {
      const date = new Date(monday);
      date.setDate(monday.getDate() + index);
      return formatDateKey(date);
    });
  }
  mergeWithDefaults(savedData) {
    var _a, _b;
    if (!savedData || typeof savedData !== "object") {
      return structuredClone(DEFAULT_DATA);
    }
    const partial = savedData;
    const sections = Array.isArray(partial.sections) ? this.migrateSections(partial.sections) : structuredClone(DEFAULT_DATA.sections);
    const merged = {
      ...structuredClone(DEFAULT_DATA),
      ...partial,
      dataVersion: "0.7.0",
      banner: {
        ...DEFAULT_DATA.banner,
        ...partial.banner
      },
      userSettings: {
        ...DEFAULT_DATA.userSettings,
        ...partial.userSettings
      },
      sections,
      moduleLayouts: this.migrateModuleLayouts(partial),
      habits: (_a = partial.habits) != null ? _a : {},
      todayFocusTasks: Array.isArray(partial.todayFocusTasks) ? partial.todayFocusTasks : structuredClone(DEFAULT_DATA.todayFocusTasks),
      researchProjects: Array.isArray(partial.researchProjects) ? partial.researchProjects.map((project) => this.normalizeResearchProject(project)) : structuredClone(DEFAULT_DATA.researchProjects).map((project) => this.normalizeResearchProject(project)),
      researchPapers: Array.isArray(partial.researchPapers) ? partial.researchPapers.map((paper) => this.normalizeResearchPaper(paper)) : structuredClone(DEFAULT_DATA.researchPapers).map((paper) => this.normalizeResearchPaper(paper)),
      literatureNotes: Array.isArray(partial.literatureNotes) ? partial.literatureNotes.map((note) => this.normalizeLiteratureNote(note)).filter((note) => note.notePath) : this.createInitialLiteratureNotes(partial),
      paperStatuses: this.mergePaperStatuses(partial),
      paperVenues: this.mergePaperVenues(partial),
      paperTags: this.mergePaperTags(partial),
      experimentPlans: Array.isArray(partial.experimentPlans) ? partial.experimentPlans.map((item) => this.normalizeExperiment(item, "plan")) : structuredClone(DEFAULT_DATA.experimentPlans),
      experimentRecords: Array.isArray(partial.experimentRecords) ? partial.experimentRecords.map((item) => this.normalizeExperiment(item, "records")) : structuredClone(DEFAULT_DATA.experimentRecords),
      researchDeadlines: Array.isArray(partial.researchDeadlines) ? partial.researchDeadlines : structuredClone(DEFAULT_DATA.researchDeadlines),
      researchMemos: Array.isArray(partial.researchMemos) ? partial.researchMemos : structuredClone(DEFAULT_DATA.researchMemos),
      dataAnalysisTasks: Array.isArray(partial.dataAnalysisTasks) ? partial.dataAnalysisTasks : structuredClone(DEFAULT_DATA.dataAnalysisTasks),
      bookTags: this.mergeBookTags(partial),
      books: Array.isArray(partial.books) ? partial.books.map((book) => this.normalizeBook(book)) : structuredClone(DEFAULT_DATA.books).map((book) => this.normalizeBook(book)),
      wantToReadItems: Array.isArray(partial.wantToReadItems) ? partial.wantToReadItems.map((item) => this.normalizeWantToReadItem(item)) : this.createInitialWantToReadItems(partial),
      readingQuotes: Array.isArray(partial.readingQuotes) ? partial.readingQuotes.map((quote) => this.normalizeReadingQuote(quote)) : structuredClone(DEFAULT_DATA.readingQuotes).map((quote) => this.normalizeReadingQuote(quote)),
      readingPlans: Array.isArray(partial.readingPlans) ? partial.readingPlans.map((plan) => this.normalizeReadingPlan(plan)) : this.createInitialReadingPlans(partial),
      readingNotes: Array.isArray(partial.readingNotes) ? partial.readingNotes.map((note) => this.normalizeReadingNote(note)).filter((note) => note.notePath && note.bookId) : this.createInitialReadingNotes(partial),
      workouts: Array.isArray(partial.workouts) ? partial.workouts.map((workout) => this.normalizeWorkout(workout)) : structuredClone(DEFAULT_DATA.workouts).map((workout) => this.normalizeWorkout(workout)),
      bodyMeasurements: Array.isArray(partial.bodyMeasurements) ? partial.bodyMeasurements.map((item) => this.normalizeBodyMeasurement(item)) : structuredClone(DEFAULT_DATA.bodyMeasurements),
      fitnessGoals: Array.isArray(partial.fitnessGoals) ? partial.fitnessGoals.map((item) => this.normalizeFitnessGoal(item)) : structuredClone(DEFAULT_DATA.fitnessGoals),
      healthReminders: Array.isArray(partial.healthReminders) ? partial.healthReminders.map((item) => this.normalizeHealthReminder(item)) : structuredClone(DEFAULT_DATA.healthReminders),
      healthReminderLogs: Array.isArray(partial.healthReminderLogs) ? partial.healthReminderLogs : structuredClone(DEFAULT_DATA.healthReminderLogs),
      transactions: Array.isArray(partial.transactions) ? partial.transactions : structuredClone(DEFAULT_DATA.transactions),
      budgets: Array.isArray(partial.budgets) ? partial.budgets : structuredClone(DEFAULT_DATA.budgets),
      accounts: Array.isArray(partial.accounts) ? partial.accounts.map((account) => this.normalizeAccount(account)) : structuredClone(DEFAULT_DATA.accounts).map((account) => this.normalizeAccount(account)),
      savingGoals: Array.isArray(partial.savingGoals) ? partial.savingGoals : structuredClone(DEFAULT_DATA.savingGoals),
      bills: Array.isArray(partial.bills) ? partial.bills : structuredClone(DEFAULT_DATA.bills),
      financeTodos: Array.isArray(partial.financeTodos) ? partial.financeTodos.map((item) => this.normalizeFinanceTodo(item)) : structuredClone(DEFAULT_DATA.financeTodos),
      tasks: Array.isArray(partial.tasks) ? partial.tasks.map((item) => this.normalizeTask(item)) : structuredClone(DEFAULT_DATA.tasks).map((item) => this.normalizeTask(item)),
      taskProjects: Array.isArray(partial.taskProjects) ? partial.taskProjects.map((item) => this.normalizeTaskProject(item)) : structuredClone(DEFAULT_DATA.taskProjects),
      taskSettings: {
        ...DEFAULT_DATA.taskSettings,
        ...partial.taskSettings
      },
      goals: Array.isArray(partial.goals) ? partial.goals : structuredClone(DEFAULT_DATA.goals),
      goalActions: Array.isArray(partial.goalActions) ? partial.goalActions.map((action) => this.normalizeGoalAction(action)) : this.createInitialGoalActions(partial),
      objectives: Array.isArray(partial.objectives) ? partial.objectives.map((item) => this.normalizeObjective(item)) : structuredClone(DEFAULT_DATA.objectives).map((item) => this.normalizeObjective(item)),
      keyResults: Array.isArray(partial.keyResults) ? partial.keyResults.map((item) => this.normalizeKeyResult(item)) : structuredClone(DEFAULT_DATA.keyResults).map((item) => this.normalizeKeyResult(item)),
      milestones: Array.isArray(partial.milestones) ? partial.milestones : structuredClone(DEFAULT_DATA.milestones),
      risks: Array.isArray(partial.risks) ? partial.risks : structuredClone(DEFAULT_DATA.risks),
      reviewItems: Array.isArray(partial.reviewItems) ? partial.reviewItems.map((item) => this.normalizeReviewItem(item)) : structuredClone(DEFAULT_DATA.reviewItems),
      calendarSettings: {
        ...DEFAULT_DATA.calendarSettings,
        ...partial.calendarSettings
      },
      calendarTodos: Array.isArray(partial.calendarTodos) ? partial.calendarTodos : structuredClone(DEFAULT_DATA.calendarTodos),
      apexHabitSettings: {
        ...DEFAULT_DATA.apexHabitSettings,
        ...partial.apexHabitSettings,
        customHabits: Array.isArray((_b = partial.apexHabitSettings) == null ? void 0 : _b.customHabits) ? partial.apexHabitSettings.customHabits : structuredClone(DEFAULT_DATA.apexHabitSettings.customHabits)
      },
      checkInDefinitions: this.mergeCheckInDefinitions(partial),
      checkInRecords: this.mergeCheckInRecords(partial),
      quickActions: Array.isArray(partial.quickActions) ? partial.quickActions : structuredClone(DEFAULT_DATA.quickActions),
      focusSettings: {
        ...DEFAULT_DATA.focusSettings,
        ...partial.focusSettings
      },
      focusState: {
        ...DEFAULT_DATA.focusState,
        ...partial.focusState
      },
      focusRecords: Array.isArray(partial.focusRecords) ? partial.focusRecords.map((record) => this.normalizeFocusRecord(record)) : structuredClone(DEFAULT_DATA.focusRecords),
      trainingPlans: Array.isArray(partial.trainingPlans) ? partial.trainingPlans.map((plan) => this.normalizeTrainingPlan(plan)) : this.createInitialTrainingPlans(partial),
      fitnessDailyRecords: Array.isArray(partial.fitnessDailyRecords) ? partial.fitnessDailyRecords.map((item) => this.normalizeFitnessDailyRecord(item)) : structuredClone(DEFAULT_DATA.fitnessDailyRecords),
      fitnessHabitDefinitions: Array.isArray(partial.fitnessHabitDefinitions) ? partial.fitnessHabitDefinitions.map((item, index) => this.normalizeFitnessHabitDefinition(item, index)) : structuredClone(DEFAULT_DATA.fitnessHabitDefinitions),
      fitnessHabitRecords: Array.isArray(partial.fitnessHabitRecords) ? partial.fitnessHabitRecords : structuredClone(DEFAULT_DATA.fitnessHabitRecords),
      investmentWatchItems: Array.isArray(partial.investmentWatchItems) ? partial.investmentWatchItems.map((item) => this.normalizeInvestmentWatchItem(item)) : structuredClone(DEFAULT_DATA.investmentWatchItems),
      investmentSnapshots: Array.isArray(partial.investmentSnapshots) ? partial.investmentSnapshots : this.createInitialInvestmentSnapshots(Array.isArray(partial.investmentWatchItems) ? partial.investmentWatchItems : DEFAULT_DATA.investmentWatchItems),
      priorityMatrixItems: Array.isArray(partial.priorityMatrixItems) ? partial.priorityMatrixItems : structuredClone(DEFAULT_DATA.priorityMatrixItems),
      theme: {
        ...DEFAULT_DATA.theme,
        ...partial.theme
      }
    };
    return this.repairFitnessRelations(this.repairResearchRelations(this.repairBookTagRelations(merged)));
  }
  normalizeBodyMeasurement(measurement) {
    var _a, _b, _c, _d;
    const timestamp = (_a = measurement.createdAt) != null ? _a : `${measurement.date || todayKey()}T00:00:00.000Z`;
    return {
      id: (_b = measurement.id) != null ? _b : `measure-${measurement.date || Date.now()}`,
      date: measurement.date || todayKey(),
      weight: Number(measurement.weight) || 0,
      bmi: Number(measurement.bmi) || 0,
      waist: Number(measurement.waist) || 0,
      chest: Number(measurement.chest) || 0,
      hip: Number(measurement.hip) || 0,
      note: (_c = measurement.note) != null ? _c : "",
      createdAt: timestamp,
      updatedAt: (_d = measurement.updatedAt) != null ? _d : timestamp
    };
  }
  normalizeWorkout(workout) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m;
    const timestamp = (_a = workout.createdAt) != null ? _a : `${workout.date || todayKey()}T00:00:00.000Z`;
    const workoutType = this.normalizeWorkoutType((_b = workout.workoutType) != null ? _b : workout.type);
    const durationMinutes = Number((_c = workout.durationMinutes) != null ? _c : workout.duration) || 0;
    return {
      ...workout,
      id: (_d = workout.id) != null ? _d : `workout-${Date.now()}`,
      title: ((_e = workout.title) == null ? void 0 : _e.trim()) || ((_f = workout.note) == null ? void 0 : _f.trim()) || `${workoutType}\u8BAD\u7EC3`,
      date: workout.date || todayKey(),
      type: (_g = workout.type) != null ? _g : workoutType,
      workoutType,
      duration: durationMinutes,
      durationMinutes,
      calories: Number(workout.calories) || 0,
      distanceKm: workout.distanceKm !== void 0 && Number(workout.distanceKm) > 0 ? Number(workout.distanceKm) : void 0,
      exerciseDetails: (_h = workout.exerciseDetails) != null ? _h : "",
      feeling: (_i = workout.feeling) != null ? _i : "",
      resultSummary: (_j = workout.resultSummary) != null ? _j : "",
      markdownPath: typeof workout.markdownPath === "string" && workout.markdownPath.length > 0 ? workout.markdownPath : void 0,
      completed: (_k = workout.completed) != null ? _k : true,
      note: (_l = workout.note) != null ? _l : "",
      createdAt: timestamp,
      updatedAt: (_m = workout.updatedAt) != null ? _m : timestamp
    };
  }
  normalizeWorkoutType(type) {
    if (type === "\u8DD1\u6B65" || type === "\u529B\u91CF" || type === "\u9A91\u884C" || type === "\u6E38\u6CF3" || type === "\u745C\u4F3D" || type === "\u5176\u5B83") return type;
    if (type === "\u62C9\u4F38") return "\u745C\u4F3D";
    if (type === "\u6709\u6C27") return "\u8DD1\u6B65";
    return "\u5176\u5B83";
  }
  normalizeAccount(account) {
    var _a;
    return {
      ...account,
      id: (_a = account.id) != null ? _a : `account-${Date.now()}`,
      name: account.name || "\u672A\u547D\u540D\u8D26\u6237",
      type: account.type || "\u5176\u4ED6",
      balance: Number(account.balance) || 0,
      icon: this.accountTypeIcon(account.type)
    };
  }
  accountTypeIcon(type) {
    if (type === "\u73B0\u91D1") return "wallet";
    if (type === "\u50A8\u84C4\u5361") return "landmark";
    if (type === "\u4FE1\u7528\u5361") return "credit-card";
    if (type === "\u652F\u4ED8\u5B9D" || type === "\u5FAE\u4FE1\u94B1\u5305") return "smartphone";
    if (type === "\u6295\u8D44\u8D26\u6237" || type === "\u8BC1\u5238") return "chart-no-axes-combined";
    return "circle-dollar-sign";
  }
  normalizeFitnessGoal(goal) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l;
    const timestamp = (_a = goal.createdAt) != null ? _a : `${goal.startDate || goal.deadline || todayKey()}T00:00:00.000Z`;
    const currentValue = Number((_c = (_b = goal.currentValue) != null ? _b : goal.current) != null ? _c : 0);
    const targetValue = Number((_e = (_d = goal.targetValue) != null ? _d : goal.target) != null ? _e : 0);
    const progress = Math.max(0, Math.min(100, Number((_f = goal.progress) != null ? _f : targetValue === 0 ? 0 : Math.round(currentValue / targetValue * 100)) || 0));
    const status = this.getNormalizedFitnessGoalStatus({
      ...goal,
      currentValue,
      targetValue,
      progress
    });
    return {
      ...goal,
      id: (_g = goal.id) != null ? _g : `fitness-goal-${Date.now()}`,
      title: goal.title || "\u5065\u8EAB\u76EE\u6807",
      type: (_h = goal.type) != null ? _h : "\u5176\u5B83",
      description: (_i = goal.description) != null ? _i : "",
      currentValue,
      targetValue,
      unit: goal.unit || goal.targetUnit || "",
      targetUnit: goal.targetUnit || goal.unit || "",
      progress,
      startDate: (_j = goal.startDate) != null ? _j : todayKey(),
      deadline: goal.deadline || todayKey(),
      completedDate: status === "completed" ? (_k = goal.completedDate) != null ? _k : todayKey() : goal.completedDate,
      status,
      createdAt: timestamp,
      updatedAt: (_l = goal.updatedAt) != null ? _l : timestamp
    };
  }
  getNormalizedFitnessGoalStatus(goal) {
    if (goal.status === "archived") return "archived";
    if (goal.status === "completed" || goal.completedDate || goal.progress === 100) return "completed";
    if (goal.startDate && todayKey() < goal.startDate) return "planned";
    if (goal.deadline && goal.deadline < todayKey()) return "overdue";
    return "active";
  }
  normalizeTrainingPlan(plan) {
    var _a, _b, _c, _d, _e, _f;
    const timestamp = (_a = plan.createdAt) != null ? _a : `${plan.startDate || todayKey()}T00:00:00.000Z`;
    const startDate = plan.startDate || todayKey();
    const endDate = plan.endDate && plan.endDate >= startDate ? plan.endDate : startDate;
    const status = this.getNormalizedTrainingPlanStatus({ ...plan, startDate, endDate });
    return {
      id: (_b = plan.id) != null ? _b : `training-plan-${Date.now()}`,
      title: plan.title || "\u8BAD\u7EC3\u8BA1\u5212",
      fitnessGoalId: plan.fitnessGoalId,
      startDate,
      endDate,
      weeklyFrequency: Math.max(0, Number(plan.weeklyFrequency) || 0),
      description: (_c = plan.description) != null ? _c : "",
      note: (_d = plan.note) != null ? _d : "",
      markdownPath: typeof plan.markdownPath === "string" && plan.markdownPath.length > 0 ? plan.markdownPath : void 0,
      status,
      completedDate: status === "completed" ? (_e = plan.completedDate) != null ? _e : todayKey() : plan.completedDate,
      createdAt: timestamp,
      updatedAt: (_f = plan.updatedAt) != null ? _f : timestamp
    };
  }
  withComputedTrainingPlanStatus(plan) {
    return { ...plan, status: this.getNormalizedTrainingPlanStatus(plan) };
  }
  getNormalizedTrainingPlanStatus(plan) {
    if (plan.status === "completed" || plan.completedDate) return "completed";
    if (todayKey() < plan.startDate) return "planned";
    if (todayKey() > plan.endDate) return "overdue";
    return "active";
  }
  createInitialTrainingPlans(partial) {
    var _a;
    if (!Array.isArray(partial.workouts)) {
      return structuredClone(DEFAULT_DATA.trainingPlans).map((plan) => this.normalizeTrainingPlan(plan));
    }
    const firstGoalId = (_a = (Array.isArray(partial.fitnessGoals) ? partial.fitnessGoals : DEFAULT_DATA.fitnessGoals)[0]) == null ? void 0 : _a.id;
    const pending = partial.workouts.filter((workout) => !workout.completed);
    if (pending.length === 0) {
      return structuredClone(DEFAULT_DATA.trainingPlans).map((plan) => this.normalizeTrainingPlan(plan));
    }
    return pending.map((workout) => this.normalizeTrainingPlan({
      id: `training-plan-${workout.id}`,
      title: workout.note || `${workout.type}\u8BAD\u7EC3\u8BA1\u5212`,
      fitnessGoalId: firstGoalId,
      startDate: workout.date,
      endDate: workout.date,
      weeklyFrequency: 1,
      description: `${workout.type} \xB7 ${workout.duration}min`,
      note: workout.note,
      createdAt: nowIso(),
      updatedAt: nowIso()
    }));
  }
  normalizeHealthReminder(reminder) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i;
    const timestamp = (_a = reminder.createdAt) != null ? _a : nowIso();
    return {
      id: (_b = reminder.id) != null ? _b : `health-${Date.now()}`,
      title: reminder.title || "\u5065\u5EB7\u63D0\u9192",
      date: (_c = reminder.date) != null ? _c : todayKey(),
      time: (_d = reminder.time) != null ? _d : "09:00",
      repeatType: (_e = reminder.repeatType) != null ? _e : "once",
      repeatDays: (_f = reminder.repeatDays) != null ? _f : [],
      note: (_g = reminder.note) != null ? _g : "",
      enabled: (_h = reminder.enabled) != null ? _h : true,
      createdAt: timestamp,
      updatedAt: (_i = reminder.updatedAt) != null ? _i : timestamp
    };
  }
  normalizeFitnessDailyRecord(record) {
    var _a, _b, _c, _d, _e;
    return {
      date: record.date || todayKey(),
      waterCups: Number(record.waterCups) || 0,
      waterGoal: Number(record.waterGoal) || 8,
      waterNote: (_a = record.waterNote) != null ? _a : "",
      sleepHours: Number(record.sleepHours) || 0,
      sleepGoal: Number(record.sleepGoal) || 8,
      bedtime: (_b = record.bedtime) != null ? _b : "",
      wakeTime: (_c = record.wakeTime) != null ? _c : "",
      sleepNote: (_d = record.sleepNote) != null ? _d : "",
      updatedAt: (_e = record.updatedAt) != null ? _e : nowIso()
    };
  }
  normalizeFitnessHabitDefinition(definition, index) {
    var _a, _b, _c;
    const timestamp = (_a = definition.createdAt) != null ? _a : nowIso();
    return {
      id: (_b = definition.id) != null ? _b : `fitness-habit-${Date.now()}-${index}`,
      name: definition.name || "\u81EA\u5B9A\u4E49\u4E60\u60EF",
      targetName: definition.targetName || "\u6BCF\u65E5\u76EE\u6807",
      targetValue: Number(definition.targetValue) || 0,
      unit: definition.unit || "",
      order: Number(definition.order) || (index + 1) * 10,
      createdAt: timestamp,
      updatedAt: (_c = definition.updatedAt) != null ? _c : timestamp
    };
  }
  normalizeFinanceTodo(todo) {
    var _a, _b, _c, _d, _e, _f;
    const timestamp = (_a = todo.createdAt) != null ? _a : nowIso();
    return {
      id: (_b = todo.id) != null ? _b : `finance-todo-${Date.now()}`,
      title: todo.title || "\u7406\u8D22\u5F85\u529E",
      completed: (_c = todo.completed) != null ? _c : false,
      date: (_d = todo.date) != null ? _d : todayKey(),
      note: (_e = todo.note) != null ? _e : "",
      createdAt: timestamp,
      updatedAt: (_f = todo.updatedAt) != null ? _f : timestamp
    };
  }
  mergeCheckInDefinitions(partial) {
    var _a, _b, _c;
    const definitions = /* @__PURE__ */ new Map();
    const add = (definition, index = 0) => {
      if (!definition.id) return;
      const existing = definitions.get(definition.id);
      const normalized = this.normalizeCheckInDefinition(definition, index, existing);
      definitions.set(normalized.id, normalized);
    };
    createDefaultCheckInDefinitions().forEach((definition, index) => add(definition, index));
    ((_b = (_a = partial.apexHabitSettings) == null ? void 0 : _a.customHabits) != null ? _b : []).forEach((habit, index) => {
      add({
        id: habit.id.replace(/^custom-/, ""),
        moduleId: "overview",
        title: habit.label,
        enabled: habit.enabled,
        order: habit.order,
        icon: "sparkles",
        color: "#ff8fbc"
      }, index);
    });
    ((_c = partial.checkInDefinitions) != null ? _c : []).forEach((definition, index) => add(definition, index));
    return [...definitions.values()].sort((left, right) => {
      if (left.moduleId === right.moduleId) return left.order - right.order;
      return this.getCheckInModuleOrder(left.moduleId) - this.getCheckInModuleOrder(right.moduleId);
    });
  }
  mergeCheckInRecords(partial) {
    var _a, _b;
    const records = /* @__PURE__ */ new Map();
    const add = (record) => {
      var _a2;
      if (!record.checkInId || !record.date) return;
      const date = record.date.slice(0, 10);
      const key = `${record.checkInId}::${date}`;
      if (records.has(key)) return;
      records.set(key, {
        id: (_a2 = record.id) != null ? _a2 : `checkin-record-${record.checkInId}-${date}`,
        checkInId: record.checkInId,
        date,
        completedAt: Number(record.completedAt) || Date.now()
      });
    };
    ((_a = partial.checkInRecords) != null ? _a : []).forEach(add);
    Object.entries((_b = partial.habits) != null ? _b : {}).forEach(([checkInId, byDate]) => {
      Object.entries(byDate != null ? byDate : {}).forEach(([date, completed]) => {
        if (completed) add({ checkInId, date, completedAt: (/* @__PURE__ */ new Date(`${date}T12:00:00`)).getTime() });
      });
    });
    return [...records.values()].sort((left, right) => left.date.localeCompare(right.date));
  }
  normalizeCheckInDefinition(definition, index, existing) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u;
    const now = Date.now();
    return {
      id: (_b = (_a = definition.id) != null ? _a : existing == null ? void 0 : existing.id) != null ? _b : `checkin-${now}-${index}`,
      moduleId: (_d = (_c = definition.moduleId) != null ? _c : existing == null ? void 0 : existing.moduleId) != null ? _d : "overview",
      title: (_f = (_e = definition.title) != null ? _e : existing == null ? void 0 : existing.title) != null ? _f : "\u672A\u547D\u540D\u6253\u5361",
      icon: (_h = (_g = definition.icon) != null ? _g : existing == null ? void 0 : existing.icon) != null ? _h : "circle",
      color: (_j = (_i = definition.color) != null ? _i : existing == null ? void 0 : existing.color) != null ? _j : "#ff8fbc",
      enabled: (_l = (_k = definition.enabled) != null ? _k : existing == null ? void 0 : existing.enabled) != null ? _l : true,
      archived: (_n = (_m = definition.archived) != null ? _m : existing == null ? void 0 : existing.archived) != null ? _n : false,
      inactiveFrom: (_o = definition.inactiveFrom) != null ? _o : existing == null ? void 0 : existing.inactiveFrom,
      order: Number((_q = (_p = definition.order) != null ? _p : existing == null ? void 0 : existing.order) != null ? _q : (index + 1) * 10),
      createdAt: Number((_s = (_r = definition.createdAt) != null ? _r : existing == null ? void 0 : existing.createdAt) != null ? _s : now),
      updatedAt: Number((_u = (_t = definition.updatedAt) != null ? _t : existing == null ? void 0 : existing.updatedAt) != null ? _u : now)
    };
  }
  dedupeCheckInRecordsInMemory() {
    const records = /* @__PURE__ */ new Map();
    this.data.checkInRecords.forEach((record) => {
      const key = `${record.checkInId}::${record.date}`;
      if (!records.has(key)) records.set(key, record);
    });
    this.data.checkInRecords = [...records.values()];
  }
  getCheckInModuleOrder(moduleId) {
    const order = ["overview", "research", "reading", "fitness", "finance", "goals"].indexOf(moduleId);
    return order === -1 ? 99 : order;
  }
  normalizeTask(task) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    const timestamp = (_a = task.createdAt) != null ? _a : nowIso();
    const status = this.normalizeTaskStatus(task.status);
    const recurrence = task.recurrence && task.recurrence.frequency !== "none" ? {
      frequency: this.normalizeRecurrenceFrequency(task.recurrence.frequency),
      interval: Math.max(1, Number(task.recurrence.interval) || 1),
      nextDate: task.recurrence.nextDate,
      enabled: (_b = task.recurrence.enabled) != null ? _b : true,
      lastGeneratedDate: task.recurrence.lastGeneratedDate
    } : ((_c = task.recurrence) == null ? void 0 : _c.frequency) === "none" ? { frequency: "none", enabled: false } : void 0;
    return {
      ...task,
      id: (_d = task.id) != null ? _d : `task-${Date.now()}`,
      title: task.title || "\u672A\u547D\u540D\u4EFB\u52A1",
      description: (_e = task.description) != null ? _e : "",
      status,
      priority: this.normalizeTaskPriority(task.priority),
      sourceModule: (_f = task.sourceModule) != null ? _f : "general",
      projectId: task.projectId || void 0,
      goalId: task.goalId || void 0,
      parentTaskId: task.parentTaskId || void 0,
      tags: Array.isArray(task.tags) ? task.tags.filter(Boolean) : [],
      plannedDate: task.plannedDate || void 0,
      startDate: task.startDate || void 0,
      dueDate: task.dueDate || void 0,
      completedDate: status === "done" ? (_g = task.completedDate) != null ? _g : todayKey() : task.completedDate,
      estimatedMinutes: Number(task.estimatedMinutes) > 0 ? Number(task.estimatedMinutes) : void 0,
      actualMinutes: Number(task.actualMinutes) > 0 ? Number(task.actualMinutes) : void 0,
      recurrence,
      linkedNote: task.linkedNote || void 0,
      createdAt: timestamp,
      updatedAt: (_h = task.updatedAt) != null ? _h : timestamp
    };
  }
  normalizeTaskProject(project) {
    var _a, _b, _c;
    return {
      id: (_a = project.id) != null ? _a : `task-project-${Date.now()}`,
      name: project.name || "\u672A\u547D\u540D\u9879\u76EE",
      description: (_b = project.description) != null ? _b : "",
      createdAt: (_c = project.createdAt) != null ? _c : nowIso()
    };
  }
  normalizeTaskStatus(status) {
    if (status === "inbox" || status === "todo" || status === "doing" || status === "waiting" || status === "done" || status === "cancelled") return status;
    return "inbox";
  }
  normalizeTaskPriority(priority) {
    if (priority === "high" || priority === "medium" || priority === "low" || priority === "none") return priority;
    return "none";
  }
  normalizeRecurrenceFrequency(frequency) {
    if (frequency === "daily" || frequency === "weekly" || frequency === "monthly" || frequency === "custom") return frequency;
    return "daily";
  }
  nextRecurrenceDate(date, frequency, interval = 1) {
    const next = /* @__PURE__ */ new Date(`${date}T00:00:00`);
    const step = Math.max(1, Number(interval) || 1);
    if (frequency === "weekly") next.setDate(next.getDate() + 7 * step);
    else if (frequency === "monthly") next.setMonth(next.getMonth() + step);
    else next.setDate(next.getDate() + step);
    return formatDateKey(next);
  }
  normalizeBook(book) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i;
    const readingStatus = (_a = book.readingStatus) != null ? _a : this.readingStatusFromLegacy(book.status);
    const timestamp = (_b = book.createdAt) != null ? _b : nowIso();
    const tagIds = Array.from(/* @__PURE__ */ new Set([
      ...Array.isArray(book.tagIds) ? book.tagIds : [],
      ...Array.isArray(book.tags) ? book.tags.map((tag) => this.definitionId("book-tag", tag)) : []
    ]));
    const tagNames2 = tagIds.map((id) => {
      var _a2, _b2, _c2, _d2, _e2;
      return (_e2 = (_c2 = (_b2 = (_a2 = this.data) == null ? void 0 : _a2.bookTags) == null ? void 0 : _b2.find((tag) => tag.id === id)) == null ? void 0 : _c2.name) != null ? _e2 : (_d2 = DEFAULT_DATA.bookTags.find((tag) => tag.id === id)) == null ? void 0 : _d2.name;
    }).filter(Boolean);
    const normalized = {
      ...book,
      id: (_c = book.id) != null ? _c : `book-${Date.now()}`,
      title: book.title || "\u672A\u547D\u540D\u4E66\u7C4D",
      author: book.author || "\u672A\u77E5\u4F5C\u8005",
      totalPages: Math.max(1, Number(book.totalPages) || 1),
      currentPage: Math.max(0, Math.min(Number(book.currentPage) || 0, Math.max(1, Number(book.totalPages) || 1))),
      status: this.legacyBookStatus(readingStatus),
      readingStatus,
      shelfStatus: (_d = book.shelfStatus) != null ? _d : "on-shelf",
      coverPath: typeof book.coverPath === "string" && book.coverPath.length > 0 ? book.coverPath : void 0,
      coverUrl: typeof ((_e = book.coverUrl) != null ? _e : book.cover) === "string" && ((_g = (_f = book.coverUrl) != null ? _f : book.cover) == null ? void 0 : _g.length) ? (_h = book.coverUrl) != null ? _h : book.cover : void 0,
      bookFilePath: typeof book.bookFilePath === "string" && book.bookFilePath.length > 0 ? book.bookFilePath : void 0,
      notePath: typeof book.notePath === "string" && book.notePath.length > 0 ? book.notePath : void 0,
      startDate: book.startDate,
      finishDate: readingStatus === "finished" ? book.finishDate : book.finishDate,
      createdAt: timestamp,
      updatedAt: (_i = book.updatedAt) != null ? _i : timestamp,
      tagIds,
      tags: tagNames2.length > 0 ? tagNames2 : Array.isArray(book.tags) ? book.tags : []
    };
    if (normalized.readingStatus === "reading") normalized.startDate = normalized.startDate || formatDateKey(/* @__PURE__ */ new Date());
    if (normalized.readingStatus === "finished") {
      normalized.currentPage = normalized.totalPages;
      normalized.startDate = normalized.startDate || formatDateKey(/* @__PURE__ */ new Date());
      normalized.finishDate = normalized.finishDate || formatDateKey(/* @__PURE__ */ new Date());
    }
    return normalized;
  }
  normalizeWantToReadItem(item) {
    var _a, _b, _c, _d, _e;
    const timestamp = (_a = item.createdAt) != null ? _a : nowIso();
    return {
      id: (_b = item.id) != null ? _b : `want-read-${Date.now()}`,
      title: item.title || "\u672A\u547D\u540D\u4E66\u7C4D",
      author: (_c = item.author) != null ? _c : "",
      summary: (_d = item.summary) != null ? _d : "",
      status: item.status === "added" ? "added" : "pending",
      bookId: item.bookId,
      createdAt: timestamp,
      updatedAt: (_e = item.updatedAt) != null ? _e : timestamp
    };
  }
  normalizeBookTag(tag, index = 0) {
    var _a;
    return {
      id: tag.id || this.definitionId("book-tag", tag.name || `\u6807\u7B7E${index + 1}`),
      name: tag.name || `\u6807\u7B7E${index + 1}`,
      color: tag.color || "#f8a8c4",
      order: (_a = tag.order) != null ? _a : (index + 1) * 10
    };
  }
  normalizeReadingQuote(quote) {
    var _a, _b, _c, _d, _e;
    return {
      id: (_a = quote.id) != null ? _a : `quote-${Date.now()}`,
      text: quote.text || "",
      source: (_b = quote.source) != null ? _b : "",
      bookId: quote.bookId,
      note: (_c = quote.note) != null ? _c : "",
      style: (_d = quote.style) != null ? _d : "default",
      backgroundColor: (_e = quote.backgroundColor) != null ? _e : "#fff7fb"
    };
  }
  normalizeResearchPaper(paper) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
    const now = Date.now();
    const statusName2 = typeof paper.status === "string" ? paper.status : "\u672A\u8BFB";
    const venueName2 = typeof paper.venue === "string" ? paper.venue : "";
    const tagNames2 = Array.isArray(paper.tags) ? paper.tags : [];
    const statusId = paper.statusId || this.paperStatusIdFromName(statusName2);
    const venueId = paper.venueId || (venueName2 ? this.definitionId("venue", venueName2) : void 0);
    const tagIds = Array.from(/* @__PURE__ */ new Set([
      ...Array.isArray(paper.tagIds) ? paper.tagIds : [],
      ...tagNames2.map((tag) => this.definitionId("paper-tag", tag))
    ]));
    const statusDefinition = (_c = (_b = (_a = this.data) == null ? void 0 : _a.paperStatuses) == null ? void 0 : _b.find((item) => item.id === statusId)) != null ? _c : DEFAULT_DATA.paperStatuses.find((item) => item.id === statusId);
    return {
      ...paper,
      id: (_d = paper.id) != null ? _d : `paper-${now}`,
      title: paper.title || "\u672A\u547D\u540D\u8BBA\u6587",
      venue: venueName2 || ((_g = (_f = (_e = this.data) == null ? void 0 : _e.paperVenues) == null ? void 0 : _f.find((item) => item.id === venueId)) == null ? void 0 : _g.name) || "",
      venueId,
      year: Number(paper.year) || (/* @__PURE__ */ new Date()).getFullYear(),
      status: (_h = statusDefinition == null ? void 0 : statusDefinition.name) != null ? _h : statusName2,
      statusId,
      readingProgress: Math.max(0, Math.min(100, Number(paper.readingProgress) || 0)),
      paperUrl: paper.paperUrl,
      doi: paper.doi,
      readingStartDate: paper.readingStartDate,
      readingEndDate: paper.readingEndDate,
      researchProjectId: paper.researchProjectId,
      tags: tagNames2,
      tagIds,
      notePath: paper.notePath,
      createdAt: (_i = paper.createdAt) != null ? _i : now,
      updatedAt: (_j = paper.updatedAt) != null ? _j : now,
      zoteroItemKey: paper.zoteroItemKey,
      citekey: paper.citekey
    };
  }
  normalizeResearchProject(project) {
    var _a;
    const tagIds = Array.from(/* @__PURE__ */ new Set([
      ...Array.isArray(project.tagIds) ? project.tagIds : [],
      ...(Array.isArray(project.tags) ? project.tags : []).map((tag) => {
        var _a2, _b, _c;
        return (_c = (_b = (_a2 = this.data) == null ? void 0 : _a2.paperTags) == null ? void 0 : _b.find((definition) => definition.name.toLowerCase() === tag.toLowerCase())) == null ? void 0 : _c.id;
      }).filter(Boolean)
    ]));
    return {
      ...project,
      title: project.title || "\u672A\u547D\u540D\u9879\u76EE",
      description: (_a = project.description) != null ? _a : "",
      progress: Math.max(0, Math.min(100, Number(project.progress) || 0)),
      tags: Array.isArray(project.tags) ? project.tags : [],
      tagIds
    };
  }
  normalizeLiteratureNote(note) {
    var _a, _b, _c, _d;
    const now = Date.now();
    return {
      id: (_a = note.id) != null ? _a : `literature-note-${now}`,
      title: note.title || "\u6587\u732E\u7B14\u8BB0",
      notePath: typeof note.notePath === "string" ? note.notePath : "",
      paperReadingId: note.paperReadingId,
      createdAt: (_b = note.createdAt) != null ? _b : now,
      updatedAt: (_d = (_c = note.updatedAt) != null ? _c : note.createdAt) != null ? _d : now
    };
  }
  normalizeExperiment(experiment, mode) {
    var _a;
    return {
      ...experiment,
      id: (_a = experiment.id) != null ? _a : `experiment-${Date.now()}`,
      title: experiment.title || "\u672A\u547D\u540D\u5B9E\u9A8C",
      date: experiment.date || todayKey(),
      status: experiment.status || "\u8BA1\u5212\u4E2D",
      notePath: typeof experiment.notePath === "string" && experiment.notePath.length > 0 ? experiment.notePath : void 0,
      researchProjectId: mode === "plan" ? experiment.researchProjectId : void 0,
      experimentPlanId: mode === "records" ? experiment.experimentPlanId : void 0
    };
  }
  repairResearchRelations(data) {
    const projectIds = new Set(data.researchProjects.map((project) => project.id));
    const paperIds = new Set(data.researchPapers.map((paper) => paper.id));
    const planIds = new Set(data.experimentPlans.map((plan) => plan.id));
    data.researchPapers.forEach((paper) => {
      if (paper.researchProjectId && !projectIds.has(paper.researchProjectId)) paper.researchProjectId = void 0;
    });
    data.experimentPlans.forEach((plan) => {
      if (plan.researchProjectId && !projectIds.has(plan.researchProjectId)) plan.researchProjectId = void 0;
    });
    data.literatureNotes.forEach((note) => {
      if (note.paperReadingId && !paperIds.has(note.paperReadingId)) note.paperReadingId = void 0;
    });
    data.experimentRecords.forEach((record) => {
      if (record.experimentPlanId && !planIds.has(record.experimentPlanId)) record.experimentPlanId = void 0;
    });
    return data;
  }
  repairBookTagRelations(data) {
    const tagIds = new Set(data.bookTags.map((tag) => tag.id));
    data.books.forEach((book) => {
      var _a, _b;
      book.tagIds = ((_a = book.tagIds) != null ? _a : []).filter((id) => tagIds.has(id));
      book.tags = ((_b = book.tagIds) != null ? _b : []).map((id) => {
        var _a2;
        return (_a2 = data.bookTags.find((tag) => tag.id === id)) == null ? void 0 : _a2.name;
      }).filter(Boolean);
    });
    return data;
  }
  repairFitnessRelations(data) {
    const goalIds = new Set(data.fitnessGoals.map((goal) => goal.id));
    const planIds = new Set(data.trainingPlans.map((plan) => plan.id));
    data.trainingPlans.forEach((plan) => {
      if (plan.fitnessGoalId && !goalIds.has(plan.fitnessGoalId)) plan.fitnessGoalId = void 0;
    });
    data.workouts.forEach((workout) => {
      if (workout.trainingPlanId && !planIds.has(workout.trainingPlanId)) workout.trainingPlanId = void 0;
      if (workout.fitnessGoalId && !goalIds.has(workout.fitnessGoalId)) workout.fitnessGoalId = void 0;
    });
    return data;
  }
  createInitialLiteratureNotes(partial) {
    if (!Array.isArray(partial.researchPapers)) {
      return structuredClone(DEFAULT_DATA.literatureNotes).map((note) => this.normalizeLiteratureNote(note)).filter((note) => note.notePath);
    }
    return partial.researchPapers.filter((paper) => typeof paper.notePath === "string" && paper.notePath.length > 0).map((paper) => {
      var _a, _b, _c;
      return this.normalizeLiteratureNote({
        id: `literature-note-${paper.id}`,
        title: `${paper.title || "\u6587\u732E"} \u7B14\u8BB0`,
        notePath: (_a = paper.notePath) != null ? _a : "",
        paperReadingId: paper.id,
        createdAt: (_b = paper.createdAt) != null ? _b : Date.now(),
        updatedAt: (_c = paper.updatedAt) != null ? _c : Date.now()
      });
    });
  }
  mergePaperStatuses(partial) {
    const map = /* @__PURE__ */ new Map();
    structuredClone(DEFAULT_DATA.paperStatuses).forEach((item) => map.set(item.id, item));
    if (Array.isArray(partial.paperStatuses)) {
      partial.paperStatuses.forEach((item) => map.set(item.id, item));
    }
    if (Array.isArray(partial.researchPapers)) {
      partial.researchPapers.forEach((paper) => {
        if (paper.status && !map.has(this.paperStatusIdFromName(paper.status))) {
          const id = this.paperStatusIdFromName(paper.status);
          map.set(id, { id, name: paper.status, color: "#f8a8c4", order: map.size * 10 + 10 });
        }
      });
    }
    return [...map.values()].sort((a, b) => a.order - b.order);
  }
  mergePaperVenues(partial) {
    const map = /* @__PURE__ */ new Map();
    structuredClone(DEFAULT_DATA.paperVenues).forEach((item) => map.set(item.id, item));
    if (Array.isArray(partial.paperVenues)) {
      partial.paperVenues.forEach((item) => map.set(item.id, item));
    }
    if (Array.isArray(partial.researchPapers)) {
      partial.researchPapers.forEach((paper) => {
        if (!paper.venue) return;
        const id = paper.venueId || this.definitionId("venue", paper.venue);
        if (!map.has(id)) map.set(id, { id, name: paper.venue, type: "other", color: "#76c7f2" });
      });
    }
    return [...map.values()];
  }
  mergePaperTags(partial) {
    const map = /* @__PURE__ */ new Map();
    structuredClone(DEFAULT_DATA.paperTags).forEach((item) => map.set(item.id, item));
    if (Array.isArray(partial.paperTags)) {
      partial.paperTags.forEach((item) => map.set(item.id, item));
    }
    if (Array.isArray(partial.researchPapers)) {
      partial.researchPapers.forEach((paper) => {
        var _a;
        ((_a = paper.tags) != null ? _a : []).forEach((tag) => {
          const id = this.definitionId("paper-tag", tag);
          if (!map.has(id)) map.set(id, { id, name: tag, color: "#ffd166" });
        });
      });
    }
    return [...map.values()];
  }
  mergeBookTags(partial) {
    const map = /* @__PURE__ */ new Map();
    structuredClone(DEFAULT_DATA.bookTags).forEach((item, index) => map.set(item.id, this.normalizeBookTag(item, index)));
    if (Array.isArray(partial.bookTags)) {
      partial.bookTags.forEach((item, index) => map.set(item.id, this.normalizeBookTag(item, index)));
    }
    if (Array.isArray(partial.books)) {
      partial.books.forEach((book) => {
        var _a;
        ((_a = book.tags) != null ? _a : []).forEach((name) => {
          const id = this.definitionId("book-tag", name);
          if (!map.has(id)) map.set(id, { id, name, color: "#f8a8c4", order: map.size * 10 + 10 });
        });
      });
    }
    return [...map.values()].sort((a, b) => {
      var _a, _b;
      return ((_a = a.order) != null ? _a : 0) - ((_b = b.order) != null ? _b : 0);
    });
  }
  createInitialWantToReadItems(partial) {
    if (!Array.isArray(partial.books)) return structuredClone(DEFAULT_DATA.wantToReadItems).map((item) => this.normalizeWantToReadItem(item));
    return partial.books.map((book) => this.normalizeBook(book)).filter((book) => book.readingStatus === "want-to-read").map((book) => {
      var _a, _b, _c, _d;
      return this.normalizeWantToReadItem({
        id: `want-read-${book.id}`,
        title: book.title,
        author: book.author,
        summary: (_a = book.description) != null ? _a : "",
        status: "pending",
        createdAt: (_b = book.createdAt) != null ? _b : nowIso(),
        updatedAt: (_d = (_c = book.updatedAt) != null ? _c : book.createdAt) != null ? _d : nowIso()
      });
    });
  }
  syncLegacyBookTags() {
    this.data.books.forEach((book) => {
      const ids = Array.isArray(book.tagIds) ? book.tagIds : [];
      book.tags = ids.map((id) => {
        var _a;
        return (_a = this.data.bookTags.find((tag) => tag.id === id)) == null ? void 0 : _a.name;
      }).filter(Boolean);
    });
  }
  paperStatusIdFromName(name) {
    if (name === "\u672A\u5F00\u59CB" || name === "\u672A\u8BFB") return "paper-status-unread";
    if (name === "\u8BA1\u5212\u9605\u8BFB") return "paper-status-planned";
    if (name === "\u8FDB\u884C\u4E2D" || name === "\u9605\u8BFB\u4E2D") return "paper-status-reading";
    if (name === "\u5DF2\u5B8C\u6210" || name === "\u5DF2\u8BFB") return "paper-status-finished";
    if (name === "\u6682\u505C") return "paper-status-paused";
    return this.definitionId("paper-status", name);
  }
  definitionId(prefix, name) {
    const slug = name.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "");
    if (slug) return `${prefix}-${slug}`;
    let hash = 0;
    for (let index = 0; index < name.length; index += 1) {
      hash = (hash << 5) - hash + name.charCodeAt(index) | 0;
    }
    return `${prefix}-${Math.abs(hash)}`;
  }
  findExistingZoteroPaper(item) {
    return this.data.researchPapers.find((paper) => {
      var _a;
      if (item.zoteroItemKey && paper.zoteroItemKey === item.zoteroItemKey) return true;
      if (item.citekey && paper.citekey === item.citekey) return true;
      if (item.doi && ((_a = paper.doi) == null ? void 0 : _a.toLowerCase()) === item.doi.toLowerCase()) return true;
      if (item.paperUrl && paper.paperUrl === item.paperUrl) return true;
      return false;
    });
  }
  async ensurePaperVenue(name) {
    const existing = this.data.paperVenues.find((venue) => venue.name.toLowerCase() === name.toLowerCase());
    if (existing) return existing.id;
    const id = this.definitionId("venue", name);
    this.data.paperVenues.push({ id, name, type: "other", color: "#76c7f2" });
    return id;
  }
  async ensurePaperTags(tags) {
    return tags.filter(Boolean).map((name) => {
      const existing = this.data.paperTags.find((tag) => tag.name.toLowerCase() === name.toLowerCase());
      if (existing) return existing.id;
      const id = this.definitionId("paper-tag", name);
      this.data.paperTags.push({ id, name, color: "#ffd166" });
      return id;
    });
  }
  readingStatusFromLegacy(status) {
    if (status === "\u5728\u8BFB") return "reading";
    if (status === "\u5DF2\u8BFB") return "finished";
    return "want-to-read";
  }
  legacyBookStatus(status) {
    if (status === "reading") return "\u5728\u8BFB";
    if (status === "finished") return "\u5DF2\u8BFB";
    return "\u60F3\u8BFB";
  }
  normalizeReadingPlan(plan) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
    const timestamp = (_a = plan.createdAt) != null ? _a : nowIso();
    const today6 = todayKey();
    const startDate = plan.startDate || today6;
    const endDate = plan.endDate && plan.endDate >= startDate ? plan.endDate : startDate;
    const progress = Math.max(0, Math.min(100, Number((_b = plan.progress) != null ? _b : plan.status === "completed" ? 100 : 0) || 0));
    const completedDate = (_d = plan.completedDate) != null ? _d : (_c = plan.completedAt) == null ? void 0 : _c.slice(0, 10);
    const status = progress >= 100 || plan.status === "completed" || completedDate ? "completed" : (_e = plan.status) != null ? _e : "planned";
    const completedAt = status === "completed" ? (_f = plan.completedAt) != null ? _f : `${completedDate != null ? completedDate : today6}T00:00:00.000Z` : void 0;
    return {
      ...plan,
      id: (_g = plan.id) != null ? _g : `reading-plan-${Date.now()}`,
      bookId: plan.bookId,
      startDate,
      endDate,
      targetPages: plan.targetPages === void 0 ? void 0 : Math.max(0, Number(plan.targetPages) || 0),
      goal: (_h = plan.goal) != null ? _h : plan.targetPages ? `${plan.targetPages} \u9875` : "\u8BFB\u5B8C\u672C\u4E66",
      progress,
      note: (_i = plan.note) != null ? _i : "",
      status,
      completedDate: status === "completed" ? completedDate != null ? completedDate : today6 : void 0,
      completedAt,
      createdAt: timestamp,
      updatedAt: (_j = plan.updatedAt) != null ? _j : timestamp
    };
  }
  withComputedReadingPlanStatus(plan) {
    var _a, _b;
    const completedDate = (_b = plan.completedDate) != null ? _b : (_a = plan.completedAt) == null ? void 0 : _a.slice(0, 10);
    if (plan.progress === 100 || plan.status === "completed" || completedDate) {
      return { ...plan, status: "completed", completedDate: completedDate != null ? completedDate : todayKey() };
    }
    const today6 = todayKey();
    if (today6 < plan.startDate) return { ...plan, status: "planned" };
    if (today6 > plan.endDate) return { ...plan, status: "overdue" };
    return { ...plan, status: "active" };
  }
  normalizeReadingNote(note) {
    var _a, _b, _c;
    const timestamp = (_a = note.createdAt) != null ? _a : nowIso();
    return {
      id: (_b = note.id) != null ? _b : `reading-note-${Date.now()}`,
      title: note.title || this.fileName(note.notePath) || "\u9605\u8BFB\u7B14\u8BB0",
      notePath: typeof note.notePath === "string" ? note.notePath : "",
      bookId: note.bookId,
      createdAt: timestamp,
      updatedAt: (_c = note.updatedAt) != null ? _c : timestamp
    };
  }
  createInitialReadingPlans(partial) {
    if (!Array.isArray(partial.books)) {
      return structuredClone(DEFAULT_DATA.readingPlans).map((plan) => this.normalizeReadingPlan(plan));
    }
    const timestamp = nowIso();
    return partial.books.map((book) => this.normalizeBook(book)).filter((book) => book.readingStatus === "reading").map((book) => this.normalizeReadingPlan({
      id: `reading-plan-${book.id}`,
      bookId: book.id,
      startDate: book.startDate || todayKey(),
      endDate: book.finishDate || todayKey(),
      targetPages: book.totalPages,
      goal: "\u8BFB\u5B8C\u6574\u672C\u4E66",
      progress: book.totalPages > 0 ? Math.round(book.currentPage / book.totalPages * 100) : 0,
      note: "\u7531\u65E7\u9605\u8BFB\u72B6\u6001\u8FC1\u79FB\u751F\u6210\u3002",
      status: "active",
      createdAt: timestamp,
      updatedAt: timestamp
    }));
  }
  createInitialReadingNotes(partial) {
    const books = Array.isArray(partial.books) ? partial.books : DEFAULT_DATA.books;
    return books.map((book) => this.normalizeBook(book)).filter((book) => typeof book.notePath === "string" && book.notePath.length > 0).map((book) => {
      var _a, _b, _c, _d;
      return this.normalizeReadingNote({
        id: `reading-note-${book.id}`,
        title: `${book.title} \u9605\u8BFB\u7B14\u8BB0`,
        notePath: (_a = book.notePath) != null ? _a : "",
        bookId: book.id,
        createdAt: (_b = book.createdAt) != null ? _b : nowIso(),
        updatedAt: (_d = (_c = book.updatedAt) != null ? _c : book.createdAt) != null ? _d : nowIso()
      });
    });
  }
  fileName(path) {
    var _a, _b;
    return (_b = (_a = path.split(/[\\/]/).pop()) == null ? void 0 : _a.replace(/\.md$/i, "")) != null ? _b : "";
  }
  createInitialGoalActions(partial) {
    const goals = Array.isArray(partial.goals) ? partial.goals : DEFAULT_DATA.goals;
    const fallbackGoal = goals[0];
    const actions = [];
    goals.forEach((goal, index) => {
      actions.push(this.normalizeGoalAction({
        id: `goal-action-root-${goal.id}`,
        goalId: goal.id,
        title: `${goal.title} \u62C6\u89E3`,
        description: goal.description,
        status: goal.status === "\u5DF2\u5B8C\u6210" ? "completed" : goal.status === "\u672A\u5F00\u59CB" ? "todo" : "in-progress",
        deadline: goal.deadline,
        progress: goal.progress,
        note: "",
        createdAt: Date.now() + index,
        updatedAt: Date.now() + index
      }));
    });
    if (Array.isArray(partial.milestones)) {
      partial.milestones.forEach((milestone, index) => {
        actions.push(this.normalizeGoalAction({
          id: `goal-action-milestone-${milestone.id}`,
          goalId: milestone.goalId || (fallbackGoal == null ? void 0 : fallbackGoal.id) || "goal-default",
          title: milestone.title,
          status: milestone.status === "\u5DF2\u5B8C\u6210" ? "completed" : milestone.status === "\u672A\u5F00\u59CB" ? "todo" : "in-progress",
          deadline: milestone.date,
          progress: milestone.status === "\u5DF2\u5B8C\u6210" ? 100 : 0,
          isMilestone: true,
          milestoneDate: milestone.date,
          note: "",
          createdAt: Date.now() + 100 + index,
          updatedAt: Date.now() + 100 + index
        }));
      });
    }
    if (Array.isArray(partial.priorityMatrixItems)) {
      partial.priorityMatrixItems.forEach((item, index) => {
        actions.push(this.normalizeGoalAction({
          id: `goal-action-priority-${item.id}`,
          goalId: (fallbackGoal == null ? void 0 : fallbackGoal.id) || "goal-default",
          title: item.title,
          status: item.completed ? "completed" : "todo",
          progress: item.completed ? 100 : 0,
          importance: item.quadrant.includes("important") && !item.quadrant.includes("not-important") ? "important" : "not-important",
          urgency: item.quadrant.includes("urgent") && !item.quadrant.includes("not-urgent") ? "urgent" : "not-urgent",
          note: item.note,
          createdAt: Date.now() + 200 + index,
          updatedAt: Date.now() + 200 + index
        }));
      });
    }
    return actions;
  }
  syncGoalActionSegments(action) {
    const normalized = this.normalizeGoalAction(action);
    const existingMonthly = new Map(this.data.keyResults.filter((item) => item.autoGenerated && item.sourceGoalActionId === normalized.id).map((item) => [`${item.segmentStartDate}|${item.segmentEndDate}`, item]));
    const existingQuarterly = new Map(this.data.objectives.filter((item) => item.autoGenerated && item.sourceGoalActionId === normalized.id).map((item) => [`${item.segmentStartDate}|${item.segmentEndDate}`, item]));
    this.removeAutoSegmentsForGoalAction(normalized.id);
    if (!normalized.startDate || !normalized.deadline) return;
    if (normalized.durationDays && normalized.durationDays <= 30) {
      this.getMonthSegments(normalized.startDate, normalized.deadline).forEach((segment, index) => {
        var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
        const key = `${segment.startDate}|${segment.endDate}`;
        const previous = existingMonthly.get(key);
        this.data.keyResults.push(this.normalizeKeyResult({
          id: (_a = previous == null ? void 0 : previous.id) != null ? _a : `kr-auto-${normalized.id}-${segment.year}-${segment.month}-${index}`,
          objectiveId: (_b = previous == null ? void 0 : previous.objectiveId) != null ? _b : "",
          title: normalized.title,
          progress: normalized.status === "completed" ? 100 : (_e = (_d = (_c = previous == null ? void 0 : previous.segmentProgress) != null ? _c : previous == null ? void 0 : previous.progress) != null ? _d : normalized.progress) != null ? _e : 0,
          completed: normalized.status === "completed",
          completedDate: normalized.status === "completed" ? (_f = normalized.completedDate) != null ? _f : formatDateKey(/* @__PURE__ */ new Date()) : void 0,
          year: segment.year,
          month: segment.month,
          autoGenerated: true,
          sourceGoalActionId: normalized.id,
          segmentType: "monthly",
          segmentStartDate: segment.startDate,
          segmentEndDate: segment.endDate,
          segmentNote: (_g = previous == null ? void 0 : previous.segmentNote) != null ? _g : "",
          segmentProgress: normalized.status === "completed" ? 100 : (_j = (_i = (_h = previous == null ? void 0 : previous.segmentProgress) != null ? _h : previous == null ? void 0 : previous.progress) != null ? _i : normalized.progress) != null ? _j : 0,
          updatedAt: Date.now()
        }));
      });
      return;
    }
    this.getQuarterSegments(normalized.startDate, normalized.deadline).forEach((segment, index) => {
      var _a, _b, _c, _d, _e, _f, _g, _h, _i;
      const key = `${segment.startDate}|${segment.endDate}`;
      const previous = existingQuarterly.get(key);
      this.data.objectives.push(this.normalizeObjective({
        id: (_a = previous == null ? void 0 : previous.id) != null ? _a : `objective-auto-${normalized.id}-${segment.year}-q${segment.quarterNumber}-${index}`,
        title: normalized.title,
        quarter: `${segment.year} Q${segment.quarterNumber}`,
        year: segment.year,
        quarterNumber: segment.quarterNumber,
        progress: normalized.status === "completed" ? 100 : (_d = (_c = (_b = previous == null ? void 0 : previous.segmentProgress) != null ? _b : previous == null ? void 0 : previous.progress) != null ? _c : normalized.progress) != null ? _d : 0,
        completedDate: normalized.status === "completed" ? (_e = normalized.completedDate) != null ? _e : formatDateKey(/* @__PURE__ */ new Date()) : void 0,
        autoGenerated: true,
        sourceGoalActionId: normalized.id,
        segmentType: "quarterly",
        segmentStartDate: segment.startDate,
        segmentEndDate: segment.endDate,
        segmentNote: (_f = previous == null ? void 0 : previous.segmentNote) != null ? _f : "",
        segmentProgress: normalized.status === "completed" ? 100 : (_i = (_h = (_g = previous == null ? void 0 : previous.segmentProgress) != null ? _g : previous == null ? void 0 : previous.progress) != null ? _h : normalized.progress) != null ? _i : 0,
        updatedAt: Date.now()
      }));
    });
  }
  syncGoalActionWithAncestors(action) {
    let current = action;
    const seen = /* @__PURE__ */ new Set();
    while (current && !seen.has(current.id)) {
      seen.add(current.id);
      this.syncGoalActionSegments(this.withGoalActionComputedState(current));
      current = current.parentId ? this.data.goalActions.find((item) => item.id === (current == null ? void 0 : current.parentId)) : void 0;
    }
  }
  removeAutoSegmentsForGoalAction(actionId) {
    this.data.keyResults = this.data.keyResults.filter((item) => !(item.autoGenerated && item.sourceGoalActionId === actionId));
    this.data.objectives = this.data.objectives.filter((item) => !(item.autoGenerated && item.sourceGoalActionId === actionId));
  }
  getNaturalDurationDays(startDate, deadline) {
    const start = this.parseDateOnly(startDate);
    const end = this.parseDateOnly(deadline);
    if (!start || !end) return 1;
    return Math.max(1, Math.floor((end.getTime() - start.getTime()) / 864e5) + 1);
  }
  getMonthSegments(startDate, deadline) {
    const start = this.parseDateOnly(startDate);
    const end = this.parseDateOnly(deadline);
    if (!start || !end) return [];
    const segments = [];
    const cursor = new Date(start.getFullYear(), start.getMonth(), 1);
    while (cursor <= end) {
      const year = cursor.getFullYear();
      const month = cursor.getMonth() + 1;
      const monthStart = new Date(year, cursor.getMonth(), 1);
      const monthEnd = new Date(year, cursor.getMonth() + 1, 0);
      const segmentStart = start > monthStart ? start : monthStart;
      const segmentEnd = end < monthEnd ? end : monthEnd;
      segments.push({ year, month, startDate: formatDateKey(segmentStart), endDate: formatDateKey(segmentEnd) });
      cursor.setMonth(cursor.getMonth() + 1);
    }
    return segments;
  }
  getQuarterSegments(startDate, deadline) {
    const start = this.parseDateOnly(startDate);
    const end = this.parseDateOnly(deadline);
    if (!start || !end) return [];
    const segments = [];
    const cursor = new Date(start.getFullYear(), Math.floor(start.getMonth() / 3) * 3, 1);
    while (cursor <= end) {
      const year = cursor.getFullYear();
      const quarterNumber = this.getQuarter(cursor);
      const quarterStart = new Date(year, (quarterNumber - 1) * 3, 1);
      const quarterEnd = new Date(year, quarterNumber * 3, 0);
      const segmentStart = start > quarterStart ? start : quarterStart;
      const segmentEnd = end < quarterEnd ? end : quarterEnd;
      segments.push({ year, quarterNumber, startDate: formatDateKey(segmentStart), endDate: formatDateKey(segmentEnd) });
      cursor.setMonth(cursor.getMonth() + 3);
    }
    return segments;
  }
  getQuarter(date) {
    return Math.floor(date.getMonth() / 3) + 1;
  }
  parseDateOnly(value) {
    const match = value.match(/^(\d{4})-(\d{2})-(\d{2})$/);
    if (!match) return null;
    return new Date(Number(match[1]), Number(match[2]) - 1, Number(match[3]));
  }
  normalizeInvestmentWatchItem(item) {
    var _a, _b;
    return {
      id: (_a = item.id) != null ? _a : `watch-${Date.now()}`,
      name: item.name || "\u6295\u8D44\u89C2\u5BDF",
      code: item.code || "",
      price: Number(item.price) || 0,
      changePercent: Number(item.changePercent) || 0,
      type: item.type || "\u5176\u5B83",
      note: (_b = item.note) != null ? _b : ""
    };
  }
  createInitialInvestmentSnapshots(items) {
    const date = todayKey();
    const createdAt = nowIso();
    return items.map((item) => {
      var _a;
      return {
        id: `snapshot-${item.id}-${date}`,
        investmentId: item.id,
        date,
        price: Number(item.price) || 0,
        changePercent: Number(item.changePercent) || 0,
        note: (_a = item.note) != null ? _a : "",
        createdAt
      };
    });
  }
  migrateSections(sections) {
    const pages = ["overview", "research", "reading", "fitness", "finance", "goals", "modules"];
    const migrated = sections.filter((section) => section.type !== "module-settings");
    pages.forEach((page) => {
      if (!sections.some((section) => section.page === page)) {
        migrated.push(...structuredClone(DEFAULT_DATA.sections.filter((section) => section.page === page)));
      }
    });
    migrated.forEach((section) => {
      if (section.type === "water-sleep-habits") {
        section.title = "\u4E60\u60EF";
      }
      if (section.type === "quarterly-okr") {
        section.title = "\u5B63\u5EA6\u76EE\u6807";
      }
      if (section.type === "monthly-key-results") {
        section.title = "\u6708\u5EA6\u76EE\u6807";
      }
    });
    const hasNewOverviewLayout = migrated.some((section) => section.type === "weekly-completion");
    if (!hasNewOverviewLayout) {
      return this.withRequiredSections([
        ...structuredClone(DEFAULT_DATA.sections.filter((section) => section.page === "overview")),
        ...migrated.filter((section) => section.page !== "overview")
      ]);
    }
    return this.withRequiredSections(migrated);
  }
  migrateModuleLayouts(partial) {
    var _a, _b, _c;
    const legacy = partial;
    const fallbackMode = (_c = (_b = legacy.layoutMode) != null ? _b : (_a = partial.userSettings) == null ? void 0 : _a.overviewLayout) != null ? _c : DEFAULT_DATA.userSettings.overviewLayout;
    const layouts = structuredClone(DEFAULT_MODULE_LAYOUTS);
    Object.keys(layouts).forEach((page) => {
      var _a2;
      const saved = (_a2 = partial.moduleLayouts) == null ? void 0 : _a2[page];
      layouts[page] = this.normalizeModuleLayout(saved != null ? saved : { ...layouts[page], mode: fallbackMode });
    });
    return layouts;
  }
  normalizeModuleLayout(layout) {
    var _a;
    const mode = (layout == null ? void 0 : layout.mode) === "compact" || (layout == null ? void 0 : layout.mode) === "minimal" || (layout == null ? void 0 : layout.mode) === "custom" ? layout.mode : "default";
    const requestedColumns = Number(layout == null ? void 0 : layout.columns) || this.columnsForLayoutMode(mode);
    const columns = mode === "minimal" ? 1 : mode === "default" ? 12 : mode === "compact" ? 16 : Math.max(2, Math.min(12, requestedColumns));
    const sections = {};
    Object.entries((_a = layout == null ? void 0 : layout.sections) != null ? _a : {}).forEach(([sectionId, sectionLayout]) => {
      sections[sectionId] = {
        order: sectionLayout.order,
        colSpan: sectionLayout.colSpan === void 0 ? void 0 : Math.max(1, Math.min(columns, Number(sectionLayout.colSpan) || 1)),
        rowSpan: sectionLayout.rowSpan === void 0 ? void 0 : Math.max(1, Math.min(12, Number(sectionLayout.rowSpan) || 1))
      };
    });
    return {
      mode,
      columns,
      templateId: typeof (layout == null ? void 0 : layout.templateId) === "string" ? layout.templateId : void 0,
      sections
    };
  }
  ensureSectionLayout(page, sectionId, fallbackOrder) {
    var _a, _b;
    const layout = (_a = this.data.moduleLayouts[page]) != null ? _a : structuredClone(DEFAULT_MODULE_LAYOUTS[page]);
    layout.sections = (_b = layout.sections) != null ? _b : {};
    layout.sections[sectionId] = {
      order: fallbackOrder,
      colSpan: 1,
      rowSpan: 1,
      ...layout.sections[sectionId]
    };
    this.data.moduleLayouts[page] = this.normalizeModuleLayout(layout);
  }
  getSectionLayoutOrder(section, layout) {
    var _a, _b, _c;
    return (_c = (_b = (_a = layout.sections) == null ? void 0 : _a[section.id]) == null ? void 0 : _b.order) != null ? _c : section.order;
  }
  columnsForLayoutMode(mode) {
    if (mode === "compact") return 16;
    if (mode === "minimal") return 1;
    return 12;
  }
  normalizeFocusRecord(record) {
    var _a, _b, _c, _d, _e, _f, _g;
    const createdAt = (_b = (_a = record.createdAt) != null ? _a : record.endedAt) != null ? _b : (/* @__PURE__ */ new Date()).toISOString();
    const date = (_c = record.date) != null ? _c : createdAt.slice(0, 10);
    const actualDuration = (_e = (_d = record.actualDurationMinutes) != null ? _d : record.duration) != null ? _e : 0;
    const plannedDuration = (_g = (_f = record.plannedDurationMinutes) != null ? _f : record.plannedDuration) != null ? _g : actualDuration;
    return {
      ...record,
      date,
      createdAt,
      duration: actualDuration,
      actualDurationMinutes: actualDuration,
      plannedDuration,
      plannedDurationMinutes: plannedDuration
    };
  }
  withRequiredSections(sections) {
    const migrated = [...sections];
    if (!migrated.some((section) => section.type === "section-manager")) {
      const sectionManager = DEFAULT_DATA.sections.find((section) => section.type === "section-manager");
      if (sectionManager) {
        migrated.push(structuredClone(sectionManager));
      }
    }
    if (!migrated.some((section) => section.page === "finance" && section.type === "finance-ledger")) {
      const ledger = DEFAULT_DATA.sections.find((section) => section.type === "finance-ledger");
      if (ledger) {
        migrated.push(structuredClone(ledger));
      }
    }
    if (!migrated.some((section) => section.page === "research" && section.type === "paper-field-manager")) {
      const paperFields = DEFAULT_DATA.sections.find((section) => section.type === "paper-field-manager");
      if (paperFields) {
        migrated.push(structuredClone(paperFields));
      }
    }
    return migrated;
  }
};
function formatDateKey(date) {
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, "0");
  const day = String(date.getDate()).padStart(2, "0");
  return `${year}-${month}-${day}`;
}

// src/settings/WorkbenchSettingTab.ts
var import_obsidian = require("obsidian");
var WorkbenchSettingTab = class extends import_obsidian.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
  }
  display() {
    const { containerEl } = this;
    containerEl.empty();
    containerEl.createEl("h2", { text: "Cute Obsidian Workbench" });
    new import_obsidian.Setting(containerEl).setName("Banner \u6587\u6848").setDesc("\u663E\u793A\u5728\u5DE5\u4F5C\u53F0\u9876\u90E8 Banner \u4E2D\u7684\u6FC0\u52B1\u6587\u6848\u3002").addText((text) => {
      text.setValue(this.plugin.store.getData().banner.message).onChange(async (value) => {
        await this.plugin.store.updateBannerMessage(value.trim() || "\u8981\u6210\u529F\uFF0C\u5148\u53D1\u75AF\uFF0C\u4E0D\u987E\u4E00\u5207\u5411\u524D\u51B2\u3002");
      });
    });
    new import_obsidian.Setting(containerEl).setName("Zotero / Better BibTeX JSON \u8DEF\u5F84").setDesc("\u586B\u5199 Vault \u5185 JSON \u6587\u4EF6\u8DEF\u5F84\uFF0C\u4F8B\u5982 Zotero/library.json\u3002\u7528\u4E8E\u8BBA\u6587\u961F\u5217\u5BFC\u5165\uFF0C\u4E0D\u4F9D\u8D56 Zotero Integration \u79C1\u6709 API\u3002").addText((text) => {
      var _a;
      text.setPlaceholder("Zotero/library.json").setValue((_a = this.plugin.store.getData().userSettings.zoteroJsonPath) != null ? _a : "").onChange(async (value) => {
        await this.plugin.store.updateUserSettings({ zoteroJsonPath: value.trim() });
      });
    });
  }
};

// src/services/HealthReminderService.ts
var import_obsidian2 = require("obsidian");
var HealthReminderService = class {
  constructor(app, store) {
    this.app = app;
    this.store = store;
  }
  async checkMissedReminders() {
    var _a, _b, _c;
    const missed = [];
    const today6 = formatDateKey(/* @__PURE__ */ new Date());
    const now = /* @__PURE__ */ new Date();
    for (const reminder of this.store.getHealthReminders()) {
      if (!this.shouldCheck(reminder)) continue;
      const date = (_a = reminder.date) != null ? _a : today6;
      if (date > today6) continue;
      if (!this.isScheduledForDate(reminder, date)) continue;
      const scheduled = this.getScheduledDate(reminder, date);
      if (!scheduled || scheduled >= now) continue;
      const id = this.getLogId(reminder.id, date, (_b = reminder.time) != null ? _b : "09:00");
      if (this.store.getHealthReminderLogs().some((log) => log.id === id)) continue;
      missed.push({
        id,
        reminderId: reminder.id,
        date,
        scheduledTime: (_c = reminder.time) != null ? _c : "09:00",
        status: "missed"
      });
    }
    for (const log of missed) {
      await this.store.addHealthReminderLog(log);
    }
    if (missed.length > 0) {
      new import_obsidian2.Notice(`\u4F60\u6709 ${missed.length} \u4E2A\u9519\u8FC7\u7684\u5065\u5EB7\u63D0\u9192\u3002`);
    }
  }
  async tick() {
    var _a;
    const now = /* @__PURE__ */ new Date();
    const date = formatDateKey(now);
    const minute = `${String(now.getHours()).padStart(2, "0")}:${String(now.getMinutes()).padStart(2, "0")}`;
    for (const reminder of this.store.getHealthReminders()) {
      if (!this.shouldCheck(reminder) || !this.isScheduledForDate(reminder, date) || ((_a = reminder.time) != null ? _a : "09:00") !== minute) {
        continue;
      }
      const id = this.getLogId(reminder.id, date, minute);
      if (this.store.getHealthReminderLogs().some((log) => log.id === id)) continue;
      await this.store.addHealthReminderLog({
        id,
        reminderId: reminder.id,
        date,
        scheduledTime: minute,
        triggeredAt: now.toISOString(),
        status: "triggered"
      });
      new import_obsidian2.Notice(`\u5065\u5EB7\u63D0\u9192\uFF1A${reminder.title}`);
    }
  }
  shouldCheck(reminder) {
    return reminder.enabled !== false && Boolean(reminder.title);
  }
  isScheduledForDate(reminder, date) {
    var _a, _b, _c, _d, _e;
    const repeatType = (_a = reminder.repeatType) != null ? _a : "once";
    if (repeatType === "once") return ((_b = reminder.date) != null ? _b : date) === date;
    if (((_c = reminder.date) != null ? _c : date) > date) return false;
    const day = (/* @__PURE__ */ new Date(`${date}T00:00:00`)).getDay();
    if (repeatType === "daily") return true;
    if (repeatType === "weekdays") return day >= 1 && day <= 5;
    if (repeatType === "weekly") return day === (/* @__PURE__ */ new Date(`${(_d = reminder.date) != null ? _d : date}T00:00:00`)).getDay();
    if (repeatType === "custom") return ((_e = reminder.repeatDays) != null ? _e : []).includes(day);
    return false;
  }
  getScheduledDate(reminder, date) {
    var _a;
    const [hour, minute] = ((_a = reminder.time) != null ? _a : "09:00").split(":").map((part) => Number(part));
    if (!Number.isFinite(hour) || !Number.isFinite(minute)) return null;
    return /* @__PURE__ */ new Date(`${date}T${String(hour).padStart(2, "0")}:${String(minute).padStart(2, "0")}:00`);
  }
  getLogId(reminderId, date, time) {
    return `health-log-${reminderId}-${date}-${time}`;
  }
};

// src/views/WorkbenchView.ts
var import_obsidian85 = require("obsidian");

// src/core/DashboardRouter.ts
var ROUTE_CHANGED_EVENT = "route-changed";
var DashboardRouter = class {
  constructor(eventBus, currentPage) {
    this.eventBus = eventBus;
    this.currentPage = currentPage;
  }
  getCurrentPage() {
    return this.currentPage;
  }
  navigate(page) {
    if (this.currentPage === page) {
      return;
    }
    this.currentPage = page;
    this.eventBus.emit(ROUTE_CHANGED_EVENT, page);
  }
};

// src/core/EventBus.ts
var EventBus = class {
  constructor() {
    this.handlers = /* @__PURE__ */ new Map();
  }
  on(eventName, handler) {
    var _a;
    const eventHandlers = (_a = this.handlers.get(eventName)) != null ? _a : /* @__PURE__ */ new Set();
    eventHandlers.add(handler);
    this.handlers.set(eventName, eventHandlers);
    return () => this.off(eventName, handler);
  }
  off(eventName, handler) {
    var _a;
    (_a = this.handlers.get(eventName)) == null ? void 0 : _a.delete(handler);
  }
  emit(eventName, payload) {
    var _a;
    (_a = this.handlers.get(eventName)) == null ? void 0 : _a.forEach((handler) => handler(payload));
  }
  clear() {
    this.handlers.clear();
  }
};

// src/components/Sidebar.ts
var import_obsidian4 = require("obsidian");

// src/core/PageLabels.ts
var PAGE_LABELS = {
  overview: "\u603B\u89C8",
  research: "\u79D1\u7814",
  reading: "\u9605\u8BFB",
  fitness: "\u5065\u8EAB",
  finance: "\u7406\u8D22",
  tasks: "\u4EFB\u52A1\u7BA1\u7406",
  goals: "\u76EE\u6807\u7BA1\u7406",
  modules: "\u6A21\u5757\u7BA1\u7406"
};

// src/services/CalendarService.ts
var CalendarService = class {
  getToday() {
    return /* @__PURE__ */ new Date();
  }
  getDateKey(date) {
    const year = date.getFullYear();
    const month = String(date.getMonth() + 1).padStart(2, "0");
    const day = String(date.getDate()).padStart(2, "0");
    return `${year}-${month}-${day}`;
  }
  getMonthTitle(date) {
    return `${date.getFullYear()}\u5E74${date.getMonth() + 1}\u6708`;
  }
  getWeekdayLabel(date) {
    return ["\u661F\u671F\u65E5", "\u661F\u671F\u4E00", "\u661F\u671F\u4E8C", "\u661F\u671F\u4E09", "\u661F\u671F\u56DB", "\u661F\u671F\u4E94", "\u661F\u671F\u516D"][date.getDay()];
  }
  addMonths(date, amount) {
    return new Date(date.getFullYear(), date.getMonth() + amount, 1);
  }
  isSameDate(left, right) {
    return this.getDateKey(left) === this.getDateKey(right);
  }
  getMonthCells(month, weekStartsOn = "sunday") {
    const firstDay = new Date(month.getFullYear(), month.getMonth(), 1);
    const daysInMonth = new Date(month.getFullYear(), month.getMonth() + 1, 0).getDate();
    const cells = [];
    const offset = weekStartsOn === "monday" ? (firstDay.getDay() + 6) % 7 : firstDay.getDay();
    for (let index = 0; index < offset; index += 1) {
      cells.push(null);
    }
    for (let day = 1; day <= daysInMonth; day += 1) {
      cells.push(new Date(month.getFullYear(), month.getMonth(), day));
    }
    while (cells.length % 7 !== 0) {
      cells.push(null);
    }
    return cells;
  }
};

// src/components/AvatarPickerModal.ts
var import_obsidian3 = require("obsidian");
var AVATAR_PRESETS = [
  { id: "dog", label: "\u624B\u7ED8\u72D7\u72D7", icon: "dog" },
  { id: "heart", label: "\u7231\u5FC3", icon: "heart" },
  { id: "star", label: "\u661F\u661F", icon: "sparkles" },
  { id: "flower", label: "\u5C0F\u82B1", icon: "flower-2" },
  { id: "moon", label: "\u6708\u4EAE", icon: "moon" }
];
function renderWorkbenchAvatar(container, avatar, className) {
  var _a;
  const value = avatar != null ? avatar : { type: "preset", value: "dog" };
  const wrapperClass = className === "cow-banner-dog" ? "cow-banner-avatar-wrapper" : "cow-sidebar-avatar-wrapper";
  const imageClass = className === "cow-banner-dog" ? "cow-banner-avatar-image" : "cow-sidebar-avatar-image";
  const root = container.createDiv({ cls: `${className} ${wrapperClass} cow-custom-avatar cow-avatar-${value.type === "preset" ? value.value : "image"}` });
  if (value.type === "image") {
    root.createEl("img", { cls: imageClass, attr: { src: value.value, alt: "" } });
    return root;
  }
  if (value.value === "dog") {
    root.createDiv({ cls: className === "cow-banner-dog" ? "cow-dog-face" : "cow-mini-dog" });
    return root;
  }
  const preset = (_a = AVATAR_PRESETS.find((item) => item.id === value.value)) != null ? _a : AVATAR_PRESETS[0];
  (0, import_obsidian3.setIcon)(root.createSpan(), preset.icon);
  return root;
}
var AvatarPickerModal = class extends import_obsidian3.Modal {
  constructor(app, title, current, onPick) {
    super(app);
    this.title = title;
    this.current = current;
    this.onPick = onPick;
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: this.title });
    this.contentEl.createEl("p", { text: "\u9009\u62E9\u9884\u8BBE\u56FE\u6807\uFF0C\u6216\u4E0A\u4F20\u4E00\u5F20\u672C\u5730\u56FE\u7247\u4FDD\u5B58\u4E3A data URL\u3002" });
    const grid = this.contentEl.createDiv({ cls: "cow-avatar-choice-grid" });
    AVATAR_PRESETS.forEach((preset) => {
      var _a;
      const button = grid.createEl("button", {
        cls: ((_a = this.current) == null ? void 0 : _a.type) === "preset" && this.current.value === preset.id ? "is-active" : "",
        attr: { type: "button" }
      });
      const preview = button.createDiv({ cls: `cow-avatar-preview cow-avatar-${preset.id}` });
      if (preset.id === "dog") {
        preview.createDiv({ cls: "cow-mini-dog" });
      } else {
        (0, import_obsidian3.setIcon)(preview.createSpan(), preset.icon);
      }
      button.createSpan({ text: preset.label });
      button.addEventListener("click", async () => {
        await this.onPick({ type: "preset", value: preset.id });
        new import_obsidian3.Notice("\u56FE\u6807\u5DF2\u4FDD\u5B58\u3002");
        this.close();
      });
    });
    const fileInput = this.contentEl.createEl("input", {
      cls: "cow-hidden-input",
      attr: { type: "file", accept: "image/*" }
    });
    fileInput.addEventListener("change", () => {
      var _a;
      const file = (_a = fileInput.files) == null ? void 0 : _a[0];
      if (!file) return;
      const reader = new FileReader();
      reader.onload = async () => {
        await this.onPick({ type: "image", value: String(reader.result) });
        new import_obsidian3.Notice("\u56FE\u7247\u56FE\u6807\u5DF2\u4FDD\u5B58\u3002");
        this.close();
      };
      reader.readAsDataURL(file);
    });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    const upload = actions.createEl("button", { attr: { type: "button" } });
    (0, import_obsidian3.setIcon)(upload.createSpan(), "image-plus");
    upload.createSpan({ text: "\u4E0A\u4F20\u56FE\u7247" });
    upload.addEventListener("click", () => fileInput.click());
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
  }
};

// src/components/Sidebar.ts
var Sidebar = class {
  constructor(app, currentPage, actions, onQuickCreate, onOpenDay, getData, onCustomizeAvatar) {
    this.app = app;
    this.currentPage = currentPage;
    this.actions = actions;
    this.onQuickCreate = onQuickCreate;
    this.onOpenDay = onOpenDay;
    this.getData = getData;
    this.onCustomizeAvatar = onCustomizeAvatar;
    this.currentDateKey = "";
    this.calendar = new CalendarService();
    this.visibleMonth = /* @__PURE__ */ new Date();
    this.selectedDate = /* @__PURE__ */ new Date();
  }
  render(container) {
    const sidebar = container.createDiv({ cls: "cow-sidebar" });
    const quickCreate = sidebar.createEl("button", {
      cls: "cow-sidebar-create-button",
      attr: { type: "button", "aria-label": "\u5FEB\u901F\u521B\u5EFA" }
    });
    (0, import_obsidian4.setIcon)(quickCreate, "plus");
    quickCreate.addEventListener("click", this.onQuickCreate);
    const profile = sidebar.createDiv({ cls: "cow-profile" });
    const avatarButton = profile.createEl("button", {
      cls: "cow-profile-avatar-button",
      attr: { type: "button", "aria-label": "\u4FEE\u6539\u5DE6\u4FA7\u5934\u50CF" }
    });
    renderWorkbenchAvatar(avatarButton, this.getData().banner.sidebarAvatar, "cow-profile-avatar");
    avatarButton.addEventListener("click", this.onCustomizeAvatar);
    profile.createEl("h2", { text: "\u6211\u7684\u5DE5\u4F5C\u53F0" });
    profile.createEl("p", { text: "\u8BB0\u5F55\u3001\u601D\u8003\u3001\u6210\u957F\u3001\u53EF\u7231\u5411\u524D" });
    this.clockEl = sidebar.createDiv({ cls: "cow-clock" });
    this.dateEl = this.clockEl.createEl("p");
    this.timeEl = this.clockEl.createEl("strong");
    this.pageLabelEl = this.clockEl.createEl("span");
    this.updateClock();
    this.timer = window.setInterval(() => this.updateClock(), 3e4);
    this.renderMiniCalendar(sidebar);
    this.renderActions(sidebar);
  }
  destroy() {
    if (this.timer) {
      window.clearInterval(this.timer);
      this.timer = void 0;
    }
  }
  renderMiniCalendar(container) {
    this.calendarEl = container.createDiv({ cls: "cow-mini-calendar" });
    this.renderMiniCalendarContent();
  }
  renderMiniCalendarContent() {
    if (!this.calendarEl) return;
    this.calendarEl.empty();
    const header = this.calendarEl.createDiv({ cls: "cow-mini-calendar-header" });
    const prev = header.createEl("button", { attr: { type: "button", "aria-label": "\u4E0A\u4E00\u6708" } });
    (0, import_obsidian4.setIcon)(prev, "chevron-left");
    prev.addEventListener("click", () => {
      this.visibleMonth = this.calendar.addMonths(this.visibleMonth, -1);
      this.renderMiniCalendarContent();
    });
    header.createEl("h3", { text: this.calendar.getMonthTitle(this.visibleMonth) });
    const next = header.createEl("button", { attr: { type: "button", "aria-label": "\u4E0B\u4E00\u6708" } });
    (0, import_obsidian4.setIcon)(next, "chevron-right");
    next.addEventListener("click", () => {
      this.visibleMonth = this.calendar.addMonths(this.visibleMonth, 1);
      this.renderMiniCalendarContent();
    });
    const todayButton = this.calendarEl.createEl("button", {
      cls: "cow-mini-calendar-today",
      text: "\u4ECA\u5929",
      attr: { type: "button" }
    });
    todayButton.addEventListener("click", () => {
      const today6 = /* @__PURE__ */ new Date();
      this.visibleMonth = new Date(today6.getFullYear(), today6.getMonth(), 1);
      this.selectedDate = today6;
      this.renderMiniCalendarContent();
      this.onOpenDay(today6);
    });
    const weekdays = ["\u65E5", "\u4E00", "\u4E8C", "\u4E09", "\u56DB", "\u4E94", "\u516D"];
    const grid = this.calendarEl.createDiv({ cls: "cow-mini-calendar-grid" });
    weekdays.forEach((weekday) => grid.createSpan({ cls: "cow-weekday", text: weekday }));
    this.calendar.getMonthCells(this.visibleMonth).forEach((date) => {
      if (!date) {
        grid.createSpan({ cls: "cow-empty-day" });
        return;
      }
      const today6 = /* @__PURE__ */ new Date();
      const button = grid.createEl("button", {
        cls: `cow-day ${this.calendar.isSameDate(date, today6) ? "is-today" : ""} ${this.calendar.isSameDate(date, this.selectedDate) ? "is-selected" : ""}`,
        text: String(date.getDate()),
        attr: { type: "button", "aria-label": this.calendar.getDateKey(date) }
      });
      button.addEventListener("click", () => {
        this.selectedDate = date;
        this.renderMiniCalendarContent();
        this.onOpenDay(date);
      });
    });
  }
  renderActions(container) {
    const actions = container.createDiv({ cls: "cow-sidebar-actions" });
    this.actions.forEach((action) => {
      const button = actions.createEl("button", {
        cls: "cow-sidebar-action",
        attr: { type: "button" }
      });
      (0, import_obsidian4.setIcon)(button.createSpan(), action.icon);
      button.createSpan({ text: action.label });
      button.addEventListener("click", action.onClick);
    });
    const fileHint = actions.createDiv({ cls: "cow-file-shortcut" });
    (0, import_obsidian4.setIcon)(fileHint.createSpan(), "folder-open");
    fileHint.createSpan({ text: `\u6587\u4EF6\u5E93\uFF1A${this.app.vault.getName()}` });
  }
  updateClock() {
    if (!this.dateEl || !this.timeEl || !this.pageLabelEl) {
      return;
    }
    const now = /* @__PURE__ */ new Date();
    const dateText = now.toLocaleDateString("zh-CN", {
      year: "numeric",
      month: "long",
      day: "numeric",
      weekday: "short"
    });
    const timeText = now.toLocaleTimeString("zh-CN", {
      hour: "2-digit",
      minute: "2-digit"
    });
    this.dateEl.setText(dateText);
    this.timeEl.setText(timeText);
    this.pageLabelEl.setText(PAGE_LABELS[this.currentPage]);
    const dateKey = this.calendar.getDateKey(now);
    if (this.currentDateKey && this.currentDateKey !== dateKey) {
      this.renderMiniCalendarContent();
    }
    this.currentDateKey = dateKey;
  }
};

// src/components/TopBanner.ts
var import_obsidian5 = require("obsidian");
var TopBanner = class {
  constructor(getData, onCustomize, onCustomizeAvatar) {
    this.getData = getData;
    this.onCustomize = onCustomize;
    this.onCustomizeAvatar = onCustomizeAvatar;
  }
  render(container) {
    var _a;
    const banner = container.createDiv({ cls: "cow-top-banner" });
    const data = this.getData();
    banner.addClass(`cow-banner-bg-${data.banner.background}`);
    banner.style.backgroundPosition = data.banner.backgroundPosition;
    banner.style.opacity = String(data.banner.opacity);
    if (data.banner.imageDataUrl) {
      banner.style.backgroundImage = `linear-gradient(rgba(255, 224, 237, ${data.banner.overlay ? "0.45" : "0"}), rgba(255, 247, 223, ${data.banner.overlay ? "0.45" : "0"})), url("${data.banner.imageDataUrl}")`;
      banner.style.backgroundSize = "cover";
    }
    const dog = banner.createEl("button", {
      cls: "cow-banner-avatar-button",
      attr: { type: "button", "aria-label": "\u4FEE\u6539 Banner \u56FE\u6807" }
    });
    renderWorkbenchAvatar(dog, data.banner.bannerAvatar, "cow-banner-dog");
    dog.addEventListener("click", this.onCustomizeAvatar);
    const copy = banner.createDiv({ cls: "cow-banner-copy" });
    copy.createEl("p", { cls: "cow-banner-kicker", text: "\u51B2\u9E2D\uFF01" });
    copy.createEl("h2", { text: data.banner.message });
    copy.createEl("p", { text: (_a = data.banner.subtitle) != null ? _a : "\u628A\u60F3\u6CD5\u53D8\u6210\u884C\u52A8\uFF0C\u8BA9\u6BCF\u4E00\u5929\u90FD\u66F4\u9760\u8FD1\u7406\u60F3\u7684\u81EA\u5DF1\u3002" });
    const button = banner.createEl("button", {
      cls: "cow-banner-button",
      attr: { type: "button", "aria-label": "\u81EA\u5B9A\u4E49\u5DE5\u4F5C\u53F0" }
    });
    (0, import_obsidian5.setIcon)(button.createSpan(), "plus");
    button.createSpan({ text: "\u81EA\u5B9A\u4E49" });
    button.addEventListener("click", this.onCustomize);
  }
};

// src/components/TopNavigation.ts
var import_obsidian6 = require("obsidian");
var TopNavigation = class {
  constructor(pages, getCurrentPage, onNavigate) {
    this.pages = pages;
    this.getCurrentPage = getCurrentPage;
    this.onNavigate = onNavigate;
  }
  render(container) {
    const nav = container.createDiv({ cls: "cow-top-nav" });
    this.pages.forEach((page) => {
      const button = nav.createEl("button", {
        cls: `cow-nav-button ${this.getCurrentPage() === page.id ? "is-active" : ""}`,
        attr: { type: "button", "aria-label": page.label }
      });
      const icon = button.createSpan({ cls: "cow-nav-icon" });
      (0, import_obsidian6.setIcon)(icon, page.icon);
      button.createSpan({ text: page.label });
      button.addEventListener("click", () => this.onNavigate(page.id));
    });
  }
};

// src/components/DashboardSection.ts
var import_obsidian77 = require("obsidian");

// src/components/SectionActionMenu.ts
var import_obsidian25 = require("obsidian");

// src/core/SectionCapabilities.ts
var ADDABLE_SECTION_TYPES = /* @__PURE__ */ new Set([
  "research-projects",
  "experiment-plan",
  "experiment-records",
  "data-analysis-tasks",
  "literature-notes",
  "research-timeline",
  "research-memo",
  "reading-notes",
  "reading-quotes",
  "reading-tag-manager",
  "wishlist-books",
  "workout-plan",
  "workout-log",
  "account-overview",
  "saving-goals",
  "bill-reminders",
  "today-focus"
]);
function getSectionCapabilities(sectionType) {
  const canAdd = ADDABLE_SECTION_TYPES.has(sectionType);
  return {
    canAdd,
    canEdit: canAdd,
    canDeleteItems: canAdd,
    canOpenStats: sectionType === "habit-overview" || sectionType === "monthly-progress" || sectionType === "today-focus",
    canManage: canAdd
  };
}

// src/components/SectionContentActions.ts
var import_obsidian24 = require("obsidian");

// src/components/CrudItemModal.ts
var import_obsidian7 = require("obsidian");
var CrudItemModal = class extends import_obsidian7.Modal {
  constructor(app, title, initialValues, fields, onSubmit) {
    super(app);
    this.title = title;
    this.fields = fields;
    this.onSubmit = onSubmit;
    this.values = { ...initialValues };
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: this.title });
    this.fields.forEach((field) => this.renderField(field));
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      await this.onSubmit(this.values);
      this.close();
    });
  }
  renderField(field) {
    const currentValue = this.values[field.key];
    const setting = new import_obsidian7.Setting(this.contentEl).setName(field.name);
    if (field.type === "textarea") {
      setting.addTextArea((text) => text.setValue(String(currentValue != null ? currentValue : "")).onChange((value) => {
        this.values[field.key] = value;
      }));
      return;
    }
    if (field.type === "number") {
      setting.addText((text) => {
        text.inputEl.type = "number";
        text.setValue(String(currentValue != null ? currentValue : 0));
        text.onChange((value) => {
          this.values[field.key] = Number(value);
        });
      });
      return;
    }
    if (field.type === "checkbox") {
      setting.addToggle((toggle) => toggle.setValue(Boolean(currentValue)).onChange((value) => {
        this.values[field.key] = value;
      }));
      return;
    }
    if (field.type === "select") {
      setting.addDropdown((dropdown) => {
        var _a, _b, _c, _d;
        (_a = field.options) == null ? void 0 : _a.forEach((option) => dropdown.addOption(option.value, option.label));
        dropdown.setValue(String((_d = currentValue != null ? currentValue : (_c = (_b = field.options) == null ? void 0 : _b[0]) == null ? void 0 : _c.value) != null ? _d : ""));
        dropdown.onChange((value) => {
          this.values[field.key] = value;
        });
      });
      return;
    }
    setting.addText((text) => text.setValue(String(currentValue != null ? currentValue : "")).onChange((value) => {
      this.values[field.key] = value;
    }));
  }
};

// src/components/reading/AddBookModal.ts
var import_obsidian9 = require("obsidian");

// src/components/ResizableModal.ts
function applyResizableModal(modal, options) {
  const classNames = options.className.split(/\s+/).filter(Boolean);
  const kind = inferModalLayoutKind(classNames);
  modal.modalEl.addClass("cute-resizable-modal", "cow-modal-layout", `cow-modal-kind-${kind}`, ...classNames);
  modal.modalEl.setAttr("data-cow-modal-kind", kind);
  modal.modalEl.style.resize = "both";
  modal.modalEl.style.overflow = "hidden";
  modal.modalEl.style.width = options.width;
  if (options.height) {
    modal.modalEl.style.height = options.height;
  } else {
    modal.modalEl.style.removeProperty("height");
  }
  modal.modalEl.style.maxWidth = options.maxWidth;
  modal.modalEl.style.maxHeight = options.maxHeight;
  if (options.minWidth) {
    modal.modalEl.style.minWidth = options.minWidth;
  }
  if (options.minHeight) {
    modal.modalEl.style.minHeight = options.minHeight;
  }
}
function inferModalLayoutKind(classNames) {
  const name = classNames.join(" ").toLowerCase();
  if (/(manager|all-books|zotero)/.test(name)) return "manager";
  if (/(statistics|stats|history|records)/.test(name)) return "statistics";
  if (/detail/.test(name)) return "detail";
  return "form";
}

// src/components/research/MarkdownFilePicker.ts
var import_obsidian8 = require("obsidian");
var MarkdownFilePicker = class {
  constructor(app, options) {
    this.app = app;
    this.options = options;
    this.query = "";
    var _a;
    this.markdownFiles = this.app.vault.getMarkdownFiles();
    this.notePath = (_a = options.value) != null ? _a : "";
  }
  render(container) {
    var _a;
    const root = container.createDiv({ cls: "cow-markdown-picker" });
    const row = root.createDiv({ cls: "cow-book-form-row cow-markdown-picker-search" });
    row.createEl("label", { text: this.options.label });
    this.searchInput = row.createEl("input", { attr: { type: "text", placeholder: (_a = this.options.placeholder) != null ? _a : "\u641C\u7D22 Obsidian \u4E2D\u7684 Markdown\u2026\u2026" } });
    this.searchInput.value = this.query;
    this.searchInput.addEventListener("input", () => {
      var _a2, _b;
      this.query = (_b = (_a2 = this.searchInput) == null ? void 0 : _a2.value) != null ? _b : "";
      this.renderResults();
    });
    this.selectedFileEl = root.createDiv({ cls: "cow-selected-note-file" });
    this.resultsEl = root.createDiv({ cls: "cow-note-picker-list" });
    this.renderSelectedFile();
    this.renderResults();
  }
  renderResults() {
    const list = this.resultsEl;
    if (!list) return;
    list.empty();
    const query = this.query.trim().toLowerCase();
    const files = (query ? this.markdownFiles.filter((file) => `${file.basename} ${file.name} ${file.path}`.toLowerCase().includes(query)) : [...this.markdownFiles].sort((a, b) => b.stat.mtime - a.stat.mtime)).slice(0, 50);
    if (files.length === 0) {
      list.createDiv({ cls: "cow-empty-state", text: query ? "\u6CA1\u6709\u627E\u5230\u5339\u914D\u7684 Markdown \u6587\u4EF6\u3002" : "Vault \u4E2D\u6682\u65E0 Markdown \u6587\u4EF6\u3002" });
      return;
    }
    files.forEach((file) => {
      const button = list.createEl("button", { cls: "cow-data-card cow-click-card", attr: { type: "button" } });
      button.createEl("strong", { text: file.basename });
      button.createDiv({ cls: "cow-meta-line", text: file.path });
      button.addEventListener("click", () => {
        this.notePath = file.path;
        this.query = "";
        if (this.searchInput) this.searchInput.value = "";
        this.options.onChange(file.path);
        this.renderSelectedFile();
        this.renderResults();
      });
    });
  }
  renderSelectedFile() {
    const container = this.selectedFileEl;
    if (!container) return;
    container.empty();
    container.createEl("strong", { text: "\u5DF2\u5173\u8054" });
    if (!this.notePath) {
      container.createDiv({ cls: "cow-meta-line", text: "\u5C1A\u672A\u9009\u62E9 Markdown \u6587\u4EF6\u3002" });
      return;
    }
    const file = this.app.vault.getAbstractFileByPath(this.notePath);
    container.createDiv({ cls: "cow-selected-note-title", text: `\u{1F4C4} ${file instanceof import_obsidian8.TFile ? file.name : fileName(this.notePath)}` });
    container.createDiv({ cls: "cow-meta-line", text: this.notePath });
    const actions = container.createDiv({ cls: "cow-list-item-actions" });
    actions.createEl("button", { text: "\u6253\u5F00", attr: { type: "button" } }).addEventListener("click", () => void openVaultMarkdown(this.app, this.notePath));
    actions.createEl("button", { text: "\u66F4\u6362", attr: { type: "button" } }).addEventListener("click", () => {
      var _a;
      return (_a = this.searchInput) == null ? void 0 : _a.focus();
    });
    actions.createEl("button", { text: "\u89E3\u9664\u5173\u8054", attr: { type: "button" } }).addEventListener("click", () => {
      this.notePath = "";
      this.options.onChange("");
      this.renderSelectedFile();
    });
  }
};
async function openVaultMarkdown(app, path) {
  if (!path) {
    new import_obsidian8.Notice("\u5C1A\u672A\u5173\u8054 Obsidian \u6587\u6863\u3002");
    return;
  }
  const file = app.vault.getAbstractFileByPath(path);
  if (!(file instanceof import_obsidian8.TFile)) {
    new import_obsidian8.Notice("\u672A\u627E\u5230\u5173\u8054\u7684 Obsidian \u6587\u6863\u3002");
    return;
  }
  await app.workspace.getLeaf(false).openFile(file);
}
function fileName(path) {
  return path.split(/[\\/]/).pop() || path;
}

// src/components/reading/AddBookModal.ts
var AddBookModal = class extends import_obsidian9.Modal {
  constructor(app, store, onDataChanged, book, options = {}) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p;
    super(app);
    this.store = store;
    this.onDataChanged = onDataChanged;
    this.book = book;
    this.options = options;
    this.activeTab = "search";
    this.searchResults = [];
    this.draft = {
      ...book,
      ...options.initialDraft,
      title: (_c = (_b = (_a = options.initialDraft) == null ? void 0 : _a.title) != null ? _b : book == null ? void 0 : book.title) != null ? _c : "",
      author: (_f = (_e = (_d = options.initialDraft) == null ? void 0 : _d.author) != null ? _e : book == null ? void 0 : book.author) != null ? _f : "",
      description: (_h = (_g = options.initialDraft) == null ? void 0 : _g.description) != null ? _h : book == null ? void 0 : book.description,
      totalPages: (_k = (_j = (_i = options.initialDraft) == null ? void 0 : _i.totalPages) != null ? _j : book == null ? void 0 : book.totalPages) != null ? _k : 200,
      currentPage: (_l = book == null ? void 0 : book.currentPage) != null ? _l : 0,
      readingStatus: (_m = book == null ? void 0 : book.readingStatus) != null ? _m : (book == null ? void 0 : book.status) === "\u5728\u8BFB" ? "reading" : (book == null ? void 0 : book.status) === "\u5DF2\u8BFB" ? "finished" : "want-to-read",
      shelfStatus: (_n = book == null ? void 0 : book.shelfStatus) != null ? _n : "on-shelf",
      tags: (_o = book == null ? void 0 : book.tags) != null ? _o : [],
      tagIds: (_p = book == null ? void 0 : book.tagIds) != null ? _p : []
    };
    if (book) this.activeTab = "manual";
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-book-modal",
      width: "min(760px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "90vh",
      minWidth: "min(520px, 92vw)",
      minHeight: "min(430px, 82vh)"
    });
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-book-modal");
    this.contentEl.createEl("h2", { text: this.book ? "\u7F16\u8F91\u4E66\u7C4D" : "\u589E\u52A0\u4E66\u7C4D" });
    this.renderTabs();
    const body = this.contentEl.createDiv({ cls: "cow-book-modal-body" });
    if (this.activeTab === "search") this.renderSearchTab(body);
    if (this.activeTab === "local") this.renderLocalTab(body);
    if (this.activeTab === "wishlist") this.renderWishlistTab(body);
    this.renderBookForm(body);
    if (this.book) this.renderLinkedReadingNotes(body);
    this.renderActions();
  }
  renderTabs() {
    const tabs = this.contentEl.createDiv({ cls: "cow-book-tabs" });
    const tabItems = [
      { id: "search", label: "\u641C\u7D22\u5BFC\u5165" },
      { id: "local", label: "\u672C\u5730\u6587\u4EF6" },
      { id: "manual", label: "\u624B\u52A8\u6DFB\u52A0" }
    ];
    if (!this.book) tabItems.push({ id: "wishlist", label: "\u4ECE\u60F3\u8BFB\u6E05\u5355\u6DFB\u52A0" });
    tabItems.forEach((tab) => {
      const button = tabs.createEl("button", { text: tab.label, cls: this.activeTab === tab.id ? "is-active" : "", attr: { type: "button" } });
      button.addEventListener("click", () => {
        this.activeTab = tab.id;
        this.render();
      });
    });
  }
  renderSearchTab(container) {
    const panel = container.createDiv({ cls: "cow-book-search-panel" });
    const search = container.createDiv({ cls: "cow-book-search" });
    const input = search.createEl("input", { attr: { type: "search", placeholder: "\u8F93\u5165\u4E66\u540D\u3001\u4F5C\u8005\u6216 ISBN" } });
    input.value = [this.draft.title, this.draft.author].filter(Boolean).join(" ");
    const button = search.createEl("button", { attr: { type: "button" } });
    (0, import_obsidian9.setIcon)(button.createSpan(), "search");
    button.createSpan({ text: "\u641C\u7D22" });
    button.addEventListener("click", async () => {
      this.searchResults = await this.searchBooks(input.value.trim());
      this.render();
    });
    const results = container.createDiv({ cls: "cow-book-search-results" });
    this.searchResults.forEach((result) => this.renderSearchResult(results, result));
    panel.appendChild(search);
    panel.appendChild(results);
  }
  renderSearchResult(container, result) {
    var _a, _b;
    const item = container.createDiv({ cls: "cow-book-search-result" });
    const cover = item.createDiv({ cls: "cow-book-result-cover" });
    if (result.coverUrl) this.renderCoverImage(cover, result.coverUrl, result.title);
    else cover.createSpan({ text: result.title.slice(0, 2) });
    const body = item.createDiv({ cls: "cow-book-result-body" });
    body.createEl("strong", { text: result.title });
    body.createSpan({ text: [result.author, result.publisher, result.publishDate].filter(Boolean).join(" \xB7 ") || "\u672A\u77E5\u4F5C\u8005" });
    body.createSpan({ text: [`ISBN ${(_b = (_a = result.isbn13) != null ? _a : result.isbn10) != null ? _b : "--"}`, result.totalPages ? `${result.totalPages} \u9875` : ""].filter(Boolean).join(" \xB7 ") });
    const select = item.createEl("button", { text: "\u9009\u62E9", attr: { type: "button" } });
    select.addEventListener("click", () => {
      var _a2, _b2;
      this.draft = {
        ...this.draft,
        ...result,
        author: result.author || this.draft.author,
        totalPages: (_b2 = (_a2 = result.totalPages) != null ? _a2 : this.draft.totalPages) != null ? _b2 : 200,
        bookFilePath: this.draft.bookFilePath
      };
      new import_obsidian9.Notice("\u5DF2\u586B\u5165\u4E66\u7C4D\u5143\u6570\u636E\u3002\u641C\u7D22\u7ED3\u679C\u4E0D\u5305\u542B\u5168\u6587\uFF0C\u8BF7\u6309\u9700\u5173\u8054\u672C\u5730\u7535\u5B50\u4E66\u3002");
      this.activeTab = "manual";
      this.render();
    });
  }
  renderLocalTab(container) {
    const panel = container.createDiv({ cls: "cow-book-local-panel" });
    panel.createEl("p", { text: "\u9009\u62E9 PDF / EPUB / FB2 / MOBI / AZW3 \u6587\u4EF6\u540E\uFF0C\u4F1A\u590D\u5236\u5230\u5F53\u524D Vault \u7684 Books/Files/\uFF0C\u4E0D\u4F1A\u4FDD\u5B58\u672C\u673A\u7EDD\u5BF9\u8DEF\u5F84\u3002" });
    const input = panel.createEl("input", { attr: { type: "file", accept: ".pdf,.epub,.fb2,.mobi,.azw3,application/pdf" } });
    input.addEventListener("change", async () => {
      var _a;
      const file = (_a = input.files) == null ? void 0 : _a[0];
      if (!file) return;
      const path = await this.copyBookFile(file);
      if (!path) return;
      this.draft.bookFilePath = path;
      if (!this.draft.title) this.draft.title = file.name.replace(/\.[^.]+$/, "");
      await this.ensureMetadataFromQuery(this.draft.title);
      this.render();
    });
  }
  renderWishlistTab(container) {
    const panel = container.createDiv({ cls: "cow-want-read-picker-panel" });
    panel.createEl("p", { text: "\u9009\u62E9\u5F85\u5904\u7406\u60F3\u8BFB\u8BB0\u5F55\u540E\uFF0C\u4F1A\u9884\u586B\u4E66\u540D\u3001\u4F5C\u8005\u548C\u7B80\u4ECB\uFF1B\u4FDD\u5B58\u540E\u624D\u4F1A\u521B\u5EFA\u6B63\u5F0F Book\u3002" });
    const pending = this.store.getWantToReadItems().filter((item) => item.status === "pending");
    if (pending.length === 0) {
      panel.createDiv({ cls: "cow-empty-state", text: "\u5F53\u524D\u6CA1\u6709\u5F85\u5904\u7406\u7684\u60F3\u8BFB\u8BB0\u5F55\u3002" });
      return;
    }
    const list = panel.createDiv({ cls: "cow-data-list" });
    pending.forEach((item) => {
      const button = list.createEl("button", {
        cls: `cow-data-card cow-click-card ${this.selectedWantToReadId === item.id ? "is-selected" : ""}`,
        attr: { type: "button" }
      });
      button.createEl("strong", { text: item.title });
      button.createDiv({ cls: "cow-meta-line", text: item.author || "\u672A\u77E5\u4F5C\u8005" });
      if (item.summary) button.createEl("p", { text: item.summary });
      button.addEventListener("click", () => {
        var _a, _b;
        this.selectedWantToReadId = item.id;
        this.draft = {
          ...this.draft,
          title: item.title,
          author: (_a = item.author) != null ? _a : "",
          description: (_b = item.summary) != null ? _b : ""
        };
        this.activeTab = "manual";
        this.render();
      });
    });
  }
  renderBookForm(container) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k;
    const form = container.createDiv({ cls: "cow-book-form" });
    const cover = form.createDiv({ cls: "cow-book-form-cover" });
    const coverSrc = this.draft.coverPath ? this.app.vault.adapter.getResourcePath(this.draft.coverPath) : (_a = this.draft.coverUrl) != null ? _a : this.draft.cover;
    if (coverSrc) this.renderCoverImage(cover, coverSrc, this.draft.title || "\u5C01\u9762");
    else cover.createSpan({ text: (this.draft.title || "\u4E66").slice(0, 2) });
    const coverInput = cover.createEl("input", { attr: { type: "file", accept: "image/*" } });
    coverInput.addEventListener("change", async () => {
      var _a2;
      const file = (_a2 = coverInput.files) == null ? void 0 : _a2[0];
      if (!file) return;
      this.draft.coverPath = await this.copyCoverFile(file);
      this.render();
    });
    const fields = form.createDiv({ cls: "cow-book-form-fields" });
    this.bindInput(this.textField(fields, "\u4E66\u540D", this.draft.title), (value) => this.draft.title = value);
    this.bindInput(this.textField(fields, "\u4F5C\u8005", this.draft.author), (value) => this.draft.author = value);
    this.bindInput(this.textField(fields, "\u51FA\u7248\u793E", (_b = this.draft.publisher) != null ? _b : ""), (value) => this.draft.publisher = value);
    this.bindInput(this.textField(fields, "\u51FA\u7248\u65E5\u671F", (_c = this.draft.publishDate) != null ? _c : ""), (value) => this.draft.publishDate = value);
    this.bindInput(this.textField(fields, "ISBN", (_e = (_d = this.draft.isbn13) != null ? _d : this.draft.isbn10) != null ? _e : ""), (value) => {
      if (value.length === 10) this.draft.isbn10 = value;
      else this.draft.isbn13 = value;
    });
    this.bindInput(this.textField(fields, "\u603B\u9875\u6570", String((_f = this.draft.totalPages) != null ? _f : 200), "number"), (value) => this.draft.totalPages = Number(value) || 1);
    this.bindInput(this.textField(fields, "\u5F53\u524D\u9875", String((_g = this.draft.currentPage) != null ? _g : 0), "number"), (value) => this.draft.currentPage = Number(value) || 0);
    this.bindInput(this.textField(fields, "\u5206\u7C7B", (_h = this.draft.category) != null ? _h : ""), (value) => this.draft.category = value);
    this.renderDateField(fields, "\u5F00\u59CB\u9605\u8BFB\u65E5\u671F", "startDate");
    this.renderDateField(fields, "\u7ED3\u675F\u9605\u8BFB\u65E5\u671F", "finishDate");
    this.renderBookFileField(fields);
    this.renderSelect(fields, "\u9605\u8BFB\u72B6\u6001", (_i = this.draft.readingStatus) != null ? _i : "want-to-read", [
      { value: "want-to-read", label: "\u60F3\u8BFB" },
      { value: "reading", label: "\u5728\u8BFB" },
      { value: "finished", label: "\u5DF2\u8BFB" }
    ], (value) => {
      var _a2, _b2;
      this.draft.readingStatus = value;
      if (value === "reading") this.draft.startDate = this.draft.startDate || this.today();
      if (value === "finished") {
        this.draft.startDate = this.draft.startDate || this.today();
        this.draft.finishDate = this.draft.finishDate || this.today();
        this.draft.currentPage = (_b2 = (_a2 = this.draft.totalPages) != null ? _a2 : this.draft.currentPage) != null ? _b2 : 0;
      }
      this.render();
    });
    this.renderSelect(fields, "\u4E66\u67B6\u72B6\u6001", (_j = this.draft.shelfStatus) != null ? _j : "on-shelf", [
      { value: "on-shelf", label: "\u4E0A\u67B6" },
      { value: "off-shelf", label: "\u4E0B\u67B6" }
    ], (value) => this.draft.shelfStatus = value);
    this.renderTagPicker(fields);
    this.bindInput(this.textareaField(fields, "\u7B80\u4ECB", (_k = this.draft.description) != null ? _k : ""), (value) => this.draft.description = value);
  }
  renderBookFileField(container) {
    var _a;
    const row = container.createDiv({ cls: "cow-book-form-row cow-book-file-field" });
    row.createEl("label", { text: "\u4E66\u7C4D\u6587\u4EF6" });
    row.createSpan({ text: (_a = this.draft.bookFilePath) != null ? _a : "\u672A\u5173\u8054\u4E66\u7C4D\u6587\u4EF6\u3002\u641C\u7D22\u5BFC\u5165\u53EA\u4F1A\u5BFC\u5165\u5143\u6570\u636E\uFF1B\u5982\u9700\u9605\u8BFB\u5168\u6587\uFF0C\u8BF7\u9009\u62E9\u672C\u5730 PDF / EPUB \u6587\u4EF6\u3002" });
    const input = row.createEl("input", { attr: { type: "file", accept: ".pdf,.epub,.fb2,.mobi,.azw3,application/pdf" } });
    input.addEventListener("change", async () => {
      var _a2;
      const file = (_a2 = input.files) == null ? void 0 : _a2[0];
      if (!file) return;
      const path = await this.copyBookFile(file);
      if (!path) return;
      this.draft.bookFilePath = path;
      if (!this.draft.title) this.draft.title = file.name.replace(/\.[^.]+$/, "");
      this.render();
    });
  }
  renderActions() {
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: this.book ? "\u4FDD\u5B58" : "\u6DFB\u52A0", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", () => void this.saveBook());
  }
  async saveBook() {
    var _a, _b;
    if (!this.draft.title.trim()) {
      new import_obsidian9.Notice("\u8BF7\u8F93\u5165\u4E66\u540D\u3002");
      return;
    }
    if (this.draft.bookFilePath && !await this.verifyVaultFile(this.draft.bookFilePath)) {
      new import_obsidian9.Notice(`\u4E66\u7C4D\u6587\u4EF6\u5BFC\u5165\u5931\u8D25\uFF1A${this.draft.bookFilePath} \u4E0D\u5B58\u5728\u3002\u5DF2\u53D6\u6D88\u521B\u5EFA\u8BB0\u5F55\u3002`);
      return;
    }
    const book = await this.prepareBook();
    if (this.book) await this.store.updateBook(this.book.id, book);
    else await this.store.addBook(book);
    if (!this.book && this.selectedWantToReadId) {
      await this.store.updateWantToReadItem(this.selectedWantToReadId, { status: "added", bookId: book.id });
    }
    await ((_b = (_a = this.options).onBookSaved) == null ? void 0 : _b.call(_a, book));
    this.onDataChanged();
    this.close();
  }
  async prepareBook() {
    var _a, _b, _c, _d, _e, _f, _g;
    const readingStatus = (_a = this.draft.readingStatus) != null ? _a : "want-to-read";
    if (this.draft.coverUrl && !this.draft.coverPath) {
      this.draft.coverPath = await this.downloadCover(this.draft.coverUrl, this.draft.title);
    }
    return {
      id: (_c = (_b = this.book) == null ? void 0 : _b.id) != null ? _c : `book-${Date.now()}`,
      ...this.book,
      ...this.draft,
      title: this.draft.title.trim(),
      author: this.draft.author.trim() || "\u672A\u77E5\u4F5C\u8005",
      totalPages: Math.max(1, Number(this.draft.totalPages) || 1),
      currentPage: Math.max(0, Math.min(Number(this.draft.currentPage) || 0, Math.max(1, Number(this.draft.totalPages) || 1))),
      status: readingStatus === "reading" ? "\u5728\u8BFB" : readingStatus === "finished" ? "\u5DF2\u8BFB" : "\u60F3\u8BFB",
      readingStatus,
      shelfStatus: (_d = this.draft.shelfStatus) != null ? _d : "on-shelf",
      notePath: this.draft.notePath,
      createdAt: (_f = (_e = this.book) == null ? void 0 : _e.createdAt) != null ? _f : (/* @__PURE__ */ new Date()).toISOString(),
      updatedAt: (/* @__PURE__ */ new Date()).toISOString(),
      tagIds: (_g = this.draft.tagIds) != null ? _g : [],
      tags: this.store.getBookTags().filter((tag) => {
        var _a2;
        return ((_a2 = this.draft.tagIds) != null ? _a2 : []).includes(tag.id);
      }).map((tag) => tag.name)
    };
  }
  renderTagPicker(container) {
    var _a;
    const row = container.createDiv({ cls: "cow-book-form-row cow-book-tag-picker-row" });
    row.createEl("label", { text: "\u6807\u7B7E" });
    const chips = row.createDiv({ cls: "cow-book-tag-chip-list" });
    const selected = new Set((_a = this.draft.tagIds) != null ? _a : []);
    const tags = this.store.getBookTags();
    if (tags.length === 0) {
      chips.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u9605\u8BFB\u6807\u7B7E\uFF0C\u53EF\u5728\u9605\u8BFB\u9875\u7684\u6807\u7B7E\u7BA1\u7406\u4E2D\u65B0\u589E\u3002" });
      return;
    }
    tags.forEach((tag) => {
      const active = selected.has(tag.id);
      const chip = chips.createEl("button", {
        cls: `cow-book-tag-chip ${active ? "is-selected" : ""}`,
        attr: { type: "button" }
      });
      chip.createSpan({ text: active ? "\u2713" : "" });
      chip.createSpan({ text: tag.name });
      chip.style.setProperty("--book-tag-color", tag.color);
      chip.addEventListener("click", () => {
        if (selected.has(tag.id)) selected.delete(tag.id);
        else selected.add(tag.id);
        this.draft.tagIds = [...selected];
        this.draft.tags = this.store.getBookTags().filter((item) => selected.has(item.id)).map((item) => item.name);
        this.render();
      });
    });
  }
  async searchBooks(query) {
    var _a, _b, _c, _d;
    if (!query) return [];
    const results = [];
    try {
      const google = await (0, import_obsidian9.requestUrl)({ url: `https://www.googleapis.com/books/v1/volumes?q=${encodeURIComponent(query)}&maxResults=8` });
      const items = (_b = (_a = google.json) == null ? void 0 : _a.items) != null ? _b : [];
      items.forEach((item) => {
        var _a2, _b2, _c2, _d2, _e, _f, _g;
        const info = (_a2 = item.volumeInfo) != null ? _a2 : {};
        const identifiers = Array.isArray(info.industryIdentifiers) ? info.industryIdentifiers : [];
        const imageLinks = info.imageLinks;
        results.push({
          title: String((_b2 = info.title) != null ? _b2 : ""),
          author: Array.isArray(info.authors) ? info.authors.join(", ") : "",
          publisher: String((_c2 = info.publisher) != null ? _c2 : ""),
          publishDate: String((_d2 = info.publishedDate) != null ? _d2 : ""),
          isbn10: (_e = identifiers.find((id) => id.type === "ISBN_10")) == null ? void 0 : _e.identifier,
          isbn13: (_f = identifiers.find((id) => id.type === "ISBN_13")) == null ? void 0 : _f.identifier,
          totalPages: Number(info.pageCount) || void 0,
          category: Array.isArray(info.categories) ? String(info.categories[0]) : "",
          description: String((_g = info.description) != null ? _g : ""),
          coverUrl: typeof (imageLinks == null ? void 0 : imageLinks.thumbnail) === "string" ? imageLinks.thumbnail.replace("http://", "https://") : void 0
        });
      });
    } catch (e) {
      new import_obsidian9.Notice("Google Books \u641C\u7D22\u5931\u8D25\uFF0C\u5DF2\u5C1D\u8BD5\u7EE7\u7EED\u4F7F\u7528\u5176\u5B83\u6570\u636E\u6E90\u3002");
    }
    try {
      const openLibrary = await (0, import_obsidian9.requestUrl)({ url: `https://openlibrary.org/search.json?q=${encodeURIComponent(query)}&limit=8` });
      const docs = (_d = (_c = openLibrary.json) == null ? void 0 : _c.docs) != null ? _d : [];
      docs.forEach((doc) => {
        var _a2;
        return results.push({
          title: String((_a2 = doc.title) != null ? _a2 : ""),
          author: Array.isArray(doc.author_name) ? doc.author_name.join(", ") : "",
          publisher: Array.isArray(doc.publisher) ? String(doc.publisher[0]) : "",
          publishDate: doc.first_publish_year ? String(doc.first_publish_year) : "",
          isbn10: Array.isArray(doc.isbn) ? doc.isbn.find((value) => String(value).length === 10) : void 0,
          isbn13: Array.isArray(doc.isbn) ? doc.isbn.find((value) => String(value).length === 13) : void 0,
          totalPages: Number(doc.number_of_pages_median) || void 0,
          coverUrl: doc.cover_i ? `https://covers.openlibrary.org/b/id/${doc.cover_i}-M.jpg` : void 0
        });
      });
    } catch (e) {
      new import_obsidian9.Notice("Open Library \u641C\u7D22\u5931\u8D25\uFF0C\u53EF\u7EE7\u7EED\u624B\u52A8\u6DFB\u52A0\u3002");
    }
    return results.filter((item) => item.title).slice(0, 10);
  }
  async ensureMetadataFromQuery(query) {
    var _a;
    const results = await this.searchBooks(query);
    this.searchResults = results;
    if (results[0]) this.draft = { ...this.draft, ...results[0], totalPages: (_a = results[0].totalPages) != null ? _a : this.draft.totalPages };
  }
  async copyBookFile(file) {
    await this.ensureFolder("Books/Files");
    const target = await this.resolveConflict(`Books/Files/${this.safeName(file.name)}`);
    if (!target) return void 0;
    await this.app.vault.adapter.writeBinary(target, await file.arrayBuffer());
    if (!await this.verifyVaultFile(target)) {
      new import_obsidian9.Notice("\u4E66\u7C4D\u6587\u4EF6\u5199\u5165\u5931\u8D25\uFF0C\u6CA1\u6709\u521B\u5EFA\u4E66\u7C4D\u8BB0\u5F55\u3002");
      return void 0;
    }
    return target;
  }
  async copyCoverFile(file) {
    await this.ensureFolder("Books/Covers");
    const target = await this.uniquePath(`Books/Covers/${this.safeName(file.name)}`);
    await this.app.vault.adapter.writeBinary(target, await file.arrayBuffer());
    return target;
  }
  async verifyVaultFile(path) {
    if (!path) return false;
    if (!await this.app.vault.adapter.exists(path)) return false;
    try {
      const stat = await this.app.vault.adapter.stat(path);
      return !stat || stat.size > 0;
    } catch (e) {
      return true;
    }
  }
  async downloadCover(url, title) {
    var _a;
    try {
      await this.ensureFolder("Books/Covers");
      const response = await (0, import_obsidian9.requestUrl)({ url });
      const contentType = (_a = response.headers["content-type"]) != null ? _a : "";
      const extension = contentType.includes("png") ? "png" : "jpg";
      const target = await this.uniquePath(`Books/Covers/${this.safeName(title)}.${extension}`);
      await this.app.vault.adapter.writeBinary(target, response.arrayBuffer);
      return target;
    } catch (e) {
      new import_obsidian9.Notice("\u5C01\u9762\u4FDD\u5B58\u5931\u8D25\uFF0C\u5C06\u4F7F\u7528\u9ED8\u8BA4\u5360\u4F4D\u5C01\u9762\u3002");
      return void 0;
    }
  }
  textField(container, label, value, type = "text") {
    const row = container.createDiv({ cls: "cow-book-form-row" });
    row.createEl("label", { text: label });
    return row.createEl("input", { attr: { type, value } });
  }
  textareaField(container, label, value) {
    const row = container.createDiv({ cls: "cow-book-form-row" });
    row.createEl("label", { text: label });
    return row.createEl("textarea", { text: value });
  }
  bindInput(input, onInput) {
    input.addEventListener("input", () => onInput(input.value));
  }
  renderDateField(container, label, key) {
    var _a;
    const row = container.createDiv({ cls: "cow-book-form-row is-picker" });
    row.createEl("label", { text: label });
    const input = row.createEl("input", { attr: { type: "date", value: (_a = this.draft[key]) != null ? _a : "" } });
    row.addEventListener("click", () => {
      var _a2;
      input.focus();
      try {
        (_a2 = input.showPicker) == null ? void 0 : _a2.call(input);
      } catch (e) {
        input.focus();
      }
    });
    input.addEventListener("change", () => this.draft[key] = input.value);
  }
  renderSelect(container, label, value, options, onChange) {
    const row = container.createDiv({ cls: "cow-book-form-row" });
    row.createEl("label", { text: label });
    const select = row.createEl("select");
    options.forEach((option) => select.createEl("option", { value: option.value, text: option.label }));
    select.value = value;
    select.addEventListener("change", () => onChange(select.value));
  }
  async resolveConflict(path) {
    if (!await this.app.vault.adapter.exists(path)) return path;
    const choice = await new Promise((resolve) => new FileConflictModal(this.app, path, resolve).open());
    if (choice === "cancel") return void 0;
    if (choice === "overwrite") return path;
    return this.uniquePath(path);
  }
  async uniquePath(path) {
    if (!await this.app.vault.adapter.exists(path)) return path;
    const dot = path.lastIndexOf(".");
    const base = dot > -1 ? path.slice(0, dot) : path;
    const ext = dot > -1 ? path.slice(dot) : "";
    let index = 2;
    let candidate = `${base}-${index}${ext}`;
    while (await this.app.vault.adapter.exists(candidate)) {
      index += 1;
      candidate = `${base}-${index}${ext}`;
    }
    return candidate;
  }
  async ensureFolder(path) {
    const parts = path.split("/");
    let current = "";
    for (const part of parts) {
      current = current ? `${current}/${part}` : part;
      if (!await this.app.vault.adapter.exists(current)) await this.app.vault.createFolder(current);
    }
  }
  safeName(value) {
    return value.replace(/[\\/:*?"<>|#^[\]]/g, " ").replace(/\s+/g, " ").trim() || "book";
  }
  renderCoverImage(container, src, title) {
    const img = container.createEl("img", { attr: { src, alt: title } });
    img.addEventListener("error", () => {
      container.empty();
      container.createSpan({ text: title.slice(0, 2) });
    });
  }
  today() {
    return (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
  }
  renderLinkedReadingNotes(container) {
    if (!this.book) return;
    const section = container.createDiv({ cls: "cow-linked-notes-panel" });
    section.createEl("strong", { text: "\u5173\u8054\u9605\u8BFB\u7B14\u8BB0" });
    const notes = this.store.getReadingNotesForBook(this.book.id);
    if (notes.length === 0) {
      section.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u5DF2\u7ED1\u5B9A\u7684 Vault Markdown \u9605\u8BFB\u7B14\u8BB0\u3002" });
      return;
    }
    const list = section.createDiv({ cls: "cow-data-list cow-reading-note-mini-list" });
    notes.forEach((note) => {
      const row = list.createDiv({ cls: "cow-data-card cow-click-card" });
      const head = row.createDiv({ cls: "cow-list-item-head" });
      head.createEl("strong", { text: note.title });
      head.createDiv({ cls: "cow-list-item-actions" }).createEl("button", { text: "\u6253\u5F00", attr: { type: "button" } }).addEventListener("click", (event) => {
        event.stopPropagation();
        void openVaultMarkdown(this.app, note.notePath);
      });
      row.createDiv({ cls: "cow-meta-line", text: note.notePath });
      row.addEventListener("click", () => void openVaultMarkdown(this.app, note.notePath));
    });
  }
};
var FileConflictModal = class extends import_obsidian9.Modal {
  constructor(app, path, resolve) {
    super(app);
    this.path = path;
    this.resolve = resolve;
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-file-conflict-modal",
      width: "min(520px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "80vh",
      minWidth: "min(360px, 90vw)",
      minHeight: "min(220px, 70vh)"
    });
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: "\u6587\u4EF6\u5DF2\u5B58\u5728" });
    this.contentEl.createEl("p", { text: this.path });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.closeWith("cancel"));
    actions.createEl("button", { text: "\u81EA\u52A8\u91CD\u547D\u540D", attr: { type: "button" } }).addEventListener("click", () => this.closeWith("rename"));
    actions.createEl("button", { text: "\u8986\u76D6", cls: "mod-warning", attr: { type: "button" } }).addEventListener("click", () => this.closeWith("overwrite"));
  }
  closeWith(choice) {
    this.resolve(choice);
    this.close();
  }
};

// src/components/research/LiteratureNoteModals.ts
var import_obsidian10 = require("obsidian");
function openLiteratureNoteModal(app, store, onDone, note, preset) {
  new LiteratureNoteModal(app, store, onDone, note, preset).open();
}
var LiteratureNoteModal = class extends import_obsidian10.Modal {
  constructor(app, store, onDone, note, preset) {
    var _a, _b, _c, _d, _e, _f;
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.note = note;
    this.title = (_b = (_a = note == null ? void 0 : note.title) != null ? _a : preset == null ? void 0 : preset.title) != null ? _b : "";
    this.notePath = (_d = (_c = note == null ? void 0 : note.notePath) != null ? _c : preset == null ? void 0 : preset.notePath) != null ? _d : "";
    this.paperReadingId = (_f = (_e = note == null ? void 0 : note.paperReadingId) != null ? _e : preset == null ? void 0 : preset.paperReadingId) != null ? _f : "";
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-literature-note-modal",
      width: "min(760px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "92vh",
      minWidth: "min(520px, 90vw)",
      minHeight: "min(420px, 82vh)"
    });
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-literature-note-modal");
    this.contentEl.createEl("h2", { text: this.note ? "\u7F16\u8F91\u6587\u732E\u7B14\u8BB0" : "\u65B0\u589E\u6587\u732E\u7B14\u8BB0" });
    const form = this.contentEl.createDiv({ cls: "cow-paper-form" });
    this.titleInput = this.inputField(form, "\u6807\u9898", this.title, (value) => this.title = value);
    this.paperSelect(form);
    new MarkdownFilePicker(this.app, {
      label: "\u5173\u8054 Markdown",
      placeholder: "\u641C\u7D22 Vault \u4E2D\u7684 Markdown \u6587\u4EF6\u2026\u2026",
      value: this.notePath,
      onChange: (path) => {
        this.notePath = path;
        if (path && !this.title.trim()) {
          const file = this.app.vault.getAbstractFileByPath(path);
          this.title = file instanceof import_obsidian10.TFile ? file.basename : fileName2(path).replace(/\.md$/i, "");
          if (this.titleInput) this.titleInput.value = this.title;
        }
      }
    }).render(this.contentEl);
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", () => void this.save());
  }
  paperSelect(container) {
    const row = container.createDiv({ cls: "cow-book-form-row" });
    row.createEl("label", { text: "\u6240\u5C5E\u8BBA\u6587" });
    const select = row.createEl("select");
    select.createEl("option", { value: "", text: "\u65E0" });
    this.store.getResearchPapers().forEach((paper) => select.createEl("option", { value: paper.id, text: paper.title }));
    select.value = this.paperReadingId;
    select.addEventListener("change", () => this.paperReadingId = select.value);
  }
  inputField(container, label, value, onInput) {
    const row = container.createDiv({ cls: "cow-book-form-row" });
    row.createEl("label", { text: label });
    const input = row.createEl("input", { attr: { type: "text", value } });
    input.addEventListener("input", () => onInput(input.value));
    return input;
  }
  async save() {
    var _a, _b, _c, _d;
    if (!this.title.trim()) {
      new import_obsidian10.Notice("\u8BF7\u8F93\u5165\u6587\u732E\u7B14\u8BB0\u6807\u9898\u3002");
      return;
    }
    if (!this.notePath.trim()) {
      new import_obsidian10.Notice("\u8BF7\u9009\u62E9\u6216\u586B\u5199 Markdown \u7B14\u8BB0\u8DEF\u5F84\u3002");
      return;
    }
    const file = this.app.vault.getFileByPath(this.notePath);
    if (!(file instanceof import_obsidian10.TFile)) {
      new import_obsidian10.Notice("\u7B14\u8BB0\u6587\u4EF6\u4E0D\u5B58\u5728\u3002");
      return;
    }
    const now = Date.now();
    const payload = {
      id: (_b = (_a = this.note) == null ? void 0 : _a.id) != null ? _b : `literature-note-${now}`,
      title: this.title.trim(),
      notePath: file.path,
      paperReadingId: this.paperReadingId || void 0,
      createdAt: (_d = (_c = this.note) == null ? void 0 : _c.createdAt) != null ? _d : now,
      updatedAt: now
    };
    if (this.note) await this.store.updateLiteratureNote(this.note.id, payload);
    else await this.store.upsertLiteratureNote(payload);
    this.onDone();
    this.close();
  }
};
var MissingLiteratureNoteModal = class extends import_obsidian10.Modal {
  constructor(app, store, note, onDone) {
    super(app);
    this.store = store;
    this.note = note;
    this.onDone = onDone;
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-literature-note-modal",
      width: "min(560px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "84vh",
      minWidth: "min(420px, 90vw)",
      minHeight: "min(260px, 70vh)"
    });
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: "\u7B14\u8BB0\u6587\u4EF6\u4E0D\u5B58\u5728" });
    this.contentEl.createEl("p", { text: this.note.notePath });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u91CD\u65B0\u5173\u8054", attr: { type: "button" } }).addEventListener("click", () => {
      this.close();
      openLiteratureNoteModal(this.app, this.store, this.onDone, this.note);
    });
    actions.createEl("button", { text: "\u6E05\u7406\u5931\u6548\u94FE\u63A5", cls: "mod-warning", attr: { type: "button" } }).addEventListener("click", () => void this.clear());
  }
  async clear() {
    await this.store.deleteLiteratureNote(this.note.id);
    this.onDone();
    this.close();
  }
};
var DeleteLiteratureNoteModal = class extends import_obsidian10.Modal {
  constructor(app, store, note, onDone) {
    super(app);
    this.store = store;
    this.note = note;
    this.onDone = onDone;
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-literature-note-modal",
      width: "min(580px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "84vh",
      minWidth: "min(420px, 90vw)",
      minHeight: "min(300px, 70vh)"
    });
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-delete-modal");
    this.contentEl.createEl("h2", { text: "\u5220\u9664\u6587\u732E\u7B14\u8BB0" });
    const summary = this.contentEl.createDiv({ cls: "cow-delete-summary" });
    summary.createSpan({ text: "\u7B14\u8BB0\uFF1A" });
    summary.createEl("strong", { text: `\u300A${this.note.title}\u300B` });
    const description = this.contentEl.createDiv({ cls: "cow-delete-description" });
    description.createEl("p", { text: "\u53EF\u4EE5\u53EA\u4ECE\u79D1\u7814\u6587\u732E\u7B14\u8BB0\u4E2D\u79FB\u9664\u8BB0\u5F55\uFF0C\u4E5F\u53EF\u4EE5\u540C\u65F6\u628A\u5BF9\u5E94 Markdown \u79FB\u5165\u56DE\u6536\u7AD9\u3002" });
    description.createEl("p", { text: "\u4E0D\u4F1A\u5220\u9664 Zotero \u6761\u76EE\u3001Zotero PDF \u6216\u5176\u5B83\u672C\u5730\u8BBA\u6587\u6587\u4EF6\u3002" });
    description.createDiv({ cls: "cow-meta-line", text: this.note.notePath });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4EC5\u4ECE\u79D1\u7814\u6587\u732E\u7B14\u8BB0\u4E2D\u79FB\u9664", attr: { type: "button" } }).addEventListener("click", () => void this.removeRecordOnly());
    actions.createEl("button", { text: "\u540C\u65F6\u5220\u9664 Obsidian \u7B14\u8BB0\u6587\u4EF6", cls: "mod-warning", attr: { type: "button" } }).addEventListener("click", () => void this.trashMarkdownAndRemove());
  }
  async removeRecordOnly() {
    await this.store.deleteLiteratureNote(this.note.id);
    new import_obsidian10.Notice("\u6587\u732E\u7B14\u8BB0\u8BB0\u5F55\u5DF2\u79FB\u9664\uFF0CMarkdown \u6587\u4EF6\u5DF2\u4FDD\u7559\u3002");
    this.onDone();
    this.close();
  }
  async trashMarkdownAndRemove() {
    const file = this.app.vault.getFileByPath(this.note.notePath);
    if (file instanceof import_obsidian10.TFile) {
      await this.app.vault.trash(file, true);
    } else {
      new import_obsidian10.Notice("\u7B14\u8BB0\u6587\u4EF6\u4E0D\u5B58\u5728\uFF0C\u5DF2\u6E05\u7406\u79D1\u7814\u8BB0\u5F55\u3002");
    }
    await this.store.deleteLiteratureNote(this.note.id, { clearPaperNotePath: true });
    new import_obsidian10.Notice("\u6587\u732E\u7B14\u8BB0\u5DF2\u79FB\u9664\u3002");
    this.onDone();
    this.close();
  }
};
async function openLiteratureNoteFile(app, store, note, onDone) {
  const file = app.vault.getAbstractFileByPath(note.notePath);
  if (!(file instanceof import_obsidian10.TFile)) {
    new import_obsidian10.Notice("\u672A\u627E\u5230\u5BF9\u5E94 Markdown \u6587\u4EF6\u3002");
    new MissingLiteratureNoteModal(app, store, note, onDone).open();
    return;
  }
  await app.workspace.getLeaf(false).openFile(file);
}
function paperLabel(paper) {
  var _a;
  return (_a = paper == null ? void 0 : paper.title) != null ? _a : "\u672A\u5173\u8054\u8BBA\u6587";
}
function fileName2(path) {
  return path.split(/[\\/]/).pop() || path;
}

// src/components/research/ExperimentModals.ts
var import_obsidian11 = require("obsidian");
function openExperimentEditModal(app, store, mode, onDone, item) {
  new ExperimentEditModal(app, store, mode, onDone, item).open();
}
var ExperimentEditModal = class extends import_obsidian11.Modal {
  constructor(app, store, mode, onDone, item) {
    var _a, _b, _c, _d, _e, _f;
    super(app);
    this.store = store;
    this.mode = mode;
    this.onDone = onDone;
    this.item = item;
    this.title = (_a = item == null ? void 0 : item.title) != null ? _a : "";
    this.date = (_b = item == null ? void 0 : item.date) != null ? _b : todayKey2();
    this.status = (_c = item == null ? void 0 : item.status) != null ? _c : "\u8BA1\u5212\u4E2D";
    this.notePath = (_d = item == null ? void 0 : item.notePath) != null ? _d : "";
    this.researchProjectId = (_e = item == null ? void 0 : item.researchProjectId) != null ? _e : "";
    this.experimentPlanId = (_f = item == null ? void 0 : item.experimentPlanId) != null ? _f : "";
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-experiment-modal",
      width: "min(760px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "92vh",
      minWidth: "min(520px, 90vw)",
      minHeight: "min(360px, 78vh)"
    });
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-experiment-modal");
    this.contentEl.createEl("h2", { text: this.item ? "\u7F16\u8F91\u5B9E\u9A8C" : this.mode === "plan" ? "\u65B0\u589E\u5B9E\u9A8C\u8BA1\u5212" : "\u65B0\u589E\u5B9E\u9A8C\u8BB0\u5F55" });
    const form = this.contentEl.createDiv({ cls: "cow-paper-form" });
    inputField(form, "\u6807\u9898", this.title, (value) => this.title = value);
    dateField(form, "\u65E5\u671F", this.date, (value) => this.date = value);
    selectField(form, "\u72B6\u6001", this.status, ["\u672A\u5F00\u59CB", "\u8BA1\u5212\u4E2D", "\u8FDB\u884C\u4E2D", "\u5DF2\u5B8C\u6210"].map((value) => ({ value, label: value })), (value) => this.status = value);
    if (this.mode === "plan") {
      selectField(form, "\u7814\u7A76\u9879\u76EE", this.researchProjectId, [
        { value: "", label: "\u672A\u5173\u8054" },
        ...this.store.getResearchProjects().map((project) => ({ value: project.id, label: project.title }))
      ], (value) => this.researchProjectId = value);
    }
    if (this.mode === "records") {
      selectField(form, "\u6240\u5C5E\u5B9E\u9A8C\u8BA1\u5212", this.experimentPlanId, [
        { value: "", label: "\u65E0" },
        ...this.store.getExperimentPlans().map((plan) => ({ value: plan.id, label: plan.title }))
      ], (value) => this.experimentPlanId = value);
    }
    new MarkdownFilePicker(this.app, {
      label: this.mode === "plan" ? "\u5173\u8054\u5B9E\u9A8C\u65B9\u6848\u7B14\u8BB0" : "\u5173\u8054\u5B9E\u9A8C\u8BB0\u5F55\u7B14\u8BB0",
      placeholder: "\u641C\u7D22 Vault \u4E2D\u7684 Markdown \u6587\u4EF6\u2026\u2026",
      value: this.notePath,
      onChange: (path) => this.notePath = path
    }).render(this.contentEl);
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", () => void this.save());
  }
  async save() {
    var _a, _b;
    if (!this.title.trim()) {
      new import_obsidian11.Notice("\u8BF7\u8F93\u5165\u6807\u9898\u3002");
      return;
    }
    const payload = {
      id: (_b = (_a = this.item) == null ? void 0 : _a.id) != null ? _b : `experiment-${Date.now()}`,
      title: this.title.trim(),
      date: this.date,
      status: this.status,
      notePath: this.notePath || void 0,
      researchProjectId: this.mode === "plan" ? this.researchProjectId || void 0 : void 0,
      experimentPlanId: this.mode === "records" ? this.experimentPlanId || void 0 : void 0
    };
    if (this.item) await this.store.updateExperiment(this.mode, this.item.id, payload);
    else await this.store.addExperiment(this.mode, payload);
    this.onDone();
    this.close();
  }
};
var ExperimentPlanDetailModal = class extends import_obsidian11.Modal {
  constructor(app, store, plan, onDone) {
    super(app);
    this.store = store;
    this.plan = plan;
    this.onDone = onDone;
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-experiment-modal",
      width: "min(760px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "92vh",
      minWidth: "min(520px, 90vw)",
      minHeight: "min(360px, 78vh)"
    });
    this.render();
  }
  render() {
    var _a;
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-experiment-modal");
    this.contentEl.createEl("h2", { text: this.plan.title });
    const project = this.store.getResearchProjects().find((item) => item.id === this.plan.researchProjectId);
    const summary = this.contentEl.createDiv({ cls: "cow-experiment-summary-grid" });
    renderInfoItem(summary, "\u72B6\u6001", this.plan.status);
    renderInfoItem(summary, "\u65E5\u671F", this.plan.date);
    renderInfoItem(summary, "\u6240\u5C5E\u9879\u76EE", (_a = project == null ? void 0 : project.title) != null ? _a : "\u672A\u5173\u8054\u9879\u76EE");
    renderLinkedMarkdown(this.app, this.contentEl, "\u5B9E\u9A8C\u65B9\u6848\u7B14\u8BB0", this.plan.notePath);
    this.contentEl.createEl("h3", { text: "\u5B9E\u9A8C\u8BB0\u5F55" });
    const records = this.store.getExperimentRecords().filter((record) => record.experimentPlanId === this.plan.id);
    const list = this.contentEl.createDiv({ cls: "cow-data-list" });
    if (records.length === 0) {
      list.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u5173\u8054\u5B9E\u9A8C\u8BB0\u5F55\u3002" });
      return;
    }
    records.forEach((record) => {
      const row = list.createEl("button", { cls: "cow-data-card cow-click-card", attr: { type: "button" } });
      row.createEl("strong", { text: record.title });
      row.createDiv({ cls: "cow-meta-line", text: record.date });
      row.createDiv({ cls: "cow-meta-line", text: record.status });
      row.addEventListener("click", () => new ExperimentRecordDetailModal(this.app, this.store, record, this.onDone).open());
    });
  }
};
var ExperimentRecordDetailModal = class extends import_obsidian11.Modal {
  constructor(app, store, record, onDone) {
    super(app);
    this.store = store;
    this.record = record;
    this.onDone = onDone;
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-experiment-modal",
      width: "min(680px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "90vh",
      minWidth: "min(480px, 90vw)",
      minHeight: "min(320px, 74vh)"
    });
    this.render();
  }
  render() {
    var _a;
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-experiment-modal");
    this.contentEl.createEl("h2", { text: this.record.title });
    const plan = this.store.getExperimentPlans().find((item) => item.id === this.record.experimentPlanId);
    const project = plan ? this.store.getResearchProjects().find((item) => item.id === plan.researchProjectId) : void 0;
    const summary = this.contentEl.createDiv({ cls: "cow-experiment-summary-grid" });
    renderInfoItem(summary, "\u65E5\u671F", this.record.date);
    renderInfoItem(summary, "\u72B6\u6001", this.record.status);
    renderInfoItem(summary, "\u6240\u5C5E\u9879\u76EE", (_a = project == null ? void 0 : project.title) != null ? _a : "\u672A\u5173\u8054\u9879\u76EE");
    const planSection = this.contentEl.createDiv({ cls: "cow-selected-note-file cow-experiment-plan-link" });
    planSection.createEl("strong", { text: "\u6240\u5C5E\u5B9E\u9A8C\u8BA1\u5212" });
    if (plan) {
      const link = planSection.createEl("button", { cls: "cow-inline-link-card", attr: { type: "button" } });
      (0, import_obsidian11.setIcon)(link.createSpan(), "link");
      link.createSpan({ text: plan.title });
      link.addEventListener("click", () => new ExperimentPlanDetailModal(this.app, this.store, plan, this.onDone).open());
    } else {
      planSection.createDiv({ cls: "cow-meta-line", text: "\u672A\u5173\u8054\u5B9E\u9A8C\u8BA1\u5212" });
    }
    const result = this.contentEl.createDiv({ cls: "cow-experiment-result-summary" });
    result.createEl("strong", { text: "\u7ED3\u679C\u6458\u8981" });
    result.createDiv({ cls: "cow-meta-line", text: this.record.status === "\u5DF2\u5B8C\u6210" ? "\u5B9E\u9A8C\u8BB0\u5F55\u5DF2\u5B8C\u6210\uFF0C\u53EF\u6253\u5F00\u5173\u8054\u7B14\u8BB0\u67E5\u770B\u8BE6\u7EC6\u7ED3\u679C\u3002" : "\u6682\u672A\u8BB0\u5F55\u7ED3\u679C\u6458\u8981\u3002" });
    renderLinkedMarkdown(this.app, this.contentEl, "\u5173\u8054\u5B9E\u9A8C\u8BB0\u5F55\u7B14\u8BB0", this.record.notePath);
  }
};
var DeleteExperimentPlanModal = class extends import_obsidian11.Modal {
  constructor(app, store, plan, onDone) {
    super(app);
    this.store = store;
    this.plan = plan;
    this.onDone = onDone;
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-experiment-modal",
      width: "min(620px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "86vh",
      minWidth: "min(420px, 90vw)",
      minHeight: "min(260px, 70vh)"
    });
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    const records = this.store.getExperimentRecords().filter((record) => record.experimentPlanId === this.plan.id);
    this.contentEl.createEl("h2", { text: "\u5220\u9664\u5B9E\u9A8C\u8BA1\u5212\uFF1F" });
    this.contentEl.createEl("p", { text: records.length > 0 ? `\u5B58\u5728 ${records.length} \u6761\u5B9E\u9A8C\u8BB0\u5F55\u5173\u8054\u5230\u8BE5\u8BA1\u5212\u3002` : "\u6CA1\u6709\u5B9E\u9A8C\u8BB0\u5F55\u5173\u8054\u5230\u8BE5\u8BA1\u5212\u3002" });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    if (records.length > 0) {
      actions.createEl("button", { text: "\u89E3\u9664\u5173\u8054", attr: { type: "button" } }).addEventListener("click", () => void this.deleteWithMigration(void 0));
      const other = this.store.getExperimentPlans().find((plan) => plan.id !== this.plan.id);
      if (other) {
        actions.createEl("button", { text: `\u8FC1\u79FB\u5230\uFF1A${other.title}`, attr: { type: "button" } }).addEventListener("click", () => void this.deleteWithMigration(other.id));
      }
    } else {
      actions.createEl("button", { text: "\u5220\u9664", cls: "mod-warning", attr: { type: "button" } }).addEventListener("click", () => void this.deleteWithMigration(void 0));
    }
  }
  async deleteWithMigration(targetPlanId) {
    await this.store.migrateExperimentRecordsToPlan(this.plan.id, targetPlanId);
    await this.store.deleteExperiment("plan", this.plan.id);
    this.onDone();
    this.close();
  }
};
function inputField(container, label, value, onInput) {
  const row = container.createDiv({ cls: "cow-book-form-row" });
  row.createEl("label", { text: label });
  const input = row.createEl("input", { attr: { type: "text", value } });
  input.addEventListener("input", () => onInput(input.value));
}
function dateField(container, label, value, onChange) {
  const row = container.createDiv({ cls: "cow-book-form-row is-picker" });
  row.createEl("label", { text: label });
  const input = row.createEl("input", { attr: { type: "date", value } });
  row.addEventListener("click", () => {
    var _a;
    input.focus();
    try {
      (_a = input.showPicker) == null ? void 0 : _a.call(input);
    } catch (e) {
      input.focus();
    }
  });
  input.addEventListener("change", () => onChange(input.value));
}
function selectField(container, label, value, options, onChange) {
  const row = container.createDiv({ cls: "cow-book-form-row" });
  row.createEl("label", { text: label });
  const select = row.createEl("select");
  options.forEach((option) => select.createEl("option", { value: option.value, text: option.label }));
  select.value = value;
  select.addEventListener("change", () => onChange(select.value));
}
function renderLinkedMarkdown(app, container, title, notePath) {
  const section = container.createDiv({ cls: "cow-selected-note-file" });
  section.createEl("strong", { text: title });
  if (!notePath) {
    section.createDiv({ cls: "cow-meta-line", text: "\u5C1A\u672A\u5173\u8054 Markdown \u6587\u4EF6\u3002" });
    return;
  }
  section.createDiv({ cls: "cow-selected-note-title", text: `\u{1F4C4} ${fileName3(notePath)}` });
  section.createDiv({ cls: "cow-meta-line", text: notePath });
  const actions = section.createDiv({ cls: "cow-list-item-actions" });
  actions.createEl("button", { text: "\u6253\u5F00", attr: { type: "button" } }).addEventListener("click", () => void openVaultMarkdown(app, notePath));
}
function renderInfoItem(container, label, value) {
  const item = container.createDiv({ cls: "cow-experiment-info-item" });
  item.createSpan({ cls: "cow-meta-line", text: label });
  item.createEl("strong", { text: value });
}
function fileName3(path) {
  return path.split(/[\\/]/).pop() || path;
}
function todayKey2() {
  return (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
}

// src/components/finance/AddTransactionModal.ts
var import_obsidian13 = require("obsidian");

// src/components/finance/accountIcons.ts
var import_obsidian12 = require("obsidian");
function getAccountTypeIcon(type) {
  if (type === "\u73B0\u91D1") return "wallet";
  if (type === "\u50A8\u84C4\u5361") return "landmark";
  if (type === "\u4FE1\u7528\u5361") return "credit-card";
  if (type === "\u652F\u4ED8\u5B9D" || type === "\u5FAE\u4FE1\u94B1\u5305") return "smartphone";
  if (type === "\u6295\u8D44\u8D26\u6237" || type === "\u8BC1\u5238") return "chart-no-axes-combined";
  return "circle-dollar-sign";
}
function renderAccountIcon(container, type) {
  const icon = container.createSpan({ cls: "cow-account-icon" });
  (0, import_obsidian12.setIcon)(icon, getAccountTypeIcon(type));
  return icon;
}
function accountLabel(account) {
  return account ? `${account.type} \xB7 ${account.name}` : "\u672A\u9009\u62E9\u8D26\u6237";
}

// src/components/finance/AddTransactionModal.ts
var INCOME_CATEGORIES = ["\u5DE5\u8D44", "\u5956\u91D1", "\u517C\u804C", "\u6295\u8D44", "\u9000\u6B3E", "\u5176\u5B83"];
function today() {
  return formatDateKey(/* @__PURE__ */ new Date());
}
function createClickableInput(container, label, type, value) {
  const row = container.createDiv({ cls: `cow-finance-form-row ${type === "date" || type === "time" ? "is-picker" : ""}` });
  row.createEl("label", { text: label });
  const input = row.createEl("input", { attr: { type, value } });
  if (type === "date" || type === "time") {
    row.addEventListener("click", () => {
      var _a;
      input.focus();
      try {
        (_a = input.showPicker) == null ? void 0 : _a.call(input);
      } catch (e) {
        input.focus();
      }
    });
  }
  return input;
}
var AddTransactionModal = class extends import_obsidian13.Modal {
  constructor(app, onSubmit, transaction, budgets = [], accounts = []) {
    var _a;
    super(app);
    this.onSubmit = onSubmit;
    this.transaction = transaction;
    this.budgets = budgets;
    this.accounts = accounts;
    this.type = (_a = transaction == null ? void 0 : transaction.type) != null ? _a : "expense";
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-finance-edit-modal",
      width: "min(760px, 90vw)",
      maxWidth: "94vw",
      maxHeight: "88vh",
      minWidth: "min(420px, 90vw)",
      minHeight: "min(320px, 80vh)"
    });
    this.render();
  }
  render() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-finance-modal");
    this.contentEl.createEl("h2", { text: this.transaction ? "\u7F16\u8F91\u8BB0\u8D26" : "\u65B0\u589E\u8BB0\u8D26" });
    const segment = this.contentEl.createDiv({ cls: "cow-finance-segment" });
    ["expense", "income"].forEach((type) => {
      const button = segment.createEl("button", {
        cls: this.type === type ? "is-active" : "",
        text: type === "expense" ? "\u652F\u51FA" : "\u6536\u5165",
        attr: { type: "button" }
      });
      button.addEventListener("click", () => {
        this.type = type;
        this.render();
      });
    });
    const form = this.contentEl.createDiv({ cls: "cow-finance-form-grid" });
    const categoryRow = form.createDiv({ cls: "cow-finance-form-row" });
    categoryRow.createEl("label", { text: "\u5206\u7C7B" });
    const category = categoryRow.createEl("select");
    const options = this.type === "income" ? INCOME_CATEGORIES : this.getExpenseCategories();
    options.forEach((item) => category.createEl("option", { text: item, value: item }));
    category.value = ((_a = this.transaction) == null ? void 0 : _a.category) && options.includes(this.transaction.category) ? this.transaction.category : (_b = options[0]) != null ? _b : "\u5176\u5B83";
    const amount = createClickableInput(form, "\u91D1\u989D", "number", String((_d = (_c = this.transaction) == null ? void 0 : _c.amount) != null ? _d : ""));
    amount.min = "0.01";
    amount.step = "0.01";
    const date = createClickableInput(form, "\u65E5\u671F", "date", (_f = (_e = this.transaction) == null ? void 0 : _e.date) != null ? _f : today());
    const accountRow = form.createDiv({ cls: "cow-finance-form-row" });
    accountRow.createEl("label", { text: "\u8D26\u6237" });
    const account = accountRow.createEl("select");
    account.createEl("option", { value: "", text: "\u672A\u9009\u62E9\u8D26\u6237" });
    this.accounts.forEach((item) => account.createEl("option", { value: item.id, text: `${item.type} \xB7 ${item.name}` }));
    account.value = (_h = (_g = this.transaction) == null ? void 0 : _g.accountId) != null ? _h : "";
    const accountPreview = this.contentEl.createDiv({ cls: "cow-transaction-account-preview" });
    const renderPreview = () => {
      accountPreview.empty();
      const selected = this.accounts.find((item) => item.id === account.value);
      if (selected) renderAccountIcon(accountPreview, selected.type);
      accountPreview.createSpan({ text: selected ? accountLabel(selected) : "\u672A\u9009\u62E9\u8D26\u6237\uFF0C\u53EF\u4EE5\u5148\u5728\u201C\u8D26\u6237\u603B\u89C8\u201D\u4E2D\u6DFB\u52A0\u8D26\u6237\u3002" });
    };
    account.addEventListener("change", renderPreview);
    renderPreview();
    const noteRow = this.contentEl.createDiv({ cls: "cow-finance-form-row" });
    noteRow.createEl("label", { text: "\u5907\u6CE8" });
    const note = noteRow.createEl("textarea", { text: (_j = (_i = this.transaction) == null ? void 0 : _i.note) != null ? _j : "" });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58\u8BB0\u8D26", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2;
      const numericAmount = Number(amount.value) || 0;
      if (numericAmount <= 0) {
        new import_obsidian13.Notice("\u91D1\u989D\u5FC5\u987B\u5927\u4E8E 0\u3002");
        return;
      }
      await this.onSubmit({
        id: (_b2 = (_a2 = this.transaction) == null ? void 0 : _a2.id) != null ? _b2 : `tx-${Date.now()}`,
        type: this.type,
        category: category.value || "\u5176\u5B83",
        amount: Math.abs(numericAmount),
        date: date.value || today(),
        note: note.value.trim(),
        accountId: account.value || void 0
      });
      this.close();
    });
  }
  getExpenseCategories() {
    const categories = this.budgets.map((budget) => budget.category).filter(Boolean);
    return categories.length > 0 ? categories : ["\u9910\u996E", "\u5C45\u4F4F", "\u4EA4\u901A", "\u8D2D\u7269", "\u5B66\u4E60", "\u5A31\u4E50", "\u5176\u5B83"];
  }
};

// src/components/goals/GoalModals.ts
var import_obsidian14 = require("obsidian");
var GoalEditorModal = class extends import_obsidian14.Modal {
  constructor(app, goal, onSubmit) {
    var _a, _b, _c, _d, _e, _f;
    super(app);
    this.goal = goal;
    this.onSubmit = onSubmit;
    this.title = (_a = goal == null ? void 0 : goal.title) != null ? _a : "";
    this.description = (_b = goal == null ? void 0 : goal.description) != null ? _b : "";
    this.category = (_c = goal == null ? void 0 : goal.category) != null ? _c : "\u4E2A\u4EBA";
    this.progress = (_d = goal == null ? void 0 : goal.progress) != null ? _d : 0;
    this.deadline = (_e = goal == null ? void 0 : goal.deadline) != null ? _e : formatDateKey(/* @__PURE__ */ new Date());
    this.status = (_f = goal == null ? void 0 : goal.status) != null ? _f : "\u8FDB\u884C\u4E2D";
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: this.goal ? "\u4FEE\u6539\u76EE\u6807" : "\u65B0\u589E\u76EE\u6807" });
    new import_obsidian14.Setting(this.contentEl).setName("\u6807\u9898").addText((text) => text.setValue(this.title).onChange((value) => this.title = value.trim()));
    new import_obsidian14.Setting(this.contentEl).setName("\u63CF\u8FF0").addTextArea((text) => text.setValue(this.description).onChange((value) => this.description = value.trim()));
    new import_obsidian14.Setting(this.contentEl).setName("\u5206\u7C7B").addText((text) => text.setValue(this.category).onChange((value) => this.category = value.trim() || "\u4E2A\u4EBA"));
    new import_obsidian14.Setting(this.contentEl).setName("\u8FDB\u5EA6").addText((text) => {
      text.inputEl.type = "number";
      text.setValue(String(this.progress));
      text.onChange((value) => this.progress = Number(value) || 0);
    });
    new import_obsidian14.Setting(this.contentEl).setName("\u622A\u6B62\u65E5\u671F").addText((text) => text.setValue(this.deadline).onChange((value) => this.deadline = value.trim()));
    new import_obsidian14.Setting(this.contentEl).setName("\u72B6\u6001").addDropdown((dropdown) => {
      ["\u672A\u5F00\u59CB", "\u8FDB\u884C\u4E2D", "\u5DF2\u5B8C\u6210", "\u6682\u505C"].forEach((status) => dropdown.addOption(status, status));
      dropdown.setValue(this.status);
      dropdown.onChange((value) => this.status = value);
    });
    this.renderActions(async () => {
      var _a, _b;
      if (!this.title) return;
      await this.onSubmit({
        id: (_b = (_a = this.goal) == null ? void 0 : _a.id) != null ? _b : `goal-${Date.now()}`,
        title: this.title,
        description: this.description,
        category: this.category,
        progress: Math.max(0, Math.min(100, this.progress)),
        deadline: this.deadline,
        status: this.status
      });
    });
  }
  renderActions(onSave) {
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", attr: { type: "button" } }).addEventListener("click", async () => {
      await onSave();
      this.close();
    });
  }
};
var KeyResultModal = class extends import_obsidian14.Modal {
  constructor(app, objectives, onSubmit) {
    var _a, _b;
    super(app);
    this.onSubmit = onSubmit;
    this.title = "";
    this.progress = 0;
    this.objectiveId = (_b = (_a = objectives[0]) == null ? void 0 : _a.id) != null ? _b : "";
    this.objectives = objectives;
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: "\u65B0\u589E\u6708\u5EA6\u76EE\u6807" });
    new import_obsidian14.Setting(this.contentEl).setName("\u5B63\u5EA6\u76EE\u6807").addDropdown((dropdown) => {
      this.objectives.forEach((objective) => dropdown.addOption(objective.id, `${objective.quarter} \xB7 ${objective.title}`));
      dropdown.setValue(this.objectiveId);
      dropdown.onChange((value) => this.objectiveId = value);
    });
    new import_obsidian14.Setting(this.contentEl).setName("\u6807\u9898").addText((text) => text.onChange((value) => this.title = value.trim()));
    new import_obsidian14.Setting(this.contentEl).setName("\u8FDB\u5EA6").addText((text) => {
      text.inputEl.type = "number";
      text.onChange((value) => this.progress = Number(value) || 0);
    });
    this.renderActions(async () => {
      if (!this.title || !this.objectiveId) return;
      await this.onSubmit({
        id: `kr-${Date.now()}`,
        objectiveId: this.objectiveId,
        title: this.title,
        progress: Math.max(0, Math.min(100, this.progress)),
        completed: false
      });
    });
  }
  renderActions(onSave) {
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u6DFB\u52A0", attr: { type: "button" } }).addEventListener("click", async () => {
      await onSave();
      this.close();
    });
  }
};
var MilestoneModal = class extends import_obsidian14.Modal {
  constructor(app, goals, onSubmit) {
    var _a, _b;
    super(app);
    this.goals = goals;
    this.onSubmit = onSubmit;
    this.title = "";
    this.date = formatDateKey(/* @__PURE__ */ new Date());
    this.goalId = (_b = (_a = goals[0]) == null ? void 0 : _a.id) != null ? _b : "";
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: "\u65B0\u589E\u91CC\u7A0B\u7891" });
    new import_obsidian14.Setting(this.contentEl).setName("\u76EE\u6807").addDropdown((dropdown) => {
      this.goals.forEach((goal) => dropdown.addOption(goal.id, goal.title));
      dropdown.setValue(this.goalId);
      dropdown.onChange((value) => this.goalId = value);
    });
    new import_obsidian14.Setting(this.contentEl).setName("\u6807\u9898").addText((text) => text.onChange((value) => this.title = value.trim()));
    new import_obsidian14.Setting(this.contentEl).setName("\u65E5\u671F").addText((text) => text.setValue(this.date).onChange((value) => this.date = value.trim()));
    this.renderActions(async () => {
      if (!this.title || !this.goalId) return;
      await this.onSubmit({
        id: `milestone-${Date.now()}`,
        goalId: this.goalId,
        title: this.title,
        date: this.date,
        status: "\u672A\u5F00\u59CB"
      });
    });
  }
  renderActions(onSave) {
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u6DFB\u52A0", attr: { type: "button" } }).addEventListener("click", async () => {
      await onSave();
      this.close();
    });
  }
};
var RiskModal = class extends import_obsidian14.Modal {
  constructor(app, onSubmit) {
    super(app);
    this.onSubmit = onSubmit;
    this.title = "";
    this.level = "medium";
    this.solution = "";
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: "\u6DFB\u52A0\u98CE\u9669" });
    new import_obsidian14.Setting(this.contentEl).setName("\u98CE\u9669").addText((text) => text.onChange((value) => this.title = value.trim()));
    new import_obsidian14.Setting(this.contentEl).setName("\u7B49\u7EA7").addDropdown((dropdown) => {
      dropdown.addOption("low", "\u4F4E");
      dropdown.addOption("medium", "\u4E2D");
      dropdown.addOption("high", "\u9AD8");
      dropdown.setValue(this.level);
      dropdown.onChange((value) => this.level = value);
    });
    new import_obsidian14.Setting(this.contentEl).setName("\u65B9\u6848").addTextArea((text) => text.onChange((value) => this.solution = value.trim()));
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u6DFB\u52A0", attr: { type: "button" } }).addEventListener("click", async () => {
      if (!this.title) return;
      await this.onSubmit({ id: `risk-${Date.now()}`, title: this.title, level: this.level, solution: this.solution });
      this.close();
    });
  }
};

// src/components/DashboardEditModals.ts
var import_obsidian15 = require("obsidian");
var ACCOUNT_TYPES = ["\u73B0\u91D1", "\u50A8\u84C4\u5361", "\u4FE1\u7528\u5361", "\u6295\u8D44\u8D26\u6237", "\u652F\u4ED8\u5B9D", "\u5FAE\u4FE1\u94B1\u5305", "\u8BC1\u5238", "\u5176\u4ED6"];
var QUADRANTS = [
  { value: "important-urgent", label: "\u91CD\u8981\u4E14\u7D27\u6025" },
  { value: "important-not-urgent", label: "\u91CD\u8981\u4E0D\u7D27\u6025" },
  { value: "not-important-urgent", label: "\u4E0D\u91CD\u8981\u4F46\u7D27\u6025" },
  { value: "not-important-not-urgent", label: "\u4E0D\u91CD\u8981\u4E0D\u7D27\u6025" }
];
function openBodyMeasurementModal(app, onSubmit, item) {
  var _a, _b, _c, _d, _e, _f;
  new CrudItemModal(app, item ? "\u7F16\u8F91\u56F4\u5EA6\u8BB0\u5F55" : "\u8BB0\u5F55\u8EAB\u4F53\u6570\u636E", {
    date: (_a = item == null ? void 0 : item.date) != null ? _a : formatDateKey(/* @__PURE__ */ new Date()),
    weight: (_b = item == null ? void 0 : item.weight) != null ? _b : 0,
    bmi: (_c = item == null ? void 0 : item.bmi) != null ? _c : 0,
    waist: (_d = item == null ? void 0 : item.waist) != null ? _d : 0,
    chest: (_e = item == null ? void 0 : item.chest) != null ? _e : 0,
    hip: (_f = item == null ? void 0 : item.hip) != null ? _f : 0
  }, [
    { key: "date", name: "\u65E5\u671F" },
    { key: "weight", name: "\u4F53\u91CD", type: "number" },
    { key: "bmi", name: "BMI", type: "number" },
    { key: "waist", name: "\u8170\u56F4", type: "number" },
    { key: "chest", name: "\u80F8\u56F4", type: "number" },
    { key: "hip", name: "\u81C0\u56F4", type: "number" }
  ], async (values) => {
    var _a2;
    return onSubmit({ ...values, id: (_a2 = item == null ? void 0 : item.id) != null ? _a2 : `measure-${Date.now()}` });
  }).open();
}
function openFitnessDailyModal(app, record, onSubmit) {
  new CrudItemModal(app, "\u7F16\u8F91\u4E60\u60EF", { ...record }, [
    { key: "date", name: "\u65E5\u671F" },
    { key: "waterCups", name: "\u996E\u6C34\u676F\u6570", type: "number" },
    { key: "waterGoal", name: "\u996E\u6C34\u76EE\u6807", type: "number" },
    { key: "sleepHours", name: "\u7761\u7720\u65F6\u957F", type: "number" },
    { key: "sleepGoal", name: "\u7761\u7720\u76EE\u6807", type: "number" },
    { key: "bedtime", name: "\u5165\u7761\u65F6\u95F4" },
    { key: "wakeTime", name: "\u8D77\u5E8A\u65F6\u95F4" }
  ], onSubmit).open();
}
function openBudgetModal(app, onSubmit, budget) {
  var _a, _b, _c, _d;
  new CrudItemModal(app, budget ? "\u7F16\u8F91\u652F\u51FA\u5206\u7C7B" : "\u65B0\u589E\u652F\u51FA\u5206\u7C7B", {
    category: (_a = budget == null ? void 0 : budget.category) != null ? _a : "",
    amount: (_b = budget == null ? void 0 : budget.amount) != null ? _b : 0,
    spent: (_c = budget == null ? void 0 : budget.spent) != null ? _c : 0,
    color: (_d = budget == null ? void 0 : budget.color) != null ? _d : "#ff7fb4"
  }, [
    { key: "category", name: "\u5206\u7C7B\u540D\u79F0" },
    { key: "amount", name: "\u9884\u7B97\u91D1\u989D", type: "number" },
    { key: "color", name: "\u989C\u8272" }
  ], async (values) => {
    var _a2, _b2;
    return onSubmit({ ...values, id: (_a2 = budget == null ? void 0 : budget.id) != null ? _a2 : `budget-${Date.now()}`, spent: (_b2 = budget == null ? void 0 : budget.spent) != null ? _b2 : 0 });
  }).open();
}
function openAccountModal(app, onSubmit, account) {
  new AccountModal(app, onSubmit, account).open();
}
var AccountModal = class extends import_obsidian15.Modal {
  constructor(app, onSubmit, account) {
    super(app);
    this.onSubmit = onSubmit;
    this.account = account;
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f;
    applyResizableModal(this, {
      className: "cute-finance-edit-modal",
      width: "min(640px, 90vw)",
      maxWidth: "94vw",
      maxHeight: "88vh",
      minWidth: "min(360px, 90vw)",
      minHeight: "min(280px, 80vh)"
    });
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-finance-modal");
    this.contentEl.createEl("h2", { text: this.account ? "\u7F16\u8F91\u8D26\u6237" : "\u65B0\u589E\u8D26\u6237" });
    const form = this.contentEl.createDiv({ cls: "cow-finance-form-grid" });
    const nameRow = form.createDiv({ cls: "cow-finance-form-row" });
    nameRow.createEl("label", { text: "\u8D26\u6237\u540D\u79F0" });
    const name = nameRow.createEl("input", { attr: { type: "text", value: (_b = (_a = this.account) == null ? void 0 : _a.name) != null ? _b : "" } });
    const typeRow = form.createDiv({ cls: "cow-finance-form-row" });
    typeRow.createEl("label", { text: "\u8D26\u6237\u7C7B\u578B" });
    const type = typeRow.createEl("select");
    ACCOUNT_TYPES.forEach((item) => type.createEl("option", { value: item, text: item }));
    type.value = (_d = (_c = this.account) == null ? void 0 : _c.type) != null ? _d : "\u50A8\u84C4\u5361";
    const balanceRow = form.createDiv({ cls: "cow-finance-form-row" });
    balanceRow.createEl("label", { text: "\u4F59\u989D" });
    const balance = balanceRow.createEl("input", { attr: { type: "number", value: String((_f = (_e = this.account) == null ? void 0 : _e.balance) != null ? _f : 0), step: "0.01" } });
    const preview = this.contentEl.createDiv({ cls: "cow-account-icon-preview" });
    const renderPreview = () => {
      preview.empty();
      const icon = preview.createSpan({ cls: "cow-account-icon" });
      (0, import_obsidian15.setIcon)(icon, getAccountTypeIcon(type.value));
      preview.createSpan({ text: `${type.value} \xB7 ${name.value.trim() || "\u672A\u547D\u540D\u8D26\u6237"}` });
    };
    type.addEventListener("change", renderPreview);
    name.addEventListener("input", renderPreview);
    renderPreview();
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2;
      const accountType = type.value;
      await this.onSubmit({
        id: (_b2 = (_a2 = this.account) == null ? void 0 : _a2.id) != null ? _b2 : `account-${Date.now()}`,
        name: name.value.trim() || "\u672A\u547D\u540D\u8D26\u6237",
        type: accountType,
        balance: Number(balance.value) || 0,
        icon: getAccountTypeIcon(accountType)
      });
      this.close();
    });
  }
};
function openInvestmentWatchModal(app, onSubmit, item) {
  var _a, _b, _c, _d, _e;
  new CrudItemModal(app, item ? "\u7F16\u8F91\u6295\u8D44\u89C2\u5BDF" : "\u65B0\u589E\u6295\u8D44\u89C2\u5BDF", {
    name: (_a = item == null ? void 0 : item.name) != null ? _a : "",
    code: (_b = item == null ? void 0 : item.code) != null ? _b : "",
    price: (_c = item == null ? void 0 : item.price) != null ? _c : 0,
    changePercent: (_d = item == null ? void 0 : item.changePercent) != null ? _d : 0,
    type: (_e = item == null ? void 0 : item.type) != null ? _e : "\u6307\u6570"
  }, [
    { key: "name", name: "\u540D\u79F0" },
    { key: "code", name: "\u4EE3\u7801" },
    { key: "price", name: "\u5F53\u524D\u4EF7\u683C", type: "number" },
    { key: "changePercent", name: "\u53D8\u5316\u767E\u5206\u6BD4", type: "number" },
    { key: "type", name: "\u7C7B\u578B" }
  ], async (values) => {
    var _a2;
    return onSubmit({ ...values, id: (_a2 = item == null ? void 0 : item.id) != null ? _a2 : `watch-${Date.now()}` });
  }).open();
}
function openPriorityItemModal(app, onSubmit, item) {
  var _a, _b, _c, _d;
  new CrudItemModal(app, item ? "\u7F16\u8F91\u4F18\u5148\u7EA7\u4EFB\u52A1" : "\u65B0\u589E\u4F18\u5148\u7EA7\u4EFB\u52A1", {
    title: (_a = item == null ? void 0 : item.title) != null ? _a : "",
    quadrant: (_b = item == null ? void 0 : item.quadrant) != null ? _b : "important-urgent",
    note: (_c = item == null ? void 0 : item.note) != null ? _c : "",
    completed: (_d = item == null ? void 0 : item.completed) != null ? _d : false
  }, [
    { key: "title", name: "\u6807\u9898" },
    { key: "quadrant", name: "\u8C61\u9650", type: "select", options: QUADRANTS },
    { key: "note", name: "\u5907\u6CE8", type: "textarea" },
    { key: "completed", name: "\u5DF2\u5B8C\u6210", type: "checkbox" }
  ], async (values) => {
    var _a2;
    return onSubmit({ ...values, id: (_a2 = item == null ? void 0 : item.id) != null ? _a2 : `priority-${Date.now()}` });
  }).open();
}
function showUsedCategoryNotice(category) {
  new import_obsidian15.Notice(`\u201C${category}\u201D\u4ECD\u6709\u8BB0\u8D26\u8BB0\u5F55\uFF0C\u6682\u4E0D\u80FD\u5220\u9664\u3002`);
}

// src/components/overview/TodoStatisticsModal.ts
var import_obsidian16 = require("obsidian");
var CATEGORIES = ["\u79D1\u7814", "\u9605\u8BFB", "\u5065\u8EAB", "\u7406\u8D22", "\u4E2A\u4EBA"];
var TodoStatisticsModal = class extends import_obsidian16.Modal {
  constructor(app, store, date, onDataChanged) {
    super(app);
    this.store = store;
    this.onDataChanged = onDataChanged;
    this.calendar = new CalendarService();
    this.date = date;
  }
  onOpen() {
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-todo-stat-modal");
    const dateKey = this.calendar.getDateKey(this.date);
    const tasks = this.store.getTodayFocusTasksForDate(dateKey);
    const done = tasks.filter((task) => task.completed).length;
    const rate = tasks.length === 0 ? 0 : Math.round(done / tasks.length * 100);
    const header = this.contentEl.createDiv({ cls: "cow-stats-modal-header" });
    const prev = header.createEl("button", { attr: { type: "button", "aria-label": "\u4E0A\u4E00\u5929" } });
    (0, import_obsidian16.setIcon)(prev, "chevron-left");
    prev.addEventListener("click", () => {
      this.date.setDate(this.date.getDate() - 1);
      this.render();
    });
    header.createEl("h2", { text: dateKey });
    const next = header.createEl("button", { attr: { type: "button", "aria-label": "\u4E0B\u4E00\u5929" } });
    (0, import_obsidian16.setIcon)(next, "chevron-right");
    next.addEventListener("click", () => {
      this.date.setDate(this.date.getDate() + 1);
      this.render();
    });
    const summary = this.contentEl.createDiv({ cls: "cow-stats-card-grid" });
    [["\u603B\u4EFB\u52A1", tasks.length], ["\u5DF2\u5B8C\u6210", done], ["\u672A\u5B8C\u6210", tasks.length - done], ["\u5B8C\u6210\u7387", `${rate}%`]].forEach(([label, value]) => {
      const card = summary.createDiv({ cls: "cow-stats-card" });
      card.createEl("strong", { text: String(value) });
      card.createSpan({ text: String(label) });
    });
    const add = this.contentEl.createEl("button", { cls: "cow-small-action", attr: { type: "button" } });
    (0, import_obsidian16.setIcon)(add.createSpan(), "plus");
    add.createSpan({ text: "\u65B0\u589E\u5F85\u529E" });
    add.addEventListener("click", () => {
      new TodayFocusTaskModal(this.app, dateKey, async (values) => {
        await this.store.addTodayFocusTask(values.label, values.category, values.date);
        this.onDataChanged();
        this.render();
      }).open();
    });
    const list = this.contentEl.createDiv({ cls: "cow-data-list" });
    if (tasks.length === 0) {
      list.createEl("p", { cls: "cow-empty-state", text: "\u8FD9\u4E00\u5929\u8FD8\u6CA1\u6709 Todo\u3002" });
      return;
    }
    tasks.forEach((task) => {
      var _a;
      const row = list.createDiv({ cls: "cow-data-card cow-todo-stat-item" });
      const checkbox = row.createEl("input", { attr: { type: "checkbox" } });
      checkbox.checked = task.completed;
      checkbox.addEventListener("change", async () => {
        await this.store.toggleTodayFocusTask(task.id);
        this.onDataChanged();
        this.render();
      });
      const body = row.createDiv({ cls: "cow-list-item-head" });
      const text = body.createDiv();
      text.createEl("strong", { text: task.label });
      text.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${task.category} \xB7 ${(_a = task.date) != null ? _a : dateKey}` });
      const actions = body.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91" } });
      (0, import_obsidian16.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => {
        new TodayFocusTaskModal(this.app, dateKey, async (values) => {
          await this.store.updateTodayFocusTask(task.id, values);
          this.onDataChanged();
          this.render();
        }, task).open();
      });
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664" } });
      (0, import_obsidian16.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        await this.store.deleteTodayFocusTask(task.id);
        this.onDataChanged();
        this.render();
      });
    });
  }
};
var TodayFocusTaskModal = class extends import_obsidian16.Modal {
  constructor(app, date, onSubmit, task) {
    var _a, _b, _c;
    super(app);
    this.onSubmit = onSubmit;
    this.label = (_a = task == null ? void 0 : task.label) != null ? _a : "";
    this.category = (_b = task == null ? void 0 : task.category) != null ? _b : "\u4E2A\u4EBA";
    this.date = (_c = task == null ? void 0 : task.date) != null ? _c : date;
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: this.label ? "\u7F16\u8F91\u4ECA\u65E5\u7126\u70B9" : "\u65B0\u589E\u4ECA\u65E5\u7126\u70B9" });
    new import_obsidian16.Setting(this.contentEl).setName("\u6807\u9898").addText((text) => text.setValue(this.label).onChange((value) => {
      this.label = value;
    }));
    new import_obsidian16.Setting(this.contentEl).setName("\u5206\u7C7B").addDropdown((dropdown) => {
      CATEGORIES.forEach((category) => dropdown.addOption(category, category));
      dropdown.setValue(this.category);
      dropdown.onChange((value) => {
        this.category = value;
      });
    });
    new import_obsidian16.Setting(this.contentEl).setName("\u65E5\u671F").addText((text) => text.setValue(this.date).onChange((value) => {
      this.date = value.trim() || formatDateKey(/* @__PURE__ */ new Date());
    }));
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    const save = actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } });
    save.addEventListener("click", async () => {
      const label = this.label.trim();
      if (!label) return;
      await this.onSubmit({ label, category: this.category, date: this.date });
      this.close();
    });
  }
};

// src/components/reading/ReadingNotesSection.ts
var import_obsidian17 = require("obsidian");
var ReadingNotesSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const notes = this.store.getReadingNotes();
    const list = container.createDiv({ cls: "cow-data-list cow-reading-note-list" });
    if (notes.length === 0) {
      list.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u5DF2\u7ED1\u5B9A\u7684\u9605\u8BFB\u7B14\u8BB0\u3002\u53EF\u4EE5\u4ECE\u4E0B\u65B9\u9009\u62E9\u5DF2\u6709 Vault Markdown \u5173\u8054\u5230\u4E66\u7C4D\u3002" });
    } else {
      notes.forEach((note) => this.renderNote(list, note));
    }
    const add = container.createEl("button", { cls: "cow-bottom-add-button", attr: { type: "button" } });
    (0, import_obsidian17.setIcon)(add.createSpan(), "plus");
    add.createSpan({ text: "\u6DFB\u52A0\u9605\u8BFB\u7B14\u8BB0" });
    add.addEventListener("click", () => openReadingNoteModal(this.app, this.store, this.onDataChanged));
  }
  renderNote(container, note) {
    const book = this.store.getBooks().find((item) => item.id === note.bookId);
    const card = container.createDiv({ cls: "cow-data-card cow-reading-note-card" });
    const head = card.createDiv({ cls: "cow-list-item-head" });
    const title = head.createEl("button", { cls: "cow-link-button", text: note.title, attr: { type: "button", title: note.title } });
    title.addEventListener("click", () => void this.openNote(note));
    const actions = head.createDiv({ cls: "cow-list-item-actions" });
    this.iconButton(actions, "external-link", "\u6253\u5F00\u9605\u8BFB\u7B14\u8BB0", () => void this.openNote(note));
    this.iconButton(actions, "pencil", "\u7F16\u8F91\u5173\u8054", () => openReadingNoteModal(this.app, this.store, this.onDataChanged, note));
    this.iconButton(actions, "trash-2", "\u5220\u9664\u5173\u8054", () => new ReadingNoteDeleteModal(this.app, this.store, note, this.onDataChanged).open());
    card.createDiv({ cls: "cow-meta-line", text: book ? `\u5173\u8054\u4E66\u7C4D\uFF1A${book.title}` : "\u5173\u8054\u4E66\u7C4D\uFF1A\u672A\u627E\u5230\u4E66\u7C4D\u8BB0\u5F55" });
    card.createDiv({ cls: "cow-meta-line", text: note.notePath });
  }
  async openNote(note) {
    const file = this.app.vault.getFileByPath(note.notePath);
    if (!(file instanceof import_obsidian17.TFile)) {
      new import_obsidian17.Notice("\u9605\u8BFB\u7B14\u8BB0\u6587\u4EF6\u4E0D\u5B58\u5728\u3002");
      return;
    }
    await this.app.workspace.getLeaf(false).openFile(file);
  }
  iconButton(container, icon, label, onClick) {
    const button = container.createEl("button", { attr: { type: "button", "aria-label": label } });
    (0, import_obsidian17.setIcon)(button, icon);
    button.addEventListener("click", onClick);
  }
};
function openReadingNoteModal(app, store, onDataChanged, note) {
  new ReadingNoteModal(app, store, onDataChanged, note).open();
}
async function openReadingNoteForBook(app, store, book) {
  const notes = store.getReadingNotesForBook(book.id);
  if (notes.length === 0) {
    new import_obsidian17.Notice("\u8FD9\u672C\u4E66\u8FD8\u6CA1\u6709\u7ED1\u5B9A\u9605\u8BFB\u7B14\u8BB0\u3002");
    return;
  }
  if (notes.length === 1) {
    await openVaultMarkdown(app, notes[0].notePath);
    return;
  }
  new ReadingNoteChoiceModal(app, notes).open();
}
var ReadingNoteModal = class extends import_obsidian17.Modal {
  constructor(app, store, onDataChanged, note) {
    var _a, _b, _c, _d, _e;
    super(app);
    this.store = store;
    this.onDataChanged = onDataChanged;
    this.note = note;
    this.selectedBookId = (_c = (_b = note == null ? void 0 : note.bookId) != null ? _b : (_a = store.getBooks()[0]) == null ? void 0 : _a.id) != null ? _c : "";
    this.noteTitle = (_d = note == null ? void 0 : note.title) != null ? _d : "";
    this.notePath = (_e = note == null ? void 0 : note.notePath) != null ? _e : "";
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-reading-note-modal",
      width: "min(760px, 90vw)",
      height: "min(640px, 86vh)",
      maxWidth: "96vw",
      maxHeight: "92vh",
      minWidth: "min(500px, 92vw)",
      minHeight: "min(420px, 82vh)"
    });
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-reading-note-modal-content");
    this.contentEl.createEl("h2", { text: this.note ? "\u7F16\u8F91\u9605\u8BFB\u7B14\u8BB0\u5173\u8054" : "\u6DFB\u52A0\u9605\u8BFB\u7B14\u8BB0" });
    const form = this.contentEl.createDiv({ cls: "cow-book-form cow-reading-note-form" });
    this.renderBookSelect(form);
    this.renderTitleField(form);
    new MarkdownFilePicker(this.app, {
      label: "\u5173\u8054 Markdown",
      placeholder: "\u641C\u7D22 Vault \u4E2D\u7684 Markdown \u6587\u4EF6\u2026\u2026",
      value: this.notePath,
      onChange: (path) => {
        this.notePath = path;
        if (!this.noteTitle) this.noteTitle = fileName4(path);
      }
    }).render(form);
    this.renderActions();
  }
  renderBookSelect(container) {
    const row = container.createDiv({ cls: "cow-book-form-row" });
    row.createEl("label", { text: "\u5173\u8054\u4E66\u7C4D" });
    const select = row.createEl("select");
    this.store.getBooks().forEach((book) => select.createEl("option", { value: book.id, text: `${book.title} \xB7 ${book.author}` }));
    select.value = this.selectedBookId;
    select.addEventListener("change", () => this.selectedBookId = select.value);
  }
  renderTitleField(container) {
    const row = container.createDiv({ cls: "cow-book-form-row" });
    row.createEl("label", { text: "\u7B14\u8BB0\u6807\u9898" });
    const input = row.createEl("input", { value: this.noteTitle, attr: { type: "text", placeholder: "\u9ED8\u8BA4\u4F7F\u7528 Markdown \u6587\u4EF6\u540D" } });
    input.addEventListener("input", () => this.noteTitle = input.value);
  }
  renderActions() {
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", () => void this.save());
  }
  async save() {
    var _a, _b, _c, _d;
    if (!this.selectedBookId) {
      new import_obsidian17.Notice("\u8BF7\u5148\u9009\u62E9\u4E00\u672C\u4E66\u3002");
      return;
    }
    const file = this.app.vault.getFileByPath(this.notePath);
    if (!(file instanceof import_obsidian17.TFile)) {
      new import_obsidian17.Notice("\u8BF7\u9009\u62E9\u4E00\u4E2A\u5B58\u5728\u7684 Vault Markdown \u6587\u4EF6\u3002");
      return;
    }
    const now = (/* @__PURE__ */ new Date()).toISOString();
    const payload = {
      id: (_b = (_a = this.note) == null ? void 0 : _a.id) != null ? _b : `reading-note-${Date.now()}`,
      title: this.noteTitle.trim() || file.basename,
      notePath: file.path,
      bookId: this.selectedBookId,
      createdAt: (_d = (_c = this.note) == null ? void 0 : _c.createdAt) != null ? _d : now,
      updatedAt: now
    };
    if (this.note) await this.store.updateReadingNote(this.note.id, payload);
    else await this.store.addReadingNote(payload);
    this.onDataChanged();
    this.close();
  }
};
var ReadingNoteDeleteModal = class extends import_obsidian17.Modal {
  constructor(app, store, note, onDone) {
    super(app);
    this.store = store;
    this.note = note;
    this.onDone = onDone;
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-reading-note-delete-modal",
      width: "min(560px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "82vh",
      minWidth: "min(380px, 90vw)",
      minHeight: "min(240px, 70vh)"
    });
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-reading-note-delete-modal-content");
    this.contentEl.createEl("h2", { text: "\u5220\u9664\u9605\u8BFB\u7B14\u8BB0\u5173\u8054" });
    this.contentEl.createEl("p", { text: `\u5C06\u4ECE\u5DE5\u4F5C\u53F0\u79FB\u9664\u201C${this.note.title}\u201D\u7684\u5173\u8054\u8BB0\u5F55\uFF0C\u4E0D\u4F1A\u5220\u9664 Vault \u4E2D\u7684 Markdown \u6587\u4EF6\u3002` });
    this.contentEl.createDiv({ cls: "cow-meta-line", text: this.note.notePath });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u5220\u9664\u5173\u8054", cls: "mod-warning", attr: { type: "button" } }).addEventListener("click", () => void this.unlinkOnly());
  }
  async unlinkOnly() {
    await this.store.deleteReadingNote(this.note.id);
    this.onDone();
    this.close();
  }
};
var ReadingNoteChoiceModal = class extends import_obsidian17.Modal {
  constructor(app, notes) {
    super(app);
    this.notes = notes;
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-reading-note-choice-modal",
      width: "min(560px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "82vh",
      minWidth: "min(360px, 90vw)",
      minHeight: "min(260px, 70vh)"
    });
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-reading-note-choice-modal-content");
    this.contentEl.createEl("h2", { text: "\u9009\u62E9\u9605\u8BFB\u7B14\u8BB0" });
    const list = this.contentEl.createDiv({ cls: "cow-data-list" });
    this.notes.forEach((note) => {
      const button = list.createEl("button", { cls: "cow-data-card cow-click-card", attr: { type: "button" } });
      button.createEl("strong", { text: note.title });
      button.createDiv({ cls: "cow-meta-line", text: note.notePath });
      button.addEventListener("click", () => {
        void openVaultMarkdown(this.app, note.notePath);
        this.close();
      });
    });
  }
};
function fileName4(path) {
  var _a, _b;
  return (_b = (_a = path.split(/[\\/]/).pop()) == null ? void 0 : _a.replace(/\.md$/i, "")) != null ? _b : "";
}

// src/components/reading/ReadingPlanModals.ts
var import_obsidian18 = require("obsidian");

// src/components/goals/GoalStatisticsLayout.ts
function renderGoalStatisticsLayout(container, title, periodActions = [], renderExtraControls) {
  const shell = container.createDiv({ cls: "cow-goal-stats-layout" });
  const header = shell.createDiv({ cls: "cow-goal-stats-header" });
  header.createEl("h2", { text: title });
  const toolbar = header.createDiv({ cls: "cow-goal-stats-toolbar" });
  periodActions.forEach((action) => {
    const button = toolbar.createEl("button", {
      text: action.label,
      cls: action.active ? "is-active" : "",
      attr: { type: "button" }
    });
    if (action.onClick) button.addEventListener("click", action.onClick);
  });
  renderExtraControls == null ? void 0 : renderExtraControls(toolbar);
  return shell.createDiv({ cls: "cow-goal-stats-content" });
}

// src/components/reading/ReadingPlanModals.ts
function openReadingPlanModal(app, store, onDataChanged, plan) {
  new ReadingPlanModal(app, store, onDataChanged, plan).open();
}
var ReadingPlanModal = class extends import_obsidian18.Modal {
  constructor(app, store, onDataChanged, plan) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    super(app);
    this.store = store;
    this.onDataChanged = onDataChanged;
    this.plan = plan;
    this.query = "";
    const today6 = todayKey3();
    this.selectedBookId = (_c = (_b = plan == null ? void 0 : plan.bookId) != null ? _b : (_a = store.getBooks()[0]) == null ? void 0 : _a.id) != null ? _c : "";
    this.startDate = (_d = plan == null ? void 0 : plan.startDate) != null ? _d : today6;
    this.endDate = (_e = plan == null ? void 0 : plan.endDate) != null ? _e : today6;
    this.goal = (_f = plan == null ? void 0 : plan.goal) != null ? _f : (plan == null ? void 0 : plan.targetPages) ? `${plan.targetPages} \u9875` : "\u8BFB\u5B8C\u672C\u4E66";
    this.progress = Math.max(0, Math.min(100, Number((_g = plan == null ? void 0 : plan.progress) != null ? _g : (plan == null ? void 0 : plan.status) === "completed" ? 100 : 0) || 0));
    this.note = (_h = plan == null ? void 0 : plan.note) != null ? _h : "";
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-reading-plan-modal",
      width: "min(760px, 90vw)",
      height: "min(680px, 86vh)",
      maxWidth: "96vw",
      maxHeight: "92vh",
      minWidth: "min(520px, 92vw)",
      minHeight: "min(430px, 82vh)"
    });
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-reading-plan-modal");
    this.contentEl.createEl("h2", { text: this.plan ? "\u7F16\u8F91\u9605\u8BFB\u8BA1\u5212" : "\u65B0\u589E\u9605\u8BFB\u8BA1\u5212" });
    const body = this.contentEl.createDiv({ cls: "cow-reading-plan-modal-body" });
    this.renderBookPicker(body);
    this.renderPlanForm(body);
    this.renderActions();
  }
  renderBookPicker(container) {
    const section = container.createDiv({ cls: "cow-reading-plan-picker" });
    const head = section.createDiv({ cls: "cow-list-item-head" });
    head.createEl("strong", { text: "\u9009\u62E9\u4E66\u7C4D" });
    const addBook = head.createEl("button", { cls: "cow-section-add-button", attr: { type: "button" } });
    (0, import_obsidian18.setIcon)(addBook.createSpan(), "plus");
    addBook.createSpan({ text: "\u589E\u52A0\u4E66\u7C4D" });
    addBook.addEventListener("click", () => {
      new AddBookModal(this.app, this.store, () => {
        var _a, _b;
        this.onDataChanged();
        const books2 = this.store.getBooks();
        this.selectedBookId = (_b = (_a = books2[books2.length - 1]) == null ? void 0 : _a.id) != null ? _b : this.selectedBookId;
        this.render();
      }).open();
    });
    const search = section.createEl("input", {
      value: this.query,
      attr: { type: "search", placeholder: "\u641C\u7D22\u4E66\u540D\u6216\u4F5C\u8005" }
    });
    search.addEventListener("input", () => {
      this.query = search.value;
      this.render();
    });
    const list = section.createDiv({ cls: "cow-reading-plan-book-list" });
    const books = this.filterBooks();
    if (books.length === 0) {
      list.createDiv({ cls: "cow-empty-state", text: "\u8FD8\u6CA1\u6709\u53EF\u9009\u62E9\u7684\u4E66\u7C4D\uFF0C\u53EF\u4EE5\u5148\u589E\u52A0\u4E00\u672C\u3002" });
      return;
    }
    books.forEach((book) => {
      const row = list.createEl("button", {
        cls: `cow-reading-plan-book-option ${this.selectedBookId === book.id ? "is-selected" : ""}`,
        attr: { type: "button" }
      });
      const cover = row.createDiv({ cls: "cow-reading-plan-book-cover" });
      const src = this.getCoverSrc(book);
      if (src) renderCoverImage(cover, src, book.title);
      else cover.createSpan({ text: book.title.slice(0, 2) });
      const body = row.createDiv({ cls: "cow-reading-plan-book-meta" });
      body.createEl("strong", { text: book.title });
      body.createSpan({ text: `${book.author} \xB7 ${bookStatusLabel(book)}` });
      row.addEventListener("click", () => {
        this.selectedBookId = book.id;
        this.render();
      });
    });
  }
  renderPlanForm(container) {
    const form = container.createDiv({ cls: "cow-reading-plan-form" });
    this.renderDateField(form, "\u5F00\u59CB\u65E5\u671F", this.startDate, (value) => {
      this.startDate = value;
      if (this.endDate < this.startDate) this.endDate = this.startDate;
      this.render();
    });
    this.renderDateField(form, "\u7ED3\u675F\u65E5\u671F", this.endDate, (value) => {
      this.endDate = value;
      this.render();
    });
    const goalRow = form.createDiv({ cls: "cow-book-form-row" });
    goalRow.createEl("label", { text: "\u9605\u8BFB\u76EE\u6807" });
    const goal = goalRow.createEl("input", {
      value: this.goal,
      attr: { type: "text", placeholder: "\u4F8B\u5982\uFF1A\u8BFB\u5B8C\u6574\u672C\u4E66 / \u7B2C 1-4 \u7AE0 / 300 \u9875" }
    });
    goal.addEventListener("input", () => this.goal = goal.value);
    const progressRow = form.createDiv({ cls: "cow-book-form-row cow-progress-editor-row" });
    progressRow.createEl("label", { text: "\u8BA1\u5212\u8FDB\u5EA6" });
    const progressControls = progressRow.createDiv({ cls: "cow-progress-editor-inline" });
    const range = progressControls.createEl("input", { attr: { type: "range", min: "0", max: "100", step: "1" } });
    const number = progressControls.createEl("input", { value: String(this.progress), attr: { type: "number", min: "0", max: "100", step: "1", "aria-label": "\u8FDB\u5EA6\u767E\u5206\u6BD4" } });
    const percent = progressControls.createSpan({ text: `${this.progress}%` });
    range.value = String(this.progress);
    const updateProgress = (value) => {
      this.progress = Math.max(0, Math.min(100, Math.round(value)));
      range.value = String(this.progress);
      number.value = String(this.progress);
      percent.setText(`${this.progress}%`);
    };
    range.addEventListener("input", () => updateProgress(Number(range.value)));
    number.addEventListener("input", () => updateProgress(Number(number.value) || 0));
    const noteRow = form.createDiv({ cls: "cow-book-form-row" });
    noteRow.createEl("label", { text: "\u5907\u6CE8" });
    const note = noteRow.createEl("textarea", { text: this.note });
    note.addEventListener("input", () => {
      this.note = note.value;
    });
  }
  renderDateField(container, label, value, onChange) {
    const row = container.createDiv({ cls: "cow-book-form-row cow-clickable-date-row" });
    row.createEl("label", { text: label });
    const input = row.createEl("input", { value, attr: { type: "date" } });
    row.addEventListener("click", () => {
      var _a;
      const dateInput = input;
      (_a = dateInput.showPicker) == null ? void 0 : _a.call(dateInput);
      dateInput.focus();
    });
    input.addEventListener("change", () => onChange(input.value));
  }
  renderActions() {
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    const cancel = actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } });
    cancel.addEventListener("click", () => this.close());
    const save = actions.createEl("button", { text: "\u4FDD\u5B58\u8BA1\u5212", cls: "mod-cta", attr: { type: "button" } });
    save.addEventListener("click", async () => {
      var _a, _b, _c, _d, _e, _f, _g, _h, _i;
      if (!this.selectedBookId) {
        new import_obsidian18.Notice("\u8BF7\u5148\u9009\u62E9\u4E00\u672C\u4E66\u3002");
        return;
      }
      if (this.endDate < this.startDate) {
        new import_obsidian18.Notice("\u7ED3\u675F\u65E5\u671F\u4E0D\u80FD\u65E9\u4E8E\u5F00\u59CB\u65E5\u671F\u3002");
        return;
      }
      const now = (/* @__PURE__ */ new Date()).toISOString();
      const status = computeReadingPlanStatus(this.startDate, this.endDate, this.progress);
      const completedDate = status === "completed" ? (_b = (_a = this.plan) == null ? void 0 : _a.completedDate) != null ? _b : todayKey3() : void 0;
      const completedAt = status === "completed" ? (_d = (_c = this.plan) == null ? void 0 : _c.completedAt) != null ? _d : now : void 0;
      const payload = {
        id: (_f = (_e = this.plan) == null ? void 0 : _e.id) != null ? _f : `reading-plan-${Date.now()}`,
        bookId: this.selectedBookId,
        startDate: this.startDate,
        endDate: this.endDate,
        targetPages: (_g = this.plan) == null ? void 0 : _g.targetPages,
        goal: this.goal.trim() || "\u8BFB\u5B8C\u672C\u4E66",
        progress: this.progress,
        note: this.note.trim(),
        status,
        completedDate,
        completedAt,
        createdAt: (_i = (_h = this.plan) == null ? void 0 : _h.createdAt) != null ? _i : now,
        updatedAt: now
      };
      if (this.plan) await this.store.updateReadingPlan(this.plan.id, payload);
      else await this.store.addReadingPlan(payload);
      this.onDataChanged();
      this.close();
    });
  }
  filterBooks() {
    const query = this.query.trim().toLowerCase();
    return this.store.getBooks().filter((book) => {
      if (!query) return true;
      return `${book.title} ${book.author}`.toLowerCase().includes(query);
    });
  }
  getCoverSrc(book) {
    var _a;
    if (book.coverPath) return this.app.vault.adapter.getResourcePath(book.coverPath);
    return (_a = book.cover) != null ? _a : book.coverUrl;
  }
};
function openReadingPlanDetailModal(app, store, onDataChanged, plan) {
  new ReadingPlanDetailModal(app, store, onDataChanged, plan).open();
}
var ReadingPlanDetailModal = class extends import_obsidian18.Modal {
  constructor(app, store, onDataChanged, plan) {
    super(app);
    this.store = store;
    this.onDataChanged = onDataChanged;
    this.plan = plan;
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-reading-plan-detail-modal",
      width: "min(760px, 90vw)",
      height: "min(620px, 86vh)",
      maxWidth: "96vw",
      maxHeight: "92vh",
      minWidth: "min(500px, 92vw)",
      minHeight: "min(380px, 82vh)"
    });
    this.render();
  }
  render() {
    var _a, _b, _c, _d, _e, _f;
    const plan = (_a = this.store.getReadingPlans().find((item) => item.id === this.plan.id)) != null ? _a : this.plan;
    const book = this.store.getBooks().find((item) => item.id === plan.bookId);
    const status = plan.status;
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-reading-plan-detail-modal-content");
    this.contentEl.createEl("h2", { text: "\u9605\u8BFB\u8BA1\u5212\u8BE6\u60C5" });
    const card = this.contentEl.createDiv({ cls: `cow-reading-plan-detail-card is-${status}` });
    const head = card.createDiv({ cls: "cow-list-item-head" });
    const title = head.createEl("button", { cls: "cow-link-button", text: (_b = book == null ? void 0 : book.title) != null ? _b : "\u672A\u627E\u5230\u5173\u8054\u4E66\u7C4D", attr: { type: "button" } });
    title.addEventListener("click", () => {
      if (!book) return;
      new AddBookModal(this.app, this.store, () => {
        this.onDataChanged();
        this.render();
      }, book).open();
    });
    head.createSpan({ cls: `cow-reading-plan-status is-${status}`, text: status === "completed" ? `\u2713 ${readingPlanStatusLabel(status)}` : readingPlanStatusLabel(status) });
    card.createDiv({ cls: "cow-meta-line", text: book ? `${book.author} \xB7 ${book.currentPage}/${book.totalPages} \u9875` : "\u8FD9\u6761\u8BA1\u5212\u4FDD\u7559\u4E86\u6570\u636E\uFF0C\u4F46\u4E66\u7C4D\u8BB0\u5F55\u53EF\u80FD\u5DF2\u88AB\u5220\u9664\u3002" });
    card.createDiv({ cls: "cow-meta-line", text: formatDateRange(plan.startDate, plan.endDate) });
    if ((_c = plan.completedAt) != null ? _c : plan.completedDate) card.createDiv({ cls: "cow-meta-line", text: `\u5B8C\u6210\uFF1A${(_e = (_d = plan.completedAt) != null ? _d : plan.completedDate) == null ? void 0 : _e.slice(0, 10)}` });
    card.createEl("p", { text: (_f = plan.goal) != null ? _f : "\u8BFB\u5B8C\u672C\u4E66" });
    this.renderProgress(card, plan);
    this.renderRhythm(card, plan, book);
    if (plan.note) card.createEl("p", { text: plan.note });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u7F16\u8F91", attr: { type: "button" } }).addEventListener("click", () => openReadingPlanModal(this.app, this.store, () => {
      this.onDataChanged();
      this.close();
    }, plan));
    if (status !== "completed") {
      actions.createEl("button", { text: "\u5B8C\u6210", attr: { type: "button" } }).addEventListener("click", async () => {
        await this.store.completeReadingPlan(plan.id);
        this.onDataChanged();
        this.render();
      });
    }
    actions.createEl("button", { text: "\u5173\u95ED", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", () => this.close());
  }
  renderProgress(container, plan) {
    const progress = Math.max(0, Math.min(100, Number(plan.progress) || 0));
    const row = container.createDiv({ cls: "cow-reading-plan-progress" });
    row.createSpan({ text: `\u8BA1\u5212\u8FDB\u5EA6 ${progress}%` });
    const track = row.createDiv({ cls: "cow-month-progress-track" });
    track.createDiv({ cls: "cow-month-progress-fill is-pink", attr: { style: `width: ${progress}%` } });
  }
  renderRhythm(container, plan, book) {
    const today6 = todayKey3();
    const daysLeft = Math.max(0, daysBetween(today6, plan.endDate) + 1);
    const rhythm = container.createDiv({ cls: "cow-reading-rhythm" });
    if (book == null ? void 0 : book.totalPages) {
      const remainingPages = Math.max(0, book.totalPages - book.currentPage);
      const pagesPerDay = daysLeft <= 0 ? remainingPages : Math.ceil(remainingPages / daysLeft);
      rhythm.createSpan({ text: `\u5269\u4F59 ${remainingPages} \u9875` });
      rhythm.createSpan({ text: `${daysLeft} \u5929` });
      rhythm.createSpan({ text: `\u5EFA\u8BAE ${pagesPerDay} \u9875/\u5929` });
      return;
    }
    rhythm.createSpan({ text: `\u5269\u4F59 ${daysLeft} \u5929` });
  }
};
var ReadingPlanStatisticsModal = class extends import_obsidian18.Modal {
  constructor(app, store) {
    super(app);
    this.store = store;
    this.year = (/* @__PURE__ */ new Date()).getFullYear();
    this.month = (/* @__PURE__ */ new Date()).getMonth() + 1;
    this.selectedDate = todayKey3();
    this.statusFilter = "all";
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-goal-stats-modal cute-reading-plan-stats-modal",
      width: "min(1050px, 92vw)",
      height: "min(720px, 86vh)",
      maxWidth: "96vw",
      maxHeight: "92vh",
      minWidth: "min(560px, 92vw)",
      minHeight: "min(460px, 82vh)"
    });
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-goal-stats-modal", "cow-reading-plan-stats");
    const content = renderGoalStatisticsLayout(
      this.contentEl,
      "\u9605\u8BFB\u8BA1\u5212\u7EDF\u8BA1",
      [
        { label: "<", onClick: () => {
          this.year -= 1;
          this.render();
        } },
        { label: String(this.year), active: true },
        { label: ">", onClick: () => {
          this.year += 1;
          this.render();
        } }
      ],
      (toolbar) => {
        const months = toolbar.createDiv({ cls: "cow-goal-period-tabs is-months" });
        const all = months.createEl("button", { text: "\u5168\u90E8\u6708\u4EFD", cls: this.month === "all" ? "is-active" : "", attr: { type: "button" } });
        all.addEventListener("click", () => {
          this.month = "all";
          this.render();
        });
        for (let month = 1; month <= 12; month += 1) {
          const button = months.createEl("button", { text: `${month}\u6708`, cls: this.month === month ? "is-active" : "", attr: { type: "button" } });
          button.addEventListener("click", () => {
            this.month = month;
            this.render();
          });
        }
      }
    );
    this.renderStats(content);
    const layout = content.createDiv({ cls: "cow-reading-plan-stats-layout" });
    this.renderCalendar(layout);
    this.renderPlanDetails(layout);
  }
  renderStats(container) {
    const plans = this.getPeriodPlans();
    const completed = plans.filter((plan) => this.resolveStatus(plan) === "completed").length;
    const active = plans.filter((plan) => this.resolveStatus(plan) === "active").length;
    const overdue = plans.filter((plan) => this.resolveStatus(plan) === "overdue").length;
    const rate = plans.length === 0 ? 0 : Math.round(completed / plans.length * 100);
    const stats = [
      ["\u8BA1\u5212\u603B\u6570", plans.length],
      ["\u5B8C\u6210", completed],
      ["\u8FDB\u884C\u4E2D", active],
      ["\u903E\u671F", overdue],
      ["\u5B8C\u6210\u7387", `${rate}%`]
    ];
    const grid = container.createDiv({ cls: "cow-stats-card-grid" });
    stats.forEach(([label, value]) => {
      const card = grid.createDiv({ cls: "cow-stats-card" });
      card.createEl("strong", { text: String(value) });
      card.createSpan({ text: String(label) });
    });
  }
  renderCalendar(container) {
    const shell = container.createDiv({ cls: "cow-reading-plan-calendar-panel" });
    shell.createEl("strong", { text: this.month === "all" ? `${this.year} \u5168\u5E74\u8BA1\u5212\u5206\u5E03` : `${this.year}\u5E74${this.month}\u6708\u8BA1\u5212\u65E5\u5386` });
    if (this.month === "all") {
      const months = shell.createDiv({ cls: "cow-reading-plan-year-months" });
      for (let month = 1; month <= 12; month += 1) {
        const count = this.store.getReadingPlans().filter((plan) => planOverlapsMonth(plan, this.year, month)).length;
        const button = months.createEl("button", { cls: count > 0 ? "has-plan" : "", attr: { type: "button" } });
        button.createEl("strong", { text: `${month}\u6708` });
        button.createSpan({ text: count > 0 ? `${count} \u4E2A\u8BA1\u5212` : "\u65E0\u8BA1\u5212" });
        button.addEventListener("click", () => {
          this.month = month;
          this.selectedDate = `${this.year}-${String(month).padStart(2, "0")}-01`;
          this.render();
        });
      }
      return;
    }
    const grid = shell.createDiv({ cls: "cow-review-calendar cow-reading-plan-calendar" });
    ["\u4E00", "\u4E8C", "\u4E09", "\u56DB", "\u4E94", "\u516D", "\u65E5"].forEach((day) => grid.createDiv({ cls: "cow-review-calendar-week", text: day }));
    const first = new Date(this.year, this.month - 1, 1);
    const firstDay = first.getDay() === 0 ? 6 : first.getDay() - 1;
    for (let index = 0; index < firstDay; index += 1) grid.createDiv({ cls: "cow-review-calendar-empty" });
    const days = new Date(this.year, this.month, 0).getDate();
    for (let day = 1; day <= days; day += 1) {
      const key = `${this.year}-${String(this.month).padStart(2, "0")}-${String(day).padStart(2, "0")}`;
      const hasPlan = this.store.getReadingPlans().some((plan) => dateInRange(key, plan.startDate, plan.endDate));
      const button = grid.createEl("button", {
        cls: `cow-review-calendar-day ${hasPlan ? "has-review" : ""} ${this.selectedDate === key ? "is-selected" : ""}`,
        attr: { type: "button" }
      });
      button.createSpan({ text: String(day) });
      if (hasPlan) button.createSpan({ cls: "cow-calendar-dot" });
      button.addEventListener("click", () => {
        this.selectedDate = key;
        this.render();
      });
    }
  }
  renderPlanDetails(container) {
    const shell = container.createDiv({ cls: "cow-goal-stats-list cow-reading-plan-detail-list" });
    const title = this.month === "all" ? `${this.year} \u5E74\u9605\u8BFB\u8BA1\u5212` : `${formatMonthDay(this.selectedDate)} \u6D89\u53CA\u7684\u8BA1\u5212`;
    shell.createEl("strong", { text: title });
    const filters = shell.createDiv({ cls: "cow-goal-period-tabs" });
    [
      ["all", "\u5168\u90E8"],
      ["planned", "\u8BA1\u5212\u4E2D"],
      ["active", "\u8FDB\u884C\u4E2D"],
      ["completed", "\u5DF2\u5B8C\u6210"],
      ["overdue", "\u5DF2\u903E\u671F"]
    ].forEach(([value, label]) => {
      const button = filters.createEl("button", { text: label, cls: this.statusFilter === value ? "is-active" : "", attr: { type: "button" } });
      button.addEventListener("click", () => {
        this.statusFilter = value;
        this.render();
      });
    });
    const plans = this.getVisibleDetailPlans();
    if (plans.length === 0) {
      shell.createDiv({ cls: "cow-empty-state", text: "\u8FD9\u4E2A\u65F6\u95F4\u8303\u56F4\u6682\u65E0\u9605\u8BFB\u8BA1\u5212\u3002" });
      return;
    }
    plans.forEach((plan) => {
      var _a, _b, _c, _d;
      const book = this.findBook(plan.bookId);
      const card = shell.createDiv({ cls: `cow-data-card ${this.resolveStatus(plan) === "completed" ? "is-complete" : ""}` });
      card.createEl("strong", { text: (_a = book == null ? void 0 : book.title) != null ? _a : "\u672A\u77E5\u4E66\u7C4D" });
      card.createDiv({ cls: "cow-meta-line", text: `${formatDateRange(plan.startDate, plan.endDate)} \xB7 ${readingPlanStatusLabel(this.resolveStatus(plan))}` });
      card.createDiv({ cls: "cow-meta-line", text: `${(_b = book == null ? void 0 : book.author) != null ? _b : "\u672A\u77E5\u4F5C\u8005"} \xB7 ${(_c = plan.goal) != null ? _c : "\u8BFB\u5B8C\u672C\u4E66"} \xB7 ${(_d = plan.progress) != null ? _d : 0}%` });
      if (plan.note) card.createEl("p", { text: plan.note });
    });
  }
  getPeriodPlans() {
    return this.store.getReadingPlans().filter((plan) => {
      if (this.month === "all") return plan.startDate.slice(0, 4) <= String(this.year) && plan.endDate.slice(0, 4) >= String(this.year);
      return planOverlapsMonth(plan, this.year, this.month);
    });
  }
  getVisibleDetailPlans() {
    const plans = this.month === "all" ? this.getPeriodPlans() : this.store.getReadingPlans().filter((plan) => dateInRange(this.selectedDate, plan.startDate, plan.endDate));
    return plans.filter((plan) => this.statusFilter === "all" || this.resolveStatus(plan) === this.statusFilter);
  }
  resolveStatus(plan) {
    var _a, _b;
    return (_b = (_a = this.store.getReadingPlans().find((item) => item.id === plan.id)) == null ? void 0 : _a.status) != null ? _b : plan.status;
  }
  findBook(bookId) {
    return this.store.getBooks().find((book) => book.id === bookId);
  }
};
function todayKey3() {
  const date = /* @__PURE__ */ new Date();
  return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, "0")}-${String(date.getDate()).padStart(2, "0")}`;
}
function dateInRange(date, startDate, endDate) {
  return date >= startDate && date <= endDate;
}
function planOverlapsMonth(plan, year, month) {
  const start = `${year}-${String(month).padStart(2, "0")}-01`;
  const end = `${year}-${String(month).padStart(2, "0")}-${String(new Date(year, month, 0).getDate()).padStart(2, "0")}`;
  return plan.startDate <= end && plan.endDate >= start;
}
function formatMonthDay(date) {
  const [, month, day] = date.split("-");
  return `${Number(month)}\u6708${Number(day)}\u65E5`;
}
function formatDateRange(startDate, endDate) {
  return `${formatMonthDay(startDate)} - ${formatMonthDay(endDate)}`;
}
function readingPlanStatusLabel(status) {
  if (status === "active") return "\u8FDB\u884C\u4E2D";
  if (status === "completed") return "\u5DF2\u5B8C\u6210";
  if (status === "overdue") return "\u5DF2\u903E\u671F";
  return "\u672A\u5F00\u59CB";
}
function computeReadingPlanStatus(startDate, endDate, progress) {
  if (progress >= 100) return "completed";
  const today6 = todayKey3();
  if (today6 < startDate) return "planned";
  if (today6 > endDate) return "overdue";
  return "active";
}
function daysBetween(startDate, endDate) {
  const start = (/* @__PURE__ */ new Date(`${startDate}T00:00:00`)).getTime();
  const end = (/* @__PURE__ */ new Date(`${endDate}T00:00:00`)).getTime();
  return Math.floor((end - start) / 864e5);
}
function bookStatusLabel(book) {
  if (book.readingStatus === "reading") return "\u5728\u8BFB";
  if (book.readingStatus === "finished") return "\u5DF2\u8BFB";
  return "\u60F3\u8BFB";
}
function renderCoverImage(container, src, title) {
  const img = container.createEl("img", { attr: { src, alt: title } });
  img.addEventListener("error", () => {
    container.empty();
    container.createSpan({ text: title.slice(0, 2) });
  });
}

// src/components/reading/ReadingQuotesSection.ts
var import_obsidian19 = require("obsidian");
var QUOTE_COLORS = ["#fff7fb", "#fff1ad", "#ffe0ed", "#e4ffed", "#e5f4ff", "#f1e9ff"];
var QUOTE_STYLES = [
  { value: "default", label: "\u9ED8\u8BA4" },
  { value: "sticky", label: "\u4FBF\u7B7E" },
  { value: "soft", label: "\u67D4\u548C" },
  { value: "card", label: "\u5361\u7247" }
];
var ReadingQuotesSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createDiv({ cls: "cow-reading-quote-grid" });
    const quotes = this.store.getReadingQuotes();
    if (quotes.length === 0) {
      list.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u91D1\u53E5\u6458\u5F55\u3002" });
    } else {
      quotes.forEach((quote) => this.renderQuote(list, quote));
    }
    const add = container.createEl("button", { cls: "cow-bottom-add-button", attr: { type: "button" } });
    (0, import_obsidian19.setIcon)(add.createSpan(), "plus");
    add.createSpan({ text: "\u6DFB\u52A0\u91D1\u53E5" });
    add.addEventListener("click", () => openReadingQuoteModal(this.app, this.store, this.onDataChanged));
  }
  renderQuote(container, quote) {
    var _a, _b;
    const book = quote.bookId ? this.store.getBooks().find((item) => item.id === quote.bookId) : void 0;
    const card = container.createDiv({ cls: `cow-reading-quote-card is-${(_a = quote.style) != null ? _a : "default"}` });
    card.style.backgroundColor = (_b = quote.backgroundColor) != null ? _b : "#fff7fb";
    const head = card.createDiv({ cls: "cow-list-item-head" });
    head.createEl("blockquote", { text: quote.text });
    const actions = head.createDiv({ cls: "cow-list-item-actions" });
    this.iconButton(actions, "pencil", "\u7F16\u8F91\u91D1\u53E5", () => openReadingQuoteModal(this.app, this.store, this.onDataChanged, quote));
    this.iconButton(actions, "trash-2", "\u5220\u9664\u91D1\u53E5", async () => {
      if (!confirm("\u5220\u9664\u8FD9\u6761\u91D1\u53E5\u6458\u5F55\uFF1F")) return;
      await this.store.deleteReadingQuote(quote.id);
      this.onDataChanged();
    });
    card.createDiv({ cls: "cow-quote-source", text: book ? `\u2014\u2014\u300A${book.title}\u300B` : quote.source ? `\u2014\u2014${quote.source}` : "\u2014\u2014\u672A\u5173\u8054\u4E66\u7C4D" });
    if (quote.note) card.createEl("p", { text: quote.note });
  }
  iconButton(container, icon, label, onClick) {
    const button = container.createEl("button", { attr: { type: "button", "aria-label": label } });
    (0, import_obsidian19.setIcon)(button, icon);
    button.addEventListener("click", onClick);
  }
};
function openReadingQuoteModal(app, store, onDataChanged, quote) {
  new ReadingQuoteModal(app, store, onDataChanged, quote).open();
}
var ReadingQuoteModal = class extends import_obsidian19.Modal {
  constructor(app, store, onDataChanged, quote) {
    var _a, _b, _c, _d, _e;
    super(app);
    this.store = store;
    this.onDataChanged = onDataChanged;
    this.quote = quote;
    this.text = (_a = quote == null ? void 0 : quote.text) != null ? _a : "";
    this.bookId = (_b = quote == null ? void 0 : quote.bookId) != null ? _b : "";
    this.note = (_c = quote == null ? void 0 : quote.note) != null ? _c : "";
    this.style = (_d = quote == null ? void 0 : quote.style) != null ? _d : "default";
    this.backgroundColor = (_e = quote == null ? void 0 : quote.backgroundColor) != null ? _e : "#fff7fb";
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-reading-quote-modal",
      width: "min(680px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "88vh",
      minWidth: "min(440px, 92vw)",
      minHeight: "min(390px, 80vh)"
    });
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-reading-quote-modal-content");
    this.contentEl.createEl("h2", { text: this.quote ? "\u7F16\u8F91\u91D1\u53E5" : "\u6DFB\u52A0\u91D1\u53E5" });
    const form = this.contentEl.createDiv({ cls: "cow-book-form" });
    this.renderTextarea(form, "\u91D1\u53E5\u5185\u5BB9", this.text, (value) => this.text = value);
    this.renderBookSelect(form);
    this.renderTextarea(form, "\u5907\u6CE8", this.note, (value) => this.note = value);
    this.renderStylePicker(form);
    this.renderColorPicker(form);
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", () => void this.save());
  }
  renderBookSelect(container) {
    const row = container.createDiv({ cls: "cow-book-form-row" });
    row.createEl("label", { text: "\u6765\u6E90\u4E66\u7C4D" });
    const select = row.createEl("select");
    select.createEl("option", { value: "", text: "\u4E0D\u5173\u8054\u4E66\u7C4D" });
    this.store.getBooks().forEach((book) => select.createEl("option", { value: book.id, text: book.title }));
    select.value = this.bookId;
    select.addEventListener("change", () => this.bookId = select.value);
  }
  renderStylePicker(container) {
    const row = container.createDiv({ cls: "cow-book-form-row" });
    row.createEl("label", { text: "\u80CC\u666F\u98CE\u683C" });
    const chips = row.createDiv({ cls: "cow-quote-style-list" });
    QUOTE_STYLES.forEach((style) => {
      const chip = chips.createEl("button", { text: style.label, cls: this.style === style.value ? "is-selected" : "", attr: { type: "button" } });
      chip.addEventListener("click", () => {
        this.style = style.value;
        this.render();
      });
    });
  }
  renderColorPicker(container) {
    const row = container.createDiv({ cls: "cow-book-form-row" });
    row.createEl("label", { text: "\u80CC\u666F\u989C\u8272" });
    const chips = row.createDiv({ cls: "cow-quote-color-list" });
    QUOTE_COLORS.forEach((color) => {
      const chip = chips.createEl("button", { cls: this.backgroundColor === color ? "is-selected" : "", attr: { type: "button", "aria-label": color } });
      chip.style.backgroundColor = color;
      chip.addEventListener("click", () => {
        this.backgroundColor = color;
        this.render();
      });
    });
  }
  renderTextarea(container, label, value, onInput) {
    const row = container.createDiv({ cls: "cow-book-form-row" });
    row.createEl("label", { text: label });
    const input = row.createEl("textarea", { text: value });
    input.addEventListener("input", () => onInput(input.value));
  }
  async save() {
    var _a, _b, _c;
    if (!this.text.trim()) return;
    const book = this.bookId ? this.store.getBooks().find((item) => item.id === this.bookId) : void 0;
    const payload = {
      id: (_b = (_a = this.quote) == null ? void 0 : _a.id) != null ? _b : `quote-${Date.now()}`,
      text: this.text.trim(),
      source: (_c = book == null ? void 0 : book.title) != null ? _c : "",
      bookId: this.bookId || void 0,
      note: this.note.trim(),
      style: this.style,
      backgroundColor: this.backgroundColor
    };
    if (this.quote) await this.store.updateReadingQuote(this.quote.id, payload);
    else await this.store.addReadingQuote(payload);
    this.onDataChanged();
    this.close();
  }
};

// src/components/reading/ReadingTagManagerSection.ts
var import_obsidian20 = require("obsidian");
var DEFAULT_COLORS = ["#f8a8c4", "#ffd166", "#7bd88f", "#76c7f2", "#c9b6ff", "#fff1ad"];
var ReadingTagManagerSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createDiv({ cls: "cow-data-list cow-book-tag-manager-list" });
    const tags = this.store.getBookTags().sort((a, b) => {
      var _a, _b;
      return ((_a = a.order) != null ? _a : 0) - ((_b = b.order) != null ? _b : 0);
    });
    if (tags.length === 0) {
      list.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u9605\u8BFB\u6807\u7B7E\u3002" });
    } else {
      tags.forEach((tag) => this.renderTag(list, tag));
    }
    const add = container.createEl("button", { cls: "cow-bottom-add-button", attr: { type: "button" } });
    (0, import_obsidian20.setIcon)(add.createSpan(), "plus");
    add.createSpan({ text: "\u65B0\u589E\u6807\u7B7E" });
    add.addEventListener("click", () => openBookTagModal(this.app, this.store, this.onDataChanged));
  }
  renderTag(container, tag) {
    const card = container.createDiv({ cls: "cow-data-card cow-book-tag-manager-card" });
    card.style.setProperty("--book-tag-color", tag.color);
    const head = card.createDiv({ cls: "cow-list-item-head" });
    const label = head.createDiv({ cls: "cow-book-tag-preview" });
    label.createSpan({ text: tag.name });
    const actions = head.createDiv({ cls: "cow-list-item-actions" });
    this.iconButton(actions, "pencil", "\u7F16\u8F91\u6807\u7B7E", () => openBookTagModal(this.app, this.store, this.onDataChanged, tag));
    this.iconButton(actions, "trash-2", "\u5220\u9664\u6807\u7B7E", async () => {
      if (!confirm(`\u5220\u9664\u201C${tag.name}\u201D\u6807\u7B7E\uFF1F\u4F1A\u4ECE\u6240\u6709\u4E66\u7C4D\u4E2D\u79FB\u9664\u8BE5\u6807\u7B7E\uFF0C\u4F46\u4E0D\u4F1A\u5220\u9664\u4E66\u7C4D\u3002`)) return;
      await this.store.deleteBookTag(tag.id);
      this.onDataChanged();
    });
    const count = this.store.getBooks().filter((book) => {
      var _a;
      return ((_a = book.tagIds) != null ? _a : []).includes(tag.id);
    }).length;
    card.createDiv({ cls: "cow-meta-line", text: `${count} \u672C\u4E66\u4F7F\u7528` });
  }
  iconButton(container, icon, label, onClick) {
    const button = container.createEl("button", { attr: { type: "button", "aria-label": label } });
    (0, import_obsidian20.setIcon)(button, icon);
    button.addEventListener("click", onClick);
  }
};
function openBookTagModal(app, store, onDataChanged, tag) {
  new BookTagModal(app, store, onDataChanged, tag).open();
}
var BookTagModal = class extends import_obsidian20.Modal {
  constructor(app, store, onDataChanged, tag) {
    var _a, _b;
    super(app);
    this.store = store;
    this.onDataChanged = onDataChanged;
    this.tag = tag;
    this.name = (_a = tag == null ? void 0 : tag.name) != null ? _a : "";
    this.color = (_b = tag == null ? void 0 : tag.color) != null ? _b : DEFAULT_COLORS[0];
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-book-tag-modal",
      width: "min(560px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "84vh",
      minWidth: "min(380px, 92vw)",
      minHeight: "min(300px, 76vh)"
    });
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-book-tag-modal-content");
    this.contentEl.createEl("h2", { text: this.tag ? "\u7F16\u8F91\u9605\u8BFB\u6807\u7B7E" : "\u65B0\u589E\u9605\u8BFB\u6807\u7B7E" });
    const form = this.contentEl.createDiv({ cls: "cow-book-form" });
    const nameRow = form.createDiv({ cls: "cow-book-form-row" });
    nameRow.createEl("label", { text: "\u6807\u7B7E\u540D\u79F0" });
    const input = nameRow.createEl("input", { value: this.name, attr: { type: "text" } });
    input.addEventListener("input", () => this.name = input.value);
    const colorRow = form.createDiv({ cls: "cow-book-form-row" });
    colorRow.createEl("label", { text: "\u989C\u8272" });
    const colors = colorRow.createDiv({ cls: "cow-book-tag-color-grid" });
    DEFAULT_COLORS.forEach((color) => {
      const chip = colors.createEl("button", { cls: this.color === color ? "is-selected" : "", attr: { type: "button", "aria-label": color } });
      chip.style.backgroundColor = color;
      chip.addEventListener("click", () => {
        this.color = color;
        this.render();
      });
    });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", () => void this.save());
  }
  async save() {
    var _a, _b, _c, _d;
    if (!this.name.trim()) return;
    const payload = {
      id: (_b = (_a = this.tag) == null ? void 0 : _a.id) != null ? _b : `book-tag-${Date.now()}`,
      name: this.name.trim(),
      color: this.color,
      order: (_d = (_c = this.tag) == null ? void 0 : _c.order) != null ? _d : this.store.getBookTags().length * 10 + 10
    };
    if (this.tag) await this.store.updateBookTag(this.tag.id, payload);
    else await this.store.addBookTag(payload);
    this.onDataChanged();
    this.close();
  }
};

// src/components/reading/WantToReadSection.ts
var import_obsidian21 = require("obsidian");
var WantToReadSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const pending = this.store.getWantToReadItems().filter((item) => item.status === "pending");
    const added = this.store.getWantToReadItems().filter((item) => item.status === "added");
    const list = container.createDiv({ cls: "cow-data-list cow-want-read-list" });
    if (pending.length === 0) {
      list.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u5F85\u5904\u7406\u60F3\u8BFB\u8BB0\u5F55\u3002\u8FD9\u91CC\u9002\u5408\u5FEB\u901F\u8BB0\u4E0B\u4E66\u540D\u3001\u4F5C\u8005\u548C\u6982\u8FF0\u3002" });
    } else {
      pending.forEach((item) => this.renderItem(list, item));
    }
    if (added.length > 0) {
      const history = container.createDiv({ cls: "cow-want-read-history" });
      history.createEl("strong", { text: "\u5DF2\u52A0\u5165\u4E66\u67B6" });
      added.slice(0, 6).forEach((item) => {
        const row = history.createDiv({ cls: "cow-meta-line" });
        row.createSpan({ text: `\u2713 ${item.title}` });
      });
    }
    const add = container.createEl("button", { cls: "cow-bottom-add-button", attr: { type: "button" } });
    (0, import_obsidian21.setIcon)(add.createSpan(), "plus");
    add.createSpan({ text: "\u6DFB\u52A0\u60F3\u8BFB" });
    add.addEventListener("click", () => openWantToReadModal(this.app, this.store, this.onDataChanged));
  }
  renderItem(container, item) {
    const card = container.createDiv({ cls: "cow-data-card cow-want-read-card" });
    const head = card.createDiv({ cls: "cow-list-item-head" });
    head.createEl("strong", { text: item.title });
    const actions = head.createDiv({ cls: "cow-list-item-actions" });
    this.textButton(actions, "\u52A0\u5165\u4E66\u67B6", "library", () => this.addToShelf(item));
    this.iconButton(actions, "pencil", "\u7F16\u8F91\u60F3\u8BFB", () => openWantToReadModal(this.app, this.store, this.onDataChanged, item));
    this.iconButton(actions, "trash-2", "\u5220\u9664\u60F3\u8BFB", async () => {
      if (!confirm(`\u5220\u9664\u201C${item.title}\u201D\u60F3\u8BFB\u8BB0\u5F55\uFF1F\u4E0D\u4F1A\u5220\u9664\u4EFB\u4F55\u4E66\u7C4D\u6587\u4EF6\u3002`)) return;
      await this.store.deleteWantToReadItem(item.id);
      this.onDataChanged();
    });
    card.createDiv({ cls: "cow-meta-line", text: item.author || "\u672A\u77E5\u4F5C\u8005" });
    if (item.summary) card.createEl("p", { text: item.summary });
  }
  addToShelf(item) {
    var _a, _b;
    new AddBookModal(this.app, this.store, this.onDataChanged, void 0, {
      initialDraft: {
        title: item.title,
        author: (_a = item.author) != null ? _a : "",
        description: (_b = item.summary) != null ? _b : "",
        readingStatus: "want-to-read"
      },
      onBookSaved: async (book) => {
        await this.store.updateWantToReadItem(item.id, { status: "added", bookId: book.id });
      }
    }).open();
  }
  iconButton(container, icon, label, onClick) {
    const button = container.createEl("button", { attr: { type: "button", "aria-label": label } });
    (0, import_obsidian21.setIcon)(button, icon);
    button.addEventListener("click", onClick);
  }
  textButton(container, label, icon, onClick) {
    const button = container.createEl("button", { cls: "cow-section-add-button", attr: { type: "button" } });
    (0, import_obsidian21.setIcon)(button.createSpan(), icon);
    button.createSpan({ text: label });
    button.addEventListener("click", onClick);
  }
};
function openWantToReadModal(app, store, onDataChanged, item) {
  new WantToReadModal(app, store, onDataChanged, item).open();
}
var WantToReadModal = class extends import_obsidian21.Modal {
  constructor(app, store, onDataChanged, item) {
    var _a, _b, _c;
    super(app);
    this.store = store;
    this.onDataChanged = onDataChanged;
    this.item = item;
    this.title = (_a = item == null ? void 0 : item.title) != null ? _a : "";
    this.author = (_b = item == null ? void 0 : item.author) != null ? _b : "";
    this.summary = (_c = item == null ? void 0 : item.summary) != null ? _c : "";
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-want-read-modal",
      width: "min(620px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "86vh",
      minWidth: "min(420px, 92vw)",
      minHeight: "min(340px, 78vh)"
    });
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-want-read-modal-content");
    this.contentEl.createEl("h2", { text: this.item ? "\u7F16\u8F91\u60F3\u8BFB" : "\u6DFB\u52A0\u60F3\u8BFB" });
    const form = this.contentEl.createDiv({ cls: "cow-book-form" });
    this.bindInput(this.textField(form, "\u4E66\u540D *", this.title), (value) => this.title = value);
    this.bindInput(this.textField(form, "\u4F5C\u8005", this.author), (value) => this.author = value);
    this.bindInput(this.textareaField(form, "\u6982\u8FF0", this.summary), (value) => this.summary = value);
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", () => void this.save());
  }
  async save() {
    var _a, _b, _c, _d, _e, _f, _g;
    if (!this.title.trim()) return;
    const now = (/* @__PURE__ */ new Date()).toISOString();
    const payload = {
      id: (_b = (_a = this.item) == null ? void 0 : _a.id) != null ? _b : `want-read-${Date.now()}`,
      title: this.title.trim(),
      author: this.author.trim(),
      summary: this.summary.trim(),
      status: (_d = (_c = this.item) == null ? void 0 : _c.status) != null ? _d : "pending",
      bookId: (_e = this.item) == null ? void 0 : _e.bookId,
      createdAt: (_g = (_f = this.item) == null ? void 0 : _f.createdAt) != null ? _g : now,
      updatedAt: now
    };
    if (this.item) await this.store.updateWantToReadItem(this.item.id, payload);
    else await this.store.addWantToReadItem(payload);
    this.onDataChanged();
    this.close();
  }
  textField(container, label, value) {
    const row = container.createDiv({ cls: "cow-book-form-row" });
    row.createEl("label", { text: label });
    return row.createEl("input", { value, attr: { type: "text" } });
  }
  textareaField(container, label, value) {
    const row = container.createDiv({ cls: "cow-book-form-row" });
    row.createEl("label", { text: label });
    return row.createEl("textarea", { text: value });
  }
  bindInput(input, onInput) {
    input.addEventListener("input", () => onInput(input.value));
  }
};

// src/components/fitness/FitnessModals.ts
var import_obsidian23 = require("obsidian");

// src/components/fitness/StatisticsCalendar.ts
var import_obsidian22 = require("obsidian");
var StatisticsCalendar = class {
  constructor(options) {
    this.options = options;
    this.calendar = new CalendarService();
    var _a;
    const initial = (_a = options.initialDate) != null ? _a : /* @__PURE__ */ new Date();
    this.month = new Date(initial.getFullYear(), initial.getMonth(), 1);
    this.selectedDate = this.calendar.getDateKey(initial);
  }
  render(container) {
    container.empty();
    const root = container.createDiv({ cls: "cow-fitness-stats-calendar" });
    const calendarPanel = root.createDiv({ cls: "cow-fitness-calendar-panel" });
    const details = root.createDiv({ cls: "cow-fitness-calendar-details" });
    const header = calendarPanel.createDiv({ cls: "cow-fitness-calendar-header" });
    const previous = header.createEl("button", { attr: { type: "button", "aria-label": "\u4E0A\u4E00\u6708" } });
    (0, import_obsidian22.setIcon)(previous, "chevron-left");
    previous.addEventListener("click", () => {
      this.month = this.calendar.addMonths(this.month, -1);
      this.render(container);
    });
    header.createEl("strong", { text: this.calendar.getMonthTitle(this.month) });
    const next = header.createEl("button", { attr: { type: "button", "aria-label": "\u4E0B\u4E00\u6708" } });
    (0, import_obsidian22.setIcon)(next, "chevron-right");
    next.addEventListener("click", () => {
      this.month = this.calendar.addMonths(this.month, 1);
      this.render(container);
    });
    const today6 = header.createEl("button", { cls: "cow-calendar-today", text: "\u4ECA\u5929", attr: { type: "button" } });
    today6.addEventListener("click", () => {
      const now = /* @__PURE__ */ new Date();
      this.month = new Date(now.getFullYear(), now.getMonth(), 1);
      this.selectedDate = this.calendar.getDateKey(now);
      this.render(container);
    });
    const grid = calendarPanel.createDiv({ cls: "cow-fitness-calendar-grid" });
    ["\u4E00", "\u4E8C", "\u4E09", "\u56DB", "\u4E94", "\u516D", "\u65E5"].forEach((weekday) => grid.createSpan({ cls: "cow-weekday", text: weekday }));
    this.calendar.getMonthCells(this.month, "monday").forEach((date) => {
      if (!date) {
        grid.createSpan({ cls: "cow-calendar-empty" });
        return;
      }
      const dateKey = this.calendar.getDateKey(date);
      const day = grid.createEl("button", {
        cls: `cow-fitness-calendar-day ${dateKey === this.calendar.getDateKey(/* @__PURE__ */ new Date()) ? "is-today" : ""} ${dateKey === this.selectedDate ? "is-selected" : ""}`,
        attr: { type: "button" }
      });
      day.createSpan({ cls: "cow-calendar-day-number", text: String(date.getDate()) });
      const indicators = this.options.getIndicators(dateKey).slice(0, 4);
      const dots = day.createDiv({ cls: "cow-calendar-dots" });
      indicators.forEach((indicator) => dots.createSpan({ cls: `is-${indicator}` }));
      day.addEventListener("click", () => {
        this.selectedDate = dateKey;
        this.render(container);
      });
    });
    details.createEl("h3", { text: this.selectedDate });
    details.createDiv({ cls: "cow-meta-line" }).createSpan({ text: this.calendar.getWeekdayLabel(/* @__PURE__ */ new Date(`${this.selectedDate}T00:00:00`)) });
    this.options.renderDayDetails(details, this.selectedDate);
  }
};

// src/components/fitness/FitnessModals.ts
var FITNESS_GOAL_TYPES = ["\u51CF\u91CD", "\u51CF\u8102", "\u589E\u808C", "\u8DD1\u6B65", "\u529B\u91CF", "\u8FD0\u52A8\u9891\u7387", "\u5176\u5B83"];
var WORKOUT_TYPES = ["\u8DD1\u6B65", "\u529B\u91CF", "\u9A91\u884C", "\u6E38\u6CF3", "\u745C\u4F3D", "\u5176\u5B83"];
function today2() {
  return formatDateKey(/* @__PURE__ */ new Date());
}
function nowIso2() {
  return (/* @__PURE__ */ new Date()).toISOString();
}
function createField(container, label, type, value) {
  const isPicker = type === "date" || type === "time";
  const row = container.createDiv({ cls: `cow-fitness-form-row ${isPicker ? "is-picker" : ""}` });
  row.createEl("label", { text: label });
  const input = row.createEl("input", { attr: { type, value } });
  if (isPicker) {
    row.addEventListener("click", (event) => {
      var _a;
      if (event.target instanceof HTMLInputElement && event.target !== input) return;
      input.focus();
      try {
        (_a = input.showPicker) == null ? void 0 : _a.call(input);
      } catch (e) {
        input.focus();
      }
    });
  }
  return input;
}
function createTextArea(container, label, value) {
  const row = container.createDiv({ cls: "cow-fitness-form-row" });
  row.createEl("label", { text: label });
  return row.createEl("textarea", { text: value });
}
function toNumber(input) {
  return Number(input.value) || 0;
}
function setupEditModal(modal, className) {
  applyResizableModal(modal, {
    className,
    width: "min(760px, 90vw)",
    maxWidth: "94vw",
    maxHeight: "88vh",
    minWidth: "min(420px, 90vw)",
    minHeight: "min(320px, 80vh)"
  });
}
function setupStatsModal(modal, className) {
  applyResizableModal(modal, {
    className,
    width: "min(1050px, 92vw)",
    height: "min(720px, 86vh)",
    maxWidth: "96vw",
    maxHeight: "92vh",
    minWidth: "min(620px, 92vw)",
    minHeight: "min(420px, 86vh)"
  });
}
function openBodyMeasurementModal2(app, store, onDone, item) {
  new BodyMeasurementModal(app, store, onDone, item).open();
}
var BodyMeasurementModal = class extends import_obsidian23.Modal {
  constructor(app, store, onDone, item) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.item = item;
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n;
    setupEditModal(this, "cute-fitness-edit-modal");
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-fitness-modal");
    this.contentEl.createEl("h2", { text: this.item ? "\u7F16\u8F91\u8EAB\u4F53\u6570\u636E" : "\u8BB0\u5F55\u8EAB\u4F53\u6570\u636E" });
    const form = this.contentEl.createDiv({ cls: "cow-fitness-form-grid" });
    const date = createField(form, "\u65E5\u671F", "date", (_b = (_a = this.item) == null ? void 0 : _a.date) != null ? _b : today2());
    const weight = createField(form, "\u4F53\u91CD", "number", String((_d = (_c = this.item) == null ? void 0 : _c.weight) != null ? _d : ""));
    const bmi = createField(form, "BMI", "number", String((_f = (_e = this.item) == null ? void 0 : _e.bmi) != null ? _f : ""));
    const waist = createField(form, "\u8170\u56F4", "number", String((_h = (_g = this.item) == null ? void 0 : _g.waist) != null ? _h : ""));
    const chest = createField(form, "\u80F8\u56F4", "number", String((_j = (_i = this.item) == null ? void 0 : _i.chest) != null ? _j : ""));
    const hip = createField(form, "\u81C0\u56F4", "number", String((_l = (_k = this.item) == null ? void 0 : _k.hip) != null ? _l : ""));
    const note = createTextArea(this.contentEl, "\u5907\u6CE8", (_n = (_m = this.item) == null ? void 0 : _m.note) != null ? _n : "");
    [weight, bmi, waist, chest, hip].forEach((input) => input.setAttr("step", "0.1"));
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2, _c2, _d2, _e2;
      const values = {
        id: (_b2 = (_a2 = this.item) == null ? void 0 : _a2.id) != null ? _b2 : `measure-${Date.now()}`,
        date: date.value || today2(),
        weight: toNumber(weight),
        bmi: toNumber(bmi),
        waist: toNumber(waist),
        chest: toNumber(chest),
        hip: toNumber(hip),
        note: note.value.trim(),
        createdAt: (_d2 = (_c2 = this.item) == null ? void 0 : _c2.createdAt) != null ? _d2 : nowIso2(),
        updatedAt: nowIso2()
      };
      if (this.item) {
        await this.store.updateBodyMeasurement((_e2 = this.item.id) != null ? _e2 : this.item.date, values);
        this.onDone();
        this.close();
        return;
      }
      const sameDate = this.store.getBodyMeasurements().find((record) => record.date === values.date);
      if (sameDate) {
        new SameDayMeasurementModal(this.app, async (mode) => {
          var _a3;
          if (mode === "update") {
            await this.store.updateBodyMeasurement((_a3 = sameDate.id) != null ? _a3 : sameDate.date, { ...values, id: sameDate.id });
          } else {
            await this.store.addBodyMeasurement({ ...values, id: `measure-${Date.now()}` });
          }
          this.onDone();
          this.close();
        }).open();
        return;
      }
      await this.store.addBodyMeasurement(values);
      this.onDone();
      this.close();
    });
  }
};
var SameDayMeasurementModal = class extends import_obsidian23.Modal {
  constructor(app, onChoose) {
    super(app);
    this.onChoose = onChoose;
  }
  onOpen() {
    setupEditModal(this, "cute-fitness-confirm-modal");
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: "\u5F53\u5929\u5DF2\u6709\u8BB0\u5F55" });
    this.contentEl.createEl("p", { text: "\u9ED8\u8BA4\u5EFA\u8BAE\u66F4\u65B0\u5F53\u5929\u8BB0\u5F55\uFF0C\u4E5F\u53EF\u4EE5\u4FDD\u7559\u4E3A\u65B0\u589E\u8BB0\u5F55\u3002" });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u65B0\u589E\u8BB0\u5F55", attr: { type: "button" } }).addEventListener("click", async () => {
      await this.onChoose("new");
      this.close();
    });
    actions.createEl("button", { text: "\u66F4\u65B0\u5F53\u5929\u8BB0\u5F55", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      await this.onChoose("update");
      this.close();
    });
  }
};
var BodyMeasurementStatisticsModal = class extends import_obsidian23.Modal {
  constructor(app, store, onDone) {
    super(app);
    this.store = store;
    this.onDone = onDone;
  }
  onOpen() {
    setupStatsModal(this, "cute-fitness-stats-modal");
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-fitness-modal", "cow-fitness-stats-modal");
    this.contentEl.createEl("h2", { text: "\u8EAB\u4F53\u6570\u636E\u7EDF\u8BA1" });
    const measurements = this.store.getBodyMeasurements();
    const first = measurements[0];
    const latest = measurements[measurements.length - 1];
    const stats = this.contentEl.createDiv({ cls: "cow-stats-card-grid" });
    [
      ["\u4F53\u91CD\u53D8\u5316", this.formatDelta(latest == null ? void 0 : latest.weight, first == null ? void 0 : first.weight, "kg")],
      ["BMI\u53D8\u5316", this.formatDelta(latest == null ? void 0 : latest.bmi, first == null ? void 0 : first.bmi, "")],
      ["\u8170\u56F4\u53D8\u5316", this.formatDelta(latest == null ? void 0 : latest.waist, first == null ? void 0 : first.waist, "cm")],
      ["\u80F8\u56F4\u53D8\u5316", this.formatDelta(latest == null ? void 0 : latest.chest, first == null ? void 0 : first.chest, "cm")],
      ["\u81C0\u56F4\u53D8\u5316", this.formatDelta(latest == null ? void 0 : latest.hip, first == null ? void 0 : first.hip, "cm")]
    ].forEach(([label, value]) => {
      const card = stats.createDiv({ cls: "cow-stats-card" });
      card.createEl("strong", { text: value });
      card.createSpan({ text: label });
    });
    const calendarRoot = this.contentEl.createDiv();
    new StatisticsCalendar({
      getIndicators: (dateKey) => measurements.some((item) => item.date === dateKey) ? ["pink"] : [],
      renderDayDetails: (container, dateKey) => this.renderMeasurementDetails(container, dateKey)
    }).render(calendarRoot);
  }
  renderMeasurementDetails(container, dateKey) {
    const records = this.store.getBodyMeasurements().filter((item) => item.date === dateKey);
    if (records.length === 0) {
      container.createEl("p", { cls: "cow-empty-state", text: "\u8FD9\u4E00\u5929\u6CA1\u6709\u8EAB\u4F53\u6570\u636E\u3002" });
      return;
    }
    records.forEach((record) => {
      const row = container.createDiv({ cls: "cow-data-card" });
      row.createEl("strong", { text: `${record.weight}kg \xB7 BMI ${record.bmi}` });
      row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `\u8170\u56F4 ${record.waist}cm \xB7 \u80F8\u56F4 ${record.chest}cm \xB7 \u81C0\u56F4 ${record.hip}cm` });
      if (record.note) row.createEl("p", { text: record.note });
      const actions = row.createDiv({ cls: "cow-list-item-actions" });
      actions.createEl("button", { text: "\u7F16\u8F91", attr: { type: "button" } }).addEventListener("click", () => openBodyMeasurementModal2(this.app, this.store, () => {
        this.onDone();
        this.render();
      }, record));
      actions.createEl("button", { text: "\u5220\u9664", cls: "mod-warning", attr: { type: "button" } }).addEventListener("click", async () => {
        var _a;
        await this.store.deleteBodyMeasurement((_a = record.id) != null ? _a : record.date);
        this.onDone();
        this.render();
      });
    });
  }
  formatDelta(latest, first, unit = "") {
    if (latest === void 0 || first === void 0) return "--";
    const delta = Math.round((latest - first) * 10) / 10;
    return `${delta > 0 ? "+" : ""}${delta}${unit}`;
  }
};
function openDailyHealthHabitModal(app, store, onDone, date = today2()) {
  new DailyHealthHabitModal(app, store, onDone, date).open();
}
var DailyHealthHabitModal = class extends import_obsidian23.Modal {
  constructor(app, store, onDone, date) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.date = date;
  }
  onOpen() {
    setupEditModal(this, "cute-fitness-edit-modal");
    this.render();
  }
  render() {
    var _a, _b, _c, _d;
    const record = this.store.getFitnessDailyRecord(this.date);
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-fitness-modal");
    this.contentEl.createEl("h2", { text: "\u7F16\u8F91\u4E60\u60EF" });
    const dateInput = createField(this.contentEl, "\u65E5\u671F", "date", this.date);
    dateInput.addEventListener("change", () => {
      this.date = dateInput.value || today2();
      this.render();
    });
    const water = this.contentEl.createDiv({ cls: "cow-fitness-form-section" });
    water.createEl("h3", { text: "\u996E\u6C34" });
    const waterGoal = createField(water, "\u996E\u6C34\u76EE\u6807\uFF08\u676F\uFF09", "number", String(record.waterGoal));
    const waterCups = createField(water, "\u4ECA\u65E5\u996E\u6C34\u676F\u6570", "number", String(record.waterCups));
    const waterStep = water.createDiv({ cls: "cow-fitness-stepper" });
    waterStep.createEl("button", { text: "-", attr: { type: "button" } }).addEventListener("click", () => waterCups.value = String(Math.max(0, toNumber(waterCups) - 1)));
    waterStep.createEl("button", { text: "+", attr: { type: "button" } }).addEventListener("click", () => waterCups.value = String(toNumber(waterCups) + 1));
    const waterNote = createTextArea(water, "\u996E\u6C34\u5907\u6CE8", (_a = record.waterNote) != null ? _a : "");
    const sleep = this.contentEl.createDiv({ cls: "cow-fitness-form-section" });
    sleep.createEl("h3", { text: "\u7761\u7720" });
    const sleepGoal = createField(sleep, "\u7761\u7720\u76EE\u6807\uFF08\u5C0F\u65F6\uFF09", "number", String(record.sleepGoal));
    const sleepHours = createField(sleep, "\u7761\u7720\u65F6\u957F\uFF08\u5C0F\u65F6\uFF09", "number", String(record.sleepHours));
    const bedtime = createField(sleep, "\u5165\u7761\u65F6\u95F4", "time", (_b = record.bedtime) != null ? _b : "");
    const wakeTime = createField(sleep, "\u8D77\u5E8A\u65F6\u95F4", "time", (_c = record.wakeTime) != null ? _c : "");
    const sleepNote = createTextArea(sleep, "\u7761\u7720\u5907\u6CE8", (_d = record.sleepNote) != null ? _d : "");
    const custom = this.contentEl.createDiv({ cls: "cow-fitness-form-section" });
    const customHead = custom.createDiv({ cls: "cow-fitness-subhead" });
    customHead.createEl("h3", { text: "\u81EA\u5B9A\u4E49\u4E60\u60EF" });
    customHead.createEl("button", { text: "+ \u6DFB\u52A0\u4E60\u60EF", attr: { type: "button" } }).addEventListener("click", () => {
      new FitnessHabitDefinitionModal(this.app, this.store, () => this.render()).open();
    });
    this.store.getFitnessHabitDefinitions().forEach((definition) => this.renderCustomHabit(custom, definition));
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2;
      await this.store.updateFitnessDailyRecord(this.date, {
        waterGoal: toNumber(waterGoal),
        waterCups: toNumber(waterCups),
        waterNote: waterNote.value.trim(),
        sleepGoal: toNumber(sleepGoal),
        sleepHours: toNumber(sleepHours),
        bedtime: bedtime.value,
        wakeTime: wakeTime.value,
        sleepNote: sleepNote.value.trim()
      });
      const rows = this.contentEl.querySelectorAll("[data-fitness-habit-id]");
      for (const input of Array.from(rows)) {
        const habitId = (_a2 = input.dataset.fitnessHabitId) != null ? _a2 : "";
        const note = this.contentEl.querySelector(`[data-fitness-habit-note-id="${habitId}"]`);
        await this.store.updateFitnessHabitRecord(this.date, habitId, { actualValue: toNumber(input), note: (_b2 = note == null ? void 0 : note.value.trim()) != null ? _b2 : "" });
      }
      this.onDone();
      this.close();
    });
  }
  renderCustomHabit(container, definition) {
    var _a, _b;
    const daily = this.store.getFitnessHabitRecords(this.date).find((item) => item.habitId === definition.id);
    const row = container.createDiv({ cls: "cow-fitness-habit-row" });
    const body = row.createDiv();
    body.createEl("strong", { text: definition.name });
    body.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${definition.targetName}: ${definition.targetValue}${definition.unit}` });
    const input = row.createEl("input", { attr: { type: "number", value: String((_a = daily == null ? void 0 : daily.actualValue) != null ? _a : 0) } });
    input.dataset.fitnessHabitId = definition.id;
    const note = row.createEl("textarea", { text: (_b = daily == null ? void 0 : daily.note) != null ? _b : "", attr: { placeholder: "\u4ECA\u65E5\u5907\u6CE8" } });
    note.dataset.fitnessHabitNoteId = definition.id;
    const actions = row.createDiv({ cls: "cow-list-item-actions" });
    actions.createEl("button", { text: "\u2191", attr: { type: "button", "aria-label": "\u4E0A\u79FB" } }).addEventListener("click", async () => {
      await this.store.moveFitnessHabitDefinition(definition.id, -1);
      this.render();
    });
    actions.createEl("button", { text: "\u2193", attr: { type: "button", "aria-label": "\u4E0B\u79FB" } }).addEventListener("click", async () => {
      await this.store.moveFitnessHabitDefinition(definition.id, 1);
      this.render();
    });
    const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u4E60\u60EF" } });
    (0, import_obsidian23.setIcon)(edit, "pencil");
    edit.addEventListener("click", () => new FitnessHabitDefinitionModal(this.app, this.store, () => this.render(), definition).open());
    const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u4E60\u60EF" } });
    (0, import_obsidian23.setIcon)(remove, "trash-2");
    remove.addEventListener("click", async () => {
      await this.store.deleteFitnessHabitDefinition(definition.id);
      this.render();
    });
  }
};
var FitnessHabitDefinitionModal = class extends import_obsidian23.Modal {
  constructor(app, store, onDone, definition) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.definition = definition;
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    setupEditModal(this, "cute-fitness-edit-modal");
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-fitness-modal");
    this.contentEl.createEl("h2", { text: this.definition ? "\u7F16\u8F91\u81EA\u5B9A\u4E49\u4E60\u60EF" : "\u65B0\u589E\u81EA\u5B9A\u4E49\u4E60\u60EF" });
    const name = createField(this.contentEl, "\u4E60\u60EF\u540D\u79F0", "text", (_b = (_a = this.definition) == null ? void 0 : _a.name) != null ? _b : "");
    const targetName = createField(this.contentEl, "\u76EE\u6807\u540D\u79F0", "text", (_d = (_c = this.definition) == null ? void 0 : _c.targetName) != null ? _d : "");
    const targetValue = createField(this.contentEl, "\u76EE\u6807\u503C", "number", String((_f = (_e = this.definition) == null ? void 0 : _e.targetValue) != null ? _f : 0));
    const unit = createField(this.contentEl, "\u5355\u4F4D", "text", (_h = (_g = this.definition) == null ? void 0 : _g.unit) != null ? _h : "");
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      const values = {
        name: name.value.trim() || "\u81EA\u5B9A\u4E49\u4E60\u60EF",
        targetName: targetName.value.trim() || "\u6BCF\u65E5\u76EE\u6807",
        targetValue: toNumber(targetValue),
        unit: unit.value.trim()
      };
      if (this.definition) {
        await this.store.updateFitnessHabitDefinition(this.definition.id, values);
      } else {
        await this.store.addFitnessHabitDefinition(values);
      }
      this.onDone();
      this.close();
    });
  }
};
var HealthHabitStatisticsModal = class extends import_obsidian23.Modal {
  constructor(app, store) {
    super(app);
    this.store = store;
    this.mode = "all";
  }
  onOpen() {
    setupStatsModal(this, "cute-fitness-stats-modal");
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-fitness-modal", "cow-fitness-stats-modal");
    this.contentEl.createEl("h2", { text: "\u4E60\u60EF\u7EDF\u8BA1" });
    const filters = this.contentEl.createDiv({ cls: "cow-focus-filter-row" });
    [
      ["all", "\u5168\u90E8"],
      ["water", "\u996E\u6C34"],
      ["sleep", "\u7761\u7720"],
      ["custom", "\u81EA\u5B9A\u4E49\u4E60\u60EF"]
    ].forEach(([id, label]) => {
      const button = filters.createEl("button", { cls: this.mode === id ? "is-active" : "", text: label, attr: { type: "button" } });
      button.addEventListener("click", () => {
        this.mode = id;
        this.render();
      });
    });
    const root = this.contentEl.createDiv();
    new StatisticsCalendar({
      getIndicators: (dateKey) => this.getHabitIndicators(dateKey),
      renderDayDetails: (container, dateKey) => this.renderHealthDetails(container, dateKey)
    }).render(root);
  }
  getHabitIndicators(dateKey) {
    const record = this.store.getFitnessDailyRecord(dateKey);
    const custom = this.store.getFitnessHabitRecords(dateKey);
    const dots = [];
    if ((this.mode === "all" || this.mode === "water") && (record.waterCups > 0 || record.waterNote)) dots.push("blue");
    if ((this.mode === "all" || this.mode === "sleep") && (record.sleepHours > 0 || record.bedtime || record.wakeTime)) dots.push("purple");
    if ((this.mode === "all" || this.mode === "custom") && custom.length > 0) dots.push("green");
    return dots;
  }
  renderHealthDetails(container, dateKey) {
    const record = this.store.getFitnessDailyRecord(dateKey);
    const list = container.createDiv({ cls: "cow-data-list" });
    list.createDiv({ cls: "cow-data-card" }).createEl("strong", { text: `\u996E\u6C34 ${record.waterCups}/${record.waterGoal} \u676F` });
    if (record.waterNote) list.createDiv({ cls: "cow-data-card" }).createEl("p", { text: record.waterNote });
    list.createDiv({ cls: "cow-data-card" }).createEl("strong", { text: `\u7761\u7720 ${record.sleepHours}/${record.sleepGoal} \u5C0F\u65F6` });
    list.createDiv({ cls: "cow-data-card" }).createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${record.bedtime || "--"} - ${record.wakeTime || "--"}` });
    this.store.getFitnessHabitDefinitions().forEach((definition) => {
      var _a;
      const daily = this.store.getFitnessHabitRecords(dateKey).find((item) => item.habitId === definition.id);
      const row = list.createDiv({ cls: "cow-data-card" });
      row.createEl("strong", { text: definition.name });
      row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${(_a = daily == null ? void 0 : daily.actualValue) != null ? _a : 0}/${definition.targetValue}${definition.unit}` });
    });
  }
};
function openFitnessGoalModal(app, store, onDone, goal) {
  new FitnessGoalModal(app, store, onDone, goal).open();
}
var FitnessGoalModal = class extends import_obsidian23.Modal {
  constructor(app, store, onDone, goal) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.goal = goal;
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x;
    setupEditModal(this, "cute-fitness-edit-modal");
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-fitness-modal");
    this.contentEl.createEl("h2", { text: this.goal ? "\u7F16\u8F91\u5065\u8EAB\u76EE\u6807" : "\u65B0\u589E\u5065\u8EAB\u76EE\u6807" });
    const title = createField(this.contentEl, "\u76EE\u6807\u540D\u79F0", "text", (_b = (_a = this.goal) == null ? void 0 : _a.title) != null ? _b : "");
    const typeRow = this.contentEl.createDiv({ cls: "cow-fitness-form-row" });
    typeRow.createEl("label", { text: "\u76EE\u6807\u7C7B\u578B" });
    const type = typeRow.createEl("select");
    FITNESS_GOAL_TYPES.forEach((item) => type.createEl("option", { value: item, text: item }));
    type.value = (_d = (_c = this.goal) == null ? void 0 : _c.type) != null ? _d : "\u5176\u5B83";
    const current = createField(this.contentEl, "\u5F53\u524D\u503C", "number", String((_h = (_g = (_e = this.goal) == null ? void 0 : _e.currentValue) != null ? _g : (_f = this.goal) == null ? void 0 : _f.current) != null ? _h : 0));
    const target = createField(this.contentEl, "\u76EE\u6807\u503C", "number", String((_l = (_k = (_i = this.goal) == null ? void 0 : _i.targetValue) != null ? _k : (_j = this.goal) == null ? void 0 : _j.target) != null ? _l : 0));
    const unit = createField(this.contentEl, "\u5355\u4F4D", "text", (_p = (_o = (_m = this.goal) == null ? void 0 : _m.targetUnit) != null ? _o : (_n = this.goal) == null ? void 0 : _n.unit) != null ? _p : "");
    const progress = createField(this.contentEl, "\u8FDB\u5EA6 %", "number", String((_r = (_q = this.goal) == null ? void 0 : _q.progress) != null ? _r : 0));
    const startDate = createField(this.contentEl, "\u5F00\u59CB\u65E5\u671F", "date", (_t = (_s = this.goal) == null ? void 0 : _s.startDate) != null ? _t : today2());
    const deadline = createField(this.contentEl, "\u622A\u6B62\u65E5\u671F", "date", (_v = (_u = this.goal) == null ? void 0 : _u.deadline) != null ? _v : today2());
    const description = createTextArea(this.contentEl, "\u63CF\u8FF0", (_x = (_w = this.goal) == null ? void 0 : _w.description) != null ? _x : "");
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2, _c2, _d2, _e2, _f2, _g2;
      const values = {
        id: (_b2 = (_a2 = this.goal) == null ? void 0 : _a2.id) != null ? _b2 : `fitness-goal-${Date.now()}`,
        title: title.value.trim() || "\u5065\u8EAB\u76EE\u6807",
        type: type.value,
        description: description.value.trim(),
        currentValue: toNumber(current),
        targetValue: toNumber(target),
        unit: unit.value.trim(),
        targetUnit: unit.value.trim(),
        progress: Math.max(0, Math.min(100, toNumber(progress))),
        startDate: startDate.value || today2(),
        deadline: deadline.value || today2(),
        completedDate: (_c2 = this.goal) == null ? void 0 : _c2.completedDate,
        status: (_e2 = (_d2 = this.goal) == null ? void 0 : _d2.status) != null ? _e2 : "active",
        createdAt: (_g2 = (_f2 = this.goal) == null ? void 0 : _f2.createdAt) != null ? _g2 : nowIso2(),
        updatedAt: nowIso2()
      };
      if (this.goal) {
        await this.store.updateFitnessGoal(this.goal.id, values);
      } else {
        await this.store.addFitnessGoal(values);
      }
      this.onDone();
      this.close();
    });
  }
};
function openFitnessGoalDetailModal(app, store, onDone, goal) {
  new FitnessGoalDetailModal(app, store, onDone, goal).open();
}
var FitnessGoalDetailModal = class extends import_obsidian23.Modal {
  constructor(app, store, onDone, goal) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.goal = goal;
  }
  onOpen() {
    setupStatsModal(this, "cute-fitness-detail-modal");
    this.render();
  }
  render() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
    const goal = (_a = this.store.getFitnessGoals().find((item) => item.id === this.goal.id)) != null ? _a : this.goal;
    const status = getGoalStatus(goal);
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-fitness-modal", "cow-fitness-detail-modal");
    const header = this.contentEl.createDiv({ cls: "cow-fitness-detail-header" });
    header.createEl("h2", { text: goal.title });
    header.createSpan({ cls: `cow-status is-${statusColor(status)}`, text: goalStatusLabel(status) });
    const summary = this.contentEl.createDiv({ cls: "cow-fitness-detail-grid" });
    this.detailItem(summary, "\u7C7B\u578B", (_b = goal.type) != null ? _b : "\u5176\u5B83");
    this.detailItem(summary, "\u76EE\u6807\u503C", `${(_d = (_c = goal.targetValue) != null ? _c : goal.target) != null ? _d : 0}${(_f = (_e = goal.targetUnit) != null ? _e : goal.unit) != null ? _f : ""}`);
    this.detailItem(summary, "\u5F00\u59CB\u65E5\u671F", (_g = goal.startDate) != null ? _g : "--");
    this.detailItem(summary, "\u622A\u6B62\u65E5\u671F", (_h = goal.deadline) != null ? _h : "--");
    this.detailItem(summary, "\u5F53\u524D\u8FDB\u5EA6", `${(_i = goal.progress) != null ? _i : calculateGoalPercent(goal)}%`);
    this.detailItem(summary, "\u5B8C\u6210\u65E5\u671F", (_j = goal.completedDate) != null ? _j : "--");
    if (goal.description) this.contentEl.createEl("p", { text: goal.description });
    this.renderTrainingPlans();
    this.renderRelatedWorkouts();
    this.renderRelatedHabits();
    this.renderBodyMeasurements();
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u7F16\u8F91\u76EE\u6807", attr: { type: "button" } }).addEventListener("click", () => {
      openFitnessGoalModal(this.app, this.store, () => {
        this.onDone();
        this.render();
      }, goal);
    });
    actions.createEl("button", { text: "\u5173\u95ED", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", () => this.close());
  }
  detailItem(container, label, value) {
    const item = container.createDiv({ cls: "cow-stats-card" });
    item.createSpan({ text: label });
    item.createEl("strong", { text: value });
  }
  renderTrainingPlans() {
    const section = this.contentEl.createDiv({ cls: "cow-fitness-detail-section" });
    section.createEl("strong", { text: "\u5173\u8054\u8BAD\u7EC3\u8BA1\u5212" });
    const plans = this.store.getTrainingPlans().filter((plan) => plan.fitnessGoalId === this.goal.id);
    if (plans.length === 0) {
      section.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u5173\u8054\u8BAD\u7EC3\u8BA1\u5212\u3002" });
      return;
    }
    const list = section.createDiv({ cls: "cow-data-list" });
    plans.forEach((plan) => {
      var _a;
      const execution = calculatePlanExecution(plan, this.store.getWorkouts());
      const row = list.createEl("button", { cls: "cow-data-card cow-click-card", attr: { type: "button" } });
      row.createEl("strong", { text: plan.title });
      row.createDiv({ cls: "cow-meta-line", text: `${formatDateRange2(plan.startDate, plan.endDate)} \xB7 ${trainingPlanStatusLabel((_a = plan.status) != null ? _a : "active")} \xB7 \u6267\u884C\u7387 ${execution.rate}%` });
      row.addEventListener("click", () => openTrainingPlanDetailModal(this.app, this.store, this.onDone, plan));
    });
  }
  renderRelatedWorkouts() {
    const section = this.contentEl.createDiv({ cls: "cow-fitness-detail-section" });
    section.createEl("strong", { text: "\u76F8\u5173\u8FD0\u52A8\u8BB0\u5F55" });
    const plans = this.store.getTrainingPlans().filter((plan) => plan.fitnessGoalId === this.goal.id);
    const planIds = new Set(plans.map((plan) => plan.id));
    const workouts = this.store.getWorkouts().filter((workout) => workout.fitnessGoalId === this.goal.id || (workout.trainingPlanId ? planIds.has(workout.trainingPlanId) : false)).slice(0, 5);
    if (workouts.length === 0) {
      section.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u8FD0\u52A8\u8BB0\u5F55\u3002" });
      return;
    }
    const list = section.createDiv({ cls: "cow-data-list" });
    workouts.forEach((workout) => {
      const row = list.createEl("button", { cls: "cow-data-card cow-click-card", attr: { type: "button" } });
      row.createEl("strong", { text: workoutTitle(workout) });
      row.createDiv({ cls: "cow-meta-line", text: `${workout.date} \xB7 ${workoutTypeLabel(workout)} \xB7 ${workoutDuration(workout)}min` });
      row.addEventListener("click", () => openWorkoutRecordDetailModal(this.app, this.store, this.onDone, workout));
    });
  }
  renderRelatedHabits() {
    const section = this.contentEl.createDiv({ cls: "cow-fitness-detail-section" });
    section.createEl("strong", { text: "\u76F8\u5173\u4E60\u60EF" });
    const list = section.createDiv({ cls: "cow-data-list" });
    this.store.getFitnessHabitDefinitions().slice(0, 4).forEach((habit) => {
      const row = list.createDiv({ cls: "cow-data-card" });
      row.createEl("strong", { text: habit.name });
      row.createDiv({ cls: "cow-meta-line", text: `${habit.targetName} ${habit.targetValue}${habit.unit}` });
    });
  }
  renderBodyMeasurements() {
    const section = this.contentEl.createDiv({ cls: "cow-fitness-detail-section" });
    section.createEl("strong", { text: "\u8EAB\u4F53\u6570\u636E\u53D8\u5316" });
    const measurements = this.store.getBodyMeasurements().slice(-3);
    if (measurements.length === 0) {
      section.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u8EAB\u4F53\u6570\u636E\u3002" });
      return;
    }
    const list = section.createDiv({ cls: "cow-data-list" });
    measurements.forEach((item) => {
      const row = list.createDiv({ cls: "cow-data-card" });
      row.createEl("strong", { text: item.date });
      row.createDiv({ cls: "cow-meta-line", text: `\u4F53\u91CD ${item.weight}kg \xB7 BMI ${item.bmi} \xB7 \u8170\u56F4 ${item.waist}cm` });
    });
  }
};
function openTrainingPlanModal(app, store, onDone, plan) {
  new TrainingPlanModal(app, store, onDone, plan).open();
}
function openWorkoutRecordModal(app, store, onDone, workout) {
  new WorkoutRecordModal(app, store, onDone, workout).open();
}
var WorkoutRecordModal = class extends import_obsidian23.Modal {
  constructor(app, store, onDone, workout) {
    var _a;
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.workout = workout;
    this.markdownPath = (_a = workout == null ? void 0 : workout.markdownPath) != null ? _a : "";
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w;
    setupEditModal(this, "cute-fitness-edit-modal");
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-fitness-modal");
    this.contentEl.createEl("h2", { text: this.workout ? "\u7F16\u8F91\u8FD0\u52A8\u65E5\u5FD7" : "\u65B0\u589E\u8FD0\u52A8\u65E5\u5FD7" });
    const form = this.contentEl.createDiv({ cls: "cow-fitness-form-grid" });
    const title = createField(form, "\u8BAD\u7EC3\u540D\u79F0", "text", workoutTitle((_a = this.workout) != null ? _a : this.emptyWorkout()));
    const date = createField(form, "\u8BAD\u7EC3\u65E5\u671F", "date", (_c = (_b = this.workout) == null ? void 0 : _b.date) != null ? _c : today2());
    const typeRow = form.createDiv({ cls: "cow-fitness-form-row" });
    typeRow.createEl("label", { text: "\u8BAD\u7EC3\u7C7B\u578B" });
    const workoutType = typeRow.createEl("select");
    WORKOUT_TYPES.forEach((type) => workoutType.createEl("option", { text: type, value: type }));
    workoutType.value = (_f = (_d = this.workout) == null ? void 0 : _d.workoutType) != null ? _f : workoutTypeLabel((_e = this.workout) != null ? _e : this.emptyWorkout());
    const planRow = form.createDiv({ cls: "cow-fitness-form-row" });
    planRow.createEl("label", { text: "\u6240\u5C5E\u8BAD\u7EC3\u8BA1\u5212" });
    const planSelect = planRow.createEl("select");
    planSelect.createEl("option", { value: "", text: "\u4E0D\u5173\u8054" });
    this.store.getTrainingPlans().forEach((plan) => planSelect.createEl("option", { value: plan.id, text: plan.title }));
    planSelect.value = (_h = (_g = this.workout) == null ? void 0 : _g.trainingPlanId) != null ? _h : "";
    const goalRow = form.createDiv({ cls: "cow-fitness-form-row" });
    goalRow.createEl("label", { text: "\u5173\u8054\u5065\u8EAB\u76EE\u6807" });
    const goalSelect = goalRow.createEl("select");
    goalSelect.createEl("option", { value: "", text: "\u4E0D\u5173\u8054" });
    this.store.getFitnessGoals().forEach((goal) => goalSelect.createEl("option", { value: goal.id, text: goal.title }));
    goalSelect.value = (_l = (_k = (_i = this.workout) == null ? void 0 : _i.fitnessGoalId) != null ? _k : (_j = this.store.getTrainingPlans().find((plan) => plan.id === planSelect.value)) == null ? void 0 : _j.fitnessGoalId) != null ? _l : "";
    const duration = createField(form, "\u8BAD\u7EC3\u65F6\u957F\uFF08\u5206\u949F\uFF09", "number", String(workoutDuration((_m = this.workout) != null ? _m : this.emptyWorkout()) || 30));
    const distance = createField(form, "\u8DD1\u6B65\u8DDD\u79BB\uFF08km\uFF09", "number", String((_o = (_n = this.workout) == null ? void 0 : _n.distanceKm) != null ? _o : ""));
    duration.setAttr("min", "0");
    distance.setAttr("min", "0");
    distance.setAttr("step", "0.01");
    planSelect.addEventListener("change", () => {
      var _a2;
      const planGoalId = (_a2 = this.store.getTrainingPlans().find((plan) => plan.id === planSelect.value)) == null ? void 0 : _a2.fitnessGoalId;
      if (planGoalId) goalSelect.value = planGoalId;
    });
    const exerciseDetails = createTextArea(this.contentEl, "\u5177\u4F53\u8FD0\u52A8\u5185\u5BB9", (_q = (_p = this.workout) == null ? void 0 : _p.exerciseDetails) != null ? _q : "");
    const resultSummary = createTextArea(this.contentEl, "\u7ED3\u679C\u6458\u8981", (_s = (_r = this.workout) == null ? void 0 : _r.resultSummary) != null ? _s : "");
    const feeling = createTextArea(this.contentEl, "\u8BAD\u7EC3\u611F\u53D7", (_u = (_t = this.workout) == null ? void 0 : _t.feeling) != null ? _u : "");
    const note = createTextArea(this.contentEl, "\u5907\u6CE8", (_w = (_v = this.workout) == null ? void 0 : _v.note) != null ? _w : "");
    new MarkdownFilePicker(this.app, {
      label: "\u5173\u8054\u8BAD\u7EC3\u8BB0\u5F55\u7B14\u8BB0",
      placeholder: "\u641C\u7D22 Vault \u4E2D\u7684 Markdown \u6587\u4EF6\u2026\u2026",
      value: this.markdownPath,
      onChange: (path) => this.markdownPath = path
    }).render(this.contentEl);
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2, _c2, _d2, _e2, _f2, _g2, _h2;
      const selectedType = workoutType.value;
      const values = {
        id: (_b2 = (_a2 = this.workout) == null ? void 0 : _a2.id) != null ? _b2 : `workout-${Date.now()}`,
        title: title.value.trim() || `${selectedType}\u8BAD\u7EC3`,
        date: date.value || today2(),
        type: selectedType,
        workoutType: selectedType,
        trainingPlanId: planSelect.value || void 0,
        fitnessGoalId: goalSelect.value || void 0,
        duration: Math.max(0, toNumber(duration)),
        durationMinutes: Math.max(0, toNumber(duration)),
        calories: (_d2 = (_c2 = this.workout) == null ? void 0 : _c2.calories) != null ? _d2 : 0,
        distanceKm: toNumber(distance) > 0 ? toNumber(distance) : void 0,
        exerciseDetails: exerciseDetails.value.trim(),
        resultSummary: resultSummary.value.trim(),
        feeling: feeling.value.trim(),
        note: note.value.trim(),
        markdownPath: this.markdownPath || void 0,
        completed: (_f2 = (_e2 = this.workout) == null ? void 0 : _e2.completed) != null ? _f2 : true,
        createdAt: (_h2 = (_g2 = this.workout) == null ? void 0 : _g2.createdAt) != null ? _h2 : nowIso2(),
        updatedAt: nowIso2()
      };
      if (this.workout) await this.store.updateWorkout(this.workout.id, values);
      else await this.store.addWorkout(values);
      this.onDone();
      this.close();
    });
  }
  emptyWorkout() {
    return {
      id: "",
      title: "",
      date: today2(),
      type: "\u529B\u91CF",
      workoutType: "\u529B\u91CF",
      duration: 30,
      durationMinutes: 30,
      calories: 0,
      completed: true,
      note: ""
    };
  }
};
function openWorkoutRecordDetailModal(app, store, onDone, workout) {
  new WorkoutRecordDetailModal(app, store, onDone, workout).open();
}
var WorkoutRecordDetailModal = class extends import_obsidian23.Modal {
  constructor(app, store, onDone, workout) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.workout = workout;
  }
  onOpen() {
    setupStatsModal(this, "cute-fitness-detail-modal");
    this.render();
  }
  render() {
    var _a, _b, _c;
    const workout = (_a = this.store.getWorkouts().find((item) => item.id === this.workout.id)) != null ? _a : this.workout;
    const plan = workout.trainingPlanId ? this.store.getTrainingPlans().find((item) => item.id === workout.trainingPlanId) : void 0;
    const goal = workout.fitnessGoalId ? this.store.getFitnessGoals().find((item) => item.id === workout.fitnessGoalId) : void 0;
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-fitness-modal", "cow-fitness-detail-modal");
    this.contentEl.createEl("h2", { text: workoutTitle(workout) });
    const summary = this.contentEl.createDiv({ cls: "cow-fitness-detail-grid" });
    this.detailItem(summary, "\u65E5\u671F", workout.date);
    this.detailItem(summary, "\u7C7B\u578B", workoutTypeLabel(workout));
    this.detailItem(summary, "\u65F6\u957F", `${workoutDuration(workout)} min`);
    this.detailItem(summary, "\u8DDD\u79BB", workout.distanceKm ? `${workout.distanceKm} km` : "--");
    this.detailItem(summary, "\u914D\u901F", workout.workoutType === "\u8DD1\u6B65" || workout.type === "\u8DD1\u6B65" ? calculatePace(workout) : "--");
    this.detailItem(summary, "\u5173\u8054\u8BA1\u5212", (_b = plan == null ? void 0 : plan.title) != null ? _b : "\u672A\u5173\u8054");
    this.detailItem(summary, "\u5173\u8054\u76EE\u6807", (_c = goal == null ? void 0 : goal.title) != null ? _c : "\u672A\u5173\u8054");
    this.textBlock("\u5177\u4F53\u8FD0\u52A8\u5185\u5BB9", workout.exerciseDetails);
    this.textBlock("\u7ED3\u679C\u6458\u8981", workout.resultSummary);
    this.textBlock("\u8BAD\u7EC3\u611F\u53D7", workout.feeling);
    this.textBlock("\u5907\u6CE8", workout.note);
    const note = this.contentEl.createDiv({ cls: "cow-fitness-detail-section" });
    note.createEl("strong", { text: "\u5173\u8054\u8BAD\u7EC3\u8BB0\u5F55\u7B14\u8BB0" });
    if (workout.markdownPath) {
      const button = note.createEl("button", { cls: "cow-data-card cow-click-card", attr: { type: "button" } });
      button.createEl("strong", { text: fileName5(workout.markdownPath) });
      button.createDiv({ cls: "cow-meta-line", text: workout.markdownPath });
      button.addEventListener("click", () => void openVaultMarkdown(this.app, workout.markdownPath));
    } else {
      note.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u5173\u8054\u8BAD\u7EC3\u8BB0\u5F55\u7B14\u8BB0\u3002" });
    }
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    if (plan) actions.createEl("button", { text: "\u67E5\u770B\u8BA1\u5212", attr: { type: "button" } }).addEventListener("click", () => openTrainingPlanDetailModal(this.app, this.store, this.onDone, plan));
    actions.createEl("button", { text: "\u7F16\u8F91\u65E5\u5FD7", attr: { type: "button" } }).addEventListener("click", () => openWorkoutRecordModal(this.app, this.store, () => {
      this.onDone();
      this.render();
    }, workout));
    actions.createEl("button", { text: "\u5173\u95ED", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", () => this.close());
  }
  detailItem(container, label, value) {
    const item = container.createDiv({ cls: "cow-stats-card" });
    item.createSpan({ text: label });
    item.createEl("strong", { text: value });
  }
  textBlock(label, value) {
    if (!value) return;
    const block = this.contentEl.createDiv({ cls: "cow-fitness-detail-section" });
    block.createEl("strong", { text: label });
    block.createEl("p", { text: value });
  }
};
var TrainingPlanModal = class extends import_obsidian23.Modal {
  constructor(app, store, onDone, plan) {
    var _a;
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.plan = plan;
    this.markdownPath = (_a = plan == null ? void 0 : plan.markdownPath) != null ? _a : "";
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n;
    setupEditModal(this, "cute-fitness-edit-modal");
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-fitness-modal");
    this.contentEl.createEl("h2", { text: this.plan ? "\u7F16\u8F91\u8BAD\u7EC3\u8BA1\u5212" : "\u65B0\u589E\u8BAD\u7EC3\u8BA1\u5212" });
    const title = createField(this.contentEl, "\u8BA1\u5212\u540D\u79F0", "text", (_b = (_a = this.plan) == null ? void 0 : _a.title) != null ? _b : "");
    const goalRow = this.contentEl.createDiv({ cls: "cow-fitness-form-row" });
    goalRow.createEl("label", { text: "\u6240\u5C5E\u5065\u8EAB\u76EE\u6807" });
    const goalSelect = goalRow.createEl("select");
    goalSelect.createEl("option", { value: "", text: "\u4E0D\u5173\u8054" });
    this.store.getFitnessGoals().forEach((goal) => goalSelect.createEl("option", { value: goal.id, text: goal.title }));
    goalSelect.value = (_d = (_c = this.plan) == null ? void 0 : _c.fitnessGoalId) != null ? _d : "";
    const startDate = createField(this.contentEl, "\u5F00\u59CB\u65E5\u671F", "date", (_f = (_e = this.plan) == null ? void 0 : _e.startDate) != null ? _f : today2());
    const endDate = createField(this.contentEl, "\u7ED3\u675F\u65E5\u671F", "date", (_h = (_g = this.plan) == null ? void 0 : _g.endDate) != null ? _h : today2());
    const weeklyFrequency = createField(this.contentEl, "\u6BCF\u5468\u8BAD\u7EC3\u9891\u7387", "number", String((_j = (_i = this.plan) == null ? void 0 : _i.weeklyFrequency) != null ? _j : 3));
    const description = createTextArea(this.contentEl, "\u8BAD\u7EC3\u5185\u5BB9", (_l = (_k = this.plan) == null ? void 0 : _k.description) != null ? _l : "");
    const note = createTextArea(this.contentEl, "\u5907\u6CE8", (_n = (_m = this.plan) == null ? void 0 : _m.note) != null ? _n : "");
    new MarkdownFilePicker(this.app, {
      label: "\u5173\u8054\u8BAD\u7EC3\u65B9\u6848\u7B14\u8BB0",
      placeholder: "\u641C\u7D22 Vault \u4E2D\u7684 Markdown \u6587\u4EF6\u2026\u2026",
      value: this.markdownPath,
      onChange: (path) => this.markdownPath = path
    }).render(this.contentEl);
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2, _c2, _d2, _e2, _f2, _g2;
      const values = {
        id: (_b2 = (_a2 = this.plan) == null ? void 0 : _a2.id) != null ? _b2 : `training-plan-${Date.now()}`,
        title: title.value.trim() || "\u8BAD\u7EC3\u8BA1\u5212",
        fitnessGoalId: goalSelect.value || void 0,
        startDate: startDate.value || today2(),
        endDate: endDate.value && endDate.value >= (startDate.value || today2()) ? endDate.value : startDate.value || today2(),
        weeklyFrequency: Math.max(0, toNumber(weeklyFrequency)),
        description: description.value.trim(),
        note: note.value.trim(),
        markdownPath: this.markdownPath || void 0,
        status: (_d2 = (_c2 = this.plan) == null ? void 0 : _c2.status) != null ? _d2 : "active",
        completedDate: (_e2 = this.plan) == null ? void 0 : _e2.completedDate,
        createdAt: (_g2 = (_f2 = this.plan) == null ? void 0 : _f2.createdAt) != null ? _g2 : nowIso2(),
        updatedAt: nowIso2()
      };
      if (this.plan) await this.store.updateTrainingPlan(this.plan.id, values);
      else await this.store.addTrainingPlan(values);
      this.onDone();
      this.close();
    });
  }
};
function openTrainingPlanDetailModal(app, store, onDone, plan) {
  new TrainingPlanDetailModal(app, store, onDone, plan).open();
}
var TrainingPlanDetailModal = class extends import_obsidian23.Modal {
  constructor(app, store, onDone, plan) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.plan = plan;
  }
  onOpen() {
    setupStatsModal(this, "cute-fitness-detail-modal");
    this.render();
  }
  render() {
    var _a, _b, _c, _d;
    const plan = (_a = this.store.getTrainingPlans().find((item) => item.id === this.plan.id)) != null ? _a : this.plan;
    const goal = plan.fitnessGoalId ? this.store.getFitnessGoals().find((item) => item.id === plan.fitnessGoalId) : void 0;
    const workouts = this.store.getWorkouts().filter((workout) => workout.trainingPlanId === plan.id);
    const execution = calculatePlanExecution(plan, this.store.getWorkouts());
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-fitness-modal", "cow-fitness-detail-modal");
    this.contentEl.createEl("h2", { text: plan.title });
    const summary = this.contentEl.createDiv({ cls: "cow-fitness-detail-grid" });
    this.detailItem(summary, "\u5173\u8054\u76EE\u6807", (_b = goal == null ? void 0 : goal.title) != null ? _b : "\u672A\u5173\u8054");
    this.detailItem(summary, "\u5468\u671F", formatDateRange2(plan.startDate, plan.endDate));
    this.detailItem(summary, "\u6BCF\u5468\u9891\u7387", `${(_c = plan.weeklyFrequency) != null ? _c : 0} \u6B21`);
    this.detailItem(summary, "\u72B6\u6001", trainingPlanStatusLabel((_d = plan.status) != null ? _d : "active"));
    this.detailItem(summary, "\u672C\u5468\u671F\u8BA1\u5212", `${execution.planned} \u6B21`);
    this.detailItem(summary, "\u5B9E\u9645\u8BAD\u7EC3", `${execution.actual} \u6B21`);
    this.detailItem(summary, "\u6267\u884C\u7387", `${execution.rate}%`);
    if (plan.description) this.contentEl.createEl("p", { text: plan.description });
    if (plan.note) this.contentEl.createEl("p", { text: plan.note });
    const note = this.contentEl.createDiv({ cls: "cow-fitness-detail-section" });
    note.createEl("strong", { text: "\u5173\u8054\u8BAD\u7EC3\u65B9\u6848\u7B14\u8BB0" });
    if (plan.markdownPath) {
      const button = note.createEl("button", { cls: "cow-data-card cow-click-card", attr: { type: "button" } });
      button.createEl("strong", { text: fileName5(plan.markdownPath) });
      button.createDiv({ cls: "cow-meta-line", text: plan.markdownPath });
      button.addEventListener("click", () => void openVaultMarkdown(this.app, plan.markdownPath));
    } else {
      note.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u5173\u8054\u8BAD\u7EC3\u65B9\u6848\u7B14\u8BB0\u3002" });
    }
    const logs = this.contentEl.createDiv({ cls: "cow-fitness-detail-section" });
    logs.createEl("strong", { text: "\u8FD0\u52A8\u65E5\u5FD7" });
    if (workouts.length === 0) {
      logs.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u5173\u8054\u8FD0\u52A8\u65E5\u5FD7\u3002" });
    } else {
      const list = logs.createDiv({ cls: "cow-data-list" });
      workouts.forEach((workout) => {
        const row = list.createEl("button", { cls: "cow-data-card cow-click-card", attr: { type: "button" } });
        row.createEl("strong", { text: workoutTitle(workout) });
        row.createDiv({ cls: "cow-meta-line", text: `${workout.date} \xB7 ${workoutTypeLabel(workout)} \xB7 ${workoutDuration(workout)}min` });
        if (workout.resultSummary) row.createEl("p", { text: workout.resultSummary });
        row.addEventListener("click", () => openWorkoutRecordDetailModal(this.app, this.store, this.onDone, workout));
      });
    }
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u7F16\u8F91\u8BA1\u5212", attr: { type: "button" } }).addEventListener("click", () => openTrainingPlanModal(this.app, this.store, () => {
      this.onDone();
      this.render();
    }, plan));
    actions.createEl("button", { text: "\u5173\u95ED", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", () => this.close());
  }
  detailItem(container, label, value) {
    const item = container.createDiv({ cls: "cow-stats-card" });
    item.createSpan({ text: label });
    item.createEl("strong", { text: value });
  }
};
var FitnessGoalStatisticsModal = class extends import_obsidian23.Modal {
  constructor(app, store, onDone) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.tab = "all";
  }
  onOpen() {
    setupStatsModal(this, "cute-fitness-stats-modal");
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-fitness-modal", "cow-fitness-stats-modal");
    this.contentEl.createEl("h2", { text: "\u5065\u8EAB\u76EE\u6807\u7EDF\u8BA1" });
    const goals = this.store.getFitnessGoals();
    const active = goals.filter((goal) => getGoalStatus(goal) === "active");
    const completed = goals.filter((goal) => getGoalStatus(goal) === "completed");
    const overdue = goals.filter((goal) => getGoalStatus(goal) === "overdue");
    const stats = this.contentEl.createDiv({ cls: "cow-stats-card-grid" });
    [
      ["\u8FDB\u884C\u4E2D", active.length],
      ["\u5DF2\u5B8C\u6210", completed.length],
      ["\u5DF2\u903E\u671F", overdue.length],
      ["\u5B8C\u6210\u7387", `${goals.length ? Math.round(completed.length / goals.length * 100) : 0}%`]
    ].forEach(([label, value]) => {
      const card = stats.createDiv({ cls: "cow-stats-card" });
      card.createEl("strong", { text: String(value) });
      card.createSpan({ text: String(label) });
    });
    const tabs = this.contentEl.createDiv({ cls: "cow-focus-filter-row" });
    [
      ["all", "\u5168\u90E8"],
      ["active", "\u8FDB\u884C\u4E2D"],
      ["completed", "\u5DF2\u5B8C\u6210"],
      ["overdue", "\u5DF2\u903E\u671F"],
      ["past", "\u5F80\u65E5\u76EE\u6807"]
    ].forEach(([id, label]) => {
      tabs.createEl("button", { cls: this.tab === id ? "is-active" : "", text: label, attr: { type: "button" } }).addEventListener("click", () => {
        this.tab = id;
        this.render();
      });
    });
    const calendarRoot = this.contentEl.createDiv();
    new StatisticsCalendar({
      getIndicators: (dateKey) => this.getGoalIndicators(dateKey),
      renderDayDetails: (container, dateKey) => this.renderGoalDetails(container, dateKey)
    }).render(calendarRoot);
    this.renderGoalList(this.contentEl.createDiv({ cls: "cow-data-list cow-fitness-history-list" }));
  }
  getGoalIndicators(dateKey) {
    const dots = [];
    this.store.getFitnessGoals().forEach((goal) => {
      if (goal.startDate === dateKey) dots.push("blue");
      if (goal.deadline === dateKey) dots.push("yellow");
      if (goal.completedDate === dateKey) dots.push("green");
    });
    return dots;
  }
  renderGoalDetails(container, dateKey) {
    const goals = this.store.getFitnessGoals().filter((goal) => goal.startDate === dateKey || goal.deadline === dateKey || goal.completedDate === dateKey);
    if (goals.length === 0) {
      container.createEl("p", { cls: "cow-empty-state", text: "\u8FD9\u4E00\u5929\u6CA1\u6709\u76F8\u5173\u76EE\u6807\u3002" });
      return;
    }
    goals.forEach((goal) => container.createDiv({ cls: "cow-data-card" }).createEl("strong", { text: goal.title }));
  }
  renderGoalList(container) {
    this.store.getFitnessGoals().filter((goal) => {
      const status = getGoalStatus(goal);
      if (this.tab === "past") return status === "completed" && Boolean(goal.completedDate && goal.completedDate < today2());
      if (this.tab === "all") return true;
      return status === this.tab;
    }).forEach((goal) => {
      var _a, _b, _c, _d;
      const row = container.createDiv({ cls: `cow-data-card cow-fitness-goal-${getGoalStatus(goal)}` });
      row.createEl("strong", { text: goal.title });
      row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${(_b = (_a = goal.currentValue) != null ? _a : goal.current) != null ? _b : 0}/${(_d = (_c = goal.targetValue) != null ? _c : goal.target) != null ? _d : 0}${goal.unit} \xB7 ${goal.deadline}` });
    });
  }
};
function openHealthReminderModal(app, store, onDone, reminder) {
  new HealthReminderModal(app, store, onDone, reminder).open();
}
var HealthReminderModal = class extends import_obsidian23.Modal {
  constructor(app, store, onDone, reminder) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.reminder = reminder;
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l;
    setupEditModal(this, "cute-fitness-edit-modal");
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-fitness-modal");
    this.contentEl.createEl("h2", { text: this.reminder ? "\u7F16\u8F91\u5065\u5EB7\u63D0\u9192" : "\u65B0\u589E\u5065\u5EB7\u63D0\u9192" });
    const title = createField(this.contentEl, "\u63D0\u9192\u540D\u79F0", "text", (_b = (_a = this.reminder) == null ? void 0 : _a.title) != null ? _b : "");
    const date = createField(this.contentEl, "\u65E5\u671F", "date", (_d = (_c = this.reminder) == null ? void 0 : _c.date) != null ? _d : today2());
    const time = createField(this.contentEl, "\u65F6\u95F4", "time", (_f = (_e = this.reminder) == null ? void 0 : _e.time) != null ? _f : "09:00");
    const repeat = this.contentEl.createDiv({ cls: "cow-fitness-form-row" });
    repeat.createEl("label", { text: "\u91CD\u590D" });
    const repeatSelect = repeat.createEl("select");
    [
      ["once", "\u4EC5\u4E00\u6B21"],
      ["daily", "\u6BCF\u5929"],
      ["weekdays", "\u5DE5\u4F5C\u65E5"],
      ["weekly", "\u6BCF\u5468"],
      ["custom", "\u81EA\u5B9A\u4E49\u661F\u671F"]
    ].forEach(([value, label]) => repeatSelect.createEl("option", { text: label, value }));
    repeatSelect.value = (_h = (_g = this.reminder) == null ? void 0 : _g.repeatType) != null ? _h : "once";
    const note = createTextArea(this.contentEl, "\u5907\u6CE8", (_j = (_i = this.reminder) == null ? void 0 : _i.note) != null ? _j : "");
    const enabledRow = this.contentEl.createDiv({ cls: "cow-fitness-form-row is-inline" });
    const enabled = enabledRow.createEl("input", { attr: { type: "checkbox" } });
    enabled.checked = (_l = (_k = this.reminder) == null ? void 0 : _k.enabled) != null ? _l : true;
    enabledRow.createEl("label", { text: "\u542F\u7528\u63D0\u9192" });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2, _c2, _d2, _e2, _f2;
      const values = {
        id: (_b2 = (_a2 = this.reminder) == null ? void 0 : _a2.id) != null ? _b2 : `health-${Date.now()}`,
        title: title.value.trim() || "\u5065\u5EB7\u63D0\u9192",
        date: date.value || today2(),
        time: time.value || "09:00",
        repeatType: repeatSelect.value,
        repeatDays: (_d2 = (_c2 = this.reminder) == null ? void 0 : _c2.repeatDays) != null ? _d2 : [],
        note: note.value.trim(),
        enabled: enabled.checked,
        createdAt: (_f2 = (_e2 = this.reminder) == null ? void 0 : _e2.createdAt) != null ? _f2 : nowIso2(),
        updatedAt: nowIso2()
      };
      if (this.reminder) {
        await this.store.updateHealthReminder(this.reminder.id, values);
      } else {
        await this.store.addHealthReminder(values);
      }
      this.onDone();
      this.close();
    });
  }
};
var HealthReminderStatisticsModal = class extends import_obsidian23.Modal {
  constructor(app, store) {
    super(app);
    this.store = store;
  }
  onOpen() {
    setupStatsModal(this, "cute-fitness-stats-modal");
    this.render();
  }
  render() {
    const logs = this.store.getHealthReminderLogs();
    const week = new Set(this.store.getCurrentWeekDates());
    const weekLogs = logs.filter((log) => week.has(log.date));
    const completed = weekLogs.filter((log) => log.status === "completed" || log.status === "triggered").length;
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-fitness-modal", "cow-fitness-stats-modal");
    this.contentEl.createEl("h2", { text: "\u5065\u5EB7\u63D0\u9192\u7EDF\u8BA1" });
    const stats = this.contentEl.createDiv({ cls: "cow-stats-card-grid" });
    [
      ["\u672C\u5468\u63D0\u9192\u6B21\u6570", weekLogs.length],
      ["\u5DF2\u5B8C\u6210", completed],
      ["\u9519\u8FC7", weekLogs.filter((log) => log.status === "missed").length],
      ["\u5B8C\u6210\u7387", `${weekLogs.length ? Math.round(completed / weekLogs.length * 100) : 0}%`]
    ].forEach(([label, value]) => {
      const card = stats.createDiv({ cls: "cow-stats-card" });
      card.createEl("strong", { text: String(value) });
      card.createSpan({ text: String(label) });
    });
    const root = this.contentEl.createDiv();
    new StatisticsCalendar({
      getIndicators: (dateKey) => logs.some((log) => log.date === dateKey) ? ["red"] : [],
      renderDayDetails: (container, dateKey) => this.renderReminderDetails(container, dateKey)
    }).render(root);
  }
  renderReminderDetails(container, dateKey) {
    const logs = this.store.getHealthReminderLogs().filter((log) => log.date === dateKey);
    if (logs.length === 0) {
      container.createEl("p", { cls: "cow-empty-state", text: "\u8FD9\u4E00\u5929\u6CA1\u6709\u63D0\u9192\u8BB0\u5F55\u3002" });
      return;
    }
    logs.forEach((log) => {
      var _a;
      const reminder = this.store.getHealthReminders().find((item) => item.id === log.reminderId);
      const row = container.createDiv({ cls: "cow-data-card" });
      row.createEl("strong", { text: (_a = reminder == null ? void 0 : reminder.title) != null ? _a : "\u5065\u5EB7\u63D0\u9192" });
      row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${log.scheduledTime} \xB7 ${log.triggeredAt ? new Date(log.triggeredAt).toLocaleTimeString("zh-CN", { hour: "2-digit", minute: "2-digit" }) : "--"} \xB7 ${log.status}` });
      if (log.note) row.createEl("p", { text: log.note });
    });
  }
};
function getGoalStatus(goal) {
  if (goal.status === "archived") return "archived";
  if (goal.status === "completed" || goal.completedDate || goal.progress === 100) return "completed";
  if (goal.startDate && today2() < goal.startDate) return "planned";
  if (goal.deadline && goal.deadline < today2()) return "overdue";
  return "active";
}
function calculateGoalPercent(goal) {
  var _a, _b, _c, _d;
  if (typeof goal.progress === "number") return Math.max(0, Math.min(100, Math.round(goal.progress)));
  const current = (_b = (_a = goal.currentValue) != null ? _a : goal.current) != null ? _b : 0;
  const target = (_d = (_c = goal.targetValue) != null ? _c : goal.target) != null ? _d : 0;
  return target === 0 ? 0 : Math.max(0, Math.min(100, Math.round(current / target * 100)));
}
function goalStatusLabel(status) {
  if (status === "planned") return "\u672A\u5F00\u59CB";
  if (status === "completed") return "\u5DF2\u5B8C\u6210";
  if (status === "overdue") return "\u5DF2\u903E\u671F";
  if (status === "archived") return "\u5DF2\u5F52\u6863";
  return "\u8FDB\u884C\u4E2D";
}
function workoutTitle(workout) {
  var _a, _b, _c, _d, _e;
  return ((_a = workout.title) == null ? void 0 : _a.trim()) || ((_b = workout.note) == null ? void 0 : _b.trim()) || ((_d = (_c = workout.exerciseDetails) == null ? void 0 : _c.split("\n")[0]) == null ? void 0 : _d.trim()) || `${(_e = workout.workoutType) != null ? _e : workout.type}\u8BAD\u7EC3`;
}
function workoutTypeLabel(workout) {
  var _a;
  return (_a = workout.workoutType) != null ? _a : workout.type === "\u6709\u6C27" ? "\u8DD1\u6B65" : workout.type === "\u62C9\u4F38" ? "\u745C\u4F3D" : workout.type === "\u4F11\u606F" ? "\u5176\u5B83" : workout.type;
}
function workoutDuration(workout) {
  var _a;
  return Number((_a = workout.durationMinutes) != null ? _a : workout.duration) || 0;
}
function calculatePace(workout) {
  const distance = Number(workout.distanceKm) || 0;
  const duration = workoutDuration(workout);
  if (distance <= 0 || duration <= 0) return "--";
  const minutesPerKm = duration / distance;
  const minutes = Math.floor(minutesPerKm);
  const seconds = Math.round((minutesPerKm - minutes) * 60).toString().padStart(2, "0");
  return `${minutes}'${seconds}"/km`;
}
function calculatePlanExecution(plan, workouts, referenceDate = today2()) {
  const start = plan.startDate || referenceDate;
  const end = plan.endDate && plan.endDate < referenceDate ? plan.endDate : referenceDate;
  const frequency = Number(plan.weeklyFrequency) || 0;
  if (frequency <= 0 || end < start) return { planned: 0, actual: workouts.filter((workout) => workout.trainingPlanId === plan.id).length, rate: 0 };
  const days = Math.max(1, Math.floor(((/* @__PURE__ */ new Date(`${end}T00:00:00`)).getTime() - (/* @__PURE__ */ new Date(`${start}T00:00:00`)).getTime()) / 864e5) + 1);
  const planned = Math.max(1, Math.ceil(days / 7 * frequency));
  const actual = workouts.filter((workout) => workout.trainingPlanId === plan.id && workout.date >= start && workout.date <= end).length;
  return { planned, actual, rate: planned ? Math.round(actual / planned * 100) : 0 };
}
function trainingPlanStatusLabel(status) {
  if (status === "planned") return "\u672A\u5F00\u59CB";
  if (status === "completed") return "\u5DF2\u5B8C\u6210";
  if (status === "overdue") return "\u5DF2\u903E\u671F";
  return "\u8FDB\u884C\u4E2D";
}
function statusColor(status) {
  if (status === "completed") return "green";
  if (status === "overdue") return "red";
  if (status === "planned") return "yellow";
  return "blue";
}
function formatDateRange2(startDate, endDate) {
  return `${startDate} - ${endDate}`;
}
function fileName5(path) {
  var _a, _b;
  return (_b = (_a = path.split(/[\\/]/).pop()) == null ? void 0 : _a.replace(/\.md$/i, "")) != null ? _b : path;
}

// src/components/SectionContentActions.ts
function openAddContentModal(app, store, section, onDataChanged) {
  const refresh = () => onDataChanged();
  switch (section.type) {
    case "research-projects":
      openResearchProjectModal(app, store, async (values) => {
        await store.addResearchProject({ ...values, id: `project-${Date.now()}` });
        refresh();
      });
      break;
    case "reading-queue":
    case "literature-notes":
      openLiteratureNoteModal(app, store, refresh);
      break;
    case "experiment-plan":
    case "experiment-records":
      openExperimentEditModal(app, store, section.type === "experiment-plan" ? "plan" : "records", refresh);
      break;
    case "data-analysis-tasks":
      openDataAnalysisTaskModal(app, async (values) => {
        await store.addDataAnalysisTask({ ...values, id: `analysis-${Date.now()}` });
        refresh();
      });
      break;
    case "research-timeline":
      openDeadlineModal(app, async (values) => {
        await store.addResearchDeadline({ ...values, id: `deadline-${Date.now()}` });
        refresh();
      });
      break;
    case "research-memo":
      openTextModal(app, "\u65B0\u589E\u79D1\u7814 Memo", "Memo", "", async (text) => {
        await store.addResearchMemo(text);
        refresh();
      });
      break;
    case "bookshelf":
      new AddBookModal(app, store, async () => {
        refresh();
      }).open();
      break;
    case "wishlist-books":
      openWantToReadModal(app, store, refresh);
      break;
    case "reading-plan":
      openReadingPlanModal(app, store, refresh);
      break;
    case "reading-notes":
      openReadingNoteModal(app, store, refresh);
      break;
    case "reading-quotes":
      openReadingQuoteModal(app, store, refresh);
      break;
    case "reading-tag-manager":
      openBookTagModal(app, store, refresh);
      break;
    case "workout-plan":
      openTrainingPlanModal(app, store, refresh);
      break;
    case "workout-log":
      openWorkoutRecordModal(app, store, refresh);
      break;
    case "health-reminders":
      openTextModal(app, "\u65B0\u589E\u5065\u5EB7\u63D0\u9192", "\u63D0\u9192", "", async (text) => {
        await store.addHealthReminder(text);
        refresh();
      });
      break;
    case "saving-goals":
      openSavingGoalModal(app, async (values) => {
        await store.addSavingGoal({ ...values, id: `saving-${Date.now()}` });
        refresh();
      });
      break;
    case "bill-reminders":
      openBillModal(app, async (values) => {
        await store.addBill({ ...values, id: `bill-${Date.now()}` });
        refresh();
      });
      break;
    case "finance-todos":
      openTextModal(app, "\u65B0\u589E\u8BB0\u8D26\u5F85\u529E", "\u5F85\u529E", "", async (text) => {
        await store.addFinanceTodo(text);
        refresh();
      });
      break;
    case "yearly-goals":
      new GoalEditorModal(app, void 0, async (goal) => {
        await store.addGoal(goal);
        refresh();
      }).open();
      break;
    case "quarterly-okr":
      openObjectiveModal(app, async (values) => {
        await store.addObjective({ ...values, id: `objective-${Date.now()}` });
        refresh();
      });
      break;
    case "monthly-key-results":
      new KeyResultModal(app, store.getObjectives(), async (kr) => {
        await store.addKeyResult(kr);
        refresh();
      }).open();
      break;
    case "milestone-timeline":
      new MilestoneModal(app, store.getGoals(), async (milestone) => {
        await store.addMilestone(milestone);
        refresh();
      }).open();
      break;
    case "risks-blockers":
      new RiskModal(app, async (risk) => {
        await store.addRisk(risk);
        refresh();
      }).open();
      break;
    case "today-focus":
      new TodayFocusTaskModal(app, formatDateKey(/* @__PURE__ */ new Date()), async (values) => {
        await store.addTodayFocusTask(values.label, values.category, values.date);
        refresh();
      }).open();
      break;
    case "body-measurements":
      openBodyMeasurementModal(app, async (values) => {
        await store.addBodyMeasurement(values);
        refresh();
      });
      break;
    case "water-sleep-habits":
      openFitnessDailyModal(app, store.getFitnessDailyRecord(), async (values) => {
        await store.updateFitnessDailyRecord(values.date, values);
        refresh();
      });
      break;
    case "fitness-goals":
      openFitnessGoalModal(app, store, refresh);
      break;
    case "monthly-budget":
      openBudgetLimitModal(app, store.getMonthlyBudgetLimit(), async (value) => {
        await store.setMonthlyBudgetLimit(value);
        refresh();
      });
      break;
    case "expense-categories":
      openBudgetModal(app, async (values) => {
        await store.addBudget(values);
        refresh();
      });
      break;
    case "account-overview":
      openAccountModal(app, async (values) => {
        await store.addAccount(values);
        refresh();
      });
      break;
    case "income-expense-trend":
      new AddTransactionModal(app, async (values) => {
        await store.addTransaction(values);
        refresh();
      }, void 0, store.getBudgets(), store.getAccounts()).open();
      break;
    case "investment-watch":
      openInvestmentWatchModal(app, async (values) => {
        await store.addInvestmentWatchItem(values);
        refresh();
      });
      break;
    case "priority-matrix":
      openPriorityItemModal(app, async (values) => {
        await store.addPriorityMatrixItem(values);
        refresh();
      });
      break;
    default:
      new import_obsidian24.Notice("\u8FD9\u4E2A\u6A21\u5757\u6682\u672A\u63D0\u4F9B\u6DFB\u52A0\u5165\u53E3\u3002");
  }
}
function openManageContentModal(app, store, section, onDataChanged) {
  if (section.type === "today-focus") {
    new TodoStatisticsModal(app, store, /* @__PURE__ */ new Date(), onDataChanged).open();
    return;
  }
  openAddContentModal(app, store, section, onDataChanged);
}
function openTextModal(app, title, fieldName, initialValue, onSubmit) {
  new CrudItemModal(app, title, { value: initialValue }, [{ key: "value", name: fieldName, type: "textarea" }], async (values) => {
    var _a;
    const value = String((_a = values.value) != null ? _a : "").trim();
    if (!value) return;
    await onSubmit(value);
  }).open();
}
function openBudgetLimitModal(app, initialValue, onSubmit) {
  new CrudItemModal(app, "\u8BBE\u7F6E\u6708\u9884\u7B97", { amount: initialValue }, [
    { key: "amount", name: "\u6708\u9884\u7B97", type: "number" }
  ], async (values) => {
    await onSubmit(Number(values.amount) || 0);
  }).open();
}
function openResearchProjectModal(app, store, onSubmit, project) {
  new ResearchProjectEditModal(app, store, onSubmit, project).open();
}
var ResearchProjectEditModal = class extends import_obsidian24.Modal {
  constructor(app, store, onSubmit, project) {
    var _a, _b, _c, _d, _e, _f, _g;
    super(app);
    this.store = store;
    this.onSubmit = onSubmit;
    this.title = (_a = project == null ? void 0 : project.title) != null ? _a : "";
    this.description = (_b = project == null ? void 0 : project.description) != null ? _b : "";
    this.status = (_c = project == null ? void 0 : project.status) != null ? _c : "\u8FDB\u884C\u4E2D";
    this.progress = (_d = project == null ? void 0 : project.progress) != null ? _d : 0;
    this.startDate = (_e = project == null ? void 0 : project.startDate) != null ? _e : formatDateKey(/* @__PURE__ */ new Date());
    this.deadline = (_f = project == null ? void 0 : project.deadline) != null ? _f : formatDateKey(/* @__PURE__ */ new Date());
    this.tagIds = [...(_g = project == null ? void 0 : project.tagIds) != null ? _g : this.legacyTagIds(project)];
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-research-project-edit-modal",
      width: "min(760px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "92vh",
      minWidth: "min(520px, 90vw)",
      minHeight: "min(420px, 82vh)"
    });
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-paper-modal", "cow-research-project-edit-modal");
    this.contentEl.createEl("h2", { text: this.title ? "\u7F16\u8F91\u7814\u7A76\u9879\u76EE" : "\u65B0\u589E\u7814\u7A76\u9879\u76EE" });
    const form = this.contentEl.createDiv({ cls: "cow-paper-form" });
    this.inputField(form, "\u6807\u9898", this.title, (value) => this.title = value);
    this.selectField(form, "\u72B6\u6001", this.status, statusOptions(["\u672A\u5F00\u59CB", "\u8FDB\u884C\u4E2D", "\u64B0\u5199\u4E2D", "\u5DF2\u5B8C\u6210"]), (value) => this.status = value);
    this.inputField(form, "\u8FDB\u5EA6", String(this.progress), (value) => this.progress = Number(value) || 0, "number");
    this.inputField(form, "\u5F00\u59CB\u65E5\u671F", this.startDate, (value) => this.startDate = value, "date");
    this.inputField(form, "\u622A\u6B62\u65E5\u671F", this.deadline, (value) => this.deadline = value, "date");
    this.descriptionField(form);
    this.tagField(form);
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", () => void this.save());
  }
  inputField(container, label, value, onInput, type = "text") {
    const row = container.createDiv({ cls: "cow-book-form-row" });
    row.createEl("label", { text: label });
    const input = row.createEl("input", { attr: { type, value } });
    input.addEventListener("input", () => onInput(input.value));
  }
  selectField(container, label, value, options, onChange) {
    const row = container.createDiv({ cls: "cow-book-form-row" });
    row.createEl("label", { text: label });
    const select = row.createEl("select");
    options.forEach((option) => select.createEl("option", { value: option.value, text: option.label }));
    select.value = value;
    select.addEventListener("change", () => onChange(select.value));
  }
  descriptionField(container) {
    const row = container.createDiv({ cls: "cow-book-form-row cow-project-description-field" });
    row.createEl("label", { text: "\u9879\u76EE\u63CF\u8FF0" });
    const textarea = row.createEl("textarea", { attr: { rows: "5", placeholder: "\u7B80\u5355\u63CF\u8FF0\u7814\u7A76\u76EE\u6807\u3001\u7814\u7A76\u5185\u5BB9\u6216\u5F53\u524D\u65B9\u5411\u2026\u2026" } });
    textarea.value = this.description;
    textarea.addEventListener("input", () => this.description = textarea.value);
  }
  tagField(container) {
    const row = container.createDiv({ cls: "cow-book-form-row cow-paper-tag-picker" });
    row.createEl("label", { text: "\u6807\u7B7E" });
    const list = row.createDiv({ cls: "cow-paper-tag-options" });
    const selected = new Set(this.tagIds);
    this.store.getPaperTags().forEach((tag) => {
      const button = list.createEl("button", { cls: selected.has(tag.id) ? "is-active" : "", attr: { type: "button", style: `--paper-color: ${tag.color}` } });
      button.setText(`${selected.has(tag.id) ? "\u2713 " : ""}${tag.name}`);
      button.addEventListener("click", () => {
        if (selected.has(tag.id)) selected.delete(tag.id);
        else selected.add(tag.id);
        this.tagIds = [...selected];
        button.toggleClass("is-active", selected.has(tag.id));
        button.setText(`${selected.has(tag.id) ? "\u2713 " : ""}${tag.name}`);
      });
    });
  }
  legacyTagIds(project) {
    if (!(project == null ? void 0 : project.tags)) return [];
    return project.tags.map((tag) => {
      var _a;
      return (_a = this.store.getPaperTags().find((definition) => definition.name.toLowerCase() === tag.toLowerCase())) == null ? void 0 : _a.id;
    }).filter(Boolean);
  }
  async save() {
    if (!this.title.trim()) {
      new import_obsidian24.Notice("\u8BF7\u8F93\u5165\u7814\u7A76\u9879\u76EE\u6807\u9898\u3002");
      return;
    }
    const tags = this.tagIds.map((id) => {
      var _a;
      return (_a = this.store.getPaperTags().find((tag) => tag.id === id)) == null ? void 0 : _a.name;
    }).filter(Boolean);
    await this.onSubmit({
      title: this.title.trim(),
      description: this.description.trim(),
      status: this.status,
      progress: Math.max(0, Math.min(100, this.progress)),
      startDate: this.startDate,
      deadline: this.deadline,
      tags,
      tagIds: [...this.tagIds]
    });
    this.close();
  }
};
function openDataAnalysisTaskModal(app, onSubmit, task) {
  var _a, _b, _c;
  new CrudItemModal(app, task ? "\u7F16\u8F91\u6570\u636E\u5206\u6790\u4EFB\u52A1" : "\u65B0\u589E\u6570\u636E\u5206\u6790\u4EFB\u52A1", {
    title: (_a = task == null ? void 0 : task.title) != null ? _a : "",
    progress: (_b = task == null ? void 0 : task.progress) != null ? _b : 0,
    status: (_c = task == null ? void 0 : task.status) != null ? _c : "\u8FDB\u884C\u4E2D"
  }, [
    { key: "title", name: "\u6807\u9898" },
    { key: "progress", name: "\u8FDB\u5EA6", type: "number" },
    { key: "status", name: "\u72B6\u6001", type: "select", options: statusOptions(["\u672A\u5F00\u59CB", "\u8FDB\u884C\u4E2D", "\u5DF2\u5B8C\u6210"]) }
  ], onSubmit).open();
}
function openDeadlineModal(app, onSubmit, deadline) {
  var _a, _b, _c, _d;
  new CrudItemModal(app, deadline ? "\u7F16\u8F91\u4F1A\u8BAE / DDL" : "\u65B0\u589E\u4F1A\u8BAE / DDL", {
    title: (_a = deadline == null ? void 0 : deadline.title) != null ? _a : "",
    date: (_b = deadline == null ? void 0 : deadline.date) != null ? _b : formatDateKey(/* @__PURE__ */ new Date()),
    type: (_c = deadline == null ? void 0 : deadline.type) != null ? _c : "DDL",
    priority: (_d = deadline == null ? void 0 : deadline.priority) != null ? _d : "medium"
  }, [
    { key: "title", name: "\u6807\u9898" },
    { key: "date", name: "\u65E5\u671F" },
    { key: "type", name: "\u7C7B\u578B", type: "select", options: statusOptions(["\u4F1A\u8BAE", "DDL", "\u7EC4\u4F1A", "\u6C47\u62A5"]) },
    { key: "priority", name: "\u4F18\u5148\u7EA7", type: "select", options: [{ value: "low", label: "\u4F4E" }, { value: "medium", label: "\u4E2D" }, { value: "high", label: "\u9AD8" }] }
  ], onSubmit).open();
}
function openSavingGoalModal(app, onSubmit, goal) {
  var _a, _b, _c, _d;
  new CrudItemModal(app, goal ? "\u7F16\u8F91\u50A8\u84C4\u76EE\u6807" : "\u65B0\u589E\u50A8\u84C4\u76EE\u6807", {
    title: (_a = goal == null ? void 0 : goal.title) != null ? _a : "",
    current: (_b = goal == null ? void 0 : goal.current) != null ? _b : 0,
    target: (_c = goal == null ? void 0 : goal.target) != null ? _c : 0,
    deadline: (_d = goal == null ? void 0 : goal.deadline) != null ? _d : formatDateKey(/* @__PURE__ */ new Date())
  }, [
    { key: "title", name: "\u6807\u9898" },
    { key: "current", name: "\u5F53\u524D\u91D1\u989D", type: "number" },
    { key: "target", name: "\u76EE\u6807\u91D1\u989D", type: "number" },
    { key: "deadline", name: "\u622A\u6B62\u65E5\u671F" }
  ], onSubmit).open();
}
function openBillModal(app, onSubmit, bill) {
  var _a, _b, _c, _d;
  new CrudItemModal(app, bill ? "\u7F16\u8F91\u8D26\u5355" : "\u65B0\u589E\u8D26\u5355", {
    title: (_a = bill == null ? void 0 : bill.title) != null ? _a : "",
    amount: (_b = bill == null ? void 0 : bill.amount) != null ? _b : 0,
    dueDate: (_c = bill == null ? void 0 : bill.dueDate) != null ? _c : formatDateKey(/* @__PURE__ */ new Date()),
    paid: (_d = bill == null ? void 0 : bill.paid) != null ? _d : false
  }, [
    { key: "title", name: "\u6807\u9898" },
    { key: "amount", name: "\u91D1\u989D", type: "number" },
    { key: "dueDate", name: "\u5230\u671F\u65E5" },
    { key: "paid", name: "\u5DF2\u652F\u4ED8", type: "checkbox" }
  ], onSubmit).open();
}
function openObjectiveModal(app, onSubmit, objective) {
  var _a, _b, _c;
  new CrudItemModal(app, objective ? "\u7F16\u8F91\u5B63\u5EA6\u76EE\u6807" : "\u65B0\u589E\u5B63\u5EA6\u76EE\u6807", {
    title: (_a = objective == null ? void 0 : objective.title) != null ? _a : "",
    quarter: (_b = objective == null ? void 0 : objective.quarter) != null ? _b : "2026 Q4",
    progress: (_c = objective == null ? void 0 : objective.progress) != null ? _c : 0
  }, [
    { key: "title", name: "\u6807\u9898" },
    { key: "quarter", name: "\u5B63\u5EA6" },
    { key: "progress", name: "\u8FDB\u5EA6", type: "number" }
  ], onSubmit).open();
}
function statusOptions(values) {
  return values.map((value) => ({ value, label: value }));
}

// src/components/SectionActionMenu.ts
var CARD_COLORS = [
  { id: "default", label: "\u9ED8\u8BA4" },
  { id: "pink", label: "\u7C89\u8272" },
  { id: "cream", label: "\u5976\u6CB9\u9EC4" },
  { id: "mint", label: "\u8584\u8377\u7EFF" },
  { id: "sky", label: "\u5929\u84DD" },
  { id: "lavender", label: "\u6DE1\u7D2B" }
];
var WIDTHS = [
  { id: "sm", label: "\u5C0F" },
  { id: "md", label: "\u4E2D" },
  { id: "lg", label: "\u5927" },
  { id: "full", label: "\u6574\u884C" }
];
var SectionActionMenu = class {
  constructor(app, store, section, onRemove, onDataChanged) {
    this.app = app;
    this.store = store;
    this.section = section;
    this.onRemove = onRemove;
    this.onDataChanged = onDataChanged;
  }
  show(event) {
    const menu = new import_obsidian25.Menu();
    const capabilities = getSectionCapabilities(this.section.type);
    if (capabilities.canAdd) {
      menu.addItem((item) => item.setTitle("\u6DFB\u52A0\u5185\u5BB9").setIcon("plus").onClick(() => void this.addContent()));
    }
    if (capabilities.canManage) {
      menu.addItem((item) => item.setTitle("\u7F16\u8F91\u6570\u636E").setIcon("pencil").onClick(() => openManageContentModal(this.app, this.store, this.section, this.onDataChanged)));
    }
    menu.addItem((item) => item.setTitle("\u4FEE\u6539\u6846\u4F53\u989C\u8272").setIcon("palette").onClick(() => new SectionColorModal(this.app, this.store, this.section, this.onDataChanged).open()));
    menu.addItem((item) => item.setTitle("\u4FEE\u6539\u5BBD\u5EA6").setIcon("columns-3").onClick(() => new SectionWidthModal(this.app, this.store, this.section, this.onDataChanged).open()));
    menu.addSeparator();
    menu.addItem((item) => item.setTitle("\u4E0A\u79FB").setIcon("arrow-up").onClick(() => void this.move("up")));
    menu.addItem((item) => item.setTitle("\u4E0B\u79FB").setIcon("arrow-down").onClick(() => void this.move("down")));
    menu.addItem((item) => item.setTitle("\u9690\u85CF").setIcon("eye-off").onClick(() => void this.hide()));
    menu.addSeparator();
    menu.addItem((item) => item.setTitle("\u5220\u9664").setIcon("trash-2").setWarning(true).onClick(() => new ConfirmSectionDeleteModal(this.app, this.section, () => this.onRemove(this.section)).open()));
    menu.showAtMouseEvent(event);
  }
  async addContent() {
    openAddContentModal(this.app, this.store, this.section, this.onDataChanged);
  }
  async move(direction) {
    await this.store.moveSection(this.section.id, direction);
    this.onDataChanged();
  }
  async hide() {
    await this.store.setSectionEnabled(this.section.id, false);
    this.onDataChanged();
  }
};
var ConfirmSectionDeleteModal = class extends import_obsidian25.Modal {
  constructor(app, section, onConfirm) {
    super(app);
    this.section = section;
    this.onConfirm = onConfirm;
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: `\u5220\u9664\u201C${this.section.title}\u201D\u5206\u533A\uFF1F` });
    this.contentEl.createEl("p", { text: "\u53EA\u4F1A\u4ECE\u5F53\u524D\u5DE5\u4F5C\u53F0\u5E03\u5C40\u79FB\u9664\uFF0C\u4E0D\u4F1A\u5220\u9664\u4EFB\u4F55 Markdown \u6587\u4EF6\u6216\u4E1A\u52A1\u6570\u636E\u3002" });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    const remove = actions.createEl("button", { text: "\u5220\u9664", cls: "mod-warning", attr: { type: "button" } });
    remove.addEventListener("click", async () => {
      await this.onConfirm();
      this.close();
    });
  }
};
var SectionColorModal = class extends import_obsidian25.Modal {
  constructor(app, store, section, onDataChanged) {
    super(app);
    this.store = store;
    this.section = section;
    this.onDataChanged = onDataChanged;
  }
  onOpen() {
    var _a, _b;
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: "\u4FEE\u6539\u6846\u4F53\u989C\u8272" });
    const grid = this.contentEl.createDiv({ cls: "cow-choice-grid" });
    const current = String((_b = (_a = this.section.config) == null ? void 0 : _a.cardColor) != null ? _b : "default");
    CARD_COLORS.forEach((color) => {
      const button = grid.createEl("button", {
        cls: `cow-color-choice cow-card-color-${color.id} ${current === color.id ? "is-active" : ""}`,
        attr: { type: "button" }
      });
      button.createSpan({ text: color.label });
      button.addEventListener("click", async () => {
        await this.store.updateSectionConfig(this.section.id, { cardColor: color.id });
        this.onDataChanged();
        this.close();
      });
    });
  }
};
var SectionWidthModal = class extends import_obsidian25.Modal {
  constructor(app, store, section, onDataChanged) {
    super(app);
    this.store = store;
    this.section = section;
    this.onDataChanged = onDataChanged;
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: "\u4FEE\u6539\u5BBD\u5EA6" });
    const grid = this.contentEl.createDiv({ cls: "cow-choice-grid" });
    WIDTHS.forEach((width) => {
      const button = grid.createEl("button", {
        cls: this.section.width === width.id ? "is-active" : "",
        attr: { type: "button" }
      });
      (0, import_obsidian25.setIcon)(button.createSpan(), width.id === "full" ? "panel-top" : "columns-3");
      button.createSpan({ text: width.label });
      button.addEventListener("click", async () => {
        await this.store.updateSection(this.section.id, { width: width.id });
        this.onDataChanged();
        this.close();
      });
    });
  }
};

// src/components/overview/HabitStatisticsModal.ts
var import_obsidian26 = require("obsidian");

// src/services/StatisticsService.ts
var StatisticsService = class {
  constructor(store) {
    this.store = store;
    this.calendar = new CalendarService();
  }
  getFocusStats() {
    const today6 = this.calendar.getDateKey(/* @__PURE__ */ new Date());
    const weekKeys = new Set(this.store.getCurrentWeekDates());
    const monthPrefix = today6.slice(0, 7);
    const records = this.store.getFocusRecords();
    return {
      todayMinutes: records.filter((record) => record.date === today6).reduce((sum, record) => {
        var _a;
        return sum + ((_a = record.actualDurationMinutes) != null ? _a : record.duration);
      }, 0),
      todayPomodoros: records.filter((record) => record.date === today6 && record.completed).length,
      weekMinutes: records.filter((record) => weekKeys.has(record.date)).reduce((sum, record) => {
        var _a;
        return sum + ((_a = record.actualDurationMinutes) != null ? _a : record.duration);
      }, 0),
      monthMinutes: records.filter((record) => record.date.startsWith(monthPrefix)).reduce((sum, record) => {
        var _a;
        return sum + ((_a = record.actualDurationMinutes) != null ? _a : record.duration);
      }, 0),
      recentRecords: records.slice(0, 5)
    };
  }
  getHabitStats(month) {
    const monthDays = this.getHabitMonthDays(month);
    const year = month.getFullYear();
    const yearDays = this.getDaysBetween(new Date(year, 0, 1), new Date(year, 11, 31));
    return {
      currentStreak: this.getCurrentHabitStreak(),
      longestStreak: this.getLongestHabitStreak(year),
      monthCompletionRate: this.getCompletionRate(monthDays.map((day) => day.statuses)),
      yearCompletionRate: this.getCompletionRate(yearDays.map((date) => this.getHabitStatuses(date))),
      monthDays
    };
  }
  getMonthProgress(month) {
    const previous = this.calendar.addMonths(month, -1);
    return this.getProgressLabels().map((item) => {
      const current = this.getProgressValue(item.id, month);
      const previousValue = this.getProgressValue(item.id, previous);
      return {
        id: item.id,
        label: item.label,
        current,
        previous: previousValue,
        change: current - previousValue
      };
    });
  }
  getYearProgress(year) {
    return Array.from({ length: 12 }, (_, index) => {
      const month = new Date(year, index, 1);
      const items = this.getMonthProgress(month);
      const average = items.length === 0 ? 0 : Math.round(items.reduce((sum, item) => sum + item.current, 0) / items.length);
      return { month: index + 1, items, average };
    });
  }
  getHabitMonthDays(month) {
    const days = new Date(month.getFullYear(), month.getMonth() + 1, 0).getDate();
    return Array.from({ length: days }, (_, index) => {
      const date = new Date(month.getFullYear(), month.getMonth(), index + 1);
      const statuses = this.getHabitStatuses(date);
      return {
        date: this.calendar.getDateKey(date),
        completedCount: statuses.filter((status) => status.completed).length,
        totalCount: statuses.length,
        statuses
      };
    });
  }
  getHabitStatuses(date) {
    const dateKey = this.calendar.getDateKey(date);
    return this.store.getActiveCheckInDefinitions().map((habit) => ({
      id: habit.id,
      label: habit.title,
      completed: this.store.isCheckInCompleted(habit.id, dateKey)
    }));
  }
  getCurrentHabitStreak() {
    const today6 = /* @__PURE__ */ new Date();
    let streak = 0;
    for (let offset = 0; offset < 366; offset += 1) {
      const date = new Date(today6);
      date.setDate(today6.getDate() - offset);
      if (!this.isAllHabitsDone(date)) break;
      streak += 1;
    }
    return streak;
  }
  getLongestHabitStreak(year) {
    let longest = 0;
    let current = 0;
    this.getDaysBetween(new Date(year, 0, 1), new Date(year, 11, 31)).forEach((date) => {
      if (this.isAllHabitsDone(date)) {
        current += 1;
        longest = Math.max(longest, current);
      } else {
        current = 0;
      }
    });
    return longest;
  }
  isAllHabitsDone(date) {
    const statuses = this.getHabitStatuses(date);
    return statuses.length > 0 && statuses.every((status) => status.completed);
  }
  getCompletionRate(days) {
    const statuses = [];
    days.forEach((day) => {
      day.forEach((status) => statuses.push(status));
    });
    if (statuses.length === 0) return 0;
    return Math.round(statuses.filter((status) => status.completed).length / statuses.length * 100);
  }
  getDaysBetween(start, end) {
    const days = [];
    const cursor = new Date(start);
    while (cursor <= end) {
      days.push(new Date(cursor));
      cursor.setDate(cursor.getDate() + 1);
    }
    return days;
  }
  getProgressLabels() {
    return [
      { id: "research", label: "\u79D1\u7814" },
      { id: "reading", label: "\u9605\u8BFB" },
      { id: "fitness", label: "\u5065\u8EAB" },
      { id: "finance", label: "\u7406\u8D22" },
      { id: "goals", label: "\u76EE\u6807\u7BA1\u7406" }
    ];
  }
  getProgressValue(id, month) {
    switch (id) {
      case "research":
        return this.average(this.store.getResearchProjects().map((item) => item.progress));
      case "reading":
        return this.average(this.store.getBooks().map((book) => book.totalPages === 0 ? 0 : Math.round(book.currentPage / book.totalPages * 100)));
      case "fitness":
        return this.getFitnessProgress(month);
      case "finance":
        return this.getFinanceProgress();
      case "goals":
        return this.average(this.store.getGoals().map((goal) => goal.progress));
    }
  }
  getFitnessProgress(month) {
    const prefix = `${month.getFullYear()}-${String(month.getMonth() + 1).padStart(2, "0")}`;
    const workouts = this.store.getWorkouts().filter((workout) => workout.date.startsWith(prefix));
    if (workouts.length === 0) return 0;
    return Math.round(workouts.filter((workout) => workout.completed).length / workouts.length * 100);
  }
  getFinanceProgress() {
    const budgets = this.store.getBudgets();
    if (budgets.length === 0) return 0;
    return this.clamp(Math.round(this.average(budgets.map((budget) => budget.amount === 0 ? 100 : (budget.amount - budget.spent) / budget.amount * 100))));
  }
  average(values) {
    if (values.length === 0) return 0;
    return this.clamp(Math.round(values.reduce((sum, value) => sum + value, 0) / values.length));
  }
  clamp(value) {
    return Math.max(0, Math.min(100, value));
  }
};

// src/components/overview/HabitStatisticsModal.ts
var HabitStatisticsModal = class extends import_obsidian26.Modal {
  constructor(app, store) {
    super(app);
    this.calendar = new CalendarService();
    this.month = /* @__PURE__ */ new Date();
    this.statistics = new StatisticsService(store);
  }
  onOpen() {
    this.render();
  }
  render() {
    var _a, _b;
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    const stats = this.statistics.getHabitStats(this.month);
    this.selectedDay = (_a = this.selectedDay) != null ? _a : stats.monthDays.find((day2) => day2.date === this.calendar.getDateKey(/* @__PURE__ */ new Date()));
    const header = this.contentEl.createDiv({ cls: "cow-stats-modal-header" });
    const previous = header.createEl("button", { attr: { type: "button", "aria-label": "\u4E0A\u4E00\u6708" } });
    (0, import_obsidian26.setIcon)(previous, "chevron-left");
    previous.addEventListener("click", () => {
      this.month = this.calendar.addMonths(this.month, -1);
      this.selectedDay = void 0;
      this.render();
    });
    header.createEl("h2", { text: `\u6253\u5361\u7EDF\u8BA1 \xB7 ${this.calendar.getMonthTitle(this.month)}` });
    const next = header.createEl("button", { attr: { type: "button", "aria-label": "\u4E0B\u4E00\u6708" } });
    (0, import_obsidian26.setIcon)(next, "chevron-right");
    next.addEventListener("click", () => {
      this.month = this.calendar.addMonths(this.month, 1);
      this.selectedDay = void 0;
      this.render();
    });
    const cards = this.contentEl.createDiv({ cls: "cow-stats-card-grid" });
    [
      ["\u8FDE\u7EED\u6253\u5361", `${stats.currentStreak} \u5929`],
      ["\u6700\u957F\u8FDE\u7EED", `${stats.longestStreak} \u5929`],
      ["\u672C\u6708\u5B8C\u6210\u7387", `${stats.monthCompletionRate}%`],
      ["\u5168\u5E74\u5B8C\u6210\u7387", `${stats.yearCompletionRate}%`]
    ].forEach(([label, value]) => {
      const card = cards.createDiv({ cls: "cow-stats-card" });
      card.createEl("strong", { text: value });
      card.createSpan({ text: label });
    });
    const heatmap = this.contentEl.createDiv({ cls: "cow-habit-stats-heatmap" });
    stats.monthDays.forEach((day2) => {
      var _a2;
      const level = day2.totalCount === 0 ? 0 : Math.ceil(day2.completedCount / day2.totalCount * 4);
      const button = heatmap.createEl("button", {
        cls: `level-${level} ${((_a2 = this.selectedDay) == null ? void 0 : _a2.date) === day2.date ? "is-selected" : ""}`,
        text: day2.date.slice(-2),
        attr: { type: "button", "aria-label": day2.date }
      });
      button.addEventListener("click", () => {
        this.selectedDay = day2;
        this.render();
      });
    });
    const detail = this.contentEl.createDiv({ cls: "cow-habit-day-detail" });
    detail.createEl("h3", { text: this.selectedDay ? `\u5F53\u5929\u5B8C\u6210\u60C5\u51B5 \xB7 ${this.selectedDay.date}` : "\u5F53\u5929\u5B8C\u6210\u60C5\u51B5" });
    const day = (_b = this.selectedDay) != null ? _b : stats.monthDays[0];
    if (!day) return;
    day.statuses.forEach((status) => {
      const row = detail.createDiv({ cls: "cow-habit-day-row" });
      row.createSpan({ text: status.label });
      row.createSpan({ cls: status.completed ? "is-done" : "", text: status.completed ? "\u5DF2\u5B8C\u6210" : "\u672A\u5B8C\u6210" });
    });
  }
};

// src/components/overview/MonthlyProgressStatisticsModal.ts
var import_obsidian27 = require("obsidian");
var MonthlyProgressStatisticsModal = class extends import_obsidian27.Modal {
  constructor(app, store) {
    super(app);
    this.calendar = new CalendarService();
    this.month = /* @__PURE__ */ new Date();
    this.statistics = new StatisticsService(store);
  }
  onOpen() {
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    const progress = this.statistics.getMonthProgress(this.month);
    const header = this.contentEl.createDiv({ cls: "cow-stats-modal-header" });
    const previous = header.createEl("button", { attr: { type: "button", "aria-label": "\u4E0A\u4E00\u6708" } });
    (0, import_obsidian27.setIcon)(previous, "chevron-left");
    previous.addEventListener("click", () => {
      this.month = this.calendar.addMonths(this.month, -1);
      this.render();
    });
    header.createEl("h2", { text: `\u672C\u6708\u8FDB\u5EA6\u7EDF\u8BA1 \xB7 ${this.calendar.getMonthTitle(this.month)}` });
    const next = header.createEl("button", { attr: { type: "button", "aria-label": "\u4E0B\u4E00\u6708" } });
    (0, import_obsidian27.setIcon)(next, "chevron-right");
    next.addEventListener("click", () => {
      this.month = this.calendar.addMonths(this.month, 1);
      this.render();
    });
    const table = this.contentEl.createDiv({ cls: "cow-progress-stats-table" });
    const tableHeader = table.createDiv({ cls: "cow-progress-stats-row is-header" });
    ["\u6A21\u5757", "\u672C\u6708", "\u4E0A\u6708", "\u53D8\u5316\u503C"].forEach((label) => tableHeader.createSpan({ text: label }));
    progress.forEach((item) => {
      const row = table.createDiv({ cls: "cow-progress-stats-row" });
      row.createSpan({ text: item.label });
      row.createSpan({ text: `${item.current}%` });
      row.createSpan({ text: `${item.previous}%` });
      row.createSpan({ cls: item.change >= 0 ? "is-up" : "is-down", text: `${item.change >= 0 ? "+" : ""}${item.change}%` });
    });
    this.contentEl.createEl("h3", { text: "1\uFF5E12\u6708\u5168\u5E74\u603B\u89C8" });
    const yearGrid = this.contentEl.createDiv({ cls: "cow-year-progress-grid" });
    this.statistics.getYearProgress(this.month.getFullYear()).forEach((month) => {
      const card = yearGrid.createDiv({ cls: "cow-year-progress-card" });
      card.createEl("strong", { text: `${month.month}\u6708` });
      const track = card.createDiv({ cls: "cow-month-progress-track" });
      track.createDiv({ cls: "cow-month-progress-fill is-purple", attr: { style: `width: ${month.average}%` } });
      card.createSpan({ text: `${month.average}%` });
    });
  }
};

// src/components/overview/ContributionHeatmapSection.ts
var ContributionHeatmapSection = class {
  constructor(app) {
    this.app = app;
  }
  render(container) {
    var _a;
    const root = container.createDiv({ cls: "cow-contribution" });
    const counts = this.getDailyCreateCounts();
    for (let month = 0; month < 12; month += 1) {
      const monthEl = root.createDiv({ cls: "cow-contribution-month" });
      monthEl.createEl("strong", { text: `${month + 1}\u6708` });
      const grid = monthEl.createDiv({ cls: "cow-contribution-grid" });
      const days = new Date((/* @__PURE__ */ new Date()).getFullYear(), month + 1, 0).getDate();
      for (let day = 1; day <= days; day += 1) {
        const key = `${(/* @__PURE__ */ new Date()).getFullYear()}-${String(month + 1).padStart(2, "0")}-${String(day).padStart(2, "0")}`;
        const count = (_a = counts.get(key)) != null ? _a : 0;
        grid.createSpan({ cls: `level-${Math.min(4, count)}` });
      }
    }
  }
  getDailyCreateCounts() {
    const counts = /* @__PURE__ */ new Map();
    this.app.vault.getMarkdownFiles().forEach((file) => {
      var _a;
      const date = new Date(file.stat.ctime);
      const key = `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, "0")}-${String(date.getDate()).padStart(2, "0")}`;
      counts.set(key, ((_a = counts.get(key)) != null ? _a : 0) + 1);
    });
    return counts;
  }
};

// src/services/CheckInService.ts
var CheckInService = class {
  constructor(store) {
    this.store = store;
  }
  getDefinitions(moduleId) {
    return this.store.getActiveCheckInDefinitions(moduleId);
  }
  getAllDefinitions() {
    return this.store.getActiveCheckInDefinitions();
  }
  getTodayStatus(checkInId) {
    return this.isCompleted(checkInId, formatDateKey(/* @__PURE__ */ new Date()));
  }
  isCompleted(checkInId, date) {
    return this.store.isCheckInCompleted(checkInId, date);
  }
  async toggleCheckIn(checkInId, date) {
    return this.store.toggleCheckIn(checkInId, date);
  }
  getRecords(checkInId) {
    return this.store.getCheckInRecords(checkInId);
  }
  getRecordsByDate(date) {
    return this.store.getCheckInRecordsByDate(date);
  }
};

// src/components/checkin/CheckInSection.ts
var MODULE_ORDER = ["overview", "research", "reading", "fitness", "finance", "goals"];
var WEEK_LABELS = ["\u4E00", "\u4E8C", "\u4E09", "\u56DB", "\u4E94", "\u516D", "\u65E5"];
var CheckInSection = class {
  constructor(store, moduleId, onDataChanged) {
    this.store = store;
    this.moduleId = moduleId;
    this.onDataChanged = onDataChanged;
    this.weekStart = this.getCurrentWeekStart();
    this.followCurrentWeek = true;
    this.service = new CheckInService(store);
  }
  render(container) {
    const host = container.createDiv({ cls: `cow-checkin-section ${this.moduleId === "all" ? "is-overview-compact" : ""}` });
    host.addEventListener("cow-checkin-date-change", () => {
      if (this.followCurrentWeek) this.weekStart = this.getCurrentWeekStart();
      this.rerenderSection(host);
    });
    this.renderWeekNav(host);
    if (this.moduleId === "all") {
      this.renderOverview(host);
      return;
    }
    this.renderModule(host, this.moduleId);
  }
  renderOverview(container) {
    const definitions = this.service.getAllDefinitions();
    if (definitions.length === 0) {
      container.createDiv({ cls: "cow-empty-state", text: "\u8FD8\u6CA1\u6709\u542F\u7528\u7684\u6253\u5361\u9879\u76EE\u3002" });
      return;
    }
    const rendered = /* @__PURE__ */ new Set();
    MODULE_ORDER.forEach((moduleId) => {
      const group = definitions.filter((definition) => definition.moduleId === moduleId);
      if (group.length === 0) return;
      rendered.add(moduleId);
      const section = container.createDiv({ cls: "cow-checkin-module-group" });
      section.createEl("h4", { text: moduleId === "overview" ? "\u901A\u7528" : PAGE_LABELS[moduleId] });
      this.renderWeeklyTable(section, group);
    });
    const extra = definitions.filter((definition) => !rendered.has(definition.moduleId));
    if (extra.length > 0) {
      const section = container.createDiv({ cls: "cow-checkin-module-group" });
      section.createEl("h4", { text: "\u5176\u5B83" });
      this.renderWeeklyTable(section, extra);
    }
  }
  renderModule(container, moduleId) {
    const definitions = this.service.getDefinitions(moduleId);
    if (definitions.length === 0) {
      container.createDiv({ cls: "cow-empty-state", text: "\u8FD8\u6CA1\u6709\u542F\u7528\u7684\u6253\u5361\u9879\u76EE\uFF0C\u53EF\u70B9\u51FB\u53F3\u4E0A\u89D2\u81EA\u5B9A\u4E49\u6DFB\u52A0\u3002" });
      return;
    }
    this.renderWeeklyTable(container, definitions);
  }
  renderWeekNav(container) {
    const dates = this.getWeekDates();
    const nav = container.createDiv({ cls: "cow-checkin-week-nav" });
    nav.createEl("button", { text: "\u2039", attr: { type: "button", "aria-label": "\u4E0A\u4E00\u5468" } }).addEventListener("click", () => {
      this.followCurrentWeek = false;
      this.weekStart.setDate(this.weekStart.getDate() - 7);
      this.rerenderSection(container);
    });
    nav.createEl("strong", { text: `\u672C\u5468 ${this.shortDate(dates[0])} - ${this.shortDate(dates[6])}` });
    nav.createEl("button", { text: "\u56DE\u5230\u672C\u5468", attr: { type: "button" } }).addEventListener("click", () => {
      this.followCurrentWeek = true;
      this.weekStart = this.getCurrentWeekStart();
      this.rerenderSection(container);
    });
    nav.createEl("button", { text: "\u203A", attr: { type: "button", "aria-label": "\u4E0B\u4E00\u5468" } }).addEventListener("click", () => {
      this.followCurrentWeek = false;
      this.weekStart.setDate(this.weekStart.getDate() + 7);
      this.rerenderSection(container);
    });
  }
  renderWeeklyTable(container, definitions) {
    const dates = this.getWeekDates();
    const table = container.createDiv({ cls: "cow-checkin-week-table" });
    table.createDiv({ cls: "cow-checkin-week-corner", text: "\u6253\u5361\u9879\u76EE" });
    dates.forEach((date, index) => {
      const key = formatDateKey(date);
      const header = table.createDiv({ cls: `cow-checkin-week-day ${key === formatDateKey(/* @__PURE__ */ new Date()) ? "is-today" : ""}` });
      header.createSpan({ text: WEEK_LABELS[index] });
      header.createEl("strong", { text: String(date.getDate()) });
    });
    definitions.forEach((definition) => this.renderWeeklyRow(table, definition, dates));
    this.renderSummary(container, definitions, dates.map((date) => formatDateKey(date)));
  }
  renderWeeklyRow(table, definition, dates) {
    var _a;
    const label = table.createDiv({ cls: "cow-checkin-week-name", attr: { title: definition.title } });
    label.style.setProperty("--checkin-color", (_a = definition.color) != null ? _a : "#ff8fbc");
    label.createSpan({ text: definition.title });
    dates.forEach((date) => {
      var _a2;
      const key = formatDateKey(date);
      const status = this.getCellStatus(definition, key);
      const cell = table.createEl("button", {
        cls: `cow-checkin-week-cell is-${status}`,
        attr: { type: "button", title: `${key}
${definition.title}
${this.statusLabel(status)}`, "aria-label": `${definition.title} ${key} ${this.statusLabel(status)}` }
      });
      cell.style.setProperty("--checkin-color", (_a2 = definition.color) != null ? _a2 : "#ff8fbc");
      cell.setText(this.statusMark(status));
      const clickable = key === formatDateKey(/* @__PURE__ */ new Date()) && status !== "future" && status !== "na";
      cell.disabled = !clickable;
      if (clickable) {
        cell.addEventListener("click", async () => {
          await this.service.toggleCheckIn(definition.id, key);
          this.rerenderSection(table.closest(".cow-checkin-section"));
        });
      }
    });
  }
  renderSummary(container, definitions, dates) {
    let total = 0;
    let completed = 0;
    dates.forEach((date) => {
      definitions.forEach((definition) => {
        const status = this.getCellStatus(definition, date);
        if (status === "completed") {
          total += 1;
          completed += 1;
        } else if (status === "missed" || status === "today") {
          total += 1;
        }
      });
    });
    container.createDiv({ cls: "cow-checkin-week-summary", text: `\u672C\u5468\u5B8C\u6210\uFF1A${completed} / ${total} \xB7 \u5B8C\u6210\u7387\uFF1A${total ? Math.round(completed / total * 100) : 0}%` });
  }
  getCellStatus(definition, date) {
    const today6 = formatDateKey(/* @__PURE__ */ new Date());
    if (date > today6) return "future";
    if (!this.isDefinitionApplicable(definition, date)) return "na";
    if (this.service.isCompleted(definition.id, date)) return "completed";
    if (date === today6) return "today";
    return "missed";
  }
  isDefinitionApplicable(definition, date) {
    const activeFrom = formatDateKey(new Date(definition.createdAt));
    if (date < activeFrom) return false;
    if (definition.inactiveFrom && date >= definition.inactiveFrom) return false;
    return true;
  }
  statusMark(status) {
    if (status === "completed") return "\u2713";
    if (status === "missed") return "\xD7";
    if (status === "today") return "\u25CB";
    if (status === "na") return "\u2014";
    return "";
  }
  statusLabel(status) {
    return {
      completed: "\u5DF2\u5B8C\u6210",
      missed: "\u6F0F\u6253",
      today: "\u4ECA\u65E5\u672A\u5B8C\u6210",
      future: "\u672A\u6765",
      na: "\u4E0D\u9002\u7528"
    }[status];
  }
  getWeekDates() {
    return Array.from({ length: 7 }, (_, index) => {
      const date = new Date(this.weekStart);
      date.setDate(this.weekStart.getDate() + index);
      return date;
    });
  }
  getCurrentWeekStart() {
    const today6 = /* @__PURE__ */ new Date();
    const monday = new Date(today6.getFullYear(), today6.getMonth(), today6.getDate());
    monday.setDate(today6.getDate() - (today6.getDay() + 6) % 7);
    return monday;
  }
  shortDate(date) {
    return `${date.getMonth() + 1}/${date.getDate()}`;
  }
  rerenderSection(host) {
    if (!host) {
      this.onDataChanged();
      return;
    }
    host.empty();
    this.renderWeekNav(host);
    if (this.moduleId === "all") this.renderOverview(host);
    else this.renderModule(host, this.moduleId);
  }
};

// src/components/checkin/CheckInModals.ts
var import_obsidian28 = require("obsidian");
var COLORS = ["#ff8fbc", "#ffd166", "#7bd88f", "#9ecbff", "#c9a7ff", "#ffb4a2"];
function setup(modal, className) {
  applyResizableModal(modal, {
    className,
    width: "min(780px, 92vw)",
    height: "min(680px, 86vh)",
    maxWidth: "96vw",
    maxHeight: "92vh",
    minWidth: "min(380px, 92vw)",
    minHeight: "min(320px, 82vh)"
  });
}
function openCheckInManagerModal(app, store, moduleId, onDone) {
  new CheckInManagerModal(app, store, moduleId, onDone).open();
}
function openCheckInHistoryModal(app, store, moduleId) {
  new CheckInHistoryModal(app, store, moduleId).open();
}
var CheckInManagerModal = class extends import_obsidian28.Modal {
  constructor(app, store, moduleId, onDone) {
    super(app);
    this.store = store;
    this.moduleId = moduleId;
    this.onDone = onDone;
  }
  onOpen() {
    setup(this, "cute-checkin-manager-modal");
    this.render();
  }
  render() {
    var _a;
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-checkin-modal");
    this.contentEl.createEl("h2", { text: `${(_a = PAGE_LABELS[this.moduleId]) != null ? _a : "\u6253\u5361"} \xB7 \u81EA\u5B9A\u4E49\u6253\u5361` });
    const list = this.contentEl.createDiv({ cls: "cow-checkin-manager-list" });
    const definitions = this.store.getCheckInDefinitions(this.moduleId, false);
    if (definitions.length === 0) {
      list.createDiv({ cls: "cow-empty-state", text: "\u8FD8\u6CA1\u6709\u6253\u5361\u9879\u76EE\u3002" });
    }
    definitions.forEach((definition, index) => this.renderDefinition(list, definition, index, definitions.length));
    const add = this.contentEl.createDiv({ cls: "cow-checkin-add-row" });
    const title = add.createEl("input", { attr: { type: "text", placeholder: "\u65B0\u589E\u6253\u5361\u9879\u76EE\u540D\u79F0" } });
    const icon = add.createEl("input", { attr: { type: "text", placeholder: "\u56FE\u6807\uFF0C\u5982 book-open" } });
    const button = add.createEl("button", { text: "\u65B0\u589E", cls: "mod-cta", attr: { type: "button" } });
    button.addEventListener("click", async () => {
      const name = title.value.trim();
      if (!name) {
        new import_obsidian28.Notice("\u8BF7\u8F93\u5165\u6253\u5361\u9879\u76EE\u540D\u79F0\u3002");
        return;
      }
      await this.store.addCheckInDefinition({
        moduleId: this.moduleId,
        title: name,
        icon: icon.value.trim() || "circle",
        color: COLORS[definitions.length % COLORS.length]
      });
      this.onDone();
      this.render();
    });
  }
  renderDefinition(container, definition, index, total) {
    var _a, _b;
    const row = container.createDiv({ cls: "cow-checkin-manager-row" });
    const handle = row.createSpan({ cls: "cow-checkin-manager-icon" });
    (0, import_obsidian28.setIcon)(handle, definition.icon || "circle");
    const title = row.createEl("input", { attr: { type: "text", value: definition.title } });
    const icon = row.createEl("input", { attr: { type: "text", value: (_a = definition.icon) != null ? _a : "circle", placeholder: "icon" } });
    const color = row.createEl("input", { attr: { type: "color", value: (_b = definition.color) != null ? _b : "#ff8fbc", title: "\u989C\u8272" } });
    const enabled = row.createEl("input", { attr: { type: "checkbox", title: "\u542F\u7528" } });
    enabled.checked = definition.enabled;
    const save = row.createEl("button", { text: "\u4FDD\u5B58", attr: { type: "button" } });
    save.addEventListener("click", async () => {
      await this.store.updateCheckInDefinition(definition.id, {
        title: title.value.trim() || definition.title,
        icon: icon.value.trim() || "circle",
        color: color.value,
        enabled: enabled.checked
      });
      this.onDone();
      this.render();
    });
    const up = row.createEl("button", { text: "\u4E0A\u79FB", attr: { type: "button" } });
    up.disabled = index === 0;
    up.addEventListener("click", () => void this.move(definition, -1));
    const down = row.createEl("button", { text: "\u4E0B\u79FB", attr: { type: "button" } });
    down.disabled = index === total - 1;
    down.addEventListener("click", () => void this.move(definition, 1));
    row.createEl("button", { text: "\u5220\u9664", cls: "mod-warning", attr: { type: "button" } }).addEventListener("click", async () => {
      if (!confirm(`\u5220\u9664\u201C${definition.title}\u201D\u6253\u5361\u9879\u76EE\uFF1F\u5386\u53F2\u8BB0\u5F55\u4F1A\u4FDD\u7559\u3002`)) return;
      await this.store.archiveCheckInDefinition(definition.id);
      this.onDone();
      this.render();
    });
  }
  async move(definition, direction) {
    const definitions = this.store.getCheckInDefinitions(this.moduleId);
    const index = definitions.findIndex((item) => item.id === definition.id);
    const target = definitions[index + direction];
    if (!target) return;
    const ordered = [...definitions];
    ordered[index] = target;
    ordered[index + direction] = definition;
    await this.store.reorderCheckInDefinitions(this.moduleId, ordered.map((item) => item.id));
    this.onDone();
    this.render();
  }
};
var CheckInHistoryModal = class extends import_obsidian28.Modal {
  constructor(app, store, moduleId) {
    super(app);
    this.store = store;
    this.moduleId = moduleId;
    this.month = /* @__PURE__ */ new Date();
    this.selectedModule = "all";
    this.selectedDefinitionId = "all";
    this.selectedModule = moduleId && moduleId !== "overview" ? moduleId : "all";
  }
  onOpen() {
    setup(this, "cute-checkin-history-modal");
    this.timer = window.setInterval(() => this.render(), 6e4);
    this.render();
  }
  onClose() {
    if (this.timer) {
      window.clearInterval(this.timer);
      this.timer = void 0;
    }
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-checkin-modal");
    const definitions = this.getFilteredDefinitions();
    const header = this.contentEl.createDiv({ cls: "cow-checkin-history-header" });
    const prev = header.createEl("button", { attr: { type: "button", "aria-label": "\u4E0A\u4E00\u6708" } });
    (0, import_obsidian28.setIcon)(prev, "chevron-left");
    prev.addEventListener("click", () => {
      this.month = new Date(this.month.getFullYear(), this.month.getMonth() - 1, 1);
      this.render();
    });
    header.createEl("h2", { text: `${this.month.getFullYear()}\u5E74${this.month.getMonth() + 1}\u6708\u6253\u5361\u5386\u53F2` });
    const next = header.createEl("button", { attr: { type: "button", "aria-label": "\u4E0B\u4E00\u6708" } });
    (0, import_obsidian28.setIcon)(next, "chevron-right");
    next.addEventListener("click", () => {
      this.month = new Date(this.month.getFullYear(), this.month.getMonth() + 1, 1);
      this.render();
    });
    this.renderFilters();
    this.renderSummary(definitions);
    this.renderCalendar(definitions);
  }
  renderFilters() {
    const filters = this.contentEl.createDiv({ cls: "cow-checkin-history-filters" });
    const moduleSelect = filters.createEl("select");
    moduleSelect.createEl("option", { value: "all", text: "\u5168\u90E8\u6A21\u5757" });
    const modules = [...new Set(this.store.getCheckInDefinitions(void 0, true).map((item) => item.moduleId))];
    modules.forEach((moduleId) => {
      var _a;
      return moduleSelect.createEl("option", { value: moduleId, text: (_a = PAGE_LABELS[moduleId]) != null ? _a : moduleId });
    });
    moduleSelect.value = this.selectedModule;
    if (this.moduleId && this.moduleId !== "overview") moduleSelect.disabled = true;
    moduleSelect.addEventListener("change", () => {
      this.selectedModule = moduleSelect.value;
      this.selectedDefinitionId = "all";
      this.render();
    });
    const itemSelect = filters.createEl("select");
    itemSelect.createEl("option", { value: "all", text: "\u5168\u90E8\u6253\u5361\u9879\u76EE" });
    this.getModuleScopedDefinitions().forEach((definition) => itemSelect.createEl("option", { value: definition.id, text: definition.title }));
    itemSelect.value = this.selectedDefinitionId;
    itemSelect.addEventListener("change", () => {
      this.selectedDefinitionId = itemSelect.value;
      this.render();
    });
  }
  renderSummary(definitions) {
    const days = this.getMonthDateKeys();
    const statuses = [];
    days.forEach((date) => {
      definitions.forEach((definition) => statuses.push(this.getDayStatus(definition, date)));
    });
    const applicable = statuses.filter((status) => status !== "future" && status !== "na");
    const completed = statuses.filter((status) => status === "completed").length;
    const missed = statuses.filter((status) => status === "missed").length;
    const cards = this.contentEl.createDiv({ cls: "cow-stats-card-grid" });
    [
      ["\u672C\u6708\u6253\u5361", `${completed} / ${applicable.length}`],
      ["\u5B8C\u6210\u7387", `${applicable.length ? Math.round(completed / applicable.length * 100) : 0}%`],
      ["\u6F0F\u6253", missed],
      ["\u8FDE\u7EED\u6253\u5361", `${this.getCurrentStreak(definitions)}\u5929`]
    ].forEach(([label, value]) => {
      const card = cards.createDiv({ cls: "cow-stats-card" });
      card.createEl("strong", { text: String(value) });
      card.createSpan({ text: String(label) });
    });
  }
  renderCalendar(definitions) {
    const grid = this.contentEl.createDiv({ cls: "cow-checkin-history-calendar" });
    ["\u4E00", "\u4E8C", "\u4E09", "\u56DB", "\u4E94", "\u516D", "\u65E5"].forEach((day) => grid.createDiv({ cls: "cow-checkin-history-weekday", text: day }));
    const first = new Date(this.month.getFullYear(), this.month.getMonth(), 1);
    const leading = (first.getDay() + 6) % 7;
    for (let index = 0; index < leading; index += 1) grid.createDiv({ cls: "cow-checkin-history-day is-empty" });
    this.getMonthDateKeys().forEach((date) => {
      const day = grid.createDiv({ cls: "cow-checkin-history-day" });
      day.createEl("strong", { text: date.slice(-2) });
      const dots = day.createDiv({ cls: "cow-checkin-history-dots" });
      definitions.forEach((definition) => {
        const status = this.getDayStatus(definition, date);
        const dot = dots.createSpan({ cls: `cow-checkin-history-dot is-${status}`, attr: { title: `${definition.title} \xB7 ${this.statusLabel(status)}` } });
        if (status === "completed") dot.setText("\u2713");
        if (status === "missed") dot.setText("\xD7");
        if (status === "today") dot.setText("\u25CB");
      });
    });
  }
  getModuleScopedDefinitions() {
    const all = this.store.getCheckInDefinitions(void 0, true);
    if (this.moduleId && this.moduleId !== "overview") return all.filter((definition) => definition.moduleId === this.moduleId);
    if (this.selectedModule !== "all") return all.filter((definition) => definition.moduleId === this.selectedModule);
    return all;
  }
  getFilteredDefinitions() {
    return this.getModuleScopedDefinitions().filter((definition) => this.selectedDefinitionId === "all" || definition.id === this.selectedDefinitionId);
  }
  getMonthDateKeys() {
    const days = new Date(this.month.getFullYear(), this.month.getMonth() + 1, 0).getDate();
    return Array.from({ length: days }, (_, index) => this.toDateKey(new Date(this.month.getFullYear(), this.month.getMonth(), index + 1)));
  }
  getDayStatus(definition, date) {
    const today6 = this.toDateKey(/* @__PURE__ */ new Date());
    if (date > today6) return "future";
    if (!this.isDefinitionApplicable(definition, date)) return "na";
    if (this.store.isCheckInCompleted(definition.id, date)) return "completed";
    if (date === today6) return "today";
    return "missed";
  }
  isDefinitionApplicable(definition, date) {
    const activeFrom = this.toDateKey(new Date(definition.createdAt));
    if (date < activeFrom) return false;
    if (definition.inactiveFrom && date >= definition.inactiveFrom) return false;
    return true;
  }
  getCurrentStreak(definitions) {
    let streak = 0;
    const today6 = /* @__PURE__ */ new Date();
    for (let offset = 0; offset < 366; offset += 1) {
      const date = new Date(today6);
      date.setDate(today6.getDate() - offset);
      const key = this.toDateKey(date);
      const applicable = definitions.filter((definition) => this.isDefinitionApplicable(definition, key));
      if (applicable.length === 0) continue;
      if (!applicable.every((definition) => this.store.isCheckInCompleted(definition.id, key))) break;
      streak += 1;
    }
    return streak;
  }
  statusLabel(status) {
    return {
      completed: "\u5DF2\u5B8C\u6210",
      missed: "\u6F0F\u6253",
      today: "\u4ECA\u5929\u672A\u5B8C\u6210",
      future: "\u672A\u6765",
      na: "\u4E0D\u9002\u7528"
    }[status];
  }
  toDateKey(date) {
    const year = date.getFullYear();
    const month = String(date.getMonth() + 1).padStart(2, "0");
    const day = String(date.getDate()).padStart(2, "0");
    return `${year}-${month}-${day}`;
  }
};

// src/components/overview/MonthlyCalendarSection.ts
var import_obsidian30 = require("obsidian");

// src/services/HolidayService.ts
var FIXED_HOLIDAYS = {
  "01-01": [
    { name: "\u5143\u65E6", region: "CN" },
    { name: "New Year's Day", region: "Global" }
  ],
  "02-14": [{ name: "Valentine's Day", region: "Global" }],
  "04-01": [{ name: "April Fools' Day", region: "Global" }],
  "05-01": [{ name: "\u52B3\u52A8\u8282", region: "CN" }],
  "09-10": [{ name: "\u6559\u5E08\u8282", region: "CN" }],
  "10-01": [{ name: "\u56FD\u5E86\u8282", region: "CN" }],
  "10-31": [{ name: "Halloween", region: "Global" }],
  "12-25": [{ name: "Christmas", region: "Global" }]
};
var CHINESE_LUNAR_HOLIDAYS = {
  2024: {
    "02-10": "\u6625\u8282",
    "02-24": "\u5143\u5BB5\u8282",
    "04-04": "\u6E05\u660E\u8282",
    "06-10": "\u7AEF\u5348\u8282",
    "09-17": "\u4E2D\u79CB\u8282"
  },
  2025: {
    "01-29": "\u6625\u8282",
    "02-12": "\u5143\u5BB5\u8282",
    "04-04": "\u6E05\u660E\u8282",
    "05-31": "\u7AEF\u5348\u8282",
    "10-06": "\u4E2D\u79CB\u8282"
  },
  2026: {
    "02-17": "\u6625\u8282",
    "03-03": "\u5143\u5BB5\u8282",
    "04-05": "\u6E05\u660E\u8282",
    "06-19": "\u7AEF\u5348\u8282",
    "09-25": "\u4E2D\u79CB\u8282"
  },
  2027: {
    "02-06": "\u6625\u8282",
    "02-20": "\u5143\u5BB5\u8282",
    "04-05": "\u6E05\u660E\u8282",
    "06-09": "\u7AEF\u5348\u8282",
    "09-15": "\u4E2D\u79CB\u8282"
  },
  2028: {
    "01-26": "\u6625\u8282",
    "02-09": "\u5143\u5BB5\u8282",
    "04-04": "\u6E05\u660E\u8282",
    "05-28": "\u7AEF\u5348\u8282",
    "10-03": "\u4E2D\u79CB\u8282"
  },
  2029: {
    "02-13": "\u6625\u8282",
    "02-27": "\u5143\u5BB5\u8282",
    "04-04": "\u6E05\u660E\u8282",
    "06-16": "\u7AEF\u5348\u8282",
    "09-22": "\u4E2D\u79CB\u8282"
  },
  2030: {
    "02-03": "\u6625\u8282",
    "02-17": "\u5143\u5BB5\u8282",
    "04-05": "\u6E05\u660E\u8282",
    "06-05": "\u7AEF\u5348\u8282",
    "09-12": "\u4E2D\u79CB\u8282"
  }
};
var HolidayService = class {
  constructor() {
    this.calendar = new CalendarService();
  }
  getHolidays(date) {
    var _a, _b;
    const key = this.calendar.getDateKey(date).slice(5);
    const holidays = [...(_a = FIXED_HOLIDAYS[key]) != null ? _a : []];
    const lunarHoliday = (_b = CHINESE_LUNAR_HOLIDAYS[date.getFullYear()]) == null ? void 0 : _b[key];
    if (lunarHoliday) {
      holidays.push({ name: lunarHoliday, region: "CN" });
    }
    if (this.isThanksgiving(date)) {
      holidays.push({ name: "Thanksgiving", region: "Global" });
    }
    return holidays;
  }
  getHoliday(date) {
    const names = this.getHolidays(date).map((holiday) => holiday.name);
    return names.length > 0 ? names.join(" / ") : null;
  }
  isThanksgiving(date) {
    if (date.getMonth() !== 10 || date.getDay() !== 4) {
      return false;
    }
    const day = date.getDate();
    return day >= 22 && day <= 28;
  }
};

// src/services/NoteService.ts
var NoteService = class {
  constructor(app) {
    this.app = app;
    this.calendar = new CalendarService();
  }
  getVaultName() {
    return this.app.vault.getName();
  }
  getDailyNotePath(date) {
    return `Daily Notes/${this.calendar.getDateKey(date)}.md`;
  }
  getDailyNote(date) {
    return this.app.vault.getFileByPath(this.getDailyNotePath(date));
  }
  async openOrCreateDailyNote(date) {
    const existing = this.getDailyNote(date);
    const file = existing != null ? existing : await this.createUniqueFile(
      "Daily Notes",
      this.calendar.getDateKey(date),
      `# ${this.calendar.getDateKey(date)}

`
    );
    await this.app.workspace.getLeaf(false).openFile(file);
    return file;
  }
  getRecentMarkdownFiles(limit = 12) {
    return this.app.vault.getMarkdownFiles().sort((left, right) => right.stat.mtime - left.stat.mtime).slice(0, limit);
  }
  getNotesForDate(date) {
    const dateKey = this.calendar.getDateKey(date);
    const dailyNote = this.getDailyNote(date);
    const markdownFiles = this.app.vault.getMarkdownFiles();
    const dailyPath = dailyNote == null ? void 0 : dailyNote.path;
    const isNotDaily = (file) => file.path !== dailyPath;
    const created = markdownFiles.filter((file) => isNotDaily(file) && this.calendar.getDateKey(new Date(file.stat.ctime)) === dateKey).sort((left, right) => right.stat.ctime - left.stat.ctime);
    const modified = markdownFiles.filter((file) => isNotDaily(file) && this.calendar.getDateKey(new Date(file.stat.mtime)) === dateKey).sort((left, right) => right.stat.mtime - left.stat.mtime);
    const linked = markdownFiles.filter((file) => isNotDaily(file) && this.isExplicitlyLinkedToDate(file, dateKey)).sort((left, right) => right.stat.mtime - left.stat.mtime);
    return { dailyNote, created, modified, linked };
  }
  hasNotesForDate(date) {
    const notes = this.getNotesForDate(date);
    return Boolean(notes.dailyNote || notes.created.length > 0 || notes.modified.length > 0 || notes.linked.length > 0);
  }
  getNoteCountForDate(date) {
    const notes = this.getNotesForDate(date);
    const paths = /* @__PURE__ */ new Set();
    if (notes.dailyNote) paths.add(notes.dailyNote.path);
    notes.created.forEach((file) => paths.add(file.path));
    notes.modified.forEach((file) => paths.add(file.path));
    notes.linked.forEach((file) => paths.add(file.path));
    return paths.size;
  }
  async createNote() {
    const file = await this.createUniqueFile("Cute Workbench Notes", "\u672A\u547D\u540D\u7B14\u8BB0", "# \u672A\u547D\u540D\u7B14\u8BB0\n\n");
    await this.app.workspace.getLeaf(false).openFile(file);
    return file;
  }
  async createUniqueFile(folder, basename, content) {
    if (!this.app.vault.getAbstractFileByPath(folder)) {
      await this.app.vault.createFolder(folder);
    }
    let index = 1;
    let path = `${folder}/${basename}.md`;
    while (this.app.vault.getAbstractFileByPath(path)) {
      index += 1;
      path = `${folder}/${basename} ${index}.md`;
    }
    return this.app.vault.create(path, content);
  }
  isExplicitlyLinkedToDate(file, dateKey) {
    var _a, _b;
    const cache = this.app.metadataCache.getFileCache(file);
    const frontmatter = cache == null ? void 0 : cache.frontmatter;
    const frontmatterValues = [
      frontmatter == null ? void 0 : frontmatter.date,
      frontmatter == null ? void 0 : frontmatter.day,
      frontmatter == null ? void 0 : frontmatter.created,
      frontmatter == null ? void 0 : frontmatter.updated,
      frontmatter == null ? void 0 : frontmatter.workbenchDate
    ];
    if (frontmatterValues.some((value) => this.valueContainsDate(value, dateKey))) {
      return true;
    }
    const links = [...(_a = cache == null ? void 0 : cache.links) != null ? _a : [], ...(_b = cache == null ? void 0 : cache.embeds) != null ? _b : []];
    return links.some((link) => link.link.includes(dateKey));
  }
  valueContainsDate(value, dateKey) {
    if (Array.isArray(value)) {
      return value.some((item) => this.valueContainsDate(item, dateKey));
    }
    return typeof value === "string" && value.includes(dateKey);
  }
};

// src/components/overview/DayOverviewModal.ts
var import_obsidian29 = require("obsidian");
var DayOverviewModal = class extends import_obsidian29.Modal {
  constructor(app, store, date, onDataChanged) {
    super(app);
    this.store = store;
    this.date = date;
    this.onDataChanged = onDataChanged;
    this.calendar = new CalendarService();
    this.holidays = new HolidayService();
    this.notes = new NoteService(app);
  }
  onOpen() {
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    const dateKey = this.calendar.getDateKey(this.date);
    const holidays = this.holidays.getHolidays(this.date);
    this.contentEl.createEl("h2", { text: dateKey });
    this.contentEl.createEl("p", {
      text: `${this.calendar.getWeekdayLabel(this.date)}${holidays.length > 0 ? ` \xB7 ${holidays.map((holiday) => holiday.name).join(" / ")}` : " \xB7 \u6682\u65E0\u8282\u65E5"}`
    });
    this.renderTodos(dateKey);
    this.renderNotes();
  }
  renderTodos(dateKey) {
    const header = this.contentEl.createDiv({ cls: "cow-day-section-header" });
    header.createEl("h3", { text: "\u5F85\u529E" });
    const addButton = header.createEl("button", { attr: { type: "button" } });
    (0, import_obsidian29.setIcon)(addButton.createSpan(), "plus");
    addButton.createSpan({ text: "\u6DFB\u52A0\u5F85\u529E" });
    addButton.addEventListener("click", () => {
      new CalendarTodoModal(this.app, async (title, category) => {
        await this.store.addCalendarTodo(title, dateKey, category);
        this.onDataChanged();
        this.render();
      }).open();
    });
    const list = this.contentEl.createDiv({ cls: "cow-calendar-todo-list" });
    const todos = this.store.getCalendarTodos(dateKey);
    if (todos.length === 0) {
      list.createEl("p", { cls: "cow-empty-state", text: "\u8FD9\u4E00\u5929\u8FD8\u6CA1\u6709\u5F85\u529E\u3002" });
      return;
    }
    todos.forEach((todo) => this.renderTodoItem(list, todo));
  }
  renderTodoItem(container, todo) {
    const row = container.createDiv({ cls: `cow-calendar-todo-item ${todo.completed ? "is-complete" : ""}` });
    const checkbox = row.createEl("input", { attr: { type: "checkbox" } });
    checkbox.checked = todo.completed;
    checkbox.addEventListener("change", async () => {
      await this.store.toggleCalendarTodo(todo.id);
      this.onDataChanged();
      this.render();
    });
    const text = row.createDiv();
    text.createEl("strong", { text: todo.title });
    text.createSpan({ text: todo.category });
    const edit = row.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u5F85\u529E" } });
    (0, import_obsidian29.setIcon)(edit, "pencil");
    edit.addEventListener("click", () => {
      new CalendarTodoModal(this.app, async (title, category) => {
        await this.store.updateCalendarTodo(todo.id, { title, category });
        this.onDataChanged();
        this.render();
      }, todo).open();
    });
    const remove = row.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u5F85\u529E" } });
    (0, import_obsidian29.setIcon)(remove, "trash-2");
    remove.addEventListener("click", async () => {
      await this.store.deleteCalendarTodo(todo.id);
      this.onDataChanged();
      this.render();
    });
  }
  renderNotes() {
    this.contentEl.createEl("h3", { text: "\u7B14\u8BB0" });
    const groups = this.notes.getNotesForDate(this.date);
    const list = this.contentEl.createDiv({ cls: "cow-day-note-list" });
    this.renderDailyNote(list, groups);
    this.renderNoteGroup(list, "\u5F53\u5929\u521B\u5EFA\u7684\u7B14\u8BB0", groups.created);
    this.renderNoteGroup(list, "\u5F53\u5929\u4FEE\u6539\u7684\u7B14\u8BB0", groups.modified);
    this.renderNoteGroup(list, "\u660E\u786E\u5173\u8054\u8BE5\u65E5\u671F\u7684\u7B14\u8BB0", groups.linked);
  }
  renderDailyNote(container, groups) {
    const section = container.createDiv({ cls: "cow-day-note-group" });
    section.createEl("h4", { text: "Daily Note" });
    if (groups.dailyNote) {
      this.renderNoteButton(section, groups.dailyNote);
      return;
    }
    const button = section.createEl("button", { cls: "cow-day-note-item", attr: { type: "button" } });
    button.createEl("strong", { text: "\u521B\u5EFA\u6BCF\u65E5\u7B14\u8BB0" });
    button.createSpan({ text: this.notes.getDailyNotePath(this.date) });
    button.addEventListener("click", async () => {
      await this.notes.openOrCreateDailyNote(this.date);
      this.close();
    });
  }
  renderNoteGroup(container, title, files) {
    const section = container.createDiv({ cls: "cow-day-note-group" });
    section.createEl("h4", { text: title });
    if (files.length === 0) {
      section.createEl("p", { cls: "cow-empty-state", text: "\u6682\u65E0\u7B14\u8BB0\u3002" });
      return;
    }
    files.forEach((file) => this.renderNoteButton(section, file));
  }
  renderNoteButton(container, file) {
    const button = container.createEl("button", { cls: "cow-day-note-item", attr: { type: "button" } });
    button.createEl("strong", { text: file.basename });
    button.createSpan({ text: file.path });
    button.createEl("time", { text: new Date(file.stat.mtime).toLocaleString("zh-CN") });
    button.addEventListener("click", async () => {
      await this.app.workspace.getLeaf(false).openFile(file);
      this.close();
    });
  }
};
var CalendarTodoModal = class extends import_obsidian29.Modal {
  constructor(app, onSubmit, todo) {
    var _a, _b;
    super(app);
    this.onSubmit = onSubmit;
    this.titleValue = (_a = todo == null ? void 0 : todo.title) != null ? _a : "";
    this.categoryValue = (_b = todo == null ? void 0 : todo.category) != null ? _b : "\u5F85\u529E";
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: this.titleValue ? "\u7F16\u8F91\u5F85\u529E" : "\u6DFB\u52A0\u5F85\u529E" });
    new import_obsidian29.Setting(this.contentEl).setName("\u6807\u9898").addText((text) => text.setValue(this.titleValue).onChange((value) => {
      this.titleValue = value;
    }));
    new import_obsidian29.Setting(this.contentEl).setName("\u5206\u7C7B").addText((text) => text.setValue(this.categoryValue).onChange((value) => {
      this.categoryValue = value;
    }));
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    const save = actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } });
    save.addEventListener("click", async () => {
      const title = this.titleValue.trim();
      if (!title) return;
      await this.onSubmit(title, this.categoryValue.trim() || "\u5F85\u529E");
      this.close();
    });
  }
};

// src/components/overview/MonthlyCalendarSection.ts
var MonthlyCalendarSection = class {
  constructor(app, store, onDataChanged = () => void 0) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
    this.displayDate = /* @__PURE__ */ new Date();
    this.calendar = new CalendarService();
    this.holidays = new HolidayService();
    this.notes = new NoteService(app);
  }
  render(container) {
    this.draw(container);
  }
  draw(container) {
    container.empty();
    const root = container.createDiv({ cls: "cow-month-calendar" });
    const header = root.createDiv({ cls: "cow-month-calendar-header" });
    const previous = header.createEl("button", { attr: { type: "button", "aria-label": "\u4E0A\u4E00\u6708" } });
    (0, import_obsidian30.setIcon)(previous, "chevron-left");
    previous.addEventListener("click", () => {
      this.displayDate = this.calendar.addMonths(this.displayDate, -1);
      this.draw(container);
    });
    header.createEl("strong", { text: this.calendar.getMonthTitle(this.displayDate) });
    const today6 = header.createEl("button", { cls: "cow-calendar-today", text: "\u4ECA\u5929", attr: { type: "button" } });
    today6.addEventListener("click", () => {
      this.displayDate = /* @__PURE__ */ new Date();
      this.draw(container);
    });
    const next = header.createEl("button", { attr: { type: "button", "aria-label": "\u4E0B\u4E00\u6708" } });
    (0, import_obsidian30.setIcon)(next, "chevron-right");
    next.addEventListener("click", () => {
      this.displayDate = this.calendar.addMonths(this.displayDate, 1);
      this.draw(container);
    });
    const grid = root.createDiv({ cls: "cow-month-calendar-grid" });
    const settings = this.store.getData().calendarSettings;
    const weekdays = settings.weekStartsOn === "monday" ? ["\u4E00", "\u4E8C", "\u4E09", "\u56DB", "\u4E94", "\u516D", "\u65E5"] : ["\u65E5", "\u4E00", "\u4E8C", "\u4E09", "\u56DB", "\u4E94", "\u516D"];
    weekdays.forEach((weekday) => grid.createSpan({ cls: "cow-weekday", text: weekday }));
    const todayKey5 = this.calendar.getDateKey(/* @__PURE__ */ new Date());
    this.calendar.getMonthCells(this.displayDate, settings.weekStartsOn).forEach((date) => {
      if (!date) {
        grid.createSpan({ cls: "cow-calendar-empty" });
        return;
      }
      const dateKey = this.calendar.getDateKey(date);
      const button = grid.createEl("button", {
        cls: `cow-calendar-day ${dateKey === todayKey5 ? "is-today" : ""}`,
        attr: { type: "button", "aria-label": dateKey }
      });
      if (dateKey === todayKey5) {
        button.style.borderColor = settings.highlightColor;
        button.style.background = `${settings.highlightColor}44`;
      }
      button.createSpan({ cls: "cow-calendar-day-number", text: String(date.getDate()) });
      const holidays = this.holidays.getHolidays(date);
      if (holidays.length > 0) {
        button.createSpan({ cls: "cow-calendar-holiday", text: holidays[0].name });
      } else {
        button.createSpan({ cls: "cow-calendar-holiday is-empty", text: "" });
      }
      const dots = button.createDiv({ cls: "cow-calendar-dots cow-calendar-day-indicators" });
      this.renderBadges(dots, this.getBadges(date));
      button.addEventListener("click", () => {
        new DayOverviewModal(this.app, this.store, date, () => {
          this.onDataChanged();
          this.draw(container);
        }).open();
      });
    });
  }
  getBadges(date) {
    const settings = this.store.getData().calendarSettings;
    const dateKey = this.calendar.getDateKey(date);
    const todos = this.store.getCalendarTodos(dateKey);
    const badges = [];
    if (settings.showTaskMarkers) {
      const incomplete = todos.filter((todo) => !todo.completed).length;
      const complete = todos.filter((todo) => todo.completed).length;
      if (incomplete > 0) badges.push({ cls: "is-task", count: incomplete });
      if (complete > 0) badges.push({ cls: "is-task-done", count: complete });
    }
    if (settings.showNoteMarkers) {
      const noteCount = this.notes.getNoteCountForDate(date);
      if (noteCount > 0) badges.push({ cls: "is-note", count: noteCount });
    }
    if (settings.showEventMarkers) {
      const eventCount = this.holidays.getHolidays(date).length;
      if (eventCount > 0) badges.push({ cls: "is-event", count: eventCount });
    }
    return badges;
  }
  renderBadges(container, badges) {
    const expanded = [];
    badges.forEach((badge) => {
      for (let index = 0; index < badge.count; index += 1) {
        expanded.push(badge.cls);
      }
    });
    expanded.slice(0, 4).forEach((cls) => container.createSpan({ cls }));
    if (expanded.length > 4) {
      container.createSpan({ cls: "is-more", text: `+${expanded.length - 4}` });
    }
  }
};

// src/components/overview/MonthlyProgressSection.ts
var COLOR_MAP = {
  research: "is-pink",
  reading: "is-yellow",
  fitness: "is-green",
  finance: "is-blue",
  goals: "is-purple"
};
var MonthlyProgressSection = class {
  constructor(store) {
    this.store = store;
  }
  render(container) {
    const progress = new StatisticsService(this.store).getMonthProgress(/* @__PURE__ */ new Date());
    progress.forEach((item) => {
      const row = container.createDiv({ cls: "cow-month-progress-row" });
      row.createSpan({ text: item.label });
      const track = row.createDiv({ cls: "cow-month-progress-track" });
      track.createDiv({ cls: `cow-month-progress-fill ${COLOR_MAP[item.id]}`, attr: { style: `width: ${item.current}%` } });
      row.createSpan({ text: `${item.current}%` });
    });
  }
};

// src/components/overview/OverviewStatsSection.ts
var import_obsidian31 = require("obsidian");
var OverviewStatsSection = class {
  constructor(store, type) {
    this.store = store;
    this.type = type;
  }
  render(container) {
    const stat = this.getStat();
    const wrapper = container.createDiv({ cls: "cow-overview-stat" });
    const art = wrapper.createDiv({ cls: `cow-stat-art ${stat.className}` });
    (0, import_obsidian31.setIcon)(art.createSpan(), stat.icon);
    const body = wrapper.createDiv();
    body.createEl("strong", { text: stat.value });
    body.createEl("span", { text: stat.caption });
    const meter = body.createDiv({ cls: "cow-stat-meter" });
    meter.createDiv({ attr: { style: `width: ${stat.meter}%` } });
  }
  getStat() {
    if (this.type === "weekly-completion") {
      const rate = this.store.getWeeklyCompletionRate();
      return {
        value: `${rate}%`,
        caption: "\u518D\u63A5\u518D\u5389\uFF01",
        icon: "badge-percent",
        meter: rate,
        className: "is-green"
      };
    }
    if (this.type === "pending-tasks") {
      const pending = this.store.getPendingTaskCount();
      const total = this.store.getTodayFocusTasks().length;
      const doneRate = total === 0 ? 100 : Math.round((total - pending) / total * 100);
      return {
        value: `${total - pending}/${total}`,
        caption: pending === 0 ? "\u4ECA\u65E5\u6E05\u7A7A\u5566" : `\u8FD8\u6709${pending}\u9879\u5F85\u5B8C\u6210`,
        icon: "clipboard-list",
        meter: doneRate,
        className: "is-pink"
      };
    }
    if (this.type === "today-focus-stat") {
      return {
        value: "3.6h",
        caption: "\u4E13\u6CE8\u8BA9\u5E73\u51E1\u53D8\u4F1F\u5927",
        icon: "headphones",
        meter: 72,
        className: "is-blue"
      };
    }
    return {
      value: `${this.store.getCheckinStreakDays()}\u5929`,
      caption: "\u771F\u68D2\uFF01\u7EE7\u7EED\u4FDD\u6301",
      icon: "flame",
      meter: Math.min(100, this.store.getCheckinStreakDays() * 10),
      className: "is-yellow"
    };
  }
};

// src/components/overview/QuickActionsSection.ts
var import_obsidian32 = require("obsidian");
var QuickActionsSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const actions = this.store.getData().quickActions.filter((action) => action.enabled).sort((a, b) => a.order - b.order);
    const grid = container.createDiv({ cls: "cow-quick-action-grid" });
    actions.forEach((action) => {
      const button = grid.createEl("button", { attr: { type: "button" } });
      (0, import_obsidian32.setIcon)(button.createSpan(), this.getIcon(action));
      button.createSpan({ text: action.label });
      button.addEventListener("click", () => void this.runAction(action));
    });
  }
  async runAction(action) {
    switch (action.type) {
      case "new-note":
        await this.createNote();
        break;
      case "daily-note":
        await this.openDailyNote();
        break;
      case "search":
        this.executeCommand("global-search:open");
        break;
      case "templates":
        this.executeCommand("templates:insert-template");
        break;
      case "graph":
        this.executeCommand("graph:open");
        break;
      default:
        new import_obsidian32.Notice(action.target ? `\u81EA\u5B9A\u4E49\u5165\u53E3\uFF1A${action.target}` : "\u81EA\u5B9A\u4E49\u5FEB\u6377\u5165\u53E3\u5DF2\u89E6\u53D1\u3002");
    }
  }
  getIcon(action) {
    const icons = {
      "new-note": "file-plus",
      "daily-note": "calendar-days",
      search: "search",
      templates: "layout-template",
      graph: "git-fork",
      custom: "star"
    };
    return icons[action.type];
  }
  executeCommand(commandId) {
    var _a;
    const didRun = (_a = this.app.commands) == null ? void 0 : _a.executeCommandById(commandId);
    if (!didRun) {
      new import_obsidian32.Notice("\u8FD9\u4E2A Obsidian \u547D\u4EE4\u6682\u65F6\u4E0D\u53EF\u7528\u3002");
    }
  }
  async createNote() {
    const file = await this.createUniqueFile("Cute Workbench Notes", "\u672A\u547D\u540D\u7B14\u8BB0", "# \u672A\u547D\u540D\u7B14\u8BB0\n\n");
    await this.app.workspace.getLeaf(false).openFile(file);
  }
  async openDailyNote() {
    const folder = "Daily Notes";
    const path = `${folder}/${formatDateKey(/* @__PURE__ */ new Date())}.md`;
    const existing = this.app.vault.getFileByPath(path);
    const file = existing != null ? existing : await this.createUniqueFile(folder, formatDateKey(/* @__PURE__ */ new Date()), `# ${formatDateKey(/* @__PURE__ */ new Date())}

`);
    await this.app.workspace.getLeaf(false).openFile(file);
  }
  async addTask() {
    await this.store.addTodayFocusTask("\u65B0\u7684\u5F85\u529E\u4EFB\u52A1");
    this.onDataChanged();
    new import_obsidian32.Notice("\u5DF2\u6DFB\u52A0\u5230\u4ECA\u65E5\u7126\u70B9\u3002");
  }
  async createUniqueFile(folder, basename, content) {
    if (!this.app.vault.getAbstractFileByPath(folder)) {
      await this.app.vault.createFolder(folder);
    }
    let index = 1;
    let path = `${folder}/${basename}.md`;
    while (this.app.vault.getAbstractFileByPath(path)) {
      index += 1;
      path = `${folder}/${basename} ${index}.md`;
    }
    return this.app.vault.create(path, content);
  }
};

// src/components/overview/RecentNotesSection.ts
var RecentNotesSection = class {
  constructor(app) {
    this.app = app;
  }
  render(container) {
    const files = this.app.vault.getMarkdownFiles().sort((left, right) => right.stat.mtime - left.stat.mtime).slice(0, 5);
    if (files.length === 0) {
      container.createEl("p", { cls: "cow-empty-state", text: "\u8FD8\u6CA1\u6709 Markdown \u7B14\u8BB0\u3002" });
      return;
    }
    const list = container.createEl("ul", { cls: "cow-recent-notes" });
    files.forEach((file) => {
      const item = list.createEl("li");
      const button = item.createEl("button", { attr: { type: "button" } });
      button.createSpan({ text: file.basename });
      button.createEl("time", { text: this.formatTime(file.stat.mtime) });
      button.addEventListener("click", async () => this.openFile(file));
    });
  }
  async openFile(file) {
    const leaf = this.app.workspace.getLeaf(false);
    await leaf.openFile(file);
  }
  formatTime(timestamp) {
    return new Date(timestamp).toLocaleString("zh-CN", {
      month: "2-digit",
      day: "2-digit",
      hour: "2-digit",
      minute: "2-digit"
    });
  }
};

// src/components/overview/TodayFocusSection.ts
var import_obsidian37 = require("obsidian");

// src/services/DailyFocusService.ts
var import_obsidian36 = require("obsidian");

// src/components/tasks/TaskModals.ts
var import_obsidian33 = require("obsidian");
var STATUS_OPTIONS = [
  ["inbox", "\u6536\u96C6\u7BB1"],
  ["todo", "\u5F85\u529E"],
  ["doing", "\u8FDB\u884C\u4E2D"],
  ["waiting", "\u7B49\u5F85"],
  ["done", "\u5DF2\u5B8C\u6210"],
  ["cancelled", "\u5DF2\u53D6\u6D88"]
];
var PRIORITY_OPTIONS = [
  ["none", "\u65E0"],
  ["low", "\u4F4E"],
  ["medium", "\u4E2D"],
  ["high", "\u9AD8"]
];
var SOURCE_OPTIONS = [
  ["general", "\u901A\u7528"],
  ["research", "\u79D1\u7814"],
  ["reading", "\u9605\u8BFB"],
  ["fitness", "\u5065\u8EAB"],
  ["finance", "\u7406\u8D22"],
  ["goal", "\u76EE\u6807\u7BA1\u7406"]
];
var RECURRENCE_OPTIONS = [
  ["none", "\u4E0D\u91CD\u590D"],
  ["daily", "\u6BCF\u5929"],
  ["weekly", "\u6BCF\u5468"],
  ["monthly", "\u6BCF\u6708"],
  ["custom", "\u81EA\u5B9A\u4E49\u5468\u671F"]
];
function nowIso3() {
  return (/* @__PURE__ */ new Date()).toISOString();
}
function today3() {
  return formatDateKey(/* @__PURE__ */ new Date());
}
function setupTaskModal(modal, className = "cute-task-edit-modal") {
  applyResizableModal(modal, {
    className,
    width: "min(920px, 92vw)",
    height: "min(760px, 86vh)",
    maxWidth: "96vw",
    maxHeight: "92vh",
    minWidth: "min(420px, 92vw)",
    minHeight: "min(360px, 86vh)"
  });
}
function createField2(container, label, type, value) {
  const row = container.createDiv({ cls: `cow-task-form-row ${type === "date" ? "is-picker" : ""}` });
  row.createEl("label", { text: label });
  const input = row.createEl("input", { attr: { type, value } });
  if (type === "date") {
    row.addEventListener("click", (event) => {
      var _a;
      if (event.target instanceof HTMLInputElement && event.target !== input) return;
      input.focus();
      try {
        (_a = input.showPicker) == null ? void 0 : _a.call(input);
      } catch (e) {
        input.focus();
      }
    });
  }
  return input;
}
function createSelect(container, label, options, value) {
  const row = container.createDiv({ cls: "cow-task-form-row" });
  row.createEl("label", { text: label });
  const select = row.createEl("select");
  options.forEach(([id, text]) => select.createEl("option", { value: id, text }));
  select.value = value;
  return select;
}
function createTextarea(container, label, value) {
  const row = container.createDiv({ cls: "cow-task-form-row" });
  row.createEl("label", { text: label });
  return row.createEl("textarea", { text: value });
}
function priorityLabel(priority) {
  var _a, _b;
  return (_b = (_a = PRIORITY_OPTIONS.find(([id]) => id === priority)) == null ? void 0 : _a[1]) != null ? _b : priority;
}
function recurrenceLabel(frequency) {
  var _a, _b;
  return (_b = (_a = RECURRENCE_OPTIONS.find(([id]) => id === (frequency != null ? frequency : "none"))) == null ? void 0 : _a[1]) != null ? _b : "\u4E0D\u91CD\u590D";
}
function taskTimeLabel(task) {
  return task.estimatedMinutes ? `${task.estimatedMinutes}min` : "--";
}
function openTaskModal(app, store, onDone, task, defaults = {}) {
  new TaskEditModal(app, store, onDone, task, defaults).open();
}
var TaskEditModal = class extends import_obsidian33.Modal {
  constructor(app, store, onDone, task, defaults = {}) {
    var _a, _b;
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.task = task;
    this.defaults = defaults;
    this.linkedNote = "";
    this.pendingChildren = [];
    this.linkedNote = (_b = (_a = task == null ? void 0 : task.linkedNote) != null ? _a : defaults.linkedNote) != null ? _b : "";
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x, _y, _z, _A, _B, _C, _D, _E, _F, _G, _H, _I, _J, _K, _L, _M, _N, _O, _P, _Q;
    setupTaskModal(this);
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-task-modal");
    this.contentEl.createEl("h2", { text: this.task ? "\u7F16\u8F91\u4EFB\u52A1" : "\u65B0\u5EFA\u4EFB\u52A1" });
    const grid = this.contentEl.createDiv({ cls: "cow-task-form-grid" });
    const basic = grid.createDiv({ cls: "cow-task-form-section" });
    basic.createEl("h3", { text: "\u57FA\u672C\u4FE1\u606F" });
    const title = createField2(basic, "\u4EFB\u52A1\u540D\u79F0 *", "text", (_c = (_b = (_a = this.task) == null ? void 0 : _a.title) != null ? _b : this.defaults.title) != null ? _c : "");
    const description = createTextarea(basic, "\u4EFB\u52A1\u63CF\u8FF0", (_f = (_e = (_d = this.task) == null ? void 0 : _d.description) != null ? _e : this.defaults.description) != null ? _f : "");
    const state = grid.createDiv({ cls: "cow-task-form-section" });
    state.createEl("h3", { text: "\u72B6\u6001\u4E0E\u4F18\u5148\u7EA7" });
    const status = createSelect(state, "\u72B6\u6001", STATUS_OPTIONS, (_i = (_h = (_g = this.task) == null ? void 0 : _g.status) != null ? _h : this.defaults.status) != null ? _i : "inbox");
    const priority = createSelect(state, "\u4F18\u5148\u7EA7", PRIORITY_OPTIONS, (_l = (_k = (_j = this.task) == null ? void 0 : _j.priority) != null ? _k : this.defaults.priority) != null ? _l : "none");
    const time = grid.createDiv({ cls: "cow-task-form-section" });
    time.createEl("h3", { text: "\u65F6\u95F4" });
    const plannedDate = createField2(time, "\u8BA1\u5212\u65E5\u671F", "date", (_o = (_n = (_m = this.task) == null ? void 0 : _m.plannedDate) != null ? _n : this.defaults.plannedDate) != null ? _o : "");
    const dueDate = createField2(time, "\u622A\u6B62\u65E5\u671F", "date", (_r = (_q = (_p = this.task) == null ? void 0 : _p.dueDate) != null ? _q : this.defaults.dueDate) != null ? _r : "");
    const estimatedMinutes = createField2(time, "\u9884\u8BA1\u65F6\u957F\uFF08\u5206\u949F\uFF09", "number", String((_u = (_t = (_s = this.task) == null ? void 0 : _s.estimatedMinutes) != null ? _t : this.defaults.estimatedMinutes) != null ? _u : ""));
    const belong = grid.createDiv({ cls: "cow-task-form-section" });
    belong.createEl("h3", { text: "\u5F52\u5C5E" });
    const project = belong.createDiv({ cls: "cow-task-form-row" });
    project.createEl("label", { text: "\u6240\u5C5E\u9879\u76EE" });
    const projectSelect = project.createEl("select");
    projectSelect.createEl("option", { value: "", text: "\u4E0D\u5173\u8054" });
    this.store.getTaskProjects().forEach((item) => projectSelect.createEl("option", { value: item.id, text: item.name }));
    projectSelect.value = (_x = (_w = (_v = this.task) == null ? void 0 : _v.projectId) != null ? _w : this.defaults.projectId) != null ? _x : "";
    const source = createSelect(belong, "\u6765\u6E90\u6A21\u5757", SOURCE_OPTIONS, (_A = (_z = (_y = this.task) == null ? void 0 : _y.sourceModule) != null ? _z : this.defaults.sourceModule) != null ? _A : "general");
    const tags = createField2(belong, "\u6807\u7B7E\uFF08\u7528\u9017\u53F7\u5206\u9694\uFF09", "text", ((_D = (_C = (_B = this.task) == null ? void 0 : _B.tags) != null ? _C : this.defaults.tags) != null ? _D : []).join(", "));
    const relate = grid.createDiv({ cls: "cow-task-form-section" });
    relate.createEl("h3", { text: "\u5173\u8054" });
    const parent = relate.createDiv({ cls: "cow-task-form-row" });
    parent.createEl("label", { text: "\u7236\u4EFB\u52A1" });
    const parentSelect = parent.createEl("select");
    parentSelect.createEl("option", { value: "", text: "\u65E0" });
    this.store.getTasks().filter((item) => {
      var _a2;
      return item.id !== ((_a2 = this.task) == null ? void 0 : _a2.id) && !item.parentTaskId;
    }).forEach((item) => parentSelect.createEl("option", { value: item.id, text: item.title }));
    parentSelect.value = (_G = (_F = (_E = this.task) == null ? void 0 : _E.parentTaskId) != null ? _F : this.defaults.parentTaskId) != null ? _G : "";
    new MarkdownFilePicker(this.app, {
      label: "\u5173\u8054 Obsidian \u7B14\u8BB0",
      value: this.linkedNote,
      onChange: (path) => this.linkedNote = path
    }).render(relate);
    const recurrence = grid.createDiv({ cls: "cow-task-form-section" });
    recurrence.createEl("h3", { text: "\u5468\u671F" });
    const recurrenceSelect = createSelect(recurrence, "\u91CD\u590D", RECURRENCE_OPTIONS, (_L = (_K = (_I = (_H = this.task) == null ? void 0 : _H.recurrence) == null ? void 0 : _I.frequency) != null ? _K : (_J = this.defaults.recurrence) == null ? void 0 : _J.frequency) != null ? _L : "none");
    const interval = createField2(recurrence, "\u81EA\u5B9A\u4E49\u95F4\u9694\uFF08\u5929\uFF09", "number", String((_Q = (_P = (_N = (_M = this.task) == null ? void 0 : _M.recurrence) == null ? void 0 : _N.interval) != null ? _P : (_O = this.defaults.recurrence) == null ? void 0 : _O.interval) != null ? _Q : 1));
    this.renderChildren(grid);
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58\u4EFB\u52A1", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2, _c2, _d2, _e2, _f2, _g2, _h2, _i2;
      const name = title.value.trim();
      if (!name) {
        new import_obsidian33.Notice("\u4EFB\u52A1\u540D\u79F0\u4E0D\u80FD\u4E3A\u7A7A\u3002");
        return;
      }
      const nextStatus = status.value;
      const recurrenceFrequency = recurrenceSelect.value;
      const values = {
        id: (_b2 = (_a2 = this.task) == null ? void 0 : _a2.id) != null ? _b2 : `task-${Date.now()}`,
        title: name,
        description: description.value.trim(),
        status: nextStatus,
        priority: priority.value,
        sourceModule: source.value,
        projectId: projectSelect.value || void 0,
        parentTaskId: parentSelect.value || void 0,
        tags: tags.value.split(/[,，]/).map((tag) => tag.trim()).filter(Boolean),
        plannedDate: plannedDate.value || void 0,
        dueDate: dueDate.value || void 0,
        completedDate: nextStatus === "done" ? (_d2 = (_c2 = this.task) == null ? void 0 : _c2.completedDate) != null ? _d2 : today3() : void 0,
        estimatedMinutes: Number(estimatedMinutes.value) > 0 ? Number(estimatedMinutes.value) : void 0,
        actualMinutes: (_e2 = this.task) == null ? void 0 : _e2.actualMinutes,
        recurrence: recurrenceFrequency === "none" ? { frequency: "none", enabled: false } : {
          frequency: recurrenceFrequency,
          interval: Math.max(1, Number(interval.value) || 1),
          enabled: true,
          nextDate: plannedDate.value || dueDate.value || today3(),
          lastGeneratedDate: (_g2 = (_f2 = this.task) == null ? void 0 : _f2.recurrence) == null ? void 0 : _g2.lastGeneratedDate
        },
        linkedNote: this.linkedNote || void 0,
        createdAt: (_i2 = (_h2 = this.task) == null ? void 0 : _h2.createdAt) != null ? _i2 : nowIso3(),
        updatedAt: nowIso3()
      };
      if (this.task) await this.store.updateTask(this.task.id, values);
      else await this.store.addTask(values);
      for (const childTitle of this.pendingChildren) {
        await this.store.addTask({
          title: childTitle,
          status: "todo",
          priority: "none",
          parentTaskId: values.id,
          sourceModule: values.sourceModule,
          projectId: values.projectId,
          plannedDate: values.plannedDate
        });
      }
      this.onDone();
      this.close();
    });
  }
  renderChildren(container) {
    const section = container.createDiv({ cls: "cow-task-form-section" });
    section.createEl("h3", { text: "\u5B50\u4EFB\u52A1" });
    const list = section.createDiv({ cls: "cow-task-subtask-list" });
    if (this.task) {
      this.store.getTasks().filter((item) => {
        var _a;
        return item.parentTaskId === ((_a = this.task) == null ? void 0 : _a.id);
      }).forEach((child) => {
        const row = list.createDiv({ cls: "cow-task-subtask-row" });
        const checkbox = row.createEl("input", { attr: { type: "checkbox" } });
        checkbox.checked = child.status === "done";
        checkbox.addEventListener("change", async () => {
          await this.store.updateTask(child.id, { status: checkbox.checked ? "done" : "todo" });
          this.onDone();
        });
        row.createSpan({ text: child.title });
      });
    }
    this.pendingChildren.forEach((title) => list.createDiv({ cls: "cow-task-subtask-row" }).createSpan({ text: title }));
    const addRow = section.createDiv({ cls: "cow-task-inline-add" });
    const input = addRow.createEl("input", { attr: { type: "text", placeholder: "\u6DFB\u52A0\u5B50\u4EFB\u52A1\u2026" } });
    addRow.createEl("button", { text: "\u6DFB\u52A0", attr: { type: "button" } }).addEventListener("click", () => {
      const value = input.value.trim();
      if (!value) return;
      this.pendingChildren.push(value);
      this.onOpen();
    });
  }
};
function openTaskProjectModal(app, store, onDone, project) {
  new TaskProjectModal(app, store, onDone, project).open();
}
var TaskProjectModal = class extends import_obsidian33.Modal {
  constructor(app, store, onDone, project) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.project = project;
  }
  onOpen() {
    var _a, _b, _c, _d;
    setupTaskModal(this, "cute-task-small-modal");
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-task-modal");
    this.contentEl.createEl("h2", { text: this.project ? "\u7F16\u8F91\u9879\u76EE" : "\u65B0\u5EFA\u9879\u76EE" });
    const name = createField2(this.contentEl, "\u9879\u76EE\u540D\u79F0", "text", (_b = (_a = this.project) == null ? void 0 : _a.name) != null ? _b : "");
    const description = createTextarea(this.contentEl, "\u9879\u76EE\u63CF\u8FF0", (_d = (_c = this.project) == null ? void 0 : _c.description) != null ? _d : "");
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2, _c2, _d2;
      const values = { id: (_b2 = (_a2 = this.project) == null ? void 0 : _a2.id) != null ? _b2 : `task-project-${Date.now()}`, name: name.value.trim() || "\u672A\u547D\u540D\u9879\u76EE", description: description.value.trim(), createdAt: (_d2 = (_c2 = this.project) == null ? void 0 : _c2.createdAt) != null ? _d2 : nowIso3() };
      if (this.project) await this.store.updateTaskProject(this.project.id, values);
      else await this.store.addTaskProject(values);
      this.onDone();
      this.close();
    });
  }
};
function openTaskFilterModal(app, store, onDone) {
  new TaskFilterModal(app, store, onDone).open();
}
var TaskFilterModal = class extends import_obsidian33.Modal {
  constructor(app, store, onDone) {
    super(app);
    this.store = store;
    this.onDone = onDone;
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f;
    setupTaskModal(this, "cute-task-small-modal");
    const settings = this.store.getTaskSettings();
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-task-modal");
    this.contentEl.createEl("h2", { text: "\u7B5B\u9009\u4EFB\u52A1" });
    const form = this.contentEl.createDiv({ cls: "cow-task-form-grid" });
    const status = createSelect(form, "\u72B6\u6001", [["all", "\u5168\u90E8"], ...STATUS_OPTIONS], (_a = settings.filterStatus) != null ? _a : "all");
    const priority = createSelect(form, "\u4F18\u5148\u7EA7", [["all", "\u5168\u90E8"], ...PRIORITY_OPTIONS], (_b = settings.filterPriority) != null ? _b : "all");
    const projectRow = form.createDiv({ cls: "cow-task-form-row" });
    projectRow.createEl("label", { text: "\u9879\u76EE" });
    const project = projectRow.createEl("select");
    project.createEl("option", { value: "", text: "\u5168\u90E8\u9879\u76EE" });
    this.store.getTaskProjects().forEach((item) => project.createEl("option", { value: item.id, text: item.name }));
    project.value = (_c = settings.filterProjectId) != null ? _c : "";
    const start = createField2(form, "\u5F00\u59CB\u65E5\u671F", "date", (_d = settings.filterStartDate) != null ? _d : "");
    const end = createField2(form, "\u7ED3\u675F\u65E5\u671F", "date", (_e = settings.filterEndDate) != null ? _e : "");
    const tag = createField2(form, "\u6807\u7B7E", "text", (_f = settings.filterTag) != null ? _f : "");
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u91CD\u7F6E", attr: { type: "button" } }).addEventListener("click", async () => {
      await this.store.updateTaskSettings({ filterStatus: "all", filterPriority: "all", filterProjectId: "", filterStartDate: "", filterEndDate: "", filterTag: "" });
      this.onDone();
      this.close();
    });
    actions.createEl("button", { text: "\u5E94\u7528", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      await this.store.updateTaskSettings({
        filterStatus: status.value,
        filterPriority: priority.value,
        filterProjectId: project.value,
        filterStartDate: start.value,
        filterEndDate: end.value,
        filterTag: tag.value.trim()
      });
      this.onDone();
      this.close();
    });
  }
};
var TaskStatisticsModal = class extends import_obsidian33.Modal {
  constructor(app, store) {
    super(app);
    this.store = store;
    this.period = "week";
  }
  onOpen() {
    setupTaskModal(this, "cute-task-stats-modal");
    this.render();
  }
  render() {
    const tasks = this.store.getTasks();
    const now = /* @__PURE__ */ new Date();
    const start = new Date(now);
    start.setDate(now.getDate() - (this.period === "week" ? 6 : 29));
    const startKey = formatDateKey(start);
    const todayKey5 = today3();
    const scoped = tasks.filter((task) => {
      var _a, _b;
      return ((_b = (_a = task.plannedDate) != null ? _a : task.completedDate) != null ? _b : task.createdAt.slice(0, 10)) >= startKey;
    });
    const done = scoped.filter((task) => task.status === "done").length;
    const overdue = tasks.filter((task) => task.dueDate && task.dueDate < todayKey5 && task.status !== "done").length;
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-task-modal", "cow-task-stats-modal");
    this.contentEl.createEl("h2", { text: "\u4EFB\u52A1\u7EDF\u8BA1" });
    const filters = this.contentEl.createDiv({ cls: "cow-focus-filter-row" });
    [["week", "\u672C\u5468"], ["month", "\u672C\u6708"]].forEach(([id, label]) => {
      filters.createEl("button", { cls: this.period === id ? "is-active" : "", text: label, attr: { type: "button" } }).addEventListener("click", () => {
        this.period = id;
        this.render();
      });
    });
    const cards = this.contentEl.createDiv({ cls: "cow-stats-card-grid" });
    [
      ["\u5B8C\u6210\u7387", `${scoped.length ? Math.round(done / scoped.length * 100) : 0}%`],
      ["\u5EF6\u671F\u4EFB\u52A1", overdue],
      ["\u5B8C\u6210\u6570\u91CF", done],
      ["\u9884\u8BA1\u65F6\u95F4", `${scoped.reduce((sum, task) => {
        var _a;
        return sum + ((_a = task.estimatedMinutes) != null ? _a : 0);
      }, 0)}min`]
    ].forEach(([label, value]) => {
      const card = cards.createDiv({ cls: "cow-stats-card" });
      card.createEl("strong", { text: String(value) });
      card.createSpan({ text: String(label) });
    });
    const chart = this.contentEl.createDiv({ cls: "cow-task-mini-chart" });
    for (let offset = 6; offset >= 0; offset -= 1) {
      const date = /* @__PURE__ */ new Date();
      date.setDate(date.getDate() - offset);
      const key = formatDateKey(date);
      const count = tasks.filter((task) => task.completedDate === key).length;
      const bar = chart.createDiv({ cls: "cow-task-mini-bar" });
      bar.createDiv({ attr: { style: `height: ${Math.max(8, count * 18)}px` } });
      bar.createSpan({ text: `${date.getMonth() + 1}/${date.getDate()}` });
    }
  }
};

// src/components/finance/FinanceModals.ts
var import_obsidian34 = require("obsidian");
function today4() {
  return formatDateKey(/* @__PURE__ */ new Date());
}
function monthKey(date) {
  return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, "0")}`;
}
function currency(value) {
  return `\xA5${Math.round(value * 100) / 100}`;
}
function createInput(container, label, type, value) {
  const row = container.createDiv({ cls: `cow-finance-form-row ${type === "date" || type === "time" ? "is-picker" : ""}` });
  row.createEl("label", { text: label });
  const input = row.createEl("input", { attr: { type, value } });
  if (type === "date" || type === "time") {
    row.addEventListener("click", () => {
      var _a;
      input.focus();
      try {
        (_a = input.showPicker) == null ? void 0 : _a.call(input);
      } catch (e) {
        input.focus();
      }
    });
  }
  return input;
}
function createTextarea2(container, label, value) {
  const row = container.createDiv({ cls: "cow-finance-form-row" });
  row.createEl("label", { text: label });
  return row.createEl("textarea", { text: value });
}
function setupEditModal2(modal) {
  applyResizableModal(modal, {
    className: "cute-finance-edit-modal",
    width: "min(760px, 90vw)",
    maxWidth: "94vw",
    maxHeight: "88vh",
    minWidth: "min(420px, 90vw)",
    minHeight: "min(320px, 80vh)"
  });
}
function setupStatsModal2(modal) {
  applyResizableModal(modal, {
    className: "cute-finance-stats-modal",
    width: "min(1050px, 92vw)",
    height: "min(720px, 86vh)",
    maxWidth: "96vw",
    maxHeight: "92vh",
    minWidth: "min(620px, 92vw)",
    minHeight: "min(420px, 86vh)"
  });
}
function transactionsForMonth(store, key) {
  return store.getTransactions().filter((item) => item.date.startsWith(key));
}
function sumTransactions(transactions, type) {
  return transactions.filter((item) => item.type === type).reduce((sum, item) => sum + item.amount, 0);
}
function renderSummaryCards(container, items) {
  const grid = container.createDiv({ cls: "cow-stats-card-grid" });
  items.forEach(([label, value]) => {
    const card = grid.createDiv({ cls: "cow-stats-card" });
    card.createEl("strong", { text: String(value) });
    card.createSpan({ text: label });
  });
}
var FinanceCalendar = class {
  constructor(getDots, renderDetails) {
    this.getDots = getDots;
    this.renderDetails = renderDetails;
    this.month = /* @__PURE__ */ new Date();
    this.selected = today4();
  }
  render(container) {
    container.empty();
    const root = container.createDiv({ cls: "cow-finance-calendar-layout" });
    const calendar = root.createDiv({ cls: "cow-finance-calendar-panel" });
    const details = root.createDiv({ cls: "cow-finance-calendar-details" });
    const header = calendar.createDiv({ cls: "cow-finance-calendar-header" });
    header.createEl("button", { text: "<", attr: { type: "button" } }).addEventListener("click", () => {
      this.month = new Date(this.month.getFullYear(), this.month.getMonth() - 1, 1);
      this.render(container);
    });
    header.createEl("strong", { text: `${this.month.getFullYear()}\u5E74${this.month.getMonth() + 1}\u6708` });
    header.createEl("button", { text: ">", attr: { type: "button" } }).addEventListener("click", () => {
      this.month = new Date(this.month.getFullYear(), this.month.getMonth() + 1, 1);
      this.render(container);
    });
    header.createEl("button", { text: "\u4ECA\u5929", attr: { type: "button" } }).addEventListener("click", () => {
      this.month = /* @__PURE__ */ new Date();
      this.selected = today4();
      this.render(container);
    });
    const grid = calendar.createDiv({ cls: "cow-finance-calendar-grid" });
    ["\u4E00", "\u4E8C", "\u4E09", "\u56DB", "\u4E94", "\u516D", "\u65E5"].forEach((day) => grid.createSpan({ cls: "cow-weekday", text: day }));
    const first = new Date(this.month.getFullYear(), this.month.getMonth(), 1);
    const offset = (first.getDay() + 6) % 7;
    const days = new Date(this.month.getFullYear(), this.month.getMonth() + 1, 0).getDate();
    for (let index = 0; index < offset; index += 1) grid.createSpan({ cls: "cow-calendar-empty" });
    for (let day = 1; day <= days; day += 1) {
      const date = formatDateKey(new Date(this.month.getFullYear(), this.month.getMonth(), day));
      const button = grid.createEl("button", { cls: `cow-finance-calendar-day ${date === this.selected ? "is-selected" : ""} ${date === today4() ? "is-today" : ""}`, attr: { type: "button" } });
      button.createSpan({ text: String(day) });
      const dots = button.createDiv({ cls: "cow-calendar-dots" });
      this.getDots(date).slice(0, 4).forEach((dot) => dots.createSpan({ cls: dot }));
      button.addEventListener("click", () => {
        this.selected = date;
        this.render(container);
      });
    }
    details.createEl("h3", { text: this.selected });
    this.renderDetails(details, this.selected);
  }
};
var MonthlyFinanceSummaryModal = class extends import_obsidian34.Modal {
  constructor(app, store, onDone) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.month = /* @__PURE__ */ new Date();
  }
  onOpen() {
    setupEditModal2(this);
    this.render();
  }
  render() {
    const key = monthKey(this.month);
    const transactions = transactionsForMonth(this.store, key);
    const incomeValue = sumTransactions(transactions, "income");
    const expenseValue = sumTransactions(transactions, "expense");
    const remainingValue = this.store.getMonthlyBudgetLimit(key) - expenseValue;
    const savingRateValue = incomeValue > 0 ? Math.round((incomeValue - expenseValue) / incomeValue * 100) : 0;
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-finance-modal");
    this.contentEl.createEl("h2", { text: "\u7BA1\u7406\u6536\u652F" });
    const form = this.contentEl.createDiv({ cls: "cow-finance-form-grid" });
    const year = this.createYearSelect(form);
    const month = this.createMonthSelect(form);
    year.addEventListener("change", () => {
      this.month = new Date(Number(year.value), this.month.getMonth(), 1);
      this.render();
    });
    month.addEventListener("change", () => {
      this.month = new Date(this.month.getFullYear(), Number(month.value) - 1, 1);
      this.render();
    });
    const income = createInput(form, "\u672C\u6708\u6536\u5165\uFF08\u771F\u5B9E\u6D41\u6C34\uFF09", "number", String(incomeValue));
    const expense = createInput(form, "\u672C\u6708\u652F\u51FA\uFF08\u771F\u5B9E\u6D41\u6C34\uFF09", "number", String(expenseValue));
    const remaining = createInput(form, "\u9884\u7B97\u5269\u4F59", "number", String(remainingValue));
    const savingRate = createInput(form, "\u50A8\u84C4\u7387\uFF08\u81EA\u52A8\uFF09", "number", String(savingRateValue));
    income.disabled = true;
    expense.disabled = true;
    savingRate.disabled = true;
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58\u9884\u7B97\u989D\u5EA6", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      await this.store.setMonthlyBudgetLimit((Number(expense.value) || 0) + (Number(remaining.value) || 0), key);
      this.onDone();
      this.close();
    });
  }
  createYearSelect(container) {
    const row = container.createDiv({ cls: "cow-finance-form-row" });
    row.createEl("label", { text: "\u5E74\u4EFD" });
    const select = row.createEl("select");
    const current = (/* @__PURE__ */ new Date()).getFullYear();
    const years = /* @__PURE__ */ new Set();
    for (let year = current - 5; year <= current + 3; year += 1) years.add(year);
    this.store.getTransactions().forEach((tx) => {
      const year = Number(tx.date.slice(0, 4));
      if (Number.isFinite(year)) years.add(year);
    });
    [...years].sort((a, b) => a - b).forEach((year) => select.createEl("option", { value: String(year), text: String(year) }));
    select.value = String(this.month.getFullYear());
    return select;
  }
  createMonthSelect(container) {
    const row = container.createDiv({ cls: "cow-finance-form-row" });
    row.createEl("label", { text: "\u6708\u4EFD" });
    const select = row.createEl("select");
    for (let month = 1; month <= 12; month += 1) {
      select.createEl("option", { value: String(month), text: `${month}\u6708` });
    }
    select.value = String(this.month.getMonth() + 1);
    return select;
  }
};
var MonthlyBudgetStatisticsModal = class extends import_obsidian34.Modal {
  constructor(app, store) {
    super(app);
    this.store = store;
    this.year = (/* @__PURE__ */ new Date()).getFullYear();
  }
  onOpen() {
    setupStatsModal2(this);
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-finance-modal", "cow-finance-stats-modal");
    const header = this.contentEl.createDiv({ cls: "cow-finance-month-header" });
    header.createEl("button", { text: "\u4E0A\u4E00\u5E74", attr: { type: "button" } }).addEventListener("click", () => {
      this.year -= 1;
      this.render();
    });
    header.createEl("h2", { text: `${this.year} \u5E74\u9884\u7B97\u7EDF\u8BA1` });
    header.createEl("button", { text: "\u4E0B\u4E00\u5E74", attr: { type: "button" } }).addEventListener("click", () => {
      this.year += 1;
      this.render();
    });
    const grid = this.contentEl.createDiv({ cls: "cow-finance-year-grid" });
    for (let month = 1; month <= 12; month += 1) {
      const key = `${this.year}-${String(month).padStart(2, "0")}`;
      const tx = transactionsForMonth(this.store, key);
      const income = sumTransactions(tx, "income");
      const expense = sumTransactions(tx, "expense");
      const remaining = this.store.getMonthlyBudgetLimit(key) - expense;
      const card = grid.createDiv({ cls: "cow-data-card" });
      card.createEl("strong", { text: `${month}\u6708` });
      card.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `\u6536\u5165 ${currency(income)} \xB7 \u652F\u51FA ${currency(expense)}` });
      card.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `\u5269\u4F59 ${currency(remaining)} \xB7 \u50A8\u84C4\u7387 ${income > 0 ? Math.round((income - expense) / income * 100) : 0}%` });
    }
  }
};
function openExpenseCategoryModal(app, store, onDone, budget) {
  new ExpenseCategoryModal(app, store, onDone, budget).open();
}
var ExpenseCategoryModal = class extends import_obsidian34.Modal {
  constructor(app, store, onDone, budget) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.budget = budget;
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    setupEditModal2(this);
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-finance-modal");
    this.contentEl.createEl("h2", { text: this.budget ? "\u7F16\u8F91\u5206\u7C7B" : "\u65B0\u589E\u5206\u7C7B" });
    const category = createInput(this.contentEl, "\u5206\u7C7B\u540D\u79F0", "text", (_b = (_a = this.budget) == null ? void 0 : _a.category) != null ? _b : "");
    const amount = createInput(this.contentEl, "\u9884\u7B97\u91D1\u989D", "number", String((_d = (_c = this.budget) == null ? void 0 : _c.amount) != null ? _d : 0));
    const color = createInput(this.contentEl, "\u989C\u8272", "color", (_f = (_e = this.budget) == null ? void 0 : _e.color) != null ? _f : "#ff7fb4");
    const icon = createInput(this.contentEl, "\u56FE\u6807", "text", (_h = (_g = this.budget) == null ? void 0 : _g.icon) != null ? _h : "wallet");
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2, _c2, _d2;
      const values = {
        id: (_b2 = (_a2 = this.budget) == null ? void 0 : _a2.id) != null ? _b2 : `budget-${Date.now()}`,
        category: category.value.trim() || "\u5176\u5B83",
        amount: Number(amount.value) || 0,
        spent: (_d2 = (_c2 = this.budget) == null ? void 0 : _c2.spent) != null ? _d2 : 0,
        color: color.value,
        icon: icon.value.trim() || "wallet"
      };
      if (this.budget) await this.store.updateBudget(this.budget.id, values);
      else await this.store.addBudget(values);
      this.onDone();
      this.close();
    });
  }
};
var ExpenseCategoryStatisticsModal = class extends import_obsidian34.Modal {
  constructor(app, store) {
    super(app);
    this.store = store;
    this.month = /* @__PURE__ */ new Date();
  }
  onOpen() {
    setupStatsModal2(this);
    this.render();
  }
  render() {
    const key = monthKey(this.month);
    const expenses = transactionsForMonth(this.store, key).filter((tx) => tx.type === "expense");
    const total = sumTransactions(expenses, "expense");
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-finance-modal", "cow-finance-stats-modal");
    this.renderMonthHeader("\u652F\u51FA\u5206\u7C7B\u7EDF\u8BA1");
    const groups = /* @__PURE__ */ new Map();
    expenses.forEach((tx) => {
      var _a;
      const group = (_a = groups.get(tx.category)) != null ? _a : { amount: 0, count: 0 };
      group.amount += tx.amount;
      group.count += 1;
      groups.set(tx.category, group);
    });
    const list = this.contentEl.createDiv({ cls: "cow-data-list" });
    Array.from(groups.entries()).sort((a, b) => b[1].amount - a[1].amount).forEach(([category, value]) => {
      const row = list.createDiv({ cls: "cow-finance-category-row" });
      row.createEl("strong", { text: category });
      const percent = total > 0 ? Math.round(value.amount / total * 100) : 0;
      const track = row.createDiv({ cls: "cow-month-progress-track" });
      track.createDiv({ cls: "cow-month-progress-fill is-pink", attr: { style: `width: ${percent}%` } });
      row.createSpan({ text: `${currency(value.amount)} \xB7 ${percent}% \xB7 ${value.count}\u7B14` });
    });
  }
  renderMonthHeader(title) {
    const header = this.contentEl.createDiv({ cls: "cow-finance-month-header" });
    header.createEl("button", { text: "<", attr: { type: "button" } }).addEventListener("click", () => {
      this.month = new Date(this.month.getFullYear(), this.month.getMonth() - 1, 1);
      this.render();
    });
    header.createEl("h2", { text: `${title} \xB7 ${this.month.getFullYear()}\u5E74${this.month.getMonth() + 1}\u6708` });
    header.createEl("button", { text: ">", attr: { type: "button" } }).addEventListener("click", () => {
      this.month = new Date(this.month.getFullYear(), this.month.getMonth() + 1, 1);
      this.render();
    });
  }
};
var TransactionManagerModal = class extends import_obsidian34.Modal {
  constructor(app, store, onDone) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.filter = "";
  }
  onOpen() {
    setupStatsModal2(this);
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-finance-modal", "cow-finance-stats-modal");
    this.contentEl.createEl("h2", { text: "\u7BA1\u7406\u6536\u652F\u8BB0\u5F55" });
    const filter = createInput(this.contentEl, "\u7B5B\u9009", "text", this.filter);
    filter.addEventListener("input", () => {
      this.filter = filter.value;
      this.render();
    });
    const list = this.contentEl.createDiv({ cls: "cow-data-list" });
    this.store.getTransactions().filter((tx) => `${tx.date} ${tx.category} ${tx.note}`.includes(this.filter)).slice().reverse().forEach((tx) => this.renderTransactionRow(list, tx));
  }
  renderTransactionRow(container, tx) {
    const row = container.createDiv({ cls: "cow-data-card" });
    const head = row.createDiv({ cls: "cow-list-item-head" });
    const body = head.createDiv();
    body.createEl("strong", { text: `${tx.type === "income" ? "+" : "-"}${currency(tx.amount)} \xB7 ${tx.category}` });
    const account = tx.accountId ? this.store.getAccounts().find((item) => item.id === tx.accountId) : void 0;
    body.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${tx.date} \xB7 ${accountLabel(account)} \xB7 ${tx.note || "\u65E0\u5907\u6CE8"}` });
    const actions = head.createDiv({ cls: "cow-list-item-actions" });
    const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91" } });
    (0, import_obsidian34.setIcon)(edit, "pencil");
    edit.addEventListener("click", () => new AddTransactionModal(this.app, async (values) => {
      await this.store.updateTransaction(tx.id, values);
      this.onDone();
      this.render();
    }, tx, this.store.getBudgets(), this.store.getAccounts()).open());
    const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664" } });
    (0, import_obsidian34.setIcon)(remove, "trash-2");
    remove.addEventListener("click", async () => {
      await this.store.deleteTransaction(tx.id);
      this.onDone();
      this.render();
    });
  }
};
var IncomeExpenseStatisticsModal = class extends import_obsidian34.Modal {
  constructor(app, store) {
    super(app);
    this.store = store;
    this.period = "month";
    this.month = /* @__PURE__ */ new Date();
  }
  onOpen() {
    setupStatsModal2(this);
    this.render();
  }
  render() {
    const key = monthKey(this.month);
    let tx = this.store.getTransactions();
    if (this.period === "month") tx = transactionsForMonth(this.store, key);
    if (this.period === "year") tx = tx.filter((item) => item.date.startsWith(String(this.month.getFullYear())));
    if (this.period === "week") {
      const week = new Set(this.store.getCurrentWeekDates());
      tx = tx.filter((item) => week.has(item.date));
    }
    const income = sumTransactions(tx, "income");
    const expense = sumTransactions(tx, "expense");
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-finance-modal", "cow-finance-stats-modal");
    this.contentEl.createEl("h2", { text: "\u6536\u652F\u8D8B\u52BF\u7EDF\u8BA1" });
    const filters = this.contentEl.createDiv({ cls: "cow-focus-filter-row" });
    [["week", "\u672C\u5468"], ["month", "\u672C\u6708"], ["year", "\u672C\u5E74"]].forEach(([id, label]) => {
      filters.createEl("button", { cls: this.period === id ? "is-active" : "", text: label, attr: { type: "button" } }).addEventListener("click", () => {
        this.period = id;
        this.render();
      });
    });
    renderSummaryCards(this.contentEl, [["\u6536\u5165", currency(income)], ["\u652F\u51FA", currency(expense)], ["\u51C0\u7ED3\u4F59", currency(income - expense)]]);
    const list = this.contentEl.createDiv({ cls: "cow-data-list" });
    tx.slice().sort((a, b) => a.date.localeCompare(b.date)).forEach((item) => {
      list.createDiv({ cls: "cow-data-card" }).createEl("strong", { text: `${item.date} \xB7 ${item.type === "income" ? "+" : "-"}${currency(item.amount)} \xB7 ${item.category}` });
    });
  }
};
function openFinanceTodoModal(app, store, onDone, todo) {
  new FinanceTodoModal(app, store, onDone, todo).open();
}
var FinanceTodoModal = class extends import_obsidian34.Modal {
  constructor(app, store, onDone, todo) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.todo = todo;
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    setupEditModal2(this);
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-finance-modal");
    this.contentEl.createEl("h2", { text: this.todo ? "\u7F16\u8F91\u8BB0\u8D26\u5F85\u529E" : "\u65B0\u5EFA\u8BB0\u8D26\u5F85\u529E" });
    const title = createInput(this.contentEl, "\u6807\u9898", "text", (_b = (_a = this.todo) == null ? void 0 : _a.title) != null ? _b : "");
    const date = createInput(this.contentEl, "\u65E5\u671F", "date", (_d = (_c = this.todo) == null ? void 0 : _c.date) != null ? _d : today4());
    const note = createTextarea2(this.contentEl, "\u5907\u6CE8", (_f = (_e = this.todo) == null ? void 0 : _e.note) != null ? _f : "");
    const completedRow = this.contentEl.createDiv({ cls: "cow-finance-form-row is-inline" });
    const completed = completedRow.createEl("input", { attr: { type: "checkbox" } });
    completed.checked = (_h = (_g = this.todo) == null ? void 0 : _g.completed) != null ? _h : false;
    completedRow.createEl("label", { text: "\u5DF2\u5B8C\u6210" });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2, _c2, _d2;
      const values = {
        id: (_b2 = (_a2 = this.todo) == null ? void 0 : _a2.id) != null ? _b2 : `finance-todo-${Date.now()}`,
        title: title.value.trim() || "\u7406\u8D22\u5F85\u529E",
        date: date.value || today4(),
        note: note.value.trim(),
        completed: completed.checked,
        createdAt: (_d2 = (_c2 = this.todo) == null ? void 0 : _c2.createdAt) != null ? _d2 : (/* @__PURE__ */ new Date()).toISOString(),
        updatedAt: (/* @__PURE__ */ new Date()).toISOString()
      };
      if (this.todo) await this.store.updateFinanceTodo(this.todo.id, values);
      else await this.store.addFinanceTodo(values);
      this.onDone();
      this.close();
    });
  }
};
var FinanceTodoStatisticsModal = class extends import_obsidian34.Modal {
  constructor(app, store, onDone) {
    super(app);
    this.store = store;
    this.onDone = onDone;
  }
  onOpen() {
    setupStatsModal2(this);
    this.render();
  }
  render() {
    const todos = this.store.getFinanceTodos();
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-finance-modal", "cow-finance-stats-modal");
    this.contentEl.createEl("h2", { text: "\u8BB0\u8D26\u5F85\u529E\u7EDF\u8BA1" });
    renderSummaryCards(this.contentEl, [["\u603B\u5F85\u529E", todos.length], ["\u5DF2\u5B8C\u6210", todos.filter((item) => item.completed).length], ["\u672A\u5B8C\u6210", todos.filter((item) => !item.completed).length], ["\u5B8C\u6210\u7387", `${todos.length ? Math.round(todos.filter((item) => item.completed).length / todos.length * 100) : 0}%`]]);
    new FinanceCalendar(
      (date) => todos.some((todo) => {
        var _a;
        return ((_a = todo.date) != null ? _a : today4()) === date;
      }) ? ["is-task"] : [],
      (container, date) => this.renderTodoDetails(container, date)
    ).render(this.contentEl.createDiv());
  }
  renderTodoDetails(container, date) {
    this.store.getFinanceTodos().filter((todo) => {
      var _a;
      return ((_a = todo.date) != null ? _a : today4()) === date;
    }).forEach((todo) => {
      const row = container.createDiv({ cls: "cow-data-card" });
      row.createEl("strong", { text: `${todo.completed ? "\u2713 " : ""}${todo.title}` });
      if (todo.note) row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: todo.note });
      const actions = row.createDiv({ cls: "cow-list-item-actions" });
      actions.createEl("button", { text: "\u7F16\u8F91", attr: { type: "button" } }).addEventListener("click", () => openFinanceTodoModal(this.app, this.store, () => {
        this.onDone();
        this.render();
      }, todo));
      actions.createEl("button", { text: todo.completed ? "\u53D6\u6D88\u5B8C\u6210" : "\u5B8C\u6210", attr: { type: "button" } }).addEventListener("click", async () => {
        await this.store.updateFinanceTodo(todo.id, { completed: !todo.completed });
        this.onDone();
        this.render();
      });
      actions.createEl("button", { text: "\u5220\u9664", cls: "mod-warning", attr: { type: "button" } }).addEventListener("click", async () => {
        await this.store.deleteFinanceTodo(todo.id);
        this.onDone();
        this.render();
      });
    });
  }
};
function openInvestmentModal(app, store, onDone, item) {
  new InvestmentModal(app, store, onDone, item).open();
}
var InvestmentModal = class extends import_obsidian34.Modal {
  constructor(app, store, onDone, item) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.item = item;
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l;
    setupEditModal2(this);
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-finance-modal");
    this.contentEl.createEl("h2", { text: this.item ? "\u7F16\u8F91\u6295\u8D44\u89C2\u5BDF" : "\u65B0\u589E\u6295\u8D44\u89C2\u5BDF" });
    const name = createInput(this.contentEl, "\u540D\u79F0", "text", (_b = (_a = this.item) == null ? void 0 : _a.name) != null ? _b : "");
    const code = createInput(this.contentEl, "\u4EE3\u7801", "text", (_d = (_c = this.item) == null ? void 0 : _c.code) != null ? _d : "");
    const price = createInput(this.contentEl, "\u5F53\u524D\u4EF7\u683C", "number", String((_f = (_e = this.item) == null ? void 0 : _e.price) != null ? _f : 0));
    const change = createInput(this.contentEl, "\u53D8\u5316\u767E\u5206\u6BD4", "number", String((_h = (_g = this.item) == null ? void 0 : _g.changePercent) != null ? _h : 0));
    const type = createInput(this.contentEl, "\u7C7B\u578B", "text", (_j = (_i = this.item) == null ? void 0 : _i.type) != null ? _j : "");
    const note = createTextarea2(this.contentEl, "\u5907\u6CE8", (_l = (_k = this.item) == null ? void 0 : _k.note) != null ? _l : "");
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2;
      const values = {
        id: (_b2 = (_a2 = this.item) == null ? void 0 : _a2.id) != null ? _b2 : `watch-${Date.now()}`,
        name: name.value.trim() || "\u6295\u8D44\u89C2\u5BDF",
        code: code.value.trim(),
        price: Number(price.value) || 0,
        changePercent: Number(change.value) || 0,
        type: type.value.trim() || "\u5176\u5B83",
        note: note.value.trim()
      };
      if (this.item) await this.store.updateInvestmentWatchItem(this.item.id, values);
      else await this.store.addInvestmentWatchItem(values);
      this.onDone();
      this.close();
    });
  }
};
var InvestmentStatisticsModal = class extends import_obsidian34.Modal {
  constructor(app, store) {
    super(app);
    this.store = store;
  }
  onOpen() {
    setupStatsModal2(this);
    this.render();
  }
  render() {
    const snapshots = this.store.getInvestmentSnapshots();
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-finance-modal", "cow-finance-stats-modal");
    this.contentEl.createEl("h2", { text: "\u6295\u8D44\u89C2\u5BDF\u7EDF\u8BA1" });
    new FinanceCalendar(
      (date) => snapshots.some((snapshot) => snapshot.date === date) ? ["is-note"] : [],
      (container, date) => {
        snapshots.filter((snapshot) => snapshot.date === date).forEach((snapshot) => {
          var _a;
          const item = this.store.getInvestmentWatchItems().find((watch) => watch.id === snapshot.investmentId);
          const row = container.createDiv({ cls: "cow-data-card" });
          row.createEl("strong", { text: (_a = item == null ? void 0 : item.name) != null ? _a : "\u6295\u8D44\u89C2\u5BDF" });
          row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${currency(snapshot.price)} \xB7 ${snapshot.changePercent}% \xB7 ${snapshot.note || "\u65E0\u5907\u6CE8"}` });
        });
      }
    ).render(this.contentEl.createDiv());
  }
};
var TransactionStatisticsModal = class extends import_obsidian34.Modal {
  constructor(app, store, onDone) {
    super(app);
    this.store = store;
    this.onDone = onDone;
  }
  onOpen() {
    setupStatsModal2(this);
    this.render();
  }
  render() {
    const key = monthKey(/* @__PURE__ */ new Date());
    const tx = transactionsForMonth(this.store, key);
    const income = sumTransactions(tx, "income");
    const expense = sumTransactions(tx, "expense");
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-finance-modal", "cow-finance-stats-modal");
    this.contentEl.createEl("h2", { text: "\u8BB0\u8D26\u7EDF\u8BA1" });
    renderSummaryCards(this.contentEl, [["\u672C\u6708\u6536\u5165", currency(income)], ["\u672C\u6708\u652F\u51FA", currency(expense)], ["\u672C\u6708\u51C0\u7ED3\u4F59", currency(income - expense)], ["\u672C\u6708\u8BB0\u8D26\u7B14\u6570", tx.length]]);
    new FinanceCalendar(
      (date) => {
        const items = this.store.getTransactions().filter((item) => item.date === date);
        const dots = items.map((item) => item.type === "income" ? "is-task-done" : "is-task");
        return dots.length > 4 ? [...dots.slice(0, 4), "is-more"] : dots;
      },
      (container, date) => this.renderDayTransactions(container, date)
    ).render(this.contentEl.createDiv());
  }
  renderDayTransactions(container, date) {
    const tx = this.store.getTransactions().filter((item) => item.date === date);
    const income = sumTransactions(tx, "income");
    const expense = sumTransactions(tx, "expense");
    renderSummaryCards(container, [["\u5F53\u65E5\u6536\u5165", currency(income)], ["\u5F53\u65E5\u652F\u51FA", currency(expense)], ["\u51C0\u91D1\u989D", currency(income - expense)], ["\u8BB0\u8D26\u7B14\u6570", tx.length]]);
    tx.forEach((item) => {
      const account = item.accountId ? this.store.getAccounts().find((entry) => entry.id === item.accountId) : void 0;
      const row = container.createDiv({ cls: "cow-data-card" });
      const head = row.createDiv({ cls: "cow-list-item-head" });
      const body = head.createDiv();
      body.createEl("strong", { text: `${item.type === "income" ? "+" : "-"}${currency(item.amount)} \xB7 ${item.category}` });
      body.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${accountLabel(account)} \xB7 ${item.note || "\u65E0\u5907\u6CE8"}` });
      const actions = head.createDiv({ cls: "cow-list-item-actions" });
      actions.createEl("button", { text: "\u7F16\u8F91", attr: { type: "button" } }).addEventListener("click", () => new AddTransactionModal(this.app, async (values) => {
        await this.store.updateTransaction(item.id, values);
        this.onDone();
        this.render();
      }, item, this.store.getBudgets(), this.store.getAccounts()).open());
      actions.createEl("button", { text: "\u5220\u9664", cls: "mod-warning", attr: { type: "button" } }).addEventListener("click", async () => {
        await this.store.deleteTransaction(item.id);
        this.onDone();
        this.render();
      });
    });
  }
};

// src/components/goals/GoalActionModals.ts
var import_obsidian35 = require("obsidian");
var GOAL_QUADRANTS = [
  { id: "important-urgent", label: "\u91CD\u8981\u4E14\u7D27\u6025", importance: "important", urgency: "urgent" },
  { id: "important-not-urgent", label: "\u91CD\u8981\u4E0D\u7D27\u6025", importance: "important", urgency: "not-urgent" },
  { id: "not-important-urgent", label: "\u4E0D\u91CD\u8981\u4F46\u7D27\u6025", importance: "not-important", urgency: "urgent" },
  { id: "not-important-not-urgent", label: "\u4E0D\u91CD\u8981\u4E0D\u7D27\u6025", importance: "not-important", urgency: "not-urgent" }
];
function today5() {
  return formatDateKey(/* @__PURE__ */ new Date());
}
function setupEditModal3(modal) {
  applyResizableModal(modal, {
    className: "cute-goal-edit-modal",
    width: "min(760px, 90vw)",
    maxWidth: "94vw",
    maxHeight: "88vh",
    minWidth: "min(420px, 90vw)",
    minHeight: "min(320px, 80vh)"
  });
}
function setupStatsModal3(modal) {
  applyResizableModal(modal, {
    className: "cute-goal-stats-modal",
    width: "min(860px, 92vw)",
    height: "min(680px, 86vh)",
    maxWidth: "96vw",
    maxHeight: "92vh",
    minWidth: "min(620px, 92vw)",
    minHeight: "min(420px, 86vh)"
  });
}
function createInput2(container, label, type, value) {
  const row = container.createDiv({ cls: `cow-goal-form-row ${type === "date" ? "is-picker" : ""}` });
  row.createEl("label", { text: label });
  const input = row.createEl("input", { attr: { type, value } });
  if (type === "date") {
    row.addEventListener("click", () => {
      var _a;
      input.focus();
      try {
        (_a = input.showPicker) == null ? void 0 : _a.call(input);
      } catch (e) {
        input.focus();
      }
    });
  }
  return input;
}
function createTextarea3(container, label, value) {
  const row = container.createDiv({ cls: "cow-goal-form-row" });
  row.createEl("label", { text: label });
  return row.createEl("textarea", { text: value });
}
function createSelect2(container, label, value, options) {
  const row = container.createDiv({ cls: "cow-goal-form-row" });
  row.createEl("label", { text: label });
  const select = row.createEl("select");
  options.forEach((option) => select.createEl("option", { value: option.value, text: option.label }));
  select.value = value;
  return select;
}
function getQuadrant(action) {
  var _a;
  const match = GOAL_QUADRANTS.find((quadrant) => quadrant.importance === action.importance && quadrant.urgency === action.urgency);
  return (_a = match == null ? void 0 : match.id) != null ? _a : "";
}
function applyQuadrant(action, quadrantId) {
  const quadrant = GOAL_QUADRANTS.find((item) => item.id === quadrantId);
  action.importance = quadrant == null ? void 0 : quadrant.importance;
  action.urgency = quadrant == null ? void 0 : quadrant.urgency;
}
function openGoalActionModal(app, store, onDone, action, preset) {
  new GoalActionModal(app, store, onDone, action, preset).open();
}
var GoalActionModal = class extends import_obsidian35.Modal {
  constructor(app, store, onDone, action, preset = {}) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.action = action;
    this.preset = preset;
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x, _y, _z, _A, _B, _C, _D, _E, _F, _G, _H, _I, _J, _K;
    setupEditModal3(this);
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-goal-modal");
    this.contentEl.createEl("h2", { text: this.action ? "\u7F16\u8F91\u76EE\u6807\u4EFB\u52A1" : "\u65B0\u589E\u76EE\u6807\u4EFB\u52A1" });
    const goals = this.store.getGoals();
    if (goals.length === 0) {
      this.contentEl.createEl("p", { cls: "cow-empty-state", text: "\u8BF7\u5148\u521B\u5EFA\u5E74\u5EA6\u76EE\u6807\u3002" });
      return;
    }
    const goalId = createSelect2(this.contentEl, "\u6240\u5C5E\u76EE\u6807", (_c = (_b = (_a = this.action) == null ? void 0 : _a.goalId) != null ? _b : this.preset.goalId) != null ? _c : goals[0].id, goals.map((goal) => ({ value: goal.id, label: goal.title })));
    const parentOptions = [{ value: "", label: "\u65E0\u7236\u7EA7" }, ...this.store.getGoalActions().filter((item) => {
      var _a2;
      return item.id !== ((_a2 = this.action) == null ? void 0 : _a2.id);
    }).map((item) => ({ value: item.id, label: item.title }))];
    const parentId = createSelect2(this.contentEl, "\u7236\u7EA7\u62C6\u89E3\u8282\u70B9", (_f = (_e = (_d = this.action) == null ? void 0 : _d.parentId) != null ? _e : this.preset.parentId) != null ? _f : "", parentOptions);
    const title = createInput2(this.contentEl, "\u6807\u9898", "text", (_i = (_h = (_g = this.action) == null ? void 0 : _g.title) != null ? _h : this.preset.title) != null ? _i : "");
    const description = createTextarea3(this.contentEl, "\u63CF\u8FF0", (_l = (_k = (_j = this.action) == null ? void 0 : _j.description) != null ? _k : this.preset.description) != null ? _l : "");
    const progress = createInput2(this.contentEl, "\u8FDB\u5EA6", "number", String((_o = (_n = (_m = this.action) == null ? void 0 : _m.progress) != null ? _n : this.preset.progress) != null ? _o : 0));
    progress.min = "0";
    progress.max = "100";
    const progressMode = createSelect2(this.contentEl, "\u8FDB\u5EA6\u6A21\u5F0F", (_r = (_q = (_p = this.action) == null ? void 0 : _p.progressMode) != null ? _q : this.preset.progressMode) != null ? _r : "auto", [
      { value: "auto", label: "\u81EA\u52A8\u8FDB\u5EA6" },
      { value: "manual", label: "\u624B\u52A8\u8FDB\u5EA6" }
    ]);
    const status = createSelect2(this.contentEl, "\u72B6\u6001", (_u = (_t = (_s = this.action) == null ? void 0 : _s.status) != null ? _t : this.preset.status) != null ? _u : "todo", [
      { value: "todo", label: "\u672A\u5F00\u59CB" },
      { value: "in-progress", label: "\u8FDB\u884C\u4E2D" },
      { value: "completed", label: "\u5DF2\u5B8C\u6210" },
      { value: "overdue", label: "\u5DF2\u903E\u671F" }
    ]);
    const startDate = createInput2(this.contentEl, "\u5F00\u59CB\u65E5\u671F", "date", (_x = (_w = (_v = this.action) == null ? void 0 : _v.startDate) != null ? _w : this.preset.startDate) != null ? _x : today5());
    const deadline = createInput2(this.contentEl, "\u622A\u6B62\u65E5\u671F", "date", (_A = (_z = (_y = this.action) == null ? void 0 : _y.deadline) != null ? _z : this.preset.deadline) != null ? _A : today5());
    const milestoneRow = this.contentEl.createDiv({ cls: "cow-goal-form-row is-inline" });
    const isMilestone = milestoneRow.createEl("input", { attr: { type: "checkbox" } });
    isMilestone.checked = (_D = (_C = (_B = this.action) == null ? void 0 : _B.isMilestone) != null ? _C : this.preset.isMilestone) != null ? _D : false;
    milestoneRow.createEl("label", { text: "\u8BBE\u4E3A\u91CC\u7A0B\u7891" });
    const milestoneDate = createInput2(this.contentEl, "\u91CC\u7A0B\u7891\u65E5\u671F", "date", (_G = (_F = (_E = this.action) == null ? void 0 : _E.milestoneDate) != null ? _F : this.preset.milestoneDate) != null ? _G : deadline.value);
    const quadrant = createSelect2(this.contentEl, "\u4F18\u5148\u7EA7\u8C61\u9650", getQuadrant((_H = this.action) != null ? _H : this.preset), [{ value: "", label: "\u6682\u4E0D\u8FDB\u5165\u77E9\u9635" }, ...GOAL_QUADRANTS.map((item) => ({ value: item.id, label: item.label }))]);
    const note = createTextarea3(this.contentEl, "\u5907\u6CE8", (_K = (_J = (_I = this.action) == null ? void 0 : _I.note) != null ? _J : this.preset.note) != null ? _K : "");
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2, _c2, _d2, _e2, _f2, _g2, _h2;
      if (!title.value.trim()) {
        new import_obsidian35.Notice("\u8BF7\u8F93\u5165\u4EFB\u52A1\u6807\u9898\u3002");
        return;
      }
      const values = {
        id: (_b2 = (_a2 = this.action) == null ? void 0 : _a2.id) != null ? _b2 : `goal-action-${Date.now()}`,
        goalId: goalId.value,
        parentId: parentId.value || void 0,
        title: title.value.trim(),
        description: description.value.trim(),
        status: status.value,
        startDate: startDate.value || void 0,
        deadline: deadline.value || void 0,
        completedDate: status.value === "completed" ? (_d2 = (_c2 = this.action) == null ? void 0 : _c2.completedDate) != null ? _d2 : today5() : void 0,
        progress: Math.max(0, Math.min(100, Number(progress.value) || 0)),
        progressMode: progressMode.value,
        isMilestone: isMilestone.checked,
        milestoneDate: isMilestone.checked ? milestoneDate.value || deadline.value || today5() : void 0,
        note: note.value.trim(),
        collapsed: (_f2 = (_e2 = this.action) == null ? void 0 : _e2.collapsed) != null ? _f2 : false,
        createdAt: (_h2 = (_g2 = this.action) == null ? void 0 : _g2.createdAt) != null ? _h2 : Date.now(),
        updatedAt: Date.now()
      };
      applyQuadrant(values, quadrant.value);
      if (this.action) await this.store.updateGoalAction(this.action.id, values);
      else await this.store.addGoalAction(values);
      this.onDone();
      this.close();
    });
  }
};
function openAnnualGoalModal(app, store, onDone, goal) {
  new AnnualGoalModal(app, store, onDone, goal).open();
}
var AnnualGoalModal = class extends import_obsidian35.Modal {
  constructor(app, store, onDone, goal) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.goal = goal;
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n;
    setupEditModal3(this);
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-goal-modal");
    this.contentEl.createEl("h2", { text: this.goal ? "\u7F16\u8F91\u5E74\u5EA6\u76EE\u6807" : "\u65B0\u589E\u5E74\u5EA6\u76EE\u6807" });
    const title = createInput2(this.contentEl, "\u6807\u9898", "text", (_b = (_a = this.goal) == null ? void 0 : _a.title) != null ? _b : "");
    const description = createTextarea3(this.contentEl, "\u63CF\u8FF0", (_d = (_c = this.goal) == null ? void 0 : _c.description) != null ? _d : "");
    const category = createInput2(this.contentEl, "\u5206\u7C7B", "text", (_f = (_e = this.goal) == null ? void 0 : _e.category) != null ? _f : "\u4E2A\u4EBA");
    const progress = createInput2(this.contentEl, "\u8FDB\u5EA6", "number", String((_h = (_g = this.goal) == null ? void 0 : _g.progress) != null ? _h : 0));
    const startDate = createInput2(this.contentEl, "\u5F00\u59CB\u65E5\u671F", "date", (_j = (_i = this.goal) == null ? void 0 : _i.startDate) != null ? _j : today5());
    const deadline = createInput2(this.contentEl, "\u622A\u6B62\u65E5\u671F", "date", (_l = (_k = this.goal) == null ? void 0 : _k.deadline) != null ? _l : today5());
    const status = createSelect2(this.contentEl, "\u72B6\u6001", (_n = (_m = this.goal) == null ? void 0 : _m.status) != null ? _n : "\u8FDB\u884C\u4E2D", ["\u672A\u5F00\u59CB", "\u8FDB\u884C\u4E2D", "\u5DF2\u5B8C\u6210", "\u6682\u505C"].map((item) => ({ value: item, label: item })));
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2, _c2, _d2;
      const values = {
        id: (_b2 = (_a2 = this.goal) == null ? void 0 : _a2.id) != null ? _b2 : `goal-${Date.now()}`,
        title: title.value.trim() || "\u5E74\u5EA6\u76EE\u6807",
        description: description.value.trim(),
        category: category.value.trim() || "\u4E2A\u4EBA",
        progress: Math.max(0, Math.min(100, Number(progress.value) || 0)),
        startDate: startDate.value,
        deadline: deadline.value,
        status: status.value,
        completedDate: status.value === "\u5DF2\u5B8C\u6210" ? (_d2 = (_c2 = this.goal) == null ? void 0 : _c2.completedDate) != null ? _d2 : today5() : void 0,
        updatedAt: Date.now()
      };
      if (this.goal) await this.store.updateGoal(this.goal.id, values);
      else await this.store.addGoal(values);
      this.onDone();
      this.close();
    });
  }
};
function openRiskModal(app, store, onDone, risk) {
  new GoalRiskModal(app, store, onDone, risk).open();
}
function openQuarterlyGoalModal(app, store, onDone, objective) {
  new QuarterlyGoalModal(app, store, onDone, objective).open();
}
var QuarterlyGoalModal = class extends import_obsidian35.Modal {
  constructor(app, store, onDone, objective) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.objective = objective;
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
    setupEditModal3(this);
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-goal-modal");
    this.contentEl.createEl("h2", { text: this.objective ? "\u7F16\u8F91\u5B63\u5EA6\u76EE\u6807" : "\u65B0\u589E\u5B63\u5EA6\u76EE\u6807" });
    const currentYear = (/* @__PURE__ */ new Date()).getFullYear();
    const title = createInput2(this.contentEl, "\u6807\u9898", "text", (_b = (_a = this.objective) == null ? void 0 : _a.title) != null ? _b : "");
    const year = createSelect2(this.contentEl, "\u5E74\u4EFD", String((_d = (_c = this.objective) == null ? void 0 : _c.year) != null ? _d : currentYear), getYearOptions((_f = (_e = this.objective) == null ? void 0 : _e.year) != null ? _f : currentYear).map((value) => ({ value: String(value), label: String(value) })));
    const quarterNumber = createSelect2(this.contentEl, "\u5B63\u5EA6", String((_h = (_g = this.objective) == null ? void 0 : _g.quarterNumber) != null ? _h : Math.floor((/* @__PURE__ */ new Date()).getMonth() / 3) + 1), [1, 2, 3, 4].map((value) => ({ value: String(value), label: `Q${value}` })));
    const progress = createInput2(this.contentEl, "\u8FDB\u5EA6", "number", String((_j = (_i = this.objective) == null ? void 0 : _i.progress) != null ? _j : 0));
    progress.min = "0";
    progress.max = "100";
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2, _c2;
      const selectedYear = Number(year.value);
      const selectedQuarter = Number(quarterNumber.value);
      const values = {
        id: (_b2 = (_a2 = this.objective) == null ? void 0 : _a2.id) != null ? _b2 : `objective-${Date.now()}`,
        title: title.value.trim() || "\u5B63\u5EA6\u76EE\u6807",
        quarter: `${selectedYear} Q${selectedQuarter}`,
        year: selectedYear,
        quarterNumber: selectedQuarter,
        progress: Math.max(0, Math.min(100, Number(progress.value) || 0)),
        completedDate: (_c2 = this.objective) == null ? void 0 : _c2.completedDate,
        updatedAt: Date.now()
      };
      if (this.objective) await this.store.updateObjective(this.objective.id, values);
      else await this.store.addObjective(values);
      this.onDone();
      this.close();
    });
  }
};
function openMonthlyGoalModal(app, store, onDone, keyResult) {
  new MonthlyGoalModal(app, store, onDone, keyResult).open();
}
var MonthlyGoalModal = class extends import_obsidian35.Modal {
  constructor(app, store, onDone, keyResult) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.keyResult = keyResult;
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p;
    setupEditModal3(this);
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-goal-modal");
    this.contentEl.createEl("h2", { text: this.keyResult ? "\u7F16\u8F91\u6708\u5EA6\u76EE\u6807" : "\u65B0\u589E\u6708\u5EA6\u76EE\u6807" });
    const now = /* @__PURE__ */ new Date();
    const objectives = this.store.getObjectives();
    const objectiveId = createSelect2(this.contentEl, "\u5173\u8054\u5B63\u5EA6\u76EE\u6807", (_d = (_c = (_a = this.keyResult) == null ? void 0 : _a.objectiveId) != null ? _c : (_b = objectives[0]) == null ? void 0 : _b.id) != null ? _d : "", [{ value: "", label: "\u6682\u4E0D\u5173\u8054" }, ...objectives.map((objective) => ({ value: objective.id, label: `${objective.quarter} \xB7 ${objective.title}` }))]);
    const title = createInput2(this.contentEl, "\u6807\u9898", "text", (_f = (_e = this.keyResult) == null ? void 0 : _e.title) != null ? _f : "");
    const year = createSelect2(this.contentEl, "\u5E74\u4EFD", String((_h = (_g = this.keyResult) == null ? void 0 : _g.year) != null ? _h : now.getFullYear()), getYearOptions((_j = (_i = this.keyResult) == null ? void 0 : _i.year) != null ? _j : now.getFullYear()).map((value) => ({ value: String(value), label: String(value) })));
    const month = createSelect2(this.contentEl, "\u6708\u4EFD", String((_l = (_k = this.keyResult) == null ? void 0 : _k.month) != null ? _l : now.getMonth() + 1), Array.from({ length: 12 }, (_, index) => ({ value: String(index + 1), label: `${index + 1}\u6708` })));
    const progress = createInput2(this.contentEl, "\u8FDB\u5EA6", "number", String((_n = (_m = this.keyResult) == null ? void 0 : _m.progress) != null ? _n : 0));
    progress.min = "0";
    progress.max = "100";
    const completedRow = this.contentEl.createDiv({ cls: "cow-goal-form-row is-inline" });
    const completed = completedRow.createEl("input", { attr: { type: "checkbox" } });
    completed.checked = (_p = (_o = this.keyResult) == null ? void 0 : _o.completed) != null ? _p : false;
    completedRow.createEl("label", { text: "\u5DF2\u5B8C\u6210" });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2, _c2;
      const progressValue = Math.max(0, Math.min(100, Number(progress.value) || 0));
      const values = {
        id: (_b2 = (_a2 = this.keyResult) == null ? void 0 : _a2.id) != null ? _b2 : `kr-${Date.now()}`,
        objectiveId: objectiveId.value,
        title: title.value.trim() || "\u6708\u5EA6\u76EE\u6807",
        year: Number(year.value),
        month: Number(month.value),
        progress: completed.checked ? 100 : progressValue,
        completed: completed.checked || progressValue >= 100,
        completedDate: (_c2 = this.keyResult) == null ? void 0 : _c2.completedDate,
        updatedAt: Date.now()
      };
      if (this.keyResult) await this.store.updateKeyResult(this.keyResult.id, values);
      else await this.store.addKeyResult(values);
      this.onDone();
      this.close();
    });
  }
};
var GoalRiskModal = class extends import_obsidian35.Modal {
  constructor(app, store, onDone, risk) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.risk = risk;
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n;
    setupEditModal3(this);
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-goal-modal");
    this.contentEl.createEl("h2", { text: this.risk ? "\u7F16\u8F91\u98CE\u9669" : "\u65B0\u589E\u98CE\u9669" });
    const title = createInput2(this.contentEl, "\u540D\u79F0", "text", (_b = (_a = this.risk) == null ? void 0 : _a.title) != null ? _b : "");
    const goalOptions = [{ value: "", label: "\u4E0D\u5173\u8054\u76EE\u6807" }, ...this.store.getGoals().map((goal) => ({ value: goal.id, label: goal.title }))];
    const goalId = createSelect2(this.contentEl, "\u5173\u8054\u76EE\u6807", (_d = (_c = this.risk) == null ? void 0 : _c.goalId) != null ? _d : "", goalOptions);
    const level = createSelect2(this.contentEl, "\u4E25\u91CD\u7A0B\u5EA6", (_f = (_e = this.risk) == null ? void 0 : _e.level) != null ? _f : "medium", [{ value: "high", label: "\u9AD8" }, { value: "medium", label: "\u4E2D" }, { value: "low", label: "\u4F4E" }]);
    const status = createSelect2(this.contentEl, "\u72B6\u6001", (_h = (_g = this.risk) == null ? void 0 : _g.status) != null ? _h : "todo", [{ value: "todo", label: "\u5F85\u5904\u7406" }, { value: "in-progress", label: "\u5904\u7406\u4E2D" }, { value: "resolved", label: "\u5DF2\u89E3\u51B3" }]);
    const discoveredDate = createInput2(this.contentEl, "\u53D1\u73B0\u65E5\u671F", "date", (_j = (_i = this.risk) == null ? void 0 : _i.discoveredDate) != null ? _j : today5());
    const resolvedDate = createInput2(this.contentEl, "\u89E3\u51B3\u65E5\u671F", "date", (_l = (_k = this.risk) == null ? void 0 : _k.resolvedDate) != null ? _l : today5());
    const solution = createTextarea3(this.contentEl, "\u89E3\u51B3\u65B9\u6848 / \u5907\u6CE8", (_n = (_m = this.risk) == null ? void 0 : _m.solution) != null ? _n : "");
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2;
      const values = {
        id: (_b2 = (_a2 = this.risk) == null ? void 0 : _a2.id) != null ? _b2 : `risk-${Date.now()}`,
        title: title.value.trim() || "\u98CE\u9669",
        goalId: goalId.value || void 0,
        level: level.value,
        status: status.value,
        discoveredDate: discoveredDate.value,
        resolvedDate: status.value === "resolved" ? resolvedDate.value : void 0,
        solution: solution.value.trim(),
        note: solution.value.trim()
      };
      if (this.risk) await this.store.updateRisk(this.risk.id, values);
      else await this.store.addRisk(values);
      this.onDone();
      this.close();
    });
  }
};
var AnnualGoalStatisticsModal = class extends import_obsidian35.Modal {
  constructor(app, store) {
    super(app);
    this.store = store;
    this.year = (/* @__PURE__ */ new Date()).getFullYear();
  }
  onOpen() {
    setupStatsModal3(this);
    this.render();
  }
  render() {
    const goals = this.store.getGoals().filter((goal) => (goal.deadline || "").startsWith(String(this.year)) || (goal.startDate || "").startsWith(String(this.year)));
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-goal-modal", "cow-goal-stats-modal");
    const content = renderGoalStatisticsLayout(this.contentEl, "\u5E74\u5EA6\u76EE\u6807\u7EDF\u8BA1", [
      {
        label: "\u4E0A\u4E00\u5E74",
        onClick: () => {
          this.year -= 1;
          this.render();
        }
      },
      { label: String(this.year), active: true },
      {
        label: "\u4E0B\u4E00\u5E74",
        onClick: () => {
          this.year += 1;
          this.render();
        }
      }
    ], (toolbar) => {
      const select = toolbar.createEl("select", { attr: { "aria-label": "\u9009\u62E9\u5E74\u4EFD" } });
      getYearOptions(this.year).forEach((year) => select.createEl("option", { value: String(year), text: String(year) }));
      select.value = String(this.year);
      select.addEventListener("change", () => {
        this.year = Number(select.value);
        this.render();
      });
    });
    renderStats(content, getGoalStats(goals));
    renderGoalRows(content, this.store, goals);
  }
};
var QuarterlyGoalStatisticsModal = class extends import_obsidian35.Modal {
  constructor(app, store) {
    super(app);
    this.store = store;
    this.year = (/* @__PURE__ */ new Date()).getFullYear();
    this.quarterNumber = Math.floor((/* @__PURE__ */ new Date()).getMonth() / 3) + 1;
  }
  onOpen() {
    setupStatsModal3(this);
    this.render();
  }
  render() {
    const objectives = this.store.getObjectives().filter((objective) => objective.year === this.year && objective.quarterNumber === this.quarterNumber);
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-goal-modal", "cow-goal-stats-modal");
    const content = renderGoalStatisticsLayout(this.contentEl, "\u5B63\u5EA6\u76EE\u6807\u7EDF\u8BA1", [
      { label: "\u4E0A\u4E00\u5E74", onClick: () => {
        this.year -= 1;
        this.render();
      } },
      { label: String(this.year), active: true },
      { label: "\u4E0B\u4E00\u5E74", onClick: () => {
        this.year += 1;
        this.render();
      } }
    ], (toolbar) => {
      const quarters = toolbar.createDiv({ cls: "cow-goal-period-tabs" });
      [1, 2, 3, 4].forEach((quarter) => {
        const button = quarters.createEl("button", { text: `Q${quarter}`, cls: quarter === this.quarterNumber ? "is-active" : "", attr: { type: "button" } });
        button.addEventListener("click", () => {
          this.quarterNumber = quarter;
          this.render();
        });
      });
    });
    renderStats(content, getObjectiveStats(objectives, this.year, this.quarterNumber));
    renderObjectiveRows(content, objectives);
  }
};
var MonthlyGoalStatisticsModal = class extends import_obsidian35.Modal {
  constructor(app, store) {
    super(app);
    this.store = store;
    this.year = (/* @__PURE__ */ new Date()).getFullYear();
    this.month = (/* @__PURE__ */ new Date()).getMonth() + 1;
  }
  onOpen() {
    setupStatsModal3(this);
    this.render();
  }
  render() {
    const keyResults = this.store.getKeyResults().filter((kr) => kr.year === this.year && kr.month === this.month);
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-goal-modal", "cow-goal-stats-modal");
    const content = renderGoalStatisticsLayout(this.contentEl, "\u6708\u5EA6\u76EE\u6807\u7EDF\u8BA1", [
      { label: "\u4E0A\u4E00\u5E74", onClick: () => {
        this.year -= 1;
        this.render();
      } },
      { label: String(this.year), active: true },
      { label: "\u4E0B\u4E00\u5E74", onClick: () => {
        this.year += 1;
        this.render();
      } }
    ], (toolbar) => {
      const months = toolbar.createDiv({ cls: "cow-goal-period-tabs is-months" });
      for (let month = 1; month <= 12; month += 1) {
        const button = months.createEl("button", { text: `${month}\u6708`, cls: month === this.month ? "is-active" : "", attr: { type: "button" } });
        button.addEventListener("click", () => {
          this.month = month;
          this.render();
        });
      }
    });
    renderStats(content, getKeyResultStats(keyResults, this.year, this.month));
    renderKeyResultRows(content, keyResults);
  }
};
function renderGoalRows(container, store, goals) {
  const list = container.createDiv({ cls: "cow-goal-stats-list" });
  goals.forEach((goal) => {
    const actions = store.getGoalActionsForGoal(goal.id);
    const milestones = actions.filter((action) => action.isMilestone);
    const row = list.createDiv({ cls: "cow-data-card" });
    row.createEl("strong", { text: goal.title });
    row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${goal.progress}% \xB7 \u5B50\u4EFB\u52A1\u5B8C\u6210 ${actions.filter((item) => item.status === "completed").length}/${actions.length} \xB7 \u91CC\u7A0B\u7891\u5B8C\u6210 ${milestones.filter((item) => item.status === "completed").length}/${milestones.length} \xB7 \u903E\u671F ${actions.filter((item) => item.status === "overdue").length}` });
  });
}
var GoalBreakdownStatisticsModal = class extends import_obsidian35.Modal {
  constructor(app, store) {
    super(app);
    this.store = store;
    this.year = (/* @__PURE__ */ new Date()).getFullYear();
    this.month = (/* @__PURE__ */ new Date()).getMonth() + 1;
  }
  onOpen() {
    setupStatsModal3(this);
    this.render();
  }
  render() {
    const actions = this.store.getGoalActions().filter((action) => isActionInPeriod(action, this.year, this.month));
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-goal-modal", "cow-goal-stats-modal");
    const content = renderGoalStatisticsLayout(this.contentEl, "\u76EE\u6807\u62C6\u89E3\u7EDF\u8BA1", [
      { label: "\u4E0A\u4E00\u5E74", onClick: () => {
        this.year -= 1;
        this.render();
      } },
      { label: String(this.year), active: true },
      { label: "\u4E0B\u4E00\u5E74", onClick: () => {
        this.year += 1;
        this.render();
      } }
    ], (toolbar) => {
      const months = toolbar.createDiv({ cls: "cow-goal-period-tabs is-months" });
      [{ value: 0, label: "\u5168\u90E8\u6708\u4EFD" }, ...Array.from({ length: 12 }, (_, index) => ({ value: index + 1, label: `${index + 1}\u6708` }))].forEach((item) => {
        const button = months.createEl("button", { text: item.label, cls: item.value === this.month ? "is-active" : "", attr: { type: "button" } });
        button.addEventListener("click", () => {
          this.month = item.value;
          this.render();
        });
      });
    });
    renderStats(content, [["\u62C6\u89E3\u4EFB\u52A1\u6570\u91CF", actions.length], ["\u5B8C\u6210", actions.filter((item) => item.status === "completed").length], ["\u8FDB\u884C\u4E2D", actions.filter((item) => item.status === "in-progress").length], ["\u903E\u671F", actions.filter((item) => item.status === "overdue").length], ["\u5E73\u5747\u8FDB\u5EA6", `${actions.length ? Math.round(actions.reduce((sum, item) => {
      var _a;
      return sum + ((_a = item.progress) != null ? _a : 0);
    }, 0) / actions.length) : 0}%`]]);
    const list = content.createDiv({ cls: "cow-goal-stats-list" });
    this.store.getGoals().forEach((goal) => {
      const goalActions = actions.filter((item) => item.goalId === goal.id);
      if (goalActions.length === 0) return;
      const row = list.createDiv({ cls: "cow-data-card" });
      row.createEl("strong", { text: goal.title });
      row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `\u5B8C\u6210\u7387 ${goalActions.length ? Math.round(goalActions.reduce((sum, item) => {
        var _a;
        return sum + ((_a = item.progress) != null ? _a : 0);
      }, 0) / goalActions.length) : goal.progress}% \xB7 \u5C42\u7EA7\u4EFB\u52A1 ${goalActions.length} \xB7 \u903E\u671F ${goalActions.filter((item) => item.status === "overdue").length}` });
    });
  }
};
var MilestoneStatisticsModal = class extends import_obsidian35.Modal {
  constructor(app, store) {
    super(app);
    this.store = store;
    this.year = (/* @__PURE__ */ new Date()).getFullYear();
    this.month = (/* @__PURE__ */ new Date()).getMonth() + 1;
  }
  onOpen() {
    setupStatsModal3(this);
    this.render();
  }
  render() {
    const milestones = this.store.getGoalActions().filter((item) => item.isMilestone && isActionInPeriod(item, this.year, this.month));
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-goal-modal", "cow-goal-stats-modal");
    const content = renderGoalStatisticsLayout(this.contentEl, "\u91CC\u7A0B\u7891\u7EDF\u8BA1", [
      { label: "\u4E0A\u4E00\u5E74", onClick: () => {
        this.year -= 1;
        this.render();
      } },
      { label: String(this.year), active: true },
      { label: "\u4E0B\u4E00\u5E74", onClick: () => {
        this.year += 1;
        this.render();
      } }
    ], (toolbar) => {
      const months = toolbar.createDiv({ cls: "cow-goal-period-tabs is-months" });
      [{ value: 0, label: "\u5168\u90E8\u6708\u4EFD" }, ...Array.from({ length: 12 }, (_, index) => ({ value: index + 1, label: `${index + 1}\u6708` }))].forEach((item) => {
        const button = months.createEl("button", { text: item.label, cls: item.value === this.month ? "is-active" : "", attr: { type: "button" } });
        button.addEventListener("click", () => {
          this.month = item.value;
          this.render();
        });
      });
    });
    renderStats(content, [["\u91CC\u7A0B\u7891\u603B\u6570", milestones.length], ["\u5B8C\u6210", milestones.filter((item) => item.status === "completed").length], ["\u672A\u5B8C\u6210", milestones.filter((item) => item.status !== "completed").length], ["\u903E\u671F", milestones.filter((item) => item.status === "overdue").length], ["\u6309\u65F6\u5B8C\u6210\u7387", `${getOnTimeRate(milestones)}%`]]);
    renderMilestoneCalendar(content, milestones, this.year, this.month);
    renderTimeline(content, this.store, milestones);
  }
};
var PriorityStatisticsModal = class extends import_obsidian35.Modal {
  constructor(app, store) {
    super(app);
    this.store = store;
    this.selectedQuadrant = "important-urgent";
    this.filter = "current";
  }
  onOpen() {
    setupStatsModal3(this);
    this.render();
  }
  render() {
    const items = this.store.getGoalActions().filter((item) => item.importance && item.urgency);
    const todayKey5 = today5();
    const filteredByState = items.filter((item) => {
      if (this.filter === "all") return true;
      if (this.filter === "completed") return item.status === "completed";
      return !item.completedDate || item.completedDate >= todayKey5;
    });
    const selectedItems = filteredByState.filter((item) => getQuadrant(item) === this.selectedQuadrant);
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-goal-modal", "cow-goal-stats-modal");
    const content = renderGoalStatisticsLayout(this.contentEl, "\u4F18\u5148\u7EA7\u7EDF\u8BA1", [], (toolbar) => {
      const filters = toolbar.createDiv({ cls: "cow-goal-period-tabs" });
      [
        { id: "current", label: "\u5F53\u524D" },
        { id: "completed", label: "\u5DF2\u5B8C\u6210" },
        { id: "all", label: "\u5168\u90E8" }
      ].forEach((option) => {
        const button = filters.createEl("button", { text: option.label, cls: this.filter === option.id ? "is-active" : "", attr: { type: "button" } });
        button.addEventListener("click", () => {
          this.filter = option.id;
          this.render();
        });
      });
    });
    const cards = content.createDiv({ cls: "cow-priority-stat-grid" });
    GOAL_QUADRANTS.forEach((quadrant) => {
      const card = cards.createEl("button", { cls: `cow-priority-stat-card ${this.selectedQuadrant === quadrant.id ? "is-active" : ""}`, attr: { type: "button" } });
      card.createEl("strong", { text: String(filteredByState.filter((item) => item.importance === quadrant.importance && item.urgency === quadrant.urgency).length) });
      card.createSpan({ text: quadrant.label });
      card.addEventListener("click", () => {
        this.selectedQuadrant = quadrant.id;
        this.render();
      });
    });
    renderPriorityRows(content, this.store, selectedItems);
  }
};
var RiskStatisticsModal = class extends import_obsidian35.Modal {
  constructor(app, store) {
    super(app);
    this.store = store;
  }
  onOpen() {
    setupStatsModal3(this);
    this.render();
  }
  render() {
    const risks = this.store.getRisks();
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-goal-modal", "cow-goal-stats-modal");
    this.contentEl.createEl("h2", { text: "\u98CE\u9669\u7EDF\u8BA1" });
    renderStats(this.contentEl, [["\u603B\u98CE\u9669", risks.length], ["\u5F85\u5904\u7406", risks.filter((item) => {
      var _a;
      return ((_a = item.status) != null ? _a : "todo") === "todo";
    }).length], ["\u5904\u7406\u4E2D", risks.filter((item) => item.status === "in-progress").length], ["\u5DF2\u89E3\u51B3", risks.filter((item) => item.status === "resolved").length], ["\u9AD8\u98CE\u9669\u6570\u91CF", risks.filter((item) => item.level === "high").length]]);
    risks.forEach((risk) => {
      var _a;
      return this.contentEl.createDiv({ cls: "cow-data-card" }).createEl("strong", { text: `${risk.title} \xB7 ${risk.level} \xB7 ${(_a = risk.status) != null ? _a : "todo"}` });
    });
  }
};
var SimpleGoalStatisticsModal = class extends import_obsidian35.Modal {
  constructor(app, store, title) {
    super(app);
    this.store = store;
    this.title = title;
  }
  onOpen() {
    setupStatsModal3(this);
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-goal-modal", "cow-goal-stats-modal");
    this.contentEl.createEl("h2", { text: this.title });
    renderStats(this.contentEl, [
      ["\u5B63\u5EA6\u76EE\u6807", this.store.getObjectives().length],
      ["\u6708\u5EA6\u76EE\u6807", this.store.getKeyResults().length],
      ["\u5DF2\u5B8C\u6210\u6708\u5EA6\u76EE\u6807", this.store.getKeyResults().filter((item) => item.completed).length],
      ["\u76EE\u6807\u6253\u5361\u9879", this.store.getGoalActions().length]
    ]);
  }
};
function getYearOptions(centerYear) {
  return Array.from({ length: 7 }, (_, index) => centerYear - 3 + index);
}
function getGoalStats(goals) {
  return [
    ["\u5E74\u5EA6\u76EE\u6807\u6570", goals.length],
    ["\u5B8C\u6210\u6570", goals.filter((goal) => goal.status === "\u5DF2\u5B8C\u6210").length],
    ["\u8FDB\u884C\u4E2D", goals.filter((goal) => goal.status === "\u8FDB\u884C\u4E2D").length],
    ["\u903E\u671F", goals.filter((goal) => goal.status !== "\u5DF2\u5B8C\u6210" && goal.deadline && goal.deadline < today5()).length],
    ["\u5E73\u5747\u5B8C\u6210\u7387", `${averageProgress(goals)}%`]
  ];
}
function getObjectiveStats(objectives, year, quarter) {
  const periodEnd = getQuarterEndDate(year, quarter);
  return [
    ["\u76EE\u6807\u603B\u6570", objectives.length],
    ["\u5B8C\u6210\u6570", objectives.filter((item) => item.progress >= 100).length],
    ["\u8FDB\u884C\u4E2D", objectives.filter((item) => item.progress > 0 && item.progress < 100).length],
    ["\u903E\u671F", objectives.filter((item) => item.progress < 100 && periodEnd < today5()).length],
    ["\u5E73\u5747\u5B8C\u6210\u7387", `${averageProgress(objectives)}%`]
  ];
}
function getKeyResultStats(keyResults, year, month) {
  const periodEnd = getMonthEndDate(year, month);
  return [
    ["\u76EE\u6807\u603B\u6570", keyResults.length],
    ["\u5B8C\u6210\u6570", keyResults.filter((item) => item.completed || item.progress >= 100).length],
    ["\u8FDB\u884C\u4E2D", keyResults.filter((item) => !item.completed && item.progress > 0 && item.progress < 100).length],
    ["\u903E\u671F", keyResults.filter((item) => !item.completed && periodEnd < today5()).length],
    ["\u5E73\u5747\u5B8C\u6210\u7387", `${averageProgress(keyResults)}%`]
  ];
}
function isActionInPeriod(action, year, month) {
  var _a, _b, _c, _d;
  const periodStart = month === 0 ? `${year}-01-01` : `${year}-${String(month).padStart(2, "0")}-01`;
  const periodEnd = month === 0 ? `${year}-12-31` : getMonthEndDate(year, month);
  const actionStart = (_b = (_a = action.startDate) != null ? _a : action.deadline) != null ? _b : periodStart;
  const actionEnd = (_d = (_c = action.deadline) != null ? _c : action.startDate) != null ? _d : periodEnd;
  return actionStart <= periodEnd && actionEnd >= periodStart;
}
function averageProgress(items) {
  if (items.length === 0) return 0;
  return Math.round(items.reduce((sum, item) => sum + (Number(item.progress) || 0), 0) / items.length);
}
function getQuarterEndDate(year, quarter) {
  const month = quarter * 3;
  return getMonthEndDate(year, month);
}
function getMonthEndDate(year, month) {
  const date = new Date(year, month, 0);
  return formatDateKey(date);
}
function renderObjectiveRows(container, objectives) {
  const list = container.createDiv({ cls: "cow-goal-stats-list" });
  objectives.forEach((objective) => {
    const row = list.createDiv({ cls: `cow-data-card ${objective.progress >= 100 ? "is-complete" : ""}` });
    row.createEl("strong", { text: objective.progress >= 100 ? `\u2713 ${objective.title}` : objective.title });
    row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${objective.quarter} \xB7 ${objective.progress}%` });
  });
}
function renderKeyResultRows(container, keyResults) {
  const list = container.createDiv({ cls: "cow-goal-stats-list" });
  keyResults.forEach((keyResult) => {
    const row = list.createDiv({ cls: `cow-data-card ${keyResult.completed ? "is-complete" : ""}` });
    row.createEl("strong", { text: keyResult.completed ? `\u2713 ${keyResult.title}` : keyResult.title });
    row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${keyResult.year}\u5E74${keyResult.month}\u6708 \xB7 ${keyResult.progress}%` });
  });
}
function renderMilestoneCalendar(container, milestones, year, month) {
  if (month === 0) return;
  const calendar = container.createDiv({ cls: "cow-review-calendar cow-goal-calendar" });
  const firstDay = new Date(year, month - 1, 1);
  const totalDays = new Date(year, month, 0).getDate();
  const offset = firstDay.getDay();
  ["\u65E5", "\u4E00", "\u4E8C", "\u4E09", "\u56DB", "\u4E94", "\u516D"].forEach((label) => calendar.createDiv({ cls: "cow-review-calendar-week", text: label }));
  for (let index = 0; index < offset; index += 1) calendar.createDiv({ cls: "cow-review-calendar-empty" });
  for (let day = 1; day <= totalDays; day += 1) {
    const date = `${year}-${String(month).padStart(2, "0")}-${String(day).padStart(2, "0")}`;
    const count = milestones.filter((item) => {
      var _a;
      return ((_a = item.milestoneDate) != null ? _a : item.deadline) === date;
    }).length;
    const cell = calendar.createDiv({ cls: `cow-review-calendar-day ${count > 0 ? "has-review" : ""}` });
    cell.createSpan({ text: String(day) });
    if (count > 0) cell.createDiv({ cls: "cow-calendar-dot is-purple", text: count > 1 ? String(count) : "" });
  }
}
function renderPriorityRows(container, store, items) {
  const list = container.createDiv({ cls: "cow-goal-stats-list" });
  if (items.length === 0) {
    list.createDiv({ cls: "cow-empty-state", text: "\u5F53\u524D\u7B5B\u9009\u4E0B\u6CA1\u6709\u4EFB\u52A1\u3002" });
    return;
  }
  items.forEach((item) => {
    var _a, _b, _c;
    const goal = store.getGoals().find((candidate) => candidate.id === item.goalId);
    const row = list.createDiv({ cls: `cow-data-card ${item.status === "completed" ? "is-complete" : ""}` });
    row.createEl("strong", { text: item.status === "completed" ? `\u2713 ${item.title}` : item.title });
    row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${(_a = goal == null ? void 0 : goal.title) != null ? _a : "\u672A\u5173\u8054\u76EE\u6807"} \xB7 ${(_c = (_b = item.deadline) != null ? _b : item.startDate) != null ? _c : "--"} \xB7 ${statusLabel(item.status)}` });
    if (item.note) row.createEl("p", { text: item.note });
  });
}
function renderStats(container, items) {
  const grid = container.createDiv({ cls: "cow-stats-card-grid" });
  items.forEach(([label, value]) => {
    const card = grid.createDiv({ cls: "cow-stats-card" });
    card.createEl("strong", { text: String(value) });
    card.createSpan({ text: label });
  });
}
function renderTimeline(container, store, milestones) {
  const timeline = container.createDiv({ cls: "cow-goal-timeline" });
  milestones.slice().sort((left, right) => {
    var _a, _b, _c, _d;
    return ((_b = (_a = left.milestoneDate) != null ? _a : left.deadline) != null ? _b : "").localeCompare((_d = (_c = right.milestoneDate) != null ? _c : right.deadline) != null ? _d : "");
  }).forEach((action) => {
    var _a, _b, _c;
    const goal = store.getGoals().find((item2) => item2.id === action.goalId);
    const item = timeline.createDiv({ cls: `cow-goal-timeline-item is-${action.status}` });
    item.createEl("time", { text: (_b = (_a = action.milestoneDate) != null ? _a : action.deadline) != null ? _b : "--" });
    item.createEl("strong", { text: action.title });
    item.createSpan({ text: `${(_c = goal == null ? void 0 : goal.title) != null ? _c : "\u672A\u5173\u8054\u76EE\u6807"} \xB7 ${statusLabel(action.status)}` });
  });
}
function getOnTimeRate(milestones) {
  const completed = milestones.filter((item) => item.status === "completed");
  if (completed.length === 0) return 0;
  const onTime = completed.filter((item) => !item.completedDate || !item.milestoneDate || item.completedDate <= item.milestoneDate).length;
  return Math.round(onTime / completed.length * 100);
}
function statusLabel(status) {
  if (status === "completed") return "\u5DF2\u5B8C\u6210";
  if (status === "overdue") return "\u5DF2\u903E\u671F";
  if (status === "in-progress") return "\u8FDB\u884C\u4E2D";
  return "\u672A\u5F00\u59CB";
}

// src/services/DailyFocusService.ts
var PRIORITY_WEIGHT = {
  high: 0,
  \u9AD8: 0,
  medium: 1,
  \u4E2D: 1,
  low: 2,
  \u4F4E: 2,
  none: 3
};
var DailyFocusService = class {
  constructor(store) {
    this.store = store;
    this.providers = [];
    this.registerProvider(new ManualFocusProvider(store));
    this.registerProvider(new TaskFocusProvider(store));
    this.registerProvider(new ResearchDeadlineFocusProvider(store));
    this.registerProvider(new ReadingFocusProvider(store));
    this.registerProvider(new FitnessFocusProvider(store));
    this.registerProvider(new FinanceFocusProvider(store));
    this.registerProvider(new GoalsFocusProvider(store));
  }
  registerProvider(provider) {
    this.providers.push(provider);
  }
  getEntriesForDate(date) {
    const entries = [];
    this.providers.forEach((provider) => entries.push(...provider.getDailyFocusEntries(date)));
    return entries.sort((left, right) => this.compareEntries(left, right, date));
  }
  getTodayEntries() {
    return this.getEntriesForDate(formatDateKey(/* @__PURE__ */ new Date()));
  }
  getOverdueEntries(date = formatDateKey(/* @__PURE__ */ new Date())) {
    return this.getEntriesForDate(date).filter((entry) => entry.status === "overdue");
  }
  getUpcomingDeadlines(days, date = formatDateKey(/* @__PURE__ */ new Date())) {
    const start = /* @__PURE__ */ new Date(`${date}T00:00:00`);
    const limit = new Date(start);
    limit.setDate(start.getDate() + days);
    const limitKey = formatDateKey(limit);
    const entries = [];
    this.providers.forEach((provider) => entries.push(...provider.getDailyFocusEntries(date)));
    return entries.filter((entry) => entry.dueDate && entry.dueDate > date && entry.dueDate <= limitKey).sort((left, right) => {
      var _a, _b;
      return ((_a = left.dueDate) != null ? _a : "").localeCompare((_b = right.dueDate) != null ? _b : "");
    });
  }
  async completeEntry(entry) {
    var _a;
    if (!entry.canComplete) return;
    const provider = this.providers.find((item) => item.moduleId === entry.sourceModule);
    await ((_a = provider == null ? void 0 : provider.completeEntry) == null ? void 0 : _a.call(provider, entry.sourceId));
  }
  openSource(entry, app, onDone) {
    const provider = this.providers.find((item) => item.moduleId === entry.sourceModule);
    if (!(provider == null ? void 0 : provider.openEntry)) {
      new import_obsidian36.Notice("\u6682\u65F6\u6CA1\u6709\u53EF\u6253\u5F00\u7684\u6765\u6E90\u8BE6\u60C5\u3002");
      return;
    }
    provider.openEntry(entry.sourceId, app, onDone);
  }
  compareEntries(left, right, date) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    const group = this.groupWeight(left, date) - this.groupWeight(right, date);
    if (group !== 0) return group;
    const priority = ((_b = PRIORITY_WEIGHT[(_a = left.priority) != null ? _a : "none"]) != null ? _b : 3) - ((_d = PRIORITY_WEIGHT[(_c = right.priority) != null ? _c : "none"]) != null ? _d : 3);
    if (priority !== 0) return priority;
    return ((_f = (_e = left.dueDate) != null ? _e : left.date) != null ? _f : "").localeCompare((_h = (_g = right.dueDate) != null ? _g : right.date) != null ? _h : "");
  }
  groupWeight(entry, date) {
    if (entry.status === "overdue") return 0;
    if (entry.kind === "deadline" && entry.dueDate === date) return 1;
    if (entry.kind === "task") return 2;
    if (entry.kind === "schedule") return 3;
    if (entry.dueDate && entry.dueDate > date) return 4;
    return 5;
  }
};
var ManualFocusProvider = class {
  constructor(store) {
    this.store = store;
    this.moduleId = "overview";
  }
  getDailyFocusEntries(date) {
    return this.store.getTodayFocusTasksForDate(date).map((task) => this.toEntry(task, date));
  }
  async completeEntry(sourceId) {
    await this.store.toggleTodayFocusTask(sourceId);
  }
  openEntry(sourceId, app, onDone) {
    var _a;
    const task = this.store.getTodayFocusTasks().find((item) => item.id === sourceId);
    new TodayFocusTaskModal(app, (_a = task == null ? void 0 : task.date) != null ? _a : formatDateKey(/* @__PURE__ */ new Date()), async (values) => {
      if (task) await this.store.updateTodayFocusTask(task.id, values);
      onDone();
    }, task).open();
  }
  toEntry(task, date) {
    var _a;
    return {
      id: `overview-manual-${task.id}`,
      sourceModule: "overview",
      sourceType: "today-focus-task",
      sourceId: task.id,
      title: task.label,
      date: (_a = task.date) != null ? _a : date,
      kind: "manual",
      status: task.completed ? "completed" : "pending",
      priority: task.category,
      canComplete: true,
      canOpenSource: true,
      subtitle: `\u624B\u52A8\u4E8B\u9879 \xB7 ${task.category}`
    };
  }
};
var TaskFocusProvider = class {
  constructor(store) {
    this.store = store;
    this.moduleId = "tasks";
  }
  getDailyFocusEntries(date) {
    return this.store.getTasks().filter((task) => !task.parentTaskId).filter((task) => task.plannedDate === date || task.dueDate === date || this.isOverdue(task, date) || this.isUpcoming(task, date)).map((task) => this.toEntry(task, date));
  }
  async completeEntry(sourceId) {
    await this.store.updateTask(sourceId, { status: "done" });
  }
  openEntry(sourceId, app, onDone) {
    const task = this.store.getTasks().find((item) => item.id === sourceId);
    if (!task) {
      new import_obsidian36.Notice("\u4EFB\u52A1\u4E0D\u5B58\u5728\u3002");
      return;
    }
    openTaskModal(app, this.store, onDone, task);
  }
  toEntry(task, date) {
    return {
      id: `tasks-task-${task.id}`,
      sourceModule: "tasks",
      sourceType: "task",
      sourceId: task.id,
      title: task.title,
      date: task.plannedDate,
      dueDate: task.dueDate,
      kind: "task",
      status: task.status === "done" ? "completed" : this.isOverdue(task, date) ? "overdue" : "pending",
      priority: task.priority,
      canComplete: task.status !== "done" && task.status !== "cancelled",
      canOpenSource: true,
      subtitle: this.taskSubtitle(task)
    };
  }
  isOverdue(task, date) {
    return Boolean(task.dueDate && task.dueDate < date && task.status !== "done" && task.status !== "cancelled");
  }
  isUpcoming(task, date) {
    if (!task.dueDate || task.dueDate <= date || task.status === "done" || task.status === "cancelled") return false;
    const due = /* @__PURE__ */ new Date(`${task.dueDate}T00:00:00`);
    const current = /* @__PURE__ */ new Date(`${date}T00:00:00`);
    const diff = Math.round((due.getTime() - current.getTime()) / 864e5);
    return diff <= 3;
  }
  taskSubtitle(task) {
    const parts = ["\u4EFB\u52A1"];
    if (task.plannedDate) parts.push(`\u8BA1\u5212 ${task.plannedDate}`);
    if (task.dueDate) parts.push(`\u622A\u6B62 ${task.dueDate}`);
    return parts.join(" \xB7 ");
  }
};
var ResearchDeadlineFocusProvider = class {
  constructor(store) {
    this.store = store;
    this.moduleId = "research";
  }
  getDailyFocusEntries(date) {
    return this.store.getResearchDeadlines().filter((deadline) => deadline.date === date || deadline.date < date || this.isUpcoming(deadline, date)).map((deadline) => ({
      id: `research-deadline-${deadline.id}`,
      sourceModule: "research",
      sourceType: "research-deadline",
      sourceId: deadline.id,
      title: deadline.title,
      dueDate: deadline.date,
      kind: "deadline",
      status: deadline.date < date ? "overdue" : "pending",
      priority: deadline.priority,
      canComplete: false,
      canOpenSource: false,
      subtitle: `${deadline.type} \xB7 ${deadline.date}`
    }));
  }
  isUpcoming(deadline, date) {
    if (deadline.date <= date) return false;
    const due = /* @__PURE__ */ new Date(`${deadline.date}T00:00:00`);
    const current = /* @__PURE__ */ new Date(`${date}T00:00:00`);
    const diff = Math.round((due.getTime() - current.getTime()) / 864e5);
    return diff <= 3;
  }
};
var ReadingFocusProvider = class {
  constructor(store) {
    this.store = store;
    this.moduleId = "reading";
  }
  getDailyFocusEntries(date) {
    return this.store.getReadingPlans().filter((plan) => plan.status !== "completed").filter((plan) => this.isInRange(plan, date) || plan.endDate === date || plan.endDate < date || this.isUpcoming(plan.endDate, date)).map((plan) => this.toEntry(plan, date));
  }
  openEntry(sourceId, app, onDone) {
    const plan = this.store.getReadingPlans().find((item) => item.id === sourceId);
    openReadingPlanModal(app, this.store, onDone, plan);
  }
  toEntry(plan, date) {
    const book = this.store.getBooks().find((item) => item.id === plan.bookId);
    return {
      id: `reading-plan-${plan.id}`,
      sourceModule: "reading",
      sourceType: "reading-plan",
      sourceId: plan.id,
      title: book ? `\u300A${book.title}\u300B\u9605\u8BFB\u8BA1\u5212` : "\u9605\u8BFB\u8BA1\u5212",
      date: this.isInRange(plan, date) ? date : void 0,
      dueDate: plan.endDate,
      kind: plan.endDate <= date ? "deadline" : "schedule",
      status: plan.endDate < date ? "overdue" : "pending",
      priority: plan.status,
      canComplete: false,
      canOpenSource: true,
      subtitle: this.subtitle(plan, book)
    };
  }
  subtitle(plan, book) {
    const parts = [`${plan.startDate} - ${plan.endDate}`];
    if (book && book.totalPages > book.currentPage) {
      const remainingPages = book.totalPages - book.currentPage;
      const remainingDays = Math.max(1, Math.ceil(((/* @__PURE__ */ new Date(`${plan.endDate}T00:00:00`)).getTime() - Date.now()) / 864e5));
      parts.push(`\u5EFA\u8BAE\u7EA6 ${Math.ceil(remainingPages / remainingDays)} \u9875/\u5929`);
    }
    return parts.join(" \xB7 ");
  }
  isInRange(plan, date) {
    return plan.startDate <= date && plan.endDate >= date;
  }
  isUpcoming(dueDate, date) {
    if (dueDate <= date) return false;
    const diff = Math.round(((/* @__PURE__ */ new Date(`${dueDate}T00:00:00`)).getTime() - (/* @__PURE__ */ new Date(`${date}T00:00:00`)).getTime()) / 864e5);
    return diff <= 3;
  }
};
var FitnessFocusProvider = class {
  constructor(store) {
    this.store = store;
    this.moduleId = "fitness";
  }
  getDailyFocusEntries(date) {
    return [
      ...this.store.getTrainingPlans().filter((plan) => plan.status !== "completed").filter((plan) => plan.startDate === date || plan.endDate === date || plan.endDate < date || this.isUpcoming(plan.endDate, date)).map((plan) => this.planEntry(plan, date)),
      ...this.store.getFitnessGoals().filter((goal) => goal.status !== "completed" && goal.status !== "archived").filter((goal) => goal.deadline === date || goal.deadline < date || this.isUpcoming(goal.deadline, date)).map((goal) => this.goalEntry(goal, date)),
      ...this.store.getHealthReminders().filter((reminder) => reminder.enabled !== false).filter((reminder) => reminder.date === date || reminder.repeatType === "daily" && reminder.date && reminder.date <= date).map((reminder) => this.reminderEntry(reminder, date))
    ];
  }
  openEntry(sourceId, app, onDone) {
    const plan = this.store.getTrainingPlans().find((item) => item.id === sourceId);
    if (plan) return openTrainingPlanModal(app, this.store, onDone, plan);
    const goal = this.store.getFitnessGoals().find((item) => item.id === sourceId);
    if (goal) return openFitnessGoalModal(app, this.store, onDone, goal);
    const reminder = this.store.getHealthReminders().find((item) => item.id === sourceId);
    if (reminder) return openHealthReminderModal(app, this.store, onDone, reminder);
    new import_obsidian36.Notice("\u6765\u6E90\u4E0D\u5B58\u5728\u3002");
  }
  planEntry(plan, date) {
    return {
      id: `fitness-plan-${plan.id}`,
      sourceModule: "fitness",
      sourceType: "training-plan",
      sourceId: plan.id,
      title: plan.title,
      date: plan.startDate === date ? date : void 0,
      dueDate: plan.endDate,
      kind: plan.endDate <= date ? "deadline" : "schedule",
      status: plan.endDate < date ? "overdue" : "pending",
      priority: plan.status,
      canComplete: false,
      canOpenSource: true,
      subtitle: `\u8BAD\u7EC3\u8BA1\u5212 \xB7 ${plan.startDate} - ${plan.endDate}`
    };
  }
  goalEntry(goal, date) {
    return {
      id: `fitness-goal-${goal.id}`,
      sourceModule: "fitness",
      sourceType: "fitness-goal",
      sourceId: goal.id,
      title: goal.title,
      dueDate: goal.deadline,
      kind: "deadline",
      status: goal.deadline < date ? "overdue" : "pending",
      priority: goal.status,
      canComplete: false,
      canOpenSource: true,
      subtitle: `\u5065\u8EAB\u76EE\u6807 \xB7 \u622A\u6B62 ${goal.deadline}`
    };
  }
  reminderEntry(reminder, date) {
    return {
      id: `fitness-reminder-${reminder.id}`,
      sourceModule: "fitness",
      sourceType: "health-reminder",
      sourceId: reminder.id,
      title: reminder.title,
      date,
      kind: "schedule",
      status: "pending",
      priority: reminder.repeatType,
      canComplete: false,
      canOpenSource: true,
      subtitle: `\u5065\u5EB7\u63D0\u9192${reminder.time ? ` \xB7 ${reminder.time}` : ""}`
    };
  }
  isUpcoming(dueDate, date) {
    if (dueDate <= date) return false;
    const diff = Math.round(((/* @__PURE__ */ new Date(`${dueDate}T00:00:00`)).getTime() - (/* @__PURE__ */ new Date(`${date}T00:00:00`)).getTime()) / 864e5);
    return diff <= 3;
  }
};
var FinanceFocusProvider = class {
  constructor(store) {
    this.store = store;
    this.moduleId = "finance";
  }
  getDailyFocusEntries(date) {
    return this.store.getFinanceTodos().filter((todo) => todo.date === date || this.isOverdue(todo, date)).map((todo) => ({
      id: `finance-todo-${todo.id}`,
      sourceModule: "finance",
      sourceType: "finance-todo",
      sourceId: todo.id,
      title: todo.title,
      date: todo.date,
      kind: "task",
      status: todo.completed ? "completed" : this.isOverdue(todo, date) ? "overdue" : "pending",
      canComplete: !todo.completed,
      canOpenSource: true,
      subtitle: todo.note ? `\u7406\u8D22\u5F85\u529E \xB7 ${todo.note}` : "\u7406\u8D22\u5F85\u529E"
    }));
  }
  async completeEntry(sourceId) {
    await this.store.updateFinanceTodo(sourceId, { completed: true });
  }
  openEntry(sourceId, app, onDone) {
    const todo = this.store.getFinanceTodos().find((item) => item.id === sourceId);
    openFinanceTodoModal(app, this.store, onDone, todo);
  }
  isOverdue(todo, date) {
    return Boolean(todo.date && todo.date < date && !todo.completed);
  }
};
var GoalsFocusProvider = class {
  constructor(store) {
    this.store = store;
    this.moduleId = "goals";
  }
  getDailyFocusEntries(date) {
    return [
      ...this.store.getGoalActions().filter((action) => action.startDate === date || action.deadline === date || this.isActionOverdue(action, date) || (action.deadline ? this.isUpcoming(action.deadline, date) : false)).map((action) => this.actionEntry(action, date)),
      ...this.store.getMilestones().filter((milestone) => milestone.date === date || this.isMilestoneOverdue(milestone, date) || this.isUpcoming(milestone.date, date)).map((milestone) => this.milestoneEntry(milestone, date))
    ];
  }
  async completeEntry(sourceId) {
    if (this.store.getGoalActions().some((item) => item.id === sourceId)) {
      await this.store.updateGoalAction(sourceId, { status: "completed" });
      return;
    }
    if (this.store.getMilestones().some((item) => item.id === sourceId)) {
      await this.store.updateMilestone(sourceId, { status: "\u5DF2\u5B8C\u6210" });
    }
  }
  openEntry(sourceId, app, onDone) {
    const action = this.store.getGoalActions().find((item) => item.id === sourceId);
    if (action) return openGoalActionModal(app, this.store, onDone, action);
    const milestone = this.store.getMilestones().find((item) => item.id === sourceId);
    if (milestone) return openGoalActionModal(app, this.store, onDone, void 0, { isMilestone: true, title: milestone.title, milestoneDate: milestone.date });
    new import_obsidian36.Notice("\u6765\u6E90\u4E0D\u5B58\u5728\u3002");
  }
  actionEntry(action, date) {
    return {
      id: `goals-action-${action.id}`,
      sourceModule: "goals",
      sourceType: "goal-action",
      sourceId: action.id,
      title: action.title,
      date: action.startDate === date ? date : void 0,
      dueDate: action.deadline,
      kind: action.deadline && action.deadline <= date ? "deadline" : "task",
      status: action.status === "completed" ? "completed" : this.isActionOverdue(action, date) ? "overdue" : "pending",
      priority: action.urgency,
      canComplete: action.status !== "completed",
      canOpenSource: true,
      subtitle: `\u76EE\u6807\u884C\u52A8${action.deadline ? ` \xB7 \u622A\u6B62 ${action.deadline}` : ""}`
    };
  }
  milestoneEntry(milestone, date) {
    return {
      id: `goals-milestone-${milestone.id}`,
      sourceModule: "goals",
      sourceType: "milestone",
      sourceId: milestone.id,
      title: milestone.title,
      dueDate: milestone.date,
      kind: "deadline",
      status: milestone.status === "\u5DF2\u5B8C\u6210" ? "completed" : this.isMilestoneOverdue(milestone, date) ? "overdue" : "pending",
      priority: milestone.status,
      canComplete: milestone.status !== "\u5DF2\u5B8C\u6210",
      canOpenSource: true,
      subtitle: `\u91CC\u7A0B\u7891 \xB7 ${milestone.date}`
    };
  }
  isActionOverdue(action, date) {
    return Boolean(action.deadline && action.deadline < date && action.status !== "completed");
  }
  isMilestoneOverdue(milestone, date) {
    return milestone.date < date && milestone.status !== "\u5DF2\u5B8C\u6210";
  }
  isUpcoming(dueDate, date) {
    if (dueDate <= date) return false;
    const diff = Math.round(((/* @__PURE__ */ new Date(`${dueDate}T00:00:00`)).getTime() - (/* @__PURE__ */ new Date(`${date}T00:00:00`)).getTime()) / 864e5);
    return diff <= 3;
  }
};

// src/components/overview/TodayFocusSection.ts
var GROUPS = [
  { id: "today", title: "\u4ECA\u65E5\u5E94\u5B8C\u6210", test: (entry, today6) => entry.kind !== "deadline" && (entry.date === today6 || entry.dueDate === today6) && entry.status !== "overdue" },
  { id: "deadline", title: "\u4ECA\u65E5 DDL", test: (entry, today6) => entry.kind === "deadline" && entry.dueDate === today6 && entry.status !== "overdue" },
  { id: "overdue", title: "\u5DF2\u903E\u671F", test: (entry) => entry.status === "overdue" },
  { id: "upcoming", title: "\u5373\u5C06\u5230\u671F", test: (entry, today6) => Boolean(entry.dueDate && entry.dueDate > today6) }
];
var MODULE_LABELS = {
  overview: "\u624B\u52A8",
  research: "\u79D1\u7814",
  reading: "\u9605\u8BFB",
  fitness: "\u5065\u8EAB",
  finance: "\u7406\u8D22",
  goals: "\u76EE\u6807",
  tasks: "\u4EFB\u52A1"
};
function sourceLabel(entry) {
  var _a;
  return (_a = MODULE_LABELS[entry.sourceModule]) != null ? _a : entry.sourceModule;
}
var TodayFocusSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
    this.service = new DailyFocusService(store);
  }
  render(container) {
    const host = container.createDiv({ cls: "cow-daily-focus" });
    this.renderContent(host);
  }
  renderContent(host) {
    host.empty();
    const today6 = formatDateKey(/* @__PURE__ */ new Date());
    const entries = this.service.getEntriesForDate(today6);
    const used = /* @__PURE__ */ new Set();
    GROUPS.forEach((group) => {
      const items = entries.filter((entry) => group.test(entry, today6));
      items.forEach((entry) => used.add(entry.id));
      this.renderGroup(host, group.title, items);
    });
    this.renderGroup(host, "\u5176\u5B83\u4E8B\u9879", entries.filter((entry) => !used.has(entry.id)));
    if (entries.length === 0) {
      host.createDiv({ cls: "cow-empty-state cow-daily-focus-empty", text: "\u4ECA\u5929\u6682\u65F6\u6CA1\u6709\u9700\u8981\u5904\u7406\u7684\u4E8B\u9879 \u{1F436}" });
    }
    const add = host.createEl("button", { cls: "cow-task-add-strip", text: "+ \u6DFB\u52A0\u624B\u52A8\u4ECA\u65E5\u4E8B\u9879", attr: { type: "button" } });
    add.addEventListener("click", () => {
      new TodayFocusTaskModal(this.app, today6, async (values) => {
        await this.store.addTodayFocusTask(values.label, values.category, values.date);
        this.renderContent(host);
      }).open();
    });
  }
  renderGroup(container, title, entries) {
    if (entries.length === 0) return;
    const group = container.createDiv({ cls: "cow-daily-focus-group" });
    group.createEl("h4", { text: title });
    const list = group.createEl("ul", { cls: "cow-focus-list cow-daily-focus-list" });
    entries.forEach((entry) => this.renderEntry(list, entry, container));
  }
  renderEntry(list, entry, host) {
    const item = list.createEl("li", { cls: `cow-daily-focus-entry is-${entry.status} is-${entry.sourceModule}` });
    if (entry.canComplete) {
      const checkbox = item.createEl("input", { type: "checkbox", attr: { "aria-label": `\u5B8C\u6210${entry.title}` } });
      checkbox.checked = entry.status === "completed";
      checkbox.addEventListener("change", async () => {
        await this.service.completeEntry(entry);
        this.renderContent(host);
      });
    } else {
      item.createSpan({ cls: `cow-daily-focus-status ${entry.kind === "deadline" ? "is-deadline" : ""}`, text: entry.status === "overdue" ? "!" : "\u25CF" });
    }
    item.createSpan({ cls: `cow-pill cow-source-pill ${this.sourceClass(entry)}`, text: sourceLabel(entry) });
    const body = item.createDiv({ cls: "cow-daily-focus-body" });
    const title = body.createEl(entry.canOpenSource ? "button" : "span", {
      cls: entry.status === "completed" ? "is-complete cow-focus-task-title cow-daily-focus-title" : "cow-focus-task-title cow-daily-focus-title",
      text: entry.title,
      attr: entry.canOpenSource ? { type: "button", title: entry.title } : { title: entry.title }
    });
    if (entry.canOpenSource) {
      title.addEventListener("click", () => this.service.openSource(entry, this.app, () => this.renderContent(host)));
    }
    if (entry.subtitle) body.createDiv({ cls: "cow-meta-line", text: entry.subtitle });
    item.createSpan({ cls: `cow-daily-focus-date ${entry.status === "overdue" ? "is-overdue" : ""}`, text: this.dateLabel(entry) });
    const actions = item.createDiv({ cls: "cow-list-item-actions" });
    if (entry.canOpenSource) {
      const open = actions.createEl("button", { attr: { type: "button", "aria-label": "\u6253\u5F00\u6765\u6E90" } });
      (0, import_obsidian37.setIcon)(open, "external-link");
      open.addEventListener("click", () => this.service.openSource(entry, this.app, () => this.renderContent(host)));
    }
    if (entry.sourceModule === "overview" && entry.sourceType === "today-focus-task") {
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u624B\u52A8\u4E8B\u9879" } });
      (0, import_obsidian37.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        if (!confirm(`\u5220\u9664\u201C${entry.title}\u201D\uFF1F`)) return;
        await this.store.deleteTodayFocusTask(entry.sourceId);
        this.renderContent(host);
      });
    }
    if (!entry.canComplete && !entry.canOpenSource) {
      const info = actions.createEl("button", { attr: { type: "button", "aria-label": "\u63D0\u9192\u8BF4\u660E" } });
      (0, import_obsidian37.setIcon)(info, "info");
      info.addEventListener("click", () => new import_obsidian37.Notice("\u8FD9\u662F\u6765\u6E90\u6A21\u5757\u7684\u53EA\u8BFB\u63D0\u9192\uFF0C\u4E0D\u4F1A\u5728\u4ECA\u65E5\u7126\u70B9\u4E2D\u76F4\u63A5\u6539\u4E1A\u52A1\u72B6\u6001\u3002"));
    }
  }
  sourceClass(entry) {
    return `is-source-${entry.sourceModule}`;
  }
  dateLabel(entry) {
    var _a;
    const today6 = formatDateKey(/* @__PURE__ */ new Date());
    const target = (_a = entry.dueDate) != null ? _a : entry.date;
    if (!target) return "";
    if (target === today6) return entry.kind === "deadline" ? "\u4ECA\u5929" : "\u4ECA\u65E5";
    const diff = this.diffDays(today6, target);
    if (diff === 1) return "\u660E\u5929";
    if (diff === -1) return "\u903E\u671F1\u5929";
    if (diff < -1) return `\u903E\u671F${Math.abs(diff)}\u5929`;
    if (diff > 1 && diff <= 3) return `${diff}\u5929\u540E`;
    return target;
  }
  diffDays(from, to) {
    const start = /* @__PURE__ */ new Date(`${from}T00:00:00`);
    const end = /* @__PURE__ */ new Date(`${to}T00:00:00`);
    return Math.round((end.getTime() - start.getTime()) / 864e5);
  }
};
var DailyFocusStatisticsModal = class extends import_obsidian37.Modal {
  constructor(app, store, date = /* @__PURE__ */ new Date()) {
    super(app);
    this.store = store;
    this.date = date;
    this.service = new DailyFocusService(store);
    applyResizableModal(this, {
      className: "cute-daily-focus-stat-modal",
      width: "min(860px, 92vw)",
      height: "min(640px, 84vh)",
      maxWidth: "96vw",
      maxHeight: "92vh",
      minWidth: "min(340px, 92vw)",
      minHeight: "min(360px, 78vh)"
    });
  }
  onOpen() {
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-daily-focus-stat-modal");
    const dateKey = formatDateKey(this.date);
    const entries = this.service.getEntriesForDate(dateKey);
    const completed = entries.filter((entry) => entry.status === "completed").length;
    const overdue = entries.filter((entry) => entry.status === "overdue").length;
    const pending = entries.length - completed;
    const rate = entries.length === 0 ? 0 : Math.round(completed / entries.length * 100);
    const header = this.contentEl.createDiv({ cls: "cow-stats-modal-header cow-daily-focus-stat-header" });
    const prev = header.createEl("button", { attr: { type: "button", "aria-label": "\u4E0A\u4E00\u5929" } });
    (0, import_obsidian37.setIcon)(prev, "chevron-left");
    prev.addEventListener("click", () => {
      this.date.setDate(this.date.getDate() - 1);
      this.render();
    });
    const title = header.createDiv({ cls: "cow-daily-focus-stat-title" });
    title.createEl("h2", { text: "\u4ECA\u65E5\u7126\u70B9\u7EDF\u8BA1" });
    const picker = title.createEl("input", { type: "date", value: dateKey });
    picker.addEventListener("change", () => {
      if (!picker.value) return;
      this.date = /* @__PURE__ */ new Date(`${picker.value}T00:00:00`);
      this.render();
    });
    const next = header.createEl("button", { attr: { type: "button", "aria-label": "\u4E0B\u4E00\u5929" } });
    (0, import_obsidian37.setIcon)(next, "chevron-right");
    next.addEventListener("click", () => {
      this.date.setDate(this.date.getDate() + 1);
      this.render();
    });
    const summary = this.contentEl.createDiv({ cls: "cow-stats-card-grid" });
    [
      ["\u5B8C\u6210", `${completed} / ${entries.length}`],
      ["\u5B8C\u6210\u7387", `${rate}%`],
      ["\u5F85\u5904\u7406", pending],
      ["\u5DF2\u903E\u671F", overdue]
    ].forEach(([label, value]) => {
      const card = summary.createDiv({ cls: "cow-stats-card" });
      card.createEl("strong", { text: String(value) });
      card.createSpan({ text: String(label) });
    });
    const moduleStats = this.getModuleStats(entries);
    const moduleGrid = this.contentEl.createDiv({ cls: "cow-daily-focus-module-stats" });
    if (moduleStats.length === 0) {
      moduleGrid.createEl("p", { cls: "cow-empty-state", text: "\u8FD9\u4E00\u5929\u6CA1\u6709\u4ECA\u65E5\u7126\u70B9\u4E8B\u9879\u3002" });
    } else {
      moduleStats.forEach((stat) => {
        const card = moduleGrid.createDiv({ cls: "cow-daily-focus-module-card" });
        card.createSpan({ cls: `cow-pill cow-source-pill is-source-${stat.moduleId}`, text: stat.label });
        const value = card.createEl("strong", { text: `${stat.completed} / ${stat.total}` });
        value.setAttr("title", `${stat.label} \u5B8C\u6210 ${stat.completed} / ${stat.total}`);
        const track = card.createDiv({ cls: "cow-daily-focus-stat-track" });
        track.createDiv({ attr: { style: `width:${stat.total ? Math.round(stat.completed / stat.total * 100) : 0}%` } });
      });
    }
    const list = this.contentEl.createDiv({ cls: "cow-data-list cow-daily-focus-stat-list" });
    entries.forEach((entry) => {
      const row = list.createDiv({ cls: `cow-data-card cow-daily-focus-stat-row is-${entry.status}` });
      row.createSpan({ cls: `cow-pill cow-source-pill is-source-${entry.sourceModule}`, text: sourceLabel(entry) });
      const body = row.createDiv();
      body.createEl("strong", { text: entry.title });
      body.createDiv({ cls: "cow-meta-line", text: [entry.subtitle, entry.dueDate ? `\u622A\u6B62 ${entry.dueDate}` : entry.date].filter(Boolean).join(" \xB7 ") });
      row.createSpan({ cls: `cow-daily-focus-date ${entry.status === "overdue" ? "is-overdue" : ""}`, text: entry.status === "completed" ? "\u5DF2\u5B8C\u6210" : this.dateLabel(entry, dateKey) });
    });
    this.contentEl.createEl("p", {
      cls: "cow-muted-note",
      text: "\u5386\u53F2\u7EDF\u8BA1\u57FA\u4E8E\u4ECD\u5B58\u5728\u7684\u6765\u6E90\u6570\u636E\u548C\u5F53\u524D\u5B8C\u6210\u72B6\u6001\u52A8\u6001\u8BA1\u7B97\uFF1B\u672C\u8F6E\u4E0D\u4FDD\u5B58\u4ECA\u65E5\u7126\u70B9\u5FEB\u7167\u3002"
    });
  }
  getModuleStats(entries) {
    const result = /* @__PURE__ */ new Map();
    entries.forEach((entry) => {
      var _a;
      const current = (_a = result.get(entry.sourceModule)) != null ? _a : { moduleId: entry.sourceModule, label: sourceLabel(entry), total: 0, completed: 0 };
      current.total += 1;
      if (entry.status === "completed") current.completed += 1;
      result.set(entry.sourceModule, current);
    });
    return Array.from(result.values());
  }
  dateLabel(entry, baseDate) {
    var _a;
    const target = (_a = entry.dueDate) != null ? _a : entry.date;
    if (!target) return "";
    if (target === baseDate) return entry.kind === "deadline" ? "\u5F53\u5929 DDL" : "\u5F53\u5929";
    const diff = Math.round(((/* @__PURE__ */ new Date(`${target}T00:00:00`)).getTime() - (/* @__PURE__ */ new Date(`${baseDate}T00:00:00`)).getTime()) / 864e5);
    if (diff === 1) return "\u6B21\u65E5";
    if (diff === -1) return "\u903E\u671F1\u5929";
    if (diff < -1) return `\u903E\u671F${Math.abs(diff)}\u5929`;
    return target;
  }
};

// src/components/overview/FocusStatSection.ts
var import_obsidian38 = require("obsidian");
var FOCUS_BACKGROUNDS = [
  { id: "pink", label: "\u9ED8\u8BA4\u7C89\u8272" },
  { id: "cream", label: "\u5976\u6CB9\u6E10\u53D8" },
  { id: "sky", label: "\u5929\u7A7A" },
  { id: "forest", label: "\u68EE\u6797" },
  { id: "night", label: "\u591C\u665A" },
  { id: "desk", label: "\u4E66\u684C" },
  { id: "minimal-dark", label: "\u6781\u7B80\u6DF1\u8272" }
];
function applyFocusModalFrame(modal, options) {
  modal.modalEl.addClass("cute-focus-resizable-modal", options.className);
  modal.modalEl.style.width = options.width;
  modal.modalEl.style.height = options.height;
  modal.modalEl.style.maxWidth = options.maxWidth;
  modal.modalEl.style.maxHeight = options.maxHeight;
  if (options.minWidth) {
    modal.modalEl.style.minWidth = options.minWidth;
  }
  if (options.minHeight) {
    modal.modalEl.style.minHeight = options.minHeight;
  }
}
function logFocusModalSize(label, modalEl) {
  window.requestAnimationFrame(() => {
    const rect = modalEl.getBoundingClientRect();
    console.debug(`${label} modal size`, {
      width: Math.round(rect.width),
      height: Math.round(rect.height)
    });
  });
}
var FocusStatSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const stats = new StatisticsService(this.store).getFocusStats();
    const state = this.store.getFocusState();
    const root = container.createDiv({ cls: "cow-focus-summary-card" });
    const top = root.createDiv({ cls: "cow-focus-summary-top" });
    const art = top.createDiv({ cls: "cow-stat-art is-blue" });
    (0, import_obsidian38.setIcon)(art.createSpan(), "headphones");
    const summary = top.createDiv({ cls: "cow-focus-summary-copy" });
    summary.createEl("strong", { text: this.formatMinutes(stats.todayMinutes) });
    summary.createSpan({ text: `\u4ECA\u65E5 ${stats.todayPomodoros} \u4E2A\u756A\u8304 \xB7 \u672C\u5468 ${this.formatMinutes(stats.weekMinutes)}` });
    if (state.isRunning) {
      root.createDiv({
        cls: "cow-focus-summary-status",
        text: `\u4E13\u6CE8\u4E2D${state.currentTask ? `\uFF1A${state.currentTask}` : ""}`
      });
    }
    const actions = root.createDiv({ cls: "cow-focus-summary-actions" });
    const primary = actions.createEl("button", { cls: "mod-cta", attr: { type: "button" } });
    (0, import_obsidian38.setIcon)(primary.createSpan(), state.isRunning ? "maximize-2" : "play");
    primary.createSpan({ text: state.isRunning ? "\u8FD4\u56DE\u4E13\u6CE8" : "\u5F00\u59CB\u4E13\u6CE8" });
    primary.addEventListener("click", () => {
      if (state.isRunning) {
        new FocusSessionWindow(this.app, this.store, this.onDataChanged).open();
        return;
      }
      new FocusSetupModal(this.app, this.store, this.onDataChanged).open();
    });
    const records = actions.createEl("button", { attr: { type: "button" } });
    (0, import_obsidian38.setIcon)(records.createSpan(), "list-checks");
    records.createSpan({ text: "\u67E5\u770B\u8BB0\u5F55" });
    records.addEventListener("click", () => new FocusRecordsModal(this.app, this.store, this.onDataChanged).open());
  }
  formatMinutes(minutes) {
    return `${minutes} min`;
  }
};
var FocusSetupModal = class extends import_obsidian38.Modal {
  constructor(app, store, onDataChanged) {
    var _a;
    super(app);
    this.store = store;
    this.onDataChanged = onDataChanged;
    this.duration = 25;
    this.customDuration = "";
    this.task = "";
    this.background = "pink";
    this.error = "";
    this.background = (_a = store.getFocusSettings().defaultBackground) != null ? _a : "pink";
  }
  onOpen() {
    applyFocusModalFrame(this, {
      className: "cute-focus-setup-modal",
      width: "min(900px, 90vw)",
      height: "min(506px, 82vh)",
      maxWidth: "95vw",
      maxHeight: "90vh",
      minWidth: "min(620px, 90vw)",
      minHeight: "min(360px, 82vh)"
    });
    this.render();
    logFocusModalSize("FocusSetup", this.modalEl);
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-focus-setup-modal");
    const header = this.contentEl.createDiv({ cls: "cute-focus-modal-header" });
    header.createEl("h2", { text: "\u5F00\u59CB\u4E13\u6CE8" });
    header.createEl("p", { text: "\u9009\u62E9\u65F6\u95F4\u3001\u5199\u4E0B\u4E13\u6CE8\u5185\u5BB9\uFF0C\u518D\u8FDB\u5165\u6C89\u6D78\u5F0F\u7A97\u53E3\u3002" });
    const content = this.contentEl.createDiv({ cls: "cute-focus-modal-content" });
    content.createEl("h3", { text: "\u9009\u62E9\u4E13\u6CE8\u65F6\u95F4" });
    const presets = content.createDiv({ cls: "cow-focus-preset-grid" });
    [15, 25, 45, 60, 90].forEach((minutes) => {
      const button = presets.createEl("button", {
        cls: this.duration === minutes && !this.customDuration ? "is-active" : "",
        attr: { type: "button" }
      });
      button.createSpan({ text: `${minutes} \u5206\u949F` });
      button.addEventListener("click", () => {
        this.duration = minutes;
        this.customDuration = "";
        this.error = "";
        this.render();
      });
    });
    new import_obsidian38.Setting(content).setName("\u81EA\u5B9A\u4E49\u65F6\u95F4").setDesc("\u5355\u4F4D\uFF1A\u5206\u949F\uFF0C\u8303\u56F4 1-180\u3002").addText((text) => text.setValue(this.customDuration).onChange((value) => {
      this.customDuration = value.trim();
      const next = Number(this.customDuration);
      if (this.customDuration && (!Number.isFinite(next) || next < 1 || next > 180)) {
        this.error = "\u8BF7\u8F93\u5165 1 \u5230 180 \u4E4B\u95F4\u7684\u5206\u949F\u6570\u3002";
        return;
      }
      if (this.customDuration) {
        this.duration = Math.round(next);
      }
      this.error = "";
      this.render();
    }));
    content.createDiv({ cls: "cow-focus-selected-duration", text: `\u672C\u6B21\u4E13\u6CE8\uFF1A${this.duration} \u5206\u949F` });
    if (this.error) {
      content.createDiv({ cls: "cow-form-error", text: this.error });
    }
    new import_obsidian38.Setting(content).setName("\u4E13\u6CE8\u5185\u5BB9").addText((text) => text.setPlaceholder("\u8FD9\u6B21\u51C6\u5907\u4E13\u6CE8\u505A\u4EC0\u4E48\uFF1F").setValue(this.task).onChange((value) => {
      this.task = value;
    }));
    content.createEl("h3", { text: "\u9009\u62E9\u4E13\u6CE8\u80CC\u666F" });
    const backgrounds = content.createDiv({ cls: "cow-focus-background-picker" });
    FOCUS_BACKGROUNDS.forEach((background) => this.renderBackgroundButton(backgrounds, background.id, background.label));
    const fileInput = content.createEl("input", {
      cls: "cow-hidden-input",
      attr: { type: "file", accept: "image/*" }
    });
    fileInput.addEventListener("change", () => {
      var _a;
      const file = (_a = fileInput.files) == null ? void 0 : _a[0];
      if (!file) return;
      const reader = new FileReader();
      reader.onload = () => {
        this.background = "custom";
        this.backgroundDataUrl = String(reader.result);
        this.render();
      };
      reader.readAsDataURL(file);
    });
    const custom = backgrounds.createEl("button", {
      cls: `cow-focus-bg-custom ${this.background === "custom" ? "is-active" : ""}`,
      attr: { type: "button" }
    });
    if (this.backgroundDataUrl) {
      custom.style.backgroundImage = `linear-gradient(rgba(255, 248, 253, 0.38), rgba(255, 248, 253, 0.38)), url("${this.backgroundDataUrl}")`;
    }
    custom.createSpan({ text: "\u81EA\u5B9A\u4E49\u80CC\u666F" });
    custom.addEventListener("click", () => fileInput.click());
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions cute-focus-modal-footer" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    const start = actions.createEl("button", { cls: "mod-cta", text: "\u5F00\u59CB\u4E13\u6CE8", attr: { type: "button" } });
    start.addEventListener("click", async () => {
      if (this.duration < 1 || this.duration > 180) {
        this.error = "\u8BF7\u8F93\u5165 1 \u5230 180 \u4E4B\u95F4\u7684\u5206\u949F\u6570\u3002";
        this.render();
        return;
      }
      await this.store.startFocusSession(this.task, this.duration, this.background, this.backgroundDataUrl);
      this.onDataChanged();
      this.close();
      new FocusSessionWindow(this.app, this.store, this.onDataChanged).open();
    });
  }
  renderBackgroundButton(container, id, label) {
    const button = container.createEl("button", {
      cls: `cow-focus-bg-${id} ${this.background === id ? "is-active" : ""}`,
      attr: { type: "button" }
    });
    button.createSpan({ text: label });
    button.addEventListener("click", () => {
      this.background = id;
      this.backgroundDataUrl = void 0;
      this.render();
    });
  }
};
var FocusSessionWindow = class extends import_obsidian38.Modal {
  constructor(app, store, onDataChanged) {
    super(app);
    this.store = store;
    this.onDataChanged = onDataChanged;
    this.maximized = false;
    this.completed = false;
    this.completionTask = "";
    this.completionDuration = 0;
  }
  onOpen() {
    this.applyModalFrame();
    this.render();
    logFocusModalSize("FocusSession", this.modalEl);
    this.timer = window.setInterval(() => void this.tick(), 1e3);
  }
  onClose() {
    if (this.timer) {
      window.clearInterval(this.timer);
      this.timer = void 0;
    }
  }
  render() {
    var _a;
    this.applyModalFrame();
    this.contentEl.empty();
    this.contentEl.addClass("cow-focus-session-window", `cow-focus-bg-${(_a = this.store.getFocusState().background) != null ? _a : "pink"}`);
    const state = this.store.getFocusState();
    if (state.backgroundDataUrl) {
      this.contentEl.style.backgroundImage = `linear-gradient(rgba(255, 248, 253, 0.62), rgba(255, 248, 253, 0.62)), url("${state.backgroundDataUrl}")`;
    }
    this.renderWindowControls();
    if (this.completed) {
      this.renderCompleted();
      return;
    }
    const body = this.contentEl.createDiv({ cls: "cow-focus-window-body" });
    body.createSpan({ cls: "cow-focus-window-label", text: state.currentTask ? "\u6B63\u5728\u4E13\u6CE8\uFF1A" : "\u4E13\u6CE8\u65F6\u95F4" });
    body.createEl("h2", { text: state.currentTask || "\u4FDD\u6301\u5F53\u4E0B\u8FD9\u4E00\u8F6E" });
    body.createDiv({ cls: "cow-focus-session-countdown", text: this.formatSeconds(state.remainingSeconds) });
    body.createDiv({ cls: "cow-focus-session-status", text: state.isPaused ? "\u5DF2\u6682\u505C" : "\u4E13\u6CE8\u4E2D" });
    const actions = body.createDiv({ cls: "cow-focus-window-actions" });
    const toggle = actions.createEl("button", { cls: "mod-cta", attr: { type: "button" } });
    (0, import_obsidian38.setIcon)(toggle.createSpan(), state.isPaused ? "play" : "pause");
    toggle.createSpan({ text: state.isPaused ? "\u7EE7\u7EED" : "\u6682\u505C" });
    toggle.addEventListener("click", async () => {
      if (state.isPaused) {
        await this.store.resumeFocusSession();
      } else {
        await this.store.pauseFocusSession();
      }
      this.onDataChanged();
      this.render();
    });
    const end = actions.createEl("button", { attr: { type: "button" } });
    (0, import_obsidian38.setIcon)(end.createSpan(), "square");
    end.createSpan({ text: "\u7ED3\u675F" });
    end.addEventListener("click", () => new EndFocusConfirmModal(this.app, this.store, async () => {
      this.onDataChanged();
      this.close();
    }).open());
  }
  renderWindowControls() {
    const controls = this.contentEl.createDiv({ cls: "cow-focus-window-controls" });
    controls.createEl("button", { text: "\u2014", attr: { type: "button", "aria-label": "\u6700\u5C0F\u5316" } }).addEventListener("click", () => {
      this.onDataChanged();
      this.close();
    });
    controls.createEl("button", { text: this.maximized ? "\u25A3" : "\u25A1", attr: { type: "button", "aria-label": this.maximized ? "\u6062\u590D\u7A97\u53E3" : "\u6700\u5927\u5316" } }).addEventListener("click", () => {
      this.maximized = !this.maximized;
      this.render();
    });
    controls.createEl("button", { text: "\xD7", attr: { type: "button", "aria-label": "\u5173\u95ED" } }).addEventListener("click", () => {
      if (this.store.getFocusState().isRunning) {
        new CloseFocusWindowModal(this.app, this.store, async () => {
          this.onDataChanged();
          this.close();
        }).open();
        return;
      }
      this.close();
    });
  }
  applyModalFrame() {
    applyFocusModalFrame(this, {
      className: "cute-focus-session-modal",
      width: this.maximized ? "96vw" : "min(1100px, 94vw)",
      height: this.maximized ? "92vh" : "min(720px, 90vh)",
      maxWidth: "96vw",
      maxHeight: "94vh",
      minWidth: "min(640px, 94vw)",
      minHeight: "min(400px, 90vh)"
    });
    this.modalEl.style.resize = this.maximized ? "none" : "both";
    if (this.maximized) {
      this.modalEl.addClass("is-maximized");
    } else {
      this.modalEl.removeClass("is-maximized");
    }
  }
  renderCompleted() {
    const stats = new StatisticsService(this.store).getFocusStats();
    const body = this.contentEl.createDiv({ cls: "cow-focus-window-body cow-focus-complete-body" });
    body.createEl("h2", { text: "\u{1F389} \u4E13\u6CE8\u5B8C\u6210" });
    body.createDiv({ cls: "cow-focus-session-countdown", text: `${this.completionDuration} min` });
    body.createSpan({ text: `\u5185\u5BB9\uFF1A${this.completionTask || "\u4E13\u6CE8"}` });
    body.createSpan({ text: `\u4ECA\u65E5\u7D2F\u8BA1\uFF1A${stats.todayMinutes} min \xB7 \u4ECA\u65E5\u756A\u8304\uFF1A${stats.todayPomodoros} \u4E2A` });
    const actions = body.createDiv({ cls: "cow-focus-window-actions" });
    actions.createEl("button", { text: "\u5B8C\u6210", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u518D\u6765\u4E00\u6B21", attr: { type: "button" } }).addEventListener("click", () => {
      this.close();
      new FocusSetupModal(this.app, this.store, this.onDataChanged).open();
    });
  }
  async tick() {
    var _a, _b;
    const state = this.store.getFocusState();
    if (state.isRunning && !state.isPaused && state.remainingSeconds <= 0) {
      this.completionTask = (_a = state.currentTask) != null ? _a : "\u4E13\u6CE8";
      this.completionDuration = (_b = state.plannedDuration) != null ? _b : this.store.getFocusSettings().focusDuration;
      await this.store.completeCurrentFocusPhase();
      this.completed = true;
      this.onDataChanged();
      new import_obsidian38.Notice("\u4E13\u6CE8\u5B8C\u6210");
    }
    this.render();
  }
  formatSeconds(seconds) {
    const safe = Math.max(0, seconds);
    const mins = Math.floor(safe / 60);
    const secs = safe % 60;
    return `${String(mins).padStart(2, "0")}:${String(secs).padStart(2, "0")}`;
  }
};
var CloseFocusWindowModal = class extends import_obsidian38.Modal {
  constructor(app, store, onDone) {
    super(app);
    this.store = store;
    this.onDone = onDone;
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: "\u5F53\u524D\u4E13\u6CE8\u5C1A\u672A\u7ED3\u675F" });
    this.contentEl.createEl("p", { text: "\u8BF7\u9009\u62E9\u7EE7\u7EED\u540E\u53F0\u4E13\u6CE8\uFF0C\u6216\u7ED3\u675F\u672C\u6B21\u4E13\u6CE8\u3002" });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u7EE7\u7EED\u540E\u53F0\u4E13\u6CE8", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      await this.onDone();
      this.close();
    });
    actions.createEl("button", { text: "\u7ED3\u675F\u672C\u6B21\u4E13\u6CE8", cls: "mod-warning", attr: { type: "button" } }).addEventListener("click", () => {
      this.close();
      new EndFocusConfirmModal(this.app, this.store, this.onDone).open();
    });
  }
};
var EndFocusConfirmModal = class extends import_obsidian38.Modal {
  constructor(app, store, onDone) {
    super(app);
    this.store = store;
    this.onDone = onDone;
  }
  onOpen() {
    const elapsed = this.getElapsedText();
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: "\u7ED3\u675F\u672C\u6B21\u4E13\u6CE8\uFF1F" });
    this.contentEl.createEl("p", { text: `\u5DF2\u4E13\u6CE8\uFF1A${elapsed}` });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u7ED3\u675F\u5E76\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      await this.store.endFocusSession(false, true);
      await this.onDone();
      this.close();
    });
    actions.createEl("button", { text: "\u7ED3\u675F\u4F46\u4E0D\u4FDD\u5B58", cls: "mod-warning", attr: { type: "button" } }).addEventListener("click", async () => {
      await this.store.endFocusSession(false, false);
      await this.onDone();
      this.close();
    });
  }
  getElapsedText() {
    var _a;
    const state = this.store.getFocusState();
    const planned = ((_a = state.plannedDuration) != null ? _a : this.store.getFocusSettings().focusDuration) * 60;
    const elapsed = Math.max(0, planned - state.remainingSeconds);
    return `${Math.floor(elapsed / 60)} \u5206 ${String(elapsed % 60).padStart(2, "0")} \u79D2`;
  }
};
var FocusRecordsModal = class extends import_obsidian38.Modal {
  constructor(app, store, onDataChanged) {
    super(app);
    this.store = store;
    this.onDataChanged = onDataChanged;
    this.filter = "today";
    this.dateValue = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
  }
  onOpen() {
    applyFocusModalFrame(this, {
      className: "cute-focus-records-modal",
      width: "min(1050px, 92vw)",
      height: "min(700px, 85vh)",
      maxWidth: "96vw",
      maxHeight: "92vh",
      minWidth: "min(680px, 92vw)",
      minHeight: "min(420px, 85vh)"
    });
    this.render();
    logFocusModalSize("FocusRecords", this.modalEl);
  }
  render() {
    const stats = new StatisticsService(this.store).getFocusStats();
    const records = this.getFilteredRecords();
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-focus-records-modal");
    const header = this.contentEl.createDiv({ cls: "cute-focus-modal-header" });
    header.createEl("h2", { text: "\u4E13\u6CE8\u8BB0\u5F55" });
    const content = this.contentEl.createDiv({ cls: "cute-focus-modal-content" });
    const statGrid = content.createDiv({ cls: "cow-stats-card-grid" });
    [["\u4ECA\u65E5\u4E13\u6CE8", `${stats.todayMinutes} min`], ["\u4ECA\u65E5\u756A\u8304", stats.todayPomodoros], ["\u672C\u5468\u4E13\u6CE8", `${stats.weekMinutes} min`], ["\u672C\u6708\u4E13\u6CE8", `${stats.monthMinutes} min`]].forEach(([label, value]) => {
      const card = statGrid.createDiv({ cls: "cow-stats-card" });
      card.createEl("strong", { text: String(value) });
      card.createSpan({ text: String(label) });
    });
    const filters = content.createDiv({ cls: "cow-focus-filter-row" });
    [
      ["today", "\u4ECA\u5929"],
      ["week", "\u672C\u5468"],
      ["month", "\u672C\u6708"],
      ["all", "\u5168\u90E8"],
      ["date", "\u65E5\u671F"]
    ].forEach(([id, label]) => {
      const button = filters.createEl("button", { cls: this.filter === id ? "is-active" : "", attr: { type: "button" } });
      button.createSpan({ text: label });
      button.addEventListener("click", () => {
        this.filter = id;
        this.render();
      });
    });
    if (this.filter === "date") {
      new import_obsidian38.Setting(content).setName("\u65E5\u671F").addText((text) => text.setValue(this.dateValue).onChange((value) => {
        this.dateValue = value.trim();
        this.render();
      }));
    }
    const list = content.createDiv({ cls: "cow-focus-record-list" });
    if (records.length === 0) {
      list.createEl("p", { cls: "cow-empty-state", text: "\u6CA1\u6709\u5339\u914D\u7684\u4E13\u6CE8\u8BB0\u5F55\u3002" });
      return;
    }
    records.forEach((record) => this.renderRecord(list, record));
  }
  renderRecord(container, record) {
    var _a, _b, _c;
    const row = container.createDiv({ cls: "cow-focus-record-row" });
    const body = row.createDiv();
    body.createEl("strong", { text: record.date });
    body.createSpan({ text: `${this.formatTime(record.startedAt)} - ${this.formatTime(record.endedAt)} \xB7 ${record.task || "\u4E13\u6CE8"}` });
    body.createSpan({ text: `${(_a = record.actualDurationMinutes) != null ? _a : record.duration} min / \u8BA1\u5212 ${(_c = (_b = record.plannedDurationMinutes) != null ? _b : record.plannedDuration) != null ? _c : record.duration} min \xB7 ${record.completed ? "\u5DF2\u5B8C\u6210" : "\u63D0\u524D\u7ED3\u675F"}` });
    const actions = row.createDiv({ cls: "cow-list-item-actions" });
    const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u8BB0\u5F55" } });
    (0, import_obsidian38.setIcon)(edit, "pencil");
    edit.addEventListener("click", () => new EditFocusRecordModal(this.app, this.store, record, () => {
      this.onDataChanged();
      this.render();
    }).open());
    const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u8BB0\u5F55" } });
    (0, import_obsidian38.setIcon)(remove, "trash-2");
    remove.addEventListener("click", () => new DeleteFocusRecordModal(this.app, this.store, record, async () => {
      this.onDataChanged();
      this.render();
    }).open());
  }
  getFilteredRecords() {
    const records = this.store.getFocusRecords();
    const today6 = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
    if (this.filter === "today") return records.filter((record) => record.date === today6);
    if (this.filter === "date") return records.filter((record) => record.date === this.dateValue);
    if (this.filter === "month") return records.filter((record) => record.date.startsWith(today6.slice(0, 7)));
    if (this.filter === "week") {
      const week = new Set(this.store.getCurrentWeekDates());
      return records.filter((record) => week.has(record.date));
    }
    return records;
  }
  formatTime(value) {
    if (!value) return "--:--";
    return new Date(value).toLocaleTimeString("zh-CN", { hour: "2-digit", minute: "2-digit" });
  }
};
var EditFocusRecordModal = class extends import_obsidian38.Modal {
  constructor(app, store, record, onDone) {
    var _a;
    super(app);
    this.store = store;
    this.record = record;
    this.onDone = onDone;
    this.date = record.date;
    this.task = record.task;
    this.duration = (_a = record.actualDurationMinutes) != null ? _a : record.duration;
  }
  onOpen() {
    applyFocusModalFrame(this, {
      className: "cute-edit-focus-record-modal",
      width: "min(640px, 88vw)",
      height: "min(420px, 72vh)",
      maxWidth: "92vw",
      maxHeight: "82vh",
      minWidth: "min(420px, 88vw)",
      minHeight: "min(300px, 72vh)"
    });
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-edit-focus-record-modal");
    const header = this.contentEl.createDiv({ cls: "cute-focus-modal-header" });
    header.createEl("h2", { text: "\u7F16\u8F91\u4E13\u6CE8\u8BB0\u5F55" });
    const content = this.contentEl.createDiv({ cls: "cute-focus-modal-content" });
    new import_obsidian38.Setting(content).setName("\u65E5\u671F").addText((text) => text.setValue(this.date).onChange((value) => {
      this.date = value.trim();
    }));
    new import_obsidian38.Setting(content).setName("\u4E13\u6CE8\u5185\u5BB9").addText((text) => text.setValue(this.task).onChange((value) => {
      this.task = value;
    }));
    new import_obsidian38.Setting(content).setName("\u5B9E\u9645\u4E13\u6CE8\u65F6\u957F").setDesc("\u5355\u4F4D\uFF1A\u5206\u949F").addText((text) => {
      text.inputEl.type = "number";
      text.setValue(String(this.duration));
      text.onChange((value) => {
        this.duration = Math.max(0, Number(value) || 0);
      });
    });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions cute-focus-modal-footer" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      await this.store.updateFocusRecord(this.record.id, {
        date: this.date,
        task: this.task.trim() || "\u4E13\u6CE8",
        duration: this.duration,
        actualDurationMinutes: this.duration
      });
      this.onDone();
      this.close();
    });
  }
};
var DeleteFocusRecordModal = class extends import_obsidian38.Modal {
  constructor(app, store, record, onDone) {
    super(app);
    this.store = store;
    this.record = record;
    this.onDone = onDone;
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: "\u5220\u9664\u8FD9\u6761\u4E13\u6CE8\u8BB0\u5F55\uFF1F" });
    this.contentEl.createEl("p", { text: `${this.record.date} \xB7 ${this.record.task || "\u4E13\u6CE8"}` });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u5220\u9664", cls: "mod-warning", attr: { type: "button" } }).addEventListener("click", async () => {
      await this.store.deleteFocusRecord(this.record.id);
      await this.onDone();
      this.close();
    });
  }
};

// src/components/modules/ModulesControls.ts
var import_obsidian39 = require("obsidian");
function renderSwitch(container, checked, onChange) {
  const input = container.createEl("input", { type: "checkbox", cls: "cow-switch" });
  input.checked = checked;
  input.addEventListener("change", () => onChange(input.checked));
  return input;
}
function renderField(container, label, value, onChange, type = "text") {
  const row = container.createDiv({ cls: "cow-setting-row" });
  row.createSpan({ text: label });
  const input = row.createEl("input", { type, value });
  input.addEventListener("change", () => onChange(input.value));
}
var EnabledModulesOverviewSection = class {
  constructor(store) {
    this.store = store;
  }
  render(container) {
    const currentPage = this.store.getData().currentPage;
    const currentEnabled = this.store.getSectionsForPage(currentPage).length;
    const allEnabled = this.store.getAllSections().filter((section) => section.enabled).length;
    const total = this.store.getAllSections().length;
    const grid = container.createDiv({ cls: "cow-reading-stat-grid" });
    [
      ["\u5F53\u524D\u9875\u9762", `${currentEnabled} \u4E2A`],
      ["\u5168\u90E8\u542F\u7528", `${allEnabled} \u4E2A`],
      ["\u6A21\u5757\u603B\u6570", `${total} \u4E2A`],
      ["\u9875\u9762\u6570\u91CF", `${DASHBOARD_PAGES.length} \u4E2A`]
    ].forEach(([label, value]) => {
      const item = grid.createDiv();
      item.createEl("strong", { text: value });
      item.createSpan({ text: label });
    });
  }
};
var MANAGED_PAGES = ["overview", "research", "reading", "fitness", "finance", "goals"];
var LAYOUT_TEMPLATES = [
  { id: "two-columns", label: "\u5747\u5300\u53CC\u5217", description: "\u4E24\u4E2A\u7B49\u5BBD\u5217\uFF0C\u9002\u5408\u901A\u7528\u4FE1\u606F\u6D41\u3002", columns: 2 },
  { id: "three-columns", label: "\u5747\u5300\u4E09\u5217", description: "\u4E09\u5217\u5E76\u6392\uFF0C\u9002\u5408\u8F7B\u91CF\u5361\u7247\u3002", columns: 3 },
  { id: "left-large", label: "\u5DE6\u5927\u53F3\u5C0F", description: "\u5DE6\u4FA7\u91CD\u70B9\uFF0C\u53F3\u4FA7\u4E0A\u4E0B\u6392\u5217\u3002", columns: 4 },
  { id: "right-large", label: "\u5DE6\u5C0F\u53F3\u5927", description: "\u53F3\u4FA7\u91CD\u70B9\uFF0C\u5DE6\u4FA7\u4E0A\u4E0B\u6392\u5217\u3002", columns: 4 },
  { id: "top-full", label: "\u9876\u90E8\u901A\u680F", description: "\u9996\u4E2A\u677F\u5757\u901A\u680F\uFF0C\u4E0B\u65B9\u53CC\u5217\u3002", columns: 4 },
  { id: "left-feature", label: "\u5DE6\u5217\u91CD\u70B9", description: "\u5DE6\u4FA7\u6574\u5217\uFF0C\u53F3\u4FA7\u7F51\u683C\u3002", columns: 4 }
];
var HomeLayoutManagerSection = class {
  constructor(store, onDataChanged) {
    this.store = store;
    this.onDataChanged = onDataChanged;
    const currentPage = this.store.getData().currentPage;
    this.selectedPage = MANAGED_PAGES.includes(currentPage) ? currentPage : "overview";
  }
  render(container) {
    var _a;
    this.host = container;
    container.empty();
    const header = container.createDiv({ cls: "cow-layout-manager-header" });
    header.createSpan({ text: "\u5F53\u524D\u6A21\u5757" });
    const select = header.createEl("select", { attr: { "aria-label": "\u9009\u62E9\u8981\u8BBE\u7F6E\u5E03\u5C40\u7684\u6A21\u5757" } });
    MANAGED_PAGES.forEach((page) => select.createEl("option", { value: page, text: PAGE_LABELS[page] }));
    select.value = this.selectedPage;
    select.addEventListener("change", () => {
      this.selectedPage = select.value;
      this.selectedSectionId = void 0;
      this.render(container);
    });
    const layout = this.store.getModuleLayout(this.selectedPage);
    const current = layout.mode;
    const grid = container.createDiv({ cls: "cow-layout-picker" });
    [
      ["default", "\u9ED8\u8BA4\u5E03\u5C40", "\u4FDD\u6301\u5F53\u524D\u9875\u9762\u6807\u51C6\u5361\u7247\u8282\u594F"],
      ["compact", "\u7D27\u51D1\u5E03\u5C40", "\u589E\u52A0\u7F51\u683C\u5217\u6570\uFF0C\u51CF\u5C11\u95F4\u8DDD\u548C\u7559\u767D"],
      ["minimal", "\u6781\u7B80\u5E03\u5C40", "\u5355\u5217\u5C55\u793A\uFF0C\u51CF\u5C11\u88C5\u9970\u4F46\u4FDD\u7559\u6570\u636E"],
      ["custom", "\u81EA\u5B9A\u4E49\u5E03\u5C40", "\u4F7F\u7528\u6A21\u677F\u3001\u8DE8\u5EA6\u548C\u987A\u5E8F\u63A7\u5236\u677F\u5757"]
    ].forEach(([id, title, desc]) => {
      const button = grid.createEl("button", { cls: current === id ? "is-active" : "", attr: { type: "button" } });
      button.createEl("strong", { text: title });
      button.createSpan({ text: desc });
      button.addEventListener("click", async () => {
        await this.store.setModuleLayoutMode(this.selectedPage, id);
        this.rerender();
      });
    });
    const note = container.createDiv({ cls: "cow-meta-line" });
    note.createSpan({ text: `${PAGE_LABELS[this.selectedPage]} \u5F53\u524D\u5217\u6570\uFF1A${(_a = layout.columns) != null ? _a : 12}\u3002\u81EA\u5B9A\u4E49\u5E03\u5C40\u4F1A\u4FDD\u5B58 columns / colSpan / rowSpan\uFF0C\u5E76\u53EA\u4F5C\u7528\u4E8E\u5F53\u524D\u6A21\u5757\u3002` });
    if (layout.mode === "custom") {
      this.renderCustomLayoutEditor(container, layout);
    }
  }
  renderCustomLayoutEditor(container, layout) {
    container.createEl("h4", { text: "\u5E03\u5C40\u6A21\u677F" });
    const templates = container.createDiv({ cls: "cow-layout-template-grid" });
    LAYOUT_TEMPLATES.forEach((template) => {
      const button = templates.createEl("button", { cls: layout.templateId === template.id ? "is-active" : "", attr: { type: "button" } });
      const title = button.createDiv({ cls: "cow-layout-template-title" });
      title.createEl("strong", { text: template.label });
      if (layout.templateId === template.id) title.createSpan({ text: "\u2713" });
      this.renderTemplateMiniature(button, template.id);
      button.createSpan({ text: template.description });
      button.addEventListener("click", async () => {
        const sections = this.getManagedSections();
        await this.store.updateModuleLayout(this.selectedPage, {
          mode: "custom",
          columns: template.columns,
          templateId: template.id,
          sections: this.createTemplateSectionLayouts(template, sections)
        });
        this.rerender();
      });
    });
    container.createEl("h4", { text: "\u5E03\u5C40\u9884\u89C8" });
    this.renderPreview(container, layout);
    container.createEl("h4", { text: "\u677F\u5757\u5E03\u5C40" });
    this.renderSectionEditor(container, layout);
  }
  renderTemplateMiniature(container, id) {
    const mini = container.createDiv({ cls: `cow-layout-template-mini is-${id}` });
    Array.from({ length: id === "left-feature" ? 5 : 4 }, (_, index) => {
      mini.createDiv({ text: String.fromCharCode(65 + index) });
    });
  }
  renderPreview(container, layout) {
    var _a;
    const sections = this.getManagedSections();
    const preview = container.createDiv({ cls: "cow-layout-preview" });
    preview.style.setProperty("--preview-columns", String((_a = layout.columns) != null ? _a : 4));
    sections.forEach((section) => {
      var _a2, _b, _c, _d, _e;
      const sectionLayout = this.getSectionLayout(layout, section);
      const tile = preview.createEl("button", {
        cls: [
          "cow-layout-preview-tile",
          section.enabled ? "" : "is-disabled",
          this.selectedSectionId === section.id ? "is-selected" : ""
        ].filter(Boolean).join(" "),
        attr: { type: "button" }
      });
      tile.style.gridColumn = `span ${this.clamp((_a2 = sectionLayout.colSpan) != null ? _a2 : 1, 1, (_b = layout.columns) != null ? _b : 4)}`;
      tile.style.gridRow = `span ${this.clamp((_c = sectionLayout.rowSpan) != null ? _c : 1, 1, 3)}`;
      tile.createEl("strong", { text: section.title });
      tile.createSpan({ text: `${(_d = sectionLayout.colSpan) != null ? _d : 1}\xD7${(_e = sectionLayout.rowSpan) != null ? _e : 1}${section.enabled ? "" : " \xB7 \u5DF2\u9690\u85CF"}` });
      tile.addEventListener("click", () => {
        this.selectedSectionId = section.id;
        this.rerender();
      });
    });
  }
  renderSectionEditor(container, layout) {
    var _a, _b, _c, _d;
    const sections = this.getManagedSections();
    const selected = (_a = sections.find((section) => section.id === this.selectedSectionId)) != null ? _a : sections[0];
    if (selected && !this.selectedSectionId) this.selectedSectionId = selected.id;
    const list = container.createDiv({ cls: "cow-layout-section-list" });
    sections.forEach((section) => {
      const row = list.createDiv({ cls: `cow-layout-section-row ${this.selectedSectionId === section.id ? "is-selected" : ""} ${section.enabled ? "" : "is-disabled"}` });
      const title = row.createDiv({ cls: "cow-section-manager-title" });
      title.createEl("strong", { text: section.title });
      title.createSpan({ text: section.enabled ? "\u5DF2\u542F\u7528" : "\u5DF2\u9690\u85CF\uFF0C\u91CD\u65B0\u542F\u7528\u540E\u4F1A\u6062\u590D\u6B64\u5E03\u5C40" });
      row.addEventListener("click", () => {
        this.selectedSectionId = section.id;
        this.rerender();
      });
      const actions = row.createDiv({ cls: "cow-list-item-actions" });
      const up = actions.createEl("button", { attr: { type: "button", "aria-label": "\u4E0A\u79FB" } });
      (0, import_obsidian39.setIcon)(up, "arrow-up");
      up.addEventListener("click", async (event) => {
        event.stopPropagation();
        await this.store.moveSectionLayout(this.selectedPage, section.id, "up");
        this.selectedSectionId = section.id;
        this.rerender();
      });
      const down = actions.createEl("button", { attr: { type: "button", "aria-label": "\u4E0B\u79FB" } });
      (0, import_obsidian39.setIcon)(down, "arrow-down");
      down.addEventListener("click", async (event) => {
        event.stopPropagation();
        await this.store.moveSectionLayout(this.selectedPage, section.id, "down");
        this.selectedSectionId = section.id;
        this.rerender();
      });
    });
    if (!selected) {
      container.createDiv({ cls: "cow-empty-state", text: "\u5F53\u524D\u6A21\u5757\u6682\u65E0\u53EF\u914D\u7F6E\u677F\u5757\u3002" });
      return;
    }
    const selectedLayout = this.getSectionLayout(layout, selected);
    const panel = container.createDiv({ cls: "cow-layout-selected-panel" });
    panel.createEl("strong", { text: `\u5F53\u524D\u9009\u4E2D\uFF1A${selected.title}` });
    panel.createDiv({ cls: "cow-meta-line", text: selected.enabled ? "\u4FEE\u6539\u5BBD\u5EA6 / \u9AD8\u5EA6\u8DE8\u5EA6\u540E\u4F1A\u7ACB\u5373\u5E94\u7528\u5230\u5F53\u524D\u6A21\u5757\u3002" : "\u8BE5 Section \u5DF2\u9690\u85CF\uFF0C\u5E03\u5C40\u8BBE\u7F6E\u4F1A\u4FDD\u7559\u5230\u6062\u590D\u663E\u793A\u540E\u7EE7\u7EED\u751F\u6548\u3002" });
    const controls = panel.createDiv({ cls: "cow-layout-span-controls" });
    this.renderSpanButtons(controls, "\u5BBD\u5EA6", (_b = layout.columns) != null ? _b : 4, (_c = selectedLayout.colSpan) != null ? _c : 1, async (value) => {
      await this.store.updateSectionLayout(this.selectedPage, selected.id, { colSpan: value });
      this.selectedSectionId = selected.id;
      this.rerender();
    });
    this.renderSpanButtons(controls, "\u9AD8\u5EA6", 3, (_d = selectedLayout.rowSpan) != null ? _d : 1, async (value) => {
      await this.store.updateSectionLayout(this.selectedPage, selected.id, { rowSpan: value });
      this.selectedSectionId = selected.id;
      this.rerender();
    });
  }
  renderSpanButtons(container, label, max, current, onPick) {
    const group = container.createDiv({ cls: "cow-layout-span-group" });
    group.createSpan({ text: label });
    for (let value = 1; value <= max; value += 1) {
      const button = group.createEl("button", { cls: current === value ? "is-active" : "", text: String(value), attr: { type: "button" } });
      button.addEventListener("click", () => void onPick(value));
    }
  }
  createTemplateSectionLayouts(template, sections) {
    const layouts = {};
    sections.forEach((section, index) => {
      layouts[section.id] = {
        order: (index + 1) * 10,
        colSpan: this.templateColSpan(template.id, index),
        rowSpan: this.templateRowSpan(template.id, index)
      };
    });
    return layouts;
  }
  templateColSpan(template, index) {
    if (template === "two-columns") return 1;
    if (template === "three-columns") return 1;
    if (template === "top-full" && index === 0) return 4;
    if (template === "top-full") return 2;
    if (template === "left-feature") return index === 0 ? 2 : 1;
    return 2;
  }
  templateRowSpan(template, index) {
    if ((template === "left-large" || template === "left-feature") && index === 0) return 2;
    if (template === "right-large" && index === 1) return 2;
    return 1;
  }
  getManagedSections() {
    const layout = this.store.getModuleLayout(this.selectedPage);
    return this.store.getAllSections().filter((section) => section.page === this.selectedPage).sort((left, right) => this.getSectionLayout(layout, left).order - this.getSectionLayout(layout, right).order);
  }
  getSectionLayout(layout, section) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
    return {
      order: (_c = (_b = (_a = layout.sections) == null ? void 0 : _a[section.id]) == null ? void 0 : _b.order) != null ? _c : section.order,
      colSpan: this.clamp((_f = (_e = (_d = layout.sections) == null ? void 0 : _d[section.id]) == null ? void 0 : _e.colSpan) != null ? _f : 1, 1, (_g = layout.columns) != null ? _g : 4),
      rowSpan: this.clamp((_j = (_i = (_h = layout.sections) == null ? void 0 : _h[section.id]) == null ? void 0 : _i.rowSpan) != null ? _j : 1, 1, 3)
    };
  }
  clamp(value, min, max) {
    return Math.max(min, Math.min(max, Number(value) || min));
  }
  rerender() {
    if (this.host) this.render(this.host);
  }
};
var CARD_COLORS2 = [
  { id: "default", label: "\u9ED8\u8BA4" },
  { id: "pink", label: "\u7C89\u8272" },
  { id: "cream", label: "\u5976\u6CB9\u9EC4" },
  { id: "mint", label: "\u8584\u8377\u7EFF" },
  { id: "sky", label: "\u5929\u84DD" },
  { id: "lavender", label: "\u6DE1\u7D2B" }
];
var WIDTHS2 = [
  { id: "sm", label: "\u5C0F" },
  { id: "md", label: "\u4E2D" },
  { id: "lg", label: "\u5927" },
  { id: "full", label: "\u6574\u884C" }
];
var FunctionalSectionManagerSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
    this.selectedPage = "overview";
    const currentPage = this.store.getData().currentPage;
    this.selectedPage = MANAGED_PAGES.includes(currentPage) ? currentPage : "overview";
  }
  render(container) {
    this.host = container;
    container.empty();
    const root = container.createDiv({ cls: "cow-section-manager" });
    this.renderPagePicker(root);
    const columns = root.createDiv({ cls: "cow-section-manager-columns" });
    this.renderEnabled(columns);
    this.renderHidden(columns);
    this.renderAddable(columns);
  }
  renderPagePicker(container) {
    const picker = container.createDiv({ cls: "cow-section-manager-picker" });
    MANAGED_PAGES.forEach((page) => {
      const button = picker.createEl("button", {
        cls: page === this.selectedPage ? "is-active" : "",
        attr: { type: "button" }
      });
      button.createSpan({ text: PAGE_LABELS[page] });
      button.addEventListener("click", () => {
        this.selectedPage = page;
        container.empty();
        this.renderPagePicker(container);
        const columns = container.createDiv({ cls: "cow-section-manager-columns" });
        this.renderEnabled(columns);
        this.renderHidden(columns);
        this.renderAddable(columns);
      });
    });
  }
  renderEnabled(container) {
    const panel = container.createDiv({ cls: "cow-section-manager-panel" });
    panel.createEl("h4", { text: "\u5F53\u524D\u542F\u7528\u6A21\u5757" });
    const list = panel.createDiv({ cls: "cow-module-sort-list" });
    const sections = this.store.getAllSections().filter((section) => section.page === this.selectedPage && section.enabled);
    if (sections.length === 0) {
      list.createEl("p", { cls: "cow-empty-state", text: "\u6682\u65E0\u542F\u7528\u6A21\u5757\u3002" });
    }
    sections.forEach((section) => this.renderSectionRow(list, section, "enabled"));
  }
  renderHidden(container) {
    const panel = container.createDiv({ cls: "cow-section-manager-panel" });
    panel.createEl("h4", { text: "\u9690\u85CF\u6A21\u5757" });
    const list = panel.createDiv({ cls: "cow-module-sort-list" });
    const sections = this.store.getAllSections().filter((section) => section.page === this.selectedPage && !section.enabled);
    if (sections.length === 0) {
      list.createEl("p", { cls: "cow-empty-state", text: "\u6682\u65E0\u9690\u85CF\u6A21\u5757\u3002" });
    }
    sections.forEach((section) => this.renderSectionRow(list, section, "hidden"));
  }
  renderAddable(container) {
    const panel = container.createDiv({ cls: "cow-section-manager-panel" });
    const header = panel.createDiv({ cls: "cow-list-item-head" });
    header.createEl("h4", { text: "\u53EF\u6DFB\u52A0\u6A21\u5757" });
    const custom = header.createEl("button", { cls: "cow-small-action", attr: { type: "button" } });
    (0, import_obsidian39.setIcon)(custom.createSpan(), "plus");
    custom.createSpan({ text: "\u81EA\u5B9A\u4E49" });
    custom.addEventListener("click", () => {
      new CustomSectionModal(this.app, this.selectedPage, async (input) => {
        await this.store.addCustomSection(input);
        this.rerender();
      }).open();
    });
    const existingTypes = new Set(this.store.getAllSections().filter((section) => section.page === this.selectedPage).map((section) => section.type));
    const addable = AVAILABLE_MODULES.filter((module2) => module2.page === this.selectedPage && !existingTypes.has(module2.type));
    const list = panel.createDiv({ cls: "cow-module-sort-list" });
    if (addable.length === 0) {
      list.createEl("p", { cls: "cow-empty-state", text: "\u9ED8\u8BA4\u6A21\u5757\u90FD\u5DF2\u5728\u5E03\u5C40\u4E2D\uFF0C\u53EF\u7EE7\u7EED\u521B\u5EFA\u81EA\u5B9A\u4E49\u5206\u533A\u3002" });
    }
    addable.forEach((module2) => {
      const row = list.createDiv({ cls: "cow-section-manager-row" });
      row.createDiv().createEl("strong", { text: module2.title });
      row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: module2.description });
      const actions = row.createDiv({ cls: "cow-list-item-actions" });
      const add = actions.createEl("button", { attr: { type: "button", "aria-label": "\u6DFB\u52A0\u6A21\u5757" } });
      (0, import_obsidian39.setIcon)(add, "plus");
      add.addEventListener("click", async () => {
        await this.store.addSection(this.selectedPage, module2.type);
        this.rerender();
      });
    });
  }
  renderSectionRow(list, section, mode) {
    const row = list.createDiv({ cls: "cow-section-manager-row", attr: { draggable: mode === "enabled" ? "true" : "false", "data-id": section.id } });
    const title = row.createDiv({ cls: "cow-section-manager-title" });
    if (mode === "enabled") {
      const handle = title.createSpan({ cls: "cow-drag-handle" });
      (0, import_obsidian39.setIcon)(handle, "grip-vertical");
    }
    title.createEl("strong", { text: section.title });
    title.createSpan({ text: this.getSectionDescription(section) });
    const controls = row.createDiv({ cls: "cow-section-manager-controls" });
    this.renderColorSelect(controls, section);
    this.renderWidthSelect(controls, section);
    const actions = row.createDiv({ cls: "cow-list-item-actions" });
    if (mode === "enabled") {
      this.renderIconButton(actions, "eye-off", "\u9690\u85CF", async () => this.store.setSectionEnabled(section.id, false));
    } else {
      this.renderIconButton(actions, "eye", "\u6062\u590D", async () => this.store.setSectionEnabled(section.id, true));
    }
    this.renderIconButton(actions, "trash-2", "\u5220\u9664", async () => this.store.removeSection(section.id));
    if (mode === "enabled") {
      row.addEventListener("dragstart", () => {
        this.draggingId = section.id;
        row.addClass("is-dragging");
      });
      row.addEventListener("dragend", () => {
        this.draggingId = void 0;
        row.removeClass("is-dragging");
      });
      row.addEventListener("dragover", (event) => {
        event.preventDefault();
        if (!this.draggingId || this.draggingId === section.id) return;
        const draggingEl = list.querySelector(`[data-id="${this.draggingId}"]`);
        if (draggingEl) list.insertBefore(draggingEl, row);
      });
      row.addEventListener("drop", async () => {
        const ids = Array.from(list.querySelectorAll(".cow-section-manager-row")).map((item) => {
          var _a;
          return (_a = item.dataset.id) != null ? _a : "";
        });
        await this.store.reorderSections(this.selectedPage, ids);
        this.rerender();
      });
    }
  }
  renderColorSelect(container, section) {
    var _a, _b;
    const select = container.createEl("select", { attr: { "aria-label": "\u4FEE\u6539\u989C\u8272" } });
    CARD_COLORS2.forEach((color) => select.createEl("option", { value: color.id, text: color.label }));
    select.value = String((_b = (_a = section.config) == null ? void 0 : _a.cardColor) != null ? _b : "default");
    select.addEventListener("change", async () => {
      await this.store.updateSectionConfig(section.id, { cardColor: select.value });
      this.rerender();
    });
  }
  renderWidthSelect(container, section) {
    var _a;
    const select = container.createEl("select", { attr: { "aria-label": "\u4FEE\u6539\u5BBD\u5EA6" } });
    WIDTHS2.forEach((width) => select.createEl("option", { value: width.id, text: width.label }));
    select.value = (_a = section.width) != null ? _a : "md";
    select.addEventListener("change", async () => {
      await this.store.updateSection(section.id, { width: select.value });
      this.rerender();
    });
  }
  renderIconButton(container, icon, label, action) {
    const button = container.createEl("button", { attr: { type: "button", "aria-label": label } });
    (0, import_obsidian39.setIcon)(button, icon);
    button.addEventListener("click", async () => {
      await action();
      this.rerender();
    });
  }
  rerender() {
    if (this.host) this.render(this.host);
  }
  getSectionDescription(section) {
    var _a, _b, _c;
    const definition = AVAILABLE_MODULES.find((module2) => module2.type === section.type);
    return String((_c = (_b = (_a = section.config) == null ? void 0 : _a.description) != null ? _b : definition == null ? void 0 : definition.description) != null ? _c : section.type);
  }
};
var CustomSectionModal = class extends import_obsidian39.Modal {
  constructor(app, page, onSubmit) {
    super(app);
    this.page = page;
    this.onSubmit = onSubmit;
    this.title = "";
    this.description = "";
    this.type = "custom-text";
    this.color = "default";
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: "\u521B\u5EFA\u81EA\u5B9A\u4E49\u529F\u80FD\u5206\u533A" });
    this.contentEl.createEl("p", { text: `\u5C06\u6DFB\u52A0\u5230 ${PAGE_LABELS[this.page]} \u9875\u9762\u3002` });
    new import_obsidian39.Setting(this.contentEl).setName("\u6807\u9898").addText((text) => text.onChange((value) => {
      this.title = value.trim();
    }));
    new import_obsidian39.Setting(this.contentEl).setName("\u63CF\u8FF0").addTextArea((text) => text.onChange((value) => {
      this.description = value.trim();
    }));
    new import_obsidian39.Setting(this.contentEl).setName("\u7C7B\u578B").addDropdown((dropdown) => {
      [
        ["custom-text", "\u6587\u672C"],
        ["custom-todo-list", "Todo List"],
        ["custom-link-list", "\u94FE\u63A5\u5217\u8868"],
        ["custom-memo", "Memo"]
      ].forEach(([value, label]) => dropdown.addOption(value, label));
      dropdown.setValue(this.type);
      dropdown.onChange((value) => {
        this.type = value;
      });
    });
    new import_obsidian39.Setting(this.contentEl).setName("\u989C\u8272").addDropdown((dropdown) => {
      CARD_COLORS2.forEach((color) => dropdown.addOption(color.id, color.label));
      dropdown.setValue(this.color);
      dropdown.onChange((value) => {
        this.color = value;
      });
    });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    const save = actions.createEl("button", { text: "\u521B\u5EFA", cls: "mod-cta", attr: { type: "button" } });
    save.addEventListener("click", async () => {
      if (!this.title) return;
      await this.onSubmit({
        id: `custom-${Date.now()}`,
        page: this.page,
        title: this.title,
        description: this.description,
        type: this.type,
        color: this.color
      });
      this.close();
    });
  }
};
var BannerBackgroundSettingsSection = class {
  constructor(store, onDataChanged) {
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const banner = this.store.getData().banner;
    const choices = container.createDiv({ cls: "cow-layout-picker" });
    [
      ["pink-paper", "\u63A8\u8350\u58C1\u7EB8"],
      ["cream-stars", "\u5976\u6CB9\u661F\u661F"],
      ["soft-hearts", "\u7231\u5FC3\u80CC\u666F"]
    ].forEach(([id, label]) => {
      const button = choices.createEl("button", { cls: banner.background === id ? "is-active" : "", attr: { type: "button" } });
      button.createEl("strong", { text: label });
      button.addEventListener("click", async () => {
        await this.store.updateBanner({ background: id, imageDataUrl: void 0 });
        this.onDataChanged();
      });
    });
    const file = container.createEl("input", { type: "file", attr: { accept: "image/*" } });
    file.addEventListener("change", () => {
      var _a;
      const selected = (_a = file.files) == null ? void 0 : _a[0];
      if (!selected) return;
      const reader = new FileReader();
      reader.onload = async () => {
        await this.store.updateBanner({ background: "local-image", imageDataUrl: String(reader.result) });
        this.onDataChanged();
      };
      reader.readAsDataURL(selected);
    });
    renderField(container, "\u80CC\u666F\u4F4D\u7F6E", banner.backgroundPosition, async (value) => {
      await this.store.updateBanner({ backgroundPosition: value });
      this.onDataChanged();
    });
    renderField(container, "\u900F\u660E\u5EA6", String(banner.opacity), async (value) => {
      await this.store.updateBanner({ opacity: Number(value) || 1 });
      this.onDataChanged();
    }, "number");
    const row = container.createDiv({ cls: "cow-setting-row" });
    row.createSpan({ text: "Overlay" });
    renderSwitch(row, banner.overlay, async (checked) => {
      await this.store.updateBanner({ overlay: checked });
      this.onDataChanged();
    });
  }
};
var CalendarWidgetSettingsSection = class {
  constructor(store, onDataChanged) {
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const settings = this.store.getData().calendarSettings;
    [
      ["\u663E\u793A\u7B14\u8BB0\u6807\u8BB0", "showNoteMarkers"],
      ["\u663E\u793A\u4EFB\u52A1\u6807\u8BB0", "showTaskMarkers"],
      ["\u663E\u793A\u4E8B\u4EF6\u6807\u8BB0", "showEventMarkers"]
    ].forEach(([label, key]) => {
      const row = container.createDiv({ cls: "cow-setting-row" });
      row.createSpan({ text: label });
      renderSwitch(row, Boolean(settings[key]), async (checked) => {
        await this.store.updateCalendarSettings({ [key]: checked });
        this.onDataChanged();
      });
    });
    const week = container.createDiv({ cls: "cow-setting-row" });
    week.createSpan({ text: "\u4E00\u5468\u8D77\u59CB\u65E5" });
    const select = week.createEl("select");
    select.createEl("option", { value: "monday", text: "\u5468\u4E00" });
    select.createEl("option", { value: "sunday", text: "\u5468\u65E5" });
    select.value = settings.weekStartsOn;
    select.addEventListener("change", async () => {
      await this.store.updateCalendarSettings({ weekStartsOn: select.value });
      this.onDataChanged();
    });
    renderField(container, "\u65E5\u671F\u9AD8\u4EAE\u989C\u8272", settings.highlightColor, async (value) => {
      await this.store.updateCalendarSettings({ highlightColor: value });
      this.onDataChanged();
    }, "color");
  }
};
var ApexHabitSettingsSection = class {
  constructor(store, onDataChanged) {
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const settings = this.store.getData().apexHabitSettings;
    [
      ["\u9996\u9875\u663E\u793A", "showOnOverview"],
      ["\u8FDE\u7EED\u6253\u5361", "showStreak"],
      ["\u672C\u5468\u5B8C\u6210\u8FDB\u5EA6", "showWeeklyProgress"]
    ].forEach(([label, key]) => {
      const row = container.createDiv({ cls: "cow-setting-row" });
      row.createSpan({ text: label });
      renderSwitch(row, Boolean(settings[key]), async (checked) => {
        await this.store.updateApexHabitSettings({ [key]: checked });
        this.onDataChanged();
      });
    });
    const input = container.createEl("input", { type: "text", attr: { placeholder: "\u65B0\u589E\u6253\u5361\u9879\u76EE" } });
    input.addEventListener("keydown", async (event) => {
      if (event.key !== "Enter" || !input.value.trim()) return;
      await this.store.addCustomHabit(input.value.trim());
      this.onDataChanged();
    });
    const list = container.createDiv({ cls: "cow-module-sort-list" });
    settings.customHabits.sort((a, b) => a.order - b.order).forEach((habit) => {
      const row = list.createDiv({ cls: "cow-module-row", attr: { draggable: "true", "data-id": habit.id } });
      const handle = row.createSpan({ cls: "cow-drag-handle" });
      (0, import_obsidian39.setIcon)(handle, "grip-vertical");
      const edit = row.createEl("input", { type: "text", value: habit.label });
      edit.addEventListener("change", async () => {
        await this.store.updateCustomHabit(habit.id, { label: edit.value.trim() || habit.label });
        this.onDataChanged();
      });
      renderSwitch(row, habit.enabled, async (checked) => {
        await this.store.updateCustomHabit(habit.id, { enabled: checked });
        this.onDataChanged();
      });
      const del = row.createEl("button", { text: "\u5220\u9664", attr: { type: "button" } });
      del.addEventListener("click", async () => {
        await this.store.deleteCustomHabit(habit.id);
        this.onDataChanged();
      });
      row.addEventListener("dragstart", () => {
        this.draggingHabitId = habit.id;
        row.addClass("is-dragging");
      });
      row.addEventListener("dragend", () => {
        this.draggingHabitId = void 0;
        row.removeClass("is-dragging");
      });
      row.addEventListener("dragover", (event) => {
        event.preventDefault();
        if (!this.draggingHabitId || this.draggingHabitId === habit.id) return;
        const draggingEl = list.querySelector(`[data-id="${this.draggingHabitId}"]`);
        if (draggingEl) list.insertBefore(draggingEl, row);
      });
      row.addEventListener("drop", async () => {
        const ids = Array.from(list.querySelectorAll(".cow-module-row")).map((item) => {
          var _a;
          return (_a = item.dataset.id) != null ? _a : "";
        });
        await this.store.reorderCustomHabits(ids);
        this.onDataChanged();
      });
    });
  }
};
var QuickActionSettingsSection = class {
  constructor(store, onDataChanged) {
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createDiv({ cls: "cow-module-sort-list" });
    this.store.getData().quickActions.sort((a, b) => a.order - b.order).forEach((action) => {
      const row = list.createDiv({ cls: "cow-module-row" });
      row.createSpan({ text: action.label });
      row.createSpan({ cls: "cow-module-page", text: action.type });
      renderSwitch(row, action.enabled, async (checked) => {
        await this.store.updateQuickAction(action.id, { enabled: checked });
        this.onDataChanged();
      });
    });
    const label = container.createEl("input", { type: "text", attr: { placeholder: "\u81EA\u5B9A\u4E49\u5165\u53E3\u540D\u79F0" } });
    const target = container.createEl("input", { type: "text", attr: { placeholder: "\u76EE\u6807\u8DEF\u5F84\u6216\u547D\u4EE4\u5907\u6CE8" } });
    const add = container.createEl("button", { cls: "cow-small-action", text: "\u6DFB\u52A0\u81EA\u5B9A\u4E49\u5165\u53E3", attr: { type: "button" } });
    add.addEventListener("click", async () => {
      if (!label.value.trim()) return;
      await this.store.addQuickAction(label.value.trim(), target.value.trim());
      this.onDataChanged();
    });
  }
};
var ThemeColorSettingsSection = class {
  constructor(store, onDataChanged) {
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const theme = this.store.getData().theme;
    [
      ["\u4E3B\u8272", "cutePrimary", "color"],
      ["\u8F85\u52A9\u8272", "cuteSecondary", "color"],
      ["\u6587\u5B57", "cuteText", "color"],
      ["\u80CC\u666F", "cuteBg", "color"],
      ["\u5361\u7247\u900F\u660E\u5EA6", "cardOpacity", "number"],
      ["\u5706\u89D2", "cuteRadius", "number"],
      ["\u80CC\u666F\u7EB9\u7406\u5F3A\u5EA6", "textureStrength", "number"],
      ["\u5B57\u4F53\u5927\u5C0F", "fontSize", "number"]
    ].forEach(([label, key, type]) => {
      renderField(container, label, String(theme[key]), async (value) => {
        const numericKeys = ["cardOpacity", "cuteRadius", "textureStrength", "fontSize"];
        await this.store.updateTheme({ [key]: numericKeys.includes(key) ? Number(value) : value });
        this.onDataChanged();
      }, type);
    });
  }
};
var DataSourceStatusSection = class {
  render(container) {
    const sources = [
      ["\u7B14\u8BB0", true],
      ["\u4EFB\u52A1", false],
      ["\u65E5\u5386", true],
      ["\u4E60\u60EF", true],
      ["\u9605\u8BFB", true],
      ["\u79D1\u7814", true]
    ];
    const list = container.createDiv({ cls: "cow-data-list" });
    sources.forEach(([label, enabled]) => {
      const row = list.createDiv({ cls: "cow-data-card" });
      row.createEl("strong", { text: label });
      row.createDiv({ cls: "cow-meta-line" }).createSpan({ cls: enabled ? "cow-status is-green" : "cow-status is-yellow", text: enabled ? "\u5DF2\u542F\u7528" : "\u5F85\u63A5\u5165" });
    });
  }
};
function downloadJson(filename, content) {
  const blob = new Blob([content], { type: "application/json" });
  const url = URL.createObjectURL(blob);
  const link = document.createElement("a");
  link.href = url;
  link.download = filename;
  link.click();
  URL.revokeObjectURL(url);
}
function readJsonFile(file, onRead) {
  const reader = new FileReader();
  reader.onload = () => {
    try {
      onRead(JSON.parse(String(reader.result)));
    } catch (e) {
      new import_obsidian39.Notice("\u5BFC\u5165\u5931\u8D25\uFF1AJSON \u683C\u5F0F\u4E0D\u6B63\u786E\u3002");
    }
  };
  reader.readAsText(file);
}

// src/components/finance/AccountOverviewSection.ts
var import_obsidian40 = require("obsidian");
var AccountOverviewSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const total = container.createDiv({ cls: "cow-feature-card" });
    total.createEl("strong", { text: `\xA5${this.store.getTotalAssets()}` });
    total.createSpan({ text: "\u603B\u8D44\u4EA7\u81EA\u52A8\u6C47\u603B" });
    const add = container.createEl("button", { cls: "cow-small-action", attr: { type: "button" } });
    (0, import_obsidian40.setIcon)(add.createSpan(), "plus");
    add.createSpan({ text: "\u65B0\u589E\u8D26\u6237" });
    add.addEventListener("click", () => openAccountModal(this.app, async (account) => {
      await this.store.addAccount(account);
      this.onDataChanged();
    }));
    const list = container.createDiv({ cls: "cow-data-list" });
    this.store.getAccounts().forEach((account) => {
      const row = list.createDiv({ cls: "cow-data-card" });
      const head = row.createDiv({ cls: "cow-list-item-head" });
      const title = head.createDiv({ cls: "cow-account-card-title" });
      renderAccountIcon(title, account.type);
      const body = title.createDiv();
      body.createEl("strong", { text: account.name });
      const meta = body.createDiv({ cls: "cow-meta-line" });
      meta.createSpan({ cls: "cow-status is-blue", text: account.type });
      meta.createSpan({ text: `\xA5${account.balance}` });
      const actions = head.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u8D26\u6237" } });
      (0, import_obsidian40.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => openAccountModal(this.app, async (values) => {
        await this.store.updateAccount(account.id, values);
        this.onDataChanged();
      }, account));
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u8D26\u6237" } });
      (0, import_obsidian40.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        await this.store.deleteAccount(account.id);
        this.onDataChanged();
      });
    });
  }
};

// src/components/finance/BillRemindersSection.ts
var import_obsidian41 = require("obsidian");
var BillRemindersSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createDiv({ cls: "cow-data-list" });
    this.store.getBills().forEach((bill) => {
      const row = list.createDiv({ cls: "cow-data-card" });
      const head = row.createDiv({ cls: "cow-list-item-head" });
      head.createEl("strong", { text: bill.title });
      const actions = head.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u8D26\u5355" } });
      (0, import_obsidian41.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => openBillModal(this.app, async (values) => {
        await this.store.updateBill(bill.id, values);
        this.onDataChanged();
      }, bill));
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u8D26\u5355" } });
      (0, import_obsidian41.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        await this.store.deleteBill(bill.id);
        this.onDataChanged();
      });
      const meta = row.createDiv({ cls: "cow-meta-line" });
      meta.createSpan({ cls: bill.paid ? "cow-status is-green" : "cow-status is-yellow", text: bill.paid ? "\u5DF2\u652F\u4ED8" : "\u5F85\u5904\u7406" });
      meta.createSpan({ text: `${bill.dueDate} \xB7 \xA5${bill.amount}` });
    });
  }
};

// src/components/finance/ExpenseCategoriesSection.ts
var import_obsidian42 = require("obsidian");
var ExpenseCategoriesSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    this.store.getBudgets().forEach((budget) => {
      const percent = budget.amount === 0 ? 0 : Math.round(budget.spent / budget.amount * 100);
      const row = container.createDiv({ cls: "cow-month-progress-row" });
      row.createSpan({ text: budget.category });
      const track = row.createDiv({ cls: "cow-month-progress-track" });
      track.createDiv({ cls: "cow-month-progress-fill is-pink", attr: { style: `width: ${Math.min(100, percent)}%` } });
      row.createSpan({ text: `${percent}%` });
      const actions = row.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u5206\u7C7B" } });
      (0, import_obsidian42.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => openExpenseCategoryModal(this.app, this.store, this.onDataChanged, budget));
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u5206\u7C7B" } });
      (0, import_obsidian42.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        const deleted = await this.store.deleteBudget(budget.id);
        if (!deleted) showUsedCategoryNotice(budget.category);
        this.onDataChanged();
      });
    });
  }
};

// src/components/finance/ExpenseHeatmapSection.ts
var ExpenseHeatmapSection = class {
  constructor(store) {
    this.store = store;
  }
  render(container) {
    var _a;
    const now = /* @__PURE__ */ new Date();
    const days = new Date(now.getFullYear(), now.getMonth() + 1, 0).getDate();
    const expenseByDate = /* @__PURE__ */ new Map();
    this.store.getTransactions().filter((tx) => tx.type === "expense").forEach((tx) => {
      var _a2;
      expenseByDate.set(tx.date, ((_a2 = expenseByDate.get(tx.date)) != null ? _a2 : 0) + tx.amount);
    });
    const grid = container.createDiv({ cls: "cow-reading-heatmap" });
    for (let day = 1; day <= days; day += 1) {
      const key = formatDateKey(new Date(now.getFullYear(), now.getMonth(), day));
      const amount = (_a = expenseByDate.get(key)) != null ? _a : 0;
      const level = amount === 0 ? 0 : Math.min(5, Math.ceil(amount / 300));
      grid.createSpan({ cls: `level-${level}` });
    }
  }
};

// src/components/finance/FinanceLedgerSection.ts
var import_obsidian43 = require("obsidian");
var FinanceLedgerSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const today6 = formatDateKey(/* @__PURE__ */ new Date());
    const todayTransactions = this.store.getTransactions().filter((item) => item.date === today6);
    const income = todayTransactions.filter((item) => item.type === "income").reduce((sum, item) => sum + item.amount, 0);
    const expense = todayTransactions.filter((item) => item.type === "expense").reduce((sum, item) => sum + item.amount, 0);
    const stats = container.createDiv({ cls: "cow-reading-stat-grid cow-finance-ledger-stats" });
    [
      ["\u4ECA\u65E5\u6536\u5165", `\xA5${income}`],
      ["\u4ECA\u65E5\u652F\u51FA", `\xA5${expense}`],
      ["\u4ECA\u65E5\u7B14\u6570", todayTransactions.length]
    ].forEach(([label, value]) => {
      const item = stats.createDiv();
      item.createEl("strong", { text: String(value) });
      item.createSpan({ text: String(label) });
    });
    const list = container.createDiv({ cls: "cow-data-list cow-compact-list" });
    if (todayTransactions.length === 0) {
      const empty = list.createDiv({ cls: "cow-empty-state" });
      empty.createEl("p", { text: "\u4ECA\u5929\u8FD8\u6CA1\u6709\u8BB0\u8D26" });
    }
    todayTransactions.slice().reverse().forEach((transaction) => {
      const account = transaction.accountId ? this.store.getAccounts().find((item) => item.id === transaction.accountId) : void 0;
      const row = list.createDiv({ cls: "cow-data-card" });
      row.createEl("strong", { text: `${transaction.type === "income" ? "+" : "-"}\xA5${transaction.amount} \xB7 ${transaction.category}` });
      row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${accountLabel(account)} \xB7 ${transaction.note || "\u65E0\u5907\u6CE8"}` });
    });
    const add = container.createEl("button", { cls: "finance-add-transaction-bar", attr: { type: "button" } });
    (0, import_obsidian43.setIcon)(add.createSpan(), "plus");
    add.createSpan({ text: "\u6DFB\u52A0" });
    add.addEventListener("click", () => {
      new AddTransactionModal(this.app, async (transaction) => {
        await this.store.addTransaction(transaction);
        this.onDataChanged();
      }, void 0, this.store.getBudgets(), this.store.getAccounts()).open();
    });
  }
};

// src/components/finance/FinanceTodosSection.ts
var import_obsidian44 = require("obsidian");
var FinanceTodosSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createEl("ul", { cls: "cow-focus-list cow-editable-list" });
    this.store.getFinanceTodos().forEach((item) => {
      var _a;
      const row = list.createEl("li");
      const checkbox = row.createEl("input", { type: "checkbox" });
      checkbox.checked = item.completed;
      checkbox.addEventListener("change", async () => {
        await this.store.updateFinanceTodo(item.id, { completed: checkbox.checked });
        this.onDataChanged();
      });
      row.createSpan({ text: item.title });
      row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${(_a = item.date) != null ? _a : ""}${item.note ? ` \xB7 ${item.note}` : ""}` });
      const actions = row.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u8BB0\u8D26\u5F85\u529E" } });
      (0, import_obsidian44.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => openFinanceTodoModal(this.app, this.store, this.onDataChanged, item));
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u8BB0\u8D26\u5F85\u529E" } });
      (0, import_obsidian44.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        await this.store.deleteFinanceTodo(item.id);
        this.onDataChanged();
      });
    });
    this.renderCompleteAll(container);
  }
  renderCompleteAll(container) {
    const today6 = formatDateKey(/* @__PURE__ */ new Date());
    const todayTodos = this.store.getFinanceTodos().filter((item) => {
      var _a;
      return ((_a = item.date) != null ? _a : today6) === today6;
    });
    const pending = todayTodos.filter((item) => !item.completed);
    const button = container.createEl("button", {
      cls: `cow-finance-todo-complete-all ${pending.length === 0 ? "is-complete" : ""}`,
      attr: { type: "button" }
    });
    (0, import_obsidian44.setIcon)(button.createSpan(), pending.length === 0 ? "check-circle-2" : "check");
    button.createSpan({ text: pending.length === 0 ? "\u4ECA\u65E5\u8BB0\u8D26\u5F85\u529E\u5DF2\u5B8C\u6210" : "\u90FD\u5B8C\u6210" });
    button.addEventListener("click", async () => {
      if (pending.length === 0) return;
      for (const todo of pending) {
        await this.store.updateFinanceTodo(todo.id, { completed: true });
      }
      this.onDataChanged();
    });
  }
};

// src/components/finance/IncomeExpenseTrendSection.ts
var import_obsidian45 = require("obsidian");
var IncomeExpenseTrendSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    [
      ["\u6536\u5165", this.store.getMonthlyIncome(), "is-green"],
      ["\u652F\u51FA", this.store.getMonthlyExpense(), "is-pink"],
      ["\u9884\u7B97\u5269\u4F59", this.store.getBudgetRemaining(), "is-blue"]
    ].forEach(([label, value, className]) => {
      const row = container.createDiv({ cls: "cow-month-progress-row" });
      row.createSpan({ text: String(label) });
      const track = row.createDiv({ cls: "cow-month-progress-track" });
      track.createDiv({ cls: `cow-month-progress-fill ${className}`, attr: { style: `width: ${Math.min(100, Number(value) / 150)}%` } });
      row.createSpan({ text: `\xA5${value}` });
    });
    const list = container.createDiv({ cls: "cow-data-list cow-compact-list" });
    this.store.getTransactions().slice(-4).reverse().forEach((transaction) => {
      const account = transaction.accountId ? this.store.getAccounts().find((item) => item.id === transaction.accountId) : void 0;
      const row = list.createDiv({ cls: "cow-data-card" });
      const head = row.createDiv({ cls: "cow-list-item-head" });
      const body = head.createDiv();
      body.createEl("strong", { text: `${transaction.type === "income" ? "\u6536\u5165" : "\u652F\u51FA"} \xA5${transaction.amount}` });
      body.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${transaction.date} \xB7 ${transaction.category} \xB7 ${accountLabel(account)} \xB7 ${transaction.note || "\u65E0\u5907\u6CE8"}` });
      const actions = head.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u6536\u652F" } });
      (0, import_obsidian45.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => new AddTransactionModal(this.app, async (values) => {
        await this.store.updateTransaction(transaction.id, values);
        this.onDataChanged();
      }, transaction, this.store.getBudgets(), this.store.getAccounts()).open());
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u6536\u652F" } });
      (0, import_obsidian45.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        await this.store.deleteTransaction(transaction.id);
        this.onDataChanged();
      });
    });
  }
};

// src/components/finance/InvestmentWatchSection.ts
var import_obsidian46 = require("obsidian");
var InvestmentWatchSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createDiv({ cls: "cow-data-list" });
    this.store.getInvestmentWatchItems().forEach((item) => {
      const row = list.createDiv({ cls: "cow-data-card" });
      const head = row.createDiv({ cls: "cow-list-item-head" });
      const body = head.createDiv();
      body.createEl("strong", { text: item.name });
      body.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${item.code} \xB7 ${item.type} \xB7 \xA5${item.price} \xB7 ${item.changePercent}%${item.note ? ` \xB7 ${item.note}` : ""}` });
      const actions = head.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u89C2\u5BDF" } });
      (0, import_obsidian46.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => openInvestmentModal(this.app, this.store, this.onDataChanged, item));
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u89C2\u5BDF" } });
      (0, import_obsidian46.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        await this.store.deleteInvestmentWatchItem(item.id);
        this.onDataChanged();
      });
    });
  }
};

// src/components/finance/MonthlyBudgetSection.ts
var MonthlyBudgetSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const grid = container.createDiv({ cls: "cow-reading-stat-grid cow-finance-summary-grid" });
    [
      ["\u672C\u6708\u6536\u5165", `\xA5${this.store.getMonthlyIncome()}`],
      ["\u672C\u6708\u652F\u51FA", `\xA5${this.store.getMonthlyExpense()}`],
      ["\u9884\u7B97\u5269\u4F59", `\xA5${this.store.getBudgetRemaining()}`],
      ["\u50A8\u84C4\u7387", `${this.store.getSavingRate()}%`]
    ].forEach(([label, value]) => {
      const item = grid.createDiv();
      item.createEl("strong", { text: value });
      item.createSpan({ text: label });
    });
  }
};

// src/components/finance/SavingGoalsSection.ts
var import_obsidian47 = require("obsidian");
var SavingGoalsSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createDiv({ cls: "cow-data-list" });
    this.store.getSavingGoals().forEach((goal) => {
      const percent = goal.target === 0 ? 0 : Math.round(goal.current / goal.target * 100);
      const row = list.createDiv({ cls: "cow-data-card" });
      const head = row.createDiv({ cls: "cow-list-item-head" });
      head.createEl("strong", { text: goal.title });
      const actions = head.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u50A8\u84C4\u76EE\u6807" } });
      (0, import_obsidian47.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => openSavingGoalModal(this.app, async (values) => {
        await this.store.updateSavingGoal(goal.id, values);
        this.onDataChanged();
      }, goal));
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u50A8\u84C4\u76EE\u6807" } });
      (0, import_obsidian47.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        await this.store.deleteSavingGoal(goal.id);
        this.onDataChanged();
      });
      row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `\xA5${goal.current}/\xA5${goal.target} \xB7 ${goal.deadline}` });
      const track = row.createDiv({ cls: "cow-month-progress-track" });
      track.createDiv({ cls: "cow-month-progress-fill is-green", attr: { style: `width: ${Math.min(100, percent)}%` } });
    });
  }
};

// src/components/fitness/BodyMeasurementsSection.ts
var import_obsidian48 = require("obsidian");
var BodyMeasurementsSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    var _a, _b, _c, _d;
    const measurements = this.store.getBodyMeasurements();
    const latest = measurements[measurements.length - 1];
    const grid = container.createDiv({ cls: "cow-reading-stat-grid" });
    [
      ["\u4F53\u91CD", `${(_a = latest == null ? void 0 : latest.weight) != null ? _a : 0}kg`],
      ["BMI", `${(_b = latest == null ? void 0 : latest.bmi) != null ? _b : 0}`],
      ["\u8170\u56F4", `${(_c = latest == null ? void 0 : latest.waist) != null ? _c : 0}cm`],
      ["\u81C0\u56F4", `${(_d = latest == null ? void 0 : latest.hip) != null ? _d : 0}cm`]
    ].forEach(([label, value]) => {
      const item = grid.createDiv();
      item.createEl("strong", { text: value });
      item.createSpan({ text: label });
    });
    const list = container.createDiv({ cls: "cow-data-list cow-compact-list" });
    measurements.slice(-3).reverse().forEach((item) => {
      const row = list.createDiv({ cls: "cow-data-card" });
      const head = row.createDiv({ cls: "cow-list-item-head" });
      const body = head.createDiv();
      body.createEl("strong", { text: item.date });
      body.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${item.weight}kg \xB7 BMI ${item.bmi} \xB7 \u8170\u56F4 ${item.waist}cm` });
      const actions = head.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u8BB0\u5F55" } });
      (0, import_obsidian48.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => {
        openBodyMeasurementModal2(this.app, this.store, this.onDataChanged, item);
      });
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u8BB0\u5F55" } });
      (0, import_obsidian48.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        var _a2;
        await this.store.deleteBodyMeasurement((_a2 = item.id) != null ? _a2 : item.date);
        this.onDataChanged();
      });
    });
  }
};

// src/components/fitness/CardioStrengthSection.ts
var CardioStrengthSection = class {
  constructor(store) {
    this.store = store;
  }
  render(container) {
    const cardio = this.store.getWorkouts().filter((item) => item.type === "\u6709\u6C27").length;
    const strength = this.store.getWorkouts().filter((item) => item.type === "\u529B\u91CF").length;
    const items = [
      ["\u6709\u6C27", cardio, "is-blue"],
      ["\u529B\u91CF", strength, "is-pink"],
      ["\u62C9\u4F38", this.store.getWorkouts().filter((item) => item.type === "\u62C9\u4F38").length, "is-green"]
    ];
    items.forEach(([label, value, className]) => {
      const row = container.createDiv({ cls: "cow-month-progress-row" });
      row.createSpan({ text: label });
      const track = row.createDiv({ cls: "cow-month-progress-track" });
      track.createDiv({ cls: `cow-month-progress-fill ${className}`, attr: { style: `width: ${Math.min(100, value * 30)}%` } });
      row.createSpan({ text: `${value}\u6B21` });
    });
  }
};

// src/components/fitness/FitnessGoalsSection.ts
var import_obsidian49 = require("obsidian");
var FitnessGoalsSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createDiv({ cls: "cow-data-list" });
    this.store.getFitnessGoals().filter((goal) => !(getGoalStatus(goal) === "completed" && goal.completedDate && goal.completedDate < (/* @__PURE__ */ new Date()).toISOString().slice(0, 10))).forEach((goal) => {
      var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l;
      const status = getGoalStatus(goal);
      const row = list.createDiv({ cls: `cow-data-card cow-fitness-goal-${status}` });
      row.setAttr("role", "button");
      row.setAttr("tabindex", "0");
      row.addEventListener("click", () => openFitnessGoalDetailModal(this.app, this.store, this.onDataChanged, goal));
      row.addEventListener("keydown", (event) => {
        if (event.key === "Enter" || event.key === " ") {
          event.preventDefault();
          openFitnessGoalDetailModal(this.app, this.store, this.onDataChanged, goal);
        }
      });
      const head = row.createDiv({ cls: "cow-list-item-head" });
      const body = head.createDiv();
      body.createEl("strong", { text: `${status === "completed" ? "\u2713 " : ""}${goal.title}` });
      body.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${(_a = goal.type) != null ? _a : "\u5176\u5B83"} \xB7 ${(_c = (_b = goal.currentValue) != null ? _b : goal.current) != null ? _c : 0}/${(_e = (_d = goal.targetValue) != null ? _d : goal.target) != null ? _e : 0}${(_g = (_f = goal.targetUnit) != null ? _f : goal.unit) != null ? _g : ""} \xB7 ${(_h = goal.startDate) != null ? _h : "--"} - ${goal.deadline} \xB7 ${this.statusLabel(status)}` });
      const actions = head.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u76EE\u6807" } });
      (0, import_obsidian49.setIcon)(edit, "pencil");
      edit.addEventListener("click", (event) => {
        event.stopPropagation();
        openFitnessGoalModal(this.app, this.store, this.onDataChanged, goal);
      });
      if (status !== "completed") {
        const complete = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5B8C\u6210\u76EE\u6807" } });
        (0, import_obsidian49.setIcon)(complete, "check");
        complete.addEventListener("click", async (event) => {
          event.stopPropagation();
          await this.store.completeFitnessGoal(goal.id);
          this.onDataChanged();
        });
      }
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u76EE\u6807" } });
      (0, import_obsidian49.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async (event) => {
        event.stopPropagation();
        if (!confirm(`\u5220\u9664\u201C${goal.title}\u201D\uFF1F\u5173\u8054\u8BAD\u7EC3\u8BA1\u5212\u4F1A\u89E3\u9664\u5173\u8054\uFF0C\u4F46\u4E0D\u4F1A\u88AB\u5220\u9664\u3002`)) return;
        await this.store.deleteFitnessGoal(goal.id);
        this.onDataChanged();
      });
      const current = (_j = (_i = goal.currentValue) != null ? _i : goal.current) != null ? _j : 0;
      const target = (_l = (_k = goal.targetValue) != null ? _k : goal.target) != null ? _l : 0;
      const percent = typeof goal.progress === "number" ? goal.progress : target === 0 ? 0 : Math.round(current / target * 100);
      const track = row.createDiv({ cls: "cow-month-progress-track" });
      track.createDiv({ cls: "cow-month-progress-fill is-green", attr: { style: `width: ${Math.min(100, percent)}%` } });
    });
  }
  statusLabel(status) {
    if (status === "completed") return "\u5DF2\u5B8C\u6210";
    if (status === "overdue") return "\u5DF2\u903E\u671F";
    if (status === "archived") return "\u5DF2\u5F52\u6863";
    if (status === "planned") return "\u672A\u5F00\u59CB";
    return "\u8FDB\u884C\u4E2D";
  }
};

// src/components/fitness/FitnessHeatmapSection.ts
var FitnessHeatmapSection = class {
  constructor(store) {
    this.store = store;
  }
  render(container) {
    var _a;
    const now = /* @__PURE__ */ new Date();
    const days = new Date(now.getFullYear(), now.getMonth() + 1, 0).getDate();
    const workoutsByDate = /* @__PURE__ */ new Map();
    this.store.getWorkouts().forEach((workout) => {
      var _a2;
      workoutsByDate.set(workout.date, ((_a2 = workoutsByDate.get(workout.date)) != null ? _a2 : 0) + (workout.completed ? 2 : 1));
    });
    const grid = container.createDiv({ cls: "cow-reading-heatmap" });
    for (let day = 1; day <= days; day += 1) {
      const key = formatDateKey(new Date(now.getFullYear(), now.getMonth(), day));
      grid.createSpan({ cls: `level-${Math.min(5, (_a = workoutsByDate.get(key)) != null ? _a : 0)}` });
    }
  }
};

// src/components/fitness/FitnessStatsSection.ts
var BODY_METRICS = [
  { id: "weight", label: "\u4F53\u91CD", unit: "kg" },
  { id: "bmi", label: "BMI", unit: "" },
  { id: "waist", label: "\u8170\u56F4", unit: "cm" },
  { id: "hip", label: "\u81C0\u56F4", unit: "cm" },
  { id: "chest", label: "\u80F8\u56F4", unit: "cm" }
];
var WORKOUT_METRICS = [
  { id: "count", label: "\u8BAD\u7EC3\u6B21\u6570", unit: "\u6B21" },
  { id: "duration", label: "\u603B\u8BAD\u7EC3\u65F6\u957F", unit: "min" },
  { id: "distance", label: "\u8DD1\u6B65\u8DDD\u79BB", unit: "km" },
  { id: "frequency", label: "\u8BAD\u7EC3\u9891\u7387", unit: "\u6B21/\u65E5" }
];
var EXECUTION_METRICS = [
  { id: "habitRate", label: "\u4E60\u60EF\u5B8C\u6210\u7387", unit: "%" },
  { id: "planRate", label: "\u8BAD\u7EC3\u8BA1\u5212\u5B8C\u6210\u7387", unit: "%" }
];
var FitnessStatsSection = class {
  constructor(store) {
    this.store = store;
    this.range = "week";
    this.category = "body";
    this.metric = "weight";
    this.customStart = formatDateKey(/* @__PURE__ */ new Date());
    this.customEnd = formatDateKey(/* @__PURE__ */ new Date());
  }
  render(container) {
    const root = container.createDiv({ cls: "cow-fitness-trend" });
    this.renderContent(root);
  }
  renderContent(root) {
    var _a;
    root.empty();
    this.renderControls(root);
    const range = this.getRange();
    const metric = (_a = this.getMetrics().find((item) => item.id === this.metric)) != null ? _a : this.getMetrics()[0];
    if (!metric) {
      root.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u53EF\u7528\u6307\u6807\u3002" });
      return;
    }
    const points = this.getTrendPoints(metric.id, range.start, range.end);
    const chart = root.createDiv({ cls: "cow-fitness-trend-chart" });
    if (points.length === 0) {
      chart.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u6570\u636E" });
      return;
    }
    this.renderSvgChart(chart, points, metric);
    this.renderSummary(root, points, metric);
  }
  renderControls(root) {
    const ranges = root.createDiv({ cls: "cow-fitness-trend-toolbar" });
    [
      ["week", "\u5468"],
      ["month", "\u6708"],
      ["quarter", "\u5B63\u5EA6"],
      ["custom", "\u81EA\u5B9A\u4E49"]
    ].forEach(([id, label]) => {
      const button = ranges.createEl("button", { cls: this.range === id ? "is-active" : "", text: label, attr: { type: "button" } });
      button.addEventListener("click", () => {
        this.range = id;
        this.renderContent(root);
      });
    });
    if (this.range === "custom") {
      const start = ranges.createEl("input", { attr: { type: "date", value: this.customStart, "aria-label": "\u8D8B\u52BF\u5F00\u59CB\u65E5\u671F" } });
      const end = ranges.createEl("input", { attr: { type: "date", value: this.customEnd, "aria-label": "\u8D8B\u52BF\u7ED3\u675F\u65E5\u671F" } });
      start.addEventListener("change", () => {
        this.customStart = start.value || this.customStart;
        this.renderContent(root);
      });
      end.addEventListener("change", () => {
        this.customEnd = end.value || this.customEnd;
        this.renderContent(root);
      });
    }
    const filters = root.createDiv({ cls: "cow-fitness-trend-filters" });
    const category = filters.createEl("select", { attr: { "aria-label": "\u8D8B\u52BF\u5206\u7C7B" } });
    [
      ["body", "\u8EAB\u4F53\u6307\u6807"],
      ["workout", "\u8FD0\u52A8"],
      ["execution", "\u4E60\u60EF / \u6267\u884C"]
    ].forEach(([value, label]) => category.createEl("option", { value, text: label }));
    category.value = this.category;
    category.addEventListener("change", () => {
      var _a, _b;
      this.category = category.value;
      this.metric = (_b = (_a = this.getMetrics()[0]) == null ? void 0 : _a.id) != null ? _b : "";
      this.renderContent(root);
    });
    const metric = filters.createEl("select", { attr: { "aria-label": "\u8D8B\u52BF\u6307\u6807" } });
    this.getMetrics().forEach((item) => metric.createEl("option", { value: item.id, text: item.label }));
    metric.value = this.metric;
    metric.addEventListener("change", () => {
      this.metric = metric.value;
      this.renderContent(root);
    });
  }
  renderSvgChart(container, points, metric) {
    const width = 720;
    const height = 220;
    const padding = 28;
    const values = points.map((point) => point.value);
    const min = Math.min(...values);
    const max = Math.max(...values);
    const range = max - min || 1;
    const svg = document.createElementNS("http://www.w3.org/2000/svg", "svg");
    svg.setAttribute("viewBox", `0 0 ${width} ${height}`);
    svg.setAttribute("role", "img");
    svg.setAttribute("aria-label", `${metric.label}\u8D8B\u52BF\u56FE`);
    svg.classList.add("cow-fitness-trend-svg");
    const coords = points.map((point, index) => {
      const x = padding + (points.length === 1 ? (width - padding * 2) / 2 : index / (points.length - 1) * (width - padding * 2));
      const y = height - padding - (point.value - min) / range * (height - padding * 2);
      return { ...point, x, y };
    });
    const area = document.createElementNS("http://www.w3.org/2000/svg", "polyline");
    area.setAttribute("points", coords.map((point) => `${point.x},${point.y}`).join(" "));
    area.setAttribute("fill", "none");
    area.setAttribute("stroke", "var(--cute-primary)");
    area.setAttribute("stroke-width", "4");
    area.setAttribute("stroke-linecap", "round");
    area.setAttribute("stroke-linejoin", "round");
    svg.appendChild(area);
    coords.forEach((point) => {
      const circle = document.createElementNS("http://www.w3.org/2000/svg", "circle");
      circle.setAttribute("cx", String(point.x));
      circle.setAttribute("cy", String(point.y));
      circle.setAttribute("r", "5");
      circle.setAttribute("fill", "var(--cute-secondary)");
      const title = document.createElementNS("http://www.w3.org/2000/svg", "title");
      title.textContent = `${point.date}
${metric.label}: ${this.formatValue(point.value, metric.unit)}`;
      circle.appendChild(title);
      svg.appendChild(circle);
    });
    container.appendChild(svg);
  }
  renderSummary(root, points, metric) {
    const first = points[0];
    const latest = points[points.length - 1];
    const delta = latest.value - first.value;
    const summary = root.createDiv({ cls: "cow-fitness-trend-summary" });
    summary.createSpan({ text: `\u8D77\u59CB ${this.formatValue(first.value, metric.unit)}` });
    summary.createSpan({ text: `\u5F53\u524D ${this.formatValue(latest.value, metric.unit)}` });
    summary.createSpan({ text: `\u53D8\u5316 ${delta > 0 ? "+" : ""}${this.formatValue(Math.round(delta * 10) / 10, metric.unit)}` });
  }
  getMetrics() {
    if (this.category === "workout") return WORKOUT_METRICS;
    if (this.category === "execution") return EXECUTION_METRICS;
    return BODY_METRICS.filter((metric) => this.store.getBodyMeasurements().some((item) => Number(item[metric.id]) > 0));
  }
  getTrendPoints(metricId, start, end) {
    if (this.category === "body") return this.getBodyPoints(metricId, start, end);
    if (this.category === "workout") return this.getWorkoutPoints(metricId, start, end);
    return this.getExecutionPoints(metricId, start, end);
  }
  getBodyPoints(metricId, start, end) {
    return this.store.getBodyMeasurements().filter((item) => item.date >= start && item.date <= end).map((item) => ({ date: item.date, value: Number(item[metricId]) || 0 })).filter((point) => point.value > 0);
  }
  getWorkoutPoints(metricId, start, end) {
    const days = this.enumerateDates(start, end);
    const workouts = this.store.getWorkouts().filter((item) => item.date >= start && item.date <= end);
    return days.map((date) => {
      const daily = workouts.filter((item) => item.date === date);
      if (metricId === "duration") return { date, value: daily.reduce((sum, item) => sum + workoutDuration(item), 0) };
      if (metricId === "distance") return { date, value: Math.round(daily.reduce((sum, item) => sum + (Number(item.distanceKm) || 0), 0) * 100) / 100 };
      if (metricId === "frequency") return { date, value: daily.length > 0 ? 1 : 0 };
      return { date, value: daily.length };
    }).filter((point) => point.value > 0);
  }
  getExecutionPoints(metricId, start, end) {
    const days = this.enumerateDates(start, end);
    if (metricId === "planRate") {
      const plans = this.store.getTrainingPlans();
      return days.map((date) => ({ date, value: this.calculateDailyPlanRate(date, plans) })).filter((point) => point.value > 0);
    }
    return days.map((date) => ({ date, value: this.calculateDailyHabitRate(date) })).filter((point) => point.value > 0);
  }
  calculateDailyPlanRate(date, plans) {
    const activePlans = plans.filter((plan) => {
      var _a;
      return plan.startDate <= date && plan.endDate >= date && ((_a = plan.weeklyFrequency) != null ? _a : 0) > 0;
    });
    if (activePlans.length === 0) return 0;
    const rates = activePlans.map((plan) => calculatePlanExecution(plan, this.store.getWorkouts(), date).rate);
    return Math.round(rates.reduce((sum, item) => sum + item, 0) / rates.length);
  }
  calculateDailyHabitRate(date) {
    const definitions = this.store.getFitnessHabitDefinitions();
    if (definitions.length === 0) return 0;
    const records = this.store.getFitnessHabitRecords(date);
    const completed = definitions.filter((definition) => {
      const record = records.find((item) => item.habitId === definition.id);
      return record && record.actualValue >= definition.targetValue;
    }).length;
    return Math.round(completed / definitions.length * 100);
  }
  getRange() {
    const now = /* @__PURE__ */ new Date();
    const end = formatDateKey(now);
    if (this.range === "custom") {
      return this.customStart <= this.customEnd ? { start: this.customStart, end: this.customEnd } : { start: this.customEnd, end: this.customStart };
    }
    const start = new Date(now);
    if (this.range === "week") start.setDate(now.getDate() - 6);
    if (this.range === "month") start.setDate(now.getDate() - 29);
    if (this.range === "quarter") start.setDate(now.getDate() - 89);
    return { start: formatDateKey(start), end };
  }
  enumerateDates(start, end) {
    const dates = [];
    const cursor = /* @__PURE__ */ new Date(`${start}T00:00:00`);
    const last = /* @__PURE__ */ new Date(`${end}T00:00:00`);
    while (cursor <= last) {
      dates.push(formatDateKey(cursor));
      cursor.setDate(cursor.getDate() + 1);
    }
    return dates;
  }
  formatValue(value, unit) {
    const rounded = Math.round(value * 10) / 10;
    return `${rounded}${unit}`;
  }
};

// src/components/goals/GoalBreakdownSection.ts
var import_obsidian50 = require("obsidian");
var GoalBreakdownSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createDiv({ cls: "cow-goal-tree-list" });
    const today6 = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
    this.store.getGoals().forEach((goal) => {
      const goalActions = this.store.getGoalActionsForGoal(goal.id).filter((action) => this.store.shouldShowActiveGoalAction(action, today6));
      const rootActions = goalActions.filter((action) => !action.parentId);
      const group = list.createDiv({ cls: "cow-goal-tree-group" });
      const header = group.createDiv({ cls: "cow-goal-tree-goal" });
      header.createEl("strong", { text: goal.title });
      header.createSpan({ text: `${goal.progress}% \xB7 ${goalActions.length} \u4E2A\u62C6\u89E3\u4EFB\u52A1` });
      if (rootActions.length === 0) {
        group.createDiv({ cls: "cow-empty-state", text: "\u8FD8\u6CA1\u6709\u62C6\u89E3\u4EFB\u52A1\uFF0C\u70B9\u51FB\u53F3\u4E0A\u89D2\u65B0\u589E\u62C6\u89E3\u3002" });
      }
      rootActions.forEach((action) => this.renderAction(group, action, goalActions, 0));
    });
  }
  renderAction(container, action, allActions, depth) {
    var _a, _b;
    const children = allActions.filter((item) => item.parentId === action.id);
    const row = container.createDiv({ cls: `cow-goal-action-row is-depth-${Math.min(depth, 3)} is-${action.status}` });
    row.style.setProperty("--goal-depth", String(Math.min(depth, 3)));
    const toggle = row.createEl("button", { cls: "cow-icon-button", attr: { type: "button", "aria-label": action.collapsed ? "\u5C55\u5F00\u5B50\u4EFB\u52A1" : "\u6536\u8D77\u5B50\u4EFB\u52A1" } });
    (0, import_obsidian50.setIcon)(toggle, children.length > 0 ? action.collapsed ? "chevron-right" : "chevron-down" : "circle");
    toggle.disabled = children.length === 0;
    toggle.addEventListener("click", async () => {
      await this.store.updateGoalAction(action.id, { collapsed: !action.collapsed });
      this.onDataChanged();
    });
    const check = row.createEl("input", { attr: { type: "checkbox", "aria-label": `${action.title} \u5B8C\u6210\u72B6\u6001` } });
    check.checked = action.status === "completed";
    check.addEventListener("change", async () => {
      await this.store.toggleGoalActionCompleted(action.id);
      this.onDataChanged();
    });
    const body = row.createDiv({ cls: "cow-goal-action-body" });
    body.createEl("strong", { text: action.title });
    const dateText = [action.startDate ? `\u5F00\u59CB ${action.startDate}` : "", action.deadline ? `\u622A\u6B62 ${action.deadline}` : ""].filter(Boolean).join(" \xB7 ");
    body.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${statusLabel(action.status)} \xB7 ${(_a = action.progress) != null ? _a : 0}% \xB7 ${(_b = action.durationDays) != null ? _b : 1} \u5929${dateText ? ` \xB7 ${dateText}` : ""}` });
    if (action.description || action.note) body.createEl("p", { text: action.description || action.note || "" });
    this.renderProgressEditor(body, action, children.length > 0);
    const actions = row.createDiv({ cls: "cow-list-item-actions" });
    const addChild = actions.createEl("button", { attr: { type: "button", "aria-label": "\u65B0\u589E\u5B50\u4EFB\u52A1" } });
    (0, import_obsidian50.setIcon)(addChild, "plus");
    addChild.addEventListener("click", () => openGoalActionModal(this.app, this.store, this.onDataChanged, void 0, { goalId: action.goalId, parentId: action.id }));
    const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u4EFB\u52A1" } });
    (0, import_obsidian50.setIcon)(edit, "pencil");
    edit.addEventListener("click", () => openGoalActionModal(this.app, this.store, this.onDataChanged, action));
    const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u4EFB\u52A1" } });
    (0, import_obsidian50.setIcon)(remove, "trash-2");
    remove.addEventListener("click", async () => {
      await this.store.deleteGoalAction(action.id);
      this.onDataChanged();
    });
    if (!action.collapsed) {
      children.forEach((child) => this.renderAction(container, child, allActions, depth + 1));
    }
  }
  renderProgressEditor(container, action, hasChildren) {
    var _a, _b, _c;
    const row = container.createDiv({ cls: "cow-goal-progress-editor cow-goal-action-progress" });
    const mode = row.createEl("select", { attr: { "aria-label": `${action.title} \u8FDB\u5EA6\u6A21\u5F0F` } });
    mode.createEl("option", { value: "auto", text: "\u81EA\u52A8\u8FDB\u5EA6" });
    mode.createEl("option", { value: "manual", text: "\u624B\u52A8\u8FDB\u5EA6" });
    mode.value = (_a = action.progressMode) != null ? _a : "auto";
    const range = row.createEl("input", { type: "range", value: String((_b = action.progress) != null ? _b : 0), attr: { min: "0", max: "100", step: "1", "aria-label": `${action.title} \u8FDB\u5EA6` } });
    const number = row.createEl("input", { type: "number", value: String((_c = action.progress) != null ? _c : 0), attr: { min: "0", max: "100", step: "1", "aria-label": `${action.title} \u8FDB\u5EA6\u767E\u5206\u6BD4` } });
    const updateDisabled = () => {
      const disabled = hasChildren && mode.value !== "manual";
      range.disabled = disabled;
      number.disabled = disabled;
    };
    updateDisabled();
    mode.addEventListener("change", async () => {
      await this.store.updateGoalAction(action.id, { progressMode: mode.value });
      this.onDataChanged();
    });
    range.addEventListener("input", () => number.value = range.value);
    range.addEventListener("change", () => this.saveProgress(action.id, Number(range.value)));
    number.addEventListener("change", () => this.saveProgress(action.id, Number(number.value)));
  }
  async saveProgress(actionId, value) {
    const progress = Math.max(0, Math.min(100, value));
    await this.store.updateGoalAction(actionId, {
      progress,
      progressMode: "manual",
      status: progress >= 100 ? "completed" : progress > 0 ? "in-progress" : "todo"
    });
    this.onDataChanged();
  }
};

// src/components/goals/LongTermProgressSection.ts
var LongTermProgressSection = class {
  constructor(store) {
    this.store = store;
  }
  render(container) {
    const goals = this.store.getGoals();
    const average = goals.length === 0 ? 0 : Math.round(goals.reduce((sum, goal) => sum + goal.progress, 0) / goals.length);
    const card = container.createDiv({ cls: "cow-feature-card" });
    card.createEl("strong", { text: `${average}%` });
    card.createSpan({ text: "\u957F\u671F\u76EE\u6807\u5E73\u5747\u8FDB\u5C55" });
    card.createEl("p", { text: "\u628A\u76EE\u6807\u62C6\u5C0F\uFF0C\u6BCF\u5468\u63A8\u4E00\u70B9\uFF0C\u590D\u5229\u4F1A\u6084\u6084\u7AD9\u5230\u4F60\u8FD9\u8FB9\u3002" });
    goals.forEach((goal) => {
      const row = container.createDiv({ cls: "cow-month-progress-row" });
      row.createSpan({ text: goal.category });
      const track = row.createDiv({ cls: "cow-month-progress-track" });
      track.createDiv({ cls: "cow-month-progress-fill is-green", attr: { style: `width: ${goal.progress}%` } });
      row.createSpan({ text: `${goal.progress}%` });
    });
  }
};

// src/components/goals/MilestoneTimelineSection.ts
var import_obsidian51 = require("obsidian");
var MilestoneTimelineSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const today6 = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
    const milestones = this.store.getGoalActions().filter((action) => action.isMilestone && this.store.shouldShowActiveGoalAction(action, today6)).sort((left, right) => {
      var _a, _b, _c, _d;
      return ((_b = (_a = left.milestoneDate) != null ? _a : left.deadline) != null ? _b : "").localeCompare((_d = (_c = right.milestoneDate) != null ? _c : right.deadline) != null ? _d : "");
    });
    const timeline = container.createDiv({ cls: "cow-goal-timeline" });
    if (milestones.length === 0) {
      timeline.createDiv({ cls: "cow-empty-state", text: "\u8FD8\u6CA1\u6709\u91CC\u7A0B\u7891\uFF0C\u70B9\u51FB\u53F3\u4E0A\u89D2\u65B0\u589E\u91CC\u7A0B\u7891\u3002" });
    }
    milestones.forEach((milestone) => {
      var _a, _b, _c;
      const goal = this.store.getGoals().find((item2) => item2.id === milestone.goalId);
      const date = (_b = (_a = milestone.milestoneDate) != null ? _a : milestone.deadline) != null ? _b : "";
      const item = timeline.createDiv({ cls: `cow-goal-timeline-item is-${milestone.status}` });
      item.createEl("time", { text: date || "--" });
      item.createEl("strong", { text: milestone.status === "completed" ? `\u2713 ${milestone.title}` : milestone.title });
      item.createSpan({ text: `${(_c = goal == null ? void 0 : goal.title) != null ? _c : "\u672A\u5173\u8054\u76EE\u6807"} \xB7 ${statusLabel(milestone.status)}` });
      const actions = item.createDiv({ cls: "cow-list-item-actions" });
      const complete = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5207\u6362\u5B8C\u6210\u72B6\u6001" } });
      (0, import_obsidian51.setIcon)(complete, milestone.status === "completed" ? "rotate-ccw" : "check");
      complete.addEventListener("click", async () => {
        await this.store.toggleGoalActionCompleted(milestone.id);
        this.onDataChanged();
      });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u91CC\u7A0B\u7891" } });
      (0, import_obsidian51.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => openGoalActionModal(this.app, this.store, this.onDataChanged, milestone));
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u91CC\u7A0B\u7891" } });
      (0, import_obsidian51.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        await this.store.deleteGoalAction(milestone.id);
        this.onDataChanged();
      });
    });
  }
};

// src/components/goals/MonthlyKeyResultsSection.ts
var import_obsidian52 = require("obsidian");
var MonthlyKeyResultsSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const now = /* @__PURE__ */ new Date();
    const today6 = now.toISOString().slice(0, 10);
    const currentYear = now.getFullYear();
    const currentMonth = now.getMonth() + 1;
    container.createDiv({ cls: "cow-month-goal-period", text: `${currentYear}\u5E74${currentMonth}\u6708` });
    const list = container.createEl("ul", { cls: "cow-focus-list cow-month-goal-list" });
    this.store.getKeyResults().filter((kr) => this.store.shouldShowActiveKeyResult(kr, today6) && kr.year === currentYear && kr.month === currentMonth).forEach((kr) => {
      const item = list.createEl("li", { cls: kr.completed ? "is-complete" : "" });
      const checkbox = item.createEl("input", { attr: { type: "checkbox", "aria-label": `${kr.title} \u5B8C\u6210\u72B6\u6001` } });
      checkbox.checked = kr.completed;
      checkbox.addEventListener("change", async () => {
        await this.store.toggleKeyResult(kr.id);
        this.onDataChanged();
      });
      const body = item.createDiv({ cls: "cow-month-goal-body" });
      body.createSpan({ cls: kr.completed ? "is-complete" : "", text: kr.completed ? `\u2713 ${kr.title}` : kr.title });
      const progressRow = body.createDiv({ cls: "cow-goal-progress-editor is-compact" });
      const range = progressRow.createEl("input", { type: "range", value: String(kr.progress), attr: { min: "0", max: "100", step: "1", "aria-label": `${kr.title} \u8FDB\u5EA6` } });
      const number = progressRow.createEl("input", { type: "number", value: String(kr.progress), attr: { min: "0", max: "100", step: "1", "aria-label": `${kr.title} \u8FDB\u5EA6\u767E\u5206\u6BD4` } });
      progressRow.createSpan({ cls: "cow-pill is-purple", text: "%" });
      const saveProgress = async (value) => {
        await this.store.updateKeyResult(kr.id, { progress: Math.max(0, Math.min(100, value)), completed: value >= 100 });
        this.onDataChanged();
      };
      range.addEventListener("input", () => number.value = range.value);
      range.addEventListener("change", () => saveProgress(Number(range.value)));
      number.addEventListener("change", () => saveProgress(Number(number.value)));
      const actions = item.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u6708\u5EA6\u76EE\u6807" } });
      (0, import_obsidian52.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => openMonthlyGoalModal(this.app, this.store, this.onDataChanged, kr));
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u6708\u5EA6\u76EE\u6807" } });
      (0, import_obsidian52.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        await this.store.deleteKeyResult(kr.id);
        this.onDataChanged();
      });
    });
  }
};

// src/components/goals/PriorityMatrixSection.ts
var import_obsidian53 = require("obsidian");
var PriorityMatrixSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const grid = container.createDiv({ cls: "cow-priority-grid" });
    const today6 = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
    const matrixItems = this.store.getGoalActions().filter((item) => item.importance && item.urgency && this.store.shouldShowActiveGoalAction(item, today6));
    GOAL_QUADRANTS.forEach((quadrant) => {
      const cell = grid.createDiv({ cls: `cow-priority-cell ${quadrant.id}` });
      cell.createEl("strong", { text: quadrant.label });
      cell.addEventListener("dragover", (event) => event.preventDefault());
      cell.addEventListener("drop", async (event) => {
        var _a;
        event.preventDefault();
        const id = (_a = event.dataTransfer) == null ? void 0 : _a.getData("text/plain");
        if (!id) return;
        await this.store.updateGoalAction(id, { importance: quadrant.importance, urgency: quadrant.urgency });
        this.onDataChanged();
      });
      matrixItems.filter((item) => item.importance === quadrant.importance && item.urgency === quadrant.urgency).forEach((item) => this.renderMatrixItem(cell, item));
    });
  }
  renderMatrixItem(container, item) {
    var _a;
    const row = container.createDiv({ cls: `cow-priority-item is-${item.status}`, attr: { draggable: "true" } });
    row.addEventListener("dragstart", (event) => {
      var _a2, _b;
      (_a2 = event.dataTransfer) == null ? void 0 : _a2.setData("text/plain", item.id);
      (_b = event.dataTransfer) == null ? void 0 : _b.setData("application/cute-goal-action", item.id);
    });
    const body = row.createDiv({ cls: "cow-priority-item-body" });
    body.createSpan({ cls: item.status === "completed" ? "is-complete" : "", text: item.status === "completed" ? `\u2713 ${item.title}` : item.title });
    body.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${statusLabel(item.status)} \xB7 ${(_a = item.progress) != null ? _a : 0}%${item.deadline ? ` \xB7 ${item.deadline}` : ""}` });
    const actions = row.createDiv({ cls: "cow-list-item-actions" });
    const complete = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5207\u6362\u5B8C\u6210\u72B6\u6001" } });
    (0, import_obsidian53.setIcon)(complete, item.status === "completed" ? "rotate-ccw" : "check");
    complete.addEventListener("click", async () => {
      await this.store.toggleGoalActionCompleted(item.id);
      this.onDataChanged();
    });
    const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u4EFB\u52A1" } });
    (0, import_obsidian53.setIcon)(edit, "pencil");
    edit.addEventListener("click", () => openGoalActionModal(this.app, this.store, this.onDataChanged, item));
    const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u4EFB\u52A1" } });
    (0, import_obsidian53.setIcon)(remove, "trash-2");
    remove.addEventListener("click", async () => {
      await this.store.deleteGoalAction(item.id);
      this.onDataChanged();
    });
  }
};

// src/components/goals/QuarterlyOkrSection.ts
var import_obsidian54 = require("obsidian");
var QuarterlyOkrSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createDiv({ cls: "cow-data-list" });
    const today6 = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
    const now = /* @__PURE__ */ new Date();
    const currentYear = now.getFullYear();
    const currentQuarter = Math.floor(now.getMonth() / 3) + 1;
    this.store.getObjectives().filter((objective) => this.store.shouldShowActiveObjective(objective, today6) && objective.year === currentYear && objective.quarterNumber === currentQuarter).forEach((objective) => {
      const completed = objective.progress >= 100;
      const row = list.createDiv({ cls: `cow-data-card cow-goal-list-card ${completed ? "is-complete" : ""}` });
      const head = row.createDiv({ cls: "cow-list-item-head" });
      head.createEl("strong", { text: completed ? `\u2713 ${objective.title}` : objective.title });
      const actions = head.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u5B63\u5EA6\u76EE\u6807" } });
      (0, import_obsidian54.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => openQuarterlyGoalModal(this.app, this.store, this.onDataChanged, objective));
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u5B63\u5EA6\u76EE\u6807" } });
      (0, import_obsidian54.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        await this.store.deleteObjective(objective.id);
        this.onDataChanged();
      });
      row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${objective.quarter} \xB7 ${objective.progress}%` });
      const progressRow = row.createDiv({ cls: "cow-goal-progress-editor" });
      const range = progressRow.createEl("input", { type: "range", value: String(objective.progress), attr: { min: "0", max: "100", step: "1", "aria-label": `${objective.title} \u8FDB\u5EA6` } });
      const number = progressRow.createEl("input", { type: "number", value: String(objective.progress), attr: { min: "0", max: "100", step: "1", "aria-label": `${objective.title} \u8FDB\u5EA6\u767E\u5206\u6BD4` } });
      const saveProgress = async (value) => {
        await this.store.updateObjective(objective.id, { progress: Math.max(0, Math.min(100, value)) });
        this.onDataChanged();
      };
      range.addEventListener("input", () => number.value = range.value);
      range.addEventListener("change", () => saveProgress(Number(range.value)));
      number.addEventListener("change", () => saveProgress(Number(number.value)));
    });
  }
};

// src/components/goals/ReviewChecklistSection.ts
var import_obsidian56 = require("obsidian");

// src/components/goals/ReviewModals.ts
var import_obsidian55 = require("obsidian");
function setupReviewModal(modal) {
  applyResizableModal(modal, {
    className: "cute-goal-edit-modal",
    width: "min(720px, 90vw)",
    maxWidth: "94vw",
    maxHeight: "88vh",
    minWidth: "min(420px, 90vw)",
    minHeight: "min(320px, 80vh)"
  });
}
function setupReviewStatsModal(modal) {
  applyResizableModal(modal, {
    className: "cute-goal-stats-modal",
    width: "min(860px, 92vw)",
    height: "min(680px, 86vh)",
    maxWidth: "96vw",
    maxHeight: "92vh",
    minWidth: "min(620px, 92vw)",
    minHeight: "min(420px, 86vh)"
  });
}
function createField3(container, label, type, value) {
  const row = container.createDiv({ cls: `cow-goal-form-row ${type === "date" ? "is-picker" : ""}` });
  row.createEl("label", { text: label });
  const input = row.createEl("input", { attr: { type, value } });
  if (type === "date") {
    row.addEventListener("click", () => {
      var _a;
      input.focus();
      try {
        (_a = input.showPicker) == null ? void 0 : _a.call(input);
      } catch (e) {
        input.focus();
      }
    });
  }
  return input;
}
function createTextarea4(container, label, value) {
  const row = container.createDiv({ cls: "cow-goal-form-row" });
  row.createEl("label", { text: label });
  return row.createEl("textarea", { text: value });
}
function openReviewItemModal(app, store, onDone, review) {
  new ReviewItemModal(app, store, onDone, review).open();
}
var ReviewItemModal = class extends import_obsidian55.Modal {
  constructor(app, store, onDone, review) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.review = review;
  }
  onOpen() {
    var _a, _b, _c, _d, _e, _f, _g;
    setupReviewModal(this);
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-goal-modal");
    this.contentEl.createEl("h2", { text: this.review ? "\u7F16\u8F91\u590D\u76D8" : "\u6DFB\u52A0\u590D\u76D8" });
    const date = createField3(this.contentEl, "\u65E5\u671F", "date", (_b = (_a = this.review) == null ? void 0 : _a.date) != null ? _b : formatDateKey(/* @__PURE__ */ new Date()));
    const title = createField3(this.contentEl, "\u6807\u9898", "text", (_d = (_c = this.review) == null ? void 0 : _c.title) != null ? _d : "");
    const content = createTextarea4(this.contentEl, "\u5185\u5BB9", (_f = (_e = this.review) == null ? void 0 : _e.content) != null ? _f : "");
    const statusRow = this.contentEl.createDiv({ cls: "cow-goal-form-row is-inline" });
    const done = statusRow.createEl("input", { attr: { type: "checkbox" } });
    done.checked = ((_g = this.review) == null ? void 0 : _g.status) === "done";
    statusRow.createEl("label", { text: "\u5DF2\u5B8C\u6210\u590D\u76D8" });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", async () => {
      var _a2, _b2, _c2, _d2;
      if (!title.value.trim()) {
        new import_obsidian55.Notice("\u8BF7\u8F93\u5165\u590D\u76D8\u6807\u9898\u3002");
        return;
      }
      const now = (/* @__PURE__ */ new Date()).toISOString();
      const values = {
        id: (_b2 = (_a2 = this.review) == null ? void 0 : _a2.id) != null ? _b2 : `review-${Date.now()}`,
        date: date.value || formatDateKey(/* @__PURE__ */ new Date()),
        title: title.value.trim(),
        content: content.value.trim(),
        status: done.checked ? "done" : "todo",
        createdAt: (_d2 = (_c2 = this.review) == null ? void 0 : _c2.createdAt) != null ? _d2 : now,
        updatedAt: now
      };
      if (this.review) await this.store.updateReviewItem(this.review.id, values);
      else await this.store.addReviewItem(values);
      this.onDone();
      this.close();
    });
  }
};
var ReviewStatisticsModal = class extends import_obsidian55.Modal {
  constructor(app, store) {
    super(app);
    this.store = store;
    this.year = (/* @__PURE__ */ new Date()).getFullYear();
    this.month = (/* @__PURE__ */ new Date()).getMonth() + 1;
    this.selectedDate = formatDateKey(/* @__PURE__ */ new Date());
  }
  onOpen() {
    setupReviewStatsModal(this);
    this.render();
  }
  render() {
    var _a, _b;
    const all = this.store.getReviewItems();
    const monthly = all.filter((item) => item.date.startsWith(`${this.year}-${String(this.month).padStart(2, "0")}`));
    const yearly = all.filter((item) => item.date.startsWith(String(this.year)));
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-goal-modal", "cow-goal-stats-modal");
    const content = renderGoalStatisticsLayout(this.contentEl, "\u590D\u76D8\u7EDF\u8BA1", [
      { label: "\u4E0A\u4E00\u5E74", onClick: () => {
        this.year -= 1;
        this.render();
      } },
      { label: String(this.year), active: true },
      { label: "\u4E0B\u4E00\u5E74", onClick: () => {
        this.year += 1;
        this.render();
      } }
    ], (toolbar) => {
      const months = toolbar.createDiv({ cls: "cow-goal-period-tabs is-months" });
      for (let month = 1; month <= 12; month += 1) {
        const button = months.createEl("button", { text: `${month}\u6708`, cls: month === this.month ? "is-active" : "", attr: { type: "button" } });
        button.addEventListener("click", () => {
          this.month = month;
          this.selectedDate = `${this.year}-${String(month).padStart(2, "0")}-01`;
          this.render();
        });
      }
    });
    this.renderStats(content, [
      ["\u672C\u6708\u590D\u76D8\u6B21\u6570", monthly.length],
      ["\u672C\u5E74\u590D\u76D8\u6B21\u6570", yearly.length],
      ["\u8FDE\u7EED\u590D\u76D8\u5929\u6570", this.getStreak(all)],
      ["\u6700\u8FD1\u590D\u76D8\u65E5\u671F", (_b = (_a = all.slice().sort((a, b) => b.date.localeCompare(a.date))[0]) == null ? void 0 : _a.date) != null ? _b : "--"]
    ]);
    this.renderCalendar(content, monthly);
    this.renderSelectedDate(content, all.filter((item) => item.date === this.selectedDate));
  }
  renderStats(container, items) {
    const grid = container.createDiv({ cls: "cow-stats-card-grid" });
    items.forEach(([label, value]) => {
      const card = grid.createDiv({ cls: "cow-stats-card" });
      card.createEl("strong", { text: String(value) });
      card.createSpan({ text: label });
    });
  }
  renderCalendar(container, reviews) {
    const calendar = container.createDiv({ cls: "cow-review-calendar" });
    const firstDay = new Date(this.year, this.month - 1, 1);
    const totalDays = new Date(this.year, this.month, 0).getDate();
    const offset = firstDay.getDay();
    ["\u65E5", "\u4E00", "\u4E8C", "\u4E09", "\u56DB", "\u4E94", "\u516D"].forEach((label) => calendar.createDiv({ cls: "cow-review-calendar-week", text: label }));
    for (let index = 0; index < offset; index += 1) calendar.createDiv({ cls: "cow-review-calendar-empty" });
    for (let day = 1; day <= totalDays; day += 1) {
      const date = `${this.year}-${String(this.month).padStart(2, "0")}-${String(day).padStart(2, "0")}`;
      const count = reviews.filter((item) => item.date === date).length;
      const cell = calendar.createEl("button", { cls: `cow-review-calendar-day ${count > 0 ? "has-review" : ""} ${date === this.selectedDate ? "is-selected" : ""}`, attr: { type: "button" } });
      cell.createSpan({ text: String(day) });
      if (count > 0) cell.createDiv({ cls: "cow-calendar-dot is-purple", text: count > 1 ? String(count) : "" });
      cell.addEventListener("click", () => {
        this.selectedDate = date;
        this.render();
      });
    }
  }
  renderSelectedDate(container, reviews) {
    const list = container.createDiv({ cls: "cow-goal-stats-list" });
    list.createEl("strong", { text: `${this.selectedDate} \u590D\u76D8\u8BB0\u5F55` });
    if (reviews.length === 0) {
      list.createDiv({ cls: "cow-empty-state", text: "\u8FD9\u4E00\u5929\u8FD8\u6CA1\u6709\u590D\u76D8\u3002" });
      return;
    }
    reviews.forEach((review) => {
      const row = list.createDiv({ cls: `cow-data-card ${review.status === "done" ? "is-complete" : ""}` });
      row.createEl("strong", { text: review.status === "done" ? `\u2713 ${review.title}` : review.title });
      row.createEl("p", { text: review.content });
    });
  }
  getStreak(items) {
    const dates = new Set(items.map((item) => item.date));
    let cursor = /* @__PURE__ */ new Date();
    let streak = 0;
    while (dates.has(formatDateKey(cursor))) {
      streak += 1;
      cursor = new Date(cursor.getFullYear(), cursor.getMonth(), cursor.getDate() - 1);
    }
    return streak;
  }
};

// src/components/goals/ReviewChecklistSection.ts
var ReviewChecklistSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createDiv({ cls: "cow-data-list cow-review-list" });
    this.store.getReviewItems().slice().sort((left, right) => right.date.localeCompare(left.date)).forEach((review) => {
      const row = list.createDiv({ cls: `cow-data-card cow-review-item ${review.status === "done" ? "is-complete" : ""}` });
      const head = row.createDiv({ cls: "cow-list-item-head" });
      const title = head.createDiv({ cls: "cow-review-item-title" });
      title.createEl("time", { text: review.date });
      title.createEl("strong", { text: review.status === "done" ? `\u2713 ${review.title}` : review.title });
      const actions = head.createDiv({ cls: "cow-list-item-actions" });
      const toggle = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5207\u6362\u590D\u76D8\u72B6\u6001" } });
      (0, import_obsidian56.setIcon)(toggle, review.status === "done" ? "rotate-ccw" : "check");
      toggle.addEventListener("click", async () => {
        await this.store.updateReviewItem(review.id, { status: review.status === "done" ? "todo" : "done" });
        this.onDataChanged();
      });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u590D\u76D8" } });
      (0, import_obsidian56.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => openReviewItemModal(this.app, this.store, this.onDataChanged, review));
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u590D\u76D8" } });
      (0, import_obsidian56.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        await this.store.deleteReviewItem(review.id);
        this.onDataChanged();
      });
      row.createEl("p", { text: review.content || "\u8FD8\u6CA1\u6709\u8BB0\u5F55\u590D\u76D8\u5185\u5BB9\u3002" });
    });
    const add = container.createEl("button", { cls: "cow-bottom-add-button", attr: { type: "button" } });
    (0, import_obsidian56.setIcon)(add.createSpan(), "plus");
    add.createSpan({ text: "\u6DFB\u52A0" });
    add.addEventListener("click", () => openReviewItemModal(this.app, this.store, this.onDataChanged));
  }
};

// src/components/goals/RisksBlockersSection.ts
var import_obsidian57 = require("obsidian");
var RisksBlockersSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createDiv({ cls: "cow-data-list" });
    this.store.getRisks().forEach((risk) => {
      var _a;
      const goal = risk.goalId ? this.store.getGoals().find((item) => item.id === risk.goalId) : void 0;
      const row = list.createDiv({ cls: "cow-data-card" });
      row.createEl("strong", { text: risk.title });
      const actions = row.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u98CE\u9669" } });
      (0, import_obsidian57.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => openRiskModal(this.app, this.store, this.onDataChanged, risk));
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u98CE\u9669" } });
      (0, import_obsidian57.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        await this.store.deleteRisk(risk.id);
        this.onDataChanged();
      });
      const meta = row.createDiv({ cls: "cow-meta-line" });
      meta.createSpan({ cls: `cow-status is-${risk.level === "high" ? "yellow" : "blue"}`, text: risk.level });
      meta.createSpan({ text: `${(_a = risk.status) != null ? _a : "todo"}${goal ? ` \xB7 ${goal.title}` : ""}${risk.discoveredDate ? ` \xB7 ${risk.discoveredDate}` : ""}` });
      row.createEl("p", { text: risk.solution });
    });
  }
};

// src/components/goals/YearlyGoalsSection.ts
var import_obsidian58 = require("obsidian");
var YearlyGoalsSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createDiv({ cls: "cow-data-list" });
    const today6 = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
    const currentYear = (/* @__PURE__ */ new Date()).getFullYear();
    this.store.getGoals().filter((goal) => this.store.shouldShowActiveGoal(goal, today6) && ((goal.deadline || "").startsWith(String(currentYear)) || (goal.startDate || "").startsWith(String(currentYear)))).forEach((goal) => {
      const actions = this.store.getGoalActionsForGoal(goal.id);
      const milestones = actions.filter((action) => action.isMilestone);
      const completed = goal.status === "\u5DF2\u5B8C\u6210";
      const row = list.createDiv({ cls: `cow-data-card cow-goal-list-card ${completed ? "is-complete" : ""}` });
      const title = row.createDiv({ cls: "cow-inline-title" });
      title.createEl("strong", { text: completed ? `\u2713 ${goal.title}` : goal.title });
      const edit = title.createEl("button", { attr: { type: "button", "aria-label": "\u4FEE\u6539\u76EE\u6807" } });
      (0, import_obsidian58.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => openAnnualGoalModal(this.app, this.store, this.onDataChanged, goal));
      const remove = title.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u76EE\u6807" } });
      (0, import_obsidian58.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        await this.store.deleteGoal(goal.id);
        this.onDataChanged();
      });
      row.createEl("p", { text: goal.description });
      const meta = row.createDiv({ cls: "cow-meta-line" });
      meta.createSpan({ cls: "cow-status is-green", text: goal.status });
      meta.createSpan({ text: `${goal.category} \xB7 ${goal.deadline} \xB7 \u62C6\u89E3 ${actions.length} \xB7 \u91CC\u7A0B\u7891 ${milestones.filter((item) => item.status === "completed").length}/${milestones.length}` });
      const progressRow = row.createDiv({ cls: "cow-goal-progress-editor" });
      const input = progressRow.createEl("input", {
        type: "range",
        value: String(goal.progress),
        attr: { min: "0", max: "100", "aria-label": `${goal.title} \u8FDB\u5EA6` }
      });
      const number = progressRow.createEl("input", {
        type: "number",
        value: String(goal.progress),
        attr: { min: "0", max: "100", step: "1", "aria-label": `${goal.title} \u8FDB\u5EA6\u767E\u5206\u6BD4` }
      });
      const saveProgress = async (value) => {
        await this.store.updateGoalProgress(goal.id, Math.max(0, Math.min(100, value)));
        this.onDataChanged();
      };
      input.addEventListener("input", () => number.value = input.value);
      input.addEventListener("change", () => saveProgress(Number(input.value)));
      number.addEventListener("change", () => saveProgress(Number(number.value)));
    });
  }
};

// src/components/fitness/HealthRemindersSection.ts
var import_obsidian59 = require("obsidian");
var HealthRemindersSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createEl("ul", { cls: "cow-memo-list" });
    this.store.getHealthReminders().forEach((item) => {
      var _a, _b;
      const row = list.createEl("li");
      const head = row.createDiv({ cls: "cow-list-item-head" });
      const body = head.createDiv();
      body.createSpan({ text: item.title });
      body.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${(_a = item.date) != null ? _a : "--"} ${(_b = item.time) != null ? _b : "--"} \xB7 ${this.repeatLabel(item.repeatType)} \xB7 ${item.enabled === false ? "\u5DF2\u505C\u7528" : "\u5DF2\u542F\u7528"}` });
      const actions = head.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u63D0\u9192" } });
      (0, import_obsidian59.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => openHealthReminderModal(this.app, this.store, this.onDataChanged, item));
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u63D0\u9192" } });
      (0, import_obsidian59.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        await this.store.deleteHealthReminder(item.id);
        this.onDataChanged();
      });
    });
  }
  repeatLabel(repeatType) {
    if (repeatType === "daily") return "\u6BCF\u5929";
    if (repeatType === "weekdays") return "\u5DE5\u4F5C\u65E5";
    if (repeatType === "weekly") return "\u6BCF\u5468";
    if (repeatType === "custom") return "\u81EA\u5B9A\u4E49\u661F\u671F";
    return "\u4EC5\u4E00\u6B21";
  }
};

// src/components/fitness/TodayWorkoutSection.ts
var TodayWorkoutSection = class {
  constructor(store) {
    this.store = store;
  }
  render(container) {
    var _a, _b;
    const today6 = formatDateKey(/* @__PURE__ */ new Date());
    const workout = (_a = this.store.getWorkouts().find((item) => item.date === today6)) != null ? _a : this.store.getWorkouts()[0];
    const card = container.createDiv({ cls: "cow-feature-card" });
    card.createEl("strong", { text: (_b = workout == null ? void 0 : workout.note) != null ? _b : "\u4ECA\u5929\u5B89\u6392\u8F7B\u91CF\u6D3B\u52A8" });
    card.createSpan({ text: workout ? `${workout.type} \xB7 ${workout.duration} \u5206\u949F \xB7 ${workout.calories} kcal` : "\u7ED9\u8EAB\u4F53\u4E00\u70B9\u6E29\u67D4\u7684\u542F\u52A8" });
    card.createEl("p", { text: (workout == null ? void 0 : workout.completed) ? "\u5DF2\u5B8C\u6210\uFF0C\u592A\u7A33\u4E86\uFF01" : "\u8FD8\u6CA1\u5B8C\u6210\uFF0C\u7559\u4E00\u70B9\u65F6\u95F4\u7ED9\u81EA\u5DF1\u3002" });
  }
};

// src/components/fitness/WaterSleepHabitsSection.ts
var WaterSleepHabitsSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    var _a, _b;
    const today6 = formatDateKey(/* @__PURE__ */ new Date());
    const record = this.store.getFitnessDailyRecord(today6);
    const list = container.createDiv({ cls: "cow-data-list" });
    [
      ["\u996E\u6C34", `${record.waterCups} / ${record.waterGoal} \u676F`, (_a = record.waterNote) != null ? _a : "", record.waterGoal === 0 ? 0 : Math.round(record.waterCups / record.waterGoal * 100)],
      ["\u7761\u7720", `${record.sleepHours} / ${record.sleepGoal} \u5C0F\u65F6`, (_b = record.sleepNote) != null ? _b : "", record.sleepGoal === 0 ? 0 : Math.round(record.sleepHours / record.sleepGoal * 100)]
    ].forEach(([label, status, note, percent]) => {
      const row = list.createDiv({ cls: "cow-data-card" });
      row.createEl("strong", { text: String(label) });
      const meta = row.createDiv({ cls: "cow-fitness-metric-line" });
      meta.createSpan({ cls: "cow-fitness-metric-value", text: String(status) });
      if (note) {
        meta.createSpan({ cls: "cow-fitness-note", text: String(note) });
      }
      const track = row.createDiv({ cls: "cow-month-progress-track" });
      track.createDiv({ cls: "cow-month-progress-fill is-blue", attr: { style: `width: ${Math.min(100, Number(percent))}%` } });
    });
    this.store.getFitnessHabitDefinitions().slice(0, 3).forEach((definition) => {
      var _a2;
      const daily = this.store.getFitnessHabitRecords(today6).find((item) => item.habitId === definition.id);
      const row = list.createDiv({ cls: "cow-data-card" });
      row.createEl("strong", { text: definition.name });
      const meta = row.createDiv({ cls: "cow-fitness-metric-line" });
      meta.createSpan({ cls: "cow-fitness-metric-value", text: `${(_a2 = daily == null ? void 0 : daily.actualValue) != null ? _a2 : 0} / ${definition.targetValue} ${definition.unit}` });
      if (daily == null ? void 0 : daily.note) {
        meta.createSpan({ cls: "cow-fitness-note", text: daily.note });
      }
    });
  }
};

// src/components/fitness/WorkoutLogSection.ts
var import_obsidian60 = require("obsidian");
var WorkoutLogSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
    this.dateFilter = "";
    this.typeFilter = "";
    this.planFilter = "";
    this.goalFilter = "";
  }
  render(container) {
    const root = container.createDiv({ cls: "cow-workout-log" });
    this.renderContent(root);
  }
  renderContent(root) {
    root.empty();
    this.renderFilters(root);
    const list = root.createDiv({ cls: "cow-data-list" });
    const workouts = this.getFilteredWorkouts();
    if (workouts.length === 0) {
      list.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u7B26\u5408\u6761\u4EF6\u7684\u8FD0\u52A8\u65E5\u5FD7\u3002" });
      return;
    }
    workouts.forEach((workout) => this.renderWorkout(list, workout));
  }
  renderFilters(root) {
    const filters = root.createDiv({ cls: "cow-workout-filter-row" });
    const date = filters.createEl("input", { attr: { type: "date", value: this.dateFilter, "aria-label": "\u6309\u65E5\u671F\u7B5B\u9009" } });
    date.addEventListener("change", () => {
      this.dateFilter = date.value;
      this.renderContent(root);
    });
    const type = filters.createEl("select", { attr: { "aria-label": "\u6309\u8BAD\u7EC3\u7C7B\u578B\u7B5B\u9009" } });
    [["", "\u5168\u90E8\u7C7B\u578B"], ["\u8DD1\u6B65", "\u8DD1\u6B65"], ["\u529B\u91CF", "\u529B\u91CF"], ["\u9A91\u884C", "\u9A91\u884C"], ["\u6E38\u6CF3", "\u6E38\u6CF3"], ["\u745C\u4F3D", "\u745C\u4F3D"], ["\u5176\u5B83", "\u5176\u5B83"]].forEach(([value, label]) => {
      type.createEl("option", { value, text: label });
    });
    type.value = this.typeFilter;
    type.addEventListener("change", () => {
      this.typeFilter = type.value;
      this.renderContent(root);
    });
    const plan = filters.createEl("select", { attr: { "aria-label": "\u6309\u8BAD\u7EC3\u8BA1\u5212\u7B5B\u9009" } });
    plan.createEl("option", { value: "", text: "\u5168\u90E8\u8BA1\u5212" });
    this.store.getTrainingPlans().forEach((item) => plan.createEl("option", { value: item.id, text: item.title }));
    plan.value = this.planFilter;
    plan.addEventListener("change", () => {
      this.planFilter = plan.value;
      this.renderContent(root);
    });
    const goal = filters.createEl("select", { attr: { "aria-label": "\u6309\u5065\u8EAB\u76EE\u6807\u7B5B\u9009" } });
    goal.createEl("option", { value: "", text: "\u5168\u90E8\u76EE\u6807" });
    this.store.getFitnessGoals().forEach((item) => goal.createEl("option", { value: item.id, text: item.title }));
    goal.value = this.goalFilter;
    goal.addEventListener("change", () => {
      this.goalFilter = goal.value;
      this.renderContent(root);
    });
  }
  renderWorkout(container, workout) {
    var _a, _b;
    const plan = workout.trainingPlanId ? this.store.getTrainingPlans().find((item) => item.id === workout.trainingPlanId) : void 0;
    const goal = workout.fitnessGoalId ? this.store.getFitnessGoals().find((item) => item.id === workout.fitnessGoalId) : void 0;
    const row = container.createDiv({ cls: "cow-data-card cow-workout-card" });
    row.addEventListener("click", () => openWorkoutRecordDetailModal(this.app, this.store, this.onDataChanged, workout));
    const head = row.createDiv({ cls: "cow-list-item-head" });
    head.createEl("strong", { text: workoutTitle(workout) });
    const actions = head.createDiv({ cls: "cow-list-item-actions" });
    const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u8FD0\u52A8\u65E5\u5FD7" } });
    (0, import_obsidian60.setIcon)(edit, "pencil");
    edit.addEventListener("click", (event) => {
      event.stopPropagation();
      openWorkoutRecordModal(this.app, this.store, this.onDataChanged, workout);
    });
    const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u8FD0\u52A8\u65E5\u5FD7" } });
    (0, import_obsidian60.setIcon)(remove, "trash-2");
    remove.addEventListener("click", async (event) => {
      event.stopPropagation();
      await this.store.deleteWorkout(workout.id);
      this.onDataChanged();
    });
    row.createDiv({ cls: "cow-meta-line" }).createSpan({
      text: `${workout.date} \xB7 ${workoutTypeLabel(workout)} \xB7 ${workoutDuration(workout)}min${workout.distanceKm ? ` \xB7 ${workout.distanceKm}km` : ""}`
    });
    row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${(_a = plan == null ? void 0 : plan.title) != null ? _a : "\u672A\u5173\u8054\u8BA1\u5212"} \xB7 ${(_b = goal == null ? void 0 : goal.title) != null ? _b : "\u672A\u5173\u8054\u76EE\u6807"}` });
    if (workout.resultSummary) row.createEl("p", { text: workout.resultSummary });
  }
  getFilteredWorkouts() {
    return this.store.getWorkouts().filter((workout) => !this.dateFilter || workout.date === this.dateFilter).filter((workout) => !this.typeFilter || workoutTypeLabel(workout) === this.typeFilter).filter((workout) => !this.planFilter || workout.trainingPlanId === this.planFilter).filter((workout) => !this.goalFilter || workout.fitnessGoalId === this.goalFilter).sort((left, right) => right.date.localeCompare(left.date));
  }
};

// src/components/fitness/WorkoutPlanSection.ts
var import_obsidian61 = require("obsidian");
var WorkoutPlanSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const plans = this.store.getTrainingPlans().sort((a, b) => a.startDate.localeCompare(b.startDate));
    const list = container.createDiv({ cls: "cow-data-list cow-training-plan-list" });
    if (plans.length === 0) {
      list.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u8BAD\u7EC3\u8BA1\u5212\u3002\u8BAD\u7EC3\u8BA1\u5212\u7528\u4E8E\u8BB0\u5F55\u51C6\u5907\u600E\u4E48\u7EC3\uFF0C\u4E0D\u662F\u5B9E\u9645\u8FD0\u52A8\u65E5\u5FD7\u3002" });
      return;
    }
    plans.forEach((plan) => this.renderPlan(list, plan));
  }
  renderPlan(container, plan) {
    var _a, _b, _c, _d;
    const goal = plan.fitnessGoalId ? this.store.getFitnessGoals().find((item) => item.id === plan.fitnessGoalId) : void 0;
    const row = container.createDiv({ cls: `cow-data-card cow-training-plan-card is-${(_a = plan.status) != null ? _a : "active"}` });
    row.setAttr("role", "button");
    row.setAttr("tabindex", "0");
    row.addEventListener("click", () => openTrainingPlanDetailModal(this.app, this.store, this.onDataChanged, plan));
    row.addEventListener("keydown", (event) => {
      if (event.key === "Enter" || event.key === " ") {
        event.preventDefault();
        openTrainingPlanDetailModal(this.app, this.store, this.onDataChanged, plan);
      }
    });
    const head = row.createDiv({ cls: "cow-list-item-head" });
    const title = head.createDiv({ cls: "cow-training-plan-title" });
    title.createEl("strong", { text: plan.title });
    title.createDiv({ cls: "cow-meta-line", text: `${(_b = goal == null ? void 0 : goal.title) != null ? _b : "\u672A\u5173\u8054\u76EE\u6807"} \xB7 ${formatDateRange3(plan.startDate, plan.endDate)} \xB7 \u6BCF\u5468 ${(_c = plan.weeklyFrequency) != null ? _c : 0} \u6B21 \xB7 ${this.statusLabel((_d = plan.status) != null ? _d : "active")}` });
    const actions = head.createDiv({ cls: "cow-list-item-actions" });
    this.iconButton(actions, "pencil", "\u7F16\u8F91\u8BAD\u7EC3\u8BA1\u5212", () => openTrainingPlanModal(this.app, this.store, this.onDataChanged, plan));
    if (plan.status !== "completed") {
      this.iconButton(actions, "check", "\u5B8C\u6210\u8BAD\u7EC3\u8BA1\u5212", async () => {
        await this.store.completeTrainingPlan(plan.id);
        this.onDataChanged();
      });
    }
    this.iconButton(actions, "trash-2", "\u5220\u9664\u8BAD\u7EC3\u8BA1\u5212", async () => {
      if (!confirm(`\u5220\u9664\u201C${plan.title}\u201D\u8BAD\u7EC3\u8BA1\u5212\uFF1F\u4E0D\u4F1A\u5220\u9664\u5173\u8054\u76EE\u6807\u6216 Markdown\u3002`)) return;
      await this.store.deleteTrainingPlan(plan.id);
      this.onDataChanged();
    });
    if (plan.description) row.createEl("p", { text: plan.description });
  }
  iconButton(container, icon, label, onClick) {
    const button = container.createEl("button", { attr: { type: "button", "aria-label": label } });
    (0, import_obsidian61.setIcon)(button, icon);
    button.addEventListener("click", (event) => {
      event.stopPropagation();
      void onClick();
    });
  }
  statusLabel(status) {
    if (status === "planned") return "\u672A\u5F00\u59CB";
    if (status === "completed") return "\u5DF2\u5B8C\u6210";
    if (status === "overdue") return "\u5DF2\u903E\u671F";
    return "\u8FDB\u884C\u4E2D";
  }
};
function formatDateRange3(startDate, endDate) {
  return `${startDate} - ${endDate}`;
}

// src/components/reading/AiReadingReviewSection.ts
var AiReadingReviewSection = class {
  render(container) {
    const card = container.createDiv({ cls: "cow-ai-review" });
    card.createEl("strong", { text: "AI \u9605\u8BFB\u590D\u76D8\u51C6\u5907\u4E2D" });
    card.createEl("p", { text: "\u540E\u7EED\u4F1A\u6839\u636E\u9605\u8BFB\u7B14\u8BB0\u3001\u6458\u5F55\u548C\u8FDB\u5EA6\u751F\u6210\u9636\u6BB5\u603B\u7ED3\u3001\u4E3B\u9898\u8109\u7EDC\u548C\u4E0B\u4E00\u6B65\u9605\u8BFB\u5EFA\u8BAE\u3002" });
  }
};

// src/components/reading/AllBooksModal.ts
var import_obsidian62 = require("obsidian");
var AllBooksModal = class extends import_obsidian62.Modal {
  constructor(app, store, onDataChanged) {
    super(app);
    this.store = store;
    this.onDataChanged = onDataChanged;
    this.filter = "all";
    this.query = "";
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-book-modal",
      width: "min(1050px, 92vw)",
      height: "min(720px, 86vh)",
      maxWidth: "96vw",
      maxHeight: "92vh",
      minWidth: "min(560px, 92vw)",
      minHeight: "min(430px, 82vh)"
    });
    this.render();
  }
  render() {
    const books = this.filterBooks();
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-book-modal", "cow-all-books-modal");
    this.contentEl.createEl("h2", { text: "\u6240\u6709\u4E66\u7C4D" });
    this.renderStats();
    this.renderFilters();
    const list = this.contentEl.createDiv({ cls: "cow-all-books-list" });
    if (books.length === 0) {
      list.createDiv({ cls: "cow-empty-state", text: "\u6CA1\u6709\u627E\u5230\u5339\u914D\u7684\u4E66\u7C4D\u3002" });
      return;
    }
    books.forEach((book) => this.renderBookRow(list, book));
  }
  renderStats() {
    const books = this.store.getBooks();
    const stats = [
      ["\u603B\u4E66\u7C4D\u6570", books.length],
      ["\u60F3\u8BFB", books.filter((book) => book.readingStatus === "want-to-read").length],
      ["\u5728\u8BFB", books.filter((book) => book.readingStatus === "reading").length],
      ["\u5DF2\u8BFB", books.filter((book) => book.readingStatus === "finished").length],
      ["\u4E66\u67B6\u4E0A", books.filter((book) => book.shelfStatus !== "off-shelf").length],
      ["\u5DF2\u4E0B\u67B6", books.filter((book) => book.shelfStatus === "off-shelf").length]
    ];
    const grid = this.contentEl.createDiv({ cls: "cow-book-stats-grid" });
    stats.forEach(([label, value]) => {
      const card = grid.createDiv({ cls: "cow-stats-card" });
      card.createEl("strong", { text: String(value) });
      card.createSpan({ text: String(label) });
    });
  }
  renderFilters() {
    const controls = this.contentEl.createDiv({ cls: "cow-all-books-controls" });
    const search = controls.createEl("input", { attr: { type: "search", placeholder: "\u641C\u7D22\u6807\u9898\u3001\u4F5C\u8005\u3001ISBN" } });
    search.value = this.query;
    search.addEventListener("input", () => {
      this.query = search.value;
      this.render();
    });
    const filters = controls.createDiv({ cls: "cow-book-tabs" });
    [
      { id: "all", label: "\u5168\u90E8" },
      { id: "want-to-read", label: "\u60F3\u8BFB" },
      { id: "reading", label: "\u5728\u8BFB" },
      { id: "finished", label: "\u5DF2\u8BFB" },
      { id: "on-shelf", label: "\u4E66\u67B6\u4E0A" },
      { id: "off-shelf", label: "\u5DF2\u4E0B\u67B6" }
    ].forEach((option) => {
      const button = filters.createEl("button", { text: option.label, cls: this.filter === option.id ? "is-active" : "", attr: { type: "button" } });
      button.addEventListener("click", () => {
        this.filter = option.id;
        this.render();
      });
    });
  }
  renderBookRow(container, book) {
    var _a;
    const row = container.createDiv({ cls: "cow-all-book-row" });
    const cover = row.createDiv({ cls: "cow-book-result-cover" });
    const src = this.getCoverSrc(book);
    if (src) this.renderCoverImage(cover, src, book.title);
    else cover.createSpan({ text: book.title.slice(0, 2) });
    const body = row.createDiv({ cls: "cow-all-book-body" });
    body.createEl("strong", { text: book.title });
    body.createSpan({ text: `${book.author} \xB7 ${this.statusLabel(book)} \xB7 ${book.shelfStatus === "off-shelf" ? "\u5DF2\u4E0B\u67B6" : "\u4E66\u67B6\u4E0A"}` });
    if (((_a = book.tags) != null ? _a : []).length > 0) body.createSpan({ text: `\u6807\u7B7E\uFF1A${book.tags.join(" / ")}` });
    const noteCount = this.store.getReadingNotesForBook(book.id).length;
    body.createSpan({ text: [book.bookFilePath, noteCount > 0 ? `${noteCount} \u6761\u9605\u8BFB\u7B14\u8BB0` : ""].filter(Boolean).join(" \xB7 ") || "\u6682\u65E0\u6587\u4EF6\u8DEF\u5F84" });
    const actions = row.createDiv({ cls: "cow-list-item-actions" });
    this.iconButton(actions, "book-open", "\u6253\u5F00\u4E66\u7C4D", () => void this.openBook(book));
    this.iconButton(actions, "notebook-tabs", "\u6253\u5F00\u7B14\u8BB0", () => void this.openNote(book));
    this.iconButton(actions, "pencil", "\u7F16\u8F91", () => new AddBookModal(this.app, this.store, () => {
      this.onDataChanged();
      this.render();
    }, book).open());
    this.iconButton(actions, book.shelfStatus === "off-shelf" ? "archive-restore" : "archive", book.shelfStatus === "off-shelf" ? "\u4E0A\u67B6" : "\u4E0B\u67B6", async () => {
      await this.store.updateBookShelfStatus(book.id, book.shelfStatus === "off-shelf" ? "on-shelf" : "off-shelf");
      this.onDataChanged();
      this.render();
    });
    this.iconButton(actions, "trash-2", "\u5220\u9664\u4E66\u7C4D\u8BB0\u5F55", async () => {
      if (!confirm(`\u5220\u9664\u201C${book.title}\u201D\u4E66\u7C4D\u8BB0\u5F55\uFF1F\u4E0D\u4F1A\u5220\u9664\u7535\u5B50\u4E66\u3001\u7B14\u8BB0\u6216\u5C01\u9762\u6587\u4EF6\u3002`)) return;
      await this.store.deleteBook(book.id);
      this.onDataChanged();
      this.render();
    });
  }
  filterBooks() {
    const query = this.query.trim().toLowerCase();
    return this.store.getBooks().filter((book) => {
      const statusMatch = this.filter === "all" || book.readingStatus === this.filter || book.shelfStatus === this.filter;
      const queryMatch = !query || [book.title, book.author, book.isbn10, book.isbn13].some((value) => (value != null ? value : "").toLowerCase().includes(query));
      return statusMatch && queryMatch;
    });
  }
  iconButton(container, icon, label, onClick) {
    const button = container.createEl("button", { attr: { type: "button", "aria-label": label } });
    (0, import_obsidian62.setIcon)(button, icon);
    button.addEventListener("click", onClick);
  }
  async openBook(book) {
    var _a, _b;
    if (!book.bookFilePath) {
      new import_obsidian62.Notice("\u8FD9\u672C\u4E66\u8FD8\u6CA1\u6709\u7ED1\u5B9A\u7535\u5B50\u4E66\u6587\u4EF6\u3002");
      return;
    }
    const file = this.app.vault.getFileByPath(book.bookFilePath);
    if (!file) {
      new import_obsidian62.Notice(`\u6CA1\u6709\u627E\u5230\u4E66\u7C4D\u6587\u4EF6\uFF1A${book.bookFilePath}`);
      return;
    }
    try {
      await this.app.workspace.getLeaf(false).openFile(file);
    } catch (e) {
      const ext = (_b = (_a = book.bookFilePath.split(".").pop()) == null ? void 0 : _a.toUpperCase()) != null ? _b : "\u8BE5\u683C\u5F0F";
      new import_obsidian62.Notice(`\u5F53\u524D\u672A\u68C0\u6D4B\u5230 ${ext} \u9605\u8BFB\u5668\u63D2\u4EF6\u3002\u6587\u4EF6\u5DF2\u4FDD\u7559\u5728 Vault \u4E2D\u3002`);
    }
  }
  async openNote(book) {
    await openReadingNoteForBook(this.app, this.store, book);
  }
  getCoverSrc(book) {
    var _a;
    if (book.coverPath) return this.app.vault.adapter.getResourcePath(book.coverPath);
    return (_a = book.cover) != null ? _a : book.coverUrl;
  }
  renderCoverImage(container, src, title) {
    const img = container.createEl("img", { attr: { src, alt: title } });
    img.addEventListener("error", () => {
      container.empty();
      container.createSpan({ text: title.slice(0, 2) });
    });
  }
  statusLabel(book) {
    if (book.readingStatus === "reading") return "\u5728\u8BFB";
    if (book.readingStatus === "finished") return "\u5DF2\u8BFB";
    return "\u60F3\u8BFB";
  }
};

// src/components/reading/BookListSection.ts
var import_obsidian63 = require("obsidian");
var BookListSection = class {
  constructor(app, store, readingStatus, onDataChanged) {
    this.app = app;
    this.store = store;
    this.readingStatus = readingStatus;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createDiv({ cls: "cow-data-list" });
    this.store.getBooks().filter((book) => book.readingStatus === this.readingStatus).forEach((book) => {
      const row = list.createDiv({ cls: "cow-data-card" });
      const head = row.createDiv({ cls: "cow-list-item-head" });
      head.createEl("strong", { text: book.title });
      const actions = head.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u4E66\u7C4D" } });
      (0, import_obsidian63.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => new AddBookModal(this.app, this.store, () => {
        this.onDataChanged();
      }, book).open());
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u4E66\u7C4D" } });
      (0, import_obsidian63.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        if (!confirm(`\u5220\u9664\u300A${book.title}\u300B\u4E66\u7C4D\u8BB0\u5F55\uFF1F\u4E0D\u4F1A\u5220\u9664 PDF\u3001EPUB\u3001\u5C01\u9762\u6216 Markdown \u7B14\u8BB0\u6587\u4EF6\u3002`)) return;
        await this.store.deleteBook(book.id);
        this.onDataChanged();
      });
      row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: `${book.author} \xB7 ${book.tags.join(" / ")}` });
    });
  }
};

// src/components/reading/BookCard.ts
var import_obsidian64 = require("obsidian");
var BookCard = class {
  constructor(app, store, book, onDataChanged, variant = "current") {
    this.app = app;
    this.store = store;
    this.book = book;
    this.onDataChanged = onDataChanged;
    this.variant = variant;
  }
  render(container) {
    var _a;
    const card = container.createDiv({ cls: `cow-book-card cow-book-card-${this.variant}` });
    const cover = card.createDiv({ cls: "cow-book-cover" });
    const coverSrc = this.getCoverSrc();
    if (coverSrc) {
      this.renderCoverImage(cover, coverSrc, this.book.title);
    } else {
      cover.createSpan({ text: this.book.title.slice(0, 2) });
    }
    if (this.variant === "shelf") {
      const title = card.createEl("strong", {
        cls: "cow-book-shelf-title",
        text: this.book.title,
        attr: { title: this.book.title }
      });
      title.setAttr("aria-label", this.book.title);
      return;
    }
    const body = card.createDiv({ cls: "cow-book-body" });
    body.createSpan({ cls: `cow-reading-status-pill is-${(_a = this.book.readingStatus) != null ? _a : "want-to-read"}`, text: this.statusLabel() });
    body.createEl("strong", { cls: "cow-book-current-title", text: this.book.title, attr: { title: this.book.title } });
    const controls = body.createDiv({ cls: "cow-book-controls" });
    const openBook = controls.createEl("button", { attr: { type: "button", "aria-label": "\u9605\u8BFB\u4E66\u7C4D" } });
    (0, import_obsidian64.setIcon)(openBook, "book-open");
    openBook.addEventListener("click", () => void this.openBook());
    const openNote = controls.createEl("button", { attr: { type: "button", "aria-label": "\u6253\u5F00\u9605\u8BFB\u7B14\u8BB0" } });
    (0, import_obsidian64.setIcon)(openNote, "notebook-tabs");
    openNote.addEventListener("click", () => void this.openNote());
    const done = controls.createEl("button", { attr: { type: "button", "aria-label": "\u5B8C\u6210\u9605\u8BFB" } });
    (0, import_obsidian64.setIcon)(done, "check");
    done.addEventListener("click", async () => {
      const shouldCompletePlans = this.shouldCompleteRelatedPlans();
      await this.store.completeBook(this.book.id);
      await this.completeRelatedPlansIfConfirmed(shouldCompletePlans);
      this.onDataChanged();
    });
    const edit = controls.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u4E66\u7C4D" } });
    (0, import_obsidian64.setIcon)(edit, "pencil");
    edit.addEventListener("click", () => {
      new AddBookModal(this.app, this.store, () => {
        this.onDataChanged();
      }, this.book).open();
    });
    const remove = controls.createEl("button", { attr: { type: "button", "aria-label": "\u4E0B\u67B6" } });
    (0, import_obsidian64.setIcon)(remove, "archive");
    remove.addEventListener("click", async () => {
      await this.store.updateBookShelfStatus(this.book.id, "off-shelf");
      this.onDataChanged();
    });
  }
  async openBook() {
    var _a, _b;
    if (!this.book.bookFilePath) {
      new import_obsidian64.Notice("\u8FD9\u672C\u4E66\u8FD8\u6CA1\u6709\u7ED1\u5B9A\u7535\u5B50\u4E66\u6587\u4EF6\u3002");
      return;
    }
    const file = this.app.vault.getFileByPath(this.book.bookFilePath);
    if (!file) {
      new import_obsidian64.Notice(`\u6CA1\u6709\u627E\u5230\u4E66\u7C4D\u6587\u4EF6\uFF1A${this.book.bookFilePath}`);
      return;
    }
    try {
      await this.app.workspace.getLeaf(false).openFile(file);
    } catch (e) {
      const ext = (_b = (_a = this.book.bookFilePath.split(".").pop()) == null ? void 0 : _a.toUpperCase()) != null ? _b : "\u8BE5\u683C\u5F0F";
      new import_obsidian64.Notice(`\u5F53\u524D\u672A\u68C0\u6D4B\u5230 ${ext} \u9605\u8BFB\u5668\u63D2\u4EF6\u3002\u6587\u4EF6\u5DF2\u4FDD\u7559\u5728 Vault \u4E2D\u3002`);
    }
  }
  async openNote() {
    await openReadingNoteForBook(this.app, this.store, this.book);
  }
  getCoverSrc() {
    var _a;
    if (this.book.coverPath) return this.app.vault.adapter.getResourcePath(this.book.coverPath);
    return (_a = this.book.cover) != null ? _a : this.book.coverUrl;
  }
  renderCoverImage(container, src, title) {
    const img = container.createEl("img", { attr: { src, alt: title } });
    img.addEventListener("error", () => {
      container.empty();
      container.createSpan({ text: title.slice(0, 2) });
    });
  }
  statusLabel() {
    if (this.book.readingStatus === "reading") return "\u5728\u8BFB";
    if (this.book.readingStatus === "finished") return "\u5DF2\u8BFB";
    return "\u60F3\u8BFB";
  }
  shouldCompleteRelatedPlans() {
    const relatedPlans = this.store.getReadingPlans().filter((plan) => plan.bookId === this.book.id && plan.status !== "completed");
    if (relatedPlans.length === 0) return false;
    return confirm(`\u300A${this.book.title}\u300B\u5DF2\u6709 ${relatedPlans.length} \u6761\u672A\u5B8C\u6210\u9605\u8BFB\u8BA1\u5212\uFF0C\u662F\u5426\u540C\u65F6\u6807\u8BB0\u5B8C\u6210\uFF1F`);
  }
  async completeRelatedPlansIfConfirmed(confirmed) {
    if (!confirmed) return;
    const relatedPlans = this.store.getReadingPlans().filter((plan) => plan.bookId === this.book.id && !plan.completedDate);
    await Promise.all(relatedPlans.map((plan) => this.store.completeReadingPlan(plan.id)));
    new import_obsidian64.Notice("\u5173\u8054\u9605\u8BFB\u8BA1\u5212\u5DF2\u6807\u8BB0\u5B8C\u6210\u3002");
  }
};

// src/components/reading/BookshelfSection.ts
var BookshelfSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const grid = container.createDiv({ cls: "cow-book-grid" });
    this.store.getBooks().filter((book) => book.shelfStatus !== "off-shelf").forEach((book) => new BookCard(this.app, this.store, book, this.onDataChanged, "shelf").render(grid));
  }
};

// src/components/reading/CurrentReadingSection.ts
var CurrentReadingSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const current = this.store.getBooks().filter((book) => book.readingStatus === "reading" && book.shelfStatus !== "off-shelf").slice(0, 2);
    const grid = container.createDiv({ cls: "cow-book-grid" });
    current.forEach((book) => new BookCard(this.app, this.store, book, this.onDataChanged, "current").render(grid));
  }
};

// src/components/reading/ReadingHeatmapSection.ts
var ReadingHeatmapSection = class {
  constructor(store) {
    this.store = store;
  }
  render(container) {
    const now = /* @__PURE__ */ new Date();
    const days = new Date(now.getFullYear(), now.getMonth() + 1, 0).getDate();
    const grid = container.createDiv({ cls: "cow-reading-heatmap" });
    for (let day = 1; day <= days; day += 1) {
      const date = new Date(now.getFullYear(), now.getMonth(), day);
      const key = formatDateKey(date);
      const count = this.store.getActiveCheckInDefinitions("reading").filter((habit) => this.store.isCheckInCompleted(habit.id, key)).length;
      grid.createSpan({ cls: `level-${count}`, attr: { "aria-label": key } });
    }
  }
};

// src/components/reading/ReadingPlanSection.ts
var import_obsidian65 = require("obsidian");
var ReadingPlanSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const plans = this.store.getReadingPlans().filter((plan) => this.store.shouldShowActiveReadingPlan(plan)).sort((a, b) => a.startDate.localeCompare(b.startDate));
    if (plans.length === 0) {
      const empty = container.createDiv({ cls: "cow-empty-state" });
      empty.createSpan({ text: "\u5F53\u524D\u6CA1\u6709\u9605\u8BFB\u8BA1\u5212\uFF0C\u53EF\u4EE5\u4ECE\u53F3\u4E0A\u89D2\u65B0\u589E\u4E00\u672C\u4E66\u7684\u9605\u8BFB\u65E5\u671F\u8303\u56F4\u3002" });
      return;
    }
    const list = container.createDiv({ cls: "cow-reading-plan-list" });
    plans.forEach((plan) => this.renderPlanCard(list, plan));
  }
  renderPlanCard(container, plan) {
    var _a, _b, _c, _d;
    const book = this.store.getBooks().find((item) => item.id === plan.bookId);
    const status = this.resolveStatus(plan);
    const card = container.createDiv({ cls: `cow-reading-plan-card is-${status}` });
    card.setAttr("role", "button");
    card.setAttr("tabindex", "0");
    card.addEventListener("click", () => openReadingPlanDetailModal(this.app, this.store, this.onDataChanged, plan));
    card.addEventListener("keydown", (event) => {
      if (event.key === "Enter" || event.key === " ") {
        event.preventDefault();
        openReadingPlanDetailModal(this.app, this.store, this.onDataChanged, plan);
      }
    });
    const cover = card.createDiv({ cls: "cow-reading-plan-cover" });
    const src = book ? this.getCoverSrc(book) : void 0;
    if (src) this.renderCoverImage(cover, src, (_a = book == null ? void 0 : book.title) != null ? _a : "\u9605\u8BFB\u8BA1\u5212");
    else cover.createSpan({ text: ((_b = book == null ? void 0 : book.title) != null ? _b : "\u8BA1\u5212").slice(0, 2) });
    const body = card.createDiv({ cls: "cow-reading-plan-body" });
    const head = body.createDiv({ cls: "cow-list-item-head" });
    const title = head.createDiv({ cls: "cow-reading-plan-title" });
    title.createEl("strong", { text: (_c = book == null ? void 0 : book.title) != null ? _c : "\u672A\u627E\u5230\u5173\u8054\u4E66\u7C4D" });
    title.createSpan({ text: book ? book.author : "\u8FD9\u6761\u8BA1\u5212\u4FDD\u7559\u4E86\u6570\u636E\uFF0C\u4F46\u4E66\u7C4D\u8BB0\u5F55\u53EF\u80FD\u5DF2\u88AB\u5220\u9664\u3002" });
    const badge = head.createSpan({ cls: `cow-reading-plan-status is-${status}`, text: status === "completed" ? `\u2713 ${readingPlanStatusLabel(status)}` : readingPlanStatusLabel(status) });
    body.createDiv({ cls: "cow-meta-line", text: formatDateRange4(plan.startDate, plan.endDate) });
    body.createDiv({ cls: "cow-meta-line", text: `\u76EE\u6807\uFF1A${(_d = plan.goal) != null ? _d : "\u8BFB\u5B8C\u672C\u4E66"}` });
    if (plan.note) body.createEl("p", { text: plan.note });
    this.renderProgress(body, book, plan);
    const actions = card.createDiv({ cls: "cow-reading-plan-actions" });
    if (status === "active" && (book == null ? void 0 : book.readingStatus) === "want-to-read") {
      this.textButton(actions, "\u8BBE\u4E3A\u5728\u8BFB", "play", async () => {
        if (!confirm(`\u9605\u8BFB\u8BA1\u5212\u5DF2\u5F00\u59CB\uFF0C\u662F\u5426\u5C06\u300A${book.title}\u300B\u8BBE\u4E3A\u5728\u8BFB\uFF1F`)) return;
        await this.store.updateBookReadingStatus(book.id, "reading");
        new import_obsidian65.Notice(`\u300A${book.title}\u300B\u5DF2\u8BBE\u4E3A\u5728\u8BFB\u3002`);
        this.onDataChanged();
      });
    }
    this.iconButton(actions, "pencil", "\u7F16\u8F91\u8BA1\u5212", () => openReadingPlanModal(this.app, this.store, this.onDataChanged, plan));
    if (status !== "completed") {
      this.iconButton(actions, "check", "\u5B8C\u6210\u8BA1\u5212", async () => {
        await this.store.completeReadingPlan(plan.id);
        this.onDataChanged();
      });
    }
    this.iconButton(actions, "trash-2", "\u5220\u9664\u8BA1\u5212", async () => {
      if (!confirm("\u5220\u9664\u8FD9\u6761\u9605\u8BFB\u8BA1\u5212\uFF1F\u4E0D\u4F1A\u5220\u9664\u5BF9\u5E94\u4E66\u7C4D\u3002")) return;
      await this.store.deleteReadingPlan(plan.id);
      this.onDataChanged();
    });
    if (badge && status === "overdue") {
      badge.setText("\u5DF2\u903E\u671F");
    }
  }
  renderProgress(container, book, plan) {
    const progress = Math.max(0, Math.min(100, Number(plan.progress) || 0));
    const row = container.createDiv({ cls: "cow-reading-plan-progress" });
    row.createSpan({ text: `\u8BA1\u5212\u8FDB\u5EA6 ${progress}%` });
    if (book == null ? void 0 : book.totalPages) row.createSpan({ text: `${book.currentPage}/${book.totalPages} \u9875` });
    const track = row.createDiv({ cls: "cow-month-progress-track" });
    track.createDiv({ cls: "cow-month-progress-fill is-pink", attr: { style: `width: ${progress}%` } });
  }
  resolveStatus(plan) {
    if (plan.status === "completed" || plan.completedDate) return "completed";
    const today6 = todayKey4();
    if (today6 < plan.startDate) return "planned";
    if (today6 > plan.endDate) return "overdue";
    return "active";
  }
  getCoverSrc(book) {
    var _a;
    if (book.coverPath) return this.app.vault.adapter.getResourcePath(book.coverPath);
    return (_a = book.cover) != null ? _a : book.coverUrl;
  }
  renderCoverImage(container, src, title) {
    const img = container.createEl("img", { attr: { src, alt: title } });
    img.addEventListener("error", () => {
      container.empty();
      container.createSpan({ text: title.slice(0, 2) });
    });
  }
  iconButton(container, icon, label, onClick) {
    const button = container.createEl("button", { attr: { type: "button", "aria-label": label } });
    (0, import_obsidian65.setIcon)(button, icon);
    button.addEventListener("click", (event) => {
      event.stopPropagation();
      onClick();
    });
  }
  textButton(container, label, icon, onClick) {
    const button = container.createEl("button", { cls: "cow-section-add-button", attr: { type: "button" } });
    (0, import_obsidian65.setIcon)(button.createSpan(), icon);
    button.createSpan({ text: label });
    button.addEventListener("click", (event) => {
      event.stopPropagation();
      onClick();
    });
  }
};
function todayKey4() {
  const date = /* @__PURE__ */ new Date();
  return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, "0")}-${String(date.getDate()).padStart(2, "0")}`;
}
function formatMonthDay2(date) {
  const [, month, day] = date.split("-");
  return `${Number(month)}\u6708${Number(day)}\u65E5`;
}
function formatDateRange4(startDate, endDate) {
  return `${formatMonthDay2(startDate)} - ${formatMonthDay2(endDate)}`;
}

// src/components/reading/ReadingStatsSection.ts
var ReadingStatsSection = class {
  constructor(store) {
    this.store = store;
  }
  render(container) {
    const books = this.store.getBooks();
    const total = books.length;
    const finished = books.filter((book) => book.readingStatus === "finished").length;
    const pages = books.reduce((sum, book) => sum + book.currentPage, 0);
    const stats = [
      ["\u85CF\u4E66", `${total}\u672C`],
      ["\u5DF2\u8BFB", `${finished}\u672C`],
      ["\u7D2F\u8BA1\u9875\u6570", `${pages}\u9875`],
      ["\u5B8C\u6210\u7387", `${total === 0 ? 0 : Math.round(finished / total * 100)}%`]
    ];
    const grid = container.createDiv({ cls: "cow-reading-stat-grid" });
    stats.forEach(([label, value]) => {
      const item = grid.createDiv();
      item.createEl("strong", { text: value });
      item.createSpan({ text: label });
    });
  }
};

// src/components/research/DataAnalysisTasksSection.ts
var import_obsidian66 = require("obsidian");
var DataAnalysisTasksSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createDiv({ cls: "cow-data-list" });
    this.store.getDataAnalysisTasks().forEach((task) => {
      const row = list.createDiv({ cls: "cow-data-card" });
      const head = row.createDiv({ cls: "cow-list-item-head" });
      head.createEl("strong", { text: task.title });
      const actions = head.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u4EFB\u52A1" } });
      (0, import_obsidian66.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => openDataAnalysisTaskModal(this.app, async (values) => {
        await this.store.updateDataAnalysisTask(task.id, values);
        this.onDataChanged();
      }, task));
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u4EFB\u52A1" } });
      (0, import_obsidian66.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        await this.store.deleteDataAnalysisTask(task.id);
        this.onDataChanged();
      });
      const meta = row.createDiv({ cls: "cow-meta-line" });
      meta.createSpan({ cls: "cow-status is-green", text: task.status });
      meta.createSpan({ text: `${task.progress}%` });
      const track = row.createDiv({ cls: "cow-month-progress-track" });
      track.createDiv({ cls: "cow-month-progress-fill is-green", attr: { style: `width: ${task.progress}%` } });
    });
  }
};

// src/components/research/ExperimentSection.ts
var import_obsidian67 = require("obsidian");
var ExperimentSection = class {
  constructor(app, store, mode, onDataChanged) {
    this.app = app;
    this.store = store;
    this.mode = mode;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const items = this.mode === "plan" ? this.store.getExperimentPlans() : this.store.getExperimentRecords();
    const list = container.createDiv({ cls: "cow-data-list" });
    items.forEach((item) => this.renderItem(list, item));
  }
  renderItem(container, item) {
    const button = container.createEl("button", { cls: "cow-data-card cow-click-card", attr: { type: "button" } });
    const head = button.createDiv({ cls: "cow-list-item-head" });
    head.createEl("strong", { text: item.title });
    const actions = head.createDiv({ cls: "cow-list-item-actions" });
    const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u5B9E\u9A8C" } });
    (0, import_obsidian67.setIcon)(edit, "pencil");
    edit.addEventListener("click", (event) => {
      event.stopPropagation();
      openExperimentEditModal(this.app, this.store, this.mode, this.onDataChanged, item);
    });
    const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u5B9E\u9A8C" } });
    (0, import_obsidian67.setIcon)(remove, "trash-2");
    remove.addEventListener("click", async (event) => {
      event.stopPropagation();
      if (this.mode === "plan") {
        new DeleteExperimentPlanModal(this.app, this.store, item, this.onDataChanged).open();
        return;
      }
      if (!confirm("\u5220\u9664\u5B9E\u9A8C\u8BB0\u5F55\uFF1F\u4E0D\u4F1A\u5220\u9664\u5173\u8054 Markdown\u3002")) return;
      await this.store.deleteExperiment(this.mode, item.id);
      this.onDataChanged();
    });
    const meta = button.createDiv({ cls: "cow-meta-line" });
    meta.createSpan({ cls: "cow-status is-yellow", text: item.status });
    meta.createSpan({ text: item.date });
    if (this.mode === "records") {
      const plan = this.store.getExperimentPlans().find((plan2) => plan2.id === item.experimentPlanId);
      meta.createSpan({ text: plan ? `\u6240\u5C5E\u8BA1\u5212\uFF1A${plan.title}` : "\u672A\u5173\u8054\u8BA1\u5212" });
    }
    button.addEventListener("click", () => {
      if (this.mode === "plan") new ExperimentPlanDetailModal(this.app, this.store, item, this.onDataChanged).open();
      else new ExperimentRecordDetailModal(this.app, this.store, item, this.onDataChanged).open();
    });
  }
  async openNote(notePath) {
    if (!notePath) {
      new import_obsidian67.Notice("\u8FD9\u6761\u5B9E\u9A8C\u8BB0\u5F55\u8FD8\u6CA1\u6709\u7ED1\u5B9A Markdown\u3002");
      return;
    }
    const file = this.app.vault.getFileByPath(notePath);
    if (!file) {
      new import_obsidian67.Notice(`\u6CA1\u6709\u627E\u5230\u7B14\u8BB0\uFF1A${notePath}`);
      return;
    }
    await this.app.workspace.getLeaf(false).openFile(file);
  }
};

// src/components/research/LiteratureNotesSection.ts
var import_obsidian68 = require("obsidian");
var LiteratureNotesSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createDiv({ cls: "cow-data-list" });
    const validPaths = new Set(this.app.vault.getMarkdownFiles().map((file) => file.path));
    const allNotes = this.store.getLiteratureNotes();
    const notes = allNotes.filter((note) => validPaths.has(note.notePath));
    if (notes.length !== allNotes.length) {
      void this.store.cleanupInvalidLiteratureNotes(validPaths).then((removed) => {
        if (removed > 0) this.onDataChanged();
      });
    }
    if (notes.length === 0) {
      list.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u6587\u732E\u7B14\u8BB0\u3002\u53EF\u4EE5\u4ECE\u53F3\u4E0A\u89D2\u6DFB\u52A0\uFF0C\u6216\u5728\u8BBA\u6587\u8BE6\u60C5\u4E2D\u5173\u8054\u5DF2\u6709\u7B14\u8BB0\u3002" });
      return;
    }
    notes.forEach((note) => this.renderNote(list, note));
  }
  renderNote(container, note) {
    const card = container.createDiv({ cls: "cow-data-card cow-literature-note-row" });
    const body = card.createDiv({ cls: "cow-paper-body" });
    const head = body.createDiv({ cls: "cow-list-item-head" });
    const title = head.createEl("button", { cls: "cow-paper-title-button", text: note.title, attr: { type: "button" } });
    title.addEventListener("click", () => void openLiteratureNoteFile(this.app, this.store, note, this.onDataChanged));
    const actions = head.createDiv({ cls: "cow-list-item-actions" });
    const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u6587\u732E\u7B14\u8BB0" } });
    (0, import_obsidian68.setIcon)(edit, "pencil");
    edit.addEventListener("click", (event) => {
      event.stopPropagation();
      openLiteratureNoteModal(this.app, this.store, this.onDataChanged, note);
    });
    const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u6E05\u7406\u6587\u732E\u7B14\u8BB0\u5173\u8054" } });
    (0, import_obsidian68.setIcon)(remove, "trash-2");
    remove.addEventListener("click", (event) => {
      event.stopPropagation();
      new DeleteLiteratureNoteModal(this.app, this.store, note, this.onDataChanged).open();
    });
    const paper = this.store.getResearchPapers().find((item) => item.id === note.paperReadingId);
    body.createDiv({ cls: "cow-meta-line", text: paperLabel(paper) });
    body.createDiv({ cls: "cow-meta-line", text: note.notePath });
  }
};

// src/components/research/PaperFieldManagerSection.ts
var import_obsidian71 = require("obsidian");

// src/components/research/PaperQueueModals.ts
var import_obsidian70 = require("obsidian");

// src/services/ZoteroService.ts
var ZoteroService = class {
  constructor(app) {
    this.app = app;
  }
  getRegisteredZoteroCommands() {
    var _a, _b;
    const commands = (_b = (_a = this.app.commands) == null ? void 0 : _a.commands) != null ? _b : {};
    return Object.entries(commands).map(([id, command]) => {
      var _a2;
      return { id, name: (_a2 = command.name) != null ? _a2 : id };
    }).filter((command) => `${command.id} ${command.name}`.toLowerCase().includes("zotero"));
  }
  async loadBetterBibtexJson(vaultPath) {
    if (!vaultPath) return [];
    const exists = await this.app.vault.adapter.exists(vaultPath);
    if (!exists) return [];
    const text = await this.app.vault.adapter.read(vaultPath);
    const parsed = JSON.parse(text);
    const items = this.extractItems(parsed);
    return items.map((item) => this.toCandidate(item)).filter((item) => item.title);
  }
  extractItems(parsed) {
    if (Array.isArray(parsed)) return parsed.filter(isRecord);
    if (isRecord(parsed)) {
      const graph = parsed["@graph"];
      if (Array.isArray(graph)) return graph.filter(isRecord);
      const items = parsed.items;
      if (Array.isArray(items)) return items.filter(isRecord);
      const references = parsed.references;
      if (Array.isArray(references)) return references.filter(isRecord);
    }
    return [];
  }
  toCandidate(item) {
    const title = stringValue(item.title) || stringValue(item["container-title"]);
    const venue = firstString(item.publicationTitle) || firstString(item.conferenceName) || firstString(item["container-title"]) || firstString(item["event-title"]);
    const year = this.extractYear(item);
    const doi = firstString(item.DOI) || firstString(item.doi);
    const url = firstString(item.URL) || firstString(item.url) || (doi ? `https://doi.org/${doi}` : void 0);
    return {
      zoteroItemKey: firstString(item.itemKey) || firstString(item.key) || firstString(item.id),
      citekey: firstString(item.citekey) || firstString(item.citationKey) || firstString(item["citation-key"]),
      title,
      venue,
      year,
      doi,
      paperUrl: url,
      tags: this.extractTags(item),
      authors: this.extractAuthors(item)
    };
  }
  extractYear(item) {
    const direct = firstString(item.year) || firstString(item.date) || firstString(item.issued);
    const match = direct == null ? void 0 : direct.match(/\d{4}/);
    if (match) return Number(match[0]);
    const issued = item.issued;
    if (isRecord(issued) && Array.isArray(issued["date-parts"])) {
      const first = issued["date-parts"][0];
      if (Array.isArray(first) && typeof first[0] === "number") return first[0];
    }
    return void 0;
  }
  extractTags(item) {
    var _a, _b;
    const tags = (_b = (_a = item.tags) != null ? _a : item.keyword) != null ? _b : item.keywords;
    if (Array.isArray(tags)) {
      return tags.map((tag) => isRecord(tag) ? firstString(tag.tag) || firstString(tag.name) : String(tag)).filter((tag) => Boolean(tag));
    }
    if (typeof tags === "string") {
      return tags.split(/[,;，；]/).map((tag) => tag.trim()).filter(Boolean);
    }
    return [];
  }
  extractAuthors(item) {
    var _a, _b;
    const creators = (_b = (_a = item.creators) != null ? _a : item.author) != null ? _b : item.authors;
    if (!Array.isArray(creators)) return "";
    return creators.map((creator) => {
      if (!isRecord(creator)) return String(creator);
      const literal = firstString(creator.literal) || firstString(creator.name);
      if (literal) return literal;
      return [firstString(creator.given), firstString(creator.family)].filter(Boolean).join(" ");
    }).filter(Boolean).join(", ");
  }
};
function isRecord(value) {
  return typeof value === "object" && value !== null;
}
function stringValue(value) {
  var _a;
  if (Array.isArray(value)) return String((_a = value[0]) != null ? _a : "");
  if (typeof value === "string") return value;
  if (typeof value === "number") return String(value);
  return "";
}
function firstString(value) {
  const text = stringValue(value).trim();
  return text || void 0;
}

// src/services/ZoteroLocalApiService.ts
var import_obsidian69 = require("obsidian");
var ZoteroLocalApiError = class extends Error {
  constructor(diagnostic) {
    super(diagnostic.message);
    this.diagnostic = diagnostic;
    this.name = "ZoteroLocalApiError";
  }
};
var ZOTERO_LOCAL_API_BASES = ["http://127.0.0.1:23119/api", "http://localhost:23119/api"];
var PAPER_TYPES = /* @__PURE__ */ new Set(["journalArticle", "conferencePaper", "preprint", "thesis"]);
var ZoteroLocalApiService = class {
  async checkConnection() {
    this.activeBaseUrl = void 0;
    this.diagnostic = void 0;
    for (const baseUrl of ZOTERO_LOCAL_API_BASES) {
      try {
        const response = await this.requestZotero("/", baseUrl);
        if (isOk(response.status)) {
          this.activeBaseUrl = baseUrl;
          return true;
        }
        this.diagnostic = createDiagnostic(baseUrl, response.status);
        this.logDiagnostic(this.diagnostic);
      } catch (error) {
        this.diagnostic = toDiagnostic(baseUrl, error);
        this.logDiagnostic(this.diagnostic);
      }
    }
    return false;
  }
  async getTopItems() {
    var _a;
    if (!this.activeBaseUrl && !await this.checkConnection()) {
      throw new ZoteroLocalApiError((_a = this.diagnostic) != null ? _a : {
        url: ZOTERO_LOCAL_API_BASES[0],
        message: "Zotero Local API \u8BF7\u6C42\u5931\u8D25\u3002",
        kind: "request-failed"
      });
    }
    const items = [];
    const limit = 100;
    let start = 0;
    for (; ; ) {
      const path = `/users/0/items/top?format=json&limit=${limit}&start=${start}`;
      const response = await this.requestZotero(path, this.activeBaseUrl);
      if (!isOk(response.status)) {
        this.diagnostic = {
          url: `${this.activeBaseUrl}${path}`,
          status: response.status,
          message: `Zotero \u5DF2\u8FDE\u63A5\uFF0C\u4F46\u8BFB\u53D6\u6587\u732E\u5217\u8868\u5931\u8D25\uFF08HTTP ${response.status}\uFF09\u3002`,
          kind: "items-failed"
        };
        this.logDiagnostic(this.diagnostic);
        throw new ZoteroLocalApiError(this.diagnostic);
      }
      const page = response.json;
      if (!Array.isArray(page)) break;
      items.push(...page.filter(isZoteroTopItem));
      if (page.length < limit) break;
      start += limit;
    }
    return items;
  }
  async getPapers() {
    const items = await this.getTopItems();
    return items.filter((item) => {
      var _a, _b;
      return PAPER_TYPES.has((_b = (_a = item.data) == null ? void 0 : _a.itemType) != null ? _b : "");
    }).map((item) => {
      var _a, _b, _c, _d, _e, _f, _g, _h;
      return {
        itemKey: (_a = item.key) != null ? _a : "",
        title: (_d = (_c = (_b = item.data) == null ? void 0 : _b.title) == null ? void 0 : _c.trim()) != null ? _d : "",
        venue: firstText((_e = item.data) == null ? void 0 : _e.conferenceName, (_f = item.data) == null ? void 0 : _f.publicationTitle, (_g = item.data) == null ? void 0 : _g.proceedingsTitle),
        year: extractYear((_h = item.data) == null ? void 0 : _h.date),
        paperUrl: paperUrl(item.data)
      };
    }).filter((item) => item.itemKey && item.title);
  }
  getDiagnostic() {
    return this.diagnostic;
  }
  async requestZotero(path, baseUrl = ((_a) => (_a = this.activeBaseUrl) != null ? _a : ZOTERO_LOCAL_API_BASES[0])()) {
    const url = `${baseUrl}${path}`;
    try {
      return await (0, import_obsidian69.requestUrl)({
        url,
        method: "GET",
        headers: {
          "Zotero-Allowed-Request": "true",
          "Zotero-API-Version": "3"
        },
        throw: false
      });
    } catch (error) {
      const diagnostic = toDiagnostic(url, error);
      this.logDiagnostic(diagnostic);
      throw new ZoteroLocalApiError(diagnostic);
    }
  }
  logDiagnostic(diagnostic) {
    console.warn("[Cute Workbench][Zotero]", diagnostic);
  }
};
function zoteroErrorMessage(diagnostic) {
  if (!diagnostic) return "Zotero Local API \u8BF7\u6C42\u5931\u8D25\u3002";
  if (diagnostic.kind === "connection-refused") return "Zotero Desktop \u672A\u8FD0\u884C\uFF0C\u6216\u672C\u5730 API \u6682\u4E0D\u53EF\u8FBE\u3002";
  if (diagnostic.kind === "forbidden") return "Zotero Local API \u62D2\u7EDD\u8BF7\u6C42\uFF0C\u8BF7\u68C0\u67E5 Allow other applications... \u8BBE\u7F6E\u3002";
  if (diagnostic.kind === "items-failed") return "Zotero \u5DF2\u8FDE\u63A5\uFF0C\u4F46\u8BFB\u53D6\u6587\u732E\u5217\u8868\u5931\u8D25\u3002";
  return "Zotero Local API \u8BF7\u6C42\u5931\u8D25\u3002";
}
function isZoteroTopItem(value) {
  return typeof value === "object" && value !== null && "data" in value;
}
function firstText(...values) {
  return values.map((value) => value == null ? void 0 : value.trim()).find(Boolean);
}
function extractYear(value) {
  const match = value == null ? void 0 : value.match(/\b(19|20)\d{2}\b/);
  return match ? Number(match[0]) : void 0;
}
function paperUrl(data) {
  var _a, _b;
  const url = (_a = data == null ? void 0 : data.url) == null ? void 0 : _a.trim();
  if (url && /^https?:\/\//i.test(url)) return url;
  const doi = (_b = data == null ? void 0 : data.DOI) == null ? void 0 : _b.trim();
  return doi ? `https://doi.org/${doi.replace(/^https?:\/\/(dx\.)?doi\.org\//i, "")}` : void 0;
}
function isOk(status) {
  return status >= 200 && status < 300;
}
function createDiagnostic(url, status) {
  return {
    url,
    status,
    message: `Zotero Local API returned HTTP ${status}`,
    kind: status === 403 ? "forbidden" : "request-failed"
  };
}
function toDiagnostic(url, error) {
  const message = error instanceof Error ? error.message : String(error);
  const lower = message.toLowerCase();
  const refused = lower.includes("econnrefused") || lower.includes("failed to fetch") || lower.includes("network") || lower.includes("connect");
  return {
    url,
    message,
    kind: refused ? "connection-refused" : "request-failed"
  };
}

// src/services/PaperZoteroSyncService.ts
var PaperZoteroSyncService = class {
  constructor(store) {
    this.store = store;
  }
  async importOrUpdateMany(items) {
    const result = { created: 0, updated: 0, unchanged: 0, failed: 0 };
    for (const item of items) {
      try {
        const status = await this.importOrUpdate(item);
        result[status] += 1;
      } catch (e) {
        result.failed += 1;
      }
    }
    return result;
  }
  async importOrUpdate(item) {
    const existing = this.store.getResearchPapers().find((paper) => paper.zoteroItemKey === item.itemKey);
    const venueId = await this.resolveVenue(item);
    if (!existing) {
      await this.store.addResearchPaper(this.createPaper(item, venueId));
      return "created";
    }
    const merged = this.mergePaper(existing, item, venueId);
    if (!merged) return "unchanged";
    await this.store.updateResearchPaper(existing.id, merged);
    return "updated";
  }
  async resolveVenue(item) {
    var _a;
    const venue = (_a = item.venue) == null ? void 0 : _a.trim();
    if (!venue) return void 0;
    const existing = this.store.getPaperVenues().find((definition) => sameVenue(definition.name, venue));
    if (existing) return existing.id;
    const next = {
      id: `venue-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
      name: venue,
      type: "other",
      color: "#f7c66a"
    };
    await this.store.addPaperVenue(next);
    return next.id;
  }
  mergePaper(existing, incoming, venueId) {
    var _a, _b, _c;
    const updates = {};
    if (existing.title !== incoming.title) updates.title = incoming.title;
    if (((_a = existing.year) != null ? _a : void 0) !== ((_b = incoming.year) != null ? _b : void 0)) updates.year = incoming.year;
    if (((_c = existing.venueId) != null ? _c : void 0) !== (venueId != null ? venueId : void 0)) {
      updates.venueId = venueId;
    }
    if (!existing.paperUrl && incoming.paperUrl) updates.paperUrl = incoming.paperUrl;
    if (Object.keys(updates).length === 0) return void 0;
    updates.updatedAt = Date.now();
    return updates;
  }
  createPaper(item, venueId) {
    var _a, _b;
    const now = Date.now();
    return {
      id: `paper-${now}-${item.itemKey}`,
      title: item.title,
      venue: "",
      venueId,
      year: item.year,
      statusId: (_b = (_a = this.store.getPaperStatuses()[0]) == null ? void 0 : _a.id) != null ? _b : "paper-status-unread",
      readingProgress: 0,
      readingStartDate: void 0,
      readingEndDate: void 0,
      researchProjectId: void 0,
      tagIds: [],
      paperUrl: item.paperUrl,
      createdAt: now,
      updatedAt: now,
      zoteroItemKey: item.itemKey
    };
  }
};
function sameVenue(left, right) {
  return left.trim().toLowerCase() === right.trim().toLowerCase();
}

// src/components/research/PaperQueueModals.ts
var PaperQueueManagerModal = class extends import_obsidian70.Modal {
  constructor(app, store, onDone) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.query = "";
    this.filters = { tagIds: [] };
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-paper-manager-modal",
      width: "min(1050px, 92vw)",
      height: "min(720px, 86vh)",
      maxWidth: "96vw",
      maxHeight: "94vh",
      minWidth: "min(620px, 92vw)",
      minHeight: "min(420px, 82vh)"
    });
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-paper-modal", "cow-paper-manager-modal");
    const root = this.contentEl.createDiv({ cls: "cow-paper-manager-root" });
    const header = root.createDiv({ cls: "cow-paper-manager-header" });
    header.createEl("h2", { text: "\u8BBA\u6587\u9605\u8BFB\u961F\u5217\u7BA1\u7406" });
    const toolbar = root.createDiv({ cls: "cow-paper-manager-toolbar" });
    const search = toolbar.createEl("input", {
      cls: "cow-paper-manager-search",
      attr: { type: "search", placeholder: "\u641C\u7D22\u8BBA\u6587\u6807\u9898\u3001\u4F1A\u8BAE/\u671F\u520A\u3001\u5E74\u4EFD\u3001\u6807\u7B7E..." }
    });
    search.value = this.query;
    search.addEventListener("input", () => {
      this.query = search.value;
      this.refreshList();
    });
    const actions = toolbar.createDiv({ cls: "cow-paper-manager-actions" });
    const add = actions.createEl("button", { cls: "cow-section-add-button", attr: { type: "button" } });
    (0, import_obsidian70.setIcon)(add.createSpan(), "plus");
    add.createSpan({ text: "\u624B\u52A8\u6DFB\u52A0" });
    add.addEventListener("click", () => new PaperEditModal(this.app, this.store, () => {
      this.onDone();
      this.refreshList();
    }).open());
    const zotero = actions.createEl("button", { cls: "cow-section-add-button", attr: { type: "button" } });
    (0, import_obsidian70.setIcon)(zotero.createSpan(), "download");
    zotero.createSpan({ text: "Zotero \u5BFC\u5165" });
    if (import_obsidian70.Platform.isMobileApp) {
      zotero.disabled = true;
      zotero.setAttr("aria-label", "Zotero Local API \u4EC5\u652F\u6301\u684C\u9762\u7AEF");
    }
    zotero.addEventListener("click", () => new ZoteroPaperImportModal(this.app, this.store, () => {
      this.onDone();
      this.refreshList();
    }).open());
    const update = actions.createEl("button", { cls: "cow-section-add-button", attr: { type: "button" } });
    (0, import_obsidian70.setIcon)(update.createSpan(), "refresh-cw");
    update.createSpan({ text: "\u66F4\u65B0 Zotero" });
    update.addEventListener("click", () => void this.updateZoteroLinkedPapers());
    this.filterEl = root.createDiv({ cls: "cow-paper-manager-filterbar" });
    this.listEl = root.createDiv({ cls: "cow-paper-manager-list" });
    this.refreshFilters();
    this.refreshList();
  }
  refreshFilters() {
    var _a, _b, _c, _d, _e, _f;
    if (!this.filterEl) return;
    this.filterEl.empty();
    const filters = this.filterEl.createDiv({ cls: "cow-paper-manager-filter-controls" });
    this.renderSelect(filters, "\u9605\u8BFB\u72B6\u6001", (_a = this.filters.statusId) != null ? _a : "", [
      { value: "", label: "\u5168\u90E8\u72B6\u6001" },
      ...this.store.getPaperStatuses().map((status) => ({ value: status.id, label: status.name }))
    ], (value) => {
      this.filters.statusId = value || void 0;
      this.refreshFilters();
      this.refreshList();
    });
    this.renderSelect(filters, "\u4F1A\u8BAE / \u671F\u520A", (_b = this.filters.venueId) != null ? _b : "", [
      { value: "", label: "\u5168\u90E8\u4F1A\u8BAE / \u671F\u520A" },
      ...this.store.getPaperVenues().map((venue) => ({ value: venue.id, label: venue.name }))
    ], (value) => {
      this.filters.venueId = value || void 0;
      this.refreshFilters();
      this.refreshList();
    });
    const tagWrap = filters.createDiv({ cls: "cow-paper-manager-tag-filter" });
    tagWrap.createSpan({ text: "\u6807\u7B7E" });
    const tagList = tagWrap.createDiv({ cls: "cow-paper-filter-tags" });
    this.store.getPaperTags().forEach((tag) => {
      const active = this.filters.tagIds.includes(tag.id);
      const button = tagList.createEl("button", { text: tag.name, cls: active ? "is-active" : "", attr: { type: "button", style: `--paper-color: ${tag.color}` } });
      button.addEventListener("click", () => {
        this.filters.tagIds = active ? this.filters.tagIds.filter((id) => id !== tag.id) : [...this.filters.tagIds, tag.id];
        this.refreshFilters();
        this.refreshList();
      });
    });
    const chips = this.filterEl.createDiv({ cls: "cow-paper-filter-chips" });
    const addChip = (label, onRemove) => {
      const chip = chips.createEl("button", { text: `${label} \xD7`, attr: { type: "button" } });
      chip.addEventListener("click", () => {
        onRemove();
        this.refreshFilters();
        this.refreshList();
      });
    };
    if (this.filters.statusId) addChip((_d = (_c = this.store.getPaperStatuses().find((status) => status.id === this.filters.statusId)) == null ? void 0 : _c.name) != null ? _d : "\u72B6\u6001", () => this.filters.statusId = void 0);
    if (this.filters.venueId) addChip((_f = (_e = this.store.getPaperVenues().find((venue) => venue.id === this.filters.venueId)) == null ? void 0 : _e.name) != null ? _f : "\u4F1A\u8BAE / \u671F\u520A", () => this.filters.venueId = void 0);
    this.filters.tagIds.forEach((id) => {
      var _a2, _b2;
      return addChip((_b2 = (_a2 = this.store.getPaperTags().find((tag) => tag.id === id)) == null ? void 0 : _a2.name) != null ? _b2 : "\u6807\u7B7E", () => this.filters.tagIds = this.filters.tagIds.filter((tagId) => tagId !== id));
    });
    if (this.hasActiveFilters()) addChip("\u6E05\u9664\u7B5B\u9009", () => this.filters = { tagIds: [] });
  }
  refreshList() {
    if (!this.listEl) return;
    this.listEl.empty();
    const allPapers = this.store.getResearchPapers();
    const papers = this.filteredPapers(allPapers);
    if (allPapers.length === 0) {
      this.listEl.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u8BBA\u6587\uFF0C\u53EF\u4EE5\u901A\u8FC7\u201C\u624B\u52A8\u6DFB\u52A0\u201D\u6216\u201CZotero \u5BFC\u5165\u201D\u6DFB\u52A0\u8BBA\u6587\u3002" });
      return;
    }
    if (papers.length === 0) {
      this.listEl.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u7B26\u5408\u6761\u4EF6\u7684\u8BBA\u6587\u3002" });
      return;
    }
    papers.forEach((paper) => this.renderPaperRow(this.listEl, paper));
  }
  renderPaperRow(container, paper) {
    var _a, _b;
    const row = container.createDiv({ cls: "cow-paper-manager-item" });
    const header = row.createDiv({ cls: "cow-paper-manager-item-header" });
    const title = header.createEl("button", { cls: "cow-paper-title-button", text: paper.title, attr: { type: "button" } });
    title.addEventListener("click", () => new PaperDetailModal(this.app, this.store, paper, () => {
      this.onDone();
      this.refreshList();
    }).open());
    const actions = header.createDiv({ cls: "cow-list-item-actions" });
    iconButton(actions, "pencil", "\u7F16\u8F91\u8BBA\u6587", () => new PaperEditModal(this.app, this.store, () => {
      this.onDone();
      this.refreshList();
    }, paper).open());
    iconButton(actions, "trash-2", "\u5220\u9664\u8BBA\u6587", () => new DeletePaperReadingModal(this.app, this.store, paper, () => {
      this.onDone();
      this.refreshList();
    }).open());
    const metadata = row.createDiv({ cls: "cow-paper-meta-row" });
    metadata.createSpan({ cls: "cow-status is-blue", text: statusName(this.store, paper) });
    metadata.createSpan({ text: paperMetaText(this.store, paper) });
    const progress = row.createDiv({ cls: "cow-paper-progress-row" });
    progress.createDiv({ cls: "cow-month-progress-track" }).createDiv({ cls: "cow-month-progress-fill is-blue", attr: { style: `width: ${paper.readingProgress}%` } });
    progress.createSpan({ text: `${paper.readingProgress}%` });
    const tags = row.createDiv({ cls: "cow-paper-tags" });
    tagNames(this.store, paper).forEach((tag) => tags.createSpan({ text: tag }));
    row.createDiv({ cls: "cow-meta-line", text: `${(_a = paper.readingStartDate) != null ? _a : "-"} \u2192 ${(_b = paper.readingEndDate) != null ? _b : "-"}` });
  }
  renderSelect(container, label, value, options, onChange) {
    const wrap = container.createDiv({ cls: "cow-paper-manager-filter-field" });
    wrap.createSpan({ text: label });
    const select = wrap.createEl("select");
    options.forEach((option) => select.createEl("option", { value: option.value, text: option.label }));
    select.value = value;
    select.addEventListener("change", () => onChange(select.value));
  }
  filteredPapers(papers) {
    const query = this.query.trim().toLowerCase();
    return papers.filter((paper) => {
      if (this.filters.statusId && paper.statusId !== this.filters.statusId) return false;
      if (this.filters.venueId && paper.venueId !== this.filters.venueId) return false;
      if (this.filters.tagIds.some((tagId) => {
        var _a;
        return !((_a = paper.tagIds) != null ? _a : []).includes(tagId);
      })) return false;
      if (!query) return true;
      const haystack = [
        paper.title,
        paper.year,
        venueName(this.store, paper),
        statusName(this.store, paper),
        paperMetaText(this.store, paper),
        ...tagNames(this.store, paper)
      ].filter(Boolean).join(" ").toLowerCase();
      return haystack.includes(query);
    });
  }
  hasActiveFilters() {
    return Boolean(this.filters.statusId || this.filters.venueId || this.filters.tagIds.length > 0);
  }
  async updateZoteroLinkedPapers() {
    const candidates = await loadZoteroCandidates(this.app, this.store);
    if (candidates.length === 0) return;
    const linked = this.store.getResearchPapers().filter((paper) => paper.zoteroItemKey || paper.citekey || paper.doi || paper.paperUrl);
    const matches = linked.map((paper) => candidates.find((candidate) => candidateMatchesPaper(candidate, paper))).filter(Boolean);
    if (matches.length === 0) {
      new import_obsidian70.Notice("\u6CA1\u6709\u627E\u5230\u53EF\u66F4\u65B0\u7684 Zotero \u6761\u76EE\uFF0C\u672C\u5730\u8BBA\u6587\u5DF2\u4FDD\u7559\u3002");
      return;
    }
    const result = await this.store.importZoteroPapers(matches);
    new import_obsidian70.Notice(`Zotero \u66F4\u65B0\u5B8C\u6210\uFF1A\u65B0\u589E ${result.created}\uFF0C\u66F4\u65B0 ${result.updated}\uFF0C\u8DF3\u8FC7 ${result.skipped}\u3002`);
    this.onDone();
    this.refreshList();
  }
};
var PaperDetailModal = class extends import_obsidian70.Modal {
  constructor(app, store, paper, onDone) {
    super(app);
    this.store = store;
    this.paper = paper;
    this.onDone = onDone;
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-paper-edit-modal",
      width: "min(760px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "92vh",
      minWidth: "min(520px, 90vw)",
      minHeight: "min(360px, 78vh)"
    });
    this.render();
  }
  render() {
    var _a, _b, _c, _d, _e, _f;
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-paper-modal");
    const paper = (_a = this.store.getResearchPapers().find((item) => item.id === this.paper.id)) != null ? _a : this.paper;
    const header = this.contentEl.createDiv({ cls: "cow-list-item-head" });
    header.createEl("h2", { text: paper.title });
    const edit = header.createEl("button", { cls: "cow-section-add-button", attr: { type: "button" } });
    (0, import_obsidian70.setIcon)(edit.createSpan(), "pencil");
    edit.createSpan({ text: "\u7F16\u8F91" });
    edit.addEventListener("click", () => new PaperEditModal(this.app, this.store, () => {
      this.onDone();
      this.render();
    }, paper).open());
    const remove = header.createEl("button", { cls: "cow-section-add-button", attr: { type: "button" } });
    (0, import_obsidian70.setIcon)(remove.createSpan(), "trash-2");
    remove.createSpan({ text: "\u5220\u9664" });
    remove.addEventListener("click", () => new DeletePaperReadingModal(this.app, this.store, paper, () => {
      this.onDone();
      this.close();
    }).open());
    const refresh = header.createEl("button", { cls: "cow-section-add-button", attr: { type: "button" } });
    (0, import_obsidian70.setIcon)(refresh.createSpan(), "refresh-cw");
    refresh.createSpan({ text: "\u91CD\u65B0\u4ECE Zotero \u66F4\u65B0" });
    refresh.addEventListener("click", () => void this.updateFromZotero(paper));
    const importNote = header.createEl("button", { cls: "cow-section-add-button", attr: { type: "button" } });
    (0, import_obsidian70.setIcon)(importNote.createSpan(), "notebook-tabs");
    importNote.createSpan({ text: "\u5BFC\u5165/\u66F4\u65B0 Zotero \u7B14\u8BB0" });
    importNote.addEventListener("click", () => void this.importZoteroNote(paper));
    const linkNote = header.createEl("button", { cls: "cow-section-add-button", attr: { type: "button" } });
    (0, import_obsidian70.setIcon)(linkNote.createSpan(), "link");
    linkNote.createSpan({ text: "\u5173\u8054\u5DF2\u6709\u7B14\u8BB0" });
    linkNote.addEventListener("click", () => openLiteratureNoteModal(this.app, this.store, () => {
      this.onDone();
      this.render();
    }, void 0, { paperReadingId: paper.id, title: `${paper.title} \u7B14\u8BB0` }));
    const details = this.contentEl.createDiv({ cls: "cow-paper-detail-grid" });
    [
      ["\u4F1A\u8BAE / \u671F\u520A", venueName(this.store, paper)],
      ["\u5E74\u4EFD", String((_b = paper.year) != null ? _b : "")],
      ["\u72B6\u6001", statusName(this.store, paper)],
      ["\u9605\u8BFB\u8FDB\u5EA6", `${paper.readingProgress}%`],
      ["\u7814\u7A76\u9879\u76EE", (_d = (_c = this.store.getResearchProjects().find((project) => project.id === paper.researchProjectId)) == null ? void 0 : _c.title) != null ? _d : "\u672A\u5173\u8054"],
      ["\u9605\u8BFB\u65E5\u671F", `${(_e = paper.readingStartDate) != null ? _e : "-"} \u2192 ${(_f = paper.readingEndDate) != null ? _f : "-"}`],
      ["\u6807\u7B7E", tagNames(this.store, paper).join(" \xB7 ") || "\u65E0\u6807\u7B7E"],
      ["Zotero", paper.zoteroItemKey || paper.citekey ? [paper.zoteroItemKey, paper.citekey].filter(Boolean).join(" \xB7 ") : "\u9884\u7559\uFF0C\u6682\u672A\u5BFC\u5165"]
    ].forEach(([label, value]) => {
      const item = details.createDiv({ cls: "cow-data-card" });
      item.createEl("strong", { text: label });
      item.createSpan({ text: value });
    });
    const linkCard = details.createDiv({ cls: "cow-data-card" });
    linkCard.createEl("strong", { text: "\u8BBA\u6587\u94FE\u63A5" });
    if (paper.paperUrl) {
      const linkButton = linkCard.createEl("button", { cls: "cow-section-add-button", attr: { type: "button" } });
      (0, import_obsidian70.setIcon)(linkButton.createSpan(), "external-link");
      linkButton.createSpan({ text: "\u6253\u5F00\u8BBA\u6587" });
      linkButton.addEventListener("click", () => openPaperUrl(paper.paperUrl));
      linkCard.createDiv({ cls: "cow-meta-line", text: paper.paperUrl });
    } else {
      linkCard.createSpan({ text: "\u672A\u586B\u5199" });
    }
    this.renderLiteratureNotes(paper);
  }
  async updateFromZotero(paper) {
    const candidates = await loadZoteroCandidates(this.app, this.store);
    const match = candidates.find((candidate) => candidateMatchesPaper(candidate, paper));
    if (!match) {
      new import_obsidian70.Notice("Zotero \u6761\u76EE\u672A\u627E\u5230\uFF0C\u672C\u5730\u8BBA\u6587\u5DF2\u4FDD\u7559\u3002");
      return;
    }
    const result = await this.store.importZoteroPapers([match]);
    new import_obsidian70.Notice(`Zotero \u66F4\u65B0\u5B8C\u6210\uFF1A\u65B0\u589E ${result.created}\uFF0C\u66F4\u65B0 ${result.updated}\uFF0C\u8DF3\u8FC7 ${result.skipped}\u3002`);
    this.onDone();
    this.render();
  }
  renderLiteratureNotes(paper) {
    const section = this.contentEl.createDiv({ cls: "cow-paper-linked-notes" });
    section.createEl("h3", { text: "\u6587\u732E\u7B14\u8BB0" });
    const validPaths = new Set(this.app.vault.getMarkdownFiles().map((file) => file.path));
    const allNotes = this.store.getLiteratureNotesForPaper(paper.id);
    const notes = allNotes.filter((note) => validPaths.has(note.notePath));
    if (notes.length !== allNotes.length) {
      void this.store.cleanupInvalidLiteratureNotes(validPaths).then((removed) => {
        if (removed > 0) {
          this.onDone();
          this.render();
        }
      });
    }
    if (notes.length === 0) {
      section.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u5173\u8054\u6587\u732E\u7B14\u8BB0\u3002" });
      return;
    }
    notes.forEach((note) => {
      const row = section.createDiv({ cls: "cow-data-card cow-literature-note-row" });
      const body = row.createDiv({ cls: "cow-paper-body" });
      const title = body.createEl("button", { cls: "cow-paper-title-button", text: note.title, attr: { type: "button" } });
      title.addEventListener("click", () => void openLiteratureNoteFile(this.app, this.store, note, () => {
        this.onDone();
        this.render();
      }));
      body.createDiv({ cls: "cow-meta-line", text: note.notePath });
      const actions = row.createDiv({ cls: "cow-list-item-actions" });
      iconButton(actions, "pencil", "\u7F16\u8F91\u6587\u732E\u7B14\u8BB0", () => openLiteratureNoteModal(this.app, this.store, () => {
        this.onDone();
        this.render();
      }, note));
      iconButton(actions, "trash-2", "\u5220\u9664\u6587\u732E\u7B14\u8BB0", () => new DeleteLiteratureNoteModal(this.app, this.store, note, () => {
        this.onDone();
        this.render();
      }).open());
    });
  }
  async importZoteroNote(paper) {
    const command = findZoteroNoteCommand(this.app);
    if (!command) {
      new import_obsidian70.Notice("\u672A\u627E\u5230\u53EF\u7528\u7684 Zotero Integration \u7B14\u8BB0\u5BFC\u5165\u547D\u4EE4\u3002\u53EF\u4EE5\u4F7F\u7528\u201C\u5173\u8054\u5DF2\u6709\u7B14\u8BB0\u201D\u624B\u52A8\u5173\u8054 Markdown\u3002");
      return;
    }
    await executeCommand(this.app, command.id);
    const note = findLikelyZoteroNote(this.app, paper);
    if (!note) {
      new import_obsidian70.Notice("\u5DF2\u89E6\u53D1 Zotero Integration \u547D\u4EE4\uFF0C\u4F46\u672A\u80FD\u81EA\u52A8\u5B9A\u4F4D\u751F\u6210\u7684 Markdown\u3002\u8BF7\u4F7F\u7528\u201C\u5173\u8054\u5DF2\u6709\u7B14\u8BB0\u201D\u3002");
      return;
    }
    await this.store.upsertLiteratureNote({
      id: `literature-note-${paper.id}`,
      title: note.basename,
      notePath: note.path,
      paperReadingId: paper.id,
      createdAt: Date.now(),
      updatedAt: Date.now()
    });
    new import_obsidian70.Notice("Zotero \u7B14\u8BB0\u5DF2\u5173\u8054\u5230\u8BBA\u6587\u3002");
    this.onDone();
    this.render();
  }
};
var PaperEditModal = class extends import_obsidian70.Modal {
  constructor(app, store, onDone, paper) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p;
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.selectedLiteratureNoteId = "";
    const now = Date.now();
    this.draft = {
      id: (_a = paper == null ? void 0 : paper.id) != null ? _a : `paper-${now}`,
      title: (_b = paper == null ? void 0 : paper.title) != null ? _b : "",
      venue: (_c = paper == null ? void 0 : paper.venue) != null ? _c : "",
      venueId: paper == null ? void 0 : paper.venueId,
      year: (_d = paper == null ? void 0 : paper.year) != null ? _d : (/* @__PURE__ */ new Date()).getFullYear(),
      status: (_e = paper == null ? void 0 : paper.status) != null ? _e : "\u672A\u8BFB",
      statusId: (_g = paper == null ? void 0 : paper.statusId) != null ? _g : (_f = this.store.getPaperStatuses()[0]) == null ? void 0 : _f.id,
      readingProgress: (_h = paper == null ? void 0 : paper.readingProgress) != null ? _h : 0,
      paperUrl: (_i = paper == null ? void 0 : paper.paperUrl) != null ? _i : "",
      readingStartDate: (_j = paper == null ? void 0 : paper.readingStartDate) != null ? _j : "",
      readingEndDate: (_k = paper == null ? void 0 : paper.readingEndDate) != null ? _k : "",
      researchProjectId: paper == null ? void 0 : paper.researchProjectId,
      tags: (_l = paper == null ? void 0 : paper.tags) != null ? _l : [],
      tagIds: (_m = paper == null ? void 0 : paper.tagIds) != null ? _m : [],
      notePath: paper == null ? void 0 : paper.notePath,
      createdAt: (_n = paper == null ? void 0 : paper.createdAt) != null ? _n : now,
      updatedAt: now,
      zoteroItemKey: paper == null ? void 0 : paper.zoteroItemKey,
      citekey: paper == null ? void 0 : paper.citekey
    };
    this.selectedLiteratureNoteId = (_p = (_o = this.store.getLiteratureNotesForPaper(this.draft.id)[0]) == null ? void 0 : _o.id) != null ? _p : "";
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-paper-edit-modal",
      width: "min(760px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "92vh",
      minWidth: "min(520px, 90vw)",
      minHeight: "min(420px, 82vh)"
    });
    this.render();
  }
  render() {
    var _a, _b, _c, _d, _e, _f, _g;
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-paper-modal");
    this.contentEl.createEl("h2", { text: this.store.getResearchPapers().some((paper) => paper.id === this.draft.id) ? "\u7F16\u8F91\u8BBA\u6587" : "\u624B\u52A8\u6DFB\u52A0\u8BBA\u6587" });
    const basic = this.section("\u57FA\u7840\u4FE1\u606F");
    inputField2(basic, "\u8BBA\u6587\u540D\u79F0", this.draft.title, (value) => this.draft.title = value);
    selectField2(basic, "\u4F1A\u8BAE / \u671F\u520A", (_a = this.draft.venueId) != null ? _a : "", this.store.getPaperVenues().map((venue) => ({ value: venue.id, label: venue.name })), (value) => this.draft.venueId = value);
    inputField2(basic, "\u5E74\u4EFD", String((_b = this.draft.year) != null ? _b : (/* @__PURE__ */ new Date()).getFullYear()), (value) => this.draft.year = Number(value) || (/* @__PURE__ */ new Date()).getFullYear(), "number");
    const reading = this.section("\u9605\u8BFB\u4FE1\u606F");
    selectField2(reading, "\u9605\u8BFB\u72B6\u6001", (_c = this.draft.statusId) != null ? _c : "", this.store.getPaperStatuses().map((status) => ({ value: status.id, label: status.name })), (value) => this.draft.statusId = value);
    progressField(reading, this.draft.readingProgress, (value) => this.draft.readingProgress = value);
    dateField2(reading, "\u9605\u8BFB\u5F00\u59CB\u65E5\u671F", (_d = this.draft.readingStartDate) != null ? _d : "", (value) => this.draft.readingStartDate = value);
    dateField2(reading, "\u9605\u8BFB\u7ED3\u675F\u65E5\u671F", (_e = this.draft.readingEndDate) != null ? _e : "", (value) => this.draft.readingEndDate = value);
    const resources = this.section("\u8D44\u6E90");
    paperUrlField(resources, (_f = this.draft.paperUrl) != null ? _f : "", (value) => this.draft.paperUrl = value);
    this.literatureNoteSelect(resources);
    const category = this.section("\u5206\u7C7B");
    tagField(category, this.store.getPaperTags(), (_g = this.draft.tagIds) != null ? _g : [], (value) => this.draft.tagIds = value);
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", () => void this.save());
  }
  section(title) {
    const section = this.contentEl.createDiv({ cls: "cow-paper-edit-section" });
    section.createEl("h3", { text: title });
    return section.createDiv({ cls: "cow-paper-form" });
  }
  literatureNoteSelect(container) {
    const row = container.createDiv({ cls: "cow-book-form-row" });
    row.createEl("label", { text: "\u5173\u8054\u6587\u732E\u7B14\u8BB0" });
    const select = row.createEl("select");
    select.createEl("option", { value: "", text: "\u4E0D\u5173\u8054" });
    this.store.getLiteratureNotes().forEach((note) => {
      select.createEl("option", { value: note.id, text: note.title || fileName6(note.notePath) });
    });
    select.value = this.selectedLiteratureNoteId;
    select.addEventListener("change", () => this.selectedLiteratureNoteId = select.value);
    const selectedNote = this.store.getLiteratureNotes().find((note) => note.id === this.selectedLiteratureNoteId);
    if (selectedNote == null ? void 0 : selectedNote.notePath) row.createDiv({ cls: "cow-meta-line", text: selectedNote.notePath });
  }
  async save() {
    if (!this.draft.title.trim()) {
      new import_obsidian70.Notice("\u8BF7\u8F93\u5165\u8BBA\u6587\u540D\u79F0\u3002");
      return;
    }
    if (this.draft.readingEndDate && this.draft.readingStartDate && this.draft.readingEndDate < this.draft.readingStartDate) {
      new import_obsidian70.Notice("\u9605\u8BFB\u7ED3\u675F\u65E5\u671F\u4E0D\u80FD\u65E9\u4E8E\u5F00\u59CB\u65E5\u671F\u3002");
      return;
    }
    if (!this.confirmLiteratureNoteReassignment()) return;
    this.draft.updatedAt = Date.now();
    if (this.store.getResearchPapers().some((paper) => paper.id === this.draft.id)) {
      await this.store.updateResearchPaper(this.draft.id, this.draft);
    } else {
      await this.store.addResearchPaper(this.draft);
    }
    await this.syncLiteratureNoteLink();
    this.onDone();
    this.close();
  }
  confirmLiteratureNoteReassignment() {
    var _a;
    if (!this.selectedLiteratureNoteId) return true;
    const note = this.store.getLiteratureNotes().find((item) => item.id === this.selectedLiteratureNoteId);
    if (!(note == null ? void 0 : note.paperReadingId) || note.paperReadingId === this.draft.id) return true;
    const oldPaper = this.store.getResearchPapers().find((paper) => paper.id === note.paperReadingId);
    return confirm(`\u8BE5\u7B14\u8BB0\u5F53\u524D\u5173\u8054\u300A${(_a = oldPaper == null ? void 0 : oldPaper.title) != null ? _a : "\u5176\u5B83\u8BBA\u6587"}\u300B\uFF0C\u662F\u5426\u6539\u4E3A\u5173\u8054\u5F53\u524D\u8BBA\u6587\uFF1F`);
  }
  async syncLiteratureNoteLink() {
    const linkedToCurrent = this.store.getLiteratureNotes().filter((note) => note.paperReadingId === this.draft.id);
    for (const note of linkedToCurrent) {
      if (note.id !== this.selectedLiteratureNoteId) {
        await this.store.updateLiteratureNote(note.id, { paperReadingId: void 0 });
      }
    }
    if (!this.selectedLiteratureNoteId) return;
    await this.store.updateLiteratureNote(this.selectedLiteratureNoteId, { paperReadingId: this.draft.id });
  }
};
var DeletePaperReadingModal = class extends import_obsidian70.Modal {
  constructor(app, store, paper, onDone) {
    super(app);
    this.store = store;
    this.paper = paper;
    this.onDone = onDone;
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-paper-delete-modal",
      width: "min(560px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "84vh",
      minWidth: "min(420px, 90vw)",
      minHeight: "min(260px, 70vh)"
    });
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-paper-modal", "cow-delete-modal");
    const linkedNotes = this.store.getLiteratureNotesForPaper(this.paper.id);
    this.contentEl.createEl("h2", { text: "\u5220\u9664\u8BBA\u6587\u6761\u76EE\uFF1F" });
    const summary = this.contentEl.createDiv({ cls: "cow-delete-summary" });
    summary.createSpan({ text: "\u8BBA\u6587\uFF1A" });
    summary.createEl("strong", { text: `\u300A${this.paper.title}\u300B` });
    const description = this.contentEl.createDiv({ cls: "cow-delete-description" });
    description.createEl("p", { text: "\u8BE5\u64CD\u4F5C\u53EA\u4F1A\u5220\u9664 Cute Workbench \u4E2D\u7684\u8BBA\u6587\u9605\u8BFB\u8BB0\u5F55\u3002" });
    description.createEl("p", { text: "\u4E0D\u4F1A\u5220\u9664 Zotero \u4E2D\u7684\u539F\u59CB\u6761\u76EE\u3001Zotero PDF\u3001Obsidian Markdown \u7B14\u8BB0\u6216\u672C\u5730 PDF \u6587\u4EF6\u3002" });
    if (linkedNotes.length > 0) {
      description.createEl("p", { text: `\u8BE5\u8BBA\u6587\u5173\u8054\u4E86 ${linkedNotes.length} \u6761\u6587\u732E\u7B14\u8BB0\u3002\u5220\u9664\u8BBA\u6587\u8BB0\u5F55\u4E0D\u4F1A\u5220\u9664\u7B14\u8BB0\uFF0C\u7B14\u8BB0\u5C06\u53D8\u4E3A\u672A\u5173\u8054\u72B6\u6001\u3002` });
    }
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u5220\u9664\u8BBA\u6587\u8BB0\u5F55", cls: "mod-warning", attr: { type: "button" } }).addEventListener("click", () => void this.delete());
  }
  async delete() {
    await this.store.deletePaperReading(this.paper.id);
    new import_obsidian70.Notice("\u8BBA\u6587\u9605\u8BFB\u8BB0\u5F55\u5DF2\u5220\u9664\uFF0C\u5173\u8054\u6587\u732E\u7B14\u8BB0\u5DF2\u4FDD\u7559\u3002");
    this.onDone();
    this.close();
  }
};
var ZoteroPaperImportModal = class extends import_obsidian70.Modal {
  constructor(app, store, onDone) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.pageSize = 120;
    this.items = [];
    this.selectedKeys = /* @__PURE__ */ new Set();
    this.query = "";
    this.statusFilter = "all";
    this.visibleLimit = this.pageSize;
    this.loading = true;
    this.connected = false;
    this.error = "";
  }
  async onOpen() {
    if (import_obsidian70.Platform.isMobileApp) {
      new import_obsidian70.Notice("Zotero Local API \u4EC5\u652F\u6301\u684C\u9762\u7AEF\u3002");
      this.close();
      return;
    }
    applyResizableModal(this, {
      className: "cute-zotero-import-modal",
      width: "min(1100px, 92vw)",
      height: "min(720px, 86vh)",
      maxWidth: "96vw",
      maxHeight: "94vh",
      minWidth: "min(560px, 92vw)",
      minHeight: "min(420px, 82vh)"
    });
    await this.load();
    this.render();
  }
  async load() {
    this.loading = true;
    this.error = "";
    this.diagnostic = void 0;
    this.visibleLimit = this.pageSize;
    this.render();
    const service = new ZoteroLocalApiService();
    this.connected = await service.checkConnection();
    this.diagnostic = service.getDiagnostic();
    if (!this.connected) {
      this.items = [];
      this.loading = false;
      this.error = zoteroErrorMessage(this.diagnostic);
      this.render();
      return;
    }
    try {
      this.items = await service.getPapers();
    } catch (e) {
      this.items = [];
      this.diagnostic = service.getDiagnostic();
      this.error = zoteroErrorMessage(this.diagnostic);
    } finally {
      this.loading = false;
      this.render();
    }
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-paper-modal", "cow-zotero-import-modal", "cow-zotero-local-modal");
    const header = this.contentEl.createDiv({ cls: "cow-zotero-local-header" });
    header.createEl("h2", { text: "Zotero Local API \u8BBA\u6587\u9884\u89C8" });
    this.renderConnectionStatus(header);
    const tools = this.contentEl.createDiv({ cls: "cow-zotero-toolbar" });
    const search = tools.createEl("input", { attr: { type: "search", placeholder: "\u641C\u7D22\u6807\u9898\u3001\u4F5C\u8005\u3001\u4F1A\u8BAE/\u671F\u520A\u3001citekey" } });
    search.value = this.query;
    search.addEventListener("input", () => {
      this.query = search.value;
      this.visibleLimit = this.pageSize;
      this.render();
    });
    tools.createEl("button", { text: "Refresh Zotero", attr: { type: "button" } }).addEventListener("click", () => void this.load());
    tools.createEl("button", { text: "\u5168\u9009\u5F53\u524D\u7B5B\u9009", attr: { type: "button" } }).addEventListener("click", () => {
      this.filteredItems().forEach((item) => this.selectedKeys.add(item.itemKey));
      this.render();
    });
    tools.createEl("button", { text: "\u53D6\u6D88\u5168\u9009", attr: { type: "button" } }).addEventListener("click", () => {
      this.selectedKeys.clear();
      this.render();
    });
    this.renderStatusFilters();
    const list = this.contentEl.createDiv({ cls: "cow-zotero-list" });
    if (this.loading) {
      list.createDiv({ cls: "cow-empty-state", text: "\u6B63\u5728\u8BFB\u53D6 Zotero..." });
      this.renderSelectionSummary();
      this.renderFooter();
      return;
    }
    if (!this.connected) {
      this.renderConnectionHelp(list);
      this.renderSelectionSummary();
      this.renderFooter();
      return;
    }
    if (this.error) {
      this.renderErrorState(list);
      this.renderSelectionSummary();
      this.renderFooter();
      return;
    }
    const items = this.filteredItems();
    if (this.items.length === 0) {
      list.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u8BBA\u6587\u3002" });
      this.renderSelectionSummary();
      this.renderFooter();
      return;
    }
    if (items.length === 0) {
      list.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u7B26\u5408\u6761\u4EF6\u7684\u8BBA\u6587\u3002" });
      this.renderSelectionSummary();
      this.renderFooter();
      return;
    }
    items.slice(0, this.visibleLimit).forEach((item) => this.renderCandidate(list, item));
    if (items.length > this.visibleLimit) {
      const more = list.createEl("button", { cls: "cow-zotero-load-more", text: `\u52A0\u8F7D\u66F4\u591A\uFF08${this.visibleLimit} / ${items.length}\uFF09`, attr: { type: "button" } });
      more.addEventListener("click", () => {
        this.visibleLimit += this.pageSize;
        this.render();
      });
    }
    this.renderSelectionSummary();
    this.renderFooter();
  }
  renderConnectionStatus(container) {
    const status = container.createDiv({ cls: this.connected ? "cow-zotero-connection is-connected" : "cow-zotero-connection is-disconnected" });
    status.createSpan({ text: this.connected ? "\u25CF Zotero \u5DF2\u8FDE\u63A5" : "\u25CB Zotero \u672A\u8FDE\u63A5" });
  }
  renderConnectionHelp(container) {
    const empty = container.createDiv({ cls: "cow-empty-state cow-zotero-help" });
    empty.createEl("strong", { text: this.error || "\u65E0\u6CD5\u8FDE\u63A5 Zotero" });
    empty.createSpan({ text: this.connectionHelpText() });
    this.renderDiagnosticDetails(empty);
    empty.createEl("button", { text: "\u91CD\u65B0\u8FDE\u63A5", attr: { type: "button" } }).addEventListener("click", () => void this.load());
  }
  renderErrorState(container) {
    const empty = container.createDiv({ cls: "cow-empty-state cow-zotero-help" });
    empty.createEl("strong", { text: this.error });
    this.renderDiagnosticDetails(empty);
    empty.createEl("button", { text: "\u91CD\u65B0\u8FDE\u63A5", attr: { type: "button" } }).addEventListener("click", () => void this.load());
  }
  renderDiagnosticDetails(container) {
    if (!this.diagnostic) return;
    const details = container.createEl("details", { cls: "cow-zotero-diagnostics" });
    details.createEl("summary", { text: "\u67E5\u770B\u9519\u8BEF\u8BE6\u60C5" });
    details.createDiv({ text: `Request: ${this.diagnostic.url}` });
    if (this.diagnostic.status) details.createDiv({ text: `Status: ${this.diagnostic.status}` });
    details.createDiv({ text: `Error: ${this.diagnostic.message}` });
  }
  connectionHelpText() {
    var _a;
    if (((_a = this.diagnostic) == null ? void 0 : _a.kind) === "forbidden") {
      return "Zotero \u62D2\u7EDD\u4E86\u672C\u5730\u8BF7\u6C42\uFF0C\u8BF7\u68C0\u67E5 Zotero \u8BBE\u7F6E\u4E2D\u7684 Allow other applications on this computer to communicate with Zotero\u3002";
    }
    return "\u8BF7\u786E\u8BA4 Zotero Desktop \u6B63\u5728\u8FD0\u884C\uFF0C\u672C\u5730 API \u5730\u5740\u4E3A 127.0.0.1:23119\uFF0C\u5931\u8D25\u65F6\u4F1A\u81EA\u52A8\u5C1D\u8BD5 localhost\u3002";
  }
  renderStatusFilters() {
    const filters = this.contentEl.createDiv({ cls: "cow-zotero-status-filters" });
    const counts = this.statusCounts();
    [
      ["all", `\u5168\u90E8 ${this.items.length}`],
      ["new", `\u672A\u5BFC\u5165 ${counts.new}`],
      ["imported", `\u5DF2\u5BFC\u5165 ${counts.imported}`],
      ["update-available", `\u53EF\u66F4\u65B0 ${counts["update-available"]}`]
    ].forEach(([status, label]) => {
      const button = filters.createEl("button", { text: label, cls: this.statusFilter === status ? "is-active" : "", attr: { type: "button" } });
      button.addEventListener("click", () => {
        this.statusFilter = status;
        this.visibleLimit = this.pageSize;
        this.render();
      });
    });
  }
  renderCandidate(container, item) {
    const status = this.getImportStatus(item);
    const row = container.createEl("label", { cls: `cow-zotero-item is-${status} ${this.selectedKeys.has(item.itemKey) ? "is-selected" : ""}` });
    const checkbox = row.createEl("input", { attr: { type: "checkbox" } });
    checkbox.checked = this.selectedKeys.has(item.itemKey);
    checkbox.addEventListener("change", () => {
      if (checkbox.checked) this.selectedKeys.add(item.itemKey);
      else this.selectedKeys.delete(item.itemKey);
      row.toggleClass("is-selected", checkbox.checked);
      this.renderSelectionSummary();
    });
    const body = row.createDiv({ cls: "cow-paper-body" });
    body.createEl("strong", { text: item.title });
    body.createSpan({ text: [item.venue, item.year].filter(Boolean).join(" \xB7 ") || "\u65E0 Venue / Year \u4FE1\u606F" });
    if (item.paperUrl) body.createSpan({ cls: "cow-zotero-url-hint", text: "\u2197 \u6709\u94FE\u63A5" });
    const badge = row.createDiv({ cls: `cow-zotero-status-badge is-${status}` });
    badge.createSpan({ text: statusLabel2(status) });
  }
  filteredItems() {
    const query = this.query.trim().toLowerCase();
    return this.items.filter((item) => {
      const status = this.getImportStatus(item);
      if (this.statusFilter !== "all" && status !== this.statusFilter) return false;
      if (!query) return true;
      return [
        item.title,
        item.venue,
        item.year
      ].filter(Boolean).join(" ").toLowerCase().includes(query);
    });
  }
  statusCounts() {
    return this.items.reduce((counts, item) => {
      counts[this.getImportStatus(item)] += 1;
      return counts;
    }, { new: 0, imported: 0, "update-available": 0 });
  }
  renderFooter() {
    const footer = this.contentEl.createDiv({ cls: "cow-zotero-footer" });
    const actions = footer.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    const sync = actions.createEl("button", { text: "\u5BFC\u5165 / \u66F4\u65B0\u9009\u4E2D", cls: "mod-cta cow-zotero-sync-button", attr: { type: "button" } });
    sync.disabled = this.loading || !this.connected || this.selectedKeys.size === 0;
    sync.addEventListener("click", () => void this.syncSelected());
  }
  renderSelectionSummary() {
    const summary = this.selectedSummary();
    const text = `\u5DF2\u9009\u62E9 ${summary.total} \u7BC7 \xB7 \u65B0\u589E ${summary.new} \xB7 \u66F4\u65B0 ${summary.update} \xB7 \u672A\u53D8\u5316 ${summary.unchanged}`;
    const sync = this.contentEl.querySelector(".cow-zotero-sync-button");
    if (sync instanceof HTMLButtonElement) sync.disabled = this.loading || !this.connected || this.selectedKeys.size === 0;
    const existing = this.contentEl.querySelector(".cow-zotero-selection-summary");
    if (existing instanceof HTMLElement) {
      existing.setText(text);
      return;
    }
    this.contentEl.createDiv({ cls: "cow-zotero-selection-summary", text });
  }
  selectedSummary() {
    return this.items.filter((item) => this.selectedKeys.has(item.itemKey)).reduce((summary, item) => {
      const status = this.getImportStatus(item);
      summary.total += 1;
      if (status === "new") summary.new += 1;
      else if (status === "update-available") summary.update += 1;
      else summary.unchanged += 1;
      return summary;
    }, { total: 0, new: 0, update: 0, unchanged: 0 });
  }
  async syncSelected() {
    const selected = this.items.filter((item) => this.selectedKeys.has(item.itemKey));
    if (selected.length === 0) {
      new import_obsidian70.Notice("\u8BF7\u9009\u62E9\u8981\u5BFC\u5165\u6216\u66F4\u65B0\u7684 Zotero \u6761\u76EE\u3002");
      return;
    }
    const service = new PaperZoteroSyncService(this.store);
    const result = await service.importOrUpdateMany(selected);
    new import_obsidian70.Notice(`\u540C\u6B65\u5B8C\u6210\uFF1A\u65B0\u589E ${result.created}\uFF0C\u66F4\u65B0 ${result.updated}\uFF0C\u672A\u53D8\u5316 ${result.unchanged}\uFF0C\u5931\u8D25 ${result.failed}\u3002`);
    this.onDone();
    this.selectedKeys.clear();
    this.render();
  }
  getImportStatus(item) {
    var _a, _b;
    const paper = this.store.getResearchPapers().find((local) => local.zoteroItemKey === item.itemKey);
    if (!paper) return "new";
    const localVenue = normalizeCompare(venueName(this.store, paper) || paper.venue);
    const remoteVenue = normalizeCompare(item.venue);
    const sameTitle = normalizeCompare(paper.title) === normalizeCompare(item.title);
    const sameVenue2 = localVenue === remoteVenue;
    const sameYear = ((_a = paper.year) != null ? _a : void 0) === ((_b = item.year) != null ? _b : void 0);
    const sameUrl = Boolean(paper.paperUrl) || !item.paperUrl;
    return sameTitle && sameVenue2 && sameYear && sameUrl ? "imported" : "update-available";
  }
};
var PaperFieldManagerModal = class extends import_obsidian70.Modal {
  constructor(app, store, onDone) {
    super(app);
    this.store = store;
    this.onDone = onDone;
    this.tab = "status";
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-paper-manager-modal",
      width: "min(1050px, 92vw)",
      height: "min(720px, 86vh)",
      maxWidth: "96vw",
      maxHeight: "94vh",
      minWidth: "min(560px, 92vw)",
      minHeight: "min(420px, 82vh)"
    });
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-paper-modal");
    this.contentEl.createEl("h2", { text: "\u8BBA\u6587\u5B57\u6BB5\u7BA1\u7406" });
    const tabs = this.contentEl.createDiv({ cls: "cow-book-tabs" });
    [
      ["status", "\u9605\u8BFB\u72B6\u6001"],
      ["venue", "\u4F1A\u8BAE / \u671F\u520A"],
      ["tag", "\u6807\u7B7E"]
    ].forEach(([id, label]) => {
      const button = tabs.createEl("button", { text: label, cls: this.tab === id ? "is-active" : "", attr: { type: "button" } });
      button.addEventListener("click", () => {
        this.tab = id;
        this.render();
      });
    });
    if (this.tab === "status") this.renderStatuses();
    if (this.tab === "venue") this.renderVenues();
    if (this.tab === "tag") this.renderTags();
  }
  renderStatuses() {
    const list = this.contentEl.createDiv({ cls: "cow-paper-field-list" });
    this.store.getPaperStatuses().forEach((status) => this.renderFieldRow(list, status.name, status.color, status.order, () => this.openFieldEditor("status", status), () => void this.deleteStatus(status)));
    this.renderAddField("\u65B0\u589E\u72B6\u6001", () => this.openFieldEditor("status"));
  }
  renderVenues() {
    const list = this.contentEl.createDiv({ cls: "cow-paper-field-list" });
    this.store.getPaperVenues().forEach((venue) => this.renderFieldRow(list, `${venue.name} \xB7 ${venue.type}`, venue.color, void 0, () => this.openFieldEditor("venue", venue), () => void this.deleteVenue(venue)));
    this.renderAddField("\u65B0\u589E\u4F1A\u8BAE / \u671F\u520A", () => this.openFieldEditor("venue"));
  }
  renderTags() {
    const list = this.contentEl.createDiv({ cls: "cow-paper-field-list" });
    this.store.getPaperTags().forEach((tag) => this.renderFieldRow(list, tag.name, tag.color, void 0, () => this.openFieldEditor("tag", tag), () => void this.deleteTag(tag)));
    this.renderAddField("\u65B0\u589E\u6807\u7B7E", () => this.openFieldEditor("tag"));
  }
  renderFieldRow(container, label, color, order, onEdit, onDelete) {
    const row = container.createDiv({ cls: "cow-paper-field-row" });
    row.createSpan({ cls: "cow-paper-color-dot", attr: { style: `background: ${color}` } });
    const body = row.createDiv({ cls: "cow-paper-body" });
    body.createEl("strong", { text: label });
    if (order !== void 0) body.createSpan({ text: `\u6392\u5E8F ${order}` });
    const actions = row.createDiv({ cls: "cow-list-item-actions" });
    iconButton(actions, "pencil", "\u7F16\u8F91", onEdit);
    iconButton(actions, "trash-2", "\u5220\u9664", onDelete);
  }
  renderAddField(label, onClick) {
    const add = this.contentEl.createEl("button", { cls: "cow-bottom-add", attr: { type: "button" } });
    (0, import_obsidian70.setIcon)(add.createSpan(), "plus");
    add.createSpan({ text: label });
    add.addEventListener("click", onClick);
  }
  openFieldEditor(kind, field) {
    new PaperFieldEditModal(this.app, this.store, kind, () => {
      this.onDone();
      this.render();
    }, field).open();
  }
  async deleteStatus(status) {
    var _a;
    const used = this.store.getResearchPapers().some((paper) => paper.statusId === status.id);
    let migrateTo;
    if (used) {
      migrateTo = (_a = this.store.getPaperStatuses().find((item) => item.id !== status.id)) == null ? void 0 : _a.id;
      if (!migrateTo || !confirm(`\u72B6\u6001\u201C${status.name}\u201D\u4ECD\u88AB\u8BBA\u6587\u4F7F\u7528\uFF0C\u5220\u9664\u540E\u4F1A\u8FC1\u79FB\u5230\u5176\u5B83\u72B6\u6001\u3002\u7EE7\u7EED\uFF1F`)) return;
    }
    await this.store.deletePaperStatus(status.id, migrateTo);
    this.onDone();
    this.render();
  }
  async deleteVenue(venue) {
    var _a;
    const used = this.store.getResearchPapers().some((paper) => paper.venueId === venue.id);
    let migrateTo;
    if (used) {
      migrateTo = (_a = this.store.getPaperVenues().find((item) => item.id !== venue.id)) == null ? void 0 : _a.id;
      if (!migrateTo || !confirm(`\u4F1A\u8BAE / \u671F\u520A\u201C${venue.name}\u201D\u4ECD\u88AB\u8BBA\u6587\u4F7F\u7528\uFF0C\u5220\u9664\u540E\u4F1A\u8FC1\u79FB\u5230\u5176\u5B83\u6761\u76EE\u3002\u7EE7\u7EED\uFF1F`)) return;
    }
    await this.store.deletePaperVenue(venue.id, migrateTo);
    this.onDone();
    this.render();
  }
  async deleteTag(tag) {
    if (!confirm(`\u5220\u9664\u6807\u7B7E\u201C${tag.name}\u201D\uFF1F\u4F1A\u4ECE\u5BF9\u5E94\u8BBA\u6587\u4E2D\u79FB\u9664\u8BE5\u6807\u7B7E\u3002`)) return;
    await this.store.deletePaperTag(tag.id);
    this.onDone();
    this.render();
  }
};
var PaperFieldEditModal = class extends import_obsidian70.Modal {
  constructor(app, store, kind, onDone, field) {
    var _a, _b;
    super(app);
    this.store = store;
    this.kind = kind;
    this.onDone = onDone;
    this.name = (_a = field == null ? void 0 : field.name) != null ? _a : "";
    this.color = (_b = field == null ? void 0 : field.color) != null ? _b : "#f8a8c4";
    this.order = "order" in (field != null ? field : {}) ? field.order : this.store.getPaperStatuses().length * 10 + 10;
    this.venueType = "type" in (field != null ? field : {}) ? field.type : "other";
    this.fieldId = field == null ? void 0 : field.id;
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-paper-edit-modal",
      width: "min(560px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "86vh",
      minWidth: "min(420px, 90vw)",
      minHeight: "min(300px, 74vh)"
    });
    this.render();
  }
  render() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-paper-modal");
    this.contentEl.createEl("h2", { text: this.fieldId ? "\u7F16\u8F91\u5B57\u6BB5" : "\u65B0\u589E\u5B57\u6BB5" });
    const form = this.contentEl.createDiv({ cls: "cow-paper-form" });
    inputField2(form, "\u540D\u79F0", this.name, (value) => this.name = value);
    inputField2(form, "\u989C\u8272", this.color, (value) => this.color = value, "color");
    if (this.kind === "status") inputField2(form, "\u6392\u5E8F", String(this.order), (value) => this.order = Number(value) || 0, "number");
    if (this.kind === "venue") {
      selectField2(form, "\u7C7B\u578B", this.venueType, [
        { value: "conference", label: "conference" },
        { value: "journal", label: "journal" },
        { value: "other", label: "other" }
      ], (value) => this.venueType = value);
    }
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u4FDD\u5B58", cls: "mod-cta", attr: { type: "button" } }).addEventListener("click", () => void this.save());
  }
  async save() {
    var _a;
    if (!this.name.trim()) {
      new import_obsidian70.Notice("\u8BF7\u8F93\u5165\u540D\u79F0\u3002");
      return;
    }
    const id = (_a = this.fieldId) != null ? _a : `${this.kind}-${Date.now()}`;
    if (this.kind === "status") {
      if (this.fieldId) await this.store.updatePaperStatus(id, { name: this.name.trim(), color: this.color, order: this.order });
      else await this.store.addPaperStatus({ id, name: this.name.trim(), color: this.color, order: this.order });
    }
    if (this.kind === "venue") {
      if (this.fieldId) await this.store.updatePaperVenue(id, { name: this.name.trim(), color: this.color, type: this.venueType });
      else await this.store.addPaperVenue({ id, name: this.name.trim(), color: this.color, type: this.venueType });
    }
    if (this.kind === "tag") {
      if (this.fieldId) await this.store.updatePaperTag(id, { name: this.name.trim(), color: this.color });
      else await this.store.addPaperTag({ id, name: this.name.trim(), color: this.color });
    }
    this.onDone();
    this.close();
  }
};
function paperMetaText(store, paper) {
  var _a;
  return `${statusName(store, paper)} \xB7 ${venueName(store, paper)} \xB7 ${(_a = paper.year) != null ? _a : ""}`;
}
function statusName(store, paper) {
  var _a, _b, _c;
  return (_c = (_b = (_a = store.getPaperStatuses().find((status) => status.id === paper.statusId)) == null ? void 0 : _a.name) != null ? _b : paper.status) != null ? _c : "\u672A\u8BFB";
}
function venueName(store, paper) {
  var _a, _b, _c;
  return (_c = (_b = (_a = store.getPaperVenues().find((venue) => venue.id === paper.venueId)) == null ? void 0 : _a.name) != null ? _b : paper.venue) != null ? _c : "\u672A\u5206\u7EC4";
}
function tagNames(store, paper) {
  var _a;
  return ((_a = paper.tagIds) != null ? _a : []).map((id) => {
    var _a2;
    return (_a2 = store.getPaperTags().find((tag) => tag.id === id)) == null ? void 0 : _a2.name;
  }).filter(Boolean);
}
function inputField2(container, label, value, onInput, type = "text") {
  const row = container.createDiv({ cls: "cow-book-form-row" });
  row.createEl("label", { text: label });
  const input = row.createEl("input", { attr: { type, value } });
  input.addEventListener("input", () => onInput(input.value));
  return input;
}
function paperUrlField(container, value, onInput) {
  const row = container.createDiv({ cls: "cow-book-form-row cow-paper-url-row" });
  row.createEl("label", { text: "\u8BBA\u6587\u94FE\u63A5" });
  const controls = row.createDiv({ cls: "cow-paper-url-controls" });
  const input = controls.createEl("input", { attr: { type: "url", value, placeholder: "https://..." } });
  input.addEventListener("input", () => onInput(input.value));
  const button = controls.createEl("button", { attr: { type: "button" } });
  (0, import_obsidian70.setIcon)(button.createSpan(), "external-link");
  button.createSpan({ text: "\u6253\u5F00" });
  button.toggleAttribute("disabled", !value.trim());
  input.addEventListener("input", () => button.toggleAttribute("disabled", !input.value.trim()));
  button.addEventListener("click", () => openPaperUrl(input.value));
}
function openPaperUrl(value) {
  const url = value == null ? void 0 : value.trim();
  if (!url) return;
  if (!/^https?:\/\//i.test(url)) {
    new import_obsidian70.Notice("\u8BBA\u6587\u94FE\u63A5\u683C\u5F0F\u65E0\u6548\u3002");
    return;
  }
  window.open(url);
}
function fileName6(path) {
  var _a;
  return ((_a = path.split(/[\\/]/).pop()) == null ? void 0 : _a.replace(/\.md$/i, "")) || path;
}
function dateField2(container, label, value, onChange) {
  const row = container.createDiv({ cls: "cow-book-form-row is-picker" });
  row.createEl("label", { text: label });
  const input = row.createEl("input", { attr: { type: "date", value } });
  row.addEventListener("click", () => {
    var _a;
    input.focus();
    try {
      (_a = input.showPicker) == null ? void 0 : _a.call(input);
    } catch (e) {
      input.focus();
    }
  });
  input.addEventListener("change", () => onChange(input.value));
}
function selectField2(container, label, value, options, onChange) {
  const row = container.createDiv({ cls: "cow-book-form-row" });
  row.createEl("label", { text: label });
  const select = row.createEl("select");
  options.forEach((option) => select.createEl("option", { value: option.value, text: option.label }));
  select.value = value;
  select.addEventListener("change", () => onChange(select.value));
}
function progressField(container, value, onChange) {
  const row = container.createDiv({ cls: "cow-paper-progress-editor" });
  row.createEl("label", { text: "\u9605\u8BFB\u8FDB\u5EA6" });
  const range = row.createEl("input", { attr: { type: "range", min: "0", max: "100", step: "1", value: String(value) } });
  const number = row.createEl("input", { attr: { type: "number", min: "0", max: "100", step: "1", value: String(value) } });
  const sync = (next) => {
    const normalized = Math.max(0, Math.min(100, next));
    range.value = String(normalized);
    number.value = String(normalized);
    onChange(normalized);
  };
  range.addEventListener("input", () => sync(Number(range.value) || 0));
  number.addEventListener("input", () => sync(Number(number.value) || 0));
}
function tagField(container, tags, selected, onChange) {
  const row = container.createDiv({ cls: "cow-book-form-row cow-paper-tag-picker" });
  row.createEl("label", { text: "\u6807\u7B7E" });
  const list = row.createDiv({ cls: "cow-paper-tag-options" });
  const selectedSet = new Set(selected);
  tags.forEach((tag) => {
    const button = list.createEl("button", { cls: selectedSet.has(tag.id) ? "is-active" : "", attr: { type: "button", style: `--paper-color: ${tag.color}` } });
    button.setText(`${selectedSet.has(tag.id) ? "\u2713 " : ""}${tag.name}`);
    button.addEventListener("click", () => {
      if (selectedSet.has(tag.id)) selectedSet.delete(tag.id);
      else selectedSet.add(tag.id);
      onChange([...selectedSet]);
      button.toggleClass("is-active", selectedSet.has(tag.id));
      button.setText(`${selectedSet.has(tag.id) ? "\u2713 " : ""}${tag.name}`);
    });
  });
}
function iconButton(container, icon, label, onClick) {
  const button = container.createEl("button", { attr: { type: "button", "aria-label": label } });
  (0, import_obsidian70.setIcon)(button, icon);
  button.addEventListener("click", onClick);
}
async function loadZoteroCandidates(app, store, notify = true) {
  var _a;
  const path = (_a = store.getData().userSettings.zoteroJsonPath) != null ? _a : "";
  const service = new ZoteroService(app);
  const commands = service.getRegisteredZoteroCommands();
  if (!path) {
    if (notify) {
      new import_obsidian70.Notice(commands.length > 0 ? "\u68C0\u6D4B\u5230 Zotero Integration \u547D\u4EE4\uFF0C\u4F46\u6CA1\u6709\u516C\u5F00\u6761\u76EE\u8BFB\u53D6\u63A5\u53E3\u3002\u8BF7\u914D\u7F6E Better BibTeX JSON \u8DEF\u5F84\u3002" : "\u672A\u68C0\u6D4B\u5230\u53EF\u7528\u7684 Zotero \u6570\u636E\u6E90\u3002\u8BF7\u914D\u7F6E Better BibTeX JSON \u8DEF\u5F84\u3002");
    }
    return [];
  }
  try {
    const candidates = await service.loadBetterBibtexJson(path);
    if (candidates.length === 0 && notify) new import_obsidian70.Notice("Better BibTeX JSON \u4E2D\u6CA1\u6709\u53EF\u5BFC\u5165\u6761\u76EE\u3002");
    return candidates;
  } catch (e) {
    if (notify) new import_obsidian70.Notice("\u8BFB\u53D6 Better BibTeX JSON \u5931\u8D25\uFF0C\u8BF7\u68C0\u67E5 JSON \u8DEF\u5F84\u548C\u683C\u5F0F\u3002");
    return [];
  }
}
function candidateMatchesPaper(candidate, paper) {
  var _a;
  if (candidate.zoteroItemKey && paper.zoteroItemKey === candidate.zoteroItemKey) return true;
  if (candidate.citekey && paper.citekey === candidate.citekey) return true;
  if (candidate.doi && ((_a = paper.doi) == null ? void 0 : _a.toLowerCase()) === candidate.doi.toLowerCase()) return true;
  if (candidate.paperUrl && paper.paperUrl === candidate.paperUrl) return true;
  return false;
}
function statusLabel2(status) {
  if (status === "imported") return "\u2713 \u5DF2\u5BFC\u5165";
  if (status === "update-available") return "\u21BB \u53EF\u66F4\u65B0";
  return "\u672A\u5BFC\u5165";
}
function normalizeCompare(value) {
  return String(value != null ? value : "").trim().toLowerCase();
}
function findZoteroNoteCommand(app) {
  var _a, _b;
  const commands = (_b = (_a = app.commands) == null ? void 0 : _a.commands) != null ? _b : {};
  return Object.entries(commands).map(([id, command]) => {
    var _a2;
    return { id, name: (_a2 = command.name) != null ? _a2 : id };
  }).find((command) => {
    const text = `${command.id} ${command.name}`.toLowerCase();
    return text.includes("zotero") && (text.includes("note") || text.includes("import") || text.includes("citekey"));
  });
}
async function executeCommand(app, commandId) {
  var _a;
  const commands = app.commands;
  (_a = commands == null ? void 0 : commands.executeCommandById) == null ? void 0 : _a.call(commands, commandId);
}
function findLikelyZoteroNote(app, paper) {
  const needles = [paper.citekey, paper.zoteroItemKey, paper.title].filter(Boolean).map((value) => String(value).toLowerCase());
  if (needles.length === 0) return void 0;
  return app.vault.getMarkdownFiles().sort((a, b) => b.stat.mtime - a.stat.mtime).find((file) => {
    const text = `${file.basename} ${file.path}`.toLowerCase();
    return needles.some((needle) => text.includes(needle));
  });
}

// src/components/research/PaperFieldManagerSection.ts
var PaperFieldManagerSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const shell = container.createDiv({ cls: "cow-paper-field-summary" });
    this.renderGroup(shell, "\u9605\u8BFB\u72B6\u6001", this.store.getPaperStatuses().map((item) => ({ name: item.name, color: item.color })));
    this.renderGroup(shell, "\u4F1A\u8BAE / \u671F\u520A", this.store.getPaperVenues().map((item) => ({ name: item.name, color: item.color })));
    this.renderGroup(shell, "\u6807\u7B7E", this.store.getPaperTags().map((item) => ({ name: item.name, color: item.color })));
    const button = shell.createEl("button", { cls: "cow-bottom-add", attr: { type: "button" } });
    (0, import_obsidian71.setIcon)(button.createSpan(), "pencil");
    button.createSpan({ text: "\u7F16\u8F91\u5B57\u6BB5" });
    button.addEventListener("click", () => new PaperFieldManagerModal(this.app, this.store, this.onDataChanged).open());
  }
  renderGroup(container, title, items) {
    const group = container.createDiv({ cls: "cow-paper-field-group" });
    group.createEl("strong", { text: title });
    const chips = group.createDiv({ cls: "cow-paper-tags" });
    items.forEach((item) => chips.createSpan({ text: item.name, attr: { style: `--paper-color: ${item.color}` } }));
  }
};

// src/components/research/PaperQueueSection.ts
var import_obsidian72 = require("obsidian");
var PaperQueueSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
    this.filters = { tagIds: [] };
  }
  render(container) {
    const host = container.createDiv({ cls: "cow-paper-queue" });
    this.renderFilters(host);
    const list = host.createDiv({ cls: "cow-paper-list" });
    const papers = this.filterPapers(this.store.getResearchPapers());
    if (papers.length === 0) {
      list.createDiv({ cls: "cow-empty-state", text: "\u6CA1\u6709\u5339\u914D\u5F53\u524D\u7B5B\u9009\u6761\u4EF6\u7684\u8BBA\u6587\u3002" });
      return;
    }
    papers.forEach((paper) => this.renderPaperCard(list, paper));
  }
  renderFilters(container) {
    var _a, _b, _c;
    const filters = container.createDiv({ cls: "cow-paper-filter-panel" });
    this.renderSelect(filters, "\u9605\u8BFB\u72B6\u6001", (_a = this.filters.statusId) != null ? _a : "", [{ value: "", label: "\u5168\u90E8\u72B6\u6001" }, ...this.store.getPaperStatuses().map((item) => ({ value: item.id, label: item.name }))], (value) => {
      this.filters.statusId = value || void 0;
      container.empty();
      this.render(container);
    });
    this.renderSelect(filters, "\u4F1A\u8BAE / \u671F\u520A", (_b = this.filters.venueId) != null ? _b : "", [{ value: "", label: "\u5168\u90E8\u4F1A\u8BAE / \u671F\u520A" }, ...this.store.getPaperVenues().map((item) => ({ value: item.id, label: item.name }))], (value) => {
      this.filters.venueId = value || void 0;
      container.empty();
      this.render(container);
    });
    this.renderSelect(filters, "\u7814\u7A76\u9879\u76EE", (_c = this.filters.researchProjectId) != null ? _c : "", [{ value: "", label: "\u5168\u90E8\u7814\u7A76\u9879\u76EE" }, ...this.store.getResearchProjects().map((item) => ({ value: item.id, label: item.title }))], (value) => {
      this.filters.researchProjectId = value || void 0;
      container.empty();
      this.render(container);
    });
    const tagWrap = filters.createDiv({ cls: "cow-paper-filter-tags" });
    this.store.getPaperTags().forEach((tag) => {
      const active = this.filters.tagIds.includes(tag.id);
      const button = tagWrap.createEl("button", { text: tag.name, cls: active ? "is-active" : "", attr: { type: "button", style: `--paper-color: ${tag.color}` } });
      button.addEventListener("click", () => {
        this.filters.tagIds = active ? this.filters.tagIds.filter((id) => id !== tag.id) : [...this.filters.tagIds, tag.id];
        container.empty();
        this.render(container);
      });
    });
    this.renderChips(container);
  }
  renderChips(container) {
    var _a, _b, _c, _d, _e, _f;
    const chips = container.createDiv({ cls: "cow-paper-filter-chips" });
    const addChip = (label, onRemove) => {
      const chip = chips.createEl("button", { text: `${label} \xD7`, attr: { type: "button" } });
      chip.addEventListener("click", () => {
        onRemove();
        container.empty();
        this.render(container);
      });
    };
    if (this.filters.statusId) addChip((_b = (_a = this.store.getPaperStatuses().find((item) => item.id === this.filters.statusId)) == null ? void 0 : _a.name) != null ? _b : "\u72B6\u6001", () => this.filters.statusId = void 0);
    if (this.filters.venueId) addChip((_d = (_c = this.store.getPaperVenues().find((item) => item.id === this.filters.venueId)) == null ? void 0 : _c.name) != null ? _d : "\u4F1A\u8BAE", () => this.filters.venueId = void 0);
    if (this.filters.researchProjectId) addChip((_f = (_e = this.store.getResearchProjects().find((item) => item.id === this.filters.researchProjectId)) == null ? void 0 : _e.title) != null ? _f : "\u9879\u76EE", () => this.filters.researchProjectId = void 0);
    this.filters.tagIds.forEach((id) => {
      var _a2, _b2;
      return addChip((_b2 = (_a2 = this.store.getPaperTags().find((tag) => tag.id === id)) == null ? void 0 : _a2.name) != null ? _b2 : "\u6807\u7B7E", () => this.filters.tagIds = this.filters.tagIds.filter((tagId) => tagId !== id));
    });
    if (this.filters.statusId || this.filters.venueId || this.filters.researchProjectId || this.filters.tagIds.length > 0) {
      addChip("\u6E05\u9664\u7B5B\u9009", () => this.filters = { tagIds: [] });
    }
  }
  renderPaperCard(container, paper) {
    var _a, _b, _c, _d;
    const card = container.createDiv({ cls: "cow-paper-card cow-paper-reading-card" });
    const header = card.createDiv({ cls: "cow-paper-card-header" });
    const titleContainer = header.createDiv({ cls: "cow-paper-title-container" });
    const title = titleContainer.createEl("button", { cls: "cow-paper-title-button is-ellipsis", text: paper.title, attr: { type: "button", title: paper.title } });
    (0, import_obsidian72.setTooltip)(title, paper.title);
    title.addEventListener("click", () => new PaperDetailModal(this.app, this.store, paper, this.onDataChanged).open());
    const actions = header.createDiv({ cls: "cow-list-item-actions cow-paper-actions" });
    iconButton2(actions, "pencil", "\u7F16\u8F91\u8BBA\u6587", () => new PaperEditModal(this.app, this.store, this.onDataChanged, paper).open());
    iconButton2(actions, "trash-2", "\u5220\u9664\u8BBA\u6587", () => new DeletePaperReadingModal(this.app, this.store, paper, this.onDataChanged).open());
    iconButton2(actions, "external-link", "\u6253\u5F00\u8BBA\u6587\u94FE\u63A5", () => void this.openPaperUrl(paper));
    iconButton2(actions, "notebook-tabs", "\u6253\u5F00\u7B14\u8BB0", () => void this.openNote(paper.notePath));
    const body = card.createDiv({ cls: "cow-paper-body" });
    const meta = body.createDiv({ cls: "cow-paper-meta-row" });
    meta.createSpan({ cls: "cow-status is-blue", text: statusName(this.store, paper) });
    const project = this.store.getResearchProjects().find((item) => item.id === paper.researchProjectId);
    meta.createSpan({ text: `${venueName(this.store, paper)} \xB7 ${(_a = paper.year) != null ? _a : ""} \xB7 ${(_b = project == null ? void 0 : project.title) != null ? _b : "\u672A\u5173\u8054\u9879\u76EE"}` });
    const progress = body.createDiv({ cls: "cow-paper-progress-row" });
    progress.createDiv({ cls: "cow-month-progress-track" }).createDiv({ cls: "cow-month-progress-fill is-blue", attr: { style: `width: ${paper.readingProgress}%` } });
    progress.createSpan({ text: `${paper.readingProgress}%` });
    const tags = body.createDiv({ cls: "cow-paper-tags" });
    tagNames(this.store, paper).forEach((tag) => tags.createSpan({ text: tag }));
    body.createDiv({ cls: "cow-meta-line", text: `${(_c = paper.readingStartDate) != null ? _c : "-"} \u2192 ${(_d = paper.readingEndDate) != null ? _d : "-"}` });
  }
  filterPapers(papers) {
    return papers.filter((paper) => {
      if (this.filters.statusId && paper.statusId !== this.filters.statusId) return false;
      if (this.filters.venueId && paper.venueId !== this.filters.venueId) return false;
      if (this.filters.researchProjectId && paper.researchProjectId !== this.filters.researchProjectId) return false;
      if (this.filters.tagIds.some((tagId) => {
        var _a;
        return !((_a = paper.tagIds) != null ? _a : []).includes(tagId);
      })) return false;
      return true;
    });
  }
  renderSelect(container, label, value, options, onChange) {
    const wrap = container.createDiv({ cls: "cow-paper-filter-field" });
    wrap.createSpan({ text: label });
    const select = wrap.createEl("select");
    options.forEach((option) => select.createEl("option", { value: option.value, text: option.label }));
    select.value = value;
    select.addEventListener("change", () => onChange(select.value));
  }
  async openNote(notePath) {
    if (!notePath) {
      new import_obsidian72.Notice("\u8FD9\u7BC7\u6587\u732E\u8FD8\u6CA1\u6709\u7ED1\u5B9A\u7B14\u8BB0\u3002");
      return;
    }
    const file = this.app.vault.getFileByPath(notePath);
    if (!file) {
      new import_obsidian72.Notice(`\u6CA1\u6709\u627E\u5230\u7B14\u8BB0\uFF1A${notePath}`);
      return;
    }
    await this.app.workspace.getLeaf(false).openFile(file);
  }
  async openPaperUrl(paper) {
    if (!paper.paperUrl) {
      new import_obsidian72.Notice("\u8FD9\u7BC7\u8BBA\u6587\u8FD8\u6CA1\u6709\u586B\u5199\u94FE\u63A5\u3002");
      return;
    }
    if (!/^https?:\/\//i.test(paper.paperUrl.trim())) {
      new import_obsidian72.Notice("\u8BBA\u6587\u94FE\u63A5\u683C\u5F0F\u65E0\u6548\u3002");
      return;
    }
    window.open(paper.paperUrl);
  }
};
function iconButton2(container, icon, label, onClick) {
  const button = container.createEl("button", { attr: { type: "button", "aria-label": label } });
  (0, import_obsidian72.setIcon)(button, icon);
  button.addEventListener("click", onClick);
}

// src/components/research/ResearchMemoSection.ts
var import_obsidian73 = require("obsidian");
var ResearchMemoSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createEl("ul", { cls: "cow-memo-list" });
    this.store.getResearchMemos().forEach((memo, index) => {
      const item = list.createEl("li");
      const head = item.createDiv({ cls: "cow-list-item-head" });
      head.createSpan({ text: memo });
      const actions = head.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91 Memo" } });
      (0, import_obsidian73.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => openTextModal(this.app, "\u7F16\u8F91\u79D1\u7814 Memo", "Memo", memo, async (value) => {
        await this.store.updateResearchMemo(index, value);
        this.onDataChanged();
      }));
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664 Memo" } });
      (0, import_obsidian73.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        await this.store.deleteResearchMemo(index);
        this.onDataChanged();
      });
    });
  }
};

// src/components/research/ResearchProjectsSection.ts
var import_obsidian75 = require("obsidian");

// src/components/research/ResearchProjectModals.ts
var import_obsidian74 = require("obsidian");
var ResearchProjectDetailModal = class extends import_obsidian74.Modal {
  constructor(app, store, project, onDone) {
    super(app);
    this.store = store;
    this.project = project;
    this.onDone = onDone;
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-research-detail-modal",
      width: "min(1050px, 92vw)",
      height: "min(720px, 86vh)",
      maxWidth: "96vw",
      maxHeight: "94vh",
      minWidth: "min(560px, 92vw)",
      minHeight: "min(420px, 82vh)"
    });
    this.render();
  }
  render() {
    var _a, _b;
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-research-detail-modal");
    const project = (_a = this.store.getResearchProjects().find((item) => item.id === this.project.id)) != null ? _a : this.project;
    const header = this.contentEl.createDiv({ cls: "cow-list-item-head" });
    header.createEl("h2", { text: project.title });
    const edit = header.createEl("button", { cls: "cow-section-add-button", attr: { type: "button" } });
    (0, import_obsidian74.setIcon)(edit.createSpan(), "pencil");
    edit.createSpan({ text: "\u7F16\u8F91" });
    edit.addEventListener("click", () => openResearchProjectModal(this.app, this.store, async (values) => {
      await this.store.updateResearchProject(project.id, values);
      this.onDone();
      this.render();
    }, project));
    const summary = this.contentEl.createDiv({ cls: "cow-paper-detail-grid" });
    [
      ["\u72B6\u6001", project.status],
      ["\u8FDB\u5EA6", `${project.progress}%`],
      ["\u5468\u671F", `${project.startDate} \u2192 ${project.deadline}`],
      ["\u6807\u7B7E", projectTagNames(this.store, project).join(" \xB7 ") || "\u65E0\u6807\u7B7E"]
    ].forEach(([label, value]) => {
      const card = summary.createDiv({ cls: "cow-data-card" });
      card.createEl("strong", { text: label });
      card.createSpan({ text: value });
    });
    const description = this.contentEl.createDiv({ cls: "cow-paper-linked-notes cow-project-detail-description" });
    description.createEl("h3", { text: "\u9879\u76EE\u63CF\u8FF0" });
    description.createEl("p", { text: ((_b = project.description) == null ? void 0 : _b.trim()) || "\u6682\u65E0\u63CF\u8FF0\u3002" });
    const relations = this.contentEl.createDiv({ cls: "cow-research-relations" });
    this.renderPapers(relations, project);
    this.renderPlans(relations, project);
  }
  renderPapers(container, project) {
    const section = container.createDiv({ cls: "cow-paper-linked-notes" });
    section.createEl("h3", { text: "\u5173\u8054\u8BBA\u6587" });
    const papers = this.store.getResearchPapers().filter((paper) => paper.researchProjectId === project.id);
    if (papers.length === 0) {
      section.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u5173\u8054\u8BBA\u6587\u3002" });
      return;
    }
    papers.forEach((paper) => {
      const row = section.createEl("button", { cls: "cow-data-card cow-click-card", attr: { type: "button" } });
      row.createEl("strong", { text: paper.title });
      row.createDiv({ cls: "cow-meta-line", text: paperMetaText(this.store, paper) });
      row.addEventListener("click", () => new PaperDetailModal(this.app, this.store, paper, this.onDone).open());
    });
  }
  renderPlans(container, project) {
    const section = container.createDiv({ cls: "cow-paper-linked-notes" });
    section.createEl("h3", { text: "\u5173\u8054\u5B9E\u9A8C\u8BA1\u5212" });
    const plans = this.store.getExperimentPlans().filter((plan) => plan.researchProjectId === project.id);
    if (plans.length === 0) {
      section.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u5173\u8054\u5B9E\u9A8C\u8BA1\u5212\u3002" });
      return;
    }
    plans.forEach((plan) => {
      const row = section.createEl("button", { cls: "cow-data-card cow-click-card", attr: { type: "button" } });
      row.createEl("strong", { text: plan.title });
      row.createDiv({ cls: "cow-meta-line", text: `${plan.date} \xB7 ${plan.status}` });
      row.addEventListener("click", () => new ExperimentPlanDetailModal(this.app, this.store, plan, this.onDone).open());
    });
  }
};
function projectTagNames(store, project) {
  var _a;
  const names = ((_a = project.tagIds) != null ? _a : []).map((id) => {
    var _a2;
    return (_a2 = store.getPaperTags().find((tag) => tag.id === id)) == null ? void 0 : _a2.name;
  }).filter(Boolean);
  return names.length > 0 ? names : project.tags;
}
var DeleteResearchProjectModal = class extends import_obsidian74.Modal {
  constructor(app, store, project, onDone) {
    super(app);
    this.store = store;
    this.project = project;
    this.onDone = onDone;
  }
  onOpen() {
    applyResizableModal(this, {
      className: "cute-research-detail-modal",
      width: "min(620px, 90vw)",
      maxWidth: "96vw",
      maxHeight: "86vh",
      minWidth: "min(420px, 90vw)",
      minHeight: "min(260px, 70vh)"
    });
    const paperCount = this.store.getResearchPapers().filter((paper) => paper.researchProjectId === this.project.id).length;
    const planCount = this.store.getExperimentPlans().filter((plan) => plan.researchProjectId === this.project.id).length;
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: "\u5220\u9664\u7814\u7A76\u9879\u76EE\uFF1F" });
    this.contentEl.createEl("p", { text: `\u5173\u8054\u8BBA\u6587 ${paperCount} \u7BC7\uFF0C\u5173\u8054\u5B9E\u9A8C\u8BA1\u5212 ${planCount} \u6761\u3002\u5220\u9664\u9879\u76EE\u53EA\u4F1A\u89E3\u9664\u5173\u8054\uFF0C\u4E0D\u4F1A\u5220\u9664\u8BBA\u6587\u3001\u7B14\u8BB0\u6216\u5B9E\u9A8C\u3002` });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u89E3\u9664\u5173\u8054\u5E76\u5220\u9664", cls: "mod-warning", attr: { type: "button" } }).addEventListener("click", () => void this.delete());
  }
  async delete() {
    await this.store.deleteResearchProject(this.project.id);
    this.onDone();
    this.close();
  }
};

// src/components/research/ResearchProjectsSection.ts
var ResearchProjectsSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createDiv({ cls: "cow-data-list" });
    this.store.getResearchProjects().forEach((project) => {
      const row = list.createEl("button", { cls: "cow-data-card cow-click-card", attr: { type: "button" } });
      const head = row.createDiv({ cls: "cow-list-item-head" });
      head.createEl("strong", { text: project.title });
      const actions = head.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91\u9879\u76EE" } });
      (0, import_obsidian75.setIcon)(edit, "pencil");
      edit.addEventListener("click", (event) => {
        event.stopPropagation();
        openResearchProjectModal(this.app, this.store, async (values) => {
          await this.store.updateResearchProject(project.id, values);
          this.onDataChanged();
        }, project);
      });
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664\u9879\u76EE" } });
      (0, import_obsidian75.setIcon)(remove, "trash-2");
      remove.addEventListener("click", (event) => {
        event.stopPropagation();
        new DeleteResearchProjectModal(this.app, this.store, project, this.onDataChanged).open();
      });
      const meta = row.createDiv({ cls: "cow-meta-line" });
      meta.createSpan({ cls: "cow-status is-green", text: project.status });
      meta.createSpan({ text: `${project.startDate} -> ${project.deadline}` });
      const track = row.createDiv({ cls: "cow-month-progress-track" });
      track.createDiv({ cls: "cow-month-progress-fill is-green", attr: { style: `width: ${project.progress}%` } });
      const tags = row.createDiv({ cls: "cow-tag-row" });
      projectTagNames2(this.store, project).forEach((tag) => tags.createSpan({ text: tag }));
      if (project.description) row.createDiv({ cls: "cow-project-description", text: project.description });
      row.addEventListener("click", () => new ResearchProjectDetailModal(this.app, this.store, project, this.onDataChanged).open());
    });
  }
};
function projectTagNames2(store, project) {
  var _a;
  const names = ((_a = project.tagIds) != null ? _a : []).map((id) => {
    var _a2;
    return (_a2 = store.getPaperTags().find((tag) => tag.id === id)) == null ? void 0 : _a2.name;
  }).filter(Boolean);
  return names.length > 0 ? names : project.tags;
}

// src/components/research/ResearchTimelineSection.ts
var import_obsidian76 = require("obsidian");
var ResearchTimelineSection = class {
  constructor(app, store, onDataChanged) {
    this.app = app;
    this.store = store;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const list = container.createDiv({ cls: "cow-timeline" });
    this.store.getResearchDeadlines().forEach((ddl) => {
      const item = list.createDiv({ cls: `cow-timeline-item priority-${ddl.priority}` });
      item.createEl("time", { text: ddl.date });
      item.createEl("strong", { text: ddl.title });
      item.createSpan({ text: ddl.type });
      const actions = item.createDiv({ cls: "cow-list-item-actions" });
      const edit = actions.createEl("button", { attr: { type: "button", "aria-label": "\u7F16\u8F91 DDL" } });
      (0, import_obsidian76.setIcon)(edit, "pencil");
      edit.addEventListener("click", () => openDeadlineModal(this.app, async (values) => {
        await this.store.updateResearchDeadline(ddl.id, values);
        this.onDataChanged();
      }, ddl));
      const remove = actions.createEl("button", { attr: { type: "button", "aria-label": "\u5220\u9664 DDL" } });
      (0, import_obsidian76.setIcon)(remove, "trash-2");
      remove.addEventListener("click", async () => {
        await this.store.deleteResearchDeadline(ddl.id);
        this.onDataChanged();
      });
    });
  }
};

// src/components/DashboardSection.ts
var DashboardSection = class {
  constructor(app, store, section, onRemove, onDataChanged) {
    this.app = app;
    this.store = store;
    this.section = section;
    this.onRemove = onRemove;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    var _a, _b, _c, _d;
    const cardColor = typeof ((_a = this.section.config) == null ? void 0 : _a.cardColor) === "string" ? this.section.config.cardColor : "default";
    const layoutMeta = this.getLayoutMeta();
    const sectionEl = container.createDiv({
      cls: `cow-section cow-section-${(_b = this.section.width) != null ? _b : "md"} cow-section-height-${(_c = this.section.height) != null ? _c : "sm"} cow-section-type-${this.section.type} wb-layout-${layoutMeta.role} wb-span-${layoutMeta.span} cow-card-color-${cardColor}`
    });
    const moduleLayout = this.store.getModuleLayout(this.section.page);
    const sectionLayout = (_d = moduleLayout.sections) == null ? void 0 : _d[this.section.id];
    if (moduleLayout.mode === "custom" && (sectionLayout == null ? void 0 : sectionLayout.colSpan)) {
      sectionEl.style.gridColumn = `span ${sectionLayout.colSpan}`;
    }
    if (moduleLayout.mode === "custom" && (sectionLayout == null ? void 0 : sectionLayout.rowSpan)) {
      sectionEl.style.gridRow = `span ${sectionLayout.rowSpan}`;
    }
    const header = sectionEl.createDiv({ cls: "cow-section-header" });
    const title = header.createDiv({ cls: "cow-section-title" });
    (0, import_obsidian77.setIcon)(title.createSpan(), this.getIcon());
    title.createEl("h3", { text: this.section.title });
    const actions = header.createDiv({ cls: "cow-section-actions" });
    if (getSectionCapabilities(this.section.type).canAdd) {
      const addButton = actions.createEl("button", {
        cls: "cow-section-add-button",
        attr: { type: "button", "aria-label": `\u6DFB\u52A0${this.section.title}\u5185\u5BB9` }
      });
      (0, import_obsidian77.setIcon)(addButton.createSpan(), "plus");
      addButton.createSpan({ text: "\u6DFB\u52A0" });
      addButton.addEventListener("click", () => {
        openAddContentModal(this.app, this.store, this.section, this.onDataChanged);
      });
    }
    if (getSectionCapabilities(this.section.type).canOpenStats) {
      const statsButton = actions.createEl("button", {
        cls: "cow-section-add-button",
        attr: { type: "button", "aria-label": `${this.section.title}\u7EDF\u8BA1` }
      });
      (0, import_obsidian77.setIcon)(statsButton.createSpan(), "bar-chart-3");
      statsButton.createSpan({ text: "\u7EDF\u8BA1" });
      statsButton.addEventListener("click", () => this.openStats());
    }
    this.renderFitnessHeaderActions(actions);
    this.renderFinanceHeaderActions(actions);
    this.renderGoalHeaderActions(actions);
    this.renderReadingHeaderActions(actions);
    this.renderResearchHeaderActions(actions);
    this.renderCheckInHeaderActions(actions);
    const menuButton = actions.createEl("button", {
      cls: "cow-icon-button",
      attr: { type: "button", "aria-label": `${this.section.title}\u64CD\u4F5C\u83DC\u5355` }
    });
    (0, import_obsidian77.setIcon)(menuButton, "more-horizontal");
    menuButton.addEventListener("click", (event) => {
      new SectionActionMenu(this.app, this.store, this.section, this.onRemove, this.onDataChanged).show(event);
    });
    const content = sectionEl.createDiv({ cls: "cow-section-content" });
    this.renderContent(content);
  }
  renderFitnessHeaderActions(actions) {
    const addAction = (label, icon, onClick) => {
      const button = actions.createEl("button", {
        cls: "cow-section-add-button",
        attr: { type: "button", "aria-label": label }
      });
      (0, import_obsidian77.setIcon)(button.createSpan(), icon);
      button.createSpan({ text: label });
      button.addEventListener("click", onClick);
    };
    if (this.section.type === "body-measurements") {
      addAction("\u8BB0\u5F55\u6570\u636E", "plus", () => openBodyMeasurementModal2(this.app, this.store, this.onDataChanged));
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new BodyMeasurementStatisticsModal(this.app, this.store, this.onDataChanged).open());
    }
    if (this.section.type === "workout-plan") {
      addAction("\u65B0\u589E\u8BA1\u5212", "plus", () => openTrainingPlanModal(this.app, this.store, this.onDataChanged));
    }
    if (this.section.type === "workout-log") {
      addAction("\u65B0\u589E\u65E5\u5FD7", "plus", () => openWorkoutRecordModal(this.app, this.store, this.onDataChanged));
    }
    if (this.section.type === "water-sleep-habits") {
      addAction("\u7F16\u8F91\u4ECA\u65E5", "pencil", () => openDailyHealthHabitModal(this.app, this.store, this.onDataChanged));
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new HealthHabitStatisticsModal(this.app, this.store).open());
    }
    if (this.section.type === "fitness-goals") {
      addAction("\u65B0\u589E\u76EE\u6807", "plus", () => openFitnessGoalModal(this.app, this.store, this.onDataChanged));
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new FitnessGoalStatisticsModal(this.app, this.store, this.onDataChanged).open());
    }
    if (this.section.type === "health-reminders") {
      addAction("\u65B0\u589E\u63D0\u9192", "plus", () => openHealthReminderModal(this.app, this.store, this.onDataChanged));
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new HealthReminderStatisticsModal(this.app, this.store).open());
    }
  }
  renderCheckInHeaderActions(actions) {
    const moduleId = this.getCheckInModuleId();
    if (!moduleId) return;
    const addAction = (label, icon, onClick) => {
      const button = actions.createEl("button", {
        cls: "cow-section-add-button",
        attr: { type: "button", "aria-label": label }
      });
      (0, import_obsidian77.setIcon)(button.createSpan(), icon);
      button.createSpan({ text: label });
      button.addEventListener("click", onClick);
    };
    addAction("\u81EA\u5B9A\u4E49", "sliders-horizontal", () => openCheckInManagerModal(this.app, this.store, moduleId, this.onDataChanged));
    addAction("\u5386\u53F2\u8BB0\u5F55", "history", () => openCheckInHistoryModal(this.app, this.store, moduleId === "overview" ? void 0 : moduleId));
  }
  renderReadingHeaderActions(actions) {
    const addAction = (label, icon, onClick) => {
      const button = actions.createEl("button", {
        cls: "cow-section-add-button",
        attr: { type: "button", "aria-label": label }
      });
      (0, import_obsidian77.setIcon)(button.createSpan(), icon);
      button.createSpan({ text: label });
      button.addEventListener("click", onClick);
    };
    if (this.section.type === "bookshelf") {
      addAction("\u589E\u52A0\u4E66\u7C4D", "plus", () => new AddBookModal(this.app, this.store, this.onDataChanged).open());
      addAction("\u6240\u6709\u4E66\u7C4D", "library", () => new AllBooksModal(this.app, this.store, this.onDataChanged).open());
    }
    if (this.section.type === "reading-plan") {
      addAction("\u65B0\u589E\u8BA1\u5212", "plus", () => openReadingPlanModal(this.app, this.store, this.onDataChanged));
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new ReadingPlanStatisticsModal(this.app, this.store).open());
    }
    if (this.section.type === "reading-notes") {
      addAction("\u6DFB\u52A0\u7B14\u8BB0", "plus", () => openReadingNoteModal(this.app, this.store, this.onDataChanged));
    }
    if (this.section.type === "reading-quotes") {
      addAction("\u6DFB\u52A0\u91D1\u53E5", "plus", () => openReadingQuoteModal(this.app, this.store, this.onDataChanged));
    }
    if (this.section.type === "wishlist-books") {
      addAction("\u6DFB\u52A0\u60F3\u8BFB", "plus", () => openWantToReadModal(this.app, this.store, this.onDataChanged));
    }
    if (this.section.type === "reading-tag-manager") {
      addAction("\u65B0\u589E\u6807\u7B7E", "plus", () => openBookTagModal(this.app, this.store, this.onDataChanged));
    }
  }
  renderResearchHeaderActions(actions) {
    const addAction = (label, icon, onClick) => {
      const button = actions.createEl("button", {
        cls: "cow-section-add-button",
        attr: { type: "button", "aria-label": label }
      });
      (0, import_obsidian77.setIcon)(button.createSpan(), icon);
      button.createSpan({ text: label });
      button.addEventListener("click", onClick);
    };
    if (this.section.type === "reading-queue") {
      addAction("\u7F16\u8F91", "pencil", () => new PaperQueueManagerModal(this.app, this.store, this.onDataChanged).open());
    }
    if (this.section.type === "paper-field-manager") {
      addAction("\u7F16\u8F91", "pencil", () => new PaperFieldManagerModal(this.app, this.store, this.onDataChanged).open());
    }
  }
  renderFinanceHeaderActions(actions) {
    const addAction = (label, icon, onClick) => {
      const button = actions.createEl("button", {
        cls: "cow-section-add-button",
        attr: { type: "button", "aria-label": label }
      });
      (0, import_obsidian77.setIcon)(button.createSpan(), icon);
      button.createSpan({ text: label });
      button.addEventListener("click", onClick);
    };
    if (this.section.type === "monthly-budget") {
      addAction("\u7BA1\u7406\u6536\u652F", "list-checks", () => new MonthlyFinanceSummaryModal(this.app, this.store, this.onDataChanged).open());
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new MonthlyBudgetStatisticsModal(this.app, this.store).open());
    }
    if (this.section.type === "expense-categories") {
      addAction("\u65B0\u589E\u5206\u7C7B", "plus", () => openExpenseCategoryModal(this.app, this.store, this.onDataChanged));
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new ExpenseCategoryStatisticsModal(this.app, this.store).open());
    }
    if (this.section.type === "income-expense-trend") {
      addAction("\u7BA1\u7406\u8BB0\u5F55", "list-checks", () => new TransactionManagerModal(this.app, this.store, this.onDataChanged).open());
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new IncomeExpenseStatisticsModal(this.app, this.store).open());
    }
    if (this.section.type === "finance-todos") {
      addAction("\u65B0\u5EFA\u5F85\u529E", "plus", () => openFinanceTodoModal(this.app, this.store, this.onDataChanged));
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new FinanceTodoStatisticsModal(this.app, this.store, this.onDataChanged).open());
    }
    if (this.section.type === "investment-watch") {
      addAction("\u65B0\u589E\u89C2\u5BDF", "plus", () => openInvestmentModal(this.app, this.store, this.onDataChanged));
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new InvestmentStatisticsModal(this.app, this.store).open());
    }
    if (this.section.type === "finance-ledger") {
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new TransactionStatisticsModal(this.app, this.store, this.onDataChanged).open());
    }
  }
  renderGoalHeaderActions(actions) {
    const addAction = (label, icon, onClick) => {
      const button = actions.createEl("button", {
        cls: "cow-section-add-button",
        attr: { type: "button", "aria-label": label }
      });
      (0, import_obsidian77.setIcon)(button.createSpan(), icon);
      button.createSpan({ text: label });
      button.addEventListener("click", onClick);
    };
    if (this.section.type === "yearly-goals") {
      addAction("\u65B0\u589E\u76EE\u6807", "plus", () => openAnnualGoalModal(this.app, this.store, this.onDataChanged));
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new AnnualGoalStatisticsModal(this.app, this.store).open());
    }
    if (this.section.type === "quarterly-okr") {
      addAction("\u65B0\u589E\u5B63\u5EA6\u76EE\u6807", "plus", () => openQuarterlyGoalModal(this.app, this.store, this.onDataChanged));
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new QuarterlyGoalStatisticsModal(this.app, this.store).open());
    }
    if (this.section.type === "monthly-key-results") {
      addAction("\u65B0\u589E\u6708\u5EA6\u76EE\u6807", "plus", () => openMonthlyGoalModal(this.app, this.store, this.onDataChanged));
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new MonthlyGoalStatisticsModal(this.app, this.store).open());
    }
    if (this.section.type === "goal-breakdown") {
      addAction("\u65B0\u589E\u62C6\u89E3", "plus", () => openGoalActionModal(this.app, this.store, this.onDataChanged));
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new GoalBreakdownStatisticsModal(this.app, this.store).open());
    }
    if (this.section.type === "milestone-timeline") {
      addAction("\u65B0\u589E\u91CC\u7A0B\u7891", "plus", () => openGoalActionModal(this.app, this.store, this.onDataChanged, void 0, { isMilestone: true, status: "todo" }));
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new MilestoneStatisticsModal(this.app, this.store).open());
    }
    if (this.section.type === "priority-matrix") {
      addAction("\u65B0\u589E\u4EFB\u52A1", "plus", () => openGoalActionModal(this.app, this.store, this.onDataChanged, void 0, { importance: "important", urgency: "urgent", status: "todo" }));
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new PriorityStatisticsModal(this.app, this.store).open());
    }
    if (this.section.type === "goals-checkin") {
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new SimpleGoalStatisticsModal(this.app, this.store, "\u672C\u5468\u76EE\u6807\u6253\u5361\u7EDF\u8BA1").open());
    }
    if (this.section.type === "risks-blockers") {
      addAction("\u65B0\u589E\u98CE\u9669", "plus", () => openRiskModal(this.app, this.store, this.onDataChanged));
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new RiskStatisticsModal(this.app, this.store).open());
    }
    if (this.section.type === "long-term-progress") {
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new SimpleGoalStatisticsModal(this.app, this.store, "\u957F\u671F\u8FDB\u5C55\u7EDF\u8BA1").open());
    }
    if (this.section.type === "review-checklist") {
      addAction("\u7EDF\u8BA1", "bar-chart-3", () => new ReviewStatisticsModal(this.app, this.store).open());
    }
  }
  renderContent(container) {
    switch (this.section.type) {
      case "weekly-completion":
      case "pending-tasks":
      case "checkin-streak":
        new OverviewStatsSection(this.store, this.section.type).render(container);
        break;
      case "today-focus-stat":
        new FocusStatSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "today-focus":
      case "today-tasks":
        new TodayFocusSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "habit-overview":
      case "habit-summary":
        new CheckInSection(this.store, "all", this.onDataChanged).render(container);
        break;
      case "monthly-progress":
        new MonthlyProgressSection(this.store).render(container);
        break;
      case "monthly-calendar":
      case "month-calendar":
        new MonthlyCalendarSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "recent-notes":
        new RecentNotesSection(this.app).render(container);
        break;
      case "quick-actions":
        new QuickActionsSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "contribution-heatmap":
      case "year-heatmap":
        new ContributionHeatmapSection(this.app).render(container);
        break;
      case "research-projects":
        new ResearchProjectsSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "reading-queue":
        new PaperQueueSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "paper-field-manager":
        new PaperFieldManagerSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "research-checkin":
        new CheckInSection(this.store, "research", this.onDataChanged).render(container);
        break;
      case "experiment-plan":
        new ExperimentSection(this.app, this.store, "plan", this.onDataChanged).render(container);
        break;
      case "experiment-records":
        new ExperimentSection(this.app, this.store, "records", this.onDataChanged).render(container);
        break;
      case "data-analysis-tasks":
        new DataAnalysisTasksSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "literature-notes":
        new LiteratureNotesSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "research-timeline":
        new ResearchTimelineSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "research-memo":
        new ResearchMemoSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "current-reading":
        new CurrentReadingSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "bookshelf":
        new BookshelfSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "reading-plan":
        new ReadingPlanSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "reading-checkin":
        new CheckInSection(this.store, "reading", this.onDataChanged).render(container);
        break;
      case "reading-notes":
        new ReadingNotesSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "reading-quotes":
        new ReadingQuotesSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "reading-tag-manager":
        new ReadingTagManagerSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "finished-books":
        new BookListSection(this.app, this.store, "finished", this.onDataChanged).render(container);
        break;
      case "wishlist-books":
        new WantToReadSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "reading-stats":
        new ReadingStatsSection(this.store).render(container);
        break;
      case "reading-heatmap":
        new ReadingHeatmapSection(this.store).render(container);
        break;
      case "ai-reading-review":
        new AiReadingReviewSection().render(container);
        break;
      case "today-workout":
        new TodayWorkoutSection(this.store).render(container);
        break;
      case "workout-plan":
        new WorkoutPlanSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "fitness-checkin":
        new CheckInSection(this.store, "fitness", this.onDataChanged).render(container);
        break;
      case "body-measurements":
        new BodyMeasurementsSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "cardio-strength-plan":
        new CardioStrengthSection(this.store).render(container);
        break;
      case "water-sleep-habits":
        new WaterSleepHabitsSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "fitness-stats":
        new FitnessStatsSection(this.store).render(container);
        break;
      case "workout-log":
        new WorkoutLogSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "fitness-goals":
        new FitnessGoalsSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "health-reminders":
        new HealthRemindersSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "fitness-heatmap":
        new FitnessHeatmapSection(this.store).render(container);
        break;
      case "monthly-budget":
        new MonthlyBudgetSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "finance-ledger":
        new FinanceLedgerSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "expense-categories":
        new ExpenseCategoriesSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "account-overview":
        new AccountOverviewSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "saving-goals":
        new SavingGoalsSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "bill-reminders":
        new BillRemindersSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "finance-checkin":
        new CheckInSection(this.store, "finance", this.onDataChanged).render(container);
        break;
      case "income-expense-trend":
        new IncomeExpenseTrendSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "finance-todos":
        new FinanceTodosSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "investment-watch":
        new InvestmentWatchSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "expense-heatmap":
        new ExpenseHeatmapSection(this.store).render(container);
        break;
      case "yearly-goals":
        new YearlyGoalsSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "quarterly-okr":
        new QuarterlyOkrSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "monthly-key-results":
        new MonthlyKeyResultsSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "goal-breakdown":
        new GoalBreakdownSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "milestone-timeline":
        new MilestoneTimelineSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "priority-matrix":
        new PriorityMatrixSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "goals-checkin":
        new CheckInSection(this.store, "goals", this.onDataChanged).render(container);
        break;
      case "review-checklist":
        new ReviewChecklistSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "risks-blockers":
        new RisksBlockersSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "long-term-progress":
        new LongTermProgressSection(this.store).render(container);
        break;
      case "enabled-modules-overview":
        new EnabledModulesOverviewSection(this.store).render(container);
        break;
      case "section-manager":
        new FunctionalSectionManagerSection(this.app, this.store, this.onDataChanged).render(container);
        break;
      case "home-layout-manager":
        new HomeLayoutManagerSection(this.store, this.onDataChanged).render(container);
        break;
      case "banner-background-settings":
        new BannerBackgroundSettingsSection(this.store, this.onDataChanged).render(container);
        break;
      case "calendar-widget-settings":
        new CalendarWidgetSettingsSection(this.store, this.onDataChanged).render(container);
        break;
      case "apex-habit-settings":
        new ApexHabitSettingsSection(this.store, this.onDataChanged).render(container);
        break;
      case "quick-action-settings":
        new QuickActionSettingsSection(this.store, this.onDataChanged).render(container);
        break;
      case "theme-color-settings":
        new ThemeColorSettingsSection(this.store, this.onDataChanged).render(container);
        break;
      case "data-source-status":
        new DataSourceStatusSection().render(container);
        break;
      case "custom-text":
      case "custom-todo-list":
      case "custom-link-list":
      case "custom-memo":
        this.renderCustomSection(container);
        break;
      default:
        container.createEl("p", { text: "\u8FD9\u662F\u4E00\u4E2A\u53EF\u914D\u7F6E\u529F\u80FD\u5206\u533A\uFF0C\u540E\u7EED\u53EF\u4EE5\u63A5\u5165\u771F\u5B9E\u6A21\u5757\u7EC4\u4EF6\u3002" });
    }
  }
  openStats() {
    if (this.section.type === "habit-overview") {
      new HabitStatisticsModal(this.app, this.store).open();
      return;
    }
    if (this.section.type === "monthly-progress") {
      new MonthlyProgressStatisticsModal(this.app, this.store).open();
      return;
    }
    if (this.section.type === "today-focus") {
      new DailyFocusStatisticsModal(this.app, this.store).open();
    }
  }
  getLayoutMeta() {
    var _a;
    const roleByType = {
      "weekly-completion": { role: "compact", span: 3 },
      "pending-tasks": { role: "compact", span: 3 },
      "today-focus-stat": { role: "compact", span: 3 },
      "checkin-streak": { role: "compact", span: 3 },
      "today-focus": { role: "primary", span: 8 },
      "habit-overview": { role: "primary", span: 8 },
      "monthly-progress": { role: "secondary", span: 4 },
      "monthly-calendar": { role: "primary", span: 7 },
      "month-calendar": { role: "primary", span: 7 },
      "recent-notes": { role: "secondary", span: 5 },
      "quick-actions": { role: "compact", span: 4 },
      "contribution-heatmap": { role: "wide", span: 12 },
      "research-projects": { role: "secondary", span: 4 },
      "reading-queue": { role: "primary", span: 8 },
      "paper-field-manager": { role: "secondary", span: 4 },
      "research-checkin": { role: "secondary", span: 4 },
      "experiment-plan": { role: "list", span: 4 },
      "experiment-records": { role: "list", span: 6 },
      "data-analysis-tasks": { role: "list", span: 6 },
      "literature-notes": { role: "list", span: 4 },
      "research-timeline": { role: "secondary", span: 4 },
      "research-memo": { role: "secondary", span: 4 },
      "current-reading": { role: "primary", span: 8 },
      bookshelf: { role: "wide", span: 12 },
      "reading-plan": { role: "secondary", span: 4 },
      "reading-checkin": { role: "secondary", span: 4 },
      "reading-notes": { role: "list", span: 3 },
      "reading-quotes": { role: "secondary", span: 3 },
      "reading-tag-manager": { role: "secondary", span: 3 },
      "finished-books": { role: "list", span: 4 },
      "wishlist-books": { role: "list", span: 3 },
      "reading-stats": { role: "secondary", span: 4 },
      "reading-heatmap": { role: "visual", span: 8 },
      "ai-reading-review": { role: "secondary", span: 4 },
      "today-workout": { role: "secondary", span: 4 },
      "workout-plan": { role: "primary", span: 7 },
      "fitness-checkin": { role: "secondary", span: 4 },
      "body-measurements": { role: "secondary", span: 4 },
      "cardio-strength-plan": { role: "secondary", span: 4 },
      "water-sleep-habits": { role: "secondary", span: 4 },
      "fitness-stats": { role: "visual", span: 8 },
      "workout-log": { role: "list", span: 6 },
      "fitness-goals": { role: "primary", span: 6 },
      "health-reminders": { role: "list", span: 3 },
      "fitness-heatmap": { role: "visual", span: 6 },
      "monthly-budget": { role: "primary", span: 4 },
      "finance-ledger": { role: "primary", span: 8 },
      "expense-categories": { role: "secondary", span: 4 },
      "account-overview": { role: "secondary", span: 4 },
      "saving-goals": { role: "secondary", span: 4 },
      "bill-reminders": { role: "list", span: 4 },
      "finance-checkin": { role: "secondary", span: 4 },
      "income-expense-trend": { role: "visual", span: 8 },
      "finance-todos": { role: "list", span: 4 },
      "investment-watch": { role: "secondary", span: 5 },
      "expense-heatmap": { role: "visual", span: 6 },
      "yearly-goals": { role: "primary", span: 8 },
      "quarterly-okr": { role: "primary", span: 6 },
      "monthly-key-results": { role: "primary", span: 6 },
      "goal-breakdown": { role: "primary", span: 8 },
      "milestone-timeline": { role: "list", span: 4 },
      "priority-matrix": { role: "visual", span: 8 },
      "goals-checkin": { role: "secondary", span: 4 },
      "review-checklist": { role: "list", span: 4 },
      "risks-blockers": { role: "list", span: 4 },
      "long-term-progress": { role: "visual", span: 4 },
      "enabled-modules-overview": { role: "compact", span: 4 },
      "home-layout-manager": { role: "primary", span: 8 },
      "section-manager": { role: "primary", span: 8 },
      "banner-background-settings": { role: "secondary", span: 4 },
      "calendar-widget-settings": { role: "secondary", span: 4 },
      "apex-habit-settings": { role: "secondary", span: 4 },
      "quick-action-settings": { role: "secondary", span: 4 },
      "theme-color-settings": { role: "secondary", span: 4 },
      "data-source-status": { role: "compact", span: 4 }
    };
    return (_a = roleByType[this.section.type]) != null ? _a : this.getFallbackLayoutMeta();
  }
  getFallbackLayoutMeta() {
    if (this.section.width === "full") return { role: "wide", span: 12 };
    if (this.section.width === "lg") return { role: "primary", span: 8 };
    if (this.section.width === "sm") return { role: "compact", span: 3 };
    return { role: "secondary", span: 4 };
  }
  getCheckInModuleId() {
    switch (this.section.type) {
      case "habit-overview":
      case "habit-summary":
        return "overview";
      case "research-checkin":
        return "research";
      case "reading-checkin":
        return "reading";
      case "fitness-checkin":
        return "fitness";
      case "finance-checkin":
        return "finance";
      case "goals-checkin":
        return "goals";
      default:
        return void 0;
    }
  }
  renderCustomSection(container) {
    var _a;
    const description = typeof ((_a = this.section.config) == null ? void 0 : _a.description) === "string" ? this.section.config.description : "";
    if (description) {
      container.createEl("p", { text: description });
    }
    if (this.section.type === "custom-todo-list") {
      const list = container.createEl("ul", { cls: "cow-focus-list" });
      ["\u5F85\u8865\u5145 Todo", "\u7EE7\u7EED\u5B8C\u5584\u8FD9\u4E2A\u5206\u533A"].forEach((item) => {
        const row = list.createEl("li");
        row.createEl("input", { type: "checkbox" });
        row.createSpan({ text: item });
      });
      return;
    }
    if (this.section.type === "custom-link-list") {
      const list = container.createDiv({ cls: "cow-data-list" });
      const row = list.createDiv({ cls: "cow-data-card" });
      row.createEl("strong", { text: "\u94FE\u63A5\u5217\u8868" });
      row.createDiv({ cls: "cow-meta-line" }).createSpan({ text: "\u540E\u7EED\u53EF\u5728\u6A21\u5757\u5185\u5BB9\u7BA1\u7406\u4E2D\u6269\u5C55\u94FE\u63A5\u9879\u3002" });
      return;
    }
    if (this.section.type === "custom-memo") {
      const list = container.createEl("ul", { cls: "cow-memo-list" });
      list.createEl("li", { text: description || "\u8BB0\u5F55\u4E00\u4E2A\u53EF\u7231\u7684\u60F3\u6CD5\u3002" });
      return;
    }
    if (!description) {
      container.createEl("p", { cls: "cow-empty-state", text: "\u8FD9\u662F\u4E00\u4E2A\u81EA\u5B9A\u4E49\u6587\u672C\u5206\u533A\u3002" });
    }
  }
  getIcon() {
    var _a;
    const iconMap = {
      "weekly-completion": "badge-percent",
      "pending-tasks": "clipboard-list",
      "today-focus-stat": "headphones",
      "checkin-streak": "flame",
      "today-focus": "target",
      "today-tasks": "target",
      "habit-overview": "calendar-check",
      "habit-summary": "calendar-check",
      "monthly-progress": "bar-chart-3",
      "monthly-calendar": "calendar-days",
      "month-calendar": "calendar-days",
      "recent-notes": "file-text",
      "quick-actions": "zap",
      "contribution-heatmap": "activity",
      "year-heatmap": "activity",
      memo: "sticky-note",
      todo: "list-checks",
      projects: "folder-kanban",
      notes: "notebook-tabs",
      "research-projects": "layers",
      "reading-queue": "book-marked",
      "research-checkin": "calendar-check",
      "experiment-plan": "clipboard-check",
      "experiment-records": "file-clock",
      "data-analysis-tasks": "bar-chart-3",
      "literature-notes": "notebook-text",
      "research-timeline": "calendar-clock",
      "research-memo": "lightbulb",
      "current-reading": "book-open-check",
      bookshelf: "library",
      "reading-plan": "calendar-range",
      "reading-checkin": "calendar-check",
      "reading-notes": "notebook-tabs",
      "reading-quotes": "quote",
      "reading-tag-manager": "tags",
      "finished-books": "badge-check",
      "wishlist-books": "bookmark-plus",
      "reading-stats": "pie-chart",
      "reading-heatmap": "activity",
      "ai-reading-review": "sparkles",
      "today-workout": "dumbbell",
      "workout-plan": "clipboard-list",
      "fitness-checkin": "calendar-check",
      "body-measurements": "ruler",
      "cardio-strength-plan": "heart-pulse",
      "water-sleep-habits": "moon",
      "fitness-stats": "line-chart",
      "workout-log": "notebook-text",
      "fitness-goals": "target",
      "health-reminders": "bell-ring",
      "fitness-heatmap": "activity",
      "monthly-budget": "wallet-cards",
      "finance-ledger": "circle-plus",
      "expense-categories": "chart-pie",
      "account-overview": "landmark",
      "saving-goals": "piggy-bank",
      "bill-reminders": "receipt",
      "finance-checkin": "calendar-check",
      "income-expense-trend": "line-chart",
      "finance-todos": "list-checks",
      "investment-watch": "candlestick-chart",
      "expense-heatmap": "activity",
      "yearly-goals": "flag",
      "quarterly-okr": "target",
      "monthly-key-results": "list-checks",
      "goal-breakdown": "git-branch",
      "milestone-timeline": "milestone",
      "priority-matrix": "layout-dashboard",
      "goals-checkin": "calendar-check",
      "review-checklist": "clipboard-check",
      "risks-blockers": "triangle-alert",
      "long-term-progress": "trending-up",
      "enabled-modules-overview": "panel-top",
      "section-manager": "rows-3",
      "home-layout-manager": "layout-template",
      "banner-background-settings": "image",
      "calendar-widget-settings": "calendar-days",
      "apex-habit-settings": "calendar-check",
      "quick-action-settings": "zap",
      "theme-color-settings": "palette",
      "data-source-status": "database"
    };
    return (_a = iconMap[this.section.type]) != null ? _a : "sparkles";
  }
};

// src/components/AddSectionButton.ts
var import_obsidian78 = require("obsidian");
var AddSectionModal = class extends import_obsidian78.Modal {
  constructor(app, page, modules, onSelect) {
    super(app);
    this.page = page;
    this.modules = modules;
    this.onSelect = onSelect;
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: "\u6DFB\u52A0\u529F\u80FD\u5206\u533A" });
    this.contentEl.createEl("p", { text: "\u9009\u62E9\u4E00\u4E2A\u6A21\u5757\u540E\u4F1A\u7ACB\u5373\u52A0\u5165\u5F53\u524D\u9875\u9762\u5E76\u4FDD\u5B58\u5E03\u5C40\u3002" });
    const grid = this.contentEl.createDiv({ cls: "cow-add-module-grid" });
    this.modules.forEach((module2) => {
      const button = grid.createEl("button", {
        cls: "cow-add-module-card",
        attr: { type: "button" }
      });
      const icon = button.createSpan({ cls: "cow-add-module-icon" });
      (0, import_obsidian78.setIcon)(icon, module2.icon);
      button.createEl("strong", { cls: "cow-add-module-title", text: module2.title });
      button.createEl("span", { cls: "cow-add-module-description", text: module2.description });
      button.addEventListener("click", async () => {
        await this.onSelect(module2.type);
        this.close();
      });
    });
    if (this.modules.length === 0) {
      grid.createEl("p", { text: `${PAGE_LABELS[this.page]}\u9875\u9762\u6682\u65E0\u53EF\u6DFB\u52A0\u6A21\u5757\u3002` });
    }
  }
};
var AddSectionButton = class {
  constructor(app, page, modules, onAdd) {
    this.app = app;
    this.page = page;
    this.modules = modules;
    this.onAdd = onAdd;
  }
  render(container) {
    const button = container.createEl("button", {
      cls: "cow-add-section-button",
      attr: { type: "button" }
    });
    (0, import_obsidian78.setIcon)(button.createSpan(), "plus");
    button.createSpan({ text: "\u6DFB\u52A0\u529F\u80FD\u5206\u533A" });
    button.addEventListener("click", () => {
      new AddSectionModal(this.app, this.page, this.modules, this.onAdd).open();
    });
  }
};

// src/components/DashboardGrid.ts
var DashboardGrid = class {
  constructor(app, store, page, onDataChanged) {
    this.app = app;
    this.store = store;
    this.page = page;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    var _a;
    const layout = this.store.getModuleLayout(this.page);
    const grid = container.createDiv({ cls: `cow-dashboard-grid cow-dashboard-page-${this.page} cow-module-layout-${layout.mode}` });
    grid.style.setProperty("--layout-columns", String((_a = layout.columns) != null ? _a : 12));
    const sections = this.store.getSectionsForPage(this.page);
    sections.forEach((section) => {
      new DashboardSection(this.app, this.store, section, async (removedSection) => {
        await this.store.removeSection(removedSection.id);
        this.onDataChanged();
      }, this.onDataChanged).render(grid);
    });
    new AddSectionButton(this.app, this.page, this.store.getAvailableModules(this.page), async (moduleType) => {
      await this.store.addSection(this.page, moduleType);
      this.onDataChanged();
    }).render(container);
  }
};

// src/pages/BaseDashboardPage.ts
var BaseDashboardPage = class {
  constructor(app, store, page, onDataChanged) {
    this.app = app;
    this.store = store;
    this.page = page;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const definition = this.store.getPages().find((item) => item.id === this.page);
    const pageEl = container.createDiv({ cls: `cow-page cow-module-page cow-page-${this.page}` });
    const heading = pageEl.createDiv({ cls: "cow-page-heading" });
    const title = heading.createDiv({ cls: "cow-page-title" });
    title.createEl("h1", { text: definition.label });
    title.createEl("p", { text: definition.description });
    new DashboardGrid(this.app, this.store, this.page, this.onDataChanged).render(pageEl);
  }
};

// src/pages/OverviewPage.ts
var OverviewPage = class extends BaseDashboardPage {
};

// src/pages/ResearchPage.ts
var ResearchPage = class extends BaseDashboardPage {
};

// src/pages/ReadingPage.ts
var ReadingPage = class extends BaseDashboardPage {
};

// src/pages/FitnessPage.ts
var FitnessPage = class extends BaseDashboardPage {
};

// src/pages/FinancePage.ts
var FinancePage = class extends BaseDashboardPage {
};

// src/pages/TasksPage.ts
var import_obsidian79 = require("obsidian");
var BOARD_COLUMNS = [
  ["todo", "\u5F85\u529E"],
  ["doing", "\u8FDB\u884C\u4E2D"],
  ["waiting", "\u7B49\u5F85"],
  ["done", "\u5DF2\u5B8C\u6210"]
];
var TasksPage = class {
  constructor(app, store, page, onDataChanged) {
    this.app = app;
    this.store = store;
    this.page = page;
    this.onDataChanged = onDataChanged;
    this.weekOffset = 0;
  }
  render(container) {
    void this.store.generateDueRecurringTasks();
    const definition = this.store.getPages().find((item) => item.id === this.page);
    const pageEl = container.createDiv({ cls: "cow-page cow-module-page cow-page-tasks cow-tasks-page" });
    this.renderHeader(pageEl, definition);
    this.renderQuickAdd(pageEl);
    const topGrid = pageEl.createDiv({ cls: "cow-tasks-top-grid" });
    this.renderTodayTasks(topGrid);
    this.renderInbox(topGrid);
    this.renderStats(topGrid);
    this.renderWeekPlan(pageEl);
    this.renderBoard(pageEl);
    const bottomGrid = pageEl.createDiv({ cls: "cow-tasks-bottom-grid" });
    this.renderProjects(bottomGrid);
    this.renderRecurring(bottomGrid);
  }
  renderHeader(container, definition) {
    const heading = container.createDiv({ cls: "cow-page-heading cow-tasks-heading" });
    const title = heading.createDiv({ cls: "cow-page-title" });
    title.createEl("h1", { text: definition.label });
    title.createEl("p", { text: definition.description });
    const actions = heading.createDiv({ cls: "cow-config-actions cow-page-actions" });
    this.iconButton(actions, "plus", "\u65B0\u5EFA\u4EFB\u52A1", () => openTaskModal(this.app, this.store, this.onDataChanged, void 0, { status: "inbox", sourceModule: "general" }));
    this.iconButton(actions, "filter", "\u7B5B\u9009", () => openTaskFilterModal(this.app, this.store, this.onDataChanged));
    this.iconButton(actions, "bar-chart-3", "\u7EDF\u8BA1", () => new TaskStatisticsModal(this.app, this.store).open());
  }
  renderQuickAdd(container) {
    const card = container.createDiv({ cls: "cow-task-panel cow-task-quick-add" });
    card.createEl("h2", { text: "\u5FEB\u901F\u6DFB\u52A0\u4EFB\u52A1" });
    const row = card.createDiv({ cls: "cow-task-quick-row" });
    const input = row.createEl("input", { attr: { type: "text", placeholder: "\u8F93\u5165\u4EFB\u52A1\u5185\u5BB9\uFF0C\u6309\u56DE\u8F66\u5FEB\u901F\u6DFB\u52A0\u2026" } });
    const add = row.createEl("button", { text: "\u6DFB\u52A0", cls: "mod-cta", attr: { type: "button" } });
    const submit = async () => {
      const title = input.value.trim();
      if (!title) return;
      await this.store.addTask({ title, status: "inbox", priority: "none", sourceModule: "general" });
      input.value = "";
      this.onDataChanged();
    };
    input.addEventListener("keydown", (event) => {
      if (event.key === "Enter") void submit();
    });
    add.addEventListener("click", () => void submit());
  }
  renderTodayTasks(container) {
    const today6 = formatDateKey(/* @__PURE__ */ new Date());
    const tasks = this.filteredTasks().filter((task) => task.plannedDate === today6);
    const open = tasks.filter((task) => task.status !== "done" && task.status !== "cancelled");
    const done = tasks.filter((task) => task.status === "done");
    const card = this.panel(container, `\u4ECA\u65E5\u4EFB\u52A1 ${done.length}/${tasks.length}`, "\u4ECA\u5929\u6682\u65F6\u6CA1\u6709\u4EFB\u52A1");
    [...open, ...done].slice(0, 10).forEach((task) => this.renderTaskRow(card, task, true));
  }
  renderInbox(container) {
    const tasks = this.filteredTasks().filter((task) => task.status === "inbox" && !task.parentTaskId).slice(0, 6);
    const card = this.panel(container, "\u4EFB\u52A1\u6536\u96C6\u7BB1", "\u6536\u96C6\u7BB1\u5DF2\u7ECF\u6E05\u7A7A\u5566");
    tasks.forEach((task) => this.renderTaskRow(card, task, true));
    if (this.store.getTasks().filter((task) => task.status === "inbox").length > 6) {
      card.createEl("button", { cls: "cow-task-link-button", text: "\u67E5\u770B\u5168\u90E8", attr: { type: "button" } }).addEventListener("click", () => openTaskFilterModal(this.app, this.store, this.onDataChanged));
    }
  }
  renderStats(container) {
    const tasks = this.filteredTasks();
    const weekDates = this.getWeekDates(0).map((date) => formatDateKey(date));
    const weekly = tasks.filter((task) => task.plannedDate && weekDates.includes(task.plannedDate) || task.completedDate && weekDates.includes(task.completedDate));
    const done = weekly.filter((task) => task.status === "done").length;
    const overdue = tasks.filter((task) => task.dueDate && task.dueDate < formatDateKey(/* @__PURE__ */ new Date()) && task.status !== "done").length;
    const card = container.createDiv({ cls: "cow-task-panel cow-task-stat-panel" });
    card.createEl("h2", { text: "\u4EFB\u52A1\u7EDF\u8BA1" });
    const grid = card.createDiv({ cls: "cow-stats-card-grid" });
    [
      ["\u672C\u5468\u5B8C\u6210\u7387", `${weekly.length ? Math.round(done / weekly.length * 100) : 0}%`],
      ["\u5EF6\u671F\u4EFB\u52A1", overdue],
      ["\u672C\u5468\u5B8C\u6210", done],
      ["\u9884\u8BA1\u65F6\u95F4", `${weekly.reduce((sum, task) => {
        var _a;
        return sum + ((_a = task.estimatedMinutes) != null ? _a : 0);
      }, 0)}min`]
    ].forEach(([label, value]) => {
      const item = grid.createDiv({ cls: "cow-stats-card" });
      item.createEl("strong", { text: String(value) });
      item.createSpan({ text: String(label) });
    });
  }
  renderWeekPlan(container) {
    const card = container.createDiv({ cls: "cow-task-panel cow-task-week-panel" });
    const header = card.createDiv({ cls: "cow-task-panel-header" });
    header.createEl("h2", { text: "\u672C\u5468\u8BA1\u5212" });
    const actions = header.createDiv({ cls: "cow-list-item-actions" });
    actions.createEl("button", { text: "\u4E0A\u4E00\u5468", attr: { type: "button" } }).addEventListener("click", () => {
      this.weekOffset -= 1;
      renderWeek();
    });
    actions.createEl("button", { text: "\u672C\u5468", attr: { type: "button" } }).addEventListener("click", () => {
      this.weekOffset = 0;
      renderWeek();
    });
    actions.createEl("button", { text: "\u4E0B\u4E00\u5468", attr: { type: "button" } }).addEventListener("click", () => {
      this.weekOffset += 1;
      renderWeek();
    });
    const scroll = card.createDiv({ cls: "cow-task-week-scroll" });
    const renderWeek = () => {
      scroll.empty();
      const grid = scroll.createDiv({ cls: "cow-task-week-grid" });
      const dates = this.getWeekDates(this.weekOffset);
      dates.forEach((date) => {
        const dateKey = formatDateKey(date);
        const day = grid.createDiv({ cls: "cow-task-week-day" });
        day.createEl("strong", { text: `${date.getMonth() + 1}/${date.getDate()}` });
        day.createSpan({ cls: "cow-meta-line", text: ["\u5468\u65E5", "\u5468\u4E00", "\u5468\u4E8C", "\u5468\u4E09", "\u5468\u56DB", "\u5468\u4E94", "\u5468\u516D"][date.getDay()] });
        const tasks = this.filteredTasks().filter((task) => task.plannedDate === dateKey && !task.parentTaskId);
        if (tasks.length === 0) {
          day.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0" });
          return;
        }
        tasks.slice(0, 5).forEach((task) => {
          const item = day.createEl("button", { cls: "cow-task-chip", attr: { type: "button", title: task.title } });
          item.createSpan({ text: task.title });
          item.addEventListener("click", () => openTaskModal(this.app, this.store, this.onDataChanged, task));
        });
      });
    };
    renderWeek();
  }
  renderBoard(container) {
    const card = container.createDiv({ cls: "cow-task-panel cow-task-board-panel" });
    card.createEl("h2", { text: "\u4EFB\u52A1\u770B\u677F" });
    const board = card.createDiv({ cls: "cow-task-board" });
    BOARD_COLUMNS.forEach(([status, label]) => {
      const column = board.createDiv({ cls: "cow-task-board-column" });
      column.createEl("h3", { text: label });
      const tasks = this.filteredTasks().filter((task) => task.status === status && !task.parentTaskId).slice(0, 8);
      if (tasks.length === 0) {
        column.createDiv({ cls: "cow-empty-state", text: "\u6682\u65E0\u4EFB\u52A1" });
        return;
      }
      tasks.forEach((task) => this.renderBoardCard(column, task));
    });
  }
  renderProjects(container) {
    const card = this.panel(container, "\u9879\u76EE\u4E0E\u4EFB\u52A1\u7EC4", "\u8FD8\u6CA1\u6709\u9879\u76EE");
    const add = card.createEl("button", { cls: "cow-task-add-strip", text: "+ \u65B0\u5EFA\u9879\u76EE", attr: { type: "button" } });
    add.addEventListener("click", () => openTaskProjectModal(this.app, this.store, this.onDataChanged));
    this.store.getTaskProjects().slice(0, 6).forEach((project) => {
      const tasks = this.store.getTasks().filter((task) => task.projectId === project.id);
      const done = tasks.filter((task) => task.status === "done").length;
      const percent = tasks.length ? Math.round(done / tasks.length * 100) : 0;
      const row = card.createDiv({ cls: "cow-data-card" });
      row.createEl("strong", { text: project.name });
      row.createDiv({ cls: "cow-meta-line", text: project.description || "\u6682\u65E0\u63CF\u8FF0" });
      row.createDiv({ cls: "cow-meta-line", text: `${done} / ${tasks.length} \xB7 ${percent}%` });
      row.createDiv({ cls: "cow-month-progress-track" }).createDiv({ cls: "cow-month-progress-fill is-pink", attr: { style: `width: ${percent}%` } });
    });
  }
  renderRecurring(container) {
    const card = this.panel(container, "\u5468\u671F\u4EFB\u52A1", "\u8FD8\u6CA1\u6709\u5468\u671F\u4EFB\u52A1");
    this.store.getTasks().filter((task) => {
      var _a;
      return ((_a = task.recurrence) == null ? void 0 : _a.enabled) && task.recurrence.frequency !== "none";
    }).slice(0, 6).forEach((task) => {
      var _a, _b, _c, _d, _e;
      const row = card.createDiv({ cls: "cow-data-card" });
      const head = row.createDiv({ cls: "cow-list-item-head" });
      const body = head.createDiv();
      body.createEl("strong", { text: task.title });
      body.createDiv({ cls: "cow-meta-line", text: `${recurrenceLabel((_a = task.recurrence) == null ? void 0 : _a.frequency)} \xB7 \u4E0B\u6B21 ${(_d = (_c = (_b = task.recurrence) == null ? void 0 : _b.nextDate) != null ? _c : task.plannedDate) != null ? _d : "--"}` });
      const toggle = head.createEl("button", { text: ((_e = task.recurrence) == null ? void 0 : _e.enabled) ? "\u505C\u7528" : "\u542F\u7528", attr: { type: "button" } });
      toggle.addEventListener("click", async () => {
        var _a2, _b2, _c2;
        await this.store.updateTask(task.id, { recurrence: { ...task.recurrence, frequency: (_b2 = (_a2 = task.recurrence) == null ? void 0 : _a2.frequency) != null ? _b2 : "daily", enabled: !((_c2 = task.recurrence) == null ? void 0 : _c2.enabled) } });
        this.onDataChanged();
      });
    });
  }
  renderTaskRow(container, task, compact = false) {
    var _a;
    const row = container.createDiv({ cls: `cow-task-row ${task.status === "done" ? "is-done" : ""}` });
    const checkbox = row.createEl("input", { attr: { type: "checkbox" } });
    checkbox.checked = task.status === "done";
    checkbox.addEventListener("change", async () => {
      await this.store.updateTask(task.id, { status: checkbox.checked ? "done" : "todo" });
      this.onDataChanged();
    });
    const body = row.createDiv({ cls: "cow-task-row-body" });
    body.createEl("strong", { text: task.title });
    const meta = body.createDiv({ cls: "cow-task-meta" });
    meta.createSpan({ cls: `cow-task-priority is-${task.priority}`, text: priorityLabel(task.priority) });
    if (task.estimatedMinutes) meta.createSpan({ text: taskTimeLabel(task) });
    if (!compact && ((_a = task.tags) == null ? void 0 : _a.length)) task.tags.slice(0, 4).forEach((tag) => meta.createSpan({ cls: "cow-pill is-blue", text: tag }));
    const actions = row.createDiv({ cls: "cow-list-item-actions" });
    this.smallIcon(actions, "pencil", "\u7F16\u8F91\u4EFB\u52A1", () => openTaskModal(this.app, this.store, this.onDataChanged, task));
    this.smallIcon(actions, "trash-2", "\u5220\u9664\u4EFB\u52A1", () => void this.confirmDeleteTask(task));
  }
  renderBoardCard(container, task) {
    const card = container.createDiv({ cls: `cow-task-board-card is-${task.priority}` });
    card.addEventListener("click", () => openTaskModal(this.app, this.store, this.onDataChanged, task));
    card.createEl("strong", { text: task.title });
    const meta = card.createDiv({ cls: "cow-task-meta" });
    meta.createSpan({ cls: `cow-task-priority is-${task.priority}`, text: priorityLabel(task.priority) });
    meta.createSpan({ text: taskTimeLabel(task) });
    const status = card.createEl("select");
    BOARD_COLUMNS.forEach(([id, label]) => status.createEl("option", { value: id, text: label }));
    status.value = task.status;
    status.addEventListener("click", (event) => event.stopPropagation());
    status.addEventListener("change", async () => {
      await this.store.updateTask(task.id, { status: status.value });
      this.onDataChanged();
    });
  }
  panel(container, title, empty) {
    const card = container.createDiv({ cls: "cow-task-panel" });
    card.createEl("h2", { text: title });
    const marker = card.createDiv({ cls: "cow-task-empty-marker", text: empty });
    window.requestAnimationFrame(() => {
      if (card.querySelectorAll(".cow-task-row, .cow-data-card, .cow-task-board-card, .cow-task-chip").length > 0) marker.detach();
    });
    return card;
  }
  filteredTasks() {
    const settings = this.store.getTaskSettings();
    return this.store.getTasks().filter((task) => {
      var _a, _b, _c, _d, _e;
      if (settings.filterStatus && settings.filterStatus !== "all" && task.status !== settings.filterStatus) return false;
      if (settings.filterPriority && settings.filterPriority !== "all" && task.priority !== settings.filterPriority) return false;
      if (settings.filterProjectId && task.projectId !== settings.filterProjectId) return false;
      if (settings.filterStartDate && ((_b = (_a = task.plannedDate) != null ? _a : task.dueDate) != null ? _b : "") < settings.filterStartDate) return false;
      if (settings.filterEndDate && ((_d = (_c = task.plannedDate) != null ? _c : task.dueDate) != null ? _d : "") > settings.filterEndDate) return false;
      if (settings.filterTag && !((_e = task.tags) != null ? _e : []).some((tag) => {
        var _a2;
        return tag.includes((_a2 = settings.filterTag) != null ? _a2 : "");
      })) return false;
      return true;
    });
  }
  getWeekDates(offset) {
    const today6 = /* @__PURE__ */ new Date();
    const monday = new Date(today6);
    monday.setDate(today6.getDate() - (today6.getDay() + 6) % 7 + offset * 7);
    return Array.from({ length: 7 }, (_, index) => {
      const date = new Date(monday);
      date.setDate(monday.getDate() + index);
      return date;
    });
  }
  async confirmDeleteTask(task) {
    const children = this.store.getTasks().filter((item) => item.parentTaskId === task.id);
    if (children.length > 0) {
      const deleteChildren = confirm(`\u201C${task.title}\u201D\u5305\u542B ${children.length} \u4E2A\u5B50\u4EFB\u52A1\u3002
\u786E\u5B9A\u4F1A\u540C\u65F6\u5220\u9664\u5B50\u4EFB\u52A1\uFF1B\u53D6\u6D88\u5219\u4EC5\u5220\u9664\u7236\u4EFB\u52A1\u5E76\u4FDD\u7559\u5B50\u4EFB\u52A1\u3002`);
      await this.store.deleteTask(task.id, deleteChildren ? "delete-children" : "keep-children");
    } else {
      if (!confirm(`\u5220\u9664\u201C${task.title}\u201D\uFF1F`)) return;
      await this.store.deleteTask(task.id);
    }
    this.onDataChanged();
  }
  iconButton(container, icon, label, onClick) {
    const button = container.createEl("button", { attr: { type: "button" } });
    (0, import_obsidian79.setIcon)(button.createSpan(), icon);
    button.createSpan({ text: label });
    button.addEventListener("click", onClick);
  }
  smallIcon(container, icon, label, onClick) {
    const button = container.createEl("button", { attr: { type: "button", "aria-label": label } });
    (0, import_obsidian79.setIcon)(button, icon);
    button.addEventListener("click", (event) => {
      event.stopPropagation();
      onClick();
    });
  }
};

// src/pages/GoalsPage.ts
var GoalsPage = class extends BaseDashboardPage {
};

// src/pages/ModulesPage.ts
var import_obsidian80 = require("obsidian");
var ResetDefaultsModal = class extends import_obsidian80.Modal {
  constructor(app, onConfirm) {
    super(app);
    this.onConfirm = onConfirm;
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: "\u6062\u590D\u9ED8\u8BA4\u914D\u7F6E\uFF1F" });
    this.contentEl.createEl("p", { text: "\u8FD9\u4F1A\u91CD\u7F6E\u5DE5\u4F5C\u53F0\u5E03\u5C40\u3001\u4E3B\u9898\u3001\u6A21\u5757\u548C\u5185\u90E8\u793A\u4F8B\u6570\u636E\u3002" });
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    actions.createEl("button", { text: "\u53D6\u6D88", attr: { type: "button" } }).addEventListener("click", () => this.close());
    actions.createEl("button", { text: "\u6062\u590D\u9ED8\u8BA4", cls: "mod-warning", attr: { type: "button" } }).addEventListener("click", async () => {
      await this.onConfirm();
      this.close();
    });
  }
};
var ModulesPage = class {
  constructor(app, store, page, onDataChanged) {
    this.app = app;
    this.store = store;
    this.page = page;
    this.onDataChanged = onDataChanged;
  }
  render(container) {
    const definition = this.store.getPages().find((item) => item.id === this.page);
    const pageEl = container.createDiv({ cls: "cow-page cow-module-page cow-page-modules" });
    const heading = pageEl.createDiv({ cls: "cow-page-heading cow-modules-heading" });
    const title = heading.createDiv({ cls: "cow-page-title" });
    title.createEl("h1", { text: definition.label });
    title.createEl("p", { text: definition.description });
    this.renderActions(heading);
    new DashboardGrid(this.app, this.store, this.page, this.onDataChanged).render(pageEl);
  }
  renderActions(container) {
    const actions = container.createDiv({ cls: "cow-config-actions cow-page-actions" });
    const importInput = actions.createEl("input", { type: "file", attr: { accept: "application/json" } });
    importInput.addClass("cow-hidden-input");
    const importButton = actions.createEl("button", { attr: { type: "button" } });
    (0, import_obsidian80.setIcon)(importButton.createSpan(), "upload");
    importButton.createSpan({ text: "\u5BFC\u5165\u914D\u7F6E" });
    importButton.addEventListener("click", () => importInput.click());
    importInput.addEventListener("change", () => {
      var _a;
      const file = (_a = importInput.files) == null ? void 0 : _a[0];
      if (!file) return;
      readJsonFile(file, async (data) => {
        await this.store.importData(data);
        new import_obsidian80.Notice("\u914D\u7F6E\u5DF2\u5BFC\u5165\u3002");
        this.onDataChanged();
      });
    });
    const exportButton = actions.createEl("button", { attr: { type: "button" } });
    (0, import_obsidian80.setIcon)(exportButton.createSpan(), "download");
    exportButton.createSpan({ text: "\u5BFC\u51FA\u914D\u7F6E" });
    exportButton.addEventListener("click", () => {
      downloadJson("cute-obsidian-workbench-config.json", this.store.exportData());
    });
    const resetButton = actions.createEl("button", { cls: "mod-warning", attr: { type: "button" } });
    (0, import_obsidian80.setIcon)(resetButton.createSpan(), "rotate-ccw");
    resetButton.createSpan({ text: "\u6062\u590D\u9ED8\u8BA4" });
    resetButton.addEventListener("click", () => {
      new ResetDefaultsModal(this.app, async () => {
        await this.store.resetToDefaults();
        this.onDataChanged();
      }).open();
    });
  }
};

// src/components/QuickCreateModal.ts
var import_obsidian81 = require("obsidian");
var QuickCreateModal = class extends import_obsidian81.Modal {
  constructor(app, store, getCurrentPage, onDataChanged) {
    super(app);
    this.store = store;
    this.getCurrentPage = getCurrentPage;
    this.onDataChanged = onDataChanged;
    this.notes = new NoteService(app);
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: "\u5FEB\u901F\u521B\u5EFA" });
    this.contentEl.createEl("p", { text: "\u9009\u62E9\u4E00\u4E2A\u5165\u53E3\uFF0C\u9A6C\u4E0A\u5F00\u59CB\u8BB0\u5F55\u3002" });
    const grid = this.contentEl.createDiv({ cls: "cow-quick-create-grid" });
    this.renderAction(grid, "\u65B0\u5EFA\u7B14\u8BB0", "file-plus", async () => {
      await this.notes.createNote();
    });
    this.renderAction(grid, "\u6253\u5F00/\u521B\u5EFA\u4ECA\u65E5\u7B14\u8BB0", "calendar-days", async () => {
      await this.notes.openOrCreateDailyNote(/* @__PURE__ */ new Date());
    });
    this.renderAction(grid, "\u6DFB\u52A0\u4EFB\u52A1", "list-plus", async () => {
      await this.store.addTodayFocusTask("\u65B0\u7684\u5F85\u529E\u4EFB\u52A1");
      this.onDataChanged();
      new import_obsidian81.Notice("\u5DF2\u6DFB\u52A0\u5230\u4ECA\u65E5\u7126\u70B9\u3002");
    });
    this.renderAction(grid, "\u6DFB\u52A0\u6253\u5361\u9879\u76EE", "badge-plus", async () => {
      await this.store.addCustomHabit("\u65B0\u7684\u6253\u5361");
      this.onDataChanged();
      new import_obsidian81.Notice("\u5DF2\u6DFB\u52A0\u6253\u5361\u9879\u76EE\uFF0C\u53EF\u5728\u6A21\u5757\u7BA1\u7406\u4E2D\u7F16\u8F91\u3002");
    });
    this.renderAction(grid, "\u6DFB\u52A0\u529F\u80FD\u5206\u533A", "layout-grid", async () => {
      this.close();
      this.openAddSectionModal();
    }, false);
  }
  renderAction(container, label, icon, action, closeAfter = true) {
    const button = container.createEl("button", { cls: "cow-quick-create-card", attr: { type: "button" } });
    (0, import_obsidian81.setIcon)(button.createSpan(), icon);
    button.createSpan({ text: label });
    button.addEventListener("click", async () => {
      await action();
      if (closeAfter) this.close();
    });
  }
  openAddSectionModal() {
    const page = this.getCurrentPage();
    new AddSectionModal(this.app, page, this.store.getAvailableModules(page), async (moduleType) => {
      await this.store.addSection(page, moduleType);
      this.onDataChanged();
    }).open();
  }
};

// src/components/WorkbenchCustomizeModal.ts
var import_obsidian82 = require("obsidian");
var WorkbenchCustomizeModal = class extends import_obsidian82.Modal {
  constructor(app, store, getCurrentPage, onDataChanged) {
    var _a;
    super(app);
    this.store = store;
    this.getCurrentPage = getCurrentPage;
    this.onDataChanged = onDataChanged;
    const banner = this.store.getData().banner;
    this.titleValue = banner.message;
    this.subtitleValue = (_a = banner.subtitle) != null ? _a : "\u628A\u60F3\u6CD5\u53D8\u6210\u884C\u52A8\uFF0C\u8BA9\u6BCF\u4E00\u5929\u90FD\u66F4\u9760\u8FD1\u7406\u60F3\u7684\u81EA\u5DF1\u3002";
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    this.contentEl.createEl("h2", { text: "\u81EA\u5B9A\u4E49\u5DE5\u4F5C\u53F0" });
    this.contentEl.createEl("p", { text: "\u8C03\u6574 Banner \u548C\u5F53\u524D\u9875\u9762\u5165\u53E3\u3002" });
    const backgrounds = [
      { id: "pink-paper", label: "\u7C89\u8272\u7EB8\u5F20" },
      { id: "cream-stars", label: "\u5976\u6CB9\u661F\u661F" },
      { id: "soft-hearts", label: "\u67D4\u548C\u7231\u5FC3" }
    ];
    const choices = this.contentEl.createDiv({ cls: "cow-background-choice-grid" });
    backgrounds.forEach((background) => {
      const button = choices.createEl("button", {
        cls: this.store.getData().banner.background === background.id ? "is-active" : "",
        attr: { type: "button" }
      });
      (0, import_obsidian82.setIcon)(button.createSpan(), "image");
      button.createSpan({ text: background.label });
      button.addEventListener("click", async () => {
        await this.store.updateBanner({ background: background.id, imageDataUrl: void 0 });
        this.onDataChanged();
        this.close();
      });
    });
    const fileInput = this.contentEl.createEl("input", {
      cls: "cow-hidden-input",
      attr: { type: "file", accept: "image/*" }
    });
    fileInput.addEventListener("change", async () => {
      var _a;
      const file = (_a = fileInput.files) == null ? void 0 : _a[0];
      if (!file) return;
      const reader = new FileReader();
      reader.onload = async () => {
        await this.store.updateBanner({ background: "local-image", imageDataUrl: String(reader.result) });
        this.onDataChanged();
        this.close();
      };
      reader.readAsDataURL(file);
    });
    new import_obsidian82.Setting(this.contentEl).setName("\u672C\u5730\u56FE\u7247").setDesc("\u4FDD\u5B58\u4E3A data URL\uFF0CBRAT \u5B89\u88C5\u540E\u4E0D\u4F9D\u8D56\u989D\u5916\u8D44\u6E90\u8DEF\u5F84\u3002").addButton((button) => button.setButtonText("\u9009\u62E9\u56FE\u7247").onClick(() => fileInput.click()));
    new import_obsidian82.Setting(this.contentEl).setName("\u5DE6\u4FA7\u5934\u50CF").setDesc("\u9009\u62E9\u9884\u8BBE\u56FE\u6807\u6216\u4E0A\u4F20\u56FE\u7247\uFF0C\u5237\u65B0\u540E\u4ECD\u4FDD\u7559\u3002").addButton((button) => button.setButtonText("\u4FEE\u6539\u5DE6\u4FA7\u5934\u50CF").onClick(() => {
      const current = this.store.getData().banner.sidebarAvatar;
      new AvatarPickerModal(this.app, "\u4FEE\u6539\u5DE6\u4FA7\u5934\u50CF", current, async (avatar) => {
        await this.store.updateSidebarAvatar(avatar);
        this.onDataChanged();
      }).open();
    }));
    new import_obsidian82.Setting(this.contentEl).setName("Banner \u56FE\u6807").setDesc("\u9009\u62E9 Banner \u5DE6\u4FA7\u663E\u793A\u7684\u53EF\u7231\u56FE\u6807\u3002").addButton((button) => button.setButtonText("\u4FEE\u6539 Banner \u56FE\u6807").onClick(() => {
      const current = this.store.getData().banner.bannerAvatar;
      new AvatarPickerModal(this.app, "\u4FEE\u6539 Banner \u56FE\u6807", current, async (avatar) => {
        await this.store.updateBannerAvatar(avatar);
        this.onDataChanged();
      }).open();
    }));
    new import_obsidian82.Setting(this.contentEl).setName("Banner \u4E3B\u6807\u9898").addText((text) => text.setValue(this.titleValue).onChange((value) => {
      this.titleValue = value;
    }));
    new import_obsidian82.Setting(this.contentEl).setName("Banner \u526F\u6807\u9898").addText((text) => text.setValue(this.subtitleValue).onChange((value) => {
      this.subtitleValue = value;
    }));
    const actions = this.contentEl.createDiv({ cls: "cow-modal-actions" });
    const addSection = actions.createEl("button", { attr: { type: "button" } });
    (0, import_obsidian82.setIcon)(addSection.createSpan(), "plus");
    addSection.createSpan({ text: "\u6DFB\u52A0\u5F53\u524D\u9875\u9762\u529F\u80FD\u5206\u533A" });
    addSection.addEventListener("click", () => {
      this.close();
      this.openAddSectionModal();
    });
    const save = actions.createEl("button", { cls: "mod-cta", text: "\u4FDD\u5B58", attr: { type: "button" } });
    save.addEventListener("click", async () => {
      await this.store.updateBanner({
        message: this.titleValue.trim() || "\u4ECA\u5929\u4E5F\u8981\u53EF\u7231\u5730\u63A8\u8FDB\u4E00\u70B9\u70B9",
        subtitle: this.subtitleValue.trim() || "\u628A\u60F3\u6CD5\u53D8\u6210\u884C\u52A8\uFF0C\u8BA9\u6BCF\u4E00\u5929\u90FD\u66F4\u9760\u8FD1\u7406\u60F3\u7684\u81EA\u5DF1\u3002"
      });
      this.onDataChanged();
      new import_obsidian82.Notice("Banner \u6587\u6848\u5DF2\u4FDD\u5B58\u3002");
      this.close();
    });
  }
  openAddSectionModal() {
    const page = this.getCurrentPage();
    new AddSectionModal(this.app, page, this.store.getAvailableModules(page), async (moduleType) => {
      await this.store.addSection(page, moduleType);
      this.onDataChanged();
    }).open();
  }
};

// src/components/DayDetailModal.ts
var import_obsidian83 = require("obsidian");
var DayDetailModal = class extends import_obsidian83.Modal {
  constructor(app, store, date) {
    super(app);
    this.store = store;
    this.date = date;
    this.calendar = new CalendarService();
    this.holidays = new HolidayService();
    this.notes = new NoteService(app);
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal");
    const dateKey = this.calendar.getDateKey(this.date);
    const holidays = this.holidays.getHolidays(this.date);
    const dailyNote = this.notes.getDailyNote(this.date);
    const isToday = this.calendar.isSameDate(this.date, /* @__PURE__ */ new Date());
    this.contentEl.createEl("h2", { text: dateKey });
    this.contentEl.createEl("p", {
      text: `${this.calendar.getWeekdayLabel(this.date)}${holidays.length > 0 ? ` \xB7 ${holidays.map((holiday) => holiday.name).join(" / ")}` : " \xB7 \u6682\u65E0\u8282\u65E5"}`
    });
    this.contentEl.createEl("h3", { text: "\u5F53\u5929\u5F85\u529E" });
    const taskList = this.contentEl.createEl("ul", { cls: "cow-day-detail-list" });
    const tasks = isToday ? this.store.getTodayFocusTasks() : [];
    if (tasks.length === 0) {
      taskList.createEl("li", { text: "\u6682\u65E0\u5F85\u529E\u3002" });
    } else {
      tasks.forEach((task) => {
        taskList.createEl("li", { text: `${task.completed ? "\u2713" : "\u25CB"} ${task.category} \xB7 ${task.label}` });
      });
    }
    this.contentEl.createEl("h3", { text: "\u5F53\u5929\u7B14\u8BB0" });
    const noteRow = this.contentEl.createDiv({ cls: "cow-day-note-row" });
    noteRow.createSpan({ text: dailyNote ? dailyNote.path : "\u8FD8\u6CA1\u6709\u6BCF\u65E5\u7B14\u8BB0\u3002" });
    const button = noteRow.createEl("button", { attr: { type: "button" } });
    (0, import_obsidian83.setIcon)(button.createSpan(), dailyNote ? "file-text" : "file-plus");
    button.createSpan({ text: dailyNote ? "\u6253\u5F00\u6BCF\u65E5\u7B14\u8BB0" : "\u521B\u5EFA\u6BCF\u65E5\u7B14\u8BB0" });
    button.addEventListener("click", async () => {
      await this.notes.openOrCreateDailyNote(this.date);
      this.close();
    });
  }
};

// src/components/NotesManagerModal.ts
var import_obsidian84 = require("obsidian");
var NotesManagerModal = class extends import_obsidian84.Modal {
  constructor(app) {
    super(app);
    this.searchValue = "";
    this.notes = new NoteService(app);
  }
  onOpen() {
    this.contentEl.empty();
    this.contentEl.addClass("cow-modal", "cow-notes-manager-modal");
    const header = this.contentEl.createDiv({ cls: "cow-notes-manager-header" });
    header.createEl("h2", { text: "\u7B14\u8BB0\u7BA1\u7406" });
    header.createEl("p", { text: "\u8F7B\u91CF\u67E5\u770B\u6700\u8FD1 Markdown \u7B14\u8BB0\uFF0C\u70B9\u51FB\u5373\u53EF\u6253\u5F00\u3002" });
    const searchWrap = this.contentEl.createDiv({ cls: "cow-notes-manager-search" });
    const search = searchWrap.createEl("input", {
      cls: "cow-notes-search",
      attr: { type: "search", placeholder: "\u641C\u7D22\u6587\u4EF6\u540D\u6216\u8DEF\u5F84" }
    });
    search.addEventListener("input", () => {
      this.searchValue = search.value.trim().toLowerCase();
      this.renderList();
    });
    this.listEl = this.contentEl.createDiv({ cls: "cow-notes-manager-list" });
    this.renderList();
  }
  renderList() {
    if (!this.listEl) return;
    this.listEl.empty();
    const files = this.notes.getRecentMarkdownFiles(80).filter((file) => this.matchesSearch(file)).slice(0, 20);
    if (files.length === 0) {
      this.listEl.createEl("p", { text: "\u6CA1\u6709\u627E\u5230\u5339\u914D\u7B14\u8BB0\u3002" });
      return;
    }
    files.forEach((file) => {
      const button = this.listEl.createEl("button", { cls: "cow-note-manager-item", attr: { type: "button" } });
      const body = button.createDiv({ cls: "cow-note-manager-body" });
      body.createEl("strong", { text: file.basename });
      body.createEl("span", { text: file.path });
      button.createEl("time", { text: new Date(file.stat.mtime).toLocaleString("zh-CN") });
      button.addEventListener("click", async () => {
        await this.app.workspace.getLeaf(false).openFile(file);
        this.close();
      });
    });
  }
  matchesSearch(file) {
    if (!this.searchValue) return true;
    return file.basename.toLowerCase().includes(this.searchValue) || file.path.toLowerCase().includes(this.searchValue);
  }
};

// src/views/WorkbenchView.ts
var WORKBENCH_VIEW_TYPE = "cute-obsidian-workbench-view";
var WorkbenchView = class extends import_obsidian85.ItemView {
  constructor(leaf, plugin) {
    super(leaf);
    this.plugin = plugin;
    this.eventBus = new EventBus();
    this.lastDateKey = this.getDateKey(/* @__PURE__ */ new Date());
    this.router = new DashboardRouter(this.eventBus, this.plugin.store.getData().currentPage);
  }
  getViewType() {
    return WORKBENCH_VIEW_TYPE;
  }
  getDisplayText() {
    return "Cute Workbench";
  }
  getIcon() {
    return "leaf";
  }
  async onOpen() {
    this.eventBus.on(ROUTE_CHANGED_EVENT, async (page) => {
      await this.plugin.store.setCurrentPage(page);
      this.render();
    });
    this.dateWatcher = window.setInterval(() => this.checkDateChange(), 6e4);
    this.render();
  }
  async onClose() {
    var _a;
    if (this.dateWatcher) {
      window.clearInterval(this.dateWatcher);
      this.dateWatcher = void 0;
    }
    (_a = this.sidebar) == null ? void 0 : _a.destroy();
    this.eventBus.clear();
  }
  render() {
    var _a;
    const container = this.containerEl.children[1];
    container.empty();
    container.addClass("cute-obsidian-workbench");
    this.applyTheme(container);
    (_a = this.sidebar) == null ? void 0 : _a.destroy();
    const shell = container.createDiv({ cls: "cow-shell" });
    this.sidebar = new Sidebar(this.app, this.router.getCurrentPage(), [
      { label: "\u5DE5\u4F5C\u53F0", icon: "home", onClick: () => this.router.navigate("overview") },
      { label: "\u6BCF\u65E5\u7B14\u8BB0", icon: "calendar-days", onClick: () => void new NoteService(this.app).openOrCreateDailyNote(/* @__PURE__ */ new Date()) },
      { label: "\u7B14\u8BB0\u7BA1\u7406", icon: "notebook-tabs", onClick: () => new NotesManagerModal(this.app).open() },
      { label: "\u5168\u90E8\u6587\u4EF6", icon: "files", onClick: () => this.openFileExplorer() },
      { label: "\u6A21\u5757\u7BA1\u7406", icon: "layout-grid", onClick: () => this.router.navigate("modules") }
    ], () => {
      new QuickCreateModal(this.app, this.plugin.store, () => this.router.getCurrentPage(), () => this.render()).open();
    }, (date) => {
      new DayDetailModal(this.app, this.plugin.store, date).open();
    }, this.plugin.store.getData.bind(this.plugin.store), () => this.openAvatarPicker("sidebar"));
    this.sidebar.render(shell);
    const main = shell.createDiv({ cls: "cow-main" });
    new TopBanner(this.plugin.store.getData.bind(this.plugin.store), () => {
      new WorkbenchCustomizeModal(this.app, this.plugin.store, () => this.router.getCurrentPage(), () => this.render()).open();
    }, () => this.openAvatarPicker("banner")).render(main);
    new TopNavigation(this.plugin.store.getPages(), () => this.router.getCurrentPage(), (page) => {
      this.router.navigate(page);
    }).render(main);
    const pageHost = main.createDiv({ cls: "cow-page-host" });
    this.pageHost = pageHost;
    this.renderPage(pageHost, this.router.getCurrentPage());
  }
  renderPage(container, page) {
    const refresh = () => this.renderPreservingScroll();
    const pageMap = {
      overview: new OverviewPage(this.app, this.plugin.store, "overview", refresh),
      research: new ResearchPage(this.app, this.plugin.store, "research", refresh),
      reading: new ReadingPage(this.app, this.plugin.store, "reading", refresh),
      fitness: new FitnessPage(this.app, this.plugin.store, "fitness", refresh),
      finance: new FinancePage(this.app, this.plugin.store, "finance", refresh),
      tasks: new TasksPage(this.app, this.plugin.store, "tasks", refresh),
      goals: new GoalsPage(this.app, this.plugin.store, "goals", refresh),
      modules: new ModulesPage(this.app, this.plugin.store, "modules", refresh)
    };
    pageMap[page].render(container);
  }
  renderPreservingScroll() {
    var _a, _b;
    const scrollTop = (_b = (_a = this.pageHost) == null ? void 0 : _a.scrollTop) != null ? _b : 0;
    this.render();
    window.requestAnimationFrame(() => {
      if (this.pageHost) this.pageHost.scrollTop = scrollTop;
    });
  }
  applyTheme(container) {
    const theme = this.plugin.store.getData().theme;
    container.style.setProperty("--cute-bg", theme.cuteBg);
    container.style.setProperty("--cute-card", theme.cuteCard);
    container.style.setProperty("--cute-primary", theme.cutePrimary);
    container.style.setProperty("--cute-secondary", theme.cuteSecondary);
    container.style.setProperty("--cute-text", theme.cuteText);
    container.style.setProperty("--cute-border", theme.cuteBorder);
    container.style.setProperty("--cute-radius", `${theme.cuteRadius}px`);
    container.style.setProperty("--cute-shadow", theme.cuteShadow);
    container.style.setProperty("--cute-card-opacity", String(theme.cardOpacity));
    container.style.setProperty("--cute-texture-strength", String(theme.textureStrength));
    container.style.setProperty("--cute-font-size", `${theme.fontSize}px`);
  }
  openFileExplorer() {
    var _a;
    const didRun = (_a = this.app.commands) == null ? void 0 : _a.executeCommandById("file-explorer:open");
    if (!didRun) {
      new import_obsidian85.Notice("\u672A\u80FD\u6FC0\u6D3B Obsidian \u6587\u4EF6\u7BA1\u7406\u5668\u3002");
    }
  }
  openAvatarPicker(target) {
    const banner = this.plugin.store.getData().banner;
    new AvatarPickerModal(
      this.app,
      target === "sidebar" ? "\u4FEE\u6539\u5DE6\u4FA7\u5934\u50CF" : "\u4FEE\u6539 Banner \u56FE\u6807",
      target === "sidebar" ? banner.sidebarAvatar : banner.bannerAvatar,
      async (avatar) => {
        if (target === "sidebar") {
          await this.plugin.store.updateSidebarAvatar(avatar);
        } else {
          await this.plugin.store.updateBannerAvatar(avatar);
        }
        this.render();
      }
    ).open();
  }
  checkDateChange() {
    var _a;
    const current = this.getDateKey(/* @__PURE__ */ new Date());
    if (current === this.lastDateKey) return;
    this.lastDateKey = current;
    (_a = this.pageHost) == null ? void 0 : _a.querySelectorAll(".cow-checkin-section").forEach((section) => {
      section.dispatchEvent(new CustomEvent("cow-checkin-date-change"));
    });
  }
  getDateKey(date) {
    const year = date.getFullYear();
    const month = String(date.getMonth() + 1).padStart(2, "0");
    const day = String(date.getDate()).padStart(2, "0");
    return `${year}-${month}-${day}`;
  }
};

// src/main.ts
var CuteObsidianWorkbenchPlugin = class extends import_obsidian86.Plugin {
  async onload() {
    this.store = new DashboardStore(
      () => this.loadData(),
      (data) => this.saveData(data)
    );
    await this.store.load();
    const healthReminderService = new HealthReminderService(this.app, this.store);
    await healthReminderService.checkMissedReminders();
    this.registerInterval(window.setInterval(() => void healthReminderService.tick(), 30 * 1e3));
    this.registerView(
      WORKBENCH_VIEW_TYPE,
      (leaf) => new WorkbenchView(leaf, this)
    );
    this.addRibbonIcon("leaf", "\u6253\u5F00 Cute Workbench", async () => {
      await this.activateWorkbenchView();
    });
    this.addCommand({
      id: "open-cute-workbench",
      name: "Open Cute Workbench",
      callback: async () => {
        await this.activateWorkbenchView();
      }
    });
    this.addSettingTab(new WorkbenchSettingTab(this.app, this));
  }
  onunload() {
    this.app.workspace.detachLeavesOfType(WORKBENCH_VIEW_TYPE);
  }
  async activateWorkbenchView() {
    const existingLeaves = this.app.workspace.getLeavesOfType(WORKBENCH_VIEW_TYPE);
    let leaf = existingLeaves[0];
    if (!leaf) {
      leaf = this.app.workspace.getLeaf("tab");
      await leaf.setViewState({
        type: WORKBENCH_VIEW_TYPE,
        active: true
      });
    }
    this.app.workspace.revealLeaf(leaf);
  }
};
