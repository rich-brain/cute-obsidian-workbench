import type { WorkbenchSettings } from "../types/dashboard";

export const DEFAULT_WORKBENCH_SETTINGS: WorkbenchSettings = {
  showLeftSidebar: true,
  weekStartsOn: "monday",
  dateFormat: "YYYY-MM-DD",
  overviewLayout: "default",
  zoteroJsonPath: ""
};
